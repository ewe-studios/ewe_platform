//! Auto-generated API clients for gcp/walletobjects.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET walletobjects/v1/eventTicketClass/{resourceId}/addMessage
/// Adds a message to the event ticket class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_addmessage_execute()` to send, or `walletobjects_eventticketclass_addmessage` for simplest API.

pub fn walletobjects_eventticketclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}/addMessage
/// Adds a message to the event ticket class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_addmessage_execute()` or `walletobjects_eventticketclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<EventTicketClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}/addMessage
/// Adds a message to the event ticket class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}/addMessage
/// Adds a message to the event ticket class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_addmessage_builder()` + `walletobjects_eventticketclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Returns the event ticket class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_get_execute()` to send, or `walletobjects_eventticketclass_get` for simplest API.

pub fn walletobjects_eventticketclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Returns the event ticket class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_get_execute()` or `walletobjects_eventticketclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Returns the event ticket class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_get_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Returns the event ticket class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_get_builder()` + `walletobjects_eventticketclass_get_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketclass_get_builder(client, &args.resourceId)?;
    walletobjects_eventticketclass_get_execute(builder)
}

/// GET walletobjects/v1/eventTicketClass
/// Inserts an event ticket class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_insert_execute()` to send, or `walletobjects_eventticketclass_insert` for simplest API.

pub fn walletobjects_eventticketclass_insert_builder(
    client: &SimpleHttpClient,
    body: &EventTicketClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketClass
/// Inserts an event ticket class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_insert_execute()` or `walletobjects_eventticketclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass
/// Inserts an event ticket class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_insert_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassInsertArgs {
    /// Request body.
    pub body: EventTicketClass,
}

/// GET walletobjects/v1/eventTicketClass
/// Inserts an event ticket class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_insert_builder()` + `walletobjects_eventticketclass_insert_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketclass_insert_builder(client, &args.body)?;
    walletobjects_eventticketclass_insert_execute(builder)
}

/// GET walletobjects/v1/eventTicketClass
/// Returns a list of all event ticket classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_list_execute()` to send, or `walletobjects_eventticketclass_list` for simplest API.

pub fn walletobjects_eventticketclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/eventTicketClass
/// Returns a list of all event ticket classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_list_execute()` or `walletobjects_eventticketclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<EventTicketClassListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass
/// Returns a list of all event ticket classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_list_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketClassListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/eventTicketClass
/// Returns a list of all event ticket classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_list_builder()` + `walletobjects_eventticketclass_list_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketClassListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_eventticketclass_list_execute(builder)
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_patch_execute()` to send, or `walletobjects_eventticketclass_patch` for simplest API.

pub fn walletobjects_eventticketclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &EventTicketClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_patch_execute()` or `walletobjects_eventticketclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_patch_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: EventTicketClass,
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_patch_builder()` + `walletobjects_eventticketclass_patch_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketclass_patch_execute(builder)
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketclass_update_execute()` to send, or `walletobjects_eventticketclass_update` for simplest API.

pub fn walletobjects_eventticketclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &EventTicketClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketclass_update_execute()` or `walletobjects_eventticketclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketclass_update_task()`.
/// For the simplest API, use `walletobjects_eventticketclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: EventTicketClass,
}

/// GET walletobjects/v1/eventTicketClass/{resourceId}
/// Updates the event ticket class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketclass_update_builder()` + `walletobjects_eventticketclass_update_execute()`.
/// For task-level control, use `walletobjects_eventticketclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketclass_update_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/addMessage
/// Adds a message to the event ticket object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_addmessage_execute()` to send, or `walletobjects_eventticketobject_addmessage` for simplest API.

pub fn walletobjects_eventticketobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/addMessage
/// Adds a message to the event ticket object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_addmessage_execute()` or `walletobjects_eventticketobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<EventTicketObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/addMessage
/// Adds a message to the event ticket object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/addMessage
/// Adds a message to the event ticket object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_addmessage_builder()` + `walletobjects_eventticketobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Returns the event ticket object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_get_execute()` to send, or `walletobjects_eventticketobject_get` for simplest API.

pub fn walletobjects_eventticketobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Returns the event ticket object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_get_execute()` or `walletobjects_eventticketobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Returns the event ticket object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_get_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Returns the event ticket object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_get_builder()` + `walletobjects_eventticketobject_get_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketobject_get_builder(client, &args.resourceId)?;
    walletobjects_eventticketobject_get_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject
/// Inserts an event ticket object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_insert_execute()` to send, or `walletobjects_eventticketobject_insert` for simplest API.

pub fn walletobjects_eventticketobject_insert_builder(
    client: &SimpleHttpClient,
    body: &EventTicketObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketObject
/// Inserts an event ticket object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_insert_execute()` or `walletobjects_eventticketobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject
/// Inserts an event ticket object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_insert_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectInsertArgs {
    /// Request body.
    pub body: EventTicketObject,
}

/// GET walletobjects/v1/eventTicketObject
/// Inserts an event ticket object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_insert_builder()` + `walletobjects_eventticketobject_insert_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketobject_insert_builder(client, &args.body)?;
    walletobjects_eventticketobject_insert_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject
/// Returns a list of all event ticket objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_list_execute()` to send, or `walletobjects_eventticketobject_list` for simplest API.

pub fn walletobjects_eventticketobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/eventTicketObject
/// Returns a list of all event ticket objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_list_execute()` or `walletobjects_eventticketobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<EventTicketObjectListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject
/// Returns a list of all event ticket objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_list_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketObjectListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/eventTicketObject
/// Returns a list of all event ticket objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_list_builder()` + `walletobjects_eventticketobject_list_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventTicketObjectListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_eventticketobject_list_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the event ticket object with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_modifylinkedofferobjects_execute()` to send, or `walletobjects_eventticketobject_modifylinkedofferobjects` for simplest API.

pub fn walletobjects_eventticketobject_modifylinkedofferobjects_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &ModifyLinkedOfferObjectsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}/modifyLinkedOfferObjects",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the event ticket object with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_modifylinkedofferobjects_execute()` or `walletobjects_eventticketobject_modifylinkedofferobjects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_modifylinkedofferobjects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_modifylinkedofferobjects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the event ticket object with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_modifylinkedofferobjects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_modifylinkedofferobjects_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_modifylinkedofferobjects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_modifylinkedofferobjects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_modifylinkedofferobjects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_modifylinkedofferobjects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_modifylinkedofferobjects`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectModifylinkedofferobjectsArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: ModifyLinkedOfferObjectsRequest,
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the event ticket object with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_modifylinkedofferobjects_builder()` + `walletobjects_eventticketobject_modifylinkedofferobjects_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_modifylinkedofferobjects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_modifylinkedofferobjects(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectModifylinkedofferobjectsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_eventticketobject_modifylinkedofferobjects_builder(
        client,
        &args.resourceId,
        &args.body,
    )?;
    walletobjects_eventticketobject_modifylinkedofferobjects_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_patch_execute()` to send, or `walletobjects_eventticketobject_patch` for simplest API.

pub fn walletobjects_eventticketobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &EventTicketObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_patch_execute()` or `walletobjects_eventticketobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_patch_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: EventTicketObject,
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_patch_builder()` + `walletobjects_eventticketobject_patch_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketobject_patch_execute(builder)
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_eventticketobject_update_execute()` to send, or `walletobjects_eventticketobject_update` for simplest API.

pub fn walletobjects_eventticketobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &EventTicketObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_eventticketobject_update_execute()` or `walletobjects_eventticketobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTicketObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_eventticketobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_eventticketobject_update_task()`.
/// For the simplest API, use `walletobjects_eventticketobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_eventticketobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_eventticketobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_eventticketobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_eventticketobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: EventTicketObject,
}

/// GET walletobjects/v1/eventTicketObject/{resourceId}
/// Updates the event ticket object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_eventticketobject_update_builder()` + `walletobjects_eventticketobject_update_execute()`.
/// For task-level control, use `walletobjects_eventticketobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_eventticketobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsEventticketobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTicketObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_eventticketobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_eventticketobject_update_execute(builder)
}

/// GET walletobjects/v1/flightClass/{resourceId}/addMessage
/// Adds a message to the flight class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_addmessage_execute()` to send, or `walletobjects_flightclass_addmessage` for simplest API.

pub fn walletobjects_flightclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightClass/{resourceId}/addMessage
/// Adds a message to the flight class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_addmessage_execute()` or `walletobjects_flightclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<FlightClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass/{resourceId}/addMessage
/// Adds a message to the flight class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_flightclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FlightClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/flightClass/{resourceId}/addMessage
/// Adds a message to the flight class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_addmessage_builder()` + `walletobjects_flightclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_flightclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FlightClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_flightclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Returns the flight class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_get_execute()` to send, or `walletobjects_flightclass_get` for simplest API.

pub fn walletobjects_flightclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Returns the flight class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_get_execute()` or `walletobjects_flightclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Returns the flight class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_get_task()`.
/// For the simplest API, use `walletobjects_flightclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Returns the flight class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_get_builder()` + `walletobjects_flightclass_get_execute()`.
/// For task-level control, use `walletobjects_flightclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_flightclass_get_builder(client, &args.resourceId)?;
    walletobjects_flightclass_get_execute(builder)
}

/// GET walletobjects/v1/flightClass
/// Inserts an flight class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_insert_execute()` to send, or `walletobjects_flightclass_insert` for simplest API.

pub fn walletobjects_flightclass_insert_builder(
    client: &SimpleHttpClient,
    body: &FlightClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/flightClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightClass
/// Inserts an flight class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_insert_execute()` or `walletobjects_flightclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass
/// Inserts an flight class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_insert_task()`.
/// For the simplest API, use `walletobjects_flightclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassInsertArgs {
    /// Request body.
    pub body: FlightClass,
}

/// GET walletobjects/v1/flightClass
/// Inserts an flight class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_insert_builder()` + `walletobjects_flightclass_insert_execute()`.
/// For task-level control, use `walletobjects_flightclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_flightclass_insert_builder(client, &args.body)?;
    walletobjects_flightclass_insert_execute(builder)
}

/// GET walletobjects/v1/flightClass
/// Returns a list of all flight classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_list_execute()` to send, or `walletobjects_flightclass_list` for simplest API.

pub fn walletobjects_flightclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/flightClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/flightClass
/// Returns a list of all flight classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_list_execute()` or `walletobjects_flightclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass
/// Returns a list of all flight classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_list_task()`.
/// For the simplest API, use `walletobjects_flightclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/flightClass
/// Returns a list of all flight classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_list_builder()` + `walletobjects_flightclass_list_execute()`.
/// For task-level control, use `walletobjects_flightclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_flightclass_list_execute(builder)
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_patch_execute()` to send, or `walletobjects_flightclass_patch` for simplest API.

pub fn walletobjects_flightclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &FlightClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_patch_execute()` or `walletobjects_flightclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_patch_task()`.
/// For the simplest API, use `walletobjects_flightclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: FlightClass,
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_patch_builder()` + `walletobjects_flightclass_patch_execute()`.
/// For task-level control, use `walletobjects_flightclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_flightclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightclass_patch_execute(builder)
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightclass_update_execute()` to send, or `walletobjects_flightclass_update` for simplest API.

pub fn walletobjects_flightclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &FlightClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightclass_update_execute()` or `walletobjects_flightclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightclass_update_task()`.
/// For the simplest API, use `walletobjects_flightclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_flightclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: FlightClass,
}

/// GET walletobjects/v1/flightClass/{resourceId}
/// Updates the flight class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightclass_update_builder()` + `walletobjects_flightclass_update_execute()`.
/// For task-level control, use `walletobjects_flightclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_flightclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightclass_update_execute(builder)
}

/// GET walletobjects/v1/flightObject/{resourceId}/addMessage
/// Adds a message to the flight object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_addmessage_execute()` to send, or `walletobjects_flightobject_addmessage` for simplest API.

pub fn walletobjects_flightobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightObject/{resourceId}/addMessage
/// Adds a message to the flight object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_addmessage_execute()` or `walletobjects_flightobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<FlightObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject/{resourceId}/addMessage
/// Adds a message to the flight object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_flightobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FlightObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/flightObject/{resourceId}/addMessage
/// Adds a message to the flight object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_addmessage_builder()` + `walletobjects_flightobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_flightobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FlightObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_flightobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Returns the flight object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_get_execute()` to send, or `walletobjects_flightobject_get` for simplest API.

pub fn walletobjects_flightobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Returns the flight object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_get_execute()` or `walletobjects_flightobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Returns the flight object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_get_task()`.
/// For the simplest API, use `walletobjects_flightobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Returns the flight object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_get_builder()` + `walletobjects_flightobject_get_execute()`.
/// For task-level control, use `walletobjects_flightobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightobject_get_builder(client, &args.resourceId)?;
    walletobjects_flightobject_get_execute(builder)
}

/// GET walletobjects/v1/flightObject
/// Inserts an flight object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_insert_execute()` to send, or `walletobjects_flightobject_insert` for simplest API.

pub fn walletobjects_flightobject_insert_builder(
    client: &SimpleHttpClient,
    body: &FlightObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/flightObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightObject
/// Inserts an flight object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_insert_execute()` or `walletobjects_flightobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject
/// Inserts an flight object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_insert_task()`.
/// For the simplest API, use `walletobjects_flightobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectInsertArgs {
    /// Request body.
    pub body: FlightObject,
}

/// GET walletobjects/v1/flightObject
/// Inserts an flight object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_insert_builder()` + `walletobjects_flightobject_insert_execute()`.
/// For task-level control, use `walletobjects_flightobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightobject_insert_builder(client, &args.body)?;
    walletobjects_flightobject_insert_execute(builder)
}

/// GET walletobjects/v1/flightObject
/// Returns a list of all flight objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_list_execute()` to send, or `walletobjects_flightobject_list` for simplest API.

pub fn walletobjects_flightobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/flightObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/flightObject
/// Returns a list of all flight objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_list_execute()` or `walletobjects_flightobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject
/// Returns a list of all flight objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_list_task()`.
/// For the simplest API, use `walletobjects_flightobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/flightObject
/// Returns a list of all flight objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_list_builder()` + `walletobjects_flightobject_list_execute()`.
/// For task-level control, use `walletobjects_flightobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_flightobject_list_execute(builder)
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_patch_execute()` to send, or `walletobjects_flightobject_patch` for simplest API.

pub fn walletobjects_flightobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &FlightObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_patch_execute()` or `walletobjects_flightobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_patch_task()`.
/// For the simplest API, use `walletobjects_flightobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: FlightObject,
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_patch_builder()` + `walletobjects_flightobject_patch_execute()`.
/// For task-level control, use `walletobjects_flightobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightobject_patch_execute(builder)
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_flightobject_update_execute()` to send, or `walletobjects_flightobject_update` for simplest API.

pub fn walletobjects_flightobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &FlightObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_flightobject_update_execute()` or `walletobjects_flightobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlightObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_flightobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_flightobject_update_task()`.
/// For the simplest API, use `walletobjects_flightobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_flightobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_flightobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_flightobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_flightobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: FlightObject,
}

/// GET walletobjects/v1/flightObject/{resourceId}
/// Updates the flight object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_flightobject_update_builder()` + `walletobjects_flightobject_update_execute()`.
/// For task-level control, use `walletobjects_flightobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_flightobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsFlightobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlightObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_flightobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_flightobject_update_execute(builder)
}

/// GET walletobjects/v1/genericClass/{resourceId}/addMessage
/// Adds a message to the generic class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_addmessage_execute()` to send, or `walletobjects_genericclass_addmessage` for simplest API.

pub fn walletobjects_genericclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericClass/{resourceId}/addMessage
/// Adds a message to the generic class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_addmessage_execute()` or `walletobjects_genericclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GenericClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass/{resourceId}/addMessage
/// Adds a message to the generic class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_genericclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenericClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/genericClass/{resourceId}/addMessage
/// Adds a message to the generic class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_addmessage_builder()` + `walletobjects_genericclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_genericclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenericClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_genericclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Returns the generic class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_get_execute()` to send, or `walletobjects_genericclass_get` for simplest API.

pub fn walletobjects_genericclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Returns the generic class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_get_execute()` or `walletobjects_genericclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Returns the generic class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_get_task()`.
/// For the simplest API, use `walletobjects_genericclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Returns the generic class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_get_builder()` + `walletobjects_genericclass_get_execute()`.
/// For task-level control, use `walletobjects_genericclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericclass_get_builder(client, &args.resourceId)?;
    walletobjects_genericclass_get_execute(builder)
}

/// GET walletobjects/v1/genericClass
/// Inserts a generic class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_insert_execute()` to send, or `walletobjects_genericclass_insert` for simplest API.

pub fn walletobjects_genericclass_insert_builder(
    client: &SimpleHttpClient,
    body: &GenericClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/genericClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericClass
/// Inserts a generic class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_insert_execute()` or `walletobjects_genericclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass
/// Inserts a generic class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_insert_task()`.
/// For the simplest API, use `walletobjects_genericclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassInsertArgs {
    /// Request body.
    pub body: GenericClass,
}

/// GET walletobjects/v1/genericClass
/// Inserts a generic class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_insert_builder()` + `walletobjects_genericclass_insert_execute()`.
/// For task-level control, use `walletobjects_genericclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericclass_insert_builder(client, &args.body)?;
    walletobjects_genericclass_insert_execute(builder)
}

/// GET walletobjects/v1/genericClass
/// Returns a list of all generic classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_list_execute()` to send, or `walletobjects_genericclass_list` for simplest API.

pub fn walletobjects_genericclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/genericClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/genericClass
/// Returns a list of all generic classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_list_execute()` or `walletobjects_genericclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass
/// Returns a list of all generic classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_list_task()`.
/// For the simplest API, use `walletobjects_genericclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/genericClass
/// Returns a list of all generic classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_list_builder()` + `walletobjects_genericclass_list_execute()`.
/// For task-level control, use `walletobjects_genericclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_genericclass_list_execute(builder)
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the generic class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_patch_execute()` to send, or `walletobjects_genericclass_patch` for simplest API.

pub fn walletobjects_genericclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GenericClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the generic class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_patch_execute()` or `walletobjects_genericclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the generic class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_patch_task()`.
/// For the simplest API, use `walletobjects_genericclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GenericClass,
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the generic class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_patch_builder()` + `walletobjects_genericclass_patch_execute()`.
/// For task-level control, use `walletobjects_genericclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericclass_patch_execute(builder)
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the Generic class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericclass_update_execute()` to send, or `walletobjects_genericclass_update` for simplest API.

pub fn walletobjects_genericclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GenericClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the Generic class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericclass_update_execute()` or `walletobjects_genericclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the Generic class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericclass_update_task()`.
/// For the simplest API, use `walletobjects_genericclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GenericClass,
}

/// GET walletobjects/v1/genericClass/{resourceId}
/// Updates the Generic class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericclass_update_builder()` + `walletobjects_genericclass_update_execute()`.
/// For task-level control, use `walletobjects_genericclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericclass_update_execute(builder)
}

/// GET walletobjects/v1/genericObject/{resourceId}/addMessage
/// Adds a message to the generic object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_addmessage_execute()` to send, or `walletobjects_genericobject_addmessage` for simplest API.

pub fn walletobjects_genericobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericObject/{resourceId}/addMessage
/// Adds a message to the generic object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_addmessage_execute()` or `walletobjects_genericobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GenericObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject/{resourceId}/addMessage
/// Adds a message to the generic object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_genericobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenericObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/genericObject/{resourceId}/addMessage
/// Adds a message to the generic object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_addmessage_builder()` + `walletobjects_genericobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_genericobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenericObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_genericobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Returns the generic object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_get_execute()` to send, or `walletobjects_genericobject_get` for simplest API.

pub fn walletobjects_genericobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Returns the generic object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_get_execute()` or `walletobjects_genericobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Returns the generic object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_get_task()`.
/// For the simplest API, use `walletobjects_genericobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Returns the generic object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_get_builder()` + `walletobjects_genericobject_get_execute()`.
/// For task-level control, use `walletobjects_genericobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericobject_get_builder(client, &args.resourceId)?;
    walletobjects_genericobject_get_execute(builder)
}

/// GET walletobjects/v1/genericObject
/// Inserts a generic object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_insert_execute()` to send, or `walletobjects_genericobject_insert` for simplest API.

pub fn walletobjects_genericobject_insert_builder(
    client: &SimpleHttpClient,
    body: &GenericObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/genericObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericObject
/// Inserts a generic object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_insert_execute()` or `walletobjects_genericobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject
/// Inserts a generic object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_insert_task()`.
/// For the simplest API, use `walletobjects_genericobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectInsertArgs {
    /// Request body.
    pub body: GenericObject,
}

/// GET walletobjects/v1/genericObject
/// Inserts a generic object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_insert_builder()` + `walletobjects_genericobject_insert_execute()`.
/// For task-level control, use `walletobjects_genericobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericobject_insert_builder(client, &args.body)?;
    walletobjects_genericobject_insert_execute(builder)
}

/// GET walletobjects/v1/genericObject
/// Returns a list of all generic objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_list_execute()` to send, or `walletobjects_genericobject_list` for simplest API.

pub fn walletobjects_genericobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/genericObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/genericObject
/// Returns a list of all generic objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_list_execute()` or `walletobjects_genericobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject
/// Returns a list of all generic objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_list_task()`.
/// For the simplest API, use `walletobjects_genericobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/genericObject
/// Returns a list of all generic objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_list_builder()` + `walletobjects_genericobject_list_execute()`.
/// For task-level control, use `walletobjects_genericobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_genericobject_list_execute(builder)
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_patch_execute()` to send, or `walletobjects_genericobject_patch` for simplest API.

pub fn walletobjects_genericobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GenericObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_patch_execute()` or `walletobjects_genericobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_patch_task()`.
/// For the simplest API, use `walletobjects_genericobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GenericObject,
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_patch_builder()` + `walletobjects_genericobject_patch_execute()`.
/// For task-level control, use `walletobjects_genericobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericobject_patch_execute(builder)
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_genericobject_update_execute()` to send, or `walletobjects_genericobject_update` for simplest API.

pub fn walletobjects_genericobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GenericObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_genericobject_update_execute()` or `walletobjects_genericobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenericObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_genericobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_genericobject_update_task()`.
/// For the simplest API, use `walletobjects_genericobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_genericobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_genericobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_genericobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_genericobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GenericObject,
}

/// GET walletobjects/v1/genericObject/{resourceId}
/// Updates the generic object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_genericobject_update_builder()` + `walletobjects_genericobject_update_execute()`.
/// For task-level control, use `walletobjects_genericobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_genericobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsGenericobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenericObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_genericobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_genericobject_update_execute(builder)
}

/// GET walletobjects/v1/giftCardClass/{resourceId}/addMessage
/// Adds a message to the gift card class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_addmessage_execute()` to send, or `walletobjects_giftcardclass_addmessage` for simplest API.

pub fn walletobjects_giftcardclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}/addMessage
/// Adds a message to the gift card class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_addmessage_execute()` or `walletobjects_giftcardclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GiftCardClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}/addMessage
/// Adds a message to the gift card class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/giftCardClass/{resourceId}/addMessage
/// Adds a message to the gift card class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_addmessage_builder()` + `walletobjects_giftcardclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_giftcardclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Returns the gift card class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_get_execute()` to send, or `walletobjects_giftcardclass_get` for simplest API.

pub fn walletobjects_giftcardclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Returns the gift card class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_get_execute()` or `walletobjects_giftcardclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Returns the gift card class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_get_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Returns the gift card class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_get_builder()` + `walletobjects_giftcardclass_get_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardclass_get_builder(client, &args.resourceId)?;
    walletobjects_giftcardclass_get_execute(builder)
}

/// GET walletobjects/v1/giftCardClass
/// Inserts an gift card class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_insert_execute()` to send, or `walletobjects_giftcardclass_insert` for simplest API.

pub fn walletobjects_giftcardclass_insert_builder(
    client: &SimpleHttpClient,
    body: &GiftCardClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardClass
/// Inserts an gift card class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_insert_execute()` or `walletobjects_giftcardclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass
/// Inserts an gift card class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_insert_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassInsertArgs {
    /// Request body.
    pub body: GiftCardClass,
}

/// GET walletobjects/v1/giftCardClass
/// Inserts an gift card class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_insert_builder()` + `walletobjects_giftcardclass_insert_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardclass_insert_builder(client, &args.body)?;
    walletobjects_giftcardclass_insert_execute(builder)
}

/// GET walletobjects/v1/giftCardClass
/// Returns a list of all gift card classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_list_execute()` to send, or `walletobjects_giftcardclass_list` for simplest API.

pub fn walletobjects_giftcardclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/giftCardClass
/// Returns a list of all gift card classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_list_execute()` or `walletobjects_giftcardclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass
/// Returns a list of all gift card classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_list_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/giftCardClass
/// Returns a list of all gift card classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_list_builder()` + `walletobjects_giftcardclass_list_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_giftcardclass_list_execute(builder)
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_patch_execute()` to send, or `walletobjects_giftcardclass_patch` for simplest API.

pub fn walletobjects_giftcardclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GiftCardClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_patch_execute()` or `walletobjects_giftcardclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_patch_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GiftCardClass,
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_patch_builder()` + `walletobjects_giftcardclass_patch_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardclass_patch_execute(builder)
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardclass_update_execute()` to send, or `walletobjects_giftcardclass_update` for simplest API.

pub fn walletobjects_giftcardclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GiftCardClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardclass_update_execute()` or `walletobjects_giftcardclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardclass_update_task()`.
/// For the simplest API, use `walletobjects_giftcardclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GiftCardClass,
}

/// GET walletobjects/v1/giftCardClass/{resourceId}
/// Updates the gift card class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardclass_update_builder()` + `walletobjects_giftcardclass_update_execute()`.
/// For task-level control, use `walletobjects_giftcardclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardclass_update_execute(builder)
}

/// GET walletobjects/v1/giftCardObject/{resourceId}/addMessage
/// Adds a message to the gift card object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_addmessage_execute()` to send, or `walletobjects_giftcardobject_addmessage` for simplest API.

pub fn walletobjects_giftcardobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}/addMessage
/// Adds a message to the gift card object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_addmessage_execute()` or `walletobjects_giftcardobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GiftCardObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}/addMessage
/// Adds a message to the gift card object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/giftCardObject/{resourceId}/addMessage
/// Adds a message to the gift card object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_addmessage_builder()` + `walletobjects_giftcardobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_giftcardobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Returns the gift card object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_get_execute()` to send, or `walletobjects_giftcardobject_get` for simplest API.

pub fn walletobjects_giftcardobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Returns the gift card object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_get_execute()` or `walletobjects_giftcardobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Returns the gift card object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_get_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Returns the gift card object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_get_builder()` + `walletobjects_giftcardobject_get_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardobject_get_builder(client, &args.resourceId)?;
    walletobjects_giftcardobject_get_execute(builder)
}

/// GET walletobjects/v1/giftCardObject
/// Inserts an gift card object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_insert_execute()` to send, or `walletobjects_giftcardobject_insert` for simplest API.

pub fn walletobjects_giftcardobject_insert_builder(
    client: &SimpleHttpClient,
    body: &GiftCardObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardObject
/// Inserts an gift card object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_insert_execute()` or `walletobjects_giftcardobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject
/// Inserts an gift card object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_insert_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectInsertArgs {
    /// Request body.
    pub body: GiftCardObject,
}

/// GET walletobjects/v1/giftCardObject
/// Inserts an gift card object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_insert_builder()` + `walletobjects_giftcardobject_insert_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardobject_insert_builder(client, &args.body)?;
    walletobjects_giftcardobject_insert_execute(builder)
}

/// GET walletobjects/v1/giftCardObject
/// Returns a list of all gift card objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_list_execute()` to send, or `walletobjects_giftcardobject_list` for simplest API.

pub fn walletobjects_giftcardobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/giftCardObject
/// Returns a list of all gift card objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_list_execute()` or `walletobjects_giftcardobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject
/// Returns a list of all gift card objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_list_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardObjectListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/giftCardObject
/// Returns a list of all gift card objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_list_builder()` + `walletobjects_giftcardobject_list_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GiftCardObjectListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_giftcardobject_list_execute(builder)
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_patch_execute()` to send, or `walletobjects_giftcardobject_patch` for simplest API.

pub fn walletobjects_giftcardobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GiftCardObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_patch_execute()` or `walletobjects_giftcardobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_patch_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GiftCardObject,
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_patch_builder()` + `walletobjects_giftcardobject_patch_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_giftcardobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardobject_patch_execute(builder)
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_giftcardobject_update_execute()` to send, or `walletobjects_giftcardobject_update` for simplest API.

pub fn walletobjects_giftcardobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &GiftCardObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_giftcardobject_update_execute()` or `walletobjects_giftcardobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GiftCardObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_giftcardobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_giftcardobject_update_task()`.
/// For the simplest API, use `walletobjects_giftcardobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_giftcardobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_giftcardobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_giftcardobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_giftcardobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: GiftCardObject,
}

/// GET walletobjects/v1/giftCardObject/{resourceId}
/// Updates the gift card object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_giftcardobject_update_builder()` + `walletobjects_giftcardobject_update_execute()`.
/// For task-level control, use `walletobjects_giftcardobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_giftcardobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsGiftcardobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GiftCardObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_giftcardobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_giftcardobject_update_execute(builder)
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Returns the issuer with the given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_issuer_get_execute()` to send, or `walletobjects_issuer_get` for simplest API.

pub fn walletobjects_issuer_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Returns the issuer with the given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_issuer_get_execute()` or `walletobjects_issuer_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Issuer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Returns the issuer with the given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_issuer_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_issuer_get_task()`.
/// For the simplest API, use `walletobjects_issuer_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_issuer_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_issuer_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_issuer_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsIssuerGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Returns the issuer with the given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_issuer_get_builder()` + `walletobjects_issuer_get_execute()`.
/// For task-level control, use `walletobjects_issuer_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsIssuerGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_issuer_get_builder(client, &args.resourceId)?;
    walletobjects_issuer_get_execute(builder)
}

/// GET walletobjects/v1/issuer
/// Inserts an issuer with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_issuer_insert_execute()` to send, or `walletobjects_issuer_insert` for simplest API.

pub fn walletobjects_issuer_insert_builder(
    client: &SimpleHttpClient,
    body: &Issuer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/issuer",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/issuer
/// Inserts an issuer with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_issuer_insert_execute()` or `walletobjects_issuer_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Issuer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/issuer
/// Inserts an issuer with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_issuer_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_issuer_insert_task()`.
/// For the simplest API, use `walletobjects_issuer_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_issuer_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_issuer_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_issuer_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsIssuerInsertArgs {
    /// Request body.
    pub body: Issuer,
}

/// GET walletobjects/v1/issuer
/// Inserts an issuer with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_issuer_insert_builder()` + `walletobjects_issuer_insert_execute()`.
/// For task-level control, use `walletobjects_issuer_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsIssuerInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_issuer_insert_builder(client, &args.body)?;
    walletobjects_issuer_insert_execute(builder)
}

/// GET walletobjects/v1/issuer
/// Returns a list of all issuers shared to the caller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_issuer_list_execute()` to send, or `walletobjects_issuer_list` for simplest API.

pub fn walletobjects_issuer_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/issuer",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/issuer
/// Returns a list of all issuers shared to the caller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_issuer_list_execute()` or `walletobjects_issuer_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<IssuerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IssuerListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/issuer
/// Returns a list of all issuers shared to the caller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_issuer_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_issuer_list_task()`.
/// For the simplest API, use `walletobjects_issuer_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_issuer_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_issuer_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/issuer
/// Returns a list of all issuers shared to the caller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_issuer_list_builder()` + `walletobjects_issuer_list_execute()`.
/// For task-level control, use `walletobjects_issuer_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IssuerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_issuer_list_builder(client)?;
    walletobjects_issuer_list_execute(builder)
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_issuer_patch_execute()` to send, or `walletobjects_issuer_patch` for simplest API.

pub fn walletobjects_issuer_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &Issuer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_issuer_patch_execute()` or `walletobjects_issuer_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Issuer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_issuer_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_issuer_patch_task()`.
/// For the simplest API, use `walletobjects_issuer_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_issuer_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_issuer_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_issuer_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsIssuerPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: Issuer,
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_issuer_patch_builder()` + `walletobjects_issuer_patch_execute()`.
/// For task-level control, use `walletobjects_issuer_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsIssuerPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_issuer_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_issuer_patch_execute(builder)
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_issuer_update_execute()` to send, or `walletobjects_issuer_update` for simplest API.

pub fn walletobjects_issuer_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &Issuer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_issuer_update_execute()` or `walletobjects_issuer_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Issuer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_issuer_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_issuer_update_task()`.
/// For the simplest API, use `walletobjects_issuer_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_issuer_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_issuer_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_issuer_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_issuer_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsIssuerUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: Issuer,
}

/// GET walletobjects/v1/issuer/{resourceId}
/// Updates the issuer referenced by the given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_issuer_update_builder()` + `walletobjects_issuer_update_execute()`.
/// For task-level control, use `walletobjects_issuer_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_issuer_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsIssuerUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Issuer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_issuer_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_issuer_update_execute(builder)
}

/// GET walletobjects/v1/jwt
/// Inserts the resources in the JWT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_jwt_insert_execute()` to send, or `walletobjects_jwt_insert` for simplest API.

pub fn walletobjects_jwt_insert_builder(
    client: &SimpleHttpClient,
    body: &JwtResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/jwt",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/jwt
/// Inserts the resources in the JWT.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_jwt_insert_execute()` or `walletobjects_jwt_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_jwt_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_jwt_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<JwtInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JwtInsertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/jwt
/// Inserts the resources in the JWT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_jwt_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_jwt_insert_task()`.
/// For the simplest API, use `walletobjects_jwt_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_jwt_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_jwt_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JwtInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_jwt_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_jwt_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsJwtInsertArgs {
    /// Request body.
    pub body: JwtResource,
}

/// GET walletobjects/v1/jwt
/// Inserts the resources in the JWT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_jwt_insert_builder()` + `walletobjects_jwt_insert_execute()`.
/// For task-level control, use `walletobjects_jwt_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_jwt_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsJwtInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JwtInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_jwt_insert_builder(client, &args.body)?;
    walletobjects_jwt_insert_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}/addMessage
/// Adds a message to the loyalty class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_addmessage_execute()` to send, or `walletobjects_loyaltyclass_addmessage` for simplest API.

pub fn walletobjects_loyaltyclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}/addMessage
/// Adds a message to the loyalty class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_addmessage_execute()` or `walletobjects_loyaltyclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<LoyaltyClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}/addMessage
/// Adds a message to the loyalty class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoyaltyClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}/addMessage
/// Adds a message to the loyalty class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_addmessage_builder()` + `walletobjects_loyaltyclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoyaltyClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_loyaltyclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Returns the loyalty class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_get_execute()` to send, or `walletobjects_loyaltyclass_get` for simplest API.

pub fn walletobjects_loyaltyclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Returns the loyalty class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_get_execute()` or `walletobjects_loyaltyclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Returns the loyalty class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_get_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Returns the loyalty class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_get_builder()` + `walletobjects_loyaltyclass_get_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyclass_get_builder(client, &args.resourceId)?;
    walletobjects_loyaltyclass_get_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass
/// Inserts an loyalty class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_insert_execute()` to send, or `walletobjects_loyaltyclass_insert` for simplest API.

pub fn walletobjects_loyaltyclass_insert_builder(
    client: &SimpleHttpClient,
    body: &LoyaltyClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyClass
/// Inserts an loyalty class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_insert_execute()` or `walletobjects_loyaltyclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass
/// Inserts an loyalty class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_insert_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassInsertArgs {
    /// Request body.
    pub body: LoyaltyClass,
}

/// GET walletobjects/v1/loyaltyClass
/// Inserts an loyalty class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_insert_builder()` + `walletobjects_loyaltyclass_insert_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyclass_insert_builder(client, &args.body)?;
    walletobjects_loyaltyclass_insert_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass
/// Returns a list of all loyalty classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_list_execute()` to send, or `walletobjects_loyaltyclass_list` for simplest API.

pub fn walletobjects_loyaltyclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/loyaltyClass
/// Returns a list of all loyalty classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_list_execute()` or `walletobjects_loyaltyclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass
/// Returns a list of all loyalty classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_list_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/loyaltyClass
/// Returns a list of all loyalty classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_list_builder()` + `walletobjects_loyaltyclass_list_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_loyaltyclass_list_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_patch_execute()` to send, or `walletobjects_loyaltyclass_patch` for simplest API.

pub fn walletobjects_loyaltyclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &LoyaltyClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_patch_execute()` or `walletobjects_loyaltyclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_patch_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: LoyaltyClass,
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_patch_builder()` + `walletobjects_loyaltyclass_patch_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyclass_patch_execute(builder)
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyclass_update_execute()` to send, or `walletobjects_loyaltyclass_update` for simplest API.

pub fn walletobjects_loyaltyclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &LoyaltyClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyclass_update_execute()` or `walletobjects_loyaltyclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyclass_update_task()`.
/// For the simplest API, use `walletobjects_loyaltyclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: LoyaltyClass,
}

/// GET walletobjects/v1/loyaltyClass/{resourceId}
/// Updates the loyalty class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyclass_update_builder()` + `walletobjects_loyaltyclass_update_execute()`.
/// For task-level control, use `walletobjects_loyaltyclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyclass_update_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/addMessage
/// Adds a message to the loyalty object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_addmessage_execute()` to send, or `walletobjects_loyaltyobject_addmessage` for simplest API.

pub fn walletobjects_loyaltyobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/addMessage
/// Adds a message to the loyalty object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_addmessage_execute()` or `walletobjects_loyaltyobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<LoyaltyObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/addMessage
/// Adds a message to the loyalty object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoyaltyObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/addMessage
/// Adds a message to the loyalty object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_addmessage_builder()` + `walletobjects_loyaltyobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoyaltyObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_loyaltyobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Returns the loyalty object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_get_execute()` to send, or `walletobjects_loyaltyobject_get` for simplest API.

pub fn walletobjects_loyaltyobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Returns the loyalty object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_get_execute()` or `walletobjects_loyaltyobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Returns the loyalty object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_get_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Returns the loyalty object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_get_builder()` + `walletobjects_loyaltyobject_get_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_get_builder(client, &args.resourceId)?;
    walletobjects_loyaltyobject_get_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject
/// Inserts an loyalty object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_insert_execute()` to send, or `walletobjects_loyaltyobject_insert` for simplest API.

pub fn walletobjects_loyaltyobject_insert_builder(
    client: &SimpleHttpClient,
    body: &LoyaltyObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyObject
/// Inserts an loyalty object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_insert_execute()` or `walletobjects_loyaltyobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject
/// Inserts an loyalty object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_insert_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectInsertArgs {
    /// Request body.
    pub body: LoyaltyObject,
}

/// GET walletobjects/v1/loyaltyObject
/// Inserts an loyalty object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_insert_builder()` + `walletobjects_loyaltyobject_insert_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_insert_builder(client, &args.body)?;
    walletobjects_loyaltyobject_insert_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject
/// Returns a list of all loyalty objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_list_execute()` to send, or `walletobjects_loyaltyobject_list` for simplest API.

pub fn walletobjects_loyaltyobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/loyaltyObject
/// Returns a list of all loyalty objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_list_execute()` or `walletobjects_loyaltyobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject
/// Returns a list of all loyalty objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_list_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/loyaltyObject
/// Returns a list of all loyalty objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_list_builder()` + `walletobjects_loyaltyobject_list_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_loyaltyobject_list_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the loyalty object with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_modifylinkedofferobjects_execute()` to send, or `walletobjects_loyaltyobject_modifylinkedofferobjects` for simplest API.

pub fn walletobjects_loyaltyobject_modifylinkedofferobjects_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &ModifyLinkedOfferObjectsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}/modifyLinkedOfferObjects",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the loyalty object with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_modifylinkedofferobjects_execute()` or `walletobjects_loyaltyobject_modifylinkedofferobjects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_modifylinkedofferobjects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_modifylinkedofferobjects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the loyalty object with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_modifylinkedofferobjects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_modifylinkedofferobjects_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_modifylinkedofferobjects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_modifylinkedofferobjects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_modifylinkedofferobjects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_modifylinkedofferobjects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_modifylinkedofferobjects`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectModifylinkedofferobjectsArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: ModifyLinkedOfferObjectsRequest,
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects
/// Deprecated: Use Auto Linked Passes instead. Modifies linked offer objects for the loyalty object with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_modifylinkedofferobjects_builder()` + `walletobjects_loyaltyobject_modifylinkedofferobjects_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_modifylinkedofferobjects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_modifylinkedofferobjects(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectModifylinkedofferobjectsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_modifylinkedofferobjects_builder(
        client,
        &args.resourceId,
        &args.body,
    )?;
    walletobjects_loyaltyobject_modifylinkedofferobjects_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_patch_execute()` to send, or `walletobjects_loyaltyobject_patch` for simplest API.

pub fn walletobjects_loyaltyobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &LoyaltyObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_patch_execute()` or `walletobjects_loyaltyobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_patch_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: LoyaltyObject,
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_patch_builder()` + `walletobjects_loyaltyobject_patch_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyobject_patch_execute(builder)
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_loyaltyobject_update_execute()` to send, or `walletobjects_loyaltyobject_update` for simplest API.

pub fn walletobjects_loyaltyobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &LoyaltyObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_loyaltyobject_update_execute()` or `walletobjects_loyaltyobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoyaltyObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_loyaltyobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_loyaltyobject_update_task()`.
/// For the simplest API, use `walletobjects_loyaltyobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_loyaltyobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_loyaltyobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_loyaltyobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_loyaltyobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: LoyaltyObject,
}

/// GET walletobjects/v1/loyaltyObject/{resourceId}
/// Updates the loyalty object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_loyaltyobject_update_builder()` + `walletobjects_loyaltyobject_update_execute()`.
/// For task-level control, use `walletobjects_loyaltyobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_loyaltyobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsLoyaltyobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoyaltyObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_loyaltyobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_loyaltyobject_update_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues
/// Downloads rotating barcode values for the transit object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_media_download_execute()` to send, or `walletobjects_media_download` for simplest API.

pub fn walletobjects_media_download_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/downloadRotatingBarcodeValues",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues
/// Downloads rotating barcode values for the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_media_download_execute()` or `walletobjects_media_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_media_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Media = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues
/// Downloads rotating barcode values for the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_media_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_media_download_task()`.
/// For the simplest API, use `walletobjects_media_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_media_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_media_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_media_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsMediaDownloadArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues
/// Downloads rotating barcode values for the transit object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_media_download_builder()` + `walletobjects_media_download_execute()`.
/// For task-level control, use `walletobjects_media_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_media_download(
    client: &SimpleHttpClient,
    args: &WalletobjectsMediaDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_media_download_builder(client, &args.resourceId)?;
    walletobjects_media_download_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues
/// Uploads rotating barcode values for the transit object referenced by the given object ID. Note the max upload size is specified in google3/`production/config/cdd/apps-upload/customers/payments-consumer-passes/config`.gcl and enforced by Scotty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_media_upload_execute()` to send, or `walletobjects_media_upload` for simplest API.

pub fn walletobjects_media_upload_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &TransitObjectUploadRotatingBarcodeValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/uploadRotatingBarcodeValues",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues
/// Uploads rotating barcode values for the transit object referenced by the given object ID. Note the max upload size is specified in google3/`production/config/cdd/apps-upload/customers/payments-consumer-passes/config`.gcl and enforced by Scotty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_media_upload_execute()` or `walletobjects_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<TransitObjectUploadRotatingBarcodeValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObjectUploadRotatingBarcodeValuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues
/// Uploads rotating barcode values for the transit object referenced by the given object ID. Note the max upload size is specified in google3/`production/config/cdd/apps-upload/customers/payments-consumer-passes/config`.gcl and enforced by Scotty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_media_upload_task()`.
/// For the simplest API, use `walletobjects_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitObjectUploadRotatingBarcodeValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsMediaUploadArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: TransitObjectUploadRotatingBarcodeValuesRequest,
}

/// GET walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues
/// Uploads rotating barcode values for the transit object referenced by the given object ID. Note the max upload size is specified in google3/`production/config/cdd/apps-upload/customers/payments-consumer-passes/config`.gcl and enforced by Scotty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_media_upload_builder()` + `walletobjects_media_upload_execute()`.
/// For task-level control, use `walletobjects_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_media_upload(
    client: &SimpleHttpClient,
    args: &WalletobjectsMediaUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitObjectUploadRotatingBarcodeValuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_media_upload_builder(client, &args.resourceId, &args.body)?;
    walletobjects_media_upload_execute(builder)
}

/// GET walletobjects/v1/offerClass/{resourceId}/addMessage
/// Adds a message to the offer class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_addmessage_execute()` to send, or `walletobjects_offerclass_addmessage` for simplest API.

pub fn walletobjects_offerclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerClass/{resourceId}/addMessage
/// Adds a message to the offer class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_addmessage_execute()` or `walletobjects_offerclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<OfferClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass/{resourceId}/addMessage
/// Adds a message to the offer class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_offerclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OfferClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/offerClass/{resourceId}/addMessage
/// Adds a message to the offer class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_addmessage_builder()` + `walletobjects_offerclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_offerclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OfferClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_offerclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Returns the offer class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_get_execute()` to send, or `walletobjects_offerclass_get` for simplest API.

pub fn walletobjects_offerclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Returns the offer class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_get_execute()` or `walletobjects_offerclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Returns the offer class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_get_task()`.
/// For the simplest API, use `walletobjects_offerclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Returns the offer class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_get_builder()` + `walletobjects_offerclass_get_execute()`.
/// For task-level control, use `walletobjects_offerclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerclass_get_builder(client, &args.resourceId)?;
    walletobjects_offerclass_get_execute(builder)
}

/// GET walletobjects/v1/offerClass
/// Inserts an offer class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_insert_execute()` to send, or `walletobjects_offerclass_insert` for simplest API.

pub fn walletobjects_offerclass_insert_builder(
    client: &SimpleHttpClient,
    body: &OfferClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerClass
/// Inserts an offer class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_insert_execute()` or `walletobjects_offerclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass
/// Inserts an offer class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_insert_task()`.
/// For the simplest API, use `walletobjects_offerclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassInsertArgs {
    /// Request body.
    pub body: OfferClass,
}

/// GET walletobjects/v1/offerClass
/// Inserts an offer class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_insert_builder()` + `walletobjects_offerclass_insert_execute()`.
/// For task-level control, use `walletobjects_offerclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerclass_insert_builder(client, &args.body)?;
    walletobjects_offerclass_insert_execute(builder)
}

/// GET walletobjects/v1/offerClass
/// Returns a list of all offer classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_list_execute()` to send, or `walletobjects_offerclass_list` for simplest API.

pub fn walletobjects_offerclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/offerClass
/// Returns a list of all offer classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_list_execute()` or `walletobjects_offerclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass
/// Returns a list of all offer classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_list_task()`.
/// For the simplest API, use `walletobjects_offerclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/offerClass
/// Returns a list of all offer classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_list_builder()` + `walletobjects_offerclass_list_execute()`.
/// For task-level control, use `walletobjects_offerclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_offerclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_offerclass_list_execute(builder)
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_patch_execute()` to send, or `walletobjects_offerclass_patch` for simplest API.

pub fn walletobjects_offerclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &OfferClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_patch_execute()` or `walletobjects_offerclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_patch_task()`.
/// For the simplest API, use `walletobjects_offerclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: OfferClass,
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_patch_builder()` + `walletobjects_offerclass_patch_execute()`.
/// For task-level control, use `walletobjects_offerclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerclass_patch_execute(builder)
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerclass_update_execute()` to send, or `walletobjects_offerclass_update` for simplest API.

pub fn walletobjects_offerclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &OfferClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerclass_update_execute()` or `walletobjects_offerclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerclass_update_task()`.
/// For the simplest API, use `walletobjects_offerclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: OfferClass,
}

/// GET walletobjects/v1/offerClass/{resourceId}
/// Updates the offer class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerclass_update_builder()` + `walletobjects_offerclass_update_execute()`.
/// For task-level control, use `walletobjects_offerclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerclass_update_execute(builder)
}

/// GET walletobjects/v1/offerObject/{resourceId}/addMessage
/// Adds a message to the offer object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_addmessage_execute()` to send, or `walletobjects_offerobject_addmessage` for simplest API.

pub fn walletobjects_offerobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerObject/{resourceId}/addMessage
/// Adds a message to the offer object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_addmessage_execute()` or `walletobjects_offerobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<OfferObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject/{resourceId}/addMessage
/// Adds a message to the offer object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_offerobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OfferObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/offerObject/{resourceId}/addMessage
/// Adds a message to the offer object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_addmessage_builder()` + `walletobjects_offerobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_offerobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OfferObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_offerobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Returns the offer object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_get_execute()` to send, or `walletobjects_offerobject_get` for simplest API.

pub fn walletobjects_offerobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Returns the offer object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_get_execute()` or `walletobjects_offerobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Returns the offer object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_get_task()`.
/// For the simplest API, use `walletobjects_offerobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Returns the offer object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_get_builder()` + `walletobjects_offerobject_get_execute()`.
/// For task-level control, use `walletobjects_offerobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerobject_get_builder(client, &args.resourceId)?;
    walletobjects_offerobject_get_execute(builder)
}

/// GET walletobjects/v1/offerObject
/// Inserts an offer object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_insert_execute()` to send, or `walletobjects_offerobject_insert` for simplest API.

pub fn walletobjects_offerobject_insert_builder(
    client: &SimpleHttpClient,
    body: &OfferObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerObject
/// Inserts an offer object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_insert_execute()` or `walletobjects_offerobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject
/// Inserts an offer object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_insert_task()`.
/// For the simplest API, use `walletobjects_offerobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectInsertArgs {
    /// Request body.
    pub body: OfferObject,
}

/// GET walletobjects/v1/offerObject
/// Inserts an offer object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_insert_builder()` + `walletobjects_offerobject_insert_execute()`.
/// For task-level control, use `walletobjects_offerobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerobject_insert_builder(client, &args.body)?;
    walletobjects_offerobject_insert_execute(builder)
}

/// GET walletobjects/v1/offerObject
/// Returns a list of all offer objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_list_execute()` to send, or `walletobjects_offerobject_list` for simplest API.

pub fn walletobjects_offerobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/offerObject
/// Returns a list of all offer objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_list_execute()` or `walletobjects_offerobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject
/// Returns a list of all offer objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_list_task()`.
/// For the simplest API, use `walletobjects_offerobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/offerObject
/// Returns a list of all offer objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_list_builder()` + `walletobjects_offerobject_list_execute()`.
/// For task-level control, use `walletobjects_offerobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_offerobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_offerobject_list_execute(builder)
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_patch_execute()` to send, or `walletobjects_offerobject_patch` for simplest API.

pub fn walletobjects_offerobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &OfferObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_patch_execute()` or `walletobjects_offerobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_patch_task()`.
/// For the simplest API, use `walletobjects_offerobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: OfferObject,
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_patch_builder()` + `walletobjects_offerobject_patch_execute()`.
/// For task-level control, use `walletobjects_offerobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerobject_patch_execute(builder)
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_offerobject_update_execute()` to send, or `walletobjects_offerobject_update` for simplest API.

pub fn walletobjects_offerobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &OfferObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_offerobject_update_execute()` or `walletobjects_offerobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OfferObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_offerobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_offerobject_update_task()`.
/// For the simplest API, use `walletobjects_offerobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_offerobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_offerobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_offerobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_offerobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: OfferObject,
}

/// GET walletobjects/v1/offerObject/{resourceId}
/// Updates the offer object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_offerobject_update_builder()` + `walletobjects_offerobject_update_execute()`.
/// For task-level control, use `walletobjects_offerobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_offerobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsOfferobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OfferObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_offerobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_offerobject_update_execute(builder)
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Returns the permissions for the given issuer id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_permissions_get_execute()` to send, or `walletobjects_permissions_get` for simplest API.

pub fn walletobjects_permissions_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/permissions/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Returns the permissions for the given issuer id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_permissions_get_execute()` or `walletobjects_permissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_permissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permissions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Returns the permissions for the given issuer id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_permissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_permissions_get_task()`.
/// For the simplest API, use `walletobjects_permissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_permissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_permissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsPermissionsGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Returns the permissions for the given issuer id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_permissions_get_builder()` + `walletobjects_permissions_get_execute()`.
/// For task-level control, use `walletobjects_permissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_permissions_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsPermissionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_permissions_get_builder(client, &args.resourceId)?;
    walletobjects_permissions_get_execute(builder)
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Updates the permissions for the given issuer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_permissions_update_execute()` to send, or `walletobjects_permissions_update` for simplest API.

pub fn walletobjects_permissions_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &Permissions,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/permissions/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Updates the permissions for the given issuer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_permissions_update_execute()` or `walletobjects_permissions_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_permissions_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_permissions_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permissions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Updates the permissions for the given issuer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_permissions_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_permissions_update_task()`.
/// For the simplest API, use `walletobjects_permissions_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_permissions_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_permissions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_permissions_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_permissions_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsPermissionsUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: Permissions,
}

/// GET walletobjects/v1/permissions/{resourceId}
/// Updates the permissions for the given issuer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_permissions_update_builder()` + `walletobjects_permissions_update_execute()`.
/// For task-level control, use `walletobjects_permissions_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_permissions_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsPermissionsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permissions>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_permissions_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_permissions_update_execute(builder)
}

/// GET walletobjects/v1/smartTap
/// Inserts the smart tap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_smarttap_insert_execute()` to send, or `walletobjects_smarttap_insert` for simplest API.

pub fn walletobjects_smarttap_insert_builder(
    client: &SimpleHttpClient,
    body: &SmartTap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/smartTap",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/smartTap
/// Inserts the smart tap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_smarttap_insert_execute()` or `walletobjects_smarttap_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_smarttap_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_smarttap_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SmartTap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SmartTap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/smartTap
/// Inserts the smart tap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_smarttap_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_smarttap_insert_task()`.
/// For the simplest API, use `walletobjects_smarttap_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_smarttap_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_smarttap_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SmartTap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = walletobjects_smarttap_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_smarttap_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsSmarttapInsertArgs {
    /// Request body.
    pub body: SmartTap,
}

/// GET walletobjects/v1/smartTap
/// Inserts the smart tap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_smarttap_insert_builder()` + `walletobjects_smarttap_insert_execute()`.
/// For task-level control, use `walletobjects_smarttap_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_smarttap_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsSmarttapInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SmartTap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = walletobjects_smarttap_insert_builder(client, &args.body)?;
    walletobjects_smarttap_insert_execute(builder)
}

/// GET walletobjects/v1/transitClass/{resourceId}/addMessage
/// Adds a message to the transit class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_addmessage_execute()` to send, or `walletobjects_transitclass_addmessage` for simplest API.

pub fn walletobjects_transitclass_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitClass/{resourceId}/addMessage
/// Adds a message to the transit class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_addmessage_execute()` or `walletobjects_transitclass_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<TransitClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass/{resourceId}/addMessage
/// Adds a message to the transit class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_addmessage_task()`.
/// For the simplest API, use `walletobjects_transitclass_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/transitClass/{resourceId}/addMessage
/// Adds a message to the transit class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_addmessage_builder()` + `walletobjects_transitclass_addmessage_execute()`.
/// For task-level control, use `walletobjects_transitclass_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitClassAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_transitclass_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitclass_addmessage_execute(builder)
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Returns the transit class with the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_get_execute()` to send, or `walletobjects_transitclass_get` for simplest API.

pub fn walletobjects_transitclass_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Returns the transit class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_get_execute()` or `walletobjects_transitclass_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Returns the transit class with the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_get_task()`.
/// For the simplest API, use `walletobjects_transitclass_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Returns the transit class with the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_get_builder()` + `walletobjects_transitclass_get_execute()`.
/// For task-level control, use `walletobjects_transitclass_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitclass_get_builder(client, &args.resourceId)?;
    walletobjects_transitclass_get_execute(builder)
}

/// GET walletobjects/v1/transitClass
/// Inserts a transit class with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_insert_execute()` to send, or `walletobjects_transitclass_insert` for simplest API.

pub fn walletobjects_transitclass_insert_builder(
    client: &SimpleHttpClient,
    body: &TransitClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/transitClass",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitClass
/// Inserts a transit class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_insert_execute()` or `walletobjects_transitclass_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass
/// Inserts a transit class with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_insert_task()`.
/// For the simplest API, use `walletobjects_transitclass_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassInsertArgs {
    /// Request body.
    pub body: TransitClass,
}

/// GET walletobjects/v1/transitClass
/// Inserts a transit class with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_insert_builder()` + `walletobjects_transitclass_insert_execute()`.
/// For task-level control, use `walletobjects_transitclass_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitclass_insert_builder(client, &args.body)?;
    walletobjects_transitclass_insert_execute(builder)
}

/// GET walletobjects/v1/transitClass
/// Returns a list of all transit classes for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_list_execute()` to send, or `walletobjects_transitclass_list` for simplest API.

pub fn walletobjects_transitclass_list_builder(
    client: &SimpleHttpClient,
    issuerId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/transitClass",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issuerId {
        query_parts.push(format!("issuerId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/transitClass
/// Returns a list of all transit classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_list_execute()` or `walletobjects_transitclass_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClassListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass
/// Returns a list of all transit classes for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_list_task()`.
/// For the simplest API, use `walletobjects_transitclass_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassListArgs {
    /// Query parameter: issuerId
    pub issuerId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/transitClass
/// Returns a list of all transit classes for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_list_builder()` + `walletobjects_transitclass_list_execute()`.
/// For task-level control, use `walletobjects_transitclass_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClassListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitclass_list_builder(
        client,
        args.issuerId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_transitclass_list_execute(builder)
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_patch_execute()` to send, or `walletobjects_transitclass_patch` for simplest API.

pub fn walletobjects_transitclass_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &TransitClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_patch_execute()` or `walletobjects_transitclass_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_patch_task()`.
/// For the simplest API, use `walletobjects_transitclass_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: TransitClass,
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_patch_builder()` + `walletobjects_transitclass_patch_execute()`.
/// For task-level control, use `walletobjects_transitclass_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitclass_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitclass_patch_execute(builder)
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitclass_update_execute()` to send, or `walletobjects_transitclass_update` for simplest API.

pub fn walletobjects_transitclass_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &TransitClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitclass_update_execute()` or `walletobjects_transitclass_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitclass_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitclass_update_task()`.
/// For the simplest API, use `walletobjects_transitclass_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitclass_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitclass_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitclass_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitclass_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: TransitClass,
}

/// GET walletobjects/v1/transitClass/{resourceId}
/// Updates the transit class referenced by the given class ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitclass_update_builder()` + `walletobjects_transitclass_update_execute()`.
/// For task-level control, use `walletobjects_transitclass_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitclass_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitclassUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitclass_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitclass_update_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}/addMessage
/// Adds a message to the transit object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_addmessage_execute()` to send, or `walletobjects_transitobject_addmessage` for simplest API.

pub fn walletobjects_transitobject_addmessage_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &AddMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/addMessage",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitObject/{resourceId}/addMessage
/// Adds a message to the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_addmessage_execute()` or `walletobjects_transitobject_addmessage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_addmessage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<TransitObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}/addMessage
/// Adds a message to the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_addmessage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_addmessage_task()`.
/// For the simplest API, use `walletobjects_transitobject_addmessage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_addmessage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_addmessage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_addmessage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_addmessage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectAddmessageArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// GET walletobjects/v1/transitObject/{resourceId}/addMessage
/// Adds a message to the transit object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_addmessage_builder()` + `walletobjects_transitobject_addmessage_execute()`.
/// For task-level control, use `walletobjects_transitobject_addmessage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_addmessage(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectAddmessageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TransitObjectAddMessageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        walletobjects_transitobject_addmessage_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitobject_addmessage_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Returns the transit object with the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_get_execute()` to send, or `walletobjects_transitobject_get` for simplest API.

pub fn walletobjects_transitobject_get_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Returns the transit object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_get_execute()` or `walletobjects_transitobject_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Returns the transit object with the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_get_task()`.
/// For the simplest API, use `walletobjects_transitobject_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectGetArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Returns the transit object with the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_get_builder()` + `walletobjects_transitobject_get_execute()`.
/// For task-level control, use `walletobjects_transitobject_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_get(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitobject_get_builder(client, &args.resourceId)?;
    walletobjects_transitobject_get_execute(builder)
}

/// GET walletobjects/v1/transitObject
/// Inserts an transit object with the given ID and properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_insert_execute()` to send, or `walletobjects_transitobject_insert` for simplest API.

pub fn walletobjects_transitobject_insert_builder(
    client: &SimpleHttpClient,
    body: &TransitObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/transitObject",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitObject
/// Inserts an transit object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_insert_execute()` or `walletobjects_transitobject_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject
/// Inserts an transit object with the given ID and properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_insert_task()`.
/// For the simplest API, use `walletobjects_transitobject_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectInsertArgs {
    /// Request body.
    pub body: TransitObject,
}

/// GET walletobjects/v1/transitObject
/// Inserts an transit object with the given ID and properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_insert_builder()` + `walletobjects_transitobject_insert_execute()`.
/// For task-level control, use `walletobjects_transitobject_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_insert(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitobject_insert_builder(client, &args.body)?;
    walletobjects_transitobject_insert_execute(builder)
}

/// GET walletobjects/v1/transitObject
/// Returns a list of all transit objects for a given issuer ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_list_execute()` to send, or `walletobjects_transitobject_list` for simplest API.

pub fn walletobjects_transitobject_list_builder(
    client: &SimpleHttpClient,
    classId: Option<&str>,
    maxResults: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://walletobjects.googleapis.com/walletobjects/v1/transitObject",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = classId {
        query_parts.push(format!("classId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET walletobjects/v1/transitObject
/// Returns a list of all transit objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_list_execute()` or `walletobjects_transitobject_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObjectListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject
/// Returns a list of all transit objects for a given issuer ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_list_task()`.
/// For the simplest API, use `walletobjects_transitobject_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectListArgs {
    /// Query parameter: classId
    pub classId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
}

/// GET walletobjects/v1/transitObject
/// Returns a list of all transit objects for a given issuer ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_list_builder()` + `walletobjects_transitobject_list_execute()`.
/// For task-level control, use `walletobjects_transitobject_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_list(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObjectListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitobject_list_builder(
        client,
        args.classId.as_deref(),
        args.maxResults,
        args.token.as_deref(),
    )?;
    walletobjects_transitobject_list_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_patch_execute()` to send, or `walletobjects_transitobject_patch` for simplest API.

pub fn walletobjects_transitobject_patch_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &TransitObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_patch_execute()` or `walletobjects_transitobject_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_patch_task()`.
/// For the simplest API, use `walletobjects_transitobject_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectPatchArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: TransitObject,
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_patch_builder()` + `walletobjects_transitobject_patch_execute()`.
/// For task-level control, use `walletobjects_transitobject_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_patch(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitobject_patch_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitobject_patch_execute(builder)
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_transitobject_update_execute()` to send, or `walletobjects_transitobject_update` for simplest API.

pub fn walletobjects_transitobject_update_builder(
    client: &SimpleHttpClient,
    resourceId: &str,
    body: &TransitObject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        resourceId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_transitobject_update_execute()` or `walletobjects_transitobject_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransitObject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_transitobject_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_transitobject_update_task()`.
/// For the simplest API, use `walletobjects_transitobject_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_transitobject_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_transitobject_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_transitobject_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_transitobject_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectUpdateArgs {
    /// Path parameter: resourceId
    pub resourceId: String,
    /// Request body.
    pub body: TransitObject,
}

/// GET walletobjects/v1/transitObject/{resourceId}
/// Updates the transit object referenced by the given object ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_transitobject_update_builder()` + `walletobjects_transitobject_update_execute()`.
/// For task-level control, use `walletobjects_transitobject_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_transitobject_update(
    client: &SimpleHttpClient,
    args: &WalletobjectsTransitobjectUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransitObject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_transitobject_update_builder(client, &args.resourceId, &args.body)?;
    walletobjects_transitobject_update_execute(builder)
}

/// GET walletobjects/v1/privateContent/setPassUpdateNotice
/// Provide Google with information about awaiting private pass update. This will allow Google to provide the update notification to the device that currently holds this pass.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_execute()` to send, or `walletobjects_walletobjects_v1_private_content_set_pass_update_notice` for simplest API.

pub fn walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder(
    client: &SimpleHttpClient,
    body: &SetPassUpdateNoticeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/privateContent/setPassUpdateNotice",
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET walletobjects/v1/privateContent/setPassUpdateNotice
/// Provide Google with information about awaiting private pass update. This will allow Google to provide the update notification to the device that currently holds this pass.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_execute()` or `walletobjects_walletobjects_v1_private_content_set_pass_update_notice`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_walletobjects_v1_private_content_set_pass_update_notice_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SetPassUpdateNoticeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetPassUpdateNoticeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET walletobjects/v1/privateContent/setPassUpdateNotice
/// Provide Google with information about awaiting private pass update. This will allow Google to provide the update notification to the device that currently holds this pass.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_task()`.
/// For the simplest API, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn walletobjects_walletobjects_v1_private_content_set_pass_update_notice_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPassUpdateNoticeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = walletobjects_walletobjects_v1_private_content_set_pass_update_notice_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`walletobjects_walletobjects_v1_private_content_set_pass_update_notice`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WalletobjectsWalletobjectsV1PrivateContentSetPassUpdateNoticeArgs {
    /// Request body.
    pub body: SetPassUpdateNoticeRequest,
}

/// GET walletobjects/v1/privateContent/setPassUpdateNotice
/// Provide Google with information about awaiting private pass update. This will allow Google to provide the update notification to the device that currently holds this pass.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder()` + `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_execute()`.
/// For task-level control, use `walletobjects_walletobjects_v1_private_content_set_pass_update_notice_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn walletobjects_walletobjects_v1_private_content_set_pass_update_notice(
    client: &SimpleHttpClient,
    args: &WalletobjectsWalletobjectsV1PrivateContentSetPassUpdateNoticeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPassUpdateNoticeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = walletobjects_walletobjects_v1_private_content_set_pass_update_notice_builder(
        client, &args.body,
    )?;
    walletobjects_walletobjects_v1_private_content_set_pass_update_notice_execute(builder)
}
