//! Auto-generated API clients for gcp/healthcare.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_get_execute()` to send, or `healthcare_projects_locations_get` for simplest API.

pub fn healthcare_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_get_execute()` or `healthcare_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_get_builder()` + `healthcare_projects_locations_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_get_builder(client, &args.name)?;
    healthcare_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_list_execute()` to send, or `healthcare_projects_locations_list` for simplest API.

pub fn healthcare_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_list_execute()` or `healthcare_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_list_builder()` + `healthcare_projects_locations_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a new health dataset. Results are returned through the Operation interface which returns either an Operation.response which contains a Dataset or Operation.error. The metadata field type is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_create_execute()` to send, or `healthcare_projects_locations_datasets_create` for simplest API.

pub fn healthcare_projects_locations_datasets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    datasetId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = datasetId.as_ref() {
        query_parts.push(format!("datasetId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a new health dataset. Results are returned through the Operation interface which returns either an Operation.response which contains a Dataset or Operation.error. The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_create_execute()` or `healthcare_projects_locations_datasets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a new health dataset. Results are returned through the Operation interface which returns either an Operation.response which contains a Dataset or Operation.error. The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: datasetId
    pub datasetId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a new health dataset. Results are returned through the Operation interface which returns either an Operation.response which contains a Dataset or Operation.error. The metadata field type is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_create_builder()` + `healthcare_projects_locations_datasets_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_create_builder(
        client,
        &args.parent,
        &args.datasetId,
    )?;
    healthcare_projects_locations_datasets_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:deidentify
/// Creates a new dataset containing de-identified data from the source dataset. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifySummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The new de-identified dataset will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_deidentify_execute()` to send, or `healthcare_projects_locations_datasets_deidentify` for simplest API.

pub fn healthcare_projects_locations_datasets_deidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    sourceDataset: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}:deidentify",
        sourceDataset,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:deidentify
/// Creates a new dataset containing de-identified data from the source dataset. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifySummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The new de-identified dataset will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_deidentify_execute()` or `healthcare_projects_locations_datasets_deidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_deidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:deidentify
/// Creates a new dataset containing de-identified data from the source dataset. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifySummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The new de-identified dataset will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_deidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_deidentify_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_deidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_deidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_deidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDeidentifyArgs {
    /// Path parameter: sourceDataset
    pub sourceDataset: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:deidentify
/// Creates a new dataset containing de-identified data from the source dataset. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifySummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The new de-identified dataset will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_deidentify_builder()` + `healthcare_projects_locations_datasets_deidentify_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_deidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_deidentify(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDeidentifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_deidentify_builder(client, &args.sourceDataset)?;
    healthcare_projects_locations_datasets_deidentify_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes the specified health dataset and all data contained in the dataset. Deleting a dataset does not affect the sources from which the dataset was imported (if any).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_delete_execute()` to send, or `healthcare_projects_locations_datasets_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes the specified health dataset and all data contained in the dataset. Deleting a dataset does not affect the sources from which the dataset was imported (if any).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_delete_execute()` or `healthcare_projects_locations_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes the specified health dataset and all data contained in the dataset. Deleting a dataset does not affect the sources from which the dataset was imported (if any).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes the specified health dataset and all data contained in the dataset. Deleting a dataset does not affect the sources from which the dataset was imported (if any).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_delete_builder()` + `healthcare_projects_locations_datasets_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets any metadata associated with a dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_get_execute()` to send, or `healthcare_projects_locations_datasets_get` for simplest API.

pub fn healthcare_projects_locations_datasets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets any metadata associated with a dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_get_execute()` or `healthcare_projects_locations_datasets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets any metadata associated with a dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets any metadata associated with a dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_get_builder()` + `healthcare_projects_locations_datasets_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    healthcare_projects_locations_datasets_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists the health datasets in the current project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_list_execute()` to send, or `healthcare_projects_locations_datasets_list` for simplest API.

pub fn healthcare_projects_locations_datasets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists the health datasets in the current project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_list_execute()` or `healthcare_projects_locations_datasets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDatasetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatasetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists the health datasets in the current project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatasetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists the health datasets in the current project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_list_builder()` + `healthcare_projects_locations_datasets_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatasetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates dataset metadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_patch_execute()` to send, or `healthcare_projects_locations_datasets_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates dataset metadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_patch_execute()` or `healthcare_projects_locations_datasets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates dataset metadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates dataset metadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_patch_builder()` + `healthcare_projects_locations_datasets_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_patch_builder(client, &args.name, &args.updateMask)?;
    healthcare_projects_locations_datasets_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_set_iam_policy_builder(client, &args.resource)?;
    healthcare_projects_locations_datasets_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:checkDataAccess
/// Checks if a particular data_id of a User data mapping in the specified consent store is consented for the specified use.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_check_data_access_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_check_data_access` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_check_data_access_builder<R>(
    client: &SimpleHttpClient<R>,
    consentStore: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:checkDataAccess",
        consentStore,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:checkDataAccess
/// Checks if a particular data_id of a User data mapping in the specified consent store is consented for the specified use.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_check_data_access_execute()` or `healthcare_projects_locations_datasets_consent_stores_check_data_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_check_data_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_check_data_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckDataAccessResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckDataAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:checkDataAccess
/// Checks if a particular data_id of a User data mapping in the specified consent store is consented for the specified use.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_check_data_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_check_data_access_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_check_data_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_check_data_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_check_data_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckDataAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_check_data_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_check_data_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs {
    /// Path parameter: consentStore
    pub consentStore: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:checkDataAccess
/// Checks if a particular data_id of a User data mapping in the specified consent store is consented for the specified use.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_check_data_access_builder()` + `healthcare_projects_locations_datasets_consent_stores_check_data_access_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_check_data_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_check_data_access(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckDataAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_check_data_access_builder(
        client,
        &args.consentStore,
    )?;
    healthcare_projects_locations_datasets_consent_stores_check_data_access_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Creates a new consent store in the parent dataset. Attempting to create a consent store with the same ID as an existing store fails with an ALREADY_EXISTS error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_create_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_create` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    consentStoreId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = consentStoreId.as_ref() {
        query_parts.push(format!("consentStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Creates a new consent store in the parent dataset. Attempting to create a consent store with the same ID as an existing store fails with an ALREADY_EXISTS error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_create_execute()` or `healthcare_projects_locations_datasets_consent_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConsentStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsentStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Creates a new consent store in the parent dataset. Attempting to create a consent store with the same ID as an existing store fails with an ALREADY_EXISTS error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: consentStoreId
    pub consentStoreId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Creates a new consent store in the parent dataset. Attempting to create a consent store with the same ID as an existing store fails with an ALREADY_EXISTS error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_create_builder()` + `healthcare_projects_locations_datasets_consent_stores_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_create_builder(
        client,
        &args.parent,
        &args.consentStoreId,
    )?;
    healthcare_projects_locations_datasets_consent_stores_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Deletes the specified consent store and removes all the consent store's data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_delete_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Deletes the specified consent store and removes all the consent store's data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_delete_execute()` or `healthcare_projects_locations_datasets_consent_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Deletes the specified consent store and removes all the consent store's data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Deletes the specified consent store and removes all the consent store's data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_delete_builder()` + `healthcare_projects_locations_datasets_consent_stores_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_consent_stores_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:evaluateUserConsents
/// Evaluates the user's Consents for all matching User data mappings. Note: User data mappings are indexed asynchronously, which can cause a slight delay between the time mappings are created or updated and when they are included in EvaluateUserConsents results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder<R>(
    client: &SimpleHttpClient<R>,
    consentStore: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:evaluateUserConsents",
        consentStore,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:evaluateUserConsents
/// Evaluates the user's Consents for all matching User data mappings. Note: User data mappings are indexed asynchronously, which can cause a slight delay between the time mappings are created or updated and when they are included in EvaluateUserConsents results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_execute()` or `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EvaluateUserConsentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EvaluateUserConsentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:evaluateUserConsents
/// Evaluates the user's Consents for all matching User data mappings. Note: User data mappings are indexed asynchronously, which can cause a slight delay between the time mappings are created or updated and when they are included in EvaluateUserConsents results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EvaluateUserConsentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs {
    /// Path parameter: consentStore
    pub consentStore: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:evaluateUserConsents
/// Evaluates the user's Consents for all matching User data mappings. Note: User data mappings are indexed asynchronously, which can cause a slight delay between the time mappings are created or updated and when they are included in EvaluateUserConsents results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder()` + `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EvaluateUserConsentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder(
            client,
            &args.consentStore,
        )?;
    healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Gets the specified consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_get_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_get` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Gets the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_get_execute()` or `healthcare_projects_locations_datasets_consent_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConsentStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsentStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Gets the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Gets the specified consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_get_builder()` + `healthcare_projects_locations_datasets_consent_stores_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_consent_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_consent_stores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    healthcare_projects_locations_datasets_consent_stores_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Lists the consent stores in the specified dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_list_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_list` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Lists the consent stores in the specified dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_list_execute()` or `healthcare_projects_locations_datasets_consent_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConsentStoresResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConsentStoresResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Lists the consent stores in the specified dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConsentStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores
/// Lists the consent stores in the specified dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_list_builder()` + `healthcare_projects_locations_datasets_consent_stores_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConsentStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_consent_stores_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Updates the specified consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_patch_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Updates the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_patch_execute()` or `healthcare_projects_locations_datasets_consent_stores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConsentStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsentStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Updates the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}
/// Updates the specified consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_patch_builder()` + `healthcare_projects_locations_datasets_consent_stores_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentStore>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_consent_stores_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:queryAccessibleData
/// Queries all data_ids that are consented for a specified use in the given consent store and writes them to a specified destination. The returned Operation includes a progress counter for the number of User data mappings processed. If the request is successful, a detailed response is returned of type QueryAccessibleDataResponse, contained in the response field when the operation finishes. The metadata field type is OperationMetadata. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). For example, the following sample log entry shows a failed to evaluate consent policy error that occurred during a QueryAccessibleData call to consent store `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}`. json `jsonPayload`: { @type: "type.googleapis.`com/google`.cloud.healthcare.logging.QueryAccessibleDataLogEntry" error: { code: 9 message: "failed to evaluate consent policy" } `resourceName`: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}/consents/{consent_id}`" } `logName`: "`projects/{project_id}/logs/healthcare`.googleapis.com%2Fquery_accessible_data" operation: { id: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/operations/{operation_id}`" producer: "healthcare.googleapis.`com/QueryAccessibleData`" } `receiveTimestamp`: "TIMESTAMP" resource: { labels: { consent_store_id: "{consent_store_id}" dataset_id: "{dataset_id}" location: "{location_id}" project_id: "{project_id}" } type: "healthcare_consent_store" } severity: "`ERROR`" timestamp: "TIMESTAMP"
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_query_accessible_data` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder<R>(
    client: &SimpleHttpClient<R>,
    consentStore: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:queryAccessibleData",
        consentStore,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:queryAccessibleData
/// Queries all data_ids that are consented for a specified use in the given consent store and writes them to a specified destination. The returned Operation includes a progress counter for the number of User data mappings processed. If the request is successful, a detailed response is returned of type QueryAccessibleDataResponse, contained in the response field when the operation finishes. The metadata field type is OperationMetadata. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). For example, the following sample log entry shows a failed to evaluate consent policy error that occurred during a QueryAccessibleData call to consent store `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}`. json `jsonPayload`: { @type: "type.googleapis.`com/google`.cloud.healthcare.logging.QueryAccessibleDataLogEntry" error: { code: 9 message: "failed to evaluate consent policy" } `resourceName`: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}/consents/{consent_id}`" } `logName`: "`projects/{project_id}/logs/healthcare`.googleapis.com%2Fquery_accessible_data" operation: { id: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/operations/{operation_id}`" producer: "healthcare.googleapis.`com/QueryAccessibleData`" } `receiveTimestamp`: "TIMESTAMP" resource: { labels: { consent_store_id: "{consent_store_id}" dataset_id: "{dataset_id}" location: "{location_id}" project_id: "{project_id}" } type: "healthcare_consent_store" } severity: "`ERROR`" timestamp: "TIMESTAMP"
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_execute()` or `healthcare_projects_locations_datasets_consent_stores_query_accessible_data`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:queryAccessibleData
/// Queries all data_ids that are consented for a specified use in the given consent store and writes them to a specified destination. The returned Operation includes a progress counter for the number of User data mappings processed. If the request is successful, a detailed response is returned of type QueryAccessibleDataResponse, contained in the response field when the operation finishes. The metadata field type is OperationMetadata. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). For example, the following sample log entry shows a failed to evaluate consent policy error that occurred during a QueryAccessibleData call to consent store `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}`. json `jsonPayload`: { @type: "type.googleapis.`com/google`.cloud.healthcare.logging.QueryAccessibleDataLogEntry" error: { code: 9 message: "failed to evaluate consent policy" } `resourceName`: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}/consents/{consent_id}`" } `logName`: "`projects/{project_id}/logs/healthcare`.googleapis.com%2Fquery_accessible_data" operation: { id: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/operations/{operation_id}`" producer: "healthcare.googleapis.`com/QueryAccessibleData`" } `receiveTimestamp`: "TIMESTAMP" resource: { labels: { consent_store_id: "{consent_store_id}" dataset_id: "{dataset_id}" location: "{location_id}" project_id: "{project_id}" } type: "healthcare_consent_store" } severity: "`ERROR`" timestamp: "TIMESTAMP"
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_query_accessible_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_query_accessible_data`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs {
    /// Path parameter: consentStore
    pub consentStore: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:queryAccessibleData
/// Queries all data_ids that are consented for a specified use in the given consent store and writes them to a specified destination. The returned Operation includes a progress counter for the number of User data mappings processed. If the request is successful, a detailed response is returned of type QueryAccessibleDataResponse, contained in the response field when the operation finishes. The metadata field type is OperationMetadata. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). For example, the following sample log entry shows a failed to evaluate consent policy error that occurred during a QueryAccessibleData call to consent store `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}`. json `jsonPayload`: { @type: "type.googleapis.`com/google`.cloud.healthcare.logging.QueryAccessibleDataLogEntry" error: { code: 9 message: "failed to evaluate consent policy" } `resourceName`: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`consentStores`/{consent_store_id}/consents/{consent_id}`" } `logName`: "`projects/{project_id}/logs/healthcare`.googleapis.com%2Fquery_accessible_data" operation: { id: "`projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/operations/{operation_id}`" producer: "healthcare.googleapis.`com/QueryAccessibleData`" } `receiveTimestamp`: "TIMESTAMP" resource: { labels: { consent_store_id: "{consent_store_id}" dataset_id: "{dataset_id}" location: "{location_id}" project_id: "{project_id}" } type: "healthcare_consent_store" } severity: "`ERROR`" timestamp: "TIMESTAMP"
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder()` + `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_query_accessible_data(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder(
            client,
            &args.consentStore,
        )?;
    healthcare_projects_locations_datasets_consent_stores_query_accessible_data_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_consent_stores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_consent_stores_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Creates a new Attribute definition in the parent consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    attributeDefinitionId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = attributeDefinitionId.as_ref() {
        query_parts.push(format!("attributeDefinitionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Creates a new Attribute definition in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_execute()` or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AttributeDefinition>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AttributeDefinition = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Creates a new Attribute definition in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: attributeDefinitionId
    pub attributeDefinitionId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Creates a new Attribute definition in the parent consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder()` + `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder(
            client,
            &args.parent,
            &args.attributeDefinitionId,
        )?;
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_execute(
        builder,
    )
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Deletes the specified Attribute definition. Fails if the Attribute definition is referenced by any User data mapping, or the latest revision of any Consent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Deletes the specified Attribute definition. Fails if the Attribute definition is referenced by any User data mapping, or the latest revision of any Consent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_execute()` or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Deletes the specified Attribute definition. Fails if the Attribute definition is referenced by any User data mapping, or the latest revision of any Consent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Deletes the specified Attribute definition. Fails if the Attribute definition is referenced by any User data mapping, or the latest revision of any Consent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder()` + `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Gets the specified Attribute definition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Gets the specified Attribute definition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_execute()` or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AttributeDefinition>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AttributeDefinition = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Gets the specified Attribute definition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Gets the specified Attribute definition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder()` + `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Lists the Attribute definitions in the specified consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Lists the Attribute definitions in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_execute()` or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttributeDefinitionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttributeDefinitionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Lists the Attribute definitions in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeDefinitionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions
/// Lists the Attribute definitions in the specified consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder()` + `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeDefinitionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_execute(
        builder,
    )
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Updates the specified Attribute definition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Updates the specified Attribute definition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_execute()` or `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AttributeDefinition>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AttributeDefinition = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Updates the specified Attribute definition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/attributeDefinitions/{attributeDefinitionsId}
/// Updates the specified Attribute definition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder()` + `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AttributeDefinition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Creates a new Consent artifact in the parent consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Creates a new Consent artifact in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_execute()` or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConsentArtifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsentArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Creates a new Consent artifact in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentArtifact>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Creates a new Consent artifact in the parent consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder()` + `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentArtifact>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder(
            client,
            &args.parent,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Deletes the specified Consent artifact. Fails if the artifact is referenced by the latest revision of any Consent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Deletes the specified Consent artifact. Fails if the artifact is referenced by the latest revision of any Consent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_execute()` or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Deletes the specified Consent artifact. Fails if the artifact is referenced by the latest revision of any Consent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Deletes the specified Consent artifact. Fails if the artifact is referenced by the latest revision of any Consent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder()` + `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Gets the specified Consent artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Gets the specified Consent artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_execute()` or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConsentArtifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConsentArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Gets the specified Consent artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentArtifact>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts/{consentArtifactsId}
/// Gets the specified Consent artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder()` + `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConsentArtifact>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Lists the Consent artifacts in the specified consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Lists the Consent artifacts in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_execute()` or `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConsentArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConsentArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Lists the Consent artifacts in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConsentArtifactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consentArtifacts
/// Lists the Consent artifacts in the specified consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder()` + `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConsentArtifactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:activate
/// Activates the latest revision of the specified Consent by committing a new revision with state updated to `ACTIVE`. If the latest revision of the specified Consent is in the `ACTIVE` state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_activate_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_activate` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:activate
/// Activates the latest revision of the specified Consent by committing a new revision with state updated to `ACTIVE`. If the latest revision of the specified Consent is in the `ACTIVE` state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_activate_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:activate
/// Activates the latest revision of the specified Consent by committing a new revision with state updated to `ACTIVE`. If the latest revision of the specified Consent is in the `ACTIVE` state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_activate_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_consents_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:activate
/// Activates the latest revision of the specified Consent by committing a new revision with state updated to `ACTIVE`. If the latest revision of the specified Consent is in the `ACTIVE` state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_activate_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_activate_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_activate(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_activate_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_activate_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Creates a new Consent in the parent consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_create_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_create` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Creates a new Consent in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_create_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Creates a new Consent in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Creates a new Consent in the parent consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_create_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_create_builder(
        client,
        &args.parent,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Deletes the Consent and its revisions. To keep a record of the Consent but mark it inactive, see [RevokeConsent]. To delete a revision of a Consent, see [DeleteConsentRevision]. This operation does not delete the related Consent artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_delete_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Deletes the Consent and its revisions. To keep a record of the Consent but mark it inactive, see [RevokeConsent]. To delete a revision of a Consent, see [DeleteConsentRevision]. This operation does not delete the related Consent artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Deletes the Consent and its revisions. To keep a record of the Consent but mark it inactive, see [RevokeConsent]. To delete a revision of a Consent, see [DeleteConsentRevision]. This operation does not delete the related Consent artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Deletes the Consent and its revisions. To keep a record of the Consent but mark it inactive, see [RevokeConsent]. To delete a revision of a Consent, see [DeleteConsentRevision]. This operation does not delete the related Consent artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_delete_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_delete_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:deleteRevision
/// Deletes the specified revision of a Consent. An INVALID_ARGUMENT error occurs if the specified revision is the latest revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:deleteRevision",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:deleteRevision
/// Deletes the specified revision of a Consent. An INVALID_ARGUMENT error occurs if the specified revision is the latest revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:deleteRevision
/// Deletes the specified revision of a Consent. An INVALID_ARGUMENT error occurs if the specified revision is the latest revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_delete_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:deleteRevision
/// Deletes the specified revision of a Consent. An INVALID_ARGUMENT error occurs if the specified revision is the latest revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_revision(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Gets the specified revision of a Consent, or the latest revision if revision_id is not specified in the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_get_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_get` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Gets the specified revision of a Consent, or the latest revision if revision_id is not specified in the resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_get_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Gets the specified revision of a Consent, or the latest revision if revision_id is not specified in the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Gets the specified revision of a Consent, or the latest revision if revision_id is not specified in the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_get_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_get_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Lists the Consent in the given consent store, returning each Consent's latest revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_list_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_list` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Lists the Consent in the given consent store, returning each Consent's latest revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_list_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConsentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConsentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Lists the Consent in the given consent store, returning each Consent's latest revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConsentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents
/// Lists the Consent in the given consent store, returning each Consent's latest revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_list_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConsentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:listRevisions
/// Lists the revisions of the specified Consent in reverse chronological order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:listRevisions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:listRevisions
/// Lists the revisions of the specified Consent in reverse chronological order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConsentRevisionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConsentRevisionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:listRevisions
/// Lists the revisions of the specified Consent in reverse chronological order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConsentRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_list_revisions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:listRevisions
/// Lists the revisions of the specified Consent in reverse chronological order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_revisions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConsentRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder(
            client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Updates the latest revision of the specified Consent by committing a new revision with the changes. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_patch_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Updates the latest revision of the specified Consent by committing a new revision with the changes. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_patch_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Updates the latest revision of the specified Consent by committing a new revision with the changes. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}
/// Updates the latest revision of the specified Consent by committing a new revision with the changes. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the REJECTED or REVOKED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_patch_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:reject
/// Rejects the latest revision of the specified Consent by committing a new revision with state updated to REJECTED. If the latest revision of the specified Consent is in the REJECTED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the `ACTIVE` or REVOKED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_reject_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_reject` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_reject_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:reject",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:reject
/// Rejects the latest revision of the specified Consent by committing a new revision with state updated to REJECTED. If the latest revision of the specified Consent is in the REJECTED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the `ACTIVE` or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_reject_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_reject`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_reject_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_reject_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:reject
/// Rejects the latest revision of the specified Consent by committing a new revision with state updated to REJECTED. If the latest revision of the specified Consent is in the REJECTED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the `ACTIVE` or REVOKED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_reject_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_reject_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_reject()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_reject_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_reject_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_reject`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:reject
/// Rejects the latest revision of the specified Consent by committing a new revision with state updated to REJECTED. If the latest revision of the specified Consent is in the REJECTED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the specified Consent is in the `ACTIVE` or REVOKED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_reject_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_reject_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_reject_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_reject(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_reject_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_reject_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:revoke
/// Revokes the latest revision of the specified Consent by committing a new revision with state updated to REVOKED. If the latest revision of the specified Consent is in the REVOKED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the given consent is in DRAFT or REJECTED state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_consents_revoke` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:revoke",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:revoke
/// Revokes the latest revision of the specified Consent by committing a new revision with state updated to REVOKED. If the latest revision of the specified Consent is in the REVOKED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the given consent is in DRAFT or REJECTED state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_execute()` or `healthcare_projects_locations_datasets_consent_stores_consents_revoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_revoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Consent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Consent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:revoke
/// Revokes the latest revision of the specified Consent by committing a new revision with state updated to REVOKED. If the latest revision of the specified Consent is in the REVOKED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the given consent is in DRAFT or REJECTED state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_consents_revoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_consents_revoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/consents/{consentsId}:revoke
/// Revokes the latest revision of the specified Consent by committing a new revision with state updated to REVOKED. If the latest revision of the specified Consent is in the REVOKED state, no new revision is committed. A FAILED_PRECONDITION error occurs if the latest revision of the given consent is in DRAFT or REJECTED state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder()` + `healthcare_projects_locations_datasets_consent_stores_consents_revoke_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_consents_revoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_consents_revoke(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Consent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_consent_stores_consents_revoke_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}:archive
/// Archives the specified User data mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}:archive",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}:archive
/// Archives the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ArchiveUserDataMappingResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ArchiveUserDataMappingResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}:archive
/// Archives the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ArchiveUserDataMappingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}:archive
/// Archives the specified User data mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ArchiveUserDataMappingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Creates a new User data mapping in the parent consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Creates a new User data mapping in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserDataMapping>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserDataMapping = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Creates a new User data mapping in the parent consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Creates a new User data mapping in the parent consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder(
            client,
            &args.parent,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Deletes the specified User data mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Deletes the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Deletes the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Deletes the specified User data mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Gets the specified User data mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Gets the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserDataMapping>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserDataMapping = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Gets the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Gets the specified User data mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Lists the User data mappings in the specified consent store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Lists the User data mappings in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUserDataMappingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserDataMappingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Lists the User data mappings in the specified consent store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserDataMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings
/// Lists the User data mappings in the specified consent store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserDataMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Updates the specified User data mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_execute()` to send, or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Updates the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_execute()` or `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserDataMapping>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserDataMapping = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Updates the specified User data mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/consentStores/{consentStoresId}/userDataMappings/{userDataMappingsId}
/// Updates the specified User data mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder()` + `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserDataMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder(
            client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )?;
    healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder(
            client,
            &args.resource,
        )?;
    healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dataMapperWorkspaces/{dataMapperWorkspacesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Creates a new DICOM store within the parent dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_create_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_create` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomStoreId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dicomStoreId.as_ref() {
        query_parts.push(format!("dicomStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Creates a new DICOM store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_create_execute()` or `healthcare_projects_locations_datasets_dicom_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DicomStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DicomStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Creates a new DICOM store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dicomStoreId
    pub dicomStoreId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Creates a new DICOM store within the parent dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_create_builder()` + `healthcare_projects_locations_datasets_dicom_stores_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_create_builder(
        client,
        &args.parent,
        &args.dicomStoreId,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyDicomStoreSummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The output DICOM store will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_deidentify_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_deidentify` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_deidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    sourceStore: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:deidentify",
        sourceStore,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyDicomStoreSummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The output DICOM store will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_deidentify_execute()` or `healthcare_projects_locations_datasets_dicom_stores_deidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_deidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyDicomStoreSummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The output DICOM store will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_deidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_deidentify_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_deidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_deidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_deidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs {
    /// Path parameter: sourceStore
    pub sourceStore: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyDicomStoreSummary. If errors occur, error is set. The LRO result may still be successful if de-identification fails for some DICOM instances. The output DICOM store will not contain these failed resources. Failed resource totals are tracked in Operation.metadata. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_deidentify_builder()` + `healthcare_projects_locations_datasets_dicom_stores_deidentify_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_deidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_deidentify(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_deidentify_builder(
        client,
        &args.sourceStore,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_deidentify_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Deletes the specified DICOM store and removes all images that are contained within it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_delete_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Deletes the specified DICOM store and removes all images that are contained within it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_delete_execute()` or `healthcare_projects_locations_datasets_dicom_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Deletes the specified DICOM store and removes all images that are contained within it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Deletes the specified DICOM store and removes all images that are contained within it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_delete_builder()` + `healthcare_projects_locations_datasets_dicom_stores_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_dicom_stores_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:export
/// Exports data to the specified destination by copying it from the DICOM store. Errors are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_export_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_export` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:export
/// Exports data to the specified destination by copying it from the DICOM store. Errors are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_export_execute()` or `healthcare_projects_locations_datasets_dicom_stores_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:export
/// Exports data to the specified destination by copying it from the DICOM store. Errors are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_export_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:export
/// Exports data to the specified destination by copying it from the DICOM store. Errors are also logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_export_builder()` + `healthcare_projects_locations_datasets_dicom_stores_export_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_export(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_export_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_dicom_stores_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Gets the specified DICOM store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_get_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_get` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Gets the specified DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_get_execute()` or `healthcare_projects_locations_datasets_dicom_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DicomStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DicomStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Gets the specified DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Gets the specified DICOM store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_get_builder()` + `healthcare_projects_locations_datasets_dicom_stores_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_dicom_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getDICOMStoreMetrics
/// Gets metrics associated with the DICOM store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getDICOMStoreMetrics",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getDICOMStoreMetrics
/// Gets metrics associated with the DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_execute()` or `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DicomStoreMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DicomStoreMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getDICOMStoreMetrics
/// Gets metrics associated with the DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresGetDicomstoreMetricsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getDICOMStoreMetrics
/// Gets metrics associated with the DICOM store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder()` + `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresGetDicomstoreMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_get_dicomstore_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:import
/// Imports data into the DICOM store by copying it from the specified source. Errors are logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_import_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_import` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_import_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:import",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:import
/// Imports data into the DICOM store by copying it from the specified source. Errors are logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_import_execute()` or `healthcare_projects_locations_datasets_dicom_stores_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:import
/// Imports data into the DICOM store by copying it from the specified source. Errors are logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_import_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresImportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:import
/// Imports data into the DICOM store by copying it from the specified source. Errors are logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>). The metadata field type is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_import_builder()` + `healthcare_projects_locations_datasets_dicom_stores_import_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_import(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_import_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_dicom_stores_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Lists the DICOM stores in the given dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_list_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_list` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Lists the DICOM stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_list_execute()` or `healthcare_projects_locations_datasets_dicom_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDicomStoresResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDicomStoresResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Lists the DICOM stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDicomStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores
/// Lists the DICOM stores in the given dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_list_builder()` + `healthcare_projects_locations_datasets_dicom_stores_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDicomStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Updates the specified DICOM store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_patch_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Updates the specified DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_patch_execute()` or `healthcare_projects_locations_datasets_dicom_stores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DicomStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DicomStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Updates the specified DICOM store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}
/// Updates the specified DICOM store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_patch_builder()` + `healthcare_projects_locations_datasets_dicom_stores_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DicomStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_search_for_instances` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/instances",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_execute()` or `healthcare_projects_locations_datasets_dicom_stores_search_for_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_search_for_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder()` + `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_instances(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder(
        client,
        &args.parent,
        &args.dicomWebPath,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_search_for_instances_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_search_for_series` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/series",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_execute()` or `healthcare_projects_locations_datasets_dicom_stores_search_for_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_search_for_series_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_search_for_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder()` + `healthcare_projects_locations_datasets_dicom_stores_search_for_series_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_series(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder(
        client,
        &args.parent,
        &args.dicomWebPath,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_search_for_series_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// SearchForStudies returns a list of matching studies. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForStudies, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForStudies, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_search_for_studies` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// SearchForStudies returns a list of matching studies. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForStudies, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForStudies, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_execute()` or `healthcare_projects_locations_datasets_dicom_stores_search_for_studies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// SearchForStudies returns a list of matching studies. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForStudies, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForStudies, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_studies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_search_for_studies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// SearchForStudies returns a list of matching studies. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForStudies, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForStudies, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder()` + `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_studies(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder(
        client,
        &args.parent,
        &args.dicomWebPath,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_search_for_studies_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setBlobStorageSettings",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_execute()` or `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder()` + `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder(
            client,
            &args.resource,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_store_instances_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_store_instances` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_store_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_store_instances_execute()` or `healthcare_projects_locations_datasets_dicom_stores_store_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_store_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_store_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_store_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_store_instances_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_store_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_store_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_store_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_store_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_store_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_store_instances_builder()` + `healthcare_projects_locations_datasets_dicom_stores_store_instances_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_store_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_store_instances(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_store_instances_builder(
        client,
        &args.parent,
        &args.dicomWebPath,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_store_instances_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:getStudyMetrics
/// GetStudyMetrics returns metrics for a study.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    study: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:getStudyMetrics",
        study,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:getStudyMetrics
/// GetStudyMetrics returns metrics for a study.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_execute()` or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudyMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudyMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:getStudyMetrics
/// GetStudyMetrics returns metrics for a study.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudyMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs {
    /// Path parameter: study
    pub study: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:getStudyMetrics
/// GetStudyMetrics returns metrics for a study.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder()` + `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudyMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder(client, &args.study)?;
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:setBlobStorageSettings",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_execute()` or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}:setBlobStorageSettings
/// SetBlobStorageSettings sets the blob storage settings of the specified resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder()` + `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder(client, &args.resource)?;
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}:getSeriesMetrics
/// GetSeriesMetrics returns metrics for a series.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    series: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}:getSeriesMetrics",
        series,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}:getSeriesMetrics
/// GetSeriesMetrics returns metrics for a series.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_execute()` or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SeriesMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SeriesMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}:getSeriesMetrics
/// GetSeriesMetrics returns metrics for a series.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SeriesMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs {
    /// Path parameter: series
    pub series: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}:getSeriesMetrics
/// GetSeriesMetrics returns metrics for a series.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder()` + `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SeriesMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder(client, &args.series)?;
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}:getStorageInfo
/// GetStorageInfo returns the storage info of the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}:getStorageInfo",
        resource,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}:getStorageInfo
/// GetStorageInfo returns the storage info of the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_execute()` or `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StorageInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StorageInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}:getStorageInfo
/// GetStorageInfo returns the storage info of the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorageInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs
{
    /// Path parameter: resource
    pub resource: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}:getStorageInfo
/// GetStorageInfo returns the storage info of the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder()` + `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StorageInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder(client, &args.resource)?;
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// DeleteStudy deletes all instances within the given study. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a study that is being deleted by an operation until the operation completes. For samples that show how to call DeleteStudy, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// DeleteStudy deletes all instances within the given study. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a study that is being deleted by an operation until the operation completes. For samples that show how to call DeleteStudy, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// DeleteStudy deletes all instances within the given study. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a study that is being deleted by an operation until the operation completes. For samples that show how to call DeleteStudy, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// DeleteStudy deletes all instances within the given study. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a study that is being deleted by an operation until the operation completes. For samples that show how to call DeleteStudy, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder(
        client,
        &args.parent,
        &args.dicomWebPath,
    )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/metadata
/// RetrieveStudyMetadata returns instance associated with the given study presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudyMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudyMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/metadata",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/metadata
/// RetrieveStudyMetadata returns instance associated with the given study presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudyMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudyMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/metadata
/// RetrieveStudyMetadata returns instance associated with the given study presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudyMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudyMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/metadata
/// RetrieveStudyMetadata returns instance associated with the given study presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudyMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudyMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// RetrieveStudy returns all instances within the given study. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudy, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudy, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// RetrieveStudy returns all instances within the given study. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudy, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudy, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// RetrieveStudy returns all instances within the given study. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudy, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudy, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// RetrieveStudy returns all instances within the given study. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveStudy, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveStudy, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/instances",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series
/// SearchForSeries returns a list of matching series. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForSeries, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForSeries, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_store_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}
/// StoreInstances stores DICOM instances associated with study instance unique identifiers (SUID). See [Store Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.5>). For details on the implementation of StoreInstances, see [Store transaction](<https://cloud.google.`com/healthcare/docs/dicom`#store_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call StoreInstances, see [Store DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#store-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_store_instances(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// DeleteSeries deletes all instances within the given study and series. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a series that is being deleted by an operation until the operation completes. For samples that show how to call DeleteSeries, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// DeleteSeries deletes all instances within the given study and series. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a series that is being deleted by an operation until the operation completes. For samples that show how to call DeleteSeries, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// DeleteSeries deletes all instances within the given study and series. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a series that is being deleted by an operation until the operation completes. For samples that show how to call DeleteSeries, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// DeleteSeries deletes all instances within the given study and series. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. The method returns an Operation which will be marked successful when the deletion is complete. Warning: Instances cannot be inserted into a series that is being deleted by an operation until the operation completes. For samples that show how to call DeleteSeries, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/metadata
/// RetrieveSeriesMetadata returns instance associated with the given study and series, presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeriesMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeriesMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/metadata",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/metadata
/// RetrieveSeriesMetadata returns instance associated with the given study and series, presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeriesMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeriesMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/metadata
/// RetrieveSeriesMetadata returns instance associated with the given study and series, presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeriesMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeriesMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/metadata
/// RetrieveSeriesMetadata returns instance associated with the given study and series, presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeriesMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeriesMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// RetrieveSeries returns all instances within the given study and series. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeries, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeries, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// RetrieveSeries returns all instances within the given study and series. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeries, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeries, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// RetrieveSeries returns all instances within the given study and series. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeries, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeries, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}
/// RetrieveSeries returns all instances within the given study and series. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveSeries, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveSeries, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder(
            client,
            &args.parent,
            &args.dicomWebPath,
        )?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances
/// SearchForInstances returns a list of matching instances. See [Search Transaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.6>). For details on the implementation of SearchForInstances, see [Search transaction](<https://cloud.google.`com/healthcare/docs/dicom`#search_transaction>) in the Cloud Healthcare API conformance statement. For samples that show how to call SearchForInstances, see [Search for DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#search-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_execute(
        builder,
    )
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// DeleteInstance deletes an instance associated with the given study, series, and SOP Instance UID. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. Study and series search results can take a few seconds to be updated after an instance is deleted using DeleteInstance. For samples that show how to call DeleteInstance, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// DeleteInstance deletes an instance associated with the given study, series, and SOP Instance UID. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. Study and series search results can take a few seconds to be updated after an instance is deleted using DeleteInstance. For samples that show how to call DeleteInstance, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// DeleteInstance deletes an instance associated with the given study, series, and SOP Instance UID. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. Study and series search results can take a few seconds to be updated after an instance is deleted using DeleteInstance. For samples that show how to call DeleteInstance, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// DeleteInstance deletes an instance associated with the given study, series, and SOP Instance UID. Delete requests are equivalent to the GET requests specified in the Retrieve transaction. Study and series search results can take a few seconds to be updated after an instance is deleted using DeleteInstance. For samples that show how to call DeleteInstance, see [Delete a study, series, or instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#delete-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// RetrieveInstance returns instance associated with the given study, series, and SOP Instance UID. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstance, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) and [DICOM instances](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_instances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstance, see [Retrieve an instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-instance>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// RetrieveInstance returns instance associated with the given study, series, and SOP Instance UID. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstance, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) and [DICOM instances](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_instances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstance, see [Retrieve an instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-instance>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// RetrieveInstance returns instance associated with the given study, series, and SOP Instance UID. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstance, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) and [DICOM instances](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_instances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstance, see [Retrieve an instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-instance>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}
/// RetrieveInstance returns instance associated with the given study, series, and SOP Instance UID. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstance, see [DICOM `study/series/instances`](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_studyseriesinstances>) and [DICOM instances](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_instances>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstance, see [Retrieve an instance](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-instance>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/metadata
/// RetrieveInstanceMetadata returns instance associated with the given study, series, and SOP Instance UID presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstanceMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstanceMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/metadata",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/metadata
/// RetrieveInstanceMetadata returns instance associated with the given study, series, and SOP Instance UID presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstanceMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstanceMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/metadata
/// RetrieveInstanceMetadata returns instance associated with the given study, series, and SOP Instance UID presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstanceMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstanceMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/metadata
/// RetrieveInstanceMetadata returns instance associated with the given study, series, and SOP Instance UID presented as metadata. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveInstanceMetadata, see [Metadata resources](<https://cloud.google.`com/healthcare/docs/dicom`#metadata_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveInstanceMetadata, see [Retrieve metadata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-metadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/rendered
/// RetrieveRenderedInstance returns instance associated with the given study, series, and SOP Instance UID in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedInstance, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedInstance, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
    viewport: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/rendered",
        parent,
        dicomWebPath,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewport.as_ref() {
        query_parts.push(format!("viewport={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/rendered
/// RetrieveRenderedInstance returns instance associated with the given study, series, and SOP Instance UID in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedInstance, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedInstance, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/rendered
/// RetrieveRenderedInstance returns instance associated with the given study, series, and SOP Instance UID in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedInstance, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedInstance, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
    /// Query parameter: viewport
    pub viewport: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/rendered
/// RetrieveRenderedInstance returns instance associated with the given study, series, and SOP Instance UID in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedInstance, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedInstance, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder(client, &args.parent, &args.dicomWebPath, &args.viewport)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/bulkdata/{bulkdataId}/{bulkdataId1}
/// Returns uncompressed, unencoded bytes representing the referenced bulkdata tag from an instance. See [Retrieve Transaction](<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveBulkdata, see [Bulkdata resources](<https://cloud.google.`com/healthcare/docs/dicom`#bulkdata-resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveBulkdata, see [Retrieve bulkdata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-bulkdata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/bulkdata/{bulkdataId}/{bulkdataId1}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/bulkdata/{bulkdataId}/{bulkdataId1}
/// Returns uncompressed, unencoded bytes representing the referenced bulkdata tag from an instance. See [Retrieve Transaction](<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveBulkdata, see [Bulkdata resources](<https://cloud.google.`com/healthcare/docs/dicom`#bulkdata-resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveBulkdata, see [Retrieve bulkdata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-bulkdata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/bulkdata/{bulkdataId}/{bulkdataId1}
/// Returns uncompressed, unencoded bytes representing the referenced bulkdata tag from an instance. See [Retrieve Transaction](<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveBulkdata, see [Bulkdata resources](<https://cloud.google.`com/healthcare/docs/dicom`#bulkdata-resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveBulkdata, see [Retrieve bulkdata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-bulkdata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/bulkdata/{bulkdataId}/{bulkdataId1}
/// Returns uncompressed, unencoded bytes representing the referenced bulkdata tag from an instance. See [Retrieve Transaction](<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveBulkdata, see [Bulkdata resources](<https://cloud.google.`com/healthcare/docs/dicom`#bulkdata-resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveBulkdata, see [Retrieve bulkdata](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-bulkdata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}
/// RetrieveFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4}.> For details on the implementation of RetrieveFrames, see [DICOM frames](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_frames>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveFrames, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}",
        parent,
        dicomWebPath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}
/// RetrieveFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4}.> For details on the implementation of RetrieveFrames, see [DICOM frames](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_frames>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveFrames, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}
/// RetrieveFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4}.> For details on the implementation of RetrieveFrames, see [DICOM frames](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_frames>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveFrames, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}
/// RetrieveFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4}.> For details on the implementation of RetrieveFrames, see [DICOM frames](<https://cloud.google.`com/healthcare/docs/dicom`#dicom_frames>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveFrames, see [Retrieve DICOM data](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-dicom>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder(client, &args.parent, &args.dicomWebPath)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}/rendered
/// RetrieveRenderedFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedFrames, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedFrames, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_execute()` to send, or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered` for simplest API.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dicomWebPath: &String,
    viewport: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}/rendered",
        parent,
        dicomWebPath,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewport.as_ref() {
        query_parts.push(format!("viewport={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}/rendered
/// RetrieveRenderedFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedFrames, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedFrames, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_execute()` or `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}/rendered
/// RetrieveRenderedFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedFrames, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedFrames, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: dicomWebPath
    pub dicomWebPath: String,
    /// Query parameter: viewport
    pub viewport: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/dicomStores/{dicomStoresId}/dicomWeb/studies/{studiesId}/series/{seriesId}/instances/{instancesId}/frames/{framesId}/rendered
/// RetrieveRenderedFrames returns instances associated with the given study, series, SOP Instance UID and frame numbers in an acceptable Rendered Media Type. See [RetrieveTransaction] (<https://dicom.nema.`org/medical/dicom/current/output/html/part18`.html#sect_10.4>). For details on the implementation of RetrieveRenderedFrames, see [Rendered resources](<https://cloud.google.`com/healthcare/docs/dicom`#rendered_resources>) in the Cloud Healthcare API conformance statement. For samples that show how to call RetrieveRenderedFrames, see [Retrieve consumer image formats](<https://cloud.google.`com/healthcare/docs/how-tos/dicomweb`#retrieve-consumer>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder()` + `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder(client, &args.parent, &args.dicomWebPath, &args.viewport)?;
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents
/// Applies the admin Consent resources for the FHIR store and reindexes the underlying resources in the FHIR store according to the aggregate consents. This method also updates the consent_config.enforced_admin_consents field of the FhirStore unless validate_only=`true` in ApplyAdminConsentsRequest. Any admin Consent resource change after this operation execution (including deletion) requires you to call ApplyAdminConsents again for the change to take effect. This method returns an Operation that can be used to track the progress of the resources that were reindexed, by calling GetOperation. Upon completion, the ApplyAdminConsentsResponse additionally contains the number of resources that were reindexed. If at least one Consent resource contains an error or fails be be enforced for any reason, the method returns an error instead of an Operation. No resources will be reindexed and the consent_config.enforced_admin_consents field will be unchanged. To enforce a consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents
/// Applies the admin Consent resources for the FHIR store and reindexes the underlying resources in the FHIR store according to the aggregate consents. This method also updates the consent_config.enforced_admin_consents field of the FhirStore unless validate_only=`true` in ApplyAdminConsentsRequest. Any admin Consent resource change after this operation execution (including deletion) requires you to call ApplyAdminConsents again for the change to take effect. This method returns an Operation that can be used to track the progress of the resources that were reindexed, by calling GetOperation. Upon completion, the ApplyAdminConsentsResponse additionally contains the number of resources that were reindexed. If at least one Consent resource contains an error or fails be be enforced for any reason, the method returns an error instead of an Operation. No resources will be reindexed and the consent_config.enforced_admin_consents field will be unchanged. To enforce a consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_execute()` or `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents
/// Applies the admin Consent resources for the FHIR store and reindexes the underlying resources in the FHIR store according to the aggregate consents. This method also updates the consent_config.enforced_admin_consents field of the FhirStore unless validate_only=`true` in ApplyAdminConsentsRequest. Any admin Consent resource change after this operation execution (including deletion) requires you to call ApplyAdminConsents again for the change to take effect. This method returns an Operation that can be used to track the progress of the resources that were reindexed, by calling GetOperation. Upon completion, the ApplyAdminConsentsResponse additionally contains the number of resources that were reindexed. If at least one Consent resource contains an error or fails be be enforced for any reason, the method returns an error instead of an Operation. No resources will be reindexed and the consent_config.enforced_admin_consents field will be unchanged. To enforce a consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents
/// Applies the admin Consent resources for the FHIR store and reindexes the underlying resources in the FHIR store according to the aggregate consents. This method also updates the consent_config.enforced_admin_consents field of the FhirStore unless validate_only=`true` in ApplyAdminConsentsRequest. Any admin Consent resource change after this operation execution (including deletion) requires you to call ApplyAdminConsents again for the change to take effect. This method returns an Operation that can be used to track the progress of the resources that were reindexed, by calling GetOperation. Upon completion, the ApplyAdminConsentsResponse additionally contains the number of resources that were reindexed. If at least one Consent resource contains an error or fails be be enforced for any reason, the method returns an error instead of an Operation. No resources will be reindexed and the consent_config.enforced_admin_consents field will be unchanged. To enforce a consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder()` + `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents
/// Apply the Consent resources for the FHIR store and reindex the underlying resources in the FHIR store according to the aggregate consent. The aggregate consent of the patient in scope in this request replaces any previous call of this method. Any Consent resource change after this operation execution (including deletion) requires you to call ApplyConsents again to have effect. This method returns an Operation that can be used to track the progress of the consent resources that were processed by calling GetOperation. Upon completion, the ApplyConsentsResponse additionally contains the number of resources that was reindexed. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). To enforce consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_apply_consents` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents
/// Apply the Consent resources for the FHIR store and reindex the underlying resources in the FHIR store according to the aggregate consent. The aggregate consent of the patient in scope in this request replaces any previous call of this method. Any Consent resource change after this operation execution (including deletion) requires you to call ApplyConsents again to have effect. This method returns an Operation that can be used to track the progress of the consent resources that were processed by calling GetOperation. Upon completion, the ApplyConsentsResponse additionally contains the number of resources that was reindexed. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). To enforce consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_execute()` or `healthcare_projects_locations_datasets_fhir_stores_apply_consents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents
/// Apply the Consent resources for the FHIR store and reindex the underlying resources in the FHIR store according to the aggregate consent. The aggregate consent of the patient in scope in this request replaces any previous call of this method. Any Consent resource change after this operation execution (including deletion) requires you to call ApplyConsents again to have effect. This method returns an Operation that can be used to track the progress of the consent resources that were processed by calling GetOperation. Upon completion, the ApplyConsentsResponse additionally contains the number of resources that was reindexed. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). To enforce consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_apply_consents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_apply_consents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents
/// Apply the Consent resources for the FHIR store and reindex the underlying resources in the FHIR store according to the aggregate consent. The aggregate consent of the patient in scope in this request replaces any previous call of this method. Any Consent resource change after this operation execution (including deletion) requires you to call ApplyConsents again to have effect. This method returns an Operation that can be used to track the progress of the consent resources that were processed by calling GetOperation. Upon completion, the ApplyConsentsResponse additionally contains the number of resources that was reindexed. Errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). To enforce consent check for data access, consent_config.access_enforced must be set to `true` for the FhirStore. FHIR Consent is not supported in DSTU2 or R5.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder()` + `healthcare_projects_locations_datasets_fhir_stores_apply_consents_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_apply_consents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_apply_consents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Group/{GroupId}/$export
/// Bulk exports a Group resource and resources in the member field, including related resources for each Patient member. The export for each Patient is identical to a GetPatientEverything request. Implements the FHIR implementation guide [$export group of patients](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---group-of-patients>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only resources of the specified type(s) are exported. * _since: if provided, only resources updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. * `organizeOutputBy`: resource type to organize the output by. Required and must be set to Patient. When specified, output files are organized by instances of the specified resource type, including the resource, referenced resources, and resources that contain references to that resource. On success, the Content-Location header of response is set to a URL that you can use to query the status of the export. The URL is in the format `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    _since: &Option<Option<String>>,
    _type: &Option<Option<String>>,
    organizeOutputBy: &Option<Option<String>>,
    outputFormat: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Group/{GroupId}/$export",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _since.as_ref() {
        query_parts.push(format!("_since={}", val));
    }
    if let Some(val) = _type.as_ref() {
        query_parts.push(format!("_type={}", val));
    }
    if let Some(val) = organizeOutputBy.as_ref() {
        query_parts.push(format!("organizeOutputBy={}", val));
    }
    if let Some(val) = outputFormat.as_ref() {
        query_parts.push(format!("outputFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Group/{GroupId}/$export
/// Bulk exports a Group resource and resources in the member field, including related resources for each Patient member. The export for each Patient is identical to a GetPatientEverything request. Implements the FHIR implementation guide [$export group of patients](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---group-of-patients>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only resources of the specified type(s) are exported. * _since: if provided, only resources updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. * `organizeOutputBy`: resource type to organize the output by. Required and must be set to Patient. When specified, output files are organized by instances of the specified resource type, including the resource, referenced resources, and resources that contain references to that resource. On success, the Content-Location header of response is set to a URL that you can use to query the status of the export. The URL is in the format `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_execute()` or `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Group/{GroupId}/$export
/// Bulk exports a Group resource and resources in the member field, including related resources for each Patient member. The export for each Patient is identical to a GetPatientEverything request. Implements the FHIR implementation guide [$export group of patients](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---group-of-patients>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only resources of the specified type(s) are exported. * _since: if provided, only resources updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. * `organizeOutputBy`: resource type to organize the output by. Required and must be set to Patient. When specified, output files are organized by instances of the specified resource type, including the resource, referenced resources, and resources that contain references to that resource. On success, the Content-Location header of response is set to a URL that you can use to query the status of the export. The URL is in the format `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_bulk_export_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: _since
    pub _since: Option<Option<String>>,
    /// Query parameter: _type
    pub _type: Option<Option<String>>,
    /// Query parameter: organizeOutputBy
    pub organizeOutputBy: Option<Option<String>>,
    /// Query parameter: outputFormat
    pub outputFormat: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Group/{GroupId}/$export
/// Bulk exports a Group resource and resources in the member field, including related resources for each Patient member. The export for each Patient is identical to a GetPatientEverything request. Implements the FHIR implementation guide [$export group of patients](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---group-of-patients>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only resources of the specified type(s) are exported. * _since: if provided, only resources updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. * `organizeOutputBy`: resource type to organize the output by. Required and must be set to Patient. When specified, output files are organized by instances of the specified resource type, including the resource, referenced resources, and resources that contain references to that resource. On success, the Content-Location header of response is set to a URL that you can use to query the status of the export. The URL is in the format `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder()` + `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_export_group(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder(
        client,
        &args.name,
        &args._since,
        &args._type,
        &args.organizeOutputBy,
        &args.outputFormat,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete
/// Bulk deletes the FHIR resources from the given FHIR store. This method returns an Operation that can be used to track the progress of the deletion by calling GetOperation. The success and secondary_success counters correspond to the deleted current version and historical versions, respectively.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_bulk_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete
/// Bulk deletes the FHIR resources from the given FHIR store. This method returns an Operation that can be used to track the progress of the deletion by calling GetOperation. The success and secondary_success counters correspond to the deleted current version and historical versions, respectively.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_execute()` or `healthcare_projects_locations_datasets_fhir_stores_bulk_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete
/// Bulk deletes the FHIR resources from the given FHIR store. This method returns an Operation that can be used to track the progress of the deletion by calling GetOperation. The success and secondary_success counters correspond to the deleted current version and historical versions, respectively.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_bulk_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete
/// Bulk deletes the FHIR resources from the given FHIR store. This method returns an Operation that can be used to track the progress of the deletion by calling GetOperation. The success and secondary_success counters correspond to the deleted current version and historical versions, respectively.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder()` + `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_bulk_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Creates a new FHIR store within the parent dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_create_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_create` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    fhirStoreId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fhirStoreId.as_ref() {
        query_parts.push(format!("fhirStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Creates a new FHIR store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_create_execute()` or `healthcare_projects_locations_datasets_fhir_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FhirStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Creates a new FHIR store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: fhirStoreId
    pub fhirStoreId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Creates a new FHIR store within the parent dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_create_builder()` + `healthcare_projects_locations_datasets_fhir_stores_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_create_builder(
        client,
        &args.parent,
        &args.fhirStoreId,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_create_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyFhirStoreSummary. If errors occur, error is set. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_deidentify_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_deidentify` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify_builder<R>(
    client: &SimpleHttpClient<R>,
    sourceStore: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify",
        sourceStore,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyFhirStoreSummary. If errors occur, error is set. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_deidentify_execute()` or `healthcare_projects_locations_datasets_fhir_stores_deidentify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyFhirStoreSummary. If errors occur, error is set. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_deidentify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_deidentify_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_deidentify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_deidentify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_deidentify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_deidentify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs {
    /// Path parameter: sourceStore
    pub sourceStore: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify
/// De-identifies data from the source store and writes it to the destination store. The metadata field type is OperationMetadata. If the request is successful, the response field type is DeidentifyFhirStoreSummary. If errors occur, error is set. Error details are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_deidentify_builder()` + `healthcare_projects_locations_datasets_fhir_stores_deidentify_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_deidentify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_deidentify_builder(
        client,
        &args.sourceStore,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_deidentify_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Deletes the specified FHIR store and removes all resources within it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_delete_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Deletes the specified FHIR store and removes all resources within it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_delete_execute()` or `healthcare_projects_locations_datasets_fhir_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Deletes the specified FHIR store and removes all resources within it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Deletes the specified FHIR store and removes all resources within it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_delete_builder()` + `healthcare_projects_locations_datasets_fhir_stores_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess
/// Explains all the `permitted/denied` actor, purpose and environment for a given resource. FHIR Consent is not supported in DSTU2 or R5.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_explain_data_access` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    resourceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = resourceId.as_ref() {
        query_parts.push(format!("resourceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess
/// Explains all the `permitted/denied` actor, purpose and environment for a given resource. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_execute()` or `healthcare_projects_locations_datasets_fhir_stores_explain_data_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExplainDataAccessResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExplainDataAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess
/// Explains all the `permitted/denied` actor, purpose and environment for a given resource. FHIR Consent is not supported in DSTU2 or R5.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExplainDataAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_explain_data_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: resourceId
    pub resourceId: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess
/// Explains all the `permitted/denied` actor, purpose and environment for a given resource. FHIR Consent is not supported in DSTU2 or R5.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder()` + `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExplainDataAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder(
        client,
        &args.name,
        &args.resourceId,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_explain_data_access_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export
/// Export resources from the FHIR store to the specified destination. This method returns an Operation that can be used to track the status of the export by calling GetOperation. To improve performance, it is recommended to make the type filter as specific as possible, including only the resource types that are absolutely needed. This minimizes the size of the initial dataset to be processed and is the most effective way to improve performance. While post-filters like _since are useful for refining results, they do not speed up the initial data retrieval phase, which is primarily governed by the type filter. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ExportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_export_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_export` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export
/// Export resources from the FHIR store to the specified destination. This method returns an Operation that can be used to track the status of the export by calling GetOperation. To improve performance, it is recommended to make the type filter as specific as possible, including only the resource types that are absolutely needed. This minimizes the size of the initial dataset to be processed and is the most effective way to improve performance. While post-filters like _since are useful for refining results, they do not speed up the initial data retrieval phase, which is primarily governed by the type filter. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ExportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_export_execute()` or `healthcare_projects_locations_datasets_fhir_stores_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export
/// Export resources from the FHIR store to the specified destination. This method returns an Operation that can be used to track the status of the export by calling GetOperation. To improve performance, it is recommended to make the type filter as specific as possible, including only the resource types that are absolutely needed. This minimizes the size of the initial dataset to be processed and is the most effective way to improve performance. While post-filters like _since are useful for refining results, they do not speed up the initial data retrieval phase, which is primarily governed by the type filter. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ExportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_export_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export
/// Export resources from the FHIR store to the specified destination. This method returns an Operation that can be used to track the status of the export by calling GetOperation. To improve performance, it is recommended to make the type filter as specific as possible, including only the resource types that are absolutely needed. This minimizes the size of the initial dataset to be processed and is the most effective way to improve performance. While post-filters like _since are useful for refining results, they do not speed up the initial data retrieval phase, which is primarily governed by the type filter. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ExportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_export_builder()` + `healthcare_projects_locations_datasets_fhir_stores_export_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_export(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_export_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Gets the configuration of the specified FHIR store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_get_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_get` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Gets the configuration of the specified FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_get_execute()` or `healthcare_projects_locations_datasets_fhir_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FhirStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Gets the configuration of the specified FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Gets the configuration of the specified FHIR store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_get_builder()` + `healthcare_projects_locations_datasets_fhir_stores_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics
/// Gets metrics associated with the FHIR store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics
/// Gets metrics associated with the FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_execute()` or `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStoreMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FhirStoreMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics
/// Gets metrics associated with the FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetFhirstoreMetricsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics
/// Gets metrics associated with the FHIR store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder()` + `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresGetFhirstoreMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_get_fhirstore_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import
/// Imports resources to the FHIR store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some FHIR store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty FHIR store that is not being used by other clients. In cases where this method is not appropriate, consider using ExecuteBundle to load data. Every resource in the input must contain a client-supplied ID. Each resource is stored using the supplied ID regardless of the enable_update_create setting on the FHIR store. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and Cloud P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The import process does not enforce referential integrity, regardless of the disable_referential_integrity setting on the FHIR store. This allows the import of resources with arbitrary interdependencies without considering grouping or ordering, but if the input data contains invalid references or if some resources fail to be imported, the FHIR store might be left in a state that violates referential integrity. The import process does not trigger P`ub/Sub` notification or BigQuery streaming update, regardless of how those are configured on the FHIR store. If a resource with the specified ID already exists, the most recent version of the resource is overwritten without creating a new historical version, regardless of the disable_resource_versioning setting on the FHIR store. If transient failures occur during the import, it's possible that successfully imported resources will be overwritten more than once. The import operation is idempotent unless the input data contains multiple valid resources with the same ID but different contents. In that case, after the import completes, the store contains exactly one resource with that ID but there is no ordering guarantee on which version of the contents it will have. The operation result counters do not count duplicate IDs as an error and count one success for each resource in the input, which might result in a success count larger than the number of resources in the FHIR store. This often occurs when importing data organized in bundles produced by Patient-everything where each bundle contains its own copy of a resource such as Practitioner that might be referred to by many patients. If some resources fail to import, for example due to parsing errors, successfully imported resources are not rolled back. The location and format of the input data is specified by the parameters in ImportResourcesRequest. Note that if no format is specified, this method assumes the BUNDLE format. When using the BUNDLE format this method ignores the Bundle.type field, except that history bundles are rejected, and does not apply any of the bundle processing semantics for batch or transaction bundles. Unlike in ExecuteBundle, transaction bundles are not executed as a single transaction and bundle-internal references are not rewritten. The bundle is treated as a collection of resources to be written as provided in Bundle.entry.resource, ignoring Bundle.entry.request. As an example, this allows the import of searchset bundles produced by a FHIR search or Patient-everything operation. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ImportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_import_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_import` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_import_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import
/// Imports resources to the FHIR store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some FHIR store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty FHIR store that is not being used by other clients. In cases where this method is not appropriate, consider using ExecuteBundle to load data. Every resource in the input must contain a client-supplied ID. Each resource is stored using the supplied ID regardless of the enable_update_create setting on the FHIR store. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and Cloud P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The import process does not enforce referential integrity, regardless of the disable_referential_integrity setting on the FHIR store. This allows the import of resources with arbitrary interdependencies without considering grouping or ordering, but if the input data contains invalid references or if some resources fail to be imported, the FHIR store might be left in a state that violates referential integrity. The import process does not trigger P`ub/Sub` notification or BigQuery streaming update, regardless of how those are configured on the FHIR store. If a resource with the specified ID already exists, the most recent version of the resource is overwritten without creating a new historical version, regardless of the disable_resource_versioning setting on the FHIR store. If transient failures occur during the import, it's possible that successfully imported resources will be overwritten more than once. The import operation is idempotent unless the input data contains multiple valid resources with the same ID but different contents. In that case, after the import completes, the store contains exactly one resource with that ID but there is no ordering guarantee on which version of the contents it will have. The operation result counters do not count duplicate IDs as an error and count one success for each resource in the input, which might result in a success count larger than the number of resources in the FHIR store. This often occurs when importing data organized in bundles produced by Patient-everything where each bundle contains its own copy of a resource such as Practitioner that might be referred to by many patients. If some resources fail to import, for example due to parsing errors, successfully imported resources are not rolled back. The location and format of the input data is specified by the parameters in ImportResourcesRequest. Note that if no format is specified, this method assumes the BUNDLE format. When using the BUNDLE format this method ignores the Bundle.type field, except that history bundles are rejected, and does not apply any of the bundle processing semantics for batch or transaction bundles. Unlike in ExecuteBundle, transaction bundles are not executed as a single transaction and bundle-internal references are not rewritten. The bundle is treated as a collection of resources to be written as provided in Bundle.entry.resource, ignoring Bundle.entry.request. As an example, this allows the import of searchset bundles produced by a FHIR search or Patient-everything operation. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ImportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_import_execute()` or `healthcare_projects_locations_datasets_fhir_stores_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import
/// Imports resources to the FHIR store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some FHIR store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty FHIR store that is not being used by other clients. In cases where this method is not appropriate, consider using ExecuteBundle to load data. Every resource in the input must contain a client-supplied ID. Each resource is stored using the supplied ID regardless of the enable_update_create setting on the FHIR store. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and Cloud P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The import process does not enforce referential integrity, regardless of the disable_referential_integrity setting on the FHIR store. This allows the import of resources with arbitrary interdependencies without considering grouping or ordering, but if the input data contains invalid references or if some resources fail to be imported, the FHIR store might be left in a state that violates referential integrity. The import process does not trigger P`ub/Sub` notification or BigQuery streaming update, regardless of how those are configured on the FHIR store. If a resource with the specified ID already exists, the most recent version of the resource is overwritten without creating a new historical version, regardless of the disable_resource_versioning setting on the FHIR store. If transient failures occur during the import, it's possible that successfully imported resources will be overwritten more than once. The import operation is idempotent unless the input data contains multiple valid resources with the same ID but different contents. In that case, after the import completes, the store contains exactly one resource with that ID but there is no ordering guarantee on which version of the contents it will have. The operation result counters do not count duplicate IDs as an error and count one success for each resource in the input, which might result in a success count larger than the number of resources in the FHIR store. This often occurs when importing data organized in bundles produced by Patient-everything where each bundle contains its own copy of a resource such as Practitioner that might be referred to by many patients. If some resources fail to import, for example due to parsing errors, successfully imported resources are not rolled back. The location and format of the input data is specified by the parameters in ImportResourcesRequest. Note that if no format is specified, this method assumes the BUNDLE format. When using the BUNDLE format this method ignores the Bundle.type field, except that history bundles are rejected, and does not apply any of the bundle processing semantics for batch or transaction bundles. Unlike in ExecuteBundle, transaction bundles are not executed as a single transaction and bundle-internal references are not rewritten. The bundle is treated as a collection of resources to be written as provided in Bundle.entry.resource, ignoring Bundle.entry.request. As an example, this allows the import of searchset bundles produced by a FHIR search or Patient-everything operation. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ImportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_import_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresImportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import
/// Imports resources to the FHIR store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some FHIR store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty FHIR store that is not being used by other clients. In cases where this method is not appropriate, consider using ExecuteBundle to load data. Every resource in the input must contain a client-supplied ID. Each resource is stored using the supplied ID regardless of the enable_update_create setting on the FHIR store. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and Cloud P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The import process does not enforce referential integrity, regardless of the disable_referential_integrity setting on the FHIR store. This allows the import of resources with arbitrary interdependencies without considering grouping or ordering, but if the input data contains invalid references or if some resources fail to be imported, the FHIR store might be left in a state that violates referential integrity. The import process does not trigger P`ub/Sub` notification or BigQuery streaming update, regardless of how those are configured on the FHIR store. If a resource with the specified ID already exists, the most recent version of the resource is overwritten without creating a new historical version, regardless of the disable_resource_versioning setting on the FHIR store. If transient failures occur during the import, it's possible that successfully imported resources will be overwritten more than once. The import operation is idempotent unless the input data contains multiple valid resources with the same ID but different contents. In that case, after the import completes, the store contains exactly one resource with that ID but there is no ordering guarantee on which version of the contents it will have. The operation result counters do not count duplicate IDs as an error and count one success for each resource in the input, which might result in a success count larger than the number of resources in the FHIR store. This often occurs when importing data organized in bundles produced by Patient-everything where each bundle contains its own copy of a resource such as Practitioner that might be referred to by many patients. If some resources fail to import, for example due to parsing errors, successfully imported resources are not rolled back. The location and format of the input data is specified by the parameters in ImportResourcesRequest. Note that if no format is specified, this method assumes the BUNDLE format. When using the BUNDLE format this method ignores the Bundle.type field, except that history bundles are rejected, and does not apply any of the bundle processing semantics for batch or transaction bundles. Unlike in ExecuteBundle, transaction bundles are not executed as a single transaction and bundle-internal references are not rewritten. The bundle is treated as a collection of resources to be written as provided in Bundle.entry.resource, ignoring Bundle.entry.request. As an example, this allows the import of searchset bundles produced by a FHIR search or Patient-everything operation. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type ImportResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_import_builder()` + `healthcare_projects_locations_datasets_fhir_stores_import_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_import(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_import_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Lists the FHIR stores in the given dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_list_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_list` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Lists the FHIR stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_list_execute()` or `healthcare_projects_locations_datasets_fhir_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFhirStoresResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFhirStoresResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Lists the FHIR stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFhirStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
/// Lists the FHIR stores in the given dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_list_builder()` + `healthcare_projects_locations_datasets_fhir_stores_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFhirStoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Updates the configuration of the specified FHIR store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_patch_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Updates the configuration of the specified FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_patch_execute()` or `healthcare_projects_locations_datasets_fhir_stores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FhirStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Updates the configuration of the specified FHIR store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
/// Updates the configuration of the specified FHIR store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_patch_builder()` + `healthcare_projects_locations_datasets_fhir_stores_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FhirStore>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback
/// Rolls back resources from the FHIR store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackFhirResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_rollback_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_rollback` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_rollback_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback
/// Rolls back resources from the FHIR store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackFhirResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_rollback_execute()` or `healthcare_projects_locations_datasets_fhir_stores_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback
/// Rolls back resources from the FHIR store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackFhirResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_rollback_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback
/// Rolls back resources from the FHIR store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackFhirResourcesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_rollback_builder()` + `healthcare_projects_locations_datasets_fhir_stores_rollback_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_rollback(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_rollback_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_rollback_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary
/// Creates a FHIR Binary resource. This method can be used to create a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is created with this method using the FHIR content type this method's behavior is the same as [fhir.create](<https://cloud.google.`com/healthcare-api/docs/reference/rest/v1/projects`.locations.datasets.`fhirStores`.`fhir/create`>). If a resource type other than Binary is used in the request it's treated in the same way as non-FHIR data (e.g., images, zip archives, pdf files, documents). When a non-FHIR content type is used in the request, a Binary resource will be generated, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 1 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 201 Created response is returned. The newly created resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary
/// Creates a FHIR Binary resource. This method can be used to create a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is created with this method using the FHIR content type this method's behavior is the same as [fhir.create](<https://cloud.google.`com/healthcare-api/docs/reference/rest/v1/projects`.locations.datasets.`fhirStores`.`fhir/create`>). If a resource type other than Binary is used in the request it's treated in the same way as non-FHIR data (e.g., images, zip archives, pdf files, documents). When a non-FHIR content type is used in the request, a Binary resource will be generated, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 1 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 201 Created response is returned. The newly created resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary
/// Creates a FHIR Binary resource. This method can be used to create a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is created with this method using the FHIR content type this method's behavior is the same as [fhir.create](<https://cloud.google.`com/healthcare-api/docs/reference/rest/v1/projects`.locations.datasets.`fhirStores`.`fhir/create`>). If a resource type other than Binary is used in the request it's treated in the same way as non-FHIR data (e.g., images, zip archives, pdf files, documents). When a non-FHIR content type is used in the request, a Binary resource will be generated, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 1 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 201 Created response is returned. The newly created resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary
/// Creates a FHIR Binary resource. This method can be used to create a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is created with this method using the FHIR content type this method's behavior is the same as [fhir.create](<https://cloud.google.`com/healthcare-api/docs/reference/rest/v1/projects`.locations.datasets.`fhirStores`.`fhir/create`>). If a resource type other than Binary is used in the request it's treated in the same way as non-FHIR data (e.g., images, zip archives, pdf files, documents). When a non-FHIR content type is used in the request, a Binary resource will be generated, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 1 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 201 Created response is returned. The newly created resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder(
        client,
        &args.parent,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Gets the contents of a FHIR Binary resource. This method can be used to retrieve a Binary resource either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Gets the contents of a FHIR Binary resource. This method can be used to retrieve a Binary resource either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Gets the contents of a FHIR Binary resource. This method can be used to retrieve a Binary resource either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Gets the contents of a FHIR Binary resource. This method can be used to retrieve a Binary resource either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Updates the entire contents of a Binary resource. If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. This method can be used to update a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is updated with this method using the FHIR content type this method's behavior is the same as update. If a resource type other than Binary is used in the request it will be treated in the same way as non-FHIR data. When a non-FHIR content type is used in the request, a Binary resource will be generated using the ID from the resource path, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 2 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 200 `OK` response will be returned, or a 201 Created if the resource did not exit. The resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Updates the entire contents of a Binary resource. If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. This method can be used to update a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is updated with this method using the FHIR content type this method's behavior is the same as update. If a resource type other than Binary is used in the request it will be treated in the same way as non-FHIR data. When a non-FHIR content type is used in the request, a Binary resource will be generated using the ID from the resource path, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 2 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 200 `OK` response will be returned, or a 201 Created if the resource did not exit. The resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Updates the entire contents of a Binary resource. If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. This method can be used to update a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is updated with this method using the FHIR content type this method's behavior is the same as update. If a resource type other than Binary is used in the request it will be treated in the same way as non-FHIR data. When a non-FHIR content type is used in the request, a Binary resource will be generated using the ID from the resource path, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 2 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 200 `OK` response will be returned, or a 201 Created if the resource did not exit. The resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}
/// Updates the entire contents of a Binary resource. If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. This method can be used to update a Binary resource either by using one of the accepted FHIR JSON content types, or as a raw data stream. If a resource is updated with this method using the FHIR content type this method's behavior is the same as update. If a resource type other than Binary is used in the request it will be treated in the same way as non-FHIR data. When a non-FHIR content type is used in the request, a Binary resource will be generated using the ID from the resource path, and the uploaded data will be stored in the content field (DSTU2 and STU3), or the data field (R4 and R5). The Binary resource's `contentType` will be filled in using the value of the Content-Type header, and the `securityContext` field (not present in DSTU2) will be populated from the X-Security-Context header if it exists. At this time `securityContext` has no special behavior in the Cloud Healthcare API. Note: the limit on data ingested through this method is 2 GB. For best performance, use a non-FHIR data type instead of wrapping the data in a Binary resource. Some of the Healthcare API features, such as [exporting to BigQuery](<https://cloud.google.`com/healthcare-api/docs/how-tos/fhir-export-bigquery`>) or [P`ub/Sub` notifications](<https://cloud.google.`com/healthcare-api/docs/fhir-pubsub`#behavior_when_a_fhir_resource_is_too_large_or_traffic_is_high>) with full resource content, do not support Binary resources that are larger than 10 MB. In these cases the resource's data field will be omitted. Instead, the "<http://hl7.`org/fhir/StructureDefinition/data-absent-reason`"> extension will be present to indicate that including the data is unsupported. On success, an empty 200 `OK` response will be returned, or a 201 Created if the resource did not exit. The resource's ID and version are returned in the Location header. Using Prefer: representation=resource is not allowed for this method. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR Binary resource by version ID. This method can be used to retrieve a Binary resource version either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource version was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource version's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}/_history/{_historyId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR Binary resource by version ID. This method can be used to retrieve a Binary resource version either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource version was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource version's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR Binary resource by version ID. This method can be used to retrieve a Binary resource version either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource version was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource version's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Binary/{BinaryId}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR Binary resource by version ID. This method can be used to retrieve a Binary resource version either by using the FHIR JSON mimetype as the value for the Accept header, or as a raw data stream. If the FHIR Accept type is used this method will return a Binary resource with the data base64-encoded, regardless of how the resource version was created. The resource data can be retrieved in base64-decoded form if the Accept type of the request matches the value of the resource version's `contentType` field. The definition of the Binary REST API can be found at <https://hl7.`org/fhir/binary`.html#rest.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Consent/{ConsentId}/$consent-enforcement-status
/// Returns the consent enforcement status of a single consent resource. On success, the response body contains a JSON-encoded representation of a Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resource, containing the current enforcement status. Does not support DSTU2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Consent/{ConsentId}/$consent-enforcement-status",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Consent/{ConsentId}/$consent-enforcement-status
/// Returns the consent enforcement status of a single consent resource. On success, the response body contains a JSON-encoded representation of a Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resource, containing the current enforcement status. Does not support DSTU2.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Consent/{ConsentId}/$consent-enforcement-status
/// Returns the consent enforcement status of a single consent resource. On success, the response body contains a JSON-encoded representation of a Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resource, containing the current enforcement status. Does not support DSTU2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Consent/{ConsentId}/$consent-enforcement-status
/// Returns the consent enforcement status of a single consent resource. On success, the response body contains a JSON-encoded representation of a Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resource, containing the current enforcement status. Does not support DSTU2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$consent-enforcement-status
/// Returns the consent enforcement status of all consent resources for a patient. On success, the response body contains a JSON-encoded representation of a bundle of Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resources, containing the current enforcement status for each consent resource of the patient. Does not support DSTU2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    _count: &Option<Option<String>>,
    _page_token: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$consent-enforcement-status",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _count.as_ref() {
        query_parts.push(format!("_count={}", val));
    }
    if let Some(val) = _page_token.as_ref() {
        query_parts.push(format!("_page_token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$consent-enforcement-status
/// Returns the consent enforcement status of all consent resources for a patient. On success, the response body contains a JSON-encoded representation of a bundle of Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resources, containing the current enforcement status for each consent resource of the patient. Does not support DSTU2.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$consent-enforcement-status
/// Returns the consent enforcement status of all consent resources for a patient. On success, the response body contains a JSON-encoded representation of a bundle of Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resources, containing the current enforcement status for each consent resource of the patient. Does not support DSTU2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: _count
    pub _count: Option<Option<String>>,
    /// Query parameter: _page_token
    pub _page_token: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$consent-enforcement-status
/// Returns the consent enforcement status of all consent resources for a patient. On success, the response body contains a JSON-encoded representation of a bundle of Parameters (<http://hl7.`org/fhir/parameters`.html>) FHIR resources, containing the current enforcement status for each consent resource of the patient. Does not support DSTU2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder(client, &args.name, &args._count, &args._page_token)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$everything
/// Retrieves a Patient resource and resources related to that patient. Implements the FHIR extended operation Patient-everything ([DSTU2](<https://hl7.`org/fhir/DSTU2/patient-operations`.html#everything>), [STU3](<https://hl7.`org/fhir/STU3/patient-operations`.html#everything>), [R4](<https://hl7.`org/fhir/R4/patient-operation-everything`.html>), [R5](<https://hl7.`org/fhir/R5/patient-operation-everything`.html>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the operation. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The resources in scope for the response are: * The patient resource itself. * All the resources directly referenced by the patient resource. * Resources directly referencing the patient resource that meet the inclusion criteria. The inclusion criteria are based on the membership rules in the patient compartment definition ([DSTU2](<http://hl7.`org/fhir/DSTU2/compartment-patient`.html>), [STU3](<http://www.hl7.`org/fhir/stu3/compartmentdefinition-patient`.html>), [R4](<http://hl7.`org/fhir/R4/compartmentdefinition-patient`.html>), [R5](<http://hl7.`org/fhir/R5/compartmentdefinition-patient`.html>)), which details the eligible resource types and referencing search parameters. For samples that show how to call Patient-everything, see [Getting all patient compartment resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_all_patient_compartment_resources>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    _count: &Option<Option<String>>,
    _page_token: &Option<Option<String>>,
    _since: &Option<Option<String>>,
    _type: &Option<Option<String>>,
    end: &Option<Option<String>>,
    start: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$everything",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _count.as_ref() {
        query_parts.push(format!("_count={}", val));
    }
    if let Some(val) = _page_token.as_ref() {
        query_parts.push(format!("_page_token={}", val));
    }
    if let Some(val) = _since.as_ref() {
        query_parts.push(format!("_since={}", val));
    }
    if let Some(val) = _type.as_ref() {
        query_parts.push(format!("_type={}", val));
    }
    if let Some(val) = end.as_ref() {
        query_parts.push(format!("end={}", val));
    }
    if let Some(val) = start.as_ref() {
        query_parts.push(format!("start={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$everything
/// Retrieves a Patient resource and resources related to that patient. Implements the FHIR extended operation Patient-everything ([DSTU2](<https://hl7.`org/fhir/DSTU2/patient-operations`.html#everything>), [STU3](<https://hl7.`org/fhir/STU3/patient-operations`.html#everything>), [R4](<https://hl7.`org/fhir/R4/patient-operation-everything`.html>), [R5](<https://hl7.`org/fhir/R5/patient-operation-everything`.html>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the operation. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The resources in scope for the response are: * The patient resource itself. * All the resources directly referenced by the patient resource. * Resources directly referencing the patient resource that meet the inclusion criteria. The inclusion criteria are based on the membership rules in the patient compartment definition ([DSTU2](<http://hl7.`org/fhir/DSTU2/compartment-patient`.html>), [STU3](<http://www.hl7.`org/fhir/stu3/compartmentdefinition-patient`.html>), [R4](<http://hl7.`org/fhir/R4/compartmentdefinition-patient`.html>), [R5](<http://hl7.`org/fhir/R5/compartmentdefinition-patient`.html>)), which details the eligible resource types and referencing search parameters. For samples that show how to call Patient-everything, see [Getting all patient compartment resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_all_patient_compartment_resources>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$everything
/// Retrieves a Patient resource and resources related to that patient. Implements the FHIR extended operation Patient-everything ([DSTU2](<https://hl7.`org/fhir/DSTU2/patient-operations`.html#everything>), [STU3](<https://hl7.`org/fhir/STU3/patient-operations`.html#everything>), [R4](<https://hl7.`org/fhir/R4/patient-operation-everything`.html>), [R5](<https://hl7.`org/fhir/R5/patient-operation-everything`.html>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the operation. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The resources in scope for the response are: * The patient resource itself. * All the resources directly referenced by the patient resource. * Resources directly referencing the patient resource that meet the inclusion criteria. The inclusion criteria are based on the membership rules in the patient compartment definition ([DSTU2](<http://hl7.`org/fhir/DSTU2/compartment-patient`.html>), [STU3](<http://www.hl7.`org/fhir/stu3/compartmentdefinition-patient`.html>), [R4](<http://hl7.`org/fhir/R4/compartmentdefinition-patient`.html>), [R5](<http://hl7.`org/fhir/R5/compartmentdefinition-patient`.html>)), which details the eligible resource types and referencing search parameters. For samples that show how to call Patient-everything, see [Getting all patient compartment resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_all_patient_compartment_resources>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: _count
    pub _count: Option<Option<String>>,
    /// Query parameter: _page_token
    pub _page_token: Option<Option<String>>,
    /// Query parameter: _since
    pub _since: Option<Option<String>>,
    /// Query parameter: _type
    pub _type: Option<Option<String>>,
    /// Query parameter: end
    pub end: Option<Option<String>>,
    /// Query parameter: start
    pub start: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/Patient/{PatientId}/$everything
/// Retrieves a Patient resource and resources related to that patient. Implements the FHIR extended operation Patient-everything ([DSTU2](<https://hl7.`org/fhir/DSTU2/patient-operations`.html#everything>), [STU3](<https://hl7.`org/fhir/STU3/patient-operations`.html#everything>), [R4](<https://hl7.`org/fhir/R4/patient-operation-everything`.html>), [R5](<https://hl7.`org/fhir/R5/patient-operation-everything`.html>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the operation. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The resources in scope for the response are: * The patient resource itself. * All the resources directly referenced by the patient resource. * Resources directly referencing the patient resource that meet the inclusion criteria. The inclusion criteria are based on the membership rules in the patient compartment definition ([DSTU2](<http://hl7.`org/fhir/DSTU2/compartment-patient`.html>), [STU3](<http://www.hl7.`org/fhir/stu3/compartmentdefinition-patient`.html>), [R4](<http://hl7.`org/fhir/R4/compartmentdefinition-patient`.html>), [R5](<http://hl7.`org/fhir/R5/compartmentdefinition-patient`.html>)), which details the eligible resource types and referencing search parameters. For samples that show how to call Patient-everything, see [Getting all patient compartment resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_all_patient_compartment_resources>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder(
            client,
            &args.name,
            &args._count,
            &args._page_token,
            &args._since,
            &args._type,
            &args.end,
            &args.start,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/$purge
/// Deletes all the historical versions of a resource (excluding the current version) from the FHIR store. To remove all versions of a resource, first delete the current version and then call this method. This is not a FHIR standard operation. For samples that show how to call Resource-purge, see [Deleting historical versions of a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_historical_versions_of_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/$purge",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/$purge
/// Deletes all the historical versions of a resource (excluding the current version) from the FHIR store. To remove all versions of a resource, first delete the current version and then call this method. This is not a FHIR standard operation. For samples that show how to call Resource-purge, see [Deleting historical versions of a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_historical_versions_of_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/$purge
/// Deletes all the historical versions of a resource (excluding the current version) from the FHIR store. To remove all versions of a resource, first delete the current version and then call this method. This is not a FHIR standard operation. For samples that show how to call Resource-purge, see [Deleting historical versions of a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_historical_versions_of_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/$purge
/// Deletes all the historical versions of a resource (excluding the current version) from the FHIR store. To remove all versions of a resource, first delete the current version and then call this method. This is not a FHIR standard operation. For samples that show how to call Resource-purge, see [Deleting historical versions of a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_historical_versions_of_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/$validate
/// Validates an input FHIR resource's conformance to its profiles and the profiles configured on the FHIR store. Implements the FHIR extended operation $validate ([DSTU2](<https://hl7.`org/fhir/DSTU2/resource-operations`.html#validate>), [STU3](<https://hl7.`org/fhir/STU3/resource-operations`.html#validate>), [R4](<https://hl7.`org/fhir/R4/resource-operation-validate`.html>). or [R5](<https://hl7.`org/fhir/R5/resource-operation-validate`.html>)). The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The Parameters input syntax is not supported. The profile query parameter can be used to request that the resource only be validated against a specific profile. If a profile with the given URL cannot be found in the FHIR store then an error is returned. Errors generated by validation contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    type_rs: &String,
    profile: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/$validate",
        parent,
        type_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = profile.as_ref() {
        query_parts.push(format!("profile={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/$validate
/// Validates an input FHIR resource's conformance to its profiles and the profiles configured on the FHIR store. Implements the FHIR extended operation $validate ([DSTU2](<https://hl7.`org/fhir/DSTU2/resource-operations`.html#validate>), [STU3](<https://hl7.`org/fhir/STU3/resource-operations`.html#validate>), [R4](<https://hl7.`org/fhir/R4/resource-operation-validate`.html>). or [R5](<https://hl7.`org/fhir/R5/resource-operation-validate`.html>)). The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The Parameters input syntax is not supported. The profile query parameter can be used to request that the resource only be validated against a specific profile. If a profile with the given URL cannot be found in the FHIR store then an error is returned. Errors generated by validation contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/$validate
/// Validates an input FHIR resource's conformance to its profiles and the profiles configured on the FHIR store. Implements the FHIR extended operation $validate ([DSTU2](<https://hl7.`org/fhir/DSTU2/resource-operations`.html#validate>), [STU3](<https://hl7.`org/fhir/STU3/resource-operations`.html#validate>), [R4](<https://hl7.`org/fhir/R4/resource-operation-validate`.html>). or [R5](<https://hl7.`org/fhir/R5/resource-operation-validate`.html>)). The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The Parameters input syntax is not supported. The profile query parameter can be used to request that the resource only be validated against a specific profile. If a profile with the given URL cannot be found in the FHIR store then an error is returned. Errors generated by validation contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: type
    pub type_rs: String,
    /// Query parameter: profile
    pub profile: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/$validate
/// Validates an input FHIR resource's conformance to its profiles and the profiles configured on the FHIR store. Implements the FHIR extended operation $validate ([DSTU2](<https://hl7.`org/fhir/DSTU2/resource-operations`.html#validate>), [STU3](<https://hl7.`org/fhir/STU3/resource-operations`.html#validate>), [R4](<https://hl7.`org/fhir/R4/resource-operation-validate`.html>). or [R5](<https://hl7.`org/fhir/R5/resource-operation-validate`.html>)). The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The Parameters input syntax is not supported. The profile query parameter can be used to request that the resource only be validated against a specific profile. If a profile with the given URL cannot be found in the FHIR store then an error is returned. Errors generated by validation contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder(
            client,
            &args.parent,
            &args.type_rs,
            &args.profile,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/$export
/// Bulk exports all resources from the FHIR store to the specified destination. Implements the FHIR implementation guide [system level $export](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---system-level-export>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only the resources of the specified type(s) are exported. * _since: if provided, only the resources that are updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. On success, the Content-Location header of the response is set to a URL that the user can use to query the status of the export. The URL is in the format: `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    _since: &Option<Option<String>>,
    _type: &Option<Option<String>>,
    outputFormat: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/$export",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _since.as_ref() {
        query_parts.push(format!("_since={}", val));
    }
    if let Some(val) = _type.as_ref() {
        query_parts.push(format!("_type={}", val));
    }
    if let Some(val) = outputFormat.as_ref() {
        query_parts.push(format!("outputFormat={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/$export
/// Bulk exports all resources from the FHIR store to the specified destination. Implements the FHIR implementation guide [system level $export](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---system-level-export>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only the resources of the specified type(s) are exported. * _since: if provided, only the resources that are updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. On success, the Content-Location header of the response is set to a URL that the user can use to query the status of the export. The URL is in the format: `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/$export
/// Bulk exports all resources from the FHIR store to the specified destination. Implements the FHIR implementation guide [system level $export](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---system-level-export>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only the resources of the specified type(s) are exported. * _since: if provided, only the resources that are updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. On success, the Content-Location header of the response is set to a URL that the user can use to query the status of the export. The URL is in the format: `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: _since
    pub _since: Option<Option<String>>,
    /// Query parameter: _type
    pub _type: Option<Option<String>>,
    /// Query parameter: outputFormat
    pub outputFormat: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/$export
/// Bulk exports all resources from the FHIR store to the specified destination. Implements the FHIR implementation guide [system level $export](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#endpoint---system-level-export>). The following headers must be set in the request: * Accept: specifies the format of the OperationOutcome response. Only `application/fhir`+json is supported. * Prefer: specifies whether the response is immediate or asynchronous. Must be to respond-async because only asynchronous responses are supported. Specify the destination for the server to write result files by setting the Cloud Storage location bulk_export_gcs_destination on the FHIR store. URI of an existing Cloud Storage directory where the server writes result files, in the format gs://{bucket-id}/{`path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced. Supports the following query parameters: * _type: string of comma-delimited FHIR resource types. If provided, only the resources of the specified type(s) are exported. * _since: if provided, only the resources that are updated after the specified time are exported. * _outputFormat: optional, specify ndjson to export data in NDJSON format. Exported file names use the format: {export_id}_{resource_type}.ndjson. On success, the Content-Location header of the response is set to a URL that the user can use to query the status of the export. The URL is in the format: `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/`fhirStores`/{fhir_store_id}/operations/{export_id}`. See get-fhir-operation-status for more information. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder(
        client,
        &args.name,
        &args._since,
        &args._type,
        &args.outputFormat,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/metadata
/// Gets the FHIR capability statement ([STU3](<https://hl7.`org/fhir/STU3/capabilitystatement`.html>), [R4](<https://hl7.`org/fhir/R4/capabilitystatement`.html>), [R5](<https://hl7.`org/fhir/R5/capabilitystatement`.html>)), or the [conformance statement](<https://hl7.`org/fhir/DSTU2/conformance`.html>) in the DSTU2 case for the store, which contains a description of functionality supported by the server. Implements the FHIR standard capabilities interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#capabilities>), [R4](<https://hl7.`org/fhir/R4/http`.html#capabilities>), [R5](<https://hl7.`org/fhir/R5/http`.html#capabilities>)), or the [conformance interaction](<https://hl7.`org/fhir/DSTU2/http`.html#conformance>) in the DSTU2 case. On success, the response body contains a JSON-encoded representation of a CapabilityStatement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/metadata",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/metadata
/// Gets the FHIR capability statement ([STU3](<https://hl7.`org/fhir/STU3/capabilitystatement`.html>), [R4](<https://hl7.`org/fhir/R4/capabilitystatement`.html>), [R5](<https://hl7.`org/fhir/R5/capabilitystatement`.html>)), or the [conformance statement](<https://hl7.`org/fhir/DSTU2/conformance`.html>) in the DSTU2 case for the store, which contains a description of functionality supported by the server. Implements the FHIR standard capabilities interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#capabilities>), [R4](<https://hl7.`org/fhir/R4/http`.html#capabilities>), [R5](<https://hl7.`org/fhir/R5/http`.html#capabilities>)), or the [conformance interaction](<https://hl7.`org/fhir/DSTU2/http`.html#conformance>) in the DSTU2 case. On success, the response body contains a JSON-encoded representation of a CapabilityStatement resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/metadata
/// Gets the FHIR capability statement ([STU3](<https://hl7.`org/fhir/STU3/capabilitystatement`.html>), [R4](<https://hl7.`org/fhir/R4/capabilitystatement`.html>), [R5](<https://hl7.`org/fhir/R5/capabilitystatement`.html>)), or the [conformance statement](<https://hl7.`org/fhir/DSTU2/conformance`.html>) in the DSTU2 case for the store, which contains a description of functionality supported by the server. Implements the FHIR standard capabilities interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#capabilities>), [R4](<https://hl7.`org/fhir/R4/http`.html#capabilities>), [R5](<https://hl7.`org/fhir/R5/http`.html#capabilities>)), or the [conformance interaction](<https://hl7.`org/fhir/DSTU2/http`.html#conformance>) in the DSTU2 case. On success, the response body contains a JSON-encoded representation of a CapabilityStatement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/metadata
/// Gets the FHIR capability statement ([STU3](<https://hl7.`org/fhir/STU3/capabilitystatement`.html>), [R4](<https://hl7.`org/fhir/R4/capabilitystatement`.html>), [R5](<https://hl7.`org/fhir/R5/capabilitystatement`.html>)), or the [conformance statement](<https://hl7.`org/fhir/DSTU2/conformance`.html>) in the DSTU2 case for the store, which contains a description of functionality supported by the server. Implements the FHIR standard capabilities interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#capabilities>), [R4](<https://hl7.`org/fhir/R4/http`.html#capabilities>), [R5](<https://hl7.`org/fhir/R5/http`.html#capabilities>)), or the [conformance interaction](<https://hl7.`org/fhir/DSTU2/http`.html#conformance>) in the DSTU2 case. On success, the response body contains a JSON-encoded representation of a CapabilityStatement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Deletes a FHIR resource that match an identifier search query. Implements the FHIR standard conditional delete interaction, limited to searching by resource identifier. If multiple resources match, 412 Precondition Failed error will be returned. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resource is moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call `conditionalDelete`, see [Conditionally deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_deleting_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    type_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}",
        parent,
        type_rs,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Deletes a FHIR resource that match an identifier search query. Implements the FHIR standard conditional delete interaction, limited to searching by resource identifier. If multiple resources match, 412 Precondition Failed error will be returned. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resource is moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call `conditionalDelete`, see [Conditionally deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_deleting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Deletes a FHIR resource that match an identifier search query. Implements the FHIR standard conditional delete interaction, limited to searching by resource identifier. If multiple resources match, 412 Precondition Failed error will be returned. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resource is moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call `conditionalDelete`, see [Conditionally deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_deleting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: type
    pub type_rs: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Deletes a FHIR resource that match an identifier search query. Implements the FHIR standard conditional delete interaction, limited to searching by resource identifier. If multiple resources match, 412 Precondition Failed error will be returned. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resource is moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call `conditionalDelete`, see [Conditionally deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_deleting_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder(
            client,
            &args.parent,
            &args.type_rs,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates part of that resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard conditional patch interaction, limited to searching by resource identifier. DSTU2 doesn't define a conditional patch method, but the server supports it in the same way it supports STU3. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria doesn't identify any matches, the request returns a 404 Not Found error. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalPatch`, see [Conditionally patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_patching_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    type_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}",
        parent,
        type_rs,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates part of that resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard conditional patch interaction, limited to searching by resource identifier. DSTU2 doesn't define a conditional patch method, but the server supports it in the same way it supports STU3. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria doesn't identify any matches, the request returns a 404 Not Found error. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalPatch`, see [Conditionally patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_patching_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates part of that resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard conditional patch interaction, limited to searching by resource identifier. DSTU2 doesn't define a conditional patch method, but the server supports it in the same way it supports STU3. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria doesn't identify any matches, the request returns a 404 Not Found error. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalPatch`, see [Conditionally patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_patching_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: type
    pub type_rs: String,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates part of that resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard conditional patch interaction, limited to searching by resource identifier. DSTU2 doesn't define a conditional patch method, but the server supports it in the same way it supports STU3. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria doesn't identify any matches, the request returns a 404 Not Found error. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalPatch`, see [Conditionally patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_patching_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder(
            client,
            &args.parent,
            &args.type_rs,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates the entire contents of that resource. Implements the FHIR standard conditional update interaction, limited to searching by resource identifier. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria identify zero matches, and the supplied resource body contains an id, and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. If the search criteria identify zero matches, and the supplied resource body does not contain an id, the resource is created with a server-assigned ID as per the create method. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalUpdate`, see [Conditionally updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_updating_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    type_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}",
        parent,
        type_rs,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates the entire contents of that resource. Implements the FHIR standard conditional update interaction, limited to searching by resource identifier. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria identify zero matches, and the supplied resource body contains an id, and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. If the search criteria identify zero matches, and the supplied resource body does not contain an id, the resource is created with a server-assigned ID as per the create method. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalUpdate`, see [Conditionally updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_updating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates the entire contents of that resource. Implements the FHIR standard conditional update interaction, limited to searching by resource identifier. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria identify zero matches, and the supplied resource body contains an id, and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. If the search criteria identify zero matches, and the supplied resource body does not contain an id, the resource is created with a server-assigned ID as per the create method. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalUpdate`, see [Conditionally updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_updating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: type
    pub type_rs: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// If a resource is found with the identifier specified in the query parameters, updates the entire contents of that resource. Implements the FHIR standard conditional update interaction, limited to searching by resource identifier. Search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. If the search criteria identify more than one match, the request returns a 412 Precondition Failed error. If the search criteria identify zero matches, and the supplied resource body contains an id, and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. If the search criteria identify zero matches, and the supplied resource body does not contain an id, the resource is created with a server-assigned ID as per the create method. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call `conditionalUpdate`, see [Conditionally updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#conditionally_updating_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder(
            client,
            &args.parent,
            &args.type_rs,
        )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Creates a FHIR resource. Implements the FHIR standard create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#create>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#create>), [R4](<https://hl7.`org/fhir/R4/http`.html#create>), [R5](<https://hl7.`org/fhir/R5/http`.html#create>)), which creates a new resource with a server-assigned resource ID. Also supports the FHIR standard conditional create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#ccreate>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#ccreate>), [R4](<https://hl7.`org/fhir/R4/http`.html#ccreate>), [R5](<https://hl7.`org/fhir/R5/http`.html#ccreate>)), specified by supplying an If-None-Exist header containing a FHIR search query, limited to searching by resource identifier. If no resources match this search query, the server processes the create operation as normal. When using conditional create, the search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the resource as it was created on the server, including the server-assigned resource ID and version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call create, see [Creating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#creating_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_create` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    type_rs: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}",
        parent,
        type_rs,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Creates a FHIR resource. Implements the FHIR standard create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#create>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#create>), [R4](<https://hl7.`org/fhir/R4/http`.html#create>), [R5](<https://hl7.`org/fhir/R5/http`.html#create>)), which creates a new resource with a server-assigned resource ID. Also supports the FHIR standard conditional create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#ccreate>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#ccreate>), [R4](<https://hl7.`org/fhir/R4/http`.html#ccreate>), [R5](<https://hl7.`org/fhir/R5/http`.html#ccreate>)), specified by supplying an If-None-Exist header containing a FHIR search query, limited to searching by resource identifier. If no resources match this search query, the server processes the create operation as normal. When using conditional create, the search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the resource as it was created on the server, including the server-assigned resource ID and version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call create, see [Creating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#creating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Creates a FHIR resource. Implements the FHIR standard create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#create>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#create>), [R4](<https://hl7.`org/fhir/R4/http`.html#create>), [R5](<https://hl7.`org/fhir/R5/http`.html#create>)), which creates a new resource with a server-assigned resource ID. Also supports the FHIR standard conditional create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#ccreate>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#ccreate>), [R4](<https://hl7.`org/fhir/R4/http`.html#ccreate>), [R5](<https://hl7.`org/fhir/R5/http`.html#ccreate>)), specified by supplying an If-None-Exist header containing a FHIR search query, limited to searching by resource identifier. If no resources match this search query, the server processes the create operation as normal. When using conditional create, the search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the resource as it was created on the server, including the server-assigned resource ID and version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call create, see [Creating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#creating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: type
    pub type_rs: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}
/// Creates a FHIR resource. Implements the FHIR standard create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#create>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#create>), [R4](<https://hl7.`org/fhir/R4/http`.html#create>), [R5](<https://hl7.`org/fhir/R5/http`.html#create>)), which creates a new resource with a server-assigned resource ID. Also supports the FHIR standard conditional create interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#ccreate>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#ccreate>), [R4](<https://hl7.`org/fhir/R4/http`.html#ccreate>), [R5](<https://hl7.`org/fhir/R5/http`.html#ccreate>)), specified by supplying an If-None-Exist header containing a FHIR search query, limited to searching by resource identifier. If no resources match this search query, the server processes the create operation as normal. When using conditional create, the search term for identifier should be in the pattern identifier=system|value or identifier=value - similar to the search method on resources with a specific identifier. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. On success, the response body contains a JSON-encoded representation of the resource as it was created on the server, including the server-assigned resource ID and version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call create, see [Creating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#creating_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder(
        client,
        &args.parent,
        &args.type_rs,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Deletes a FHIR resource. Implements the FHIR standard delete interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#delete>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#delete>), [R4](<https://hl7.`org/fhir/R4/http`.html#delete>), [R5](<https://hl7.`org/fhir/R5/http`.html#delete>)). Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resources will be moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call delete, see [Deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Deletes a FHIR resource. Implements the FHIR standard delete interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#delete>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#delete>), [R4](<https://hl7.`org/fhir/R4/http`.html#delete>), [R5](<https://hl7.`org/fhir/R5/http`.html#delete>)). Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resources will be moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call delete, see [Deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Deletes a FHIR resource. Implements the FHIR standard delete interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#delete>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#delete>), [R4](<https://hl7.`org/fhir/R4/http`.html#delete>), [R5](<https://hl7.`org/fhir/R5/http`.html#delete>)). Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resources will be moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call delete, see [Deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Deletes a FHIR resource. Implements the FHIR standard delete interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#delete>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#delete>), [R4](<https://hl7.`org/fhir/R4/http`.html#delete>), [R5](<https://hl7.`org/fhir/R5/http`.html#delete>)). Note: Unless resource versioning is disabled by setting the disable_resource_versioning flag on the FHIR store, the deleted resources will be moved to a history repository that can still be retrieved through vread and related methods, unless they are removed by the purge method. For samples that show how to call delete, see [Deleting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#deleting_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir
/// Executes all the requests in the given Bundle. Implements the FHIR standard `batch/transaction` interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#transaction>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#transaction>), [R4](<https://hl7.`org/fhir/R4/http`.html#transaction>), [R5](<https://hl7.`org/fhir/R5/http`.html#transaction>)). Supports all interactions within a bundle, except search. This method accepts Bundles of type batch and transaction, processing them according to the batch processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.1>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.1>), [R4](<https://hl7.`org/fhir/R4/http`.html#brules>), [R5](<https://hl7.`org/fhir/R5/http`.html#brules>)) and transaction processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.2>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.2>), [R4](<https://hl7.`org/fhir/R4/http`.html#trules>), [R5](<https://hl7.`org/fhir/R5/http`.html#trules>)). The request body must contain a JSON-encoded FHIR Bundle resource, and the request headers must contain Content-Type: `application/fhir`+json. For a batch bundle or a successful transaction, the response body contains a JSON-encoded representation of a Bundle resource of type batch-response or transaction-response containing one entry for each entry in the request, with the outcome of processing the entry. In the case of an error for a transaction bundle, the response body contains a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. This method checks permissions for each request in the bundle. The `executeBundle` permission is required to call this method, but you must also grant sufficient permissions to execute the individual requests in the bundle. For example, if the bundle contains a request to create a FHIR resource, the caller must also have been granted the healthcare.`fhirResources`.create permission. You can use audit logs to view the permissions for `executeBundle` and each request in the bundle. For more information, see [Viewing Cloud Audit logs](<https://cloud.google.`com/healthcare-api/docs/how-tos/audit-logging`>). For samples that show how to call `executeBundle`, see [Managing FHIR resources using FHIR bundles](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-bundles`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir
/// Executes all the requests in the given Bundle. Implements the FHIR standard `batch/transaction` interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#transaction>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#transaction>), [R4](<https://hl7.`org/fhir/R4/http`.html#transaction>), [R5](<https://hl7.`org/fhir/R5/http`.html#transaction>)). Supports all interactions within a bundle, except search. This method accepts Bundles of type batch and transaction, processing them according to the batch processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.1>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.1>), [R4](<https://hl7.`org/fhir/R4/http`.html#brules>), [R5](<https://hl7.`org/fhir/R5/http`.html#brules>)) and transaction processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.2>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.2>), [R4](<https://hl7.`org/fhir/R4/http`.html#trules>), [R5](<https://hl7.`org/fhir/R5/http`.html#trules>)). The request body must contain a JSON-encoded FHIR Bundle resource, and the request headers must contain Content-Type: `application/fhir`+json. For a batch bundle or a successful transaction, the response body contains a JSON-encoded representation of a Bundle resource of type batch-response or transaction-response containing one entry for each entry in the request, with the outcome of processing the entry. In the case of an error for a transaction bundle, the response body contains a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. This method checks permissions for each request in the bundle. The `executeBundle` permission is required to call this method, but you must also grant sufficient permissions to execute the individual requests in the bundle. For example, if the bundle contains a request to create a FHIR resource, the caller must also have been granted the healthcare.`fhirResources`.create permission. You can use audit logs to view the permissions for `executeBundle` and each request in the bundle. For more information, see [Viewing Cloud Audit logs](<https://cloud.google.`com/healthcare-api/docs/how-tos/audit-logging`>). For samples that show how to call `executeBundle`, see [Managing FHIR resources using FHIR bundles](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-bundles`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir
/// Executes all the requests in the given Bundle. Implements the FHIR standard `batch/transaction` interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#transaction>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#transaction>), [R4](<https://hl7.`org/fhir/R4/http`.html#transaction>), [R5](<https://hl7.`org/fhir/R5/http`.html#transaction>)). Supports all interactions within a bundle, except search. This method accepts Bundles of type batch and transaction, processing them according to the batch processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.1>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.1>), [R4](<https://hl7.`org/fhir/R4/http`.html#brules>), [R5](<https://hl7.`org/fhir/R5/http`.html#brules>)) and transaction processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.2>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.2>), [R4](<https://hl7.`org/fhir/R4/http`.html#trules>), [R5](<https://hl7.`org/fhir/R5/http`.html#trules>)). The request body must contain a JSON-encoded FHIR Bundle resource, and the request headers must contain Content-Type: `application/fhir`+json. For a batch bundle or a successful transaction, the response body contains a JSON-encoded representation of a Bundle resource of type batch-response or transaction-response containing one entry for each entry in the request, with the outcome of processing the entry. In the case of an error for a transaction bundle, the response body contains a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. This method checks permissions for each request in the bundle. The `executeBundle` permission is required to call this method, but you must also grant sufficient permissions to execute the individual requests in the bundle. For example, if the bundle contains a request to create a FHIR resource, the caller must also have been granted the healthcare.`fhirResources`.create permission. You can use audit logs to view the permissions for `executeBundle` and each request in the bundle. For more information, see [Viewing Cloud Audit logs](<https://cloud.google.`com/healthcare-api/docs/how-tos/audit-logging`>). For samples that show how to call `executeBundle`, see [Managing FHIR resources using FHIR bundles](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-bundles`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir
/// Executes all the requests in the given Bundle. Implements the FHIR standard `batch/transaction` interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#transaction>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#transaction>), [R4](<https://hl7.`org/fhir/R4/http`.html#transaction>), [R5](<https://hl7.`org/fhir/R5/http`.html#transaction>)). Supports all interactions within a bundle, except search. This method accepts Bundles of type batch and transaction, processing them according to the batch processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.1>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.1>), [R4](<https://hl7.`org/fhir/R4/http`.html#brules>), [R5](<https://hl7.`org/fhir/R5/http`.html#brules>)) and transaction processing rules ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#2.1.0.16.2>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#2.21.0.17.2>), [R4](<https://hl7.`org/fhir/R4/http`.html#trules>), [R5](<https://hl7.`org/fhir/R5/http`.html#trules>)). The request body must contain a JSON-encoded FHIR Bundle resource, and the request headers must contain Content-Type: `application/fhir`+json. For a batch bundle or a successful transaction, the response body contains a JSON-encoded representation of a Bundle resource of type batch-response or transaction-response containing one entry for each entry in the request, with the outcome of processing the entry. In the case of an error for a transaction bundle, the response body contains a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. This method checks permissions for each request in the bundle. The `executeBundle` permission is required to call this method, but you must also grant sufficient permissions to execute the individual requests in the bundle. For example, if the bundle contains a request to create a FHIR resource, the caller must also have been granted the healthcare.`fhirResources`.create permission. You can use audit logs to view the permissions for `executeBundle` and each request in the bundle. For more information, see [Viewing Cloud Audit logs](<https://cloud.google.`com/healthcare-api/docs/how-tos/audit-logging`>). For samples that show how to call `executeBundle`, see [Managing FHIR resources using FHIR bundles](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-bundles`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder(
        client,
        &args.parent,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history
/// Lists all the versions of a resource (including the current version and deleted versions) from the FHIR store. Implements the per-resource form of the FHIR standard history interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#history>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#history>), [R4](<https://hl7.`org/fhir/R4/http`.html#history>), [R5](<https://hl7.`org/fhir/R5/http`.html#history>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type history, containing the version history sorted from most recent to oldest versions. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call history, see [Listing FHIR resource versions](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#listing_fhir_resource_versions>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_history` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    _at: &Option<Option<String>>,
    _count: &Option<Option<String>>,
    _page_token: &Option<Option<String>>,
    _since: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _at.as_ref() {
        query_parts.push(format!("_at={}", val));
    }
    if let Some(val) = _count.as_ref() {
        query_parts.push(format!("_count={}", val));
    }
    if let Some(val) = _page_token.as_ref() {
        query_parts.push(format!("_page_token={}", val));
    }
    if let Some(val) = _since.as_ref() {
        query_parts.push(format!("_since={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history
/// Lists all the versions of a resource (including the current version and deleted versions) from the FHIR store. Implements the per-resource form of the FHIR standard history interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#history>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#history>), [R4](<https://hl7.`org/fhir/R4/http`.html#history>), [R5](<https://hl7.`org/fhir/R5/http`.html#history>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type history, containing the version history sorted from most recent to oldest versions. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call history, see [Listing FHIR resource versions](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#listing_fhir_resource_versions>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_history`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_history_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history
/// Lists all the versions of a resource (including the current version and deleted versions) from the FHIR store. Implements the per-resource form of the FHIR standard history interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#history>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#history>), [R4](<https://hl7.`org/fhir/R4/http`.html#history>), [R5](<https://hl7.`org/fhir/R5/http`.html#history>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type history, containing the version history sorted from most recent to oldest versions. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call history, see [Listing FHIR resource versions](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#listing_fhir_resource_versions>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_history_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: _at
    pub _at: Option<Option<String>>,
    /// Query parameter: _count
    pub _count: Option<Option<String>>,
    /// Query parameter: _page_token
    pub _page_token: Option<Option<String>>,
    /// Query parameter: _since
    pub _since: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history
/// Lists all the versions of a resource (including the current version and deleted versions) from the FHIR store. Implements the per-resource form of the FHIR standard history interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#history>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#history>), [R4](<https://hl7.`org/fhir/R4/http`.html#history>), [R5](<https://hl7.`org/fhir/R5/http`.html#history>)). On success, the response body contains a JSON-encoded representation of a Bundle resource of type history, containing the version history sorted from most recent to oldest versions. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call history, see [Listing FHIR resource versions](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#listing_fhir_resource_versions>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_history_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_history_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_history(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder(
        client,
        &args.name,
        &args._at,
        &args._count,
        &args._page_token,
        &args._since,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_history_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates part of an existing resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard patch interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#patch>), [R4](<https://hl7.`org/fhir/R4/http`.html#patch>), [R5](<https://hl7.`org/fhir/R5/http`.html#patch>)). DSTU2 doesn't define a patch method, but the server supports it in the same way it supports STU3. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call patch, see [Patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#patching_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates part of an existing resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard patch interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#patch>), [R4](<https://hl7.`org/fhir/R4/http`.html#patch>), [R5](<https://hl7.`org/fhir/R5/http`.html#patch>)). DSTU2 doesn't define a patch method, but the server supports it in the same way it supports STU3. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call patch, see [Patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#patching_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates part of an existing resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard patch interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#patch>), [R4](<https://hl7.`org/fhir/R4/http`.html#patch>), [R5](<https://hl7.`org/fhir/R5/http`.html#patch>)). DSTU2 doesn't define a patch method, but the server supports it in the same way it supports STU3. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call patch, see [Patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#patching_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates part of an existing resource by applying the operations specified in a [JSON Patch](<http://jsonpatch.com/>) document. Implements the FHIR standard patch interaction ([STU3](<https://hl7.`org/fhir/STU3/http`.html#patch>), [R4](<https://hl7.`org/fhir/R4/http`.html#patch>), [R5](<https://hl7.`org/fhir/R5/http`.html#patch>)). DSTU2 doesn't define a patch method, but the server supports it in the same way it supports STU3. The request body must contain a JSON Patch document, and the request headers must contain Content-Type: `application/json-patch`+json. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call patch, see [Patching a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#patching_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Gets the contents of a FHIR resource. Implements the FHIR standard read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#read>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#read>), [R4](<https://hl7.`org/fhir/R4/http`.html#read>), [R5](<https://hl7.`org/fhir/R5/http`.html#read>)). Also supports the FHIR standard conditional read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#cread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#cread>), [R4](<https://hl7.`org/fhir/R4/http`.html#cread>), [R5](<https://hl7.`org/fhir/R5/http`.html#cread>)) specified by supplying an If-Modified-Since header with a `date/time` value or an If-None-Match header with an ETag value. On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call read, see [Getting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_read` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Gets the contents of a FHIR resource. Implements the FHIR standard read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#read>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#read>), [R4](<https://hl7.`org/fhir/R4/http`.html#read>), [R5](<https://hl7.`org/fhir/R5/http`.html#read>)). Also supports the FHIR standard conditional read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#cread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#cread>), [R4](<https://hl7.`org/fhir/R4/http`.html#cread>), [R5](<https://hl7.`org/fhir/R5/http`.html#cread>)) specified by supplying an If-Modified-Since header with a `date/time` value or an If-None-Match header with an ETag value. On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call read, see [Getting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_read`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_read_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Gets the contents of a FHIR resource. Implements the FHIR standard read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#read>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#read>), [R4](<https://hl7.`org/fhir/R4/http`.html#read>), [R5](<https://hl7.`org/fhir/R5/http`.html#read>)). Also supports the FHIR standard conditional read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#cread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#cread>), [R4](<https://hl7.`org/fhir/R4/http`.html#cread>), [R5](<https://hl7.`org/fhir/R5/http`.html#cread>)) specified by supplying an If-Modified-Since header with a `date/time` value or an If-None-Match header with an ETag value. On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call read, see [Getting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_read_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_read`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Gets the contents of a FHIR resource. Implements the FHIR standard read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#read>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#read>), [R4](<https://hl7.`org/fhir/R4/http`.html#read>), [R5](<https://hl7.`org/fhir/R5/http`.html#read>)). Also supports the FHIR standard conditional read interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#cread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#cread>), [R4](<https://hl7.`org/fhir/R4/http`.html#cread>), [R5](<https://hl7.`org/fhir/R5/http`.html#cread>)) specified by supplying an If-Modified-Since header with a `date/time` value or an If-None-Match header with an ETag value. On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call read, see [Getting a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#getting_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_read_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_read_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_read(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_read_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_search` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    resourceType: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/_search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = resourceType.as_ref() {
        query_parts.push(format!("resourceType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: resourceType
    pub resourceType: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_search_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder(
        client,
        &args.parent,
        &args.resourceType,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_search_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{resourceType}/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    resourceType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{resourceType}/_search",
        parent,
        resourceType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{resourceType}/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{resourceType}/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_search_type`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: resourceType
    pub resourceType: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{resourceType}/_search
/// Searches for resources in the given FHIR store according to criteria specified as query parameters. Implements the FHIR standard search interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#search>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#search>), [R4](<https://hl7.`org/fhir/R4/http`.html#search>), [R5](<https://hl7.`org/fhir/R5/http`.html#search>)) using the search semantics described in the FHIR Search specification ([DSTU2](<https://hl7.`org/fhir/DSTU2/search`.html>), [STU3](<https://hl7.`org/fhir/STU3/search`.html>), [R4](<https://hl7.`org/fhir/R4/search`.html>), [R5](<https://hl7.`org/fhir/R5/search`.html>)). Supports four methods of search defined by the specification: * GET [base]?[parameters] to search across all resources. * GET [base]/[type]?[parameters] to search resources of a specified type. * POST [base]/_search?[parameters] as an alternate form having the same semantics as the GET method across all resources. * POST [base]/[type]/_search?[parameters] as an alternate form having the same semantics as the GET method for the specified type. The GET and POST methods do not support compartment searches. The POST method does not support `application/x-www-form-urlencoded` search parameters. On success, the response body contains a JSON-encoded representation of a Bundle resource of type searchset, containing the results of the search. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The server's capability statement, retrieved through capabilities, indicates what search parameters are supported on each FHIR resource. A list of all search parameters defined by the specification can be found in the FHIR Search Parameter Registry ([STU3](<https://hl7.`org/fhir/STU3/searchparameter-registry`.html>), [R4](<https://hl7.`org/fhir/R4/searchparameter-registry`.html>), [R5](<https://hl7.`org/fhir/R5/searchparameter-registry`.html>)). FHIR search parameters for DSTU2 can be found on each resource's definition page. Supported search modifiers: :missing, :exact, :contains, :text, :in, :not-in, :above, :below, :[type], :not, and recurse (DSTU2 and STU3) or :iterate (R4 and R5). Supported search result parameters: _sort, _count, _include, _revinclude, _summary=text, _summary=data, and _elements. The maximum number of search results returned defaults to 100, which can be overridden by the _count parameter up to a maximum limit of 1000. The server might return fewer resources than requested to prevent excessively large responses. If there are additional results, the returned Bundle contains a link of relation "next", which has a _page_token parameter for an opaque pagination token that can be used to retrieve the next page. Resources with a total size larger than 5MB or a field count larger than 50,000 might not be fully searchable as the server might trim its generated search index in those cases. Note: FHIR resources are indexed asynchronously, so there might be a slight delay between the time a resource is created or changed, and the time when the change reflects in search results. The only exception is resource identifier data, which is indexed synchronously as a special index. As a result, searching using resource identifier is not subject to indexing delay. To use the special synchronous index, the search term for identifier should be in the pattern identifier=[system]|[value] or identifier=[value], and any of the following search result parameters can be used: * _count * _include * _revinclude * _summary * _elements If your query contains any other search parameters, the standard asynchronous index will be used instead. Note that searching against the special index is optimized for resolving a small number of matches. The search isn't optimized if your identifier search criteria matches a large number (i.e. more than 2,000) of resources. For a search query that will match a large number of resources, you can avoiding using the special synchronous index by including an additional _sort parameter in your query. Use _sort=-_lastUpdated if you want to keep the default sorting order. For samples and detailed information, see [Searching for FHIR resources](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-search`>) and [Advanced FHIR search features](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-advanced-search`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_type(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder(
        client,
        &args.parent,
        &args.resourceType,
    )?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates the entire contents of a resource. Implements the FHIR standard update interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#update>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#update>), [R4](<https://hl7.`org/fhir/R4/http`.html#update>), [R5](<https://hl7.`org/fhir/R5/http`.html#update>)). If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The resource must contain an id element having an identical value to the ID in the REST path of the request. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The conditional update interaction If-None-Match is supported, including the wildcard behaviour, as defined by the R5 spec. This functionality is supported in R4 and R5. For samples that show how to call update, see [Updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#updating_a_fhir_resource>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_update` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates the entire contents of a resource. Implements the FHIR standard update interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#update>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#update>), [R4](<https://hl7.`org/fhir/R4/http`.html#update>), [R5](<https://hl7.`org/fhir/R5/http`.html#update>)). If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The resource must contain an id element having an identical value to the ID in the REST path of the request. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The conditional update interaction If-None-Match is supported, including the wildcard behaviour, as defined by the R5 spec. This functionality is supported in R4 and R5. For samples that show how to call update, see [Updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#updating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates the entire contents of a resource. Implements the FHIR standard update interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#update>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#update>), [R4](<https://hl7.`org/fhir/R4/http`.html#update>), [R5](<https://hl7.`org/fhir/R5/http`.html#update>)). If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The resource must contain an id element having an identical value to the ID in the REST path of the request. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The conditional update interaction If-None-Match is supported, including the wildcard behaviour, as defined by the R5 spec. This functionality is supported in R4 and R5. For samples that show how to call update, see [Updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#updating_a_fhir_resource>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}
/// Updates the entire contents of a resource. Implements the FHIR standard update interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#update>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#update>), [R4](<https://hl7.`org/fhir/R4/http`.html#update>), [R5](<https://hl7.`org/fhir/R5/http`.html#update>)). If the specified resource does not exist and the FHIR store has enable_update_create set, creates the resource with the client-specified ID. It is strongly advised not to include or encode any sensitive data such as patient identifiers in client-specified resource IDs. Those IDs are part of the FHIR resource path recorded in Cloud Audit Logs and P`ub/Sub` notifications. Those IDs can also be contained in reference fields within other resources. The request body must contain a JSON-encoded FHIR resource, and the request headers must contain Content-Type: `application/fhir`+json. The resource must contain an id element having an identical value to the ID in the REST path of the request. On success, the response body contains a JSON-encoded representation of the updated resource, including the server-assigned version ID. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. The conditional update interaction If-None-Match is supported, including the wildcard behaviour, as defined by the R5 spec. This functionality is supported in R4 and R5. For samples that show how to call update, see [Updating a FHIR resource](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#updating_a_fhir_resource>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_update_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_update(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR resource by version ID. Implements the FHIR standard vread interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#vread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#vread>), [R4](<https://hl7.`org/fhir/R4/http`.html#vread>), [R5](<https://hl7.`org/fhir/R5/http`.html#vread>)). On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call vread, see [Retrieving a FHIR resource version](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#retrieving_a_fhir_resource_version>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_fhir_vread` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history/{_historyId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR resource by version ID. Implements the FHIR standard vread interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#vread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#vread>), [R4](<https://hl7.`org/fhir/R4/http`.html#vread>), [R5](<https://hl7.`org/fhir/R5/http`.html#vread>)). On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call vread, see [Retrieving a FHIR resource version](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#retrieving_a_fhir_resource_version>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_execute()` or `healthcare_projects_locations_datasets_fhir_stores_fhir_vread`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR resource by version ID. Implements the FHIR standard vread interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#vread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#vread>), [R4](<https://hl7.`org/fhir/R4/http`.html#vread>), [R5](<https://hl7.`org/fhir/R5/http`.html#vread>)). On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call vread, see [Retrieving a FHIR resource version](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#retrieving_a_fhir_resource_version>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_vread_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_fhir_vread`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/fhir/{fhirId}/{fhirId1}/_history/{_historyId}
/// Gets the contents of a version (current or historical) of a FHIR resource by version ID. Implements the FHIR standard vread interaction ([DSTU2](<https://hl7.`org/fhir/DSTU2/http`.html#vread>), [STU3](<https://hl7.`org/fhir/STU3/http`.html#vread>), [R4](<https://hl7.`org/fhir/R4/http`.html#vread>), [R5](<https://hl7.`org/fhir/R5/http`.html#vread>)). On success, the response body contains a JSON-encoded representation of the resource. Errors generated by the FHIR store contain a JSON-encoded OperationOutcome resource describing the reason for the error. If the request cannot be mapped to a valid API method on a FHIR store, a generic GCP error might be returned instead. For samples that show how to call vread, see [Retrieving a FHIR resource version](<https://cloud.google.`com/healthcare/docs/how-tos/fhir-resources`#retrieving_a_fhir_resource_version>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder()` + `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_vread(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_fhir_vread_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Deletes operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data delete request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-delete-request>). Returns success if the operation was successfully cancelled. If the operation is complete, or has already been cancelled, returns an error response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Deletes operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data delete request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-delete-request>). Returns success if the operation was successfully cancelled. If the operation is complete, or has already been cancelled, returns an error response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_execute()` or `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Deletes operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data delete request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-delete-request>). Returns success if the operation was successfully cancelled. If the operation is complete, or has already been cancelled, returns an error response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Deletes operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data delete request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-delete-request>). Returns success if the operation was successfully cancelled. If the operation is complete, or has already been cancelled, returns an error response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder()` + `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Gets the status of operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data status request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-status-request>). Operations can have one of these states: * in-progress: response status code is 202 and X-Progress header is set to in progress. * complete: response status code is 200 and the body is a JSON-encoded operation response as defined by the spec. For a bulk export, this response is defined in <https://build.fhir.`org/ig/HL7/bulk-data/export`.html#response---complete-status.> * error: response status code is 5XX, and the body is a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_execute()` to send, or `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status` for simplest API.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Gets the status of operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data status request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-status-request>). Operations can have one of these states: * in-progress: response status code is 202 and X-Progress header is set to in progress. * complete: response status code is 200 and the body is a JSON-encoded operation response as defined by the spec. For a bulk export, this response is defined in <https://build.fhir.`org/ig/HL7/bulk-data/export`.html#response---complete-status.> * error: response status code is 5XX, and the body is a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_execute()` or `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Gets the status of operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data status request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-status-request>). Operations can have one of these states: * in-progress: response status code is 202 and X-Progress header is set to in progress. * complete: response status code is 200 and the body is a JSON-encoded operation response as defined by the spec. For a bulk export, this response is defined in <https://build.fhir.`org/ig/HL7/bulk-data/export`.html#response---complete-status.> * error: response status code is 5XX, and the body is a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}/operations/{operationsId}
/// Gets the status of operations as defined in the FHIR specification. Implements the FHIR implementation guide [bulk data status request](<https://build.fhir.`org/ig/HL7/bulk-data/export`.html#bulk-data-status-request>). Operations can have one of these states: * in-progress: response status code is 202 and X-Progress header is set to in progress. * complete: response status code is 200 and the body is a JSON-encoded operation response as defined by the spec. For a bulk export, this response is defined in <https://build.fhir.`org/ig/HL7/bulk-data/export`.html#response---complete-status.> * error: response status code is 5XX, and the body is a JSON-encoded OperationOutcome resource describing the reason for the error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder()` + `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Creates a new HL7v2 store within the parent dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_create_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_create` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    hl7V2StoreId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = hl7V2StoreId.as_ref() {
        query_parts.push(format!("hl7V2StoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Creates a new HL7v2 store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_create_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hl7V2Store>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hl7V2Store = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Creates a new HL7v2 store within the parent dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: hl7V2StoreId
    pub hl7V2StoreId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Creates a new HL7v2 store within the parent dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_create_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_create_builder(
        client,
        &args.parent,
        &args.hl7V2StoreId,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Deletes the specified HL7v2 store and removes all messages that it contains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Deletes the specified HL7v2 store and removes all messages that it contains.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Deletes the specified HL7v2 store and removes all messages that it contains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Deletes the specified HL7v2 store and removes all messages that it contains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_hl7_v2_stores_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:export
/// Exports the messages to a destination. To filter messages to be exported, define a filter using the start and end time, relative to the message generation time (MSH.7). This API returns an Operation that can be used to track the status of the job by calling GetOperation. Immediate fatal errors appear in the error field. Otherwise, when the operation finishes, a detailed response of type ExportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_export_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_export` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:export
/// Exports the messages to a destination. To filter messages to be exported, define a filter using the start and end time, relative to the message generation time (MSH.7). This API returns an Operation that can be used to track the status of the job by calling GetOperation. Immediate fatal errors appear in the error field. Otherwise, when the operation finishes, a detailed response of type ExportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_export_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:export
/// Exports the messages to a destination. To filter messages to be exported, define a filter using the start and end time, relative to the message generation time (MSH.7). This API returns an Operation that can be used to track the status of the job by calling GetOperation. Immediate fatal errors appear in the error field. Otherwise, when the operation finishes, a detailed response of type ExportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_export_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:export
/// Exports the messages to a destination. To filter messages to be exported, define a filter using the start and end time, relative to the message generation time (MSH.7). This API returns an Operation that can be used to track the status of the job by calling GetOperation. Immediate fatal errors appear in the error field. Otherwise, when the operation finishes, a detailed response of type ExportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_export_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_export_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_export(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_export_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_hl7_v2_stores_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Gets the specified HL7v2 store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_get_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_get` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Gets the specified HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hl7V2Store>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hl7V2Store = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Gets the specified HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Gets the specified HL7v2 store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_hl7_v2_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getHL7v2StoreMetrics
/// Gets metrics associated with the HL7v2 store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getHL7v2StoreMetrics",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getHL7v2StoreMetrics
/// Gets metrics associated with the HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hl7V2StoreMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hl7V2StoreMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getHL7v2StoreMetrics
/// Gets metrics associated with the HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2StoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresGetHl7V2StoreMetricsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getHL7v2StoreMetrics
/// Gets metrics associated with the HL7v2 store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetHl7V2StoreMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2StoreMetrics>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_builder(
            client, &args.name,
        )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_get_hl7v2_store_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:import
/// Import messages to the HL7v2 store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some HL7v2 store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty HL7v2 store that is not being used by other clients. An existing message will be overwritten if a duplicate message is imported. A duplicate message is a message with the same raw bytes as a message that already exists in this HL7v2 store. When a message is overwritten, its labels will also be overwritten. The import operation is idempotent unless the input data contains multiple valid messages with the same raw bytes but different labels. In that case, after the import completes, the store contains exactly one message with those raw bytes but there is no ordering guarantee on which version of the labels it has. The operation result counters do not count duplicated raw bytes as an error and count one success for each message in the input, which might result in a success count larger than the number of messages in the HL7v2 store. If some messages fail to import, for example due to parsing errors, successfully imported messages are not rolled back. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a response of type ImportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_import_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_import` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_import_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:import",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:import
/// Import messages to the HL7v2 store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some HL7v2 store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty HL7v2 store that is not being used by other clients. An existing message will be overwritten if a duplicate message is imported. A duplicate message is a message with the same raw bytes as a message that already exists in this HL7v2 store. When a message is overwritten, its labels will also be overwritten. The import operation is idempotent unless the input data contains multiple valid messages with the same raw bytes but different labels. In that case, after the import completes, the store contains exactly one message with those raw bytes but there is no ordering guarantee on which version of the labels it has. The operation result counters do not count duplicated raw bytes as an error and count one success for each message in the input, which might result in a success count larger than the number of messages in the HL7v2 store. If some messages fail to import, for example due to parsing errors, successfully imported messages are not rolled back. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a response of type ImportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_import_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:import
/// Import messages to the HL7v2 store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some HL7v2 store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty HL7v2 store that is not being used by other clients. An existing message will be overwritten if a duplicate message is imported. A duplicate message is a message with the same raw bytes as a message that already exists in this HL7v2 store. When a message is overwritten, its labels will also be overwritten. The import operation is idempotent unless the input data contains multiple valid messages with the same raw bytes but different labels. In that case, after the import completes, the store contains exactly one message with those raw bytes but there is no ordering guarantee on which version of the labels it has. The operation result counters do not count duplicated raw bytes as an error and count one success for each message in the input, which might result in a success count larger than the number of messages in the HL7v2 store. If some messages fail to import, for example due to parsing errors, successfully imported messages are not rolled back. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a response of type ImportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_import_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:import
/// Import messages to the HL7v2 store by loading data from the specified sources. This method is optimized to load large quantities of data using import semantics that ignore some HL7v2 store configuration options and are not suitable for all use cases. It is primarily intended to load data into an empty HL7v2 store that is not being used by other clients. An existing message will be overwritten if a duplicate message is imported. A duplicate message is a message with the same raw bytes as a message that already exists in this HL7v2 store. When a message is overwritten, its labels will also be overwritten. The import operation is idempotent unless the input data contains multiple valid messages with the same raw bytes but different labels. In that case, after the import completes, the store contains exactly one message with those raw bytes but there is no ordering guarantee on which version of the labels it has. The operation result counters do not count duplicated raw bytes as an error and count one success for each message in the input, which might result in a success count larger than the number of messages in the HL7v2 store. If some messages fail to import, for example due to parsing errors, successfully imported messages are not rolled back. This method returns an Operation that can be used to track the status of the import by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a response of type ImportMessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_import_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_import_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_import(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_import_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_hl7_v2_stores_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Lists the HL7v2 stores in the given dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_list_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_list` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Lists the HL7v2 stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_list_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHl7V2StoresResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHl7V2StoresResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Lists the HL7v2 stores in the given dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHl7V2StoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores
/// Lists the HL7v2 stores in the given dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_list_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHl7V2StoresResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Updates the HL7v2 store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Updates the HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hl7V2Store>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hl7V2Store = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Updates the HL7v2 store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}
/// Updates the HL7v2 store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hl7V2Store>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:rollback
/// Rolls back messages from the HL7v2 store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackHl7V2MessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_rollback` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:rollback",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:rollback
/// Rolls back messages from the HL7v2 store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackHl7V2MessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:rollback
/// Rolls back messages from the HL7v2 store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackHl7V2MessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:rollback
/// Rolls back messages from the HL7v2 store to the specified time. This method returns an Operation that can be used to track the status of the rollback by calling GetOperation. Immediate fatal errors appear in the error field, errors are also logged to Cloud Logging (see [Viewing error logs in Cloud Logging](<https://cloud.google.`com/healthcare/docs/how-tos/logging`>)). Otherwise, when the operation finishes, a detailed response of type RollbackHl7V2MessagesResponse is returned in the response field. The metadata field type for this operation is OperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_rollback(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_hl7_v2_stores_rollback_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Message>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Message = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_create(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder(
        client,
        &args.parent,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Deletes an HL7v2 message.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Deletes an HL7v2 message.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Deletes an HL7v2 message.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Deletes an HL7v2 message.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder(
        client, &args.name,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Gets an HL7v2 message.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Gets an HL7v2 message.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Message>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Message = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Gets an HL7v2 message.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Gets an HL7v2 message.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder(
        client, &args.name, &args.view,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages:ingest
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received. If the method is successful, it generates a response containing an HL7v2 acknowledgment (ACK) message. If the method encounters an error, it returns a negative acknowledgment (NACK) message. This behavior is suitable for replying to HL7v2 interface systems that expect these acknowledgments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages:ingest",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages:ingest
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received. If the method is successful, it generates a response containing an HL7v2 acknowledgment (ACK) message. If the method encounters an error, it returns a negative acknowledgment (NACK) message. This behavior is suitable for replying to HL7v2 interface systems that expect these acknowledgments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IngestMessageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IngestMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages:ingest
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received. If the method is successful, it generates a response containing an HL7v2 acknowledgment (ACK) message. If the method encounters an error, it returns a negative acknowledgment (NACK) message. This behavior is suitable for replying to HL7v2 interface systems that expect these acknowledgments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IngestMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages:ingest
/// Parses and stores an HL7v2 message. This method triggers an asynchronous notification to any P`ub/Sub` topic configured in Hl7V2Store.Hl7V2NotificationConfig, if the filtering matches the message. If an MLLP adapter is configured to listen to a P`ub/Sub` topic, the adapter transmits the message when a notification is received. If the method is successful, it generates a response containing an HL7v2 acknowledgment (ACK) message. If the method encounters an error, it returns a negative acknowledgment (NACK) message. This behavior is suitable for replying to HL7v2 interface systems that expect these acknowledgments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IngestMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder(
        client,
        &args.parent,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Lists all the messages in the given HL7v2 store with support for filtering. Note: HL7v2 messages are indexed asynchronously, so there might be a slight delay between the time a message is created and when it can be found through a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Lists all the messages in the given HL7v2 store with support for filtering. Note: HL7v2 messages are indexed asynchronously, so there might be a slight delay between the time a message is created and when it can be found through a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMessagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Lists all the messages in the given HL7v2 store with support for filtering. Note: HL7v2 messages are indexed asynchronously, so there might be a slight delay between the time a message is created and when it can be found through a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages
/// Lists all the messages in the given HL7v2 store with support for filtering. Note: HL7v2 messages are indexed asynchronously, so there might be a slight delay between the time a message is created and when it can be found through a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Update the message. The contents of the message in Message.data and data extracted from the contents such as Message.create_time cannot be altered. Only the Message.labels field is allowed to be updated. The labels in the request are merged with the existing set of labels. Existing labels with the same keys are updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_execute()` to send, or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch` for simplest API.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Update the message. The contents of the message in Message.data and data extracted from the contents such as Message.create_time cannot be altered. Only the Message.labels field is allowed to be updated. The labels in the request are merged with the existing set of labels. Existing labels with the same keys are updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_execute()` or `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Message>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Message = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Update the message. The contents of the message in Message.data and data extracted from the contents such as Message.create_time cannot be altered. Only the Message.labels field is allowed to be updated. The labels in the request are merged with the existing set of labels. Existing labels with the same keys are updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/hl7V2Stores/{hl7V2StoresId}/messages/{messagesId}
/// Update the message. The contents of the message in Message.data and data extracted from the contents such as Message.create_time cannot be altered. Only the Message.labels field is allowed to be updated. The labels in the request are merged with the existing set of labels. Existing labels with the same keys are updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder()` + `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_operations_cancel_execute()` to send, or `healthcare_projects_locations_datasets_operations_cancel` for simplest API.

pub fn healthcare_projects_locations_datasets_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_operations_cancel_execute()` or `healthcare_projects_locations_datasets_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_cancel_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_operations_cancel_builder()` + `healthcare_projects_locations_datasets_operations_cancel_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_cancel(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_operations_cancel_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_operations_get_execute()` to send, or `healthcare_projects_locations_datasets_operations_get` for simplest API.

pub fn healthcare_projects_locations_datasets_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_operations_get_execute()` or `healthcare_projects_locations_datasets_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_get_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_operations_get_builder()` + `healthcare_projects_locations_datasets_operations_get_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_get(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        healthcare_projects_locations_datasets_operations_get_builder(client, &args.name)?;
    healthcare_projects_locations_datasets_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_datasets_operations_list_execute()` to send, or `healthcare_projects_locations_datasets_operations_list` for simplest API.

pub fn healthcare_projects_locations_datasets_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/datasets/{datasetsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_datasets_operations_list_execute()` or `healthcare_projects_locations_datasets_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_datasets_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_list_task()`.
/// For the simplest API, use `healthcare_projects_locations_datasets_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_datasets_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_datasets_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_datasets_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_datasets_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_datasets_operations_list_builder()` + `healthcare_projects_locations_datasets_operations_list_execute()`.
/// For task-level control, use `healthcare_projects_locations_datasets_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_datasets_operations_list(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsDatasetsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_datasets_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    healthcare_projects_locations_datasets_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities
/// Analyze heathcare entity in a document. Its response includes the recognized entity mentions and the relationships between them. AnalyzeEntities uses context aware models to detect entities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `healthcare_projects_locations_services_nlp_analyze_entities_execute()` to send, or `healthcare_projects_locations_services_nlp_analyze_entities` for simplest API.

pub fn healthcare_projects_locations_services_nlp_analyze_entities_builder<R>(
    client: &SimpleHttpClient<R>,
    nlpService: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{}/locations/{locationsId}/services/nlp:analyzeEntities",
        nlpService,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities
/// Analyze heathcare entity in a document. Its response includes the recognized entity mentions and the relationships between them. AnalyzeEntities uses context aware models to detect entities.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `healthcare_projects_locations_services_nlp_analyze_entities_execute()` or `healthcare_projects_locations_services_nlp_analyze_entities`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_services_nlp_analyze_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_services_nlp_analyze_entities_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeEntitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities
/// Analyze heathcare entity in a document. Its response includes the recognized entity mentions and the relationships between them. AnalyzeEntities uses context aware models to detect entities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `healthcare_projects_locations_services_nlp_analyze_entities_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `healthcare_projects_locations_services_nlp_analyze_entities_task()`.
/// For the simplest API, use `healthcare_projects_locations_services_nlp_analyze_entities()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `healthcare_projects_locations_services_nlp_analyze_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn healthcare_projects_locations_services_nlp_analyze_entities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = healthcare_projects_locations_services_nlp_analyze_entities_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`healthcare_projects_locations_services_nlp_analyze_entities`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs {
    /// Path parameter: nlpService
    pub nlpService: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities
/// Analyze heathcare entity in a document. Its response includes the recognized entity mentions and the relationships between them. AnalyzeEntities uses context aware models to detect entities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `healthcare_projects_locations_services_nlp_analyze_entities_builder()` + `healthcare_projects_locations_services_nlp_analyze_entities_execute()`.
/// For task-level control, use `healthcare_projects_locations_services_nlp_analyze_entities_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn healthcare_projects_locations_services_nlp_analyze_entities(
    client: &SimpleHttpClient,
    args: &HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = healthcare_projects_locations_services_nlp_analyze_entities_builder(
        client,
        &args.nlpService,
    )?;
    healthcare_projects_locations_services_nlp_analyze_entities_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with HealthcareProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsGetArgs) -> String {
        format!("gcp::healthcare::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with HealthcareProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsListArgs) -> String {
        format!("gcp::healthcare::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsCreateArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDeidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDeidentifyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDeidentifyArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.sourceDataset)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Dataset
// =============================================================================

/// ResourceIdentifier implementation for Dataset with HealthcareProjectsLocationsDatasetsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsGetArgs> for Dataset {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsGetArgs) -> String {
        format!("gcp::healthcare::Dataset/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Dataset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDatasetsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDatasetsResponse with HealthcareProjectsLocationsDatasetsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsListArgs> for ListDatasetsResponse {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsListArgs) -> String {
        format!("gcp::healthcare::ListDatasetsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListDatasetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Dataset
// =============================================================================

/// ResourceIdentifier implementation for Dataset with HealthcareProjectsLocationsDatasetsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsPatchArgs> for Dataset {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsPatchArgs) -> String {
        format!("gcp::healthcare::Dataset/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Dataset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CheckDataAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckDataAccessResponse with HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs>
    for CheckDataAccessResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs,
    ) -> String {
        format!(
            "gcp::healthcare::CheckDataAccessResponse/{}",
            input.consentStore
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::CheckDataAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConsentStore
// =============================================================================

/// ResourceIdentifier implementation for ConsentStore with HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs>
    for ConsentStore
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs,
    ) -> String {
        format!("gcp::healthcare::ConsentStore/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ConsentStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EvaluateUserConsentsResponse
// =============================================================================

/// ResourceIdentifier implementation for EvaluateUserConsentsResponse with HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs>
    for EvaluateUserConsentsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::EvaluateUserConsentsResponse/{}",
            input.consentStore
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::EvaluateUserConsentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConsentStore
// =============================================================================

/// ResourceIdentifier implementation for ConsentStore with HealthcareProjectsLocationsDatasetsConsentStoresGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresGetArgs> for ConsentStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresGetArgs,
    ) -> String {
        format!("gcp::healthcare::ConsentStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ConsentStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConsentStoresResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConsentStoresResponse with HealthcareProjectsLocationsDatasetsConsentStoresListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresListArgs>
    for ListConsentStoresResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresListArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ListConsentStoresResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListConsentStoresResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConsentStore
// =============================================================================

/// ResourceIdentifier implementation for ConsentStore with HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs>
    for ConsentStore
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs,
    ) -> String {
        format!("gcp::healthcare::ConsentStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ConsentStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.consentStore)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AttributeDefinition
// =============================================================================

/// ResourceIdentifier implementation for AttributeDefinition with HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs,
    > for AttributeDefinition
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs,
    ) -> String {
        format!("gcp::healthcare::AttributeDefinition/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::AttributeDefinition"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AttributeDefinition
// =============================================================================

/// ResourceIdentifier implementation for AttributeDefinition with HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs>
    for AttributeDefinition
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs,
    ) -> String {
        format!("gcp::healthcare::AttributeDefinition/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::AttributeDefinition"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAttributeDefinitionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAttributeDefinitionsResponse with HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs>
    for ListAttributeDefinitionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ListAttributeDefinitionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListAttributeDefinitionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AttributeDefinition
// =============================================================================

/// ResourceIdentifier implementation for AttributeDefinition with HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs,
    > for AttributeDefinition
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs,
    ) -> String {
        format!("gcp::healthcare::AttributeDefinition/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::AttributeDefinition"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConsentArtifact
// =============================================================================

/// ResourceIdentifier implementation for ConsentArtifact with HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs>
    for ConsentArtifact
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs,
    ) -> String {
        format!("gcp::healthcare::ConsentArtifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ConsentArtifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConsentArtifact
// =============================================================================

/// ResourceIdentifier implementation for ConsentArtifact with HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs>
    for ConsentArtifact
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs,
    ) -> String {
        format!("gcp::healthcare::ConsentArtifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ConsentArtifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConsentArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConsentArtifactsResponse with HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs>
    for ListConsentArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ListConsentArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListConsentArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConsentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConsentsResponse with HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs>
    for ListConsentsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs,
    ) -> String {
        format!("gcp::healthcare::ListConsentsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListConsentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConsentRevisionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConsentRevisionsResponse with HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs>
    for ListConsentRevisionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ListConsentRevisionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListConsentRevisionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Consent
// =============================================================================

/// ResourceIdentifier implementation for Consent with HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs>
    for Consent
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs,
    ) -> String {
        format!("gcp::healthcare::Consent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Consent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ArchiveUserDataMappingResponse
// =============================================================================

/// ResourceIdentifier implementation for ArchiveUserDataMappingResponse with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs>
    for ArchiveUserDataMappingResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ArchiveUserDataMappingResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ArchiveUserDataMappingResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserDataMapping
// =============================================================================

/// ResourceIdentifier implementation for UserDataMapping with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs>
    for UserDataMapping
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs,
    ) -> String {
        format!("gcp::healthcare::UserDataMapping/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::UserDataMapping"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserDataMapping
// =============================================================================

/// ResourceIdentifier implementation for UserDataMapping with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs>
    for UserDataMapping
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs,
    ) -> String {
        format!("gcp::healthcare::UserDataMapping/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::UserDataMapping"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUserDataMappingsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUserDataMappingsResponse with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs>
    for ListUserDataMappingsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs,
    ) -> String {
        format!(
            "gcp::healthcare::ListUserDataMappingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListUserDataMappingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserDataMapping
// =============================================================================

/// ResourceIdentifier implementation for UserDataMapping with HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs>
    for UserDataMapping
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs,
    ) -> String {
        format!("gcp::healthcare::UserDataMapping/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::UserDataMapping"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DicomStore
// =============================================================================

/// ResourceIdentifier implementation for DicomStore with HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs> for DicomStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs,
    ) -> String {
        format!("gcp::healthcare::DicomStore/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::DicomStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.sourceStore)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresExportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresExportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DicomStore
// =============================================================================

/// ResourceIdentifier implementation for DicomStore with HealthcareProjectsLocationsDatasetsDicomStoresGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresGetArgs> for DicomStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresGetArgs,
    ) -> String {
        format!("gcp::healthcare::DicomStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::DicomStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DicomStoreMetrics
// =============================================================================

/// ResourceIdentifier implementation for DicomStoreMetrics with HealthcareProjectsLocationsDatasetsDicomStoresGetDicomstoreMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresGetDicomstoreMetricsArgs>
    for DicomStoreMetrics
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresGetDicomstoreMetricsArgs,
    ) -> String {
        format!("gcp::healthcare::DicomStoreMetrics/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::DicomStoreMetrics"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresImportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresImportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDicomStoresResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDicomStoresResponse with HealthcareProjectsLocationsDatasetsDicomStoresListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresListArgs>
    for ListDicomStoresResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresListArgs,
    ) -> String {
        format!("gcp::healthcare::ListDicomStoresResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListDicomStoresResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DicomStore
// =============================================================================

/// ResourceIdentifier implementation for DicomStore with HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs> for DicomStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs,
    ) -> String {
        format!("gcp::healthcare::DicomStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::DicomStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StudyMetrics
// =============================================================================

/// ResourceIdentifier implementation for StudyMetrics with HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs,
    > for StudyMetrics
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs,
    ) -> String {
        format!("gcp::healthcare::StudyMetrics/{}", input.study)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::StudyMetrics"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SeriesMetrics
// =============================================================================

/// ResourceIdentifier implementation for SeriesMetrics with HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs,
    > for SeriesMetrics
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs,
    ) -> String {
        format!("gcp::healthcare::SeriesMetrics/{}", input.series)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::SeriesMetrics"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StorageInfo
// =============================================================================

/// ResourceIdentifier implementation for StorageInfo with HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs> for StorageInfo {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs) -> String {
        format!("gcp::healthcare::StorageInfo/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::StorageInfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs,
    ) -> String {
        format!(
            "gcp::healthcare::Operation/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs,
    ) -> String {
        format!(
            "gcp::healthcare::Operation/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs,
    ) -> String {
        format!(
            "gcp::healthcare::Empty/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.dicomWebPath
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs> for HttpBody {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs) -> String {
        format!("gcp::healthcare::HttpBody/{}/{}", input.parent, input.dicomWebPath)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs> for HttpBody {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs) -> String {
        format!("gcp::healthcare::HttpBody/{}/{}", input.parent, input.dicomWebPath)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs> for HttpBody {
    fn generate_resource_id(&self, input: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs) -> String {
        format!("gcp::healthcare::HttpBody/{}/{}", input.parent, input.dicomWebPath)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FhirStore
// =============================================================================

/// ResourceIdentifier implementation for FhirStore with HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs> for FhirStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs,
    ) -> String {
        format!("gcp::healthcare::FhirStore/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::FhirStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.sourceStore)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExplainDataAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for ExplainDataAccessResponse with HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs>
    for ExplainDataAccessResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs,
    ) -> String {
        format!("gcp::healthcare::ExplainDataAccessResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ExplainDataAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresExportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresExportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FhirStore
// =============================================================================

/// ResourceIdentifier implementation for FhirStore with HealthcareProjectsLocationsDatasetsFhirStoresGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresGetArgs> for FhirStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresGetArgs,
    ) -> String {
        format!("gcp::healthcare::FhirStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::FhirStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FhirStoreMetrics
// =============================================================================

/// ResourceIdentifier implementation for FhirStoreMetrics with HealthcareProjectsLocationsDatasetsFhirStoresGetFhirstoreMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresGetFhirstoreMetricsArgs>
    for FhirStoreMetrics
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresGetFhirstoreMetricsArgs,
    ) -> String {
        format!("gcp::healthcare::FhirStoreMetrics/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::FhirStoreMetrics"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresImportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresImportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFhirStoresResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFhirStoresResponse with HealthcareProjectsLocationsDatasetsFhirStoresListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresListArgs>
    for ListFhirStoresResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresListArgs,
    ) -> String {
        format!("gcp::healthcare::ListFhirStoresResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListFhirStoresResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FhirStore
// =============================================================================

/// ResourceIdentifier implementation for FhirStore with HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs> for FhirStore {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs,
    ) -> String {
        format!("gcp::healthcare::FhirStore/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::FhirStore"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.type_rs
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}/{}", input.parent, input.type_rs)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.type_rs
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.type_rs
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.type_rs
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs,
    ) -> String {
        format!(
            "gcp::healthcare::HttpBody/{}/{}",
            input.parent, input.resourceType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs,
    > for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs,
    ) -> String {
        format!("gcp::healthcare::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hl7V2Store
// =============================================================================

/// ResourceIdentifier implementation for Hl7V2Store with HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs> for Hl7V2Store {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs,
    ) -> String {
        format!("gcp::healthcare::Hl7V2Store/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Hl7V2Store"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hl7V2Store
// =============================================================================

/// ResourceIdentifier implementation for Hl7V2Store with HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs> for Hl7V2Store {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs,
    ) -> String {
        format!("gcp::healthcare::Hl7V2Store/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Hl7V2Store"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hl7V2StoreMetrics
// =============================================================================

/// ResourceIdentifier implementation for Hl7V2StoreMetrics with HealthcareProjectsLocationsDatasetsHl7V2StoresGetHl7V2StoreMetricsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresGetHl7V2StoreMetricsArgs>
    for Hl7V2StoreMetrics
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetHl7V2StoreMetricsArgs,
    ) -> String {
        format!("gcp::healthcare::Hl7V2StoreMetrics/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Hl7V2StoreMetrics"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListHl7V2StoresResponse
// =============================================================================

/// ResourceIdentifier implementation for ListHl7V2StoresResponse with HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs>
    for ListHl7V2StoresResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs,
    ) -> String {
        format!("gcp::healthcare::ListHl7V2StoresResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListHl7V2StoresResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hl7V2Store
// =============================================================================

/// ResourceIdentifier implementation for Hl7V2Store with HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs> for Hl7V2Store {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs,
    ) -> String {
        format!("gcp::healthcare::Hl7V2Store/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Hl7V2Store"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs,
    ) -> String {
        format!("gcp::healthcare::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::healthcare::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Message
// =============================================================================

/// ResourceIdentifier implementation for Message with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs>
    for Message
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs,
    ) -> String {
        format!("gcp::healthcare::Message/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Message"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Message
// =============================================================================

/// ResourceIdentifier implementation for Message with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs> for Message {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs,
    ) -> String {
        format!("gcp::healthcare::Message/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Message"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for IngestMessageResponse
// =============================================================================

/// ResourceIdentifier implementation for IngestMessageResponse with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs>
    for IngestMessageResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs,
    ) -> String {
        format!("gcp::healthcare::IngestMessageResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::IngestMessageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMessagesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMessagesResponse with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs>
    for ListMessagesResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs,
    ) -> String {
        format!("gcp::healthcare::ListMessagesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListMessagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Message
// =============================================================================

/// ResourceIdentifier implementation for Message with HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs>
    for Message
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs,
    ) -> String {
        format!("gcp::healthcare::Message/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Message"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HealthcareProjectsLocationsDatasetsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsOperationsCancelArgs,
    ) -> String {
        format!("gcp::healthcare::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HealthcareProjectsLocationsDatasetsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsOperationsGetArgs,
    ) -> String {
        format!("gcp::healthcare::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with HealthcareProjectsLocationsDatasetsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsDatasetsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsDatasetsOperationsListArgs,
    ) -> String {
        format!("gcp::healthcare::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnalyzeEntitiesResponse
// =============================================================================

/// ResourceIdentifier implementation for AnalyzeEntitiesResponse with HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs>
    for AnalyzeEntitiesResponse
{
    fn generate_resource_id(
        &self,
        input: &HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs,
    ) -> String {
        format!(
            "gcp::healthcare::AnalyzeEntitiesResponse/{}",
            input.nlpService
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::healthcare::AnalyzeEntitiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
