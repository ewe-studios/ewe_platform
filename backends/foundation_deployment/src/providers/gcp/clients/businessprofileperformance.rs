//! Auto-generated API clients for gcp/businessprofileperformance.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute()` to send, or `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series` for simplest API.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder(
    client: &SimpleHttpClient,
    location: String,
    dailyMetrics: Option<String>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{}:fetchMultiDailyMetricsTimeSeries",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dailyMetrics {
        query_parts.push(format!("dailyMetrics={}", val));
    }
    if let Some(val) = dailyRange_endDate_day {
        query_parts.push(format!("dailyRange.endDate.day={}", val));
    }
    if let Some(val) = dailyRange_endDate_month {
        query_parts.push(format!("dailyRange.endDate.month={}", val));
    }
    if let Some(val) = dailyRange_endDate_year {
        query_parts.push(format!("dailyRange.endDate.year={}", val));
    }
    if let Some(val) = dailyRange_startDate_day {
        query_parts.push(format!("dailyRange.startDate.day={}", val));
    }
    if let Some(val) = dailyRange_startDate_month {
        query_parts.push(format!("dailyRange.startDate.month={}", val));
    }
    if let Some(val) = dailyRange_startDate_year {
        query_parts.push(format!("dailyRange.startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute()` or `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchMultiDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_task()`.
/// For the simplest API, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`businessprofileperformance_locations_fetch_multi_daily_metrics_time_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsFetchMultiDailyMetricsTimeSeriesArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: dailyMetrics
    pub dailyMetrics: Option<String>,
    /// Query parameter: dailyRange_endDate_day
    pub dailyRange_endDate_day: Option<i32>,
    /// Query parameter: dailyRange_endDate_month
    pub dailyRange_endDate_month: Option<i32>,
    /// Query parameter: dailyRange_endDate_year
    pub dailyRange_endDate_year: Option<i32>,
    /// Query parameter: dailyRange_startDate_day
    pub dailyRange_startDate_day: Option<i32>,
    /// Query parameter: dailyRange_startDate_month
    pub dailyRange_startDate_month: Option<i32>,
    /// Query parameter: dailyRange_startDate_year
    pub dailyRange_startDate_year: Option<i32>,
}

/// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()` + `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute()`.
/// For task-level control, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series(
    client: &SimpleHttpClient,
    args: &BusinessprofileperformanceLocationsFetchMultiDailyMetricsTimeSeriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder(
            client,
            args.location.clone(),
            args.dailyMetrics.clone(),
            args.dailyRange_endDate_day.clone(),
            args.dailyRange_endDate_month.clone(),
            args.dailyRange_endDate_year.clone(),
            args.dailyRange_startDate_day.clone(),
            args.dailyRange_startDate_month.clone(),
            args.dailyRange_startDate_year.clone(),
        )?;
    businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute(builder)
}

/// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_get_daily_metrics_time_series_execute()` to send, or `businessprofileperformance_locations_get_daily_metrics_time_series` for simplest API.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series_builder(
    client: &SimpleHttpClient,
    name: String,
    dailyMetric: Option<String>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
    dailySubEntityType_dayOfWeek: Option<String>,
    dailySubEntityType_timeOfDay_hours: Option<i32>,
    dailySubEntityType_timeOfDay_minutes: Option<i32>,
    dailySubEntityType_timeOfDay_nanos: Option<i32>,
    dailySubEntityType_timeOfDay_seconds: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{}:getDailyMetricsTimeSeries",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dailyMetric {
        query_parts.push(format!("dailyMetric={}", val));
    }
    if let Some(val) = dailyRange_endDate_day {
        query_parts.push(format!("dailyRange.endDate.day={}", val));
    }
    if let Some(val) = dailyRange_endDate_month {
        query_parts.push(format!("dailyRange.endDate.month={}", val));
    }
    if let Some(val) = dailyRange_endDate_year {
        query_parts.push(format!("dailyRange.endDate.year={}", val));
    }
    if let Some(val) = dailyRange_startDate_day {
        query_parts.push(format!("dailyRange.startDate.day={}", val));
    }
    if let Some(val) = dailyRange_startDate_month {
        query_parts.push(format!("dailyRange.startDate.month={}", val));
    }
    if let Some(val) = dailyRange_startDate_year {
        query_parts.push(format!("dailyRange.startDate.year={}", val));
    }
    if let Some(val) = dailySubEntityType_dayOfWeek {
        query_parts.push(format!("dailySubEntityType.dayOfWeek={}", val));
    }
    if let Some(val) = dailySubEntityType_timeOfDay_hours {
        query_parts.push(format!("dailySubEntityType.timeOfDay.hours={}", val));
    }
    if let Some(val) = dailySubEntityType_timeOfDay_minutes {
        query_parts.push(format!("dailySubEntityType.timeOfDay.minutes={}", val));
    }
    if let Some(val) = dailySubEntityType_timeOfDay_nanos {
        query_parts.push(format!("dailySubEntityType.timeOfDay.nanos={}", val));
    }
    if let Some(val) = dailySubEntityType_timeOfDay_seconds {
        query_parts.push(format!("dailySubEntityType.timeOfDay.seconds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `businessprofileperformance_locations_get_daily_metrics_time_series_execute()` or `businessprofileperformance_locations_get_daily_metrics_time_series`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_get_daily_metrics_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `businessprofileperformance_locations_get_daily_metrics_time_series_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `businessprofileperformance_locations_get_daily_metrics_time_series_task()`.
/// For the simplest API, use `businessprofileperformance_locations_get_daily_metrics_time_series()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_get_daily_metrics_time_series_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = businessprofileperformance_locations_get_daily_metrics_time_series_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`businessprofileperformance_locations_get_daily_metrics_time_series`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsGetDailyMetricsTimeSeriesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: dailyMetric
    pub dailyMetric: Option<String>,
    /// Query parameter: dailyRange_endDate_day
    pub dailyRange_endDate_day: Option<i32>,
    /// Query parameter: dailyRange_endDate_month
    pub dailyRange_endDate_month: Option<i32>,
    /// Query parameter: dailyRange_endDate_year
    pub dailyRange_endDate_year: Option<i32>,
    /// Query parameter: dailyRange_startDate_day
    pub dailyRange_startDate_day: Option<i32>,
    /// Query parameter: dailyRange_startDate_month
    pub dailyRange_startDate_month: Option<i32>,
    /// Query parameter: dailyRange_startDate_year
    pub dailyRange_startDate_year: Option<i32>,
    /// Query parameter: dailySubEntityType_dayOfWeek
    pub dailySubEntityType_dayOfWeek: Option<String>,
    /// Query parameter: dailySubEntityType_timeOfDay_hours
    pub dailySubEntityType_timeOfDay_hours: Option<i32>,
    /// Query parameter: dailySubEntityType_timeOfDay_minutes
    pub dailySubEntityType_timeOfDay_minutes: Option<i32>,
    /// Query parameter: dailySubEntityType_timeOfDay_nanos
    pub dailySubEntityType_timeOfDay_nanos: Option<i32>,
    /// Query parameter: dailySubEntityType_timeOfDay_seconds
    pub dailySubEntityType_timeOfDay_seconds: Option<i32>,
}

/// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_get_daily_metrics_time_series_builder()` + `businessprofileperformance_locations_get_daily_metrics_time_series_execute()`.
/// For task-level control, use `businessprofileperformance_locations_get_daily_metrics_time_series_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series(
    client: &SimpleHttpClient,
    args: &BusinessprofileperformanceLocationsGetDailyMetricsTimeSeriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = businessprofileperformance_locations_get_daily_metrics_time_series_builder(
        client,
        args.name.clone(),
        args.dailyMetric.clone(),
        args.dailyRange_endDate_day.clone(),
        args.dailyRange_endDate_month.clone(),
        args.dailyRange_endDate_year.clone(),
        args.dailyRange_startDate_day.clone(),
        args.dailyRange_startDate_month.clone(),
        args.dailyRange_startDate_year.clone(),
        args.dailySubEntityType_dayOfWeek.clone(),
        args.dailySubEntityType_timeOfDay_hours.clone(),
        args.dailySubEntityType_timeOfDay_minutes.clone(),
        args.dailySubEntityType_timeOfDay_nanos.clone(),
        args.dailySubEntityType_timeOfDay_seconds.clone(),
    )?;
    businessprofileperformance_locations_get_daily_metrics_time_series_execute(builder)
}

/// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute()` to send, or `businessprofileperformance_locations_searchkeywords_impressions_monthly_list` for simplest API.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    monthlyRange_endMonth_day: Option<i32>,
    monthlyRange_endMonth_month: Option<i32>,
    monthlyRange_endMonth_year: Option<i32>,
    monthlyRange_startMonth_day: Option<i32>,
    monthlyRange_startMonth_month: Option<i32>,
    monthlyRange_startMonth_year: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{}/searchkeywords/impressions/monthly",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = monthlyRange_endMonth_day {
        query_parts.push(format!("monthlyRange.endMonth.day={}", val));
    }
    if let Some(val) = monthlyRange_endMonth_month {
        query_parts.push(format!("monthlyRange.endMonth.month={}", val));
    }
    if let Some(val) = monthlyRange_endMonth_year {
        query_parts.push(format!("monthlyRange.endMonth.year={}", val));
    }
    if let Some(val) = monthlyRange_startMonth_day {
        query_parts.push(format!("monthlyRange.startMonth.day={}", val));
    }
    if let Some(val) = monthlyRange_startMonth_month {
        query_parts.push(format!("monthlyRange.startMonth.month={}", val));
    }
    if let Some(val) = monthlyRange_startMonth_year {
        query_parts.push(format!("monthlyRange.startMonth.year={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute()` or `businessprofileperformance_locations_searchkeywords_impressions_monthly_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSearchKeywordImpressionsMonthlyResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_task()`.
/// For the simplest API, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        businessprofileperformance_locations_searchkeywords_impressions_monthly_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`businessprofileperformance_locations_searchkeywords_impressions_monthly_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsSearchkeywordsImpressionsMonthlyListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: monthlyRange_endMonth_day
    pub monthlyRange_endMonth_day: Option<i32>,
    /// Query parameter: monthlyRange_endMonth_month
    pub monthlyRange_endMonth_month: Option<i32>,
    /// Query parameter: monthlyRange_endMonth_year
    pub monthlyRange_endMonth_year: Option<i32>,
    /// Query parameter: monthlyRange_startMonth_day
    pub monthlyRange_startMonth_day: Option<i32>,
    /// Query parameter: monthlyRange_startMonth_month
    pub monthlyRange_startMonth_month: Option<i32>,
    /// Query parameter: monthlyRange_startMonth_year
    pub monthlyRange_startMonth_year: Option<i32>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()` + `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute()`.
/// For task-level control, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list(
    client: &SimpleHttpClient,
    args: &BusinessprofileperformanceLocationsSearchkeywordsImpressionsMonthlyListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder(
            client,
            args.parent.clone(),
            args.monthlyRange_endMonth_day.clone(),
            args.monthlyRange_endMonth_month.clone(),
            args.monthlyRange_endMonth_year.clone(),
            args.monthlyRange_startMonth_day.clone(),
            args.monthlyRange_startMonth_month.clone(),
            args.monthlyRange_startMonth_year.clone(),
            args.pageSize.clone(),
            args.pageToken.clone(),
        )?;
    businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute(builder)
}
