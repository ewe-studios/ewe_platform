//! Auto-generated API clients for gcp/businessprofileperformance.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+location}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute()` to send, or `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series` for simplest API.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder(
    client: &SimpleHttpClient,
    location: &str,
    dailyMetrics: Option<&str>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+location}:fetchMultiDailyMetricsTimeSeries",
        location,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dailyMetrics",
            dailyMetrics
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_day",
            dailyRange_endDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_month",
            dailyRange_endDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_year",
            dailyRange_endDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_day",
            dailyRange_startDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_month",
            dailyRange_startDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_year",
            dailyRange_startDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+location}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchMultiDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+location}:fetchMultiDailyMetricsTimeSeries
/// Returns the values for each date from a given time range and optionally the sub entity type, where applicable, that are associated with the specific daily metrics. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`fetchMultiDailyMetricsTimeSeries`?`dailyMetrics`=WEBSITE_CLICKS&`dailyMetrics`=CALL_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder()` + `businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series(
    client: &SimpleHttpClient,
    location: &str,
    dailyMetrics: Option<&str>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_builder(
            client,
            location,
            dailyMetrics,
            dailyRange_endDate_day,
            dailyRange_endDate_month,
            dailyRange_endDate_year,
            dailyRange_startDate_day,
            dailyRange_startDate_month,
            dailyRange_startDate_year,
        )?;
    businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_execute(builder)
}

/// GET v1/{+name}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_get_daily_metrics_time_series_execute()` to send, or `businessprofileperformance_locations_get_daily_metrics_time_series` for simplest API.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series_builder(
    client: &SimpleHttpClient,
    name: &str,
    dailyMetric: Option<&str>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
    dailySubEntityType_dayOfWeek: Option<&str>,
    dailySubEntityType_timeOfDay_hours: Option<i32>,
    dailySubEntityType_timeOfDay_minutes: Option<i32>,
    dailySubEntityType_timeOfDay_nanos: Option<i32>,
    dailySubEntityType_timeOfDay_seconds: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:getDailyMetricsTimeSeries",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dailyMetric",
            dailyMetric
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_day",
            dailyRange_endDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_month",
            dailyRange_endDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_endDate_year",
            dailyRange_endDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_day",
            dailyRange_startDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_month",
            dailyRange_startDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailyRange_startDate_year",
            dailyRange_startDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailySubEntityType_dayOfWeek",
            dailySubEntityType_dayOfWeek
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailySubEntityType_timeOfDay_hours",
            dailySubEntityType_timeOfDay_hours
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailySubEntityType_timeOfDay_minutes",
            dailySubEntityType_timeOfDay_minutes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailySubEntityType_timeOfDay_nanos",
            dailySubEntityType_timeOfDay_nanos
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dailySubEntityType_timeOfDay_seconds",
            dailySubEntityType_timeOfDay_seconds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `businessprofileperformance_locations_get_daily_metrics_time_series_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from businessprofileperformance_locations_get_daily_metrics_time_series_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:getDailyMetricsTimeSeries
/// Returns the values for each date from a given time range that are associated with the specific daily metric. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345`:`getDailyMetricsTimeSeries`?`dailyMetric`=WEBSITE_CLICKS&daily_range.start_date.year=2022&daily_range.start_date.month=1&daily_range.start_date.day=1&daily_range.end_date.year=2022&daily_range.end_date.month=3&daily_range.end_date.day=31>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_get_daily_metrics_time_series_builder()` + `businessprofileperformance_locations_get_daily_metrics_time_series_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_get_daily_metrics_time_series(
    client: &SimpleHttpClient,
    name: &str,
    dailyMetric: Option<&str>,
    dailyRange_endDate_day: Option<i32>,
    dailyRange_endDate_month: Option<i32>,
    dailyRange_endDate_year: Option<i32>,
    dailyRange_startDate_day: Option<i32>,
    dailyRange_startDate_month: Option<i32>,
    dailyRange_startDate_year: Option<i32>,
    dailySubEntityType_dayOfWeek: Option<&str>,
    dailySubEntityType_timeOfDay_hours: Option<i32>,
    dailySubEntityType_timeOfDay_minutes: Option<i32>,
    dailySubEntityType_timeOfDay_nanos: Option<i32>,
    dailySubEntityType_timeOfDay_seconds: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = businessprofileperformance_locations_get_daily_metrics_time_series_builder(
        client,
        name,
        dailyMetric,
        dailyRange_endDate_day,
        dailyRange_endDate_month,
        dailyRange_endDate_year,
        dailyRange_startDate_day,
        dailyRange_startDate_month,
        dailyRange_startDate_year,
        dailySubEntityType_dayOfWeek,
        dailySubEntityType_timeOfDay_hours,
        dailySubEntityType_timeOfDay_minutes,
        dailySubEntityType_timeOfDay_nanos,
        dailySubEntityType_timeOfDay_seconds,
    )?;
    businessprofileperformance_locations_get_daily_metrics_time_series_execute(builder)
}

/// GET v1/{+parent}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute()` to send, or `businessprofileperformance_locations_searchkeywords_impressions_monthly_list` for simplest API.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    monthlyRange_endMonth_day: Option<i32>,
    monthlyRange_endMonth_month: Option<i32>,
    monthlyRange_endMonth_year: Option<i32>,
    monthlyRange_startMonth_day: Option<i32>,
    monthlyRange_startMonth_month: Option<i32>,
    monthlyRange_startMonth_year: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/searchkeywords/impressions/monthly",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "monthlyRange_endMonth_day",
            monthlyRange_endMonth_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "monthlyRange_endMonth_month",
            monthlyRange_endMonth_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "monthlyRange_endMonth_year",
            monthlyRange_endMonth_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "monthlyRange_startMonth_day",
            monthlyRange_startMonth_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "monthlyRange_startMonth_month",
            monthlyRange_startMonth_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "monthlyRange_startMonth_year",
            monthlyRange_startMonth_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSearchKeywordImpressionsMonthlyResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/searchkeywords/impressions/monthly
/// Returns the search keywords used to find a business in search or maps. Each search keyword is accompanied by impressions which are aggregated on a monthly basis. Example request: GET <https://businessprofileperformance.googleapis.`com/v1/locations/12345/searchkeywords/impressions/monthly`?monthly_range.start_month.year=2022&monthly_range.start_month.month=1&monthly_range.end_month.year=2022&monthly_range.end_month.month=3>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder()` + `businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list(
    client: &SimpleHttpClient,
    parent: &str,
    monthlyRange_endMonth_day: Option<i32>,
    monthlyRange_endMonth_month: Option<i32>,
    monthlyRange_endMonth_year: Option<i32>,
    monthlyRange_startMonth_day: Option<i32>,
    monthlyRange_startMonth_month: Option<i32>,
    monthlyRange_startMonth_year: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        businessprofileperformance_locations_searchkeywords_impressions_monthly_list_builder(
            client,
            parent,
            monthlyRange_endMonth_day,
            monthlyRange_endMonth_month,
            monthlyRange_endMonth_year,
            monthlyRange_startMonth_day,
            monthlyRange_startMonth_month,
            monthlyRange_startMonth_year,
            pageSize,
            pageToken,
        )?;
    businessprofileperformance_locations_searchkeywords_impressions_monthly_list_execute(builder)
}
