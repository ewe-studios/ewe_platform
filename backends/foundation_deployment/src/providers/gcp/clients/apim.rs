//! Auto-generated API clients for gcp/apim.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_get_execute()` to send, or `apim_projects_locations_get` for simplest API.

pub fn apim_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_get_execute()` or `apim_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_get_task()`.
/// For the simplest API, use `apim_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_get_builder()` + `apim_projects_locations_get_execute()`.
/// For task-level control, use `apim_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_get_builder(client, &args.name)?;
    apim_projects_locations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/entitlement
/// GetEntitlement returns the entitlement for the provided project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_get_entitlement_execute()` to send, or `apim_projects_locations_get_entitlement` for simplest API.

pub fn apim_projects_locations_get_entitlement_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/entitlement",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/entitlement
/// GetEntitlement returns the entitlement for the provided project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_get_entitlement_execute()` or `apim_projects_locations_get_entitlement`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_get_entitlement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_get_entitlement_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entitlement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/entitlement
/// GetEntitlement returns the entitlement for the provided project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_get_entitlement_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_get_entitlement_task()`.
/// For the simplest API, use `apim_projects_locations_get_entitlement()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_get_entitlement_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_get_entitlement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_get_entitlement_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_get_entitlement`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsGetEntitlementArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/entitlement
/// GetEntitlement returns the entitlement for the provided project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_get_entitlement_builder()` + `apim_projects_locations_get_entitlement_execute()`.
/// For task-level control, use `apim_projects_locations_get_entitlement_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_get_entitlement(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsGetEntitlementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_get_entitlement_builder(client, &args.name)?;
    apim_projects_locations_get_entitlement_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_list_execute()` to send, or `apim_projects_locations_list` for simplest API.

pub fn apim_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_list_execute()` or `apim_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_list_task()`.
/// For the simplest API, use `apim_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_list_builder()` + `apim_projects_locations_list_execute()`.
/// For task-level control, use `apim_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    apim_projects_locations_list_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}:listApiObservationTags
/// ListApiObservationTags lists all extant tags on any observation in the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_list_api_observation_tags_execute()` to send, or `apim_projects_locations_list_api_observation_tags` for simplest API.

pub fn apim_projects_locations_list_api_observation_tags_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}:listApiObservationTags",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}:listApiObservationTags
/// ListApiObservationTags lists all extant tags on any observation in the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_list_api_observation_tags_execute()` or `apim_projects_locations_list_api_observation_tags`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_list_api_observation_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_list_api_observation_tags_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiObservationTagsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiObservationTagsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}:listApiObservationTags
/// ListApiObservationTags lists all extant tags on any observation in the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_list_api_observation_tags_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_list_api_observation_tags_task()`.
/// For the simplest API, use `apim_projects_locations_list_api_observation_tags()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_list_api_observation_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_list_api_observation_tags_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiObservationTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_list_api_observation_tags_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_list_api_observation_tags`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsListApiObservationTagsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}:listApiObservationTags
/// ListApiObservationTags lists all extant tags on any observation in the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_list_api_observation_tags_builder()` + `apim_projects_locations_list_api_observation_tags_execute()`.
/// For task-level control, use `apim_projects_locations_list_api_observation_tags_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_list_api_observation_tags(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsListApiObservationTagsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiObservationTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_list_api_observation_tags_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    apim_projects_locations_list_api_observation_tags_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// CreateObservationJob creates a new ObservationJob but does not have any effecton its own. It is a configuration that can be used in an Observation Job to collect data about existing APIs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_create_execute()` to send, or `apim_projects_locations_observation_jobs_create` for simplest API.

pub fn apim_projects_locations_observation_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    observationJobId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = observationJobId.as_ref() {
        query_parts.push(format!("observationJobId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// CreateObservationJob creates a new ObservationJob but does not have any effecton its own. It is a configuration that can be used in an Observation Job to collect data about existing APIs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_create_execute()` or `apim_projects_locations_observation_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// CreateObservationJob creates a new ObservationJob but does not have any effecton its own. It is a configuration that can be used in an Observation Job to collect data about existing APIs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_create_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: observationJobId
    pub observationJobId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// CreateObservationJob creates a new ObservationJob but does not have any effecton its own. It is a configuration that can be used in an Observation Job to collect data about existing APIs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_create_builder()` + `apim_projects_locations_observation_jobs_create_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_create(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_create_builder(
        client,
        &args.parent,
        &args.observationJobId,
        &args.requestId,
    )?;
    apim_projects_locations_observation_jobs_create_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// DeleteObservationJob deletes an ObservationJob. This method will fail if the observation job is currently being used by any ObservationSource, even if not enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_delete_execute()` to send, or `apim_projects_locations_observation_jobs_delete` for simplest API.

pub fn apim_projects_locations_observation_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// DeleteObservationJob deletes an ObservationJob. This method will fail if the observation job is currently being used by any ObservationSource, even if not enabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_delete_execute()` or `apim_projects_locations_observation_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// DeleteObservationJob deletes an ObservationJob. This method will fail if the observation job is currently being used by any ObservationSource, even if not enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_delete_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// DeleteObservationJob deletes an ObservationJob. This method will fail if the observation job is currently being used by any ObservationSource, even if not enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_delete_builder()` + `apim_projects_locations_observation_jobs_delete_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_delete(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_delete_builder(client, &args.name)?;
    apim_projects_locations_observation_jobs_delete_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:disable
/// Disables the given ObservationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_disable_execute()` to send, or `apim_projects_locations_observation_jobs_disable` for simplest API.

pub fn apim_projects_locations_observation_jobs_disable_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}:disable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:disable
/// Disables the given ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_disable_execute()` or `apim_projects_locations_observation_jobs_disable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_disable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:disable
/// Disables the given ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_disable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_disable_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_disable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_disable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsDisableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:disable
/// Disables the given ObservationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_disable_builder()` + `apim_projects_locations_observation_jobs_disable_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_disable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_disable(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsDisableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_disable_builder(client, &args.name)?;
    apim_projects_locations_observation_jobs_disable_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:enable
/// Enables the given ObservationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_enable_execute()` to send, or `apim_projects_locations_observation_jobs_enable` for simplest API.

pub fn apim_projects_locations_observation_jobs_enable_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}:enable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:enable
/// Enables the given ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_enable_execute()` or `apim_projects_locations_observation_jobs_enable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_enable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:enable
/// Enables the given ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_enable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_enable_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_enable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_enable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsEnableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}:enable
/// Enables the given ObservationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_enable_builder()` + `apim_projects_locations_observation_jobs_enable_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_enable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_enable(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsEnableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_enable_builder(client, &args.name)?;
    apim_projects_locations_observation_jobs_enable_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// GetObservationJob retrieves a single ObservationJob by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_get_execute()` to send, or `apim_projects_locations_observation_jobs_get` for simplest API.

pub fn apim_projects_locations_observation_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// GetObservationJob retrieves a single ObservationJob by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_get_execute()` or `apim_projects_locations_observation_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObservationJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ObservationJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// GetObservationJob retrieves a single ObservationJob by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_get_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ObservationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}
/// GetObservationJob retrieves a single ObservationJob by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_get_builder()` + `apim_projects_locations_observation_jobs_get_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ObservationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_get_builder(client, &args.name)?;
    apim_projects_locations_observation_jobs_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// ListObservationJobs gets all ObservationJobs for a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_list_execute()` to send, or `apim_projects_locations_observation_jobs_list` for simplest API.

pub fn apim_projects_locations_observation_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// ListObservationJobs gets all ObservationJobs for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_list_execute()` or `apim_projects_locations_observation_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListObservationJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListObservationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// ListObservationJobs gets all ObservationJobs for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_list_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListObservationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs
/// ListObservationJobs gets all ObservationJobs for a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_list_builder()` + `apim_projects_locations_observation_jobs_list_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListObservationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    apim_projects_locations_observation_jobs_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations:batchEditTags
/// BatchEditTagsApiObservations adds or removes Tags for ApiObservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_execute()` to send, or `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags` for simplest API.

pub fn apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations:batchEditTags",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations:batchEditTags
/// BatchEditTagsApiObservations adds or removes Tags for ApiObservations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_execute()` or `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchEditTagsApiObservationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchEditTagsApiObservationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations:batchEditTags
/// BatchEditTagsApiObservations adds or removes Tags for ApiObservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchEditTagsApiObservationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_api_observations_batch_edit_tags`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsApiObservationsBatchEditTagsArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations:batchEditTags
/// BatchEditTagsApiObservations adds or removes Tags for ApiObservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder()` + `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_batch_edit_tags(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsApiObservationsBatchEditTagsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchEditTagsApiObservationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_builder(
            client,
            &args.parent,
        )?;
    apim_projects_locations_observation_jobs_api_observations_batch_edit_tags_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}
/// GetApiObservation retrieves a single ApiObservation by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_api_observations_get_execute()` to send, or `apim_projects_locations_observation_jobs_api_observations_get` for simplest API.

pub fn apim_projects_locations_observation_jobs_api_observations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}
/// GetApiObservation retrieves a single ApiObservation by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_api_observations_get_execute()` or `apim_projects_locations_observation_jobs_api_observations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiObservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiObservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}
/// GetApiObservation retrieves a single ApiObservation by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_api_observations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_get_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_api_observations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_api_observations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiObservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_api_observations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_api_observations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsApiObservationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}
/// GetApiObservation retrieves a single ApiObservation by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_api_observations_get_builder()` + `apim_projects_locations_observation_jobs_api_observations_get_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsApiObservationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiObservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apim_projects_locations_observation_jobs_api_observations_get_builder(client, &args.name)?;
    apim_projects_locations_observation_jobs_api_observations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations
/// ListApiObservations gets all ApiObservations for a given project and location and ObservationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_api_observations_list_execute()` to send, or `apim_projects_locations_observation_jobs_api_observations_list` for simplest API.

pub fn apim_projects_locations_observation_jobs_api_observations_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations
/// ListApiObservations gets all ApiObservations for a given project and location and ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_api_observations_list_execute()` or `apim_projects_locations_observation_jobs_api_observations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiObservationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiObservationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations
/// ListApiObservations gets all ApiObservations for a given project and location and ObservationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_api_observations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_list_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_api_observations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_api_observations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiObservationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_api_observations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_api_observations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsApiObservationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations
/// ListApiObservations gets all ApiObservations for a given project and location and ObservationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_api_observations_list_builder()` + `apim_projects_locations_observation_jobs_api_observations_list_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsApiObservationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiObservationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_jobs_api_observations_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    apim_projects_locations_observation_jobs_api_observations_list_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations/{apiOperationsId}
/// GetApiOperation retrieves a single ApiOperation by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_execute()` to send, or `apim_projects_locations_observation_jobs_api_observations_api_operations_get` for simplest API.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations/{apiOperationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations/{apiOperationsId}
/// GetApiOperation retrieves a single ApiOperation by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_execute()` or `apim_projects_locations_observation_jobs_api_observations_api_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations/{apiOperationsId}
/// GetApiOperation retrieves a single ApiOperation by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        apim_projects_locations_observation_jobs_api_observations_api_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_api_observations_api_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsApiObservationsApiOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations/{apiOperationsId}
/// GetApiOperation retrieves a single ApiOperation by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder()` + `apim_projects_locations_observation_jobs_api_observations_api_operations_get_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_api_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsApiObservationsApiOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apim_projects_locations_observation_jobs_api_observations_api_operations_get_builder(
            client, &args.name,
        )?;
    apim_projects_locations_observation_jobs_api_observations_api_operations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations
/// ListApiOperations gets all ApiOperations for a given project and location and ObservationJob and ApiObservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_execute()` to send, or `apim_projects_locations_observation_jobs_api_observations_api_operations_list` for simplest API.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations
/// ListApiOperations gets all ApiOperations for a given project and location and ObservationJob and ApiObservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_execute()` or `apim_projects_locations_observation_jobs_api_observations_api_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations
/// ListApiOperations gets all ApiOperations for a given project and location and ObservationJob and ApiObservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_task()`.
/// For the simplest API, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_jobs_api_observations_api_operations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_jobs_api_observations_api_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationJobsApiObservationsApiOperationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationJobs/{observationJobsId}/apiObservations/{apiObservationsId}/apiOperations
/// ListApiOperations gets all ApiOperations for a given project and location and ObservationJob and ApiObservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder()` + `apim_projects_locations_observation_jobs_api_observations_api_operations_list_execute()`.
/// For task-level control, use `apim_projects_locations_observation_jobs_api_observations_api_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_jobs_api_observations_api_operations_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationJobsApiObservationsApiOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apim_projects_locations_observation_jobs_api_observations_api_operations_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    apim_projects_locations_observation_jobs_api_observations_api_operations_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// CreateObservationSource creates a new ObservationSource but does not affect any deployed infrastructure. It is a configuration that can be used in an Observation Job to collect data about APIs running in user's dataplane.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_sources_create_execute()` to send, or `apim_projects_locations_observation_sources_create` for simplest API.

pub fn apim_projects_locations_observation_sources_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    observationSourceId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationSources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = observationSourceId.as_ref() {
        query_parts.push(format!("observationSourceId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// CreateObservationSource creates a new ObservationSource but does not affect any deployed infrastructure. It is a configuration that can be used in an Observation Job to collect data about APIs running in user's dataplane.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_sources_create_execute()` or `apim_projects_locations_observation_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// CreateObservationSource creates a new ObservationSource but does not affect any deployed infrastructure. It is a configuration that can be used in an Observation Job to collect data about APIs running in user's dataplane.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_sources_create_task()`.
/// For the simplest API, use `apim_projects_locations_observation_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationSourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: observationSourceId
    pub observationSourceId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// CreateObservationSource creates a new ObservationSource but does not affect any deployed infrastructure. It is a configuration that can be used in an Observation Job to collect data about APIs running in user's dataplane.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_sources_create_builder()` + `apim_projects_locations_observation_sources_create_execute()`.
/// For task-level control, use `apim_projects_locations_observation_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_create(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_sources_create_builder(
        client,
        &args.parent,
        &args.observationSourceId,
        &args.requestId,
    )?;
    apim_projects_locations_observation_sources_create_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// DeleteObservationSource deletes an observation source. This method will fail if the observation source is currently being used by any ObservationJob, even if not enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_sources_delete_execute()` to send, or `apim_projects_locations_observation_sources_delete` for simplest API.

pub fn apim_projects_locations_observation_sources_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationSources/{observationSourcesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// DeleteObservationSource deletes an observation source. This method will fail if the observation source is currently being used by any ObservationJob, even if not enabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_sources_delete_execute()` or `apim_projects_locations_observation_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// DeleteObservationSource deletes an observation source. This method will fail if the observation source is currently being used by any ObservationJob, even if not enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_sources_delete_task()`.
/// For the simplest API, use `apim_projects_locations_observation_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationSourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// DeleteObservationSource deletes an observation source. This method will fail if the observation source is currently being used by any ObservationJob, even if not enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_sources_delete_builder()` + `apim_projects_locations_observation_sources_delete_execute()`.
/// For task-level control, use `apim_projects_locations_observation_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_delete(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationSourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_sources_delete_builder(client, &args.name)?;
    apim_projects_locations_observation_sources_delete_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// GetObservationSource retrieves a single ObservationSource by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_sources_get_execute()` to send, or `apim_projects_locations_observation_sources_get` for simplest API.

pub fn apim_projects_locations_observation_sources_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationSources/{observationSourcesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// GetObservationSource retrieves a single ObservationSource by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_sources_get_execute()` or `apim_projects_locations_observation_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObservationSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ObservationSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// GetObservationSource retrieves a single ObservationSource by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_sources_get_task()`.
/// For the simplest API, use `apim_projects_locations_observation_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ObservationSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationSourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources/{observationSourcesId}
/// GetObservationSource retrieves a single ObservationSource by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_sources_get_builder()` + `apim_projects_locations_observation_sources_get_execute()`.
/// For task-level control, use `apim_projects_locations_observation_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ObservationSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_sources_get_builder(client, &args.name)?;
    apim_projects_locations_observation_sources_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// ListObservationSources gets all ObservationSources for a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_observation_sources_list_execute()` to send, or `apim_projects_locations_observation_sources_list` for simplest API.

pub fn apim_projects_locations_observation_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/observationSources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// ListObservationSources gets all ObservationSources for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_observation_sources_list_execute()` or `apim_projects_locations_observation_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListObservationSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListObservationSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// ListObservationSources gets all ObservationSources for a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_observation_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_observation_sources_list_task()`.
/// For the simplest API, use `apim_projects_locations_observation_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_observation_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_observation_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListObservationSourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_observation_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_observation_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsObservationSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/observationSources
/// ListObservationSources gets all ObservationSources for a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_observation_sources_list_builder()` + `apim_projects_locations_observation_sources_list_execute()`.
/// For task-level control, use `apim_projects_locations_observation_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_observation_sources_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsObservationSourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListObservationSourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_observation_sources_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    apim_projects_locations_observation_sources_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_operations_cancel_execute()` to send, or `apim_projects_locations_operations_cancel` for simplest API.

pub fn apim_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_operations_cancel_execute()` or `apim_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `apim_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_operations_cancel_builder()` + `apim_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `apim_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_operations_cancel_builder(client, &args.name)?;
    apim_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_operations_delete_execute()` to send, or `apim_projects_locations_operations_delete` for simplest API.

pub fn apim_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_operations_delete_execute()` or `apim_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_operations_delete_task()`.
/// For the simplest API, use `apim_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_operations_delete_builder()` + `apim_projects_locations_operations_delete_execute()`.
/// For task-level control, use `apim_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_operations_delete_builder(client, &args.name)?;
    apim_projects_locations_operations_delete_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_operations_get_execute()` to send, or `apim_projects_locations_operations_get` for simplest API.

pub fn apim_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_operations_get_execute()` or `apim_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_operations_get_task()`.
/// For the simplest API, use `apim_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apim_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_operations_get_builder()` + `apim_projects_locations_operations_get_execute()`.
/// For task-level control, use `apim_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_operations_get_builder(client, &args.name)?;
    apim_projects_locations_operations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apim_projects_locations_operations_list_execute()` to send, or `apim_projects_locations_operations_list` for simplest API.

pub fn apim_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apim.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apim_projects_locations_operations_list_execute()` or `apim_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apim_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apim_projects_locations_operations_list_task()`.
/// For the simplest API, use `apim_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apim_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apim_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apim_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apim_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApimProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apim_projects_locations_operations_list_builder()` + `apim_projects_locations_operations_list_execute()`.
/// For task-level control, use `apim_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apim_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ApimProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apim_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    apim_projects_locations_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with ApimProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsGetArgs) -> String {
        format!("gcp::apim::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Entitlement
// =============================================================================

/// ResourceIdentifier implementation for Entitlement with ApimProjectsLocationsGetEntitlementArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsGetEntitlementArgs> for Entitlement {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsGetEntitlementArgs) -> String {
        format!("gcp::apim::Entitlement/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Entitlement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with ApimProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsListArgs) -> String {
        format!("gcp::apim::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiObservationTagsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiObservationTagsResponse with ApimProjectsLocationsListApiObservationTagsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsListApiObservationTagsArgs>
    for ListApiObservationTagsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsListApiObservationTagsArgs,
    ) -> String {
        format!("gcp::apim::ListApiObservationTagsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListApiObservationTagsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsCreateArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsDeleteArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationJobsDisableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsDisableArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsDisableArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationJobsEnableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsEnableArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsEnableArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ObservationJob
// =============================================================================

/// ResourceIdentifier implementation for ObservationJob with ApimProjectsLocationsObservationJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsGetArgs> for ObservationJob {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsObservationJobsGetArgs) -> String {
        format!("gcp::apim::ObservationJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ObservationJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListObservationJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListObservationJobsResponse with ApimProjectsLocationsObservationJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsListArgs>
    for ListObservationJobsResponse
{
    fn generate_resource_id(&self, input: &ApimProjectsLocationsObservationJobsListArgs) -> String {
        format!("gcp::apim::ListObservationJobsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListObservationJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchEditTagsApiObservationsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchEditTagsApiObservationsResponse with ApimProjectsLocationsObservationJobsApiObservationsBatchEditTagsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsApiObservationsBatchEditTagsArgs>
    for BatchEditTagsApiObservationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsApiObservationsBatchEditTagsArgs,
    ) -> String {
        format!(
            "gcp::apim::BatchEditTagsApiObservationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::BatchEditTagsApiObservationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiObservation
// =============================================================================

/// ResourceIdentifier implementation for ApiObservation with ApimProjectsLocationsObservationJobsApiObservationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsApiObservationsGetArgs>
    for ApiObservation
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsApiObservationsGetArgs,
    ) -> String {
        format!("gcp::apim::ApiObservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ApiObservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiObservationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiObservationsResponse with ApimProjectsLocationsObservationJobsApiObservationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsApiObservationsListArgs>
    for ListApiObservationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsApiObservationsListArgs,
    ) -> String {
        format!("gcp::apim::ListApiObservationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListApiObservationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiOperation
// =============================================================================

/// ResourceIdentifier implementation for ApiOperation with ApimProjectsLocationsObservationJobsApiObservationsApiOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsApiObservationsApiOperationsGetArgs>
    for ApiOperation
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsApiObservationsApiOperationsGetArgs,
    ) -> String {
        format!("gcp::apim::ApiOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ApiOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiOperationsResponse with ApimProjectsLocationsObservationJobsApiObservationsApiOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationJobsApiObservationsApiOperationsListArgs>
    for ListApiOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationJobsApiObservationsApiOperationsListArgs,
    ) -> String {
        format!("gcp::apim::ListApiOperationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListApiOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationSourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationSourcesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationSourcesCreateArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsObservationSourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationSourcesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationSourcesDeleteArgs,
    ) -> String {
        format!("gcp::apim::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ObservationSource
// =============================================================================

/// ResourceIdentifier implementation for ObservationSource with ApimProjectsLocationsObservationSourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationSourcesGetArgs> for ObservationSource {
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationSourcesGetArgs,
    ) -> String {
        format!("gcp::apim::ObservationSource/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ObservationSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListObservationSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListObservationSourcesResponse with ApimProjectsLocationsObservationSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsObservationSourcesListArgs>
    for ListObservationSourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &ApimProjectsLocationsObservationSourcesListArgs,
    ) -> String {
        format!("gcp::apim::ListObservationSourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListObservationSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApimProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsOperationsCancelArgs) -> String {
        format!("gcp::apim::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApimProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsOperationsDeleteArgs) -> String {
        format!("gcp::apim::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApimProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::apim::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with ApimProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApimProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &ApimProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::apim::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apim::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
