//! Auto-generated API clients for gcp/transcoder.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/transcoder`

#![cfg(feature = "gcp/transcoder")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_create_execute()` to send, or `transcoder_projects_locations_job_templates_create` for simplest API.

pub fn transcoder_projects_locations_job_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    jobTemplateId: Option<&str>,
    body: &JobTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobTemplateId {
        query_parts.push(format!("jobTemplateId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_job_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_job_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_create_builder()` + `transcoder_projects_locations_job_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    jobTemplateId: Option<&str>,
    body: &JobTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_create_builder(
        client,
        parent,
        jobTemplateId,
        body,
    )?;
    transcoder_projects_locations_job_templates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_delete_execute()` to send, or `transcoder_projects_locations_job_templates_delete` for simplest API.

pub fn transcoder_projects_locations_job_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_job_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_job_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_delete_builder()` + `transcoder_projects_locations_job_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        transcoder_projects_locations_job_templates_delete_builder(client, name, allowMissing)?;
    transcoder_projects_locations_job_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_get_execute()` to send, or `transcoder_projects_locations_job_templates_get` for simplest API.

pub fn transcoder_projects_locations_job_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_job_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_job_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_get_builder()` + `transcoder_projects_locations_job_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_get_builder(client, name)?;
    transcoder_projects_locations_job_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_list_execute()` to send, or `transcoder_projects_locations_job_templates_list` for simplest API.

pub fn transcoder_projects_locations_job_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_job_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_job_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobTemplatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_list_builder()` + `transcoder_projects_locations_job_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobTemplatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    transcoder_projects_locations_job_templates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_create_execute()` to send, or `transcoder_projects_locations_jobs_create` for simplest API.

pub fn transcoder_projects_locations_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_create_builder()` + `transcoder_projects_locations_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_create_builder(client, parent, body)?;
    transcoder_projects_locations_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_delete_execute()` to send, or `transcoder_projects_locations_jobs_delete` for simplest API.

pub fn transcoder_projects_locations_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_delete_builder()` + `transcoder_projects_locations_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_delete_builder(client, name, allowMissing)?;
    transcoder_projects_locations_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_get_execute()` to send, or `transcoder_projects_locations_jobs_get` for simplest API.

pub fn transcoder_projects_locations_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_get_builder()` + `transcoder_projects_locations_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_get_builder(client, name)?;
    transcoder_projects_locations_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_list_execute()` to send, or `transcoder_projects_locations_jobs_list` for simplest API.

pub fn transcoder_projects_locations_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{}/jobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `transcoder_projects_locations_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from transcoder_projects_locations_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_list_builder()` + `transcoder_projects_locations_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    transcoder_projects_locations_jobs_list_execute(builder)
}
