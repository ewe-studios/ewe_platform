//! Auto-generated API clients for gcp/transcoder.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_create_execute()` to send, or `transcoder_projects_locations_job_templates_create` for simplest API.

pub fn transcoder_projects_locations_job_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    jobTemplateId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobTemplateId.as_ref() {
        query_parts.push(format!("jobTemplateId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_job_templates_create_execute()` or `transcoder_projects_locations_job_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_job_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_job_templates_create_task()`.
/// For the simplest API, use `transcoder_projects_locations_job_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_job_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_job_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_job_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: jobTemplateId
    pub jobTemplateId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Creates a job template in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_create_builder()` + `transcoder_projects_locations_job_templates_create_execute()`.
/// For task-level control, use `transcoder_projects_locations_job_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_create(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_create_builder(
        client,
        &args.parent,
        &args.jobTemplateId,
    )?;
    transcoder_projects_locations_job_templates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_delete_execute()` to send, or `transcoder_projects_locations_job_templates_delete` for simplest API.

pub fn transcoder_projects_locations_job_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobTemplates/{jobTemplatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_job_templates_delete_execute()` or `transcoder_projects_locations_job_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_job_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_job_templates_delete_task()`.
/// For the simplest API, use `transcoder_projects_locations_job_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_job_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_job_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_job_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Deletes a job template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_delete_builder()` + `transcoder_projects_locations_job_templates_delete_execute()`.
/// For task-level control, use `transcoder_projects_locations_job_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_delete(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
    )?;
    transcoder_projects_locations_job_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_get_execute()` to send, or `transcoder_projects_locations_job_templates_get` for simplest API.

pub fn transcoder_projects_locations_job_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobTemplates/{jobTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_job_templates_get_execute()` or `transcoder_projects_locations_job_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_job_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_job_templates_get_task()`.
/// For the simplest API, use `transcoder_projects_locations_job_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_job_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_job_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_job_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates/{jobTemplatesId}
/// Returns the job template data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_get_builder()` + `transcoder_projects_locations_job_templates_get_execute()`.
/// For task-level control, use `transcoder_projects_locations_job_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_get(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobTemplate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_get_builder(client, &args.name)?;
    transcoder_projects_locations_job_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_job_templates_list_execute()` to send, or `transcoder_projects_locations_job_templates_list` for simplest API.

pub fn transcoder_projects_locations_job_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_job_templates_list_execute()` or `transcoder_projects_locations_job_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_job_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_job_templates_list_task()`.
/// For the simplest API, use `transcoder_projects_locations_job_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_job_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_job_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobTemplatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_job_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_job_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobTemplates
/// Lists job templates in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_job_templates_list_builder()` + `transcoder_projects_locations_job_templates_list_execute()`.
/// For task-level control, use `transcoder_projects_locations_job_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_job_templates_list(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobTemplatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobTemplatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_job_templates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    transcoder_projects_locations_job_templates_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_create_execute()` to send, or `transcoder_projects_locations_jobs_create` for simplest API.

pub fn transcoder_projects_locations_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_jobs_create_execute()` or `transcoder_projects_locations_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_jobs_create_task()`.
/// For the simplest API, use `transcoder_projects_locations_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Creates a job in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_create_builder()` + `transcoder_projects_locations_jobs_create_execute()`.
/// For task-level control, use `transcoder_projects_locations_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_create(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_create_builder(client, &args.parent)?;
    transcoder_projects_locations_jobs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_delete_execute()` to send, or `transcoder_projects_locations_jobs_delete` for simplest API.

pub fn transcoder_projects_locations_jobs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobs/{jobsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_jobs_delete_execute()` or `transcoder_projects_locations_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_jobs_delete_task()`.
/// For the simplest API, use `transcoder_projects_locations_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Deletes a job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_delete_builder()` + `transcoder_projects_locations_jobs_delete_execute()`.
/// For task-level control, use `transcoder_projects_locations_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_delete(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        transcoder_projects_locations_jobs_delete_builder(client, &args.name, &args.allowMissing)?;
    transcoder_projects_locations_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_get_execute()` to send, or `transcoder_projects_locations_jobs_get` for simplest API.

pub fn transcoder_projects_locations_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobs/{jobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_jobs_get_execute()` or `transcoder_projects_locations_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_jobs_get_task()`.
/// For the simplest API, use `transcoder_projects_locations_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
/// Returns the job data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_get_builder()` + `transcoder_projects_locations_jobs_get_execute()`.
/// For task-level control, use `transcoder_projects_locations_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_get(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_get_builder(client, &args.name)?;
    transcoder_projects_locations_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `transcoder_projects_locations_jobs_list_execute()` to send, or `transcoder_projects_locations_jobs_list` for simplest API.

pub fn transcoder_projects_locations_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{}/locations/{locationsId}/jobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `transcoder_projects_locations_jobs_list_execute()` or `transcoder_projects_locations_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `transcoder_projects_locations_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `transcoder_projects_locations_jobs_list_task()`.
/// For the simplest API, use `transcoder_projects_locations_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `transcoder_projects_locations_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn transcoder_projects_locations_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = transcoder_projects_locations_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`transcoder_projects_locations_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
/// Lists jobs in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `transcoder_projects_locations_jobs_list_builder()` + `transcoder_projects_locations_jobs_list_execute()`.
/// For task-level control, use `transcoder_projects_locations_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn transcoder_projects_locations_jobs_list(
    client: &SimpleHttpClient,
    args: &TranscoderProjectsLocationsJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = transcoder_projects_locations_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    transcoder_projects_locations_jobs_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for JobTemplate
// =============================================================================

/// ResourceIdentifier implementation for JobTemplate with TranscoderProjectsLocationsJobTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobTemplatesCreateArgs> for JobTemplate {
    fn generate_resource_id(
        &self,
        input: &TranscoderProjectsLocationsJobTemplatesCreateArgs,
    ) -> String {
        format!("gcp::transcoder::JobTemplate/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::JobTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with TranscoderProjectsLocationsJobTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobTemplatesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &TranscoderProjectsLocationsJobTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::transcoder::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for JobTemplate
// =============================================================================

/// ResourceIdentifier implementation for JobTemplate with TranscoderProjectsLocationsJobTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobTemplatesGetArgs> for JobTemplate {
    fn generate_resource_id(
        &self,
        input: &TranscoderProjectsLocationsJobTemplatesGetArgs,
    ) -> String {
        format!("gcp::transcoder::JobTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::JobTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListJobTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListJobTemplatesResponse with TranscoderProjectsLocationsJobTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobTemplatesListArgs>
    for ListJobTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &TranscoderProjectsLocationsJobTemplatesListArgs,
    ) -> String {
        format!("gcp::transcoder::ListJobTemplatesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::ListJobTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Job
// =============================================================================

/// ResourceIdentifier implementation for Job with TranscoderProjectsLocationsJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobsCreateArgs> for Job {
    fn generate_resource_id(&self, input: &TranscoderProjectsLocationsJobsCreateArgs) -> String {
        format!("gcp::transcoder::Job/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::Job"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with TranscoderProjectsLocationsJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &TranscoderProjectsLocationsJobsDeleteArgs) -> String {
        format!("gcp::transcoder::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Job
// =============================================================================

/// ResourceIdentifier implementation for Job with TranscoderProjectsLocationsJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobsGetArgs> for Job {
    fn generate_resource_id(&self, input: &TranscoderProjectsLocationsJobsGetArgs) -> String {
        format!("gcp::transcoder::Job/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::Job"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListJobsResponse with TranscoderProjectsLocationsJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TranscoderProjectsLocationsJobsListArgs> for ListJobsResponse {
    fn generate_resource_id(&self, input: &TranscoderProjectsLocationsJobsListArgs) -> String {
        format!("gcp::transcoder::ListJobsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::transcoder::ListJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
