//! Auto-generated API clients for gcp/bigquery.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET projects/{projectsId}/datasets/{datasetsId}
/// Deletes the dataset specified by the `datasetId` value. Before you can delete a dataset, you must delete all its tables, either manually or by specifying `deleteContents`. Immediately after deletion, you can create another dataset with the same name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_datasets_delete_execute()` to send, or `bigquery_datasets_delete` for simplest API.

pub fn bigquery_datasets_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    deleteContents: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deleteContents.as_ref() {
        query_parts.push(format!("deleteContents={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}
/// Deletes the dataset specified by the `datasetId` value. Before you can delete a dataset, you must delete all its tables, either manually or by specifying `deleteContents`. Immediately after deletion, you can create another dataset with the same name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_datasets_delete_execute()` or `bigquery_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}
/// Deletes the dataset specified by the `datasetId` value. Before you can delete a dataset, you must delete all its tables, either manually or by specifying `deleteContents`. Immediately after deletion, you can create another dataset with the same name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_datasets_delete_task()`.
/// For the simplest API, use `bigquery_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryDatasetsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Query parameter: deleteContents
    pub deleteContents: Option<bool>,
}

/// GET projects/{projectsId}/datasets/{datasetsId}
/// Deletes the dataset specified by the `datasetId` value. Before you can delete a dataset, you must delete all its tables, either manually or by specifying `deleteContents`. Immediately after deletion, you can create another dataset with the same name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_datasets_delete_builder()` + `bigquery_datasets_delete_execute()`.
/// For task-level control, use `bigquery_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_delete(
    client: &SimpleHttpClient,
    args: &BigqueryDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_datasets_delete_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.deleteContents,
    )?;
    bigquery_datasets_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets
/// Creates a new empty dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_datasets_insert_execute()` to send, or `bigquery_datasets_insert` for simplest API.

pub fn bigquery_datasets_insert_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    accessPolicyVersion: &Option<i32>,
    body: &Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accessPolicyVersion.as_ref() {
        query_parts.push(format!("accessPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets
/// Creates a new empty dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_datasets_insert_execute()` or `bigquery_datasets_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets
/// Creates a new empty dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_datasets_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_datasets_insert_task()`.
/// For the simplest API, use `bigquery_datasets_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_datasets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_datasets_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_datasets_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryDatasetsInsertArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: accessPolicyVersion
    pub accessPolicyVersion: Option<i32>,
    /// Request body.
    pub body: Dataset,
}

/// GET projects/{projectsId}/datasets
/// Creates a new empty dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_datasets_insert_builder()` + `bigquery_datasets_insert_execute()`.
/// For task-level control, use `bigquery_datasets_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_insert(
    client: &SimpleHttpClient,
    args: &BigqueryDatasetsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_datasets_insert_builder(
        client,
        &args.projectId,
        &args.accessPolicyVersion,
        &args.body,
    )?;
    bigquery_datasets_insert_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}:undelete
/// Undeletes a dataset which is within time travel window based on `datasetId`. If a time is specified, the dataset version deleted at that time is undeleted, else the last live version is undeleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_datasets_undelete_execute()` to send, or `bigquery_datasets_undelete` for simplest API.

pub fn bigquery_datasets_undelete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    body: &UndeleteDatasetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}:undelete",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}:undelete
/// Undeletes a dataset which is within time travel window based on `datasetId`. If a time is specified, the dataset version deleted at that time is undeleted, else the last live version is undeleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_datasets_undelete_execute()` or `bigquery_datasets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}:undelete
/// Undeletes a dataset which is within time travel window based on `datasetId`. If a time is specified, the dataset version deleted at that time is undeleted, else the last live version is undeleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_datasets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_datasets_undelete_task()`.
/// For the simplest API, use `bigquery_datasets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_datasets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_datasets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_datasets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_datasets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryDatasetsUndeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Request body.
    pub body: UndeleteDatasetRequest,
}

/// GET projects/{projectsId}/datasets/{datasetsId}:undelete
/// Undeletes a dataset which is within time travel window based on `datasetId`. If a time is specified, the dataset version deleted at that time is undeleted, else the last live version is undeleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_datasets_undelete_builder()` + `bigquery_datasets_undelete_execute()`.
/// For task-level control, use `bigquery_datasets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_datasets_undelete(
    client: &SimpleHttpClient,
    args: &BigqueryDatasetsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_datasets_undelete_builder(client, &args.projectId, &args.datasetId, &args.body)?;
    bigquery_datasets_undelete_execute(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}/cancel
/// Requests that a job be cancelled. This call will return immediately, and the client will need to poll for the job status to see if the cancel completed successfully. Cancelled jobs may still incur costs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_cancel_execute()` to send, or `bigquery_jobs_cancel` for simplest API.

pub fn bigquery_jobs_cancel_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    jobId: &String,
    location: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/jobs/{}/cancel",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location.as_ref() {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}/cancel
/// Requests that a job be cancelled. This call will return immediately, and the client will need to poll for the job status to see if the cancel completed successfully. Cancelled jobs may still incur costs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_cancel_execute()` or `bigquery_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobCancelResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobCancelResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/jobs/{jobsId}/cancel
/// Requests that a job be cancelled. This call will return immediately, and the client will need to poll for the job status to see if the cancel completed successfully. Cancelled jobs may still incur costs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_cancel_task()`.
/// For the simplest API, use `bigquery_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobCancelResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsCancelArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: location
    pub location: Option<String>,
}

/// GET projects/{projectsId}/jobs/{jobsId}/cancel
/// Requests that a job be cancelled. This call will return immediately, and the client will need to poll for the job status to see if the cancel completed successfully. Cancelled jobs may still incur costs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_cancel_builder()` + `bigquery_jobs_cancel_execute()`.
/// For task-level control, use `bigquery_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_cancel(
    client: &SimpleHttpClient,
    args: &BigqueryJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobCancelResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquery_jobs_cancel_builder(client, &args.projectId, &args.jobId, &args.location)?;
    bigquery_jobs_cancel_execute(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}/delete
/// Requests the deletion of the metadata of a job. This call returns when the job's metadata is deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_delete_execute()` to send, or `bigquery_jobs_delete` for simplest API.

pub fn bigquery_jobs_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    jobId: &String,
    location: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/jobs/{}/delete",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location.as_ref() {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}/delete
/// Requests the deletion of the metadata of a job. This call returns when the job's metadata is deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_delete_execute()` or `bigquery_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/jobs/{jobsId}/delete
/// Requests the deletion of the metadata of a job. This call returns when the job's metadata is deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_delete_task()`.
/// For the simplest API, use `bigquery_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: location
    pub location: Option<String>,
}

/// GET projects/{projectsId}/jobs/{jobsId}/delete
/// Requests the deletion of the metadata of a job. This call returns when the job's metadata is deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_delete_builder()` + `bigquery_jobs_delete_execute()`.
/// For task-level control, use `bigquery_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_delete(
    client: &SimpleHttpClient,
    args: &BigqueryJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_jobs_delete_builder(client, &args.projectId, &args.jobId, &args.location)?;
    bigquery_jobs_delete_execute(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}
/// Returns information about a specific job. Job information is available for a six month period after creation. Requires that you're the person who ran the job, or have the Is Owner project role.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_get_execute()` to send, or `bigquery_jobs_get` for simplest API.

pub fn bigquery_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    jobId: &String,
    location: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/jobs/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location.as_ref() {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/jobs/{jobsId}
/// Returns information about a specific job. Job information is available for a six month period after creation. Requires that you're the person who ran the job, or have the Is Owner project role.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_get_execute()` or `bigquery_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/jobs/{jobsId}
/// Returns information about a specific job. Job information is available for a six month period after creation. Requires that you're the person who ran the job, or have the Is Owner project role.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_get_task()`.
/// For the simplest API, use `bigquery_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: location
    pub location: Option<String>,
}

/// GET projects/{projectsId}/jobs/{jobsId}
/// Returns information about a specific job. Job information is available for a six month period after creation. Requires that you're the person who ran the job, or have the Is Owner project role.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_get_builder()` + `bigquery_jobs_get_execute()`.
/// For task-level control, use `bigquery_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_get(
    client: &SimpleHttpClient,
    args: &BigqueryJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_jobs_get_builder(client, &args.projectId, &args.jobId, &args.location)?;
    bigquery_jobs_get_execute(builder)
}

/// GET projects/{projectsId}/queries/{queriesId}
/// RPC to get the results of a query job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_get_query_results_execute()` to send, or `bigquery_jobs_get_query_results` for simplest API.

pub fn bigquery_jobs_get_query_results_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    jobId: &String,
    formatOptions_timestampOutputFormat: &Option<String>,
    formatOptions_useInt64Timestamp: &Option<bool>,
    location: &Option<String>,
    maxResults: &Option<i32>,
    pageToken: &Option<String>,
    startIndex: &Option<String>,
    timeoutMs: &Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/queries/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = formatOptions_timestampOutputFormat.as_ref() {
        query_parts.push(format!("formatOptions.timestampOutputFormat={}", val));
    }
    if let Some(val) = formatOptions_useInt64Timestamp.as_ref() {
        query_parts.push(format!("formatOptions.useInt64Timestamp={}", val));
    }
    if let Some(val) = location.as_ref() {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }
    if let Some(val) = timeoutMs.as_ref() {
        query_parts.push(format!("timeoutMs={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/queries/{queriesId}
/// RPC to get the results of a query job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_get_query_results_execute()` or `bigquery_jobs_get_query_results`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_get_query_results_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_get_query_results_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetQueryResultsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetQueryResultsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/queries/{queriesId}
/// RPC to get the results of a query job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_get_query_results_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_get_query_results_task()`.
/// For the simplest API, use `bigquery_jobs_get_query_results()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_get_query_results_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_get_query_results_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetQueryResultsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_jobs_get_query_results_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_get_query_results`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsGetQueryResultsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: formatOptions_timestampOutputFormat
    pub formatOptions_timestampOutputFormat: Option<String>,
    /// Query parameter: formatOptions_useInt64Timestamp
    pub formatOptions_useInt64Timestamp: Option<bool>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startIndex
    pub startIndex: Option<String>,
    /// Query parameter: timeoutMs
    pub timeoutMs: Option<i32>,
}

/// GET projects/{projectsId}/queries/{queriesId}
/// RPC to get the results of a query job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_get_query_results_builder()` + `bigquery_jobs_get_query_results_execute()`.
/// For task-level control, use `bigquery_jobs_get_query_results_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_get_query_results(
    client: &SimpleHttpClient,
    args: &BigqueryJobsGetQueryResultsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetQueryResultsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_jobs_get_query_results_builder(
        client,
        &args.projectId,
        &args.jobId,
        &args.formatOptions_timestampOutputFormat,
        &args.formatOptions_useInt64Timestamp,
        &args.location,
        &args.maxResults,
        &args.pageToken,
        &args.startIndex,
        &args.timeoutMs,
    )?;
    bigquery_jobs_get_query_results_execute(builder)
}

/// GET projects/{projectsId}/jobs
/// Starts a new asynchronous job. This API has two different kinds of endpoint URIs, as this method supports a variety of use cases. * The *Metadata* URI is used for most interactions, as it accepts the job configuration directly. * The *Upload* URI is ONLY for the case when you're sending both a load job configuration and a data stream together. In this case, the Upload URI accepts the job configuration and the data as two distinct multipart MIME parts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_insert_execute()` to send, or `bigquery_jobs_insert` for simplest API.

pub fn bigquery_jobs_insert_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/jobs",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/jobs
/// Starts a new asynchronous job. This API has two different kinds of endpoint URIs, as this method supports a variety of use cases. * The *Metadata* URI is used for most interactions, as it accepts the job configuration directly. * The *Upload* URI is ONLY for the case when you're sending both a load job configuration and a data stream together. In this case, the Upload URI accepts the job configuration and the data as two distinct multipart MIME parts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_insert_execute()` or `bigquery_jobs_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/jobs
/// Starts a new asynchronous job. This API has two different kinds of endpoint URIs, as this method supports a variety of use cases. * The *Metadata* URI is used for most interactions, as it accepts the job configuration directly. * The *Upload* URI is ONLY for the case when you're sending both a load job configuration and a data stream together. In this case, the Upload URI accepts the job configuration and the data as two distinct multipart MIME parts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_insert_task()`.
/// For the simplest API, use `bigquery_jobs_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_jobs_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsInsertArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: Job,
}

/// GET projects/{projectsId}/jobs
/// Starts a new asynchronous job. This API has two different kinds of endpoint URIs, as this method supports a variety of use cases. * The *Metadata* URI is used for most interactions, as it accepts the job configuration directly. * The *Upload* URI is ONLY for the case when you're sending both a load job configuration and a data stream together. In this case, the Upload URI accepts the job configuration and the data as two distinct multipart MIME parts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_insert_builder()` + `bigquery_jobs_insert_execute()`.
/// For task-level control, use `bigquery_jobs_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_insert(
    client: &SimpleHttpClient,
    args: &BigqueryJobsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_jobs_insert_builder(client, &args.projectId, &args.body)?;
    bigquery_jobs_insert_execute(builder)
}

/// GET projects/{projectsId}/queries
/// Runs a BigQuery SQL query synchronously and returns query results if the query completes within a specified timeout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_jobs_query_execute()` to send, or `bigquery_jobs_query` for simplest API.

pub fn bigquery_jobs_query_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    body: &QueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/queries",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/queries
/// Runs a BigQuery SQL query synchronously and returns query results if the query completes within a specified timeout.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_jobs_query_execute()` or `bigquery_jobs_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/queries
/// Runs a BigQuery SQL query synchronously and returns query results if the query completes within a specified timeout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_jobs_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_jobs_query_task()`.
/// For the simplest API, use `bigquery_jobs_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_jobs_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_jobs_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_jobs_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_jobs_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryJobsQueryArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: QueryRequest,
}

/// GET projects/{projectsId}/queries
/// Runs a BigQuery SQL query synchronously and returns query results if the query completes within a specified timeout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_jobs_query_builder()` + `bigquery_jobs_query_execute()`.
/// For task-level control, use `bigquery_jobs_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_jobs_query(
    client: &SimpleHttpClient,
    args: &BigqueryJobsQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_jobs_query_builder(client, &args.projectId, &args.body)?;
    bigquery_jobs_query_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models/{modelsId}
/// Deletes the model specified by `modelId` from the dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_models_delete_execute()` to send, or `bigquery_models_delete` for simplest API.

pub fn bigquery_models_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    modelId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/models/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models/{modelsId}
/// Deletes the model specified by `modelId` from the dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_models_delete_execute()` or `bigquery_models_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_models_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models/{modelsId}
/// Deletes the model specified by `modelId` from the dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_models_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_models_delete_task()`.
/// For the simplest API, use `bigquery_models_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_models_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_models_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_models_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryModelsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: modelId
    pub modelId: String,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models/{modelsId}
/// Deletes the model specified by `modelId` from the dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_models_delete_builder()` + `bigquery_models_delete_execute()`.
/// For task-level control, use `bigquery_models_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_models_delete(
    client: &SimpleHttpClient,
    args: &BigqueryModelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_models_delete_builder(client, &args.projectId, &args.datasetId, &args.modelId)?;
    bigquery_models_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models
/// Lists all models in the specified dataset. Requires the READER dataset role. After retrieving the list of models, you can get information about a particular model by calling the models.get method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_models_list_execute()` to send, or `bigquery_models_list` for simplest API.

pub fn bigquery_models_list_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    maxResults: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/models",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models
/// Lists all models in the specified dataset. Requires the READER dataset role. After retrieving the list of models, you can get information about a particular model by calling the models.get method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_models_list_execute()` or `bigquery_models_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_models_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListModelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListModelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models
/// Lists all models in the specified dataset. Requires the READER dataset role. After retrieving the list of models, you can get information about a particular model by calling the models.get method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_models_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_models_list_task()`.
/// For the simplest API, use `bigquery_models_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListModelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_models_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_models_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryModelsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/models
/// Lists all models in the specified dataset. Requires the READER dataset role. After retrieving the list of models, you can get information about a particular model by calling the models.get method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_models_list_builder()` + `bigquery_models_list_execute()`.
/// For task-level control, use `bigquery_models_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_models_list(
    client: &SimpleHttpClient,
    args: &BigqueryModelsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListModelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_models_list_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.maxResults,
        &args.pageToken,
    )?;
    bigquery_models_list_execute(builder)
}

/// GET projects/{projectsId}/serviceAccount
/// RPC to get the service account for a project used for interactions with Google Cloud KMS
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_projects_get_service_account_execute()` to send, or `bigquery_projects_get_service_account` for simplest API.

pub fn bigquery_projects_get_service_account_builder(
    client: &SimpleHttpClient,
    projectId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/serviceAccount",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/serviceAccount
/// RPC to get the service account for a project used for interactions with Google Cloud KMS
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_projects_get_service_account_execute()` or `bigquery_projects_get_service_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_projects_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_projects_get_service_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetServiceAccountResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetServiceAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/serviceAccount
/// RPC to get the service account for a project used for interactions with Google Cloud KMS
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_projects_get_service_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_projects_get_service_account_task()`.
/// For the simplest API, use `bigquery_projects_get_service_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_projects_get_service_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_projects_get_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetServiceAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_projects_get_service_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_projects_get_service_account`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryProjectsGetServiceAccountArgs {
    /// Path parameter: projectId
    pub projectId: String,
}

/// GET projects/{projectsId}/serviceAccount
/// RPC to get the service account for a project used for interactions with Google Cloud KMS
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_projects_get_service_account_builder()` + `bigquery_projects_get_service_account_execute()`.
/// For task-level control, use `bigquery_projects_get_service_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_projects_get_service_account(
    client: &SimpleHttpClient,
    args: &BigqueryProjectsGetServiceAccountArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetServiceAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_projects_get_service_account_builder(client, &args.projectId)?;
    bigquery_projects_get_service_account_execute(builder)
}

/// GET projects
/// RPC to list projects to which the user has been granted any project role. Users of this method are encouraged to consider the [Resource Manager](<https://cloud.google.`com/resource-manager/docs/`>) API, which provides the underlying data for this method and has more capabilities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_projects_list_execute()` to send, or `bigquery_projects_list` for simplest API.

pub fn bigquery_projects_list_builder(
    client: &SimpleHttpClient,
    maxResults: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://bigquery.googleapis.com/bigquery/v2/projects",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects
/// RPC to list projects to which the user has been granted any project role. Users of this method are encouraged to consider the [Resource Manager](<https://cloud.google.`com/resource-manager/docs/`>) API, which provides the underlying data for this method and has more capabilities.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_projects_list_execute()` or `bigquery_projects_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_projects_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects
/// RPC to list projects to which the user has been granted any project role. Users of this method are encouraged to consider the [Resource Manager](<https://cloud.google.`com/resource-manager/docs/`>) API, which provides the underlying data for this method and has more capabilities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_projects_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_projects_list_task()`.
/// For the simplest API, use `bigquery_projects_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_projects_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_projects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryProjectsListArgs {
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET projects
/// RPC to list projects to which the user has been granted any project role. Users of this method are encouraged to consider the [Resource Manager](<https://cloud.google.`com/resource-manager/docs/`>) API, which provides the underlying data for this method and has more capabilities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_projects_list_builder()` + `bigquery_projects_list_execute()`.
/// For task-level control, use `bigquery_projects_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_projects_list(
    client: &SimpleHttpClient,
    args: &BigqueryProjectsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_projects_list_builder(client, &args.maxResults, &args.pageToken)?;
    bigquery_projects_list_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines/{routinesId}
/// Deletes the routine specified by `routineId` from the dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_routines_delete_execute()` to send, or `bigquery_routines_delete` for simplest API.

pub fn bigquery_routines_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    routineId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/routines/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines/{routinesId}
/// Deletes the routine specified by `routineId` from the dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_routines_delete_execute()` or `bigquery_routines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_routines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_routines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines/{routinesId}
/// Deletes the routine specified by `routineId` from the dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_routines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_routines_delete_task()`.
/// For the simplest API, use `bigquery_routines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_routines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_routines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_routines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_routines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryRoutinesDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: routineId
    pub routineId: String,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines/{routinesId}
/// Deletes the routine specified by `routineId` from the dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_routines_delete_builder()` + `bigquery_routines_delete_execute()`.
/// For task-level control, use `bigquery_routines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_routines_delete(
    client: &SimpleHttpClient,
    args: &BigqueryRoutinesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_routines_delete_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.routineId,
    )?;
    bigquery_routines_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines
/// Creates a new routine in the dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_routines_insert_execute()` to send, or `bigquery_routines_insert` for simplest API.

pub fn bigquery_routines_insert_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    body: &Routine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/routines",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines
/// Creates a new routine in the dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_routines_insert_execute()` or `bigquery_routines_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_routines_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_routines_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Routine>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Routine = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines
/// Creates a new routine in the dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_routines_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_routines_insert_task()`.
/// For the simplest API, use `bigquery_routines_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_routines_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_routines_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Routine>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_routines_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_routines_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryRoutinesInsertArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Request body.
    pub body: Routine,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/routines
/// Creates a new routine in the dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_routines_insert_builder()` + `bigquery_routines_insert_execute()`.
/// For task-level control, use `bigquery_routines_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_routines_insert(
    client: &SimpleHttpClient,
    args: &BigqueryRoutinesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Routine>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_routines_insert_builder(client, &args.projectId, &args.datasetId, &args.body)?;
    bigquery_routines_insert_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies:batchDelete
/// Deletes provided row access policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_row_access_policies_batch_delete_execute()` to send, or `bigquery_row_access_policies_batch_delete` for simplest API.

pub fn bigquery_row_access_policies_batch_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
    body: &BatchDeleteRowAccessPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}/rowAccessPolicies:batchDelete",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies:batchDelete
/// Deletes provided row access policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_row_access_policies_batch_delete_execute()` or `bigquery_row_access_policies_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies:batchDelete
/// Deletes provided row access policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_row_access_policies_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_row_access_policies_batch_delete_task()`.
/// For the simplest API, use `bigquery_row_access_policies_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_row_access_policies_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_row_access_policies_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_row_access_policies_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryRowAccessPoliciesBatchDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
    /// Request body.
    pub body: BatchDeleteRowAccessPoliciesRequest,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies:batchDelete
/// Deletes provided row access policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_row_access_policies_batch_delete_builder()` + `bigquery_row_access_policies_batch_delete_execute()`.
/// For task-level control, use `bigquery_row_access_policies_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_batch_delete(
    client: &SimpleHttpClient,
    args: &BigqueryRowAccessPoliciesBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_row_access_policies_batch_delete_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.tableId,
        &args.body,
    )?;
    bigquery_row_access_policies_batch_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies/{rowAccessPoliciesId}
/// Deletes a row access policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_row_access_policies_delete_execute()` to send, or `bigquery_row_access_policies_delete` for simplest API.

pub fn bigquery_row_access_policies_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
    policyId: &String,
    force: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}/rowAccessPolicies/{}",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies/{rowAccessPoliciesId}
/// Deletes a row access policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_row_access_policies_delete_execute()` or `bigquery_row_access_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies/{rowAccessPoliciesId}
/// Deletes a row access policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_row_access_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_row_access_policies_delete_task()`.
/// For the simplest API, use `bigquery_row_access_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_row_access_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_row_access_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_row_access_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryRowAccessPoliciesDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
    /// Path parameter: policyId
    pub policyId: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies/{rowAccessPoliciesId}
/// Deletes a row access policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_row_access_policies_delete_builder()` + `bigquery_row_access_policies_delete_execute()`.
/// For task-level control, use `bigquery_row_access_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_delete(
    client: &SimpleHttpClient,
    args: &BigqueryRowAccessPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquery_row_access_policies_delete_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.tableId,
        &args.policyId,
        &args.force,
    )?;
    bigquery_row_access_policies_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies
/// Creates a row access policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_row_access_policies_insert_execute()` to send, or `bigquery_row_access_policies_insert` for simplest API.

pub fn bigquery_row_access_policies_insert_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
    body: &RowAccessPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}/rowAccessPolicies",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies
/// Creates a row access policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_row_access_policies_insert_execute()` or `bigquery_row_access_policies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RowAccessPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RowAccessPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies
/// Creates a row access policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_row_access_policies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_row_access_policies_insert_task()`.
/// For the simplest API, use `bigquery_row_access_policies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_row_access_policies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_row_access_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RowAccessPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_row_access_policies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_row_access_policies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryRowAccessPoliciesInsertArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
    /// Request body.
    pub body: RowAccessPolicy,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/rowAccessPolicies
/// Creates a row access policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_row_access_policies_insert_builder()` + `bigquery_row_access_policies_insert_execute()`.
/// For task-level control, use `bigquery_row_access_policies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_row_access_policies_insert(
    client: &SimpleHttpClient,
    args: &BigqueryRowAccessPoliciesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RowAccessPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_row_access_policies_insert_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.tableId,
        &args.body,
    )?;
    bigquery_row_access_policies_insert_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/insertAll
/// Streams data into BigQuery one record at a time without needing to run a load job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_tabledata_insert_all_execute()` to send, or `bigquery_tabledata_insert_all` for simplest API.

pub fn bigquery_tabledata_insert_all_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
    body: &TableDataInsertAllRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}/insertAll",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/insertAll
/// Streams data into BigQuery one record at a time without needing to run a load job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_tabledata_insert_all_execute()` or `bigquery_tabledata_insert_all`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tabledata_insert_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tabledata_insert_all_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TableDataInsertAllResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TableDataInsertAllResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/insertAll
/// Streams data into BigQuery one record at a time without needing to run a load job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_tabledata_insert_all_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_tabledata_insert_all_task()`.
/// For the simplest API, use `bigquery_tabledata_insert_all()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tabledata_insert_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_tabledata_insert_all_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TableDataInsertAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_tabledata_insert_all_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_tabledata_insert_all`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTabledataInsertAllArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
    /// Request body.
    pub body: TableDataInsertAllRequest,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/insertAll
/// Streams data into BigQuery one record at a time without needing to run a load job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_tabledata_insert_all_builder()` + `bigquery_tabledata_insert_all_execute()`.
/// For task-level control, use `bigquery_tabledata_insert_all_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tabledata_insert_all(
    client: &SimpleHttpClient,
    args: &BigqueryTabledataInsertAllArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TableDataInsertAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_tabledata_insert_all_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.tableId,
        &args.body,
    )?;
    bigquery_tabledata_insert_all_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/data
/// List the content of a table in rows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_tabledata_list_execute()` to send, or `bigquery_tabledata_list` for simplest API.

pub fn bigquery_tabledata_list_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
    formatOptions_timestampOutputFormat: &Option<String>,
    formatOptions_useInt64Timestamp: &Option<bool>,
    maxResults: &Option<i32>,
    pageToken: &Option<String>,
    selectedFields: &Option<String>,
    startIndex: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}/data",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = formatOptions_timestampOutputFormat.as_ref() {
        query_parts.push(format!("formatOptions.timestampOutputFormat={}", val));
    }
    if let Some(val) = formatOptions_useInt64Timestamp.as_ref() {
        query_parts.push(format!("formatOptions.useInt64Timestamp={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = selectedFields.as_ref() {
        query_parts.push(format!("selectedFields={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/data
/// List the content of a table in rows.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_tabledata_list_execute()` or `bigquery_tabledata_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tabledata_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tabledata_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TableDataList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TableDataList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/data
/// List the content of a table in rows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_tabledata_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_tabledata_list_task()`.
/// For the simplest API, use `bigquery_tabledata_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tabledata_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_tabledata_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TableDataList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquery_tabledata_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_tabledata_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTabledataListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
    /// Query parameter: formatOptions_timestampOutputFormat
    pub formatOptions_timestampOutputFormat: Option<String>,
    /// Query parameter: formatOptions_useInt64Timestamp
    pub formatOptions_useInt64Timestamp: Option<bool>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: selectedFields
    pub selectedFields: Option<String>,
    /// Query parameter: startIndex
    pub startIndex: Option<String>,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}/data
/// List the content of a table in rows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_tabledata_list_builder()` + `bigquery_tabledata_list_execute()`.
/// For task-level control, use `bigquery_tabledata_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tabledata_list(
    client: &SimpleHttpClient,
    args: &BigqueryTabledataListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TableDataList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquery_tabledata_list_builder(
        client,
        &args.projectId,
        &args.datasetId,
        &args.tableId,
        &args.formatOptions_timestampOutputFormat,
        &args.formatOptions_useInt64Timestamp,
        &args.maxResults,
        &args.pageToken,
        &args.selectedFields,
        &args.startIndex,
    )?;
    bigquery_tabledata_list_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
/// Deletes the table specified by `tableId` from the dataset. If the table contains data, all the data will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_tables_delete_execute()` to send, or `bigquery_tables_delete` for simplest API.

pub fn bigquery_tables_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    tableId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
/// Deletes the table specified by `tableId` from the dataset. If the table contains data, all the data will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_tables_delete_execute()` or `bigquery_tables_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tables_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tables_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
/// Deletes the table specified by `tableId` from the dataset. If the table contains data, all the data will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_tables_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_tables_delete_task()`.
/// For the simplest API, use `bigquery_tables_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tables_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_tables_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_tables_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_tables_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Path parameter: tableId
    pub tableId: String,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
/// Deletes the table specified by `tableId` from the dataset. If the table contains data, all the data will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_tables_delete_builder()` + `bigquery_tables_delete_execute()`.
/// For task-level control, use `bigquery_tables_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tables_delete(
    client: &SimpleHttpClient,
    args: &BigqueryTablesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_tables_delete_builder(client, &args.projectId, &args.datasetId, &args.tableId)?;
    bigquery_tables_delete_execute(builder)
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables
/// Creates a new, empty table in the dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquery_tables_insert_execute()` to send, or `bigquery_tables_insert` for simplest API.

pub fn bigquery_tables_insert_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    datasetId: &String,
    body: &Table,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquery.googleapis.com/bigquery/v2/projects/{}/datasets/{}/tables",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables
/// Creates a new, empty table in the dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquery_tables_insert_execute()` or `bigquery_tables_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tables_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tables_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables
/// Creates a new, empty table in the dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquery_tables_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquery_tables_insert_task()`.
/// For the simplest API, use `bigquery_tables_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquery_tables_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquery_tables_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquery_tables_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquery_tables_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesInsertArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Request body.
    pub body: Table,
}

/// GET projects/{projectsId}/datasets/{datasetsId}/tables
/// Creates a new, empty table in the dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquery_tables_insert_builder()` + `bigquery_tables_insert_execute()`.
/// For task-level control, use `bigquery_tables_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquery_tables_insert(
    client: &SimpleHttpClient,
    args: &BigqueryTablesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquery_tables_insert_builder(client, &args.projectId, &args.datasetId, &args.body)?;
    bigquery_tables_insert_execute(builder)
}
