//! Auto-generated API clients for gcp/trafficdirector.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/trafficdirector`

#![cfg(feature = "gcp/trafficdirector")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/discovery:client_status
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `trafficdirector_discovery_client_status_execute()` to send, or `trafficdirector_discovery_client_status` for simplest API.

pub fn trafficdirector_discovery_client_status_builder(
    client: &SimpleHttpClient,
    body: &ClientStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://trafficdirector.googleapis.com/v3/discovery:client_status",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/discovery:client_status
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `trafficdirector_discovery_client_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from trafficdirector_discovery_client_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn trafficdirector_discovery_client_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`trafficdirector_discovery_client_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TrafficdirectorDiscoveryClientStatusArgs {
    /// Request body.
    pub body: ClientStatusRequest,
}

/// GET v3/discovery:client_status
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `trafficdirector_discovery_client_status_builder()` + `trafficdirector_discovery_client_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn trafficdirector_discovery_client_status(
    client: &SimpleHttpClient,
    args: &TrafficdirectorDiscoveryClientStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = trafficdirector_discovery_client_status_builder(client, &args.body)?;
    trafficdirector_discovery_client_status_execute(builder)
}
