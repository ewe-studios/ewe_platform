//! Auto-generated API clients for gcp/dfareporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET userprofiles/{userprofilesId}/accountActiveAdSummaries/{accountActiveAdSummariesId}
/// Gets the account''s active ad summary by account ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_active_ad_summaries_get_execute()` to send, or `dfareporting_account_active_ad_summaries_get` for simplest API.

pub fn dfareporting_account_active_ad_summaries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    summaryAccountId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountActiveAdSummaries/{}",
        profileId,
        summaryAccountId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountActiveAdSummaries/{accountActiveAdSummariesId}
/// Gets the account''s active ad summary by account ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_active_ad_summaries_get_execute()` or `dfareporting_account_active_ad_summaries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_active_ad_summaries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_active_ad_summaries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountActiveAdSummary>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountActiveAdSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountActiveAdSummaries/{accountActiveAdSummariesId}
/// Gets the account''s active ad summary by account ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_active_ad_summaries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_active_ad_summaries_get_task()`.
/// For the simplest API, use `dfareporting_account_active_ad_summaries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_active_ad_summaries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_active_ad_summaries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountActiveAdSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_active_ad_summaries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_active_ad_summaries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountActiveAdSummariesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: summaryAccountId
    pub summaryAccountId: String,
}

/// GET userprofiles/{userprofilesId}/accountActiveAdSummaries/{accountActiveAdSummariesId}
/// Gets the account''s active ad summary by account ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_active_ad_summaries_get_builder()` + `dfareporting_account_active_ad_summaries_get_execute()`.
/// For task-level control, use `dfareporting_account_active_ad_summaries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_active_ad_summaries_get(
    client: &SimpleHttpClient,
    args: &DfareportingAccountActiveAdSummariesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountActiveAdSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_active_ad_summaries_get_builder(
        client,
        &args.profileId,
        &args.summaryAccountId,
    )?;
    dfareporting_account_active_ad_summaries_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups/{accountPermissionGroupsId}
/// Gets one account permission group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_permission_groups_get_execute()` to send, or `dfareporting_account_permission_groups_get` for simplest API.

pub fn dfareporting_account_permission_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountPermissionGroups/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups/{accountPermissionGroupsId}
/// Gets one account permission group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_permission_groups_get_execute()` or `dfareporting_account_permission_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permission_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permission_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountPermissionGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountPermissionGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups/{accountPermissionGroupsId}
/// Gets one account permission group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_permission_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_permission_groups_get_task()`.
/// For the simplest API, use `dfareporting_account_permission_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permission_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_permission_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountPermissionGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_permission_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_permission_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountPermissionGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups/{accountPermissionGroupsId}
/// Gets one account permission group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_permission_groups_get_builder()` + `dfareporting_account_permission_groups_get_execute()`.
/// For task-level control, use `dfareporting_account_permission_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permission_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingAccountPermissionGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountPermissionGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_account_permission_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_account_permission_groups_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups
/// Retrieves the list of account permission groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_permission_groups_list_execute()` to send, or `dfareporting_account_permission_groups_list` for simplest API.

pub fn dfareporting_account_permission_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountPermissionGroups",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups
/// Retrieves the list of account permission groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_permission_groups_list_execute()` or `dfareporting_account_permission_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permission_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permission_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountPermissionGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountPermissionGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups
/// Retrieves the list of account permission groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_permission_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_permission_groups_list_task()`.
/// For the simplest API, use `dfareporting_account_permission_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permission_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_permission_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountPermissionGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_permission_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_permission_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountPermissionGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/accountPermissionGroups
/// Retrieves the list of account permission groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_permission_groups_list_builder()` + `dfareporting_account_permission_groups_list_execute()`.
/// For task-level control, use `dfareporting_account_permission_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permission_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingAccountPermissionGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountPermissionGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_permission_groups_list_builder(client, &args.profileId)?;
    dfareporting_account_permission_groups_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissions/{accountPermissionsId}
/// Gets one account permission by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_permissions_get_execute()` to send, or `dfareporting_account_permissions_get` for simplest API.

pub fn dfareporting_account_permissions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountPermissions/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissions/{accountPermissionsId}
/// Gets one account permission by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_permissions_get_execute()` or `dfareporting_account_permissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountPermission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountPermissions/{accountPermissionsId}
/// Gets one account permission by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_permissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_permissions_get_task()`.
/// For the simplest API, use `dfareporting_account_permissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_permissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_permissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountPermissionsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/accountPermissions/{accountPermissionsId}
/// Gets one account permission by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_permissions_get_builder()` + `dfareporting_account_permissions_get_execute()`.
/// For task-level control, use `dfareporting_account_permissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permissions_get(
    client: &SimpleHttpClient,
    args: &DfareportingAccountPermissionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_permissions_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_account_permissions_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissions
/// Retrieves the list of account permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_permissions_list_execute()` to send, or `dfareporting_account_permissions_list` for simplest API.

pub fn dfareporting_account_permissions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountPermissions",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountPermissions
/// Retrieves the list of account permissions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_permissions_list_execute()` or `dfareporting_account_permissions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permissions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountPermissionsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountPermissionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountPermissions
/// Retrieves the list of account permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_permissions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_permissions_list_task()`.
/// For the simplest API, use `dfareporting_account_permissions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_permissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_permissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountPermissionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_permissions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_permissions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountPermissionsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/accountPermissions
/// Retrieves the list of account permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_permissions_list_builder()` + `dfareporting_account_permissions_list_execute()`.
/// For task-level control, use `dfareporting_account_permissions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_permissions_list(
    client: &SimpleHttpClient,
    args: &DfareportingAccountPermissionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountPermissionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_permissions_list_builder(client, &args.profileId)?;
    dfareporting_account_permissions_list_execute(builder)
}

/// GET userprofiles/{profileId}/accountUserProfiles/{accountUserProfilesId}
/// Gets one account user profile by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_user_profiles_get_execute()` to send, or `dfareporting_account_user_profiles_get` for simplest API.

pub fn dfareporting_account_user_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountUserProfiles/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/accountUserProfiles/{accountUserProfilesId}
/// Gets one account user profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_user_profiles_get_execute()` or `dfareporting_account_user_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountUserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountUserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/accountUserProfiles/{accountUserProfilesId}
/// Gets one account user profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_user_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_user_profiles_get_task()`.
/// For the simplest API, use `dfareporting_account_user_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_user_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_user_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_user_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountUserProfilesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{profileId}/accountUserProfiles/{accountUserProfilesId}
/// Gets one account user profile by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_user_profiles_get_builder()` + `dfareporting_account_user_profiles_get_execute()`.
/// For task-level control, use `dfareporting_account_user_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_get(
    client: &SimpleHttpClient,
    args: &DfareportingAccountUserProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_account_user_profiles_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_account_user_profiles_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/accountUserProfiles
/// Inserts a new account user profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_user_profiles_insert_execute()` to send, or `dfareporting_account_user_profiles_insert` for simplest API.

pub fn dfareporting_account_user_profiles_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountUserProfiles",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/accountUserProfiles
/// Inserts a new account user profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_user_profiles_insert_execute()` or `dfareporting_account_user_profiles_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountUserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountUserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/accountUserProfiles
/// Inserts a new account user profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_user_profiles_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_user_profiles_insert_task()`.
/// For the simplest API, use `dfareporting_account_user_profiles_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_user_profiles_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_user_profiles_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_user_profiles_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountUserProfilesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/accountUserProfiles
/// Inserts a new account user profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_user_profiles_insert_builder()` + `dfareporting_account_user_profiles_insert_execute()`.
/// For task-level control, use `dfareporting_account_user_profiles_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_insert(
    client: &SimpleHttpClient,
    args: &DfareportingAccountUserProfilesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_user_profiles_insert_builder(client, &args.profileId)?;
    dfareporting_account_user_profiles_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accountUserProfiles
/// Retrieves a list of account user profiles, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_user_profiles_list_execute()` to send, or `dfareporting_account_user_profiles_list` for simplest API.

pub fn dfareporting_account_user_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    subaccountId: &Option<String>,
    userRoleId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountUserProfiles",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }
    if let Some(val) = userRoleId.as_ref() {
        query_parts.push(format!("userRoleId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accountUserProfiles
/// Retrieves a list of account user profiles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_user_profiles_list_execute()` or `dfareporting_account_user_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountUserProfilesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountUserProfilesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accountUserProfiles
/// Retrieves a list of account user profiles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_user_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_user_profiles_list_task()`.
/// For the simplest API, use `dfareporting_account_user_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_user_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountUserProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_user_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_user_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountUserProfilesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
    /// Query parameter: userRoleId
    pub userRoleId: Option<String>,
}

/// GET userprofiles/{userprofilesId}/accountUserProfiles
/// Retrieves a list of account user profiles, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_user_profiles_list_builder()` + `dfareporting_account_user_profiles_list_execute()`.
/// For task-level control, use `dfareporting_account_user_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_list(
    client: &SimpleHttpClient,
    args: &DfareportingAccountUserProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccountUserProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_user_profiles_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.subaccountId,
        &args.userRoleId,
    )?;
    dfareporting_account_user_profiles_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_user_profiles_patch_execute()` to send, or `dfareporting_account_user_profiles_patch` for simplest API.

pub fn dfareporting_account_user_profiles_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountUserProfiles",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_user_profiles_patch_execute()` or `dfareporting_account_user_profiles_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountUserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountUserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_user_profiles_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_user_profiles_patch_task()`.
/// For the simplest API, use `dfareporting_account_user_profiles_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_user_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_user_profiles_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_user_profiles_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountUserProfilesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_user_profiles_patch_builder()` + `dfareporting_account_user_profiles_patch_execute()`.
/// For task-level control, use `dfareporting_account_user_profiles_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAccountUserProfilesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_account_user_profiles_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_account_user_profiles_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_account_user_profiles_update_execute()` to send, or `dfareporting_account_user_profiles_update` for simplest API.

pub fn dfareporting_account_user_profiles_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accountUserProfiles",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_account_user_profiles_update_execute()` or `dfareporting_account_user_profiles_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountUserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountUserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_account_user_profiles_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_account_user_profiles_update_task()`.
/// For the simplest API, use `dfareporting_account_user_profiles_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_account_user_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_account_user_profiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_account_user_profiles_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_account_user_profiles_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountUserProfilesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/accountUserProfiles
/// Updates an existing account user profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_account_user_profiles_update_builder()` + `dfareporting_account_user_profiles_update_execute()`.
/// For task-level control, use `dfareporting_account_user_profiles_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_account_user_profiles_update(
    client: &SimpleHttpClient,
    args: &DfareportingAccountUserProfilesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountUserProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_account_user_profiles_update_builder(client, &args.profileId)?;
    dfareporting_account_user_profiles_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accounts/{accountsId}
/// Gets one account by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_accounts_get_execute()` to send, or `dfareporting_accounts_get` for simplest API.

pub fn dfareporting_accounts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accounts/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accounts/{accountsId}
/// Gets one account by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_accounts_get_execute()` or `dfareporting_accounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accounts/{accountsId}
/// Gets one account by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_accounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_accounts_get_task()`.
/// For the simplest API, use `dfareporting_accounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_accounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/accounts/{accountsId}
/// Gets one account by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_accounts_get_builder()` + `dfareporting_accounts_get_execute()`.
/// For task-level control, use `dfareporting_accounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_get(
    client: &SimpleHttpClient,
    args: &DfareportingAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_accounts_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_accounts_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/accounts
/// Retrieves the list of accounts, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_accounts_list_execute()` to send, or `dfareporting_accounts_list` for simplest API.

pub fn dfareporting_accounts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accounts",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/accounts
/// Retrieves the list of accounts, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_accounts_list_execute()` or `dfareporting_accounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/accounts
/// Retrieves the list of accounts, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_accounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_accounts_list_task()`.
/// For the simplest API, use `dfareporting_accounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_accounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/accounts
/// Retrieves the list of accounts, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_accounts_list_builder()` + `dfareporting_accounts_list_execute()`.
/// For task-level control, use `dfareporting_accounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_list(
    client: &SimpleHttpClient,
    args: &DfareportingAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_accounts_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_accounts_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/accounts
/// Updates an existing account. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_accounts_patch_execute()` to send, or `dfareporting_accounts_patch` for simplest API.

pub fn dfareporting_accounts_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accounts",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/accounts
/// Updates an existing account. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_accounts_patch_execute()` or `dfareporting_accounts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/accounts
/// Updates an existing account. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_accounts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_accounts_patch_task()`.
/// For the simplest API, use `dfareporting_accounts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_accounts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_accounts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_accounts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/accounts
/// Updates an existing account. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_accounts_patch_builder()` + `dfareporting_accounts_patch_execute()`.
/// For task-level control, use `dfareporting_accounts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAccountsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_accounts_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_accounts_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/accounts
/// Updates an existing account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_accounts_update_execute()` to send, or `dfareporting_accounts_update` for simplest API.

pub fn dfareporting_accounts_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/accounts",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/accounts
/// Updates an existing account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_accounts_update_execute()` or `dfareporting_accounts_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/accounts
/// Updates an existing account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_accounts_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_accounts_update_task()`.
/// For the simplest API, use `dfareporting_accounts_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_accounts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_accounts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_accounts_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_accounts_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/accounts
/// Updates an existing account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_accounts_update_builder()` + `dfareporting_accounts_update_execute()`.
/// For task-level control, use `dfareporting_accounts_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_accounts_update(
    client: &SimpleHttpClient,
    args: &DfareportingAccountsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_accounts_update_builder(client, &args.profileId)?;
    dfareporting_accounts_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/ads/{adsId}
/// Gets one ad by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_ads_get_execute()` to send, or `dfareporting_ads_get` for simplest API.

pub fn dfareporting_ads_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/ads/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/ads/{adsId}
/// Gets one ad by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_ads_get_execute()` or `dfareporting_ads_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/ads/{adsId}
/// Gets one ad by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_ads_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_ads_get_task()`.
/// For the simplest API, use `dfareporting_ads_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_ads_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_ads_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_ads_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/ads/{adsId}
/// Gets one ad by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_ads_get_builder()` + `dfareporting_ads_get_execute()`.
/// For task-level control, use `dfareporting_ads_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_get(
    client: &SimpleHttpClient,
    args: &DfareportingAdsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_ads_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_ads_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/ads
/// Inserts a new ad.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_ads_insert_execute()` to send, or `dfareporting_ads_insert` for simplest API.

pub fn dfareporting_ads_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/ads",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/ads
/// Inserts a new ad.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_ads_insert_execute()` or `dfareporting_ads_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/ads
/// Inserts a new ad.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_ads_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_ads_insert_task()`.
/// For the simplest API, use `dfareporting_ads_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_ads_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_ads_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_ads_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/ads
/// Inserts a new ad.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_ads_insert_builder()` + `dfareporting_ads_insert_execute()`.
/// For task-level control, use `dfareporting_ads_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_insert(
    client: &SimpleHttpClient,
    args: &DfareportingAdsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_ads_insert_builder(client, &args.profileId)?;
    dfareporting_ads_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/ads
/// Retrieves a list of ads, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_ads_list_execute()` to send, or `dfareporting_ads_list` for simplest API.

pub fn dfareporting_ads_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    advertiserId: &Option<String>,
    archived: &Option<String>,
    audienceSegmentIds: &Option<String>,
    campaignIds: &Option<String>,
    compatibility: &Option<String>,
    creativeIds: &Option<String>,
    creativeOptimizationConfigurationIds: &Option<String>,
    dynamicClickTracker: &Option<String>,
    ids: &Option<String>,
    landingPageIds: &Option<String>,
    maxResults: &Option<String>,
    overriddenEventTagId: &Option<String>,
    pageToken: &Option<String>,
    placementIds: &Option<String>,
    remarketingListIds: &Option<String>,
    searchString: &Option<String>,
    sizeIds: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    sslCompliant: &Option<String>,
    sslRequired: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/ads",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = archived.as_ref() {
        query_parts.push(format!("archived={}", val));
    }
    if let Some(val) = audienceSegmentIds.as_ref() {
        query_parts.push(format!("audienceSegmentIds={}", val));
    }
    if let Some(val) = campaignIds.as_ref() {
        query_parts.push(format!("campaignIds={}", val));
    }
    if let Some(val) = compatibility.as_ref() {
        query_parts.push(format!("compatibility={}", val));
    }
    if let Some(val) = creativeIds.as_ref() {
        query_parts.push(format!("creativeIds={}", val));
    }
    if let Some(val) = creativeOptimizationConfigurationIds.as_ref() {
        query_parts.push(format!("creativeOptimizationConfigurationIds={}", val));
    }
    if let Some(val) = dynamicClickTracker.as_ref() {
        query_parts.push(format!("dynamicClickTracker={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = landingPageIds.as_ref() {
        query_parts.push(format!("landingPageIds={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = overriddenEventTagId.as_ref() {
        query_parts.push(format!("overriddenEventTagId={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = placementIds.as_ref() {
        query_parts.push(format!("placementIds={}", val));
    }
    if let Some(val) = remarketingListIds.as_ref() {
        query_parts.push(format!("remarketingListIds={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sizeIds.as_ref() {
        query_parts.push(format!("sizeIds={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = sslCompliant.as_ref() {
        query_parts.push(format!("sslCompliant={}", val));
    }
    if let Some(val) = sslRequired.as_ref() {
        query_parts.push(format!("sslRequired={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/ads
/// Retrieves a list of ads, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_ads_list_execute()` or `dfareporting_ads_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/ads
/// Retrieves a list of ads, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_ads_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_ads_list_task()`.
/// For the simplest API, use `dfareporting_ads_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_ads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_ads_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_ads_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: archived
    pub archived: Option<String>,
    /// Query parameter: audienceSegmentIds
    pub audienceSegmentIds: Option<String>,
    /// Query parameter: campaignIds
    pub campaignIds: Option<String>,
    /// Query parameter: compatibility
    pub compatibility: Option<String>,
    /// Query parameter: creativeIds
    pub creativeIds: Option<String>,
    /// Query parameter: creativeOptimizationConfigurationIds
    pub creativeOptimizationConfigurationIds: Option<String>,
    /// Query parameter: dynamicClickTracker
    pub dynamicClickTracker: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: landingPageIds
    pub landingPageIds: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: overriddenEventTagId
    pub overriddenEventTagId: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: placementIds
    pub placementIds: Option<String>,
    /// Query parameter: remarketingListIds
    pub remarketingListIds: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sizeIds
    pub sizeIds: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: sslCompliant
    pub sslCompliant: Option<String>,
    /// Query parameter: sslRequired
    pub sslRequired: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET userprofiles/{userprofilesId}/ads
/// Retrieves a list of ads, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_ads_list_builder()` + `dfareporting_ads_list_execute()`.
/// For task-level control, use `dfareporting_ads_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_list(
    client: &SimpleHttpClient,
    args: &DfareportingAdsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_ads_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.advertiserId,
        &args.archived,
        &args.audienceSegmentIds,
        &args.campaignIds,
        &args.compatibility,
        &args.creativeIds,
        &args.creativeOptimizationConfigurationIds,
        &args.dynamicClickTracker,
        &args.ids,
        &args.landingPageIds,
        &args.maxResults,
        &args.overriddenEventTagId,
        &args.pageToken,
        &args.placementIds,
        &args.remarketingListIds,
        &args.searchString,
        &args.sizeIds,
        &args.sortField,
        &args.sortOrder,
        &args.sslCompliant,
        &args.sslRequired,
        &args.type_rs,
    )?;
    dfareporting_ads_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/ads
/// Updates an existing ad. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_ads_patch_execute()` to send, or `dfareporting_ads_patch` for simplest API.

pub fn dfareporting_ads_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/ads",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/ads
/// Updates an existing ad. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_ads_patch_execute()` or `dfareporting_ads_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/ads
/// Updates an existing ad. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_ads_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_ads_patch_task()`.
/// For the simplest API, use `dfareporting_ads_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_ads_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_ads_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_ads_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/ads
/// Updates an existing ad. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_ads_patch_builder()` + `dfareporting_ads_patch_execute()`.
/// For task-level control, use `dfareporting_ads_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAdsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_ads_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_ads_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/ads
/// Updates an existing ad.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_ads_update_execute()` to send, or `dfareporting_ads_update` for simplest API.

pub fn dfareporting_ads_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/ads",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/ads
/// Updates an existing ad.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_ads_update_execute()` or `dfareporting_ads_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/ads
/// Updates an existing ad.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_ads_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_ads_update_task()`.
/// For the simplest API, use `dfareporting_ads_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_ads_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_ads_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_ads_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_ads_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/ads
/// Updates an existing ad.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_ads_update_builder()` + `dfareporting_ads_update_execute()`.
/// For task-level control, use `dfareporting_ads_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_ads_update(
    client: &SimpleHttpClient,
    args: &DfareportingAdsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ad>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_ads_update_builder(client, &args.profileId)?;
    dfareporting_ads_update_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Deletes an existing advertiser group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_delete_execute()` to send, or `dfareporting_advertiser_groups_delete` for simplest API.

pub fn dfareporting_advertiser_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Deletes an existing advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_delete_execute()` or `dfareporting_advertiser_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Deletes an existing advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_delete_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Deletes an existing advertiser group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_delete_builder()` + `dfareporting_advertiser_groups_delete_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_delete(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertiser_groups_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Gets one advertiser group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_get_execute()` to send, or `dfareporting_advertiser_groups_get` for simplest API.

pub fn dfareporting_advertiser_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Gets one advertiser group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_get_execute()` or `dfareporting_advertiser_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Gets one advertiser group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_get_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/advertiserGroups/{advertiserGroupsId}
/// Gets one advertiser group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_get_builder()` + `dfareporting_advertiser_groups_get_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertiser_groups_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/advertiserGroups
/// Inserts a new advertiser group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_insert_execute()` to send, or `dfareporting_advertiser_groups_insert` for simplest API.

pub fn dfareporting_advertiser_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/advertiserGroups
/// Inserts a new advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_insert_execute()` or `dfareporting_advertiser_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/advertiserGroups
/// Inserts a new advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_insert_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/advertiserGroups
/// Inserts a new advertiser group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_insert_builder()` + `dfareporting_advertiser_groups_insert_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_insert(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_insert_builder(client, &args.profileId)?;
    dfareporting_advertiser_groups_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserGroups
/// Retrieves a list of advertiser groups, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_list_execute()` to send, or `dfareporting_advertiser_groups_list` for simplest API.

pub fn dfareporting_advertiser_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserGroups
/// Retrieves a list of advertiser groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_list_execute()` or `dfareporting_advertiser_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertiserGroups
/// Retrieves a list of advertiser groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_list_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/advertiserGroups
/// Retrieves a list of advertiser groups, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_list_builder()` + `dfareporting_advertiser_groups_list_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_list_builder(
        client,
        &args.profileId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_advertiser_groups_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_patch_execute()` to send, or `dfareporting_advertiser_groups_patch` for simplest API.

pub fn dfareporting_advertiser_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_patch_execute()` or `dfareporting_advertiser_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_patch_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_patch_builder()` + `dfareporting_advertiser_groups_patch_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertiser_groups_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_groups_update_execute()` to send, or `dfareporting_advertiser_groups_update` for simplest API.

pub fn dfareporting_advertiser_groups_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserGroups",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_groups_update_execute()` or `dfareporting_advertiser_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_groups_update_task()`.
/// For the simplest API, use `dfareporting_advertiser_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserGroupsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/advertiserGroups
/// Updates an existing advertiser group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_groups_update_builder()` + `dfareporting_advertiser_groups_update_execute()`.
/// For task-level control, use `dfareporting_advertiser_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_groups_update(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertiserGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_groups_update_builder(client, &args.profileId)?;
    dfareporting_advertiser_groups_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}/invoices
/// Retrieves a list of invoices for a particular issue month. The api only works if the billing profile invoice level is set to either advertiser or campaign non-consolidated invoice level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_invoices_list_execute()` to send, or `dfareporting_advertiser_invoices_list` for simplest API.

pub fn dfareporting_advertiser_invoices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &String,
    issueMonth: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers/{}/invoices",
        profileId,
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issueMonth.as_ref() {
        query_parts.push(format!("issueMonth={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}/invoices
/// Retrieves a list of invoices for a particular issue month. The api only works if the billing profile invoice level is set to either advertiser or campaign non-consolidated invoice level.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_invoices_list_execute()` or `dfareporting_advertiser_invoices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_invoices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_invoices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserInvoicesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserInvoicesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}/invoices
/// Retrieves a list of invoices for a particular issue month. The api only works if the billing profile invoice level is set to either advertiser or campaign non-consolidated invoice level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_invoices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_invoices_list_task()`.
/// For the simplest API, use `dfareporting_advertiser_invoices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_invoices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_invoices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserInvoicesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_invoices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_invoices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserInvoicesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: issueMonth
    pub issueMonth: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}/invoices
/// Retrieves a list of invoices for a particular issue month. The api only works if the billing profile invoice level is set to either advertiser or campaign non-consolidated invoice level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_invoices_list_builder()` + `dfareporting_advertiser_invoices_list_execute()`.
/// For task-level control, use `dfareporting_advertiser_invoices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_invoices_list(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserInvoicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserInvoicesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_invoices_list_builder(
        client,
        &args.profileId,
        &args.advertiserId,
        &args.issueMonth,
        &args.maxResults,
        &args.pageToken,
    )?;
    dfareporting_advertiser_invoices_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages/{advertiserLandingPagesId}
/// Gets one landing page by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_landing_pages_get_execute()` to send, or `dfareporting_advertiser_landing_pages_get` for simplest API.

pub fn dfareporting_advertiser_landing_pages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserLandingPages/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages/{advertiserLandingPagesId}
/// Gets one landing page by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_landing_pages_get_execute()` or `dfareporting_advertiser_landing_pages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LandingPage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LandingPage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages/{advertiserLandingPagesId}
/// Gets one landing page by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_landing_pages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_landing_pages_get_task()`.
/// For the simplest API, use `dfareporting_advertiser_landing_pages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_landing_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_landing_pages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_landing_pages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserLandingPagesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages/{advertiserLandingPagesId}
/// Gets one landing page by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_landing_pages_get_builder()` + `dfareporting_advertiser_landing_pages_get_execute()`.
/// For task-level control, use `dfareporting_advertiser_landing_pages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_get(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserLandingPagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dfareporting_advertiser_landing_pages_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertiser_landing_pages_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/advertiserLandingPages
/// Inserts a new landing page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_landing_pages_insert_execute()` to send, or `dfareporting_advertiser_landing_pages_insert` for simplest API.

pub fn dfareporting_advertiser_landing_pages_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserLandingPages",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/advertiserLandingPages
/// Inserts a new landing page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_landing_pages_insert_execute()` or `dfareporting_advertiser_landing_pages_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LandingPage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LandingPage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/advertiserLandingPages
/// Inserts a new landing page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_landing_pages_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_landing_pages_insert_task()`.
/// For the simplest API, use `dfareporting_advertiser_landing_pages_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_landing_pages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_landing_pages_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_landing_pages_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserLandingPagesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/advertiserLandingPages
/// Inserts a new landing page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_landing_pages_insert_builder()` + `dfareporting_advertiser_landing_pages_insert_execute()`.
/// For task-level control, use `dfareporting_advertiser_landing_pages_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_insert(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserLandingPagesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_landing_pages_insert_builder(client, &args.profileId)?;
    dfareporting_advertiser_landing_pages_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages
/// Retrieves a list of landing pages.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_landing_pages_list_execute()` to send, or `dfareporting_advertiser_landing_pages_list` for simplest API.

pub fn dfareporting_advertiser_landing_pages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserIds: &Option<String>,
    archived: &Option<String>,
    campaignIds: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    subaccountId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserLandingPages",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = archived.as_ref() {
        query_parts.push(format!("archived={}", val));
    }
    if let Some(val) = campaignIds.as_ref() {
        query_parts.push(format!("campaignIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages
/// Retrieves a list of landing pages.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_landing_pages_list_execute()` or `dfareporting_advertiser_landing_pages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertiserLandingPagesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertiserLandingPagesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages
/// Retrieves a list of landing pages.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_landing_pages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_landing_pages_list_task()`.
/// For the simplest API, use `dfareporting_advertiser_landing_pages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_landing_pages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserLandingPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_landing_pages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_landing_pages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserLandingPagesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: archived
    pub archived: Option<String>,
    /// Query parameter: campaignIds
    pub campaignIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
}

/// GET userprofiles/{userprofilesId}/advertiserLandingPages
/// Retrieves a list of landing pages.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_landing_pages_list_builder()` + `dfareporting_advertiser_landing_pages_list_execute()`.
/// For task-level control, use `dfareporting_advertiser_landing_pages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_list(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserLandingPagesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdvertiserLandingPagesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_landing_pages_list_builder(
        client,
        &args.profileId,
        &args.advertiserIds,
        &args.archived,
        &args.campaignIds,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.subaccountId,
    )?;
    dfareporting_advertiser_landing_pages_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_landing_pages_patch_execute()` to send, or `dfareporting_advertiser_landing_pages_patch` for simplest API.

pub fn dfareporting_advertiser_landing_pages_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserLandingPages",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_landing_pages_patch_execute()` or `dfareporting_advertiser_landing_pages_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LandingPage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LandingPage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_landing_pages_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_landing_pages_patch_task()`.
/// For the simplest API, use `dfareporting_advertiser_landing_pages_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_landing_pages_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_landing_pages_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_landing_pages_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserLandingPagesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_landing_pages_patch_builder()` + `dfareporting_advertiser_landing_pages_patch_execute()`.
/// For task-level control, use `dfareporting_advertiser_landing_pages_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserLandingPagesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dfareporting_advertiser_landing_pages_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertiser_landing_pages_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertiser_landing_pages_update_execute()` to send, or `dfareporting_advertiser_landing_pages_update` for simplest API.

pub fn dfareporting_advertiser_landing_pages_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertiserLandingPages",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertiser_landing_pages_update_execute()` or `dfareporting_advertiser_landing_pages_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LandingPage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LandingPage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertiser_landing_pages_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertiser_landing_pages_update_task()`.
/// For the simplest API, use `dfareporting_advertiser_landing_pages_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertiser_landing_pages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertiser_landing_pages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertiser_landing_pages_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertiser_landing_pages_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertiserLandingPagesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/advertiserLandingPages
/// Updates an existing landing page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertiser_landing_pages_update_builder()` + `dfareporting_advertiser_landing_pages_update_execute()`.
/// For task-level control, use `dfareporting_advertiser_landing_pages_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertiser_landing_pages_update(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertiserLandingPagesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LandingPage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertiser_landing_pages_update_builder(client, &args.profileId)?;
    dfareporting_advertiser_landing_pages_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}
/// Gets one advertiser by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertisers_get_execute()` to send, or `dfareporting_advertisers_get` for simplest API.

pub fn dfareporting_advertisers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}
/// Gets one advertiser by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertisers_get_execute()` or `dfareporting_advertisers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}
/// Gets one advertiser by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertisers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertisers_get_task()`.
/// For the simplest API, use `dfareporting_advertisers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertisers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertisers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertisers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertisersGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/advertisers/{advertisersId}
/// Gets one advertiser by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertisers_get_builder()` + `dfareporting_advertisers_get_execute()`.
/// For task-level control, use `dfareporting_advertisers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_get(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertisersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertisers_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertisers_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/advertisers
/// Inserts a new advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertisers_insert_execute()` to send, or `dfareporting_advertisers_insert` for simplest API.

pub fn dfareporting_advertisers_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/advertisers
/// Inserts a new advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertisers_insert_execute()` or `dfareporting_advertisers_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/advertisers
/// Inserts a new advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertisers_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertisers_insert_task()`.
/// For the simplest API, use `dfareporting_advertisers_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertisers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertisers_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertisers_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertisersInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/advertisers
/// Inserts a new advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertisers_insert_builder()` + `dfareporting_advertisers_insert_execute()`.
/// For task-level control, use `dfareporting_advertisers_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_insert(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertisersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertisers_insert_builder(client, &args.profileId)?;
    dfareporting_advertisers_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers
/// Retrieves a list of advertisers, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertisers_list_execute()` to send, or `dfareporting_advertisers_list` for simplest API.

pub fn dfareporting_advertisers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserGroupIds: &Option<String>,
    floodlightConfigurationIds: &Option<String>,
    ids: &Option<String>,
    includeAdvertisersWithoutGroupsOnly: &Option<String>,
    maxResults: &Option<String>,
    onlyParent: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    status: &Option<String>,
    subaccountId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserGroupIds.as_ref() {
        query_parts.push(format!("advertiserGroupIds={}", val));
    }
    if let Some(val) = floodlightConfigurationIds.as_ref() {
        query_parts.push(format!("floodlightConfigurationIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = includeAdvertisersWithoutGroupsOnly.as_ref() {
        query_parts.push(format!("includeAdvertisersWithoutGroupsOnly={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = onlyParent.as_ref() {
        query_parts.push(format!("onlyParent={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/advertisers
/// Retrieves a list of advertisers, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertisers_list_execute()` or `dfareporting_advertisers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvertisersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdvertisersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/advertisers
/// Retrieves a list of advertisers, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertisers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertisers_list_task()`.
/// For the simplest API, use `dfareporting_advertisers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertisers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertisersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_advertisers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertisers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertisersListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserGroupIds
    pub advertiserGroupIds: Option<String>,
    /// Query parameter: floodlightConfigurationIds
    pub floodlightConfigurationIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: includeAdvertisersWithoutGroupsOnly
    pub includeAdvertisersWithoutGroupsOnly: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: onlyParent
    pub onlyParent: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
}

/// GET userprofiles/{userprofilesId}/advertisers
/// Retrieves a list of advertisers, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertisers_list_builder()` + `dfareporting_advertisers_list_execute()`.
/// For task-level control, use `dfareporting_advertisers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_list(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertisersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdvertisersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_advertisers_list_builder(
        client,
        &args.profileId,
        &args.advertiserGroupIds,
        &args.floodlightConfigurationIds,
        &args.ids,
        &args.includeAdvertisersWithoutGroupsOnly,
        &args.maxResults,
        &args.onlyParent,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.status,
        &args.subaccountId,
    )?;
    dfareporting_advertisers_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertisers_patch_execute()` to send, or `dfareporting_advertisers_patch` for simplest API.

pub fn dfareporting_advertisers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertisers_patch_execute()` or `dfareporting_advertisers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertisers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertisers_patch_task()`.
/// For the simplest API, use `dfareporting_advertisers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertisers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertisers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertisers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertisersPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertisers_patch_builder()` + `dfareporting_advertisers_patch_execute()`.
/// For task-level control, use `dfareporting_advertisers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_patch(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertisersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertisers_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_advertisers_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_advertisers_update_execute()` to send, or `dfareporting_advertisers_update` for simplest API.

pub fn dfareporting_advertisers_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/advertisers",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_advertisers_update_execute()` or `dfareporting_advertisers_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_advertisers_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_advertisers_update_task()`.
/// For the simplest API, use `dfareporting_advertisers_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_advertisers_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_advertisers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_advertisers_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_advertisers_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdvertisersUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/advertisers
/// Updates an existing advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_advertisers_update_builder()` + `dfareporting_advertisers_update_execute()`.
/// For task-level control, use `dfareporting_advertisers_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_advertisers_update(
    client: &SimpleHttpClient,
    args: &DfareportingAdvertisersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_advertisers_update_builder(client, &args.profileId)?;
    dfareporting_advertisers_update_execute(builder)
}

/// POST userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Inserts a new billing assignment and returns the new assignment. Only one of advertiser_id or campaign_id is support per request. If the new assignment has no effect (assigning a campaign to the parent advertiser billing profile or assigning an advertiser to the account billing profile), no assignment will be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_assignments_insert_execute()` to send, or `dfareporting_billing_assignments_insert` for simplest API.

pub fn dfareporting_billing_assignments_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    billingProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles/{}/billingAssignments",
        profileId,
        billingProfileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Inserts a new billing assignment and returns the new assignment. Only one of advertiser_id or campaign_id is support per request. If the new assignment has no effect (assigning a campaign to the parent advertiser billing profile or assigning an advertiser to the account billing profile), no assignment will be returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_assignments_insert_execute()` or `dfareporting_billing_assignments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_assignments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_assignments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingAssignment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Inserts a new billing assignment and returns the new assignment. Only one of advertiser_id or campaign_id is support per request. If the new assignment has no effect (assigning a campaign to the parent advertiser billing profile or assigning an advertiser to the account billing profile), no assignment will be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_assignments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_assignments_insert_task()`.
/// For the simplest API, use `dfareporting_billing_assignments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_assignments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_assignments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_assignments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_assignments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingAssignmentsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: billingProfileId
    pub billingProfileId: String,
}

/// POST userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Inserts a new billing assignment and returns the new assignment. Only one of advertiser_id or campaign_id is support per request. If the new assignment has no effect (assigning a campaign to the parent advertiser billing profile or assigning an advertiser to the account billing profile), no assignment will be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_assignments_insert_builder()` + `dfareporting_billing_assignments_insert_execute()`.
/// For task-level control, use `dfareporting_billing_assignments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_assignments_insert(
    client: &SimpleHttpClient,
    args: &DfareportingBillingAssignmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_billing_assignments_insert_builder(
        client,
        &args.profileId,
        &args.billingProfileId,
    )?;
    dfareporting_billing_assignments_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Retrieves a list of billing assignments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_assignments_list_execute()` to send, or `dfareporting_billing_assignments_list` for simplest API.

pub fn dfareporting_billing_assignments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    billingProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles/{}/billingAssignments",
        profileId,
        billingProfileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Retrieves a list of billing assignments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_assignments_list_execute()` or `dfareporting_billing_assignments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_assignments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_assignments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingAssignmentsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingAssignmentsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Retrieves a list of billing assignments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_assignments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_assignments_list_task()`.
/// For the simplest API, use `dfareporting_billing_assignments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_assignments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_assignments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingAssignmentsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_assignments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_assignments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingAssignmentsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: billingProfileId
    pub billingProfileId: String,
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingAssignments
/// Retrieves a list of billing assignments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_assignments_list_builder()` + `dfareporting_billing_assignments_list_execute()`.
/// For task-level control, use `dfareporting_billing_assignments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_assignments_list(
    client: &SimpleHttpClient,
    args: &DfareportingBillingAssignmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingAssignmentsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_billing_assignments_list_builder(
        client,
        &args.profileId,
        &args.billingProfileId,
    )?;
    dfareporting_billing_assignments_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}
/// Gets one billing profile by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_profiles_get_execute()` to send, or `dfareporting_billing_profiles_get` for simplest API.

pub fn dfareporting_billing_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}
/// Gets one billing profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_profiles_get_execute()` or `dfareporting_billing_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}
/// Gets one billing profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_profiles_get_task()`.
/// For the simplest API, use `dfareporting_billing_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingProfilesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}
/// Gets one billing profile by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_profiles_get_builder()` + `dfareporting_billing_profiles_get_execute()`.
/// For task-level control, use `dfareporting_billing_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_get(
    client: &SimpleHttpClient,
    args: &DfareportingBillingProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_billing_profiles_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_billing_profiles_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles
/// Retrieves a list of billing profiles, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_profiles_list_execute()` to send, or `dfareporting_billing_profiles_list` for simplest API.

pub fn dfareporting_billing_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    currency_code: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    name: &Option<String>,
    onlySuggestion: &Option<String>,
    pageToken: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    status: &Option<String>,
    subaccountIds: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currency_code.as_ref() {
        query_parts.push(format!("currency_code={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = onlySuggestion.as_ref() {
        query_parts.push(format!("onlySuggestion={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = subaccountIds.as_ref() {
        query_parts.push(format!("subaccountIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles
/// Retrieves a list of billing profiles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_profiles_list_execute()` or `dfareporting_billing_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingProfilesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingProfilesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/billingProfiles
/// Retrieves a list of billing profiles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_profiles_list_task()`.
/// For the simplest API, use `dfareporting_billing_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingProfilesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: currency_code
    pub currency_code: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: onlySuggestion
    pub onlySuggestion: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
    /// Query parameter: subaccountIds
    pub subaccountIds: Option<String>,
}

/// GET userprofiles/{userprofilesId}/billingProfiles
/// Retrieves a list of billing profiles, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_profiles_list_builder()` + `dfareporting_billing_profiles_list_execute()`.
/// For task-level control, use `dfareporting_billing_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_list(
    client: &SimpleHttpClient,
    args: &DfareportingBillingProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BillingProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_billing_profiles_list_builder(
        client,
        &args.profileId,
        &args.currency_code,
        &args.ids,
        &args.maxResults,
        &args.name,
        &args.onlySuggestion,
        &args.pageToken,
        &args.sortField,
        &args.sortOrder,
        &args.status,
        &args.subaccountIds,
    )?;
    dfareporting_billing_profiles_list_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/billingProfiles
/// Updates an existing billing profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_profiles_update_execute()` to send, or `dfareporting_billing_profiles_update` for simplest API.

pub fn dfareporting_billing_profiles_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/billingProfiles
/// Updates an existing billing profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_profiles_update_execute()` or `dfareporting_billing_profiles_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/billingProfiles
/// Updates an existing billing profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_profiles_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_profiles_update_task()`.
/// For the simplest API, use `dfareporting_billing_profiles_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_profiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_profiles_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_profiles_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingProfilesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/billingProfiles
/// Updates an existing billing profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_profiles_update_builder()` + `dfareporting_billing_profiles_update_execute()`.
/// For task-level control, use `dfareporting_billing_profiles_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_profiles_update(
    client: &SimpleHttpClient,
    args: &DfareportingBillingProfilesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_billing_profiles_update_builder(client, &args.profileId)?;
    dfareporting_billing_profiles_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingRates
/// Retrieves a list of billing rates. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_billing_rates_list_execute()` to send, or `dfareporting_billing_rates_list` for simplest API.

pub fn dfareporting_billing_rates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    billingProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/billingProfiles/{}/billingRates",
        profileId,
        billingProfileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingRates
/// Retrieves a list of billing rates. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_billing_rates_list_execute()` or `dfareporting_billing_rates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_rates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_rates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BillingRatesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BillingRatesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingRates
/// Retrieves a list of billing rates. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_billing_rates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_billing_rates_list_task()`.
/// For the simplest API, use `dfareporting_billing_rates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_billing_rates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_billing_rates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingRatesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_billing_rates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_billing_rates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBillingRatesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: billingProfileId
    pub billingProfileId: String,
}

/// GET userprofiles/{userprofilesId}/billingProfiles/{billingProfilesId}/billingRates
/// Retrieves a list of billing rates. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_billing_rates_list_builder()` + `dfareporting_billing_rates_list_execute()`.
/// For task-level control, use `dfareporting_billing_rates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_billing_rates_list(
    client: &SimpleHttpClient,
    args: &DfareportingBillingRatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BillingRatesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_billing_rates_list_builder(client, &args.profileId, &args.billingProfileId)?;
    dfareporting_billing_rates_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/browsers
/// Retrieves a list of browsers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_browsers_list_execute()` to send, or `dfareporting_browsers_list` for simplest API.

pub fn dfareporting_browsers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/browsers",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/browsers
/// Retrieves a list of browsers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_browsers_list_execute()` or `dfareporting_browsers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_browsers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_browsers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BrowsersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BrowsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/browsers
/// Retrieves a list of browsers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_browsers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_browsers_list_task()`.
/// For the simplest API, use `dfareporting_browsers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_browsers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_browsers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BrowsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_browsers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_browsers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingBrowsersListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/browsers
/// Retrieves a list of browsers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_browsers_list_builder()` + `dfareporting_browsers_list_execute()`.
/// For task-level control, use `dfareporting_browsers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_browsers_list(
    client: &SimpleHttpClient,
    args: &DfareportingBrowsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BrowsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_browsers_list_builder(client, &args.profileId)?;
    dfareporting_browsers_list_execute(builder)
}

/// POST userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Associates a creative with the specified campaign. This method creates a default ad with dimensions matching the creative in the campaign if such a default ad does not exist already.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaign_creative_associations_insert_execute()` to send, or `dfareporting_campaign_creative_associations_insert` for simplest API.

pub fn dfareporting_campaign_creative_associations_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    campaignId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns/{}/campaignCreativeAssociations",
        profileId,
        campaignId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Associates a creative with the specified campaign. This method creates a default ad with dimensions matching the creative in the campaign if such a default ad does not exist already.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaign_creative_associations_insert_execute()` or `dfareporting_campaign_creative_associations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaign_creative_associations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaign_creative_associations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CampaignCreativeAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CampaignCreativeAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Associates a creative with the specified campaign. This method creates a default ad with dimensions matching the creative in the campaign if such a default ad does not exist already.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaign_creative_associations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaign_creative_associations_insert_task()`.
/// For the simplest API, use `dfareporting_campaign_creative_associations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaign_creative_associations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaign_creative_associations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CampaignCreativeAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_campaign_creative_associations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaign_creative_associations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignCreativeAssociationsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: campaignId
    pub campaignId: String,
}

/// POST userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Associates a creative with the specified campaign. This method creates a default ad with dimensions matching the creative in the campaign if such a default ad does not exist already.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaign_creative_associations_insert_builder()` + `dfareporting_campaign_creative_associations_insert_execute()`.
/// For task-level control, use `dfareporting_campaign_creative_associations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaign_creative_associations_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignCreativeAssociationsInsertArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CampaignCreativeAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_campaign_creative_associations_insert_builder(
        client,
        &args.profileId,
        &args.campaignId,
    )?;
    dfareporting_campaign_creative_associations_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Retrieves the list of creative IDs associated with the specified campaign. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaign_creative_associations_list_execute()` to send, or `dfareporting_campaign_creative_associations_list` for simplest API.

pub fn dfareporting_campaign_creative_associations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    campaignId: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns/{}/campaignCreativeAssociations",
        profileId,
        campaignId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Retrieves the list of creative IDs associated with the specified campaign. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaign_creative_associations_list_execute()` or `dfareporting_campaign_creative_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaign_creative_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaign_creative_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CampaignCreativeAssociationsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CampaignCreativeAssociationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Retrieves the list of creative IDs associated with the specified campaign. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaign_creative_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaign_creative_associations_list_task()`.
/// For the simplest API, use `dfareporting_campaign_creative_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaign_creative_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaign_creative_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CampaignCreativeAssociationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_campaign_creative_associations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaign_creative_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignCreativeAssociationsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: campaignId
    pub campaignId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}/campaignCreativeAssociations
/// Retrieves the list of creative IDs associated with the specified campaign. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaign_creative_associations_list_builder()` + `dfareporting_campaign_creative_associations_list_execute()`.
/// For task-level control, use `dfareporting_campaign_creative_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaign_creative_associations_list(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignCreativeAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CampaignCreativeAssociationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_campaign_creative_associations_list_builder(
        client,
        &args.profileId,
        &args.campaignId,
        &args.maxResults,
        &args.pageToken,
        &args.sortOrder,
    )?;
    dfareporting_campaign_creative_associations_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}
/// Gets one campaign by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaigns_get_execute()` to send, or `dfareporting_campaigns_get` for simplest API.

pub fn dfareporting_campaigns_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}
/// Gets one campaign by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaigns_get_execute()` or `dfareporting_campaigns_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}
/// Gets one campaign by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaigns_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaigns_get_task()`.
/// For the simplest API, use `dfareporting_campaigns_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaigns_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_campaigns_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaigns_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/campaigns/{campaignsId}
/// Gets one campaign by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaigns_get_builder()` + `dfareporting_campaigns_get_execute()`.
/// For task-level control, use `dfareporting_campaigns_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_get(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_campaigns_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_campaigns_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/campaigns
/// Inserts a new campaign.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaigns_insert_execute()` to send, or `dfareporting_campaigns_insert` for simplest API.

pub fn dfareporting_campaigns_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/campaigns
/// Inserts a new campaign.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaigns_insert_execute()` or `dfareporting_campaigns_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/campaigns
/// Inserts a new campaign.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaigns_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaigns_insert_task()`.
/// For the simplest API, use `dfareporting_campaigns_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaigns_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_campaigns_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaigns_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/campaigns
/// Inserts a new campaign.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaigns_insert_builder()` + `dfareporting_campaigns_insert_execute()`.
/// For task-level control, use `dfareporting_campaigns_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_campaigns_insert_builder(client, &args.profileId)?;
    dfareporting_campaigns_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns
/// Retrieves a list of campaigns, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaigns_list_execute()` to send, or `dfareporting_campaigns_list` for simplest API.

pub fn dfareporting_campaigns_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserGroupIds: &Option<String>,
    advertiserIds: &Option<String>,
    archived: &Option<String>,
    atLeastOneOptimizationActivity: &Option<String>,
    excludedIds: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    overriddenEventTagId: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    subaccountId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserGroupIds.as_ref() {
        query_parts.push(format!("advertiserGroupIds={}", val));
    }
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = archived.as_ref() {
        query_parts.push(format!("archived={}", val));
    }
    if let Some(val) = atLeastOneOptimizationActivity.as_ref() {
        query_parts.push(format!("atLeastOneOptimizationActivity={}", val));
    }
    if let Some(val) = excludedIds.as_ref() {
        query_parts.push(format!("excludedIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = overriddenEventTagId.as_ref() {
        query_parts.push(format!("overriddenEventTagId={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/campaigns
/// Retrieves a list of campaigns, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaigns_list_execute()` or `dfareporting_campaigns_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CampaignsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CampaignsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/campaigns
/// Retrieves a list of campaigns, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaigns_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaigns_list_task()`.
/// For the simplest API, use `dfareporting_campaigns_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaigns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CampaignsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_campaigns_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaigns_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserGroupIds
    pub advertiserGroupIds: Option<String>,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: archived
    pub archived: Option<String>,
    /// Query parameter: atLeastOneOptimizationActivity
    pub atLeastOneOptimizationActivity: Option<String>,
    /// Query parameter: excludedIds
    pub excludedIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: overriddenEventTagId
    pub overriddenEventTagId: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
}

/// GET userprofiles/{userprofilesId}/campaigns
/// Retrieves a list of campaigns, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaigns_list_builder()` + `dfareporting_campaigns_list_execute()`.
/// For task-level control, use `dfareporting_campaigns_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_list(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CampaignsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_campaigns_list_builder(
        client,
        &args.profileId,
        &args.advertiserGroupIds,
        &args.advertiserIds,
        &args.archived,
        &args.atLeastOneOptimizationActivity,
        &args.excludedIds,
        &args.ids,
        &args.maxResults,
        &args.overriddenEventTagId,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.subaccountId,
    )?;
    dfareporting_campaigns_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaigns_patch_execute()` to send, or `dfareporting_campaigns_patch` for simplest API.

pub fn dfareporting_campaigns_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaigns_patch_execute()` or `dfareporting_campaigns_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaigns_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaigns_patch_task()`.
/// For the simplest API, use `dfareporting_campaigns_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaigns_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_campaigns_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaigns_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaigns_patch_builder()` + `dfareporting_campaigns_patch_execute()`.
/// For task-level control, use `dfareporting_campaigns_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_patch(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_campaigns_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_campaigns_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_campaigns_update_execute()` to send, or `dfareporting_campaigns_update` for simplest API.

pub fn dfareporting_campaigns_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/campaigns",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_campaigns_update_execute()` or `dfareporting_campaigns_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_campaigns_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_campaigns_update_task()`.
/// For the simplest API, use `dfareporting_campaigns_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_campaigns_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_campaigns_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_campaigns_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_campaigns_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCampaignsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/campaigns
/// Updates an existing campaign.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_campaigns_update_builder()` + `dfareporting_campaigns_update_execute()`.
/// For task-level control, use `dfareporting_campaigns_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_campaigns_update(
    client: &SimpleHttpClient,
    args: &DfareportingCampaignsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_campaigns_update_builder(client, &args.profileId)?;
    dfareporting_campaigns_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/changeLogs/{changeLogsId}
/// Gets one change log by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_change_logs_get_execute()` to send, or `dfareporting_change_logs_get` for simplest API.

pub fn dfareporting_change_logs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/changeLogs/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/changeLogs/{changeLogsId}
/// Gets one change log by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_change_logs_get_execute()` or `dfareporting_change_logs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_change_logs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_change_logs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChangeLog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChangeLog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/changeLogs/{changeLogsId}
/// Gets one change log by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_change_logs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_change_logs_get_task()`.
/// For the simplest API, use `dfareporting_change_logs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_change_logs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_change_logs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeLog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_change_logs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_change_logs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingChangeLogsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/changeLogs/{changeLogsId}
/// Gets one change log by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_change_logs_get_builder()` + `dfareporting_change_logs_get_execute()`.
/// For task-level control, use `dfareporting_change_logs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_change_logs_get(
    client: &SimpleHttpClient,
    args: &DfareportingChangeLogsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeLog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_change_logs_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_change_logs_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/changeLogs
/// Retrieves a list of change logs. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_change_logs_list_execute()` to send, or `dfareporting_change_logs_list` for simplest API.

pub fn dfareporting_change_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    action: &Option<String>,
    ids: &Option<String>,
    maxChangeTime: &Option<String>,
    maxResults: &Option<String>,
    minChangeTime: &Option<String>,
    objectIds: &Option<String>,
    objectType: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    userProfileIds: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/changeLogs",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action.as_ref() {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxChangeTime.as_ref() {
        query_parts.push(format!("maxChangeTime={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = minChangeTime.as_ref() {
        query_parts.push(format!("minChangeTime={}", val));
    }
    if let Some(val) = objectIds.as_ref() {
        query_parts.push(format!("objectIds={}", val));
    }
    if let Some(val) = objectType.as_ref() {
        query_parts.push(format!("objectType={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = userProfileIds.as_ref() {
        query_parts.push(format!("userProfileIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/changeLogs
/// Retrieves a list of change logs. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_change_logs_list_execute()` or `dfareporting_change_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_change_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_change_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChangeLogsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChangeLogsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/changeLogs
/// Retrieves a list of change logs. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_change_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_change_logs_list_task()`.
/// For the simplest API, use `dfareporting_change_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_change_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_change_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeLogsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_change_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_change_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingChangeLogsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: action
    pub action: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxChangeTime
    pub maxChangeTime: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: minChangeTime
    pub minChangeTime: Option<String>,
    /// Query parameter: objectIds
    pub objectIds: Option<String>,
    /// Query parameter: objectType
    pub objectType: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: userProfileIds
    pub userProfileIds: Option<String>,
}

/// GET userprofiles/{userprofilesId}/changeLogs
/// Retrieves a list of change logs. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_change_logs_list_builder()` + `dfareporting_change_logs_list_execute()`.
/// For task-level control, use `dfareporting_change_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_change_logs_list(
    client: &SimpleHttpClient,
    args: &DfareportingChangeLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeLogsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_change_logs_list_builder(
        client,
        &args.profileId,
        &args.action,
        &args.ids,
        &args.maxChangeTime,
        &args.maxResults,
        &args.minChangeTime,
        &args.objectIds,
        &args.objectType,
        &args.pageToken,
        &args.searchString,
        &args.userProfileIds,
    )?;
    dfareporting_change_logs_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/cities
/// Retrieves a list of cities, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_cities_list_execute()` to send, or `dfareporting_cities_list` for simplest API.

pub fn dfareporting_cities_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    countryDartIds: &Option<String>,
    dartIds: &Option<String>,
    namePrefix: &Option<String>,
    regionDartIds: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/cities",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = countryDartIds.as_ref() {
        query_parts.push(format!("countryDartIds={}", val));
    }
    if let Some(val) = dartIds.as_ref() {
        query_parts.push(format!("dartIds={}", val));
    }
    if let Some(val) = namePrefix.as_ref() {
        query_parts.push(format!("namePrefix={}", val));
    }
    if let Some(val) = regionDartIds.as_ref() {
        query_parts.push(format!("regionDartIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/cities
/// Retrieves a list of cities, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_cities_list_execute()` or `dfareporting_cities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_cities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_cities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CitiesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CitiesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/cities
/// Retrieves a list of cities, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_cities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_cities_list_task()`.
/// For the simplest API, use `dfareporting_cities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_cities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_cities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CitiesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_cities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_cities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCitiesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: countryDartIds
    pub countryDartIds: Option<String>,
    /// Query parameter: dartIds
    pub dartIds: Option<String>,
    /// Query parameter: namePrefix
    pub namePrefix: Option<String>,
    /// Query parameter: regionDartIds
    pub regionDartIds: Option<String>,
}

/// GET userprofiles/{userprofilesId}/cities
/// Retrieves a list of cities, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_cities_list_builder()` + `dfareporting_cities_list_execute()`.
/// For task-level control, use `dfareporting_cities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_cities_list(
    client: &SimpleHttpClient,
    args: &DfareportingCitiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CitiesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_cities_list_builder(
        client,
        &args.profileId,
        &args.countryDartIds,
        &args.dartIds,
        &args.namePrefix,
        &args.regionDartIds,
    )?;
    dfareporting_cities_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/connectionTypes/{connectionTypesId}
/// Gets one connection type by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_connection_types_get_execute()` to send, or `dfareporting_connection_types_get` for simplest API.

pub fn dfareporting_connection_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/connectionTypes/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/connectionTypes/{connectionTypesId}
/// Gets one connection type by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_connection_types_get_execute()` or `dfareporting_connection_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_connection_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_connection_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConnectionType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/connectionTypes/{connectionTypesId}
/// Gets one connection type by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_connection_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_connection_types_get_task()`.
/// For the simplest API, use `dfareporting_connection_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_connection_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_connection_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectionType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_connection_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_connection_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingConnectionTypesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/connectionTypes/{connectionTypesId}
/// Gets one connection type by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_connection_types_get_builder()` + `dfareporting_connection_types_get_execute()`.
/// For task-level control, use `dfareporting_connection_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_connection_types_get(
    client: &SimpleHttpClient,
    args: &DfareportingConnectionTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectionType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_connection_types_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_connection_types_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/connectionTypes
/// Retrieves a list of connection types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_connection_types_list_execute()` to send, or `dfareporting_connection_types_list` for simplest API.

pub fn dfareporting_connection_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/connectionTypes",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/connectionTypes
/// Retrieves a list of connection types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_connection_types_list_execute()` or `dfareporting_connection_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_connection_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_connection_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionTypesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConnectionTypesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/connectionTypes
/// Retrieves a list of connection types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_connection_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_connection_types_list_task()`.
/// For the simplest API, use `dfareporting_connection_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_connection_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_connection_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConnectionTypesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_connection_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_connection_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingConnectionTypesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/connectionTypes
/// Retrieves a list of connection types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_connection_types_list_builder()` + `dfareporting_connection_types_list_execute()`.
/// For task-level control, use `dfareporting_connection_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_connection_types_list(
    client: &SimpleHttpClient,
    args: &DfareportingConnectionTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConnectionTypesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_connection_types_list_builder(client, &args.profileId)?;
    dfareporting_connection_types_list_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Deletes an existing content category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_delete_execute()` to send, or `dfareporting_content_categories_delete` for simplest API.

pub fn dfareporting_content_categories_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Deletes an existing content category.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_delete_execute()` or `dfareporting_content_categories_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Deletes an existing content category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_delete_task()`.
/// For the simplest API, use `dfareporting_content_categories_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Deletes an existing content category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_delete_builder()` + `dfareporting_content_categories_delete_execute()`.
/// For task-level control, use `dfareporting_content_categories_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_delete(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dfareporting_content_categories_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_content_categories_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Gets one content category by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_get_execute()` to send, or `dfareporting_content_categories_get` for simplest API.

pub fn dfareporting_content_categories_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Gets one content category by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_get_execute()` or `dfareporting_content_categories_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContentCategory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContentCategory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Gets one content category by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_get_task()`.
/// For the simplest API, use `dfareporting_content_categories_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/contentCategories/{contentCategoriesId}
/// Gets one content category by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_get_builder()` + `dfareporting_content_categories_get_execute()`.
/// For task-level control, use `dfareporting_content_categories_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_get(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_content_categories_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_content_categories_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/contentCategories
/// Inserts a new content category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_insert_execute()` to send, or `dfareporting_content_categories_insert` for simplest API.

pub fn dfareporting_content_categories_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/contentCategories
/// Inserts a new content category.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_insert_execute()` or `dfareporting_content_categories_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContentCategory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContentCategory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/contentCategories
/// Inserts a new content category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_insert_task()`.
/// For the simplest API, use `dfareporting_content_categories_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/contentCategories
/// Inserts a new content category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_insert_builder()` + `dfareporting_content_categories_insert_execute()`.
/// For task-level control, use `dfareporting_content_categories_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_insert(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_content_categories_insert_builder(client, &args.profileId)?;
    dfareporting_content_categories_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/contentCategories
/// Retrieves a list of content categories, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_list_execute()` to send, or `dfareporting_content_categories_list` for simplest API.

pub fn dfareporting_content_categories_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/contentCategories
/// Retrieves a list of content categories, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_list_execute()` or `dfareporting_content_categories_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContentCategoriesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContentCategoriesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/contentCategories
/// Retrieves a list of content categories, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_list_task()`.
/// For the simplest API, use `dfareporting_content_categories_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ContentCategoriesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/contentCategories
/// Retrieves a list of content categories, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_list_builder()` + `dfareporting_content_categories_list_execute()`.
/// For task-level control, use `dfareporting_content_categories_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_list(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ContentCategoriesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_content_categories_list_builder(
        client,
        &args.profileId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_content_categories_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_patch_execute()` to send, or `dfareporting_content_categories_patch` for simplest API.

pub fn dfareporting_content_categories_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_patch_execute()` or `dfareporting_content_categories_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContentCategory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContentCategory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_patch_task()`.
/// For the simplest API, use `dfareporting_content_categories_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_patch_builder()` + `dfareporting_content_categories_patch_execute()`.
/// For task-level control, use `dfareporting_content_categories_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_patch(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_content_categories_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_content_categories_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_content_categories_update_execute()` to send, or `dfareporting_content_categories_update` for simplest API.

pub fn dfareporting_content_categories_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/contentCategories",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_content_categories_update_execute()` or `dfareporting_content_categories_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContentCategory>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContentCategory = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_content_categories_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_content_categories_update_task()`.
/// For the simplest API, use `dfareporting_content_categories_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_content_categories_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_content_categories_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_content_categories_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_content_categories_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingContentCategoriesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/contentCategories
/// Updates an existing content category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_content_categories_update_builder()` + `dfareporting_content_categories_update_execute()`.
/// For task-level control, use `dfareporting_content_categories_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_content_categories_update(
    client: &SimpleHttpClient,
    args: &DfareportingContentCategoriesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContentCategory>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_content_categories_update_builder(client, &args.profileId)?;
    dfareporting_content_categories_update_execute(builder)
}

/// POST userprofiles/{profileId}/conversions/batchinsert
/// Inserts conversions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_conversions_batchinsert_execute()` to send, or `dfareporting_conversions_batchinsert` for simplest API.

pub fn dfareporting_conversions_batchinsert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/conversions/batchinsert",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/conversions/batchinsert
/// Inserts conversions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_conversions_batchinsert_execute()` or `dfareporting_conversions_batchinsert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_conversions_batchinsert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_conversions_batchinsert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionsBatchInsertResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionsBatchInsertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/conversions/batchinsert
/// Inserts conversions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_conversions_batchinsert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_conversions_batchinsert_task()`.
/// For the simplest API, use `dfareporting_conversions_batchinsert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_conversions_batchinsert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_conversions_batchinsert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConversionsBatchInsertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_conversions_batchinsert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_conversions_batchinsert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingConversionsBatchinsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{profileId}/conversions/batchinsert
/// Inserts conversions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_conversions_batchinsert_builder()` + `dfareporting_conversions_batchinsert_execute()`.
/// For task-level control, use `dfareporting_conversions_batchinsert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_conversions_batchinsert(
    client: &SimpleHttpClient,
    args: &DfareportingConversionsBatchinsertArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConversionsBatchInsertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_conversions_batchinsert_builder(client, &args.profileId)?;
    dfareporting_conversions_batchinsert_execute(builder)
}

/// POST userprofiles/{profileId}/conversions/batchupdate
/// Updates existing conversions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_conversions_batchupdate_execute()` to send, or `dfareporting_conversions_batchupdate` for simplest API.

pub fn dfareporting_conversions_batchupdate_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/conversions/batchupdate",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/conversions/batchupdate
/// Updates existing conversions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_conversions_batchupdate_execute()` or `dfareporting_conversions_batchupdate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_conversions_batchupdate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_conversions_batchupdate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionsBatchUpdateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionsBatchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/conversions/batchupdate
/// Updates existing conversions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_conversions_batchupdate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_conversions_batchupdate_task()`.
/// For the simplest API, use `dfareporting_conversions_batchupdate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_conversions_batchupdate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_conversions_batchupdate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConversionsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_conversions_batchupdate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_conversions_batchupdate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingConversionsBatchupdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{profileId}/conversions/batchupdate
/// Updates existing conversions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_conversions_batchupdate_builder()` + `dfareporting_conversions_batchupdate_execute()`.
/// For task-level control, use `dfareporting_conversions_batchupdate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_conversions_batchupdate(
    client: &SimpleHttpClient,
    args: &DfareportingConversionsBatchupdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConversionsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_conversions_batchupdate_builder(client, &args.profileId)?;
    dfareporting_conversions_batchupdate_execute(builder)
}

/// GET userprofiles/{userprofilesId}/countries/{countriesId}
/// Gets one country by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_countries_get_execute()` to send, or `dfareporting_countries_get` for simplest API.

pub fn dfareporting_countries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    dartId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/countries/{}",
        profileId, dartId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/countries/{countriesId}
/// Gets one country by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_countries_get_execute()` or `dfareporting_countries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_countries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_countries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Country>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Country = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/countries/{countriesId}
/// Gets one country by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_countries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_countries_get_task()`.
/// For the simplest API, use `dfareporting_countries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_countries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_countries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Country>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_countries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_countries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCountriesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: dartId
    pub dartId: String,
}

/// GET userprofiles/{userprofilesId}/countries/{countriesId}
/// Gets one country by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_countries_get_builder()` + `dfareporting_countries_get_execute()`.
/// For task-level control, use `dfareporting_countries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_countries_get(
    client: &SimpleHttpClient,
    args: &DfareportingCountriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Country>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_countries_get_builder(client, &args.profileId, &args.dartId)?;
    dfareporting_countries_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/countries
/// Retrieves a list of countries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_countries_list_execute()` to send, or `dfareporting_countries_list` for simplest API.

pub fn dfareporting_countries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/countries",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/countries
/// Retrieves a list of countries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_countries_list_execute()` or `dfareporting_countries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_countries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_countries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CountriesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CountriesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/countries
/// Retrieves a list of countries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_countries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_countries_list_task()`.
/// For the simplest API, use `dfareporting_countries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_countries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_countries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CountriesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_countries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_countries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCountriesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/countries
/// Retrieves a list of countries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_countries_list_builder()` + `dfareporting_countries_list_execute()`.
/// For task-level control, use `dfareporting_countries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_countries_list(
    client: &SimpleHttpClient,
    args: &DfareportingCountriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CountriesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_countries_list_builder(client, &args.profileId)?;
    dfareporting_countries_list_execute(builder)
}

/// POST userprofiles/{userprofilesId}/creativeAssets/{creativeAssetsId}/creativeAssets
/// Inserts a new creative asset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_assets_insert_execute()` to send, or `dfareporting_creative_assets_insert` for simplest API.

pub fn dfareporting_creative_assets_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeAssets/{}/creativeAssets",
        profileId,
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/creativeAssets/{creativeAssetsId}/creativeAssets
/// Inserts a new creative asset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_assets_insert_execute()` or `dfareporting_creative_assets_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_assets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_assets_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeAssetMetadata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeAssetMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/creativeAssets/{creativeAssetsId}/creativeAssets
/// Inserts a new creative asset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_assets_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_assets_insert_task()`.
/// For the simplest API, use `dfareporting_creative_assets_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_assets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_assets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeAssetMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_assets_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_assets_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeAssetsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST userprofiles/{userprofilesId}/creativeAssets/{creativeAssetsId}/creativeAssets
/// Inserts a new creative asset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_assets_insert_builder()` + `dfareporting_creative_assets_insert_execute()`.
/// For task-level control, use `dfareporting_creative_assets_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_assets_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeAssetsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeAssetMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_creative_assets_insert_builder(client, &args.profileId, &args.advertiserId)?;
    dfareporting_creative_assets_insert_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Deletes an existing creative field value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_delete_execute()` to send, or `dfareporting_creative_field_values_delete` for simplest API.

pub fn dfareporting_creative_field_values_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues/{}",
        profileId,
        creativeFieldId,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Deletes an existing creative field value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_delete_execute()` or `dfareporting_creative_field_values_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Deletes an existing creative field value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_delete_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Deletes an existing creative field value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_delete_builder()` + `dfareporting_creative_field_values_delete_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_delete(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_delete_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
        &args.id,
    )?;
    dfareporting_creative_field_values_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Gets one creative field value by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_get_execute()` to send, or `dfareporting_creative_field_values_get` for simplest API.

pub fn dfareporting_creative_field_values_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues/{}",
        profileId,
        creativeFieldId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Gets one creative field value by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_get_execute()` or `dfareporting_creative_field_values_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldValue>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Gets one creative field value by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_get_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues/{creativeFieldValuesId}
/// Gets one creative field value by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_get_builder()` + `dfareporting_creative_field_values_get_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_get(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_get_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
        &args.id,
    )?;
    dfareporting_creative_field_values_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Inserts a new creative field value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_insert_execute()` to send, or `dfareporting_creative_field_values_insert` for simplest API.

pub fn dfareporting_creative_field_values_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues",
        profileId,
        creativeFieldId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Inserts a new creative field value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_insert_execute()` or `dfareporting_creative_field_values_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldValue>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Inserts a new creative field value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_insert_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
}

/// POST userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Inserts a new creative field value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_insert_builder()` + `dfareporting_creative_field_values_insert_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_insert_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
    )?;
    dfareporting_creative_field_values_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Retrieves a list of creative field values, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_list_execute()` to send, or `dfareporting_creative_field_values_list` for simplest API.

pub fn dfareporting_creative_field_values_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues",
        profileId,
        creativeFieldId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Retrieves a list of creative field values, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_list_execute()` or `dfareporting_creative_field_values_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldValuesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldValuesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Retrieves a list of creative field values, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_list_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeFieldValuesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Retrieves a list of creative field values, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_list_builder()` + `dfareporting_creative_field_values_list_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_list(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeFieldValuesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_list_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_creative_field_values_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_patch_execute()` to send, or `dfareporting_creative_field_values_patch` for simplest API.

pub fn dfareporting_creative_field_values_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues",
        profileId,
        creativeFieldId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_patch_execute()` or `dfareporting_creative_field_values_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldValue>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_patch_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_patch_builder()` + `dfareporting_creative_field_values_patch_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_patch(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_patch_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
        &args.id,
    )?;
    dfareporting_creative_field_values_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_field_values_update_execute()` to send, or `dfareporting_creative_field_values_update` for simplest API.

pub fn dfareporting_creative_field_values_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    creativeFieldId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}/creativeFieldValues",
        profileId,
        creativeFieldId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_field_values_update_execute()` or `dfareporting_creative_field_values_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldValue>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_field_values_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_field_values_update_task()`.
/// For the simplest API, use `dfareporting_creative_field_values_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_field_values_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_field_values_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_field_values_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_field_values_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldValuesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: creativeFieldId
    pub creativeFieldId: String,
}

/// PUT userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}/creativeFieldValues
/// Updates an existing creative field value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_field_values_update_builder()` + `dfareporting_creative_field_values_update_execute()`.
/// For task-level control, use `dfareporting_creative_field_values_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_field_values_update(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldValuesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeFieldValue>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_field_values_update_builder(
        client,
        &args.profileId,
        &args.creativeFieldId,
    )?;
    dfareporting_creative_field_values_update_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Deletes an existing creative field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_delete_execute()` to send, or `dfareporting_creative_fields_delete` for simplest API.

pub fn dfareporting_creative_fields_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Deletes an existing creative field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_delete_execute()` or `dfareporting_creative_fields_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Deletes an existing creative field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_delete_task()`.
/// For the simplest API, use `dfareporting_creative_fields_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Deletes an existing creative field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_delete_builder()` + `dfareporting_creative_fields_delete_execute()`.
/// For task-level control, use `dfareporting_creative_fields_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_delete(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_creative_fields_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Gets one creative field by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_get_execute()` to send, or `dfareporting_creative_fields_get` for simplest API.

pub fn dfareporting_creative_fields_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Gets one creative field by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_get_execute()` or `dfareporting_creative_fields_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeField>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeField = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Gets one creative field by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_get_task()`.
/// For the simplest API, use `dfareporting_creative_fields_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/creativeFields/{creativeFieldsId}
/// Gets one creative field by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_get_builder()` + `dfareporting_creative_fields_get_execute()`.
/// For task-level control, use `dfareporting_creative_fields_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_get(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_creative_fields_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/creativeFields
/// Inserts a new creative field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_insert_execute()` to send, or `dfareporting_creative_fields_insert` for simplest API.

pub fn dfareporting_creative_fields_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/creativeFields
/// Inserts a new creative field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_insert_execute()` or `dfareporting_creative_fields_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeField>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeField = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/creativeFields
/// Inserts a new creative field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_insert_task()`.
/// For the simplest API, use `dfareporting_creative_fields_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/creativeFields
/// Inserts a new creative field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_insert_builder()` + `dfareporting_creative_fields_insert_execute()`.
/// For task-level control, use `dfareporting_creative_fields_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_insert_builder(client, &args.profileId)?;
    dfareporting_creative_fields_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields
/// Retrieves a list of creative fields, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_list_execute()` to send, or `dfareporting_creative_fields_list` for simplest API.

pub fn dfareporting_creative_fields_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserIds: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeFields
/// Retrieves a list of creative fields, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_list_execute()` or `dfareporting_creative_fields_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeFieldsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeFieldsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeFields
/// Retrieves a list of creative fields, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_list_task()`.
/// For the simplest API, use `dfareporting_creative_fields_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeFieldsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/creativeFields
/// Retrieves a list of creative fields, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_list_builder()` + `dfareporting_creative_fields_list_execute()`.
/// For task-level control, use `dfareporting_creative_fields_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_list(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeFieldsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_list_builder(
        client,
        &args.profileId,
        &args.advertiserIds,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_creative_fields_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_patch_execute()` to send, or `dfareporting_creative_fields_patch` for simplest API.

pub fn dfareporting_creative_fields_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_patch_execute()` or `dfareporting_creative_fields_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeField>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeField = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_patch_task()`.
/// For the simplest API, use `dfareporting_creative_fields_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_patch_builder()` + `dfareporting_creative_fields_patch_execute()`.
/// For task-level control, use `dfareporting_creative_fields_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_patch(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_creative_fields_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_fields_update_execute()` to send, or `dfareporting_creative_fields_update` for simplest API.

pub fn dfareporting_creative_fields_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeFields",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_fields_update_execute()` or `dfareporting_creative_fields_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeField>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeField = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_fields_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_fields_update_task()`.
/// For the simplest API, use `dfareporting_creative_fields_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_fields_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_fields_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_fields_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_fields_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeFieldsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/creativeFields
/// Updates an existing creative field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_fields_update_builder()` + `dfareporting_creative_fields_update_execute()`.
/// For task-level control, use `dfareporting_creative_fields_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_fields_update(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeFieldsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeField>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_fields_update_builder(client, &args.profileId)?;
    dfareporting_creative_fields_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeGroups/{creativeGroupsId}
/// Gets one creative group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_groups_get_execute()` to send, or `dfareporting_creative_groups_get` for simplest API.

pub fn dfareporting_creative_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeGroups/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeGroups/{creativeGroupsId}
/// Gets one creative group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_groups_get_execute()` or `dfareporting_creative_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeGroups/{creativeGroupsId}
/// Gets one creative group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_groups_get_task()`.
/// For the simplest API, use `dfareporting_creative_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/creativeGroups/{creativeGroupsId}
/// Gets one creative group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_groups_get_builder()` + `dfareporting_creative_groups_get_execute()`.
/// For task-level control, use `dfareporting_creative_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_creative_groups_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/creativeGroups
/// Inserts a new creative group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_groups_insert_execute()` to send, or `dfareporting_creative_groups_insert` for simplest API.

pub fn dfareporting_creative_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeGroups",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/creativeGroups
/// Inserts a new creative group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_groups_insert_execute()` or `dfareporting_creative_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/creativeGroups
/// Inserts a new creative group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_groups_insert_task()`.
/// For the simplest API, use `dfareporting_creative_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeGroupsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/creativeGroups
/// Inserts a new creative group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_groups_insert_builder()` + `dfareporting_creative_groups_insert_execute()`.
/// For task-level control, use `dfareporting_creative_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_groups_insert_builder(client, &args.profileId)?;
    dfareporting_creative_groups_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creativeGroups
/// Retrieves a list of creative groups, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_groups_list_execute()` to send, or `dfareporting_creative_groups_list` for simplest API.

pub fn dfareporting_creative_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserIds: &Option<String>,
    groupNumber: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = groupNumber.as_ref() {
        query_parts.push(format!("groupNumber={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creativeGroups
/// Retrieves a list of creative groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_groups_list_execute()` or `dfareporting_creative_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creativeGroups
/// Retrieves a list of creative groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_groups_list_task()`.
/// For the simplest API, use `dfareporting_creative_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: groupNumber
    pub groupNumber: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/creativeGroups
/// Retrieves a list of creative groups, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_groups_list_builder()` + `dfareporting_creative_groups_list_execute()`.
/// For task-level control, use `dfareporting_creative_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreativeGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_groups_list_builder(
        client,
        &args.profileId,
        &args.advertiserIds,
        &args.groupNumber,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_creative_groups_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_groups_patch_execute()` to send, or `dfareporting_creative_groups_patch` for simplest API.

pub fn dfareporting_creative_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_groups_patch_execute()` or `dfareporting_creative_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_groups_patch_task()`.
/// For the simplest API, use `dfareporting_creative_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeGroupsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_groups_patch_builder()` + `dfareporting_creative_groups_patch_execute()`.
/// For task-level control, use `dfareporting_creative_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_patch(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_groups_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_creative_groups_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creative_groups_update_execute()` to send, or `dfareporting_creative_groups_update` for simplest API.

pub fn dfareporting_creative_groups_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creativeGroups",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creative_groups_update_execute()` or `dfareporting_creative_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativeGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creative_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creative_groups_update_task()`.
/// For the simplest API, use `dfareporting_creative_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creative_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creative_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creative_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creative_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativeGroupsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/creativeGroups
/// Updates an existing creative group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creative_groups_update_builder()` + `dfareporting_creative_groups_update_execute()`.
/// For task-level control, use `dfareporting_creative_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creative_groups_update(
    client: &SimpleHttpClient,
    args: &DfareportingCreativeGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativeGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creative_groups_update_builder(client, &args.profileId)?;
    dfareporting_creative_groups_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creatives/{creativesId}
/// Gets one creative by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creatives_get_execute()` to send, or `dfareporting_creatives_get` for simplest API.

pub fn dfareporting_creatives_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creatives/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creatives/{creativesId}
/// Gets one creative by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creatives_get_execute()` or `dfareporting_creatives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creatives/{creativesId}
/// Gets one creative by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creatives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creatives_get_task()`.
/// For the simplest API, use `dfareporting_creatives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creatives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/creatives/{creativesId}
/// Gets one creative by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creatives_get_builder()` + `dfareporting_creatives_get_execute()`.
/// For task-level control, use `dfareporting_creatives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_get(
    client: &SimpleHttpClient,
    args: &DfareportingCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creatives_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_creatives_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/creatives
/// Inserts a new creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creatives_insert_execute()` to send, or `dfareporting_creatives_insert` for simplest API.

pub fn dfareporting_creatives_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creatives",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/creatives
/// Inserts a new creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creatives_insert_execute()` or `dfareporting_creatives_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/creatives
/// Inserts a new creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creatives_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creatives_insert_task()`.
/// For the simplest API, use `dfareporting_creatives_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creatives_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creatives_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creatives_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/creatives
/// Inserts a new creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creatives_insert_builder()` + `dfareporting_creatives_insert_execute()`.
/// For task-level control, use `dfareporting_creatives_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_insert(
    client: &SimpleHttpClient,
    args: &DfareportingCreativesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creatives_insert_builder(client, &args.profileId)?;
    dfareporting_creatives_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/creatives
/// Retrieves a list of creatives, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creatives_list_execute()` to send, or `dfareporting_creatives_list` for simplest API.

pub fn dfareporting_creatives_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    advertiserId: &Option<String>,
    archived: &Option<String>,
    campaignId: &Option<String>,
    companionCreativeIds: &Option<String>,
    creativeFieldIds: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    renderingIds: &Option<String>,
    searchString: &Option<String>,
    sizeIds: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    studioCreativeId: &Option<String>,
    types: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creatives",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = archived.as_ref() {
        query_parts.push(format!("archived={}", val));
    }
    if let Some(val) = campaignId.as_ref() {
        query_parts.push(format!("campaignId={}", val));
    }
    if let Some(val) = companionCreativeIds.as_ref() {
        query_parts.push(format!("companionCreativeIds={}", val));
    }
    if let Some(val) = creativeFieldIds.as_ref() {
        query_parts.push(format!("creativeFieldIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = renderingIds.as_ref() {
        query_parts.push(format!("renderingIds={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sizeIds.as_ref() {
        query_parts.push(format!("sizeIds={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = studioCreativeId.as_ref() {
        query_parts.push(format!("studioCreativeId={}", val));
    }
    if let Some(val) = types.as_ref() {
        query_parts.push(format!("types={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/creatives
/// Retrieves a list of creatives, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creatives_list_execute()` or `dfareporting_creatives_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreativesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreativesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/creatives
/// Retrieves a list of creatives, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creatives_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creatives_list_task()`.
/// For the simplest API, use `dfareporting_creatives_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_creatives_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: archived
    pub archived: Option<String>,
    /// Query parameter: campaignId
    pub campaignId: Option<String>,
    /// Query parameter: companionCreativeIds
    pub companionCreativeIds: Option<String>,
    /// Query parameter: creativeFieldIds
    pub creativeFieldIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: renderingIds
    pub renderingIds: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sizeIds
    pub sizeIds: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: studioCreativeId
    pub studioCreativeId: Option<String>,
    /// Query parameter: types
    pub types: Option<String>,
}

/// GET userprofiles/{userprofilesId}/creatives
/// Retrieves a list of creatives, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creatives_list_builder()` + `dfareporting_creatives_list_execute()`.
/// For task-level control, use `dfareporting_creatives_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_list(
    client: &SimpleHttpClient,
    args: &DfareportingCreativesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreativesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_creatives_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.advertiserId,
        &args.archived,
        &args.campaignId,
        &args.companionCreativeIds,
        &args.creativeFieldIds,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.renderingIds,
        &args.searchString,
        &args.sizeIds,
        &args.sortField,
        &args.sortOrder,
        &args.studioCreativeId,
        &args.types,
    )?;
    dfareporting_creatives_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/creatives
/// Updates an existing creative. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creatives_patch_execute()` to send, or `dfareporting_creatives_patch` for simplest API.

pub fn dfareporting_creatives_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creatives",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/creatives
/// Updates an existing creative. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creatives_patch_execute()` or `dfareporting_creatives_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/creatives
/// Updates an existing creative. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creatives_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creatives_patch_task()`.
/// For the simplest API, use `dfareporting_creatives_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creatives_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creatives_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creatives_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/creatives
/// Updates an existing creative. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creatives_patch_builder()` + `dfareporting_creatives_patch_execute()`.
/// For task-level control, use `dfareporting_creatives_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_patch(
    client: &SimpleHttpClient,
    args: &DfareportingCreativesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creatives_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_creatives_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/creatives
/// Updates an existing creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_creatives_update_execute()` to send, or `dfareporting_creatives_update` for simplest API.

pub fn dfareporting_creatives_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/creatives",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/creatives
/// Updates an existing creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_creatives_update_execute()` or `dfareporting_creatives_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/creatives
/// Updates an existing creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_creatives_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_creatives_update_task()`.
/// For the simplest API, use `dfareporting_creatives_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_creatives_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_creatives_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_creatives_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_creatives_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingCreativesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/creatives
/// Updates an existing creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_creatives_update_builder()` + `dfareporting_creatives_update_execute()`.
/// For task-level control, use `dfareporting_creatives_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_creatives_update(
    client: &SimpleHttpClient,
    args: &DfareportingCreativesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_creatives_update_builder(client, &args.profileId)?;
    dfareporting_creatives_update_execute(builder)
}

/// POST userprofiles/{profileId}/dimensionvalues/query
/// Retrieves list of report dimension values for a list of filters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dimension_values_query_execute()` to send, or `dfareporting_dimension_values_query` for simplest API.

pub fn dfareporting_dimension_values_query_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/dimensionvalues/query",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/dimensionvalues/query
/// Retrieves list of report dimension values for a list of filters.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dimension_values_query_execute()` or `dfareporting_dimension_values_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dimension_values_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dimension_values_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DimensionValueList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DimensionValueList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/dimensionvalues/query
/// Retrieves list of report dimension values for a list of filters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dimension_values_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dimension_values_query_task()`.
/// For the simplest API, use `dfareporting_dimension_values_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dimension_values_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dimension_values_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DimensionValueList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dimension_values_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dimension_values_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDimensionValuesQueryArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// POST userprofiles/{profileId}/dimensionvalues/query
/// Retrieves list of report dimension values for a list of filters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dimension_values_query_builder()` + `dfareporting_dimension_values_query_execute()`.
/// For task-level control, use `dfareporting_dimension_values_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dimension_values_query(
    client: &SimpleHttpClient,
    args: &DfareportingDimensionValuesQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DimensionValueList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dimension_values_query_builder(
        client,
        &args.profileId,
        &args.maxResults,
        &args.pageToken,
    )?;
    dfareporting_dimension_values_query_execute(builder)
}

/// GET userprofiles/{userprofilesId}/directorySites/{directorySitesId}
/// Gets one directory site by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_directory_sites_get_execute()` to send, or `dfareporting_directory_sites_get` for simplest API.

pub fn dfareporting_directory_sites_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/directorySites/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/directorySites/{directorySitesId}
/// Gets one directory site by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_directory_sites_get_execute()` or `dfareporting_directory_sites_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DirectorySite>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DirectorySite = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/directorySites/{directorySitesId}
/// Gets one directory site by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_directory_sites_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_directory_sites_get_task()`.
/// For the simplest API, use `dfareporting_directory_sites_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_directory_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectorySite>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_directory_sites_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_directory_sites_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDirectorySitesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/directorySites/{directorySitesId}
/// Gets one directory site by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_directory_sites_get_builder()` + `dfareporting_directory_sites_get_execute()`.
/// For task-level control, use `dfareporting_directory_sites_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_get(
    client: &SimpleHttpClient,
    args: &DfareportingDirectorySitesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectorySite>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_directory_sites_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_directory_sites_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/directorySites
/// Inserts a new directory site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_directory_sites_insert_execute()` to send, or `dfareporting_directory_sites_insert` for simplest API.

pub fn dfareporting_directory_sites_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/directorySites",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/directorySites
/// Inserts a new directory site.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_directory_sites_insert_execute()` or `dfareporting_directory_sites_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DirectorySite>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DirectorySite = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/directorySites
/// Inserts a new directory site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_directory_sites_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_directory_sites_insert_task()`.
/// For the simplest API, use `dfareporting_directory_sites_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_directory_sites_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectorySite>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_directory_sites_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_directory_sites_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDirectorySitesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/directorySites
/// Inserts a new directory site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_directory_sites_insert_builder()` + `dfareporting_directory_sites_insert_execute()`.
/// For task-level control, use `dfareporting_directory_sites_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_insert(
    client: &SimpleHttpClient,
    args: &DfareportingDirectorySitesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectorySite>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_directory_sites_insert_builder(client, &args.profileId)?;
    dfareporting_directory_sites_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/directorySites
/// Retrieves a list of directory sites, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_directory_sites_list_execute()` to send, or `dfareporting_directory_sites_list` for simplest API.

pub fn dfareporting_directory_sites_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    acceptsInStreamVideoPlacements: &Option<String>,
    acceptsInterstitialPlacements: &Option<String>,
    acceptsPublisherPaidPlacements: &Option<String>,
    active: &Option<String>,
    dfpNetworkCode: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/directorySites",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acceptsInStreamVideoPlacements.as_ref() {
        query_parts.push(format!("acceptsInStreamVideoPlacements={}", val));
    }
    if let Some(val) = acceptsInterstitialPlacements.as_ref() {
        query_parts.push(format!("acceptsInterstitialPlacements={}", val));
    }
    if let Some(val) = acceptsPublisherPaidPlacements.as_ref() {
        query_parts.push(format!("acceptsPublisherPaidPlacements={}", val));
    }
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = dfpNetworkCode.as_ref() {
        query_parts.push(format!("dfpNetworkCode={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/directorySites
/// Retrieves a list of directory sites, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_directory_sites_list_execute()` or `dfareporting_directory_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DirectorySitesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DirectorySitesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/directorySites
/// Retrieves a list of directory sites, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_directory_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_directory_sites_list_task()`.
/// For the simplest API, use `dfareporting_directory_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_directory_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_directory_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DirectorySitesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_directory_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_directory_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDirectorySitesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: acceptsInStreamVideoPlacements
    pub acceptsInStreamVideoPlacements: Option<String>,
    /// Query parameter: acceptsInterstitialPlacements
    pub acceptsInterstitialPlacements: Option<String>,
    /// Query parameter: acceptsPublisherPaidPlacements
    pub acceptsPublisherPaidPlacements: Option<String>,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: dfpNetworkCode
    pub dfpNetworkCode: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/directorySites
/// Retrieves a list of directory sites, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_directory_sites_list_builder()` + `dfareporting_directory_sites_list_execute()`.
/// For task-level control, use `dfareporting_directory_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_directory_sites_list(
    client: &SimpleHttpClient,
    args: &DfareportingDirectorySitesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DirectorySitesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_directory_sites_list_builder(
        client,
        &args.profileId,
        &args.acceptsInStreamVideoPlacements,
        &args.acceptsInterstitialPlacements,
        &args.acceptsPublisherPaidPlacements,
        &args.active,
        &args.dfpNetworkCode,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_directory_sites_list_execute(builder)
}

/// GET studio/dynamicFeeds/{dynamicFeedsId}
/// Gets a dynamic feed by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_feeds_get_execute()` to send, or `dfareporting_dynamic_feeds_get` for simplest API.

pub fn dfareporting_dynamic_feeds_get_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicFeedId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicFeeds/{}",
        dynamicFeedId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET studio/dynamicFeeds/{dynamicFeedsId}
/// Gets a dynamic feed by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_feeds_get_execute()` or `dfareporting_dynamic_feeds_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicFeed>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicFeed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET studio/dynamicFeeds/{dynamicFeedsId}
/// Gets a dynamic feed by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_feeds_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_feeds_get_task()`.
/// For the simplest API, use `dfareporting_dynamic_feeds_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_feeds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_feeds_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_feeds_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicFeedsGetArgs {
    /// Path parameter: dynamicFeedId
    pub dynamicFeedId: String,
}

/// GET studio/dynamicFeeds/{dynamicFeedsId}
/// Gets a dynamic feed by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_feeds_get_builder()` + `dfareporting_dynamic_feeds_get_execute()`.
/// For task-level control, use `dfareporting_dynamic_feeds_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_get(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicFeedsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_feeds_get_builder(client, &args.dynamicFeedId)?;
    dfareporting_dynamic_feeds_get_execute(builder)
}

/// POST studio/dynamicFeeds
/// Inserts a new dynamic feed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_feeds_insert_execute()` to send, or `dfareporting_dynamic_feeds_insert` for simplest API.

pub fn dfareporting_dynamic_feeds_insert_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicFeeds",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/dynamicFeeds
/// Inserts a new dynamic feed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_feeds_insert_execute()` or `dfareporting_dynamic_feeds_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicFeed>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicFeed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/dynamicFeeds
/// Inserts a new dynamic feed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_feeds_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_feeds_insert_task()`.
/// For the simplest API, use `dfareporting_dynamic_feeds_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_feeds_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_feeds_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST studio/dynamicFeeds
/// Inserts a new dynamic feed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_feeds_insert_builder()` + `dfareporting_dynamic_feeds_insert_execute()`.
/// For task-level control, use `dfareporting_dynamic_feeds_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_insert(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_feeds_insert_builder(client)?;
    dfareporting_dynamic_feeds_insert_execute(builder)
}

/// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform
/// Retransforms a dynamic feed. Only draft feeds can be retransformed (i.e. the feed has not been published).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_feeds_retransform_execute()` to send, or `dfareporting_dynamic_feeds_retransform` for simplest API.

pub fn dfareporting_dynamic_feeds_retransform_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicFeedId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicFeeds/{}/retransform",
        dynamicFeedId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform
/// Retransforms a dynamic feed. Only draft feeds can be retransformed (i.e. the feed has not been published).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_feeds_retransform_execute()` or `dfareporting_dynamic_feeds_retransform`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_retransform_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_retransform_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicFeed>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicFeed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform
/// Retransforms a dynamic feed. Only draft feeds can be retransformed (i.e. the feed has not been published).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_feeds_retransform_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_feeds_retransform_task()`.
/// For the simplest API, use `dfareporting_dynamic_feeds_retransform()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_retransform_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_feeds_retransform_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_feeds_retransform_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_feeds_retransform`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicFeedsRetransformArgs {
    /// Path parameter: dynamicFeedId
    pub dynamicFeedId: String,
}

/// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform
/// Retransforms a dynamic feed. Only draft feeds can be retransformed (i.e. the feed has not been published).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_feeds_retransform_builder()` + `dfareporting_dynamic_feeds_retransform_execute()`.
/// For task-level control, use `dfareporting_dynamic_feeds_retransform_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_retransform(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicFeedsRetransformArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_feeds_retransform_builder(client, &args.dynamicFeedId)?;
    dfareporting_dynamic_feeds_retransform_execute(builder)
}

/// PUT studio/dynamicFeeds
/// Updates a new dynamic feed. For draft feeds, only Element can be updated. For published feeds, only FeedSchedule can be updated. Other fields will be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_feeds_update_execute()` to send, or `dfareporting_dynamic_feeds_update` for simplest API.

pub fn dfareporting_dynamic_feeds_update_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicFeeds",);

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT studio/dynamicFeeds
/// Updates a new dynamic feed. For draft feeds, only Element can be updated. For published feeds, only FeedSchedule can be updated. Other fields will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_feeds_update_execute()` or `dfareporting_dynamic_feeds_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicFeed>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicFeed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT studio/dynamicFeeds
/// Updates a new dynamic feed. For draft feeds, only Element can be updated. For published feeds, only FeedSchedule can be updated. Other fields will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_feeds_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_feeds_update_task()`.
/// For the simplest API, use `dfareporting_dynamic_feeds_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_feeds_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_feeds_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_feeds_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT studio/dynamicFeeds
/// Updates a new dynamic feed. For draft feeds, only Element can be updated. For published feeds, only FeedSchedule can be updated. Other fields will be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_feeds_update_builder()` + `dfareporting_dynamic_feeds_update_execute()`.
/// For task-level control, use `dfareporting_dynamic_feeds_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_feeds_update(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicFeed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_feeds_update_builder(client)?;
    dfareporting_dynamic_feeds_update_execute(builder)
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}/generateCode
/// Generates code for a dynamic profile, which will need unescaping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_profiles_generate_code_execute()` to send, or `dfareporting_dynamic_profiles_generate_code` for simplest API.

pub fn dfareporting_dynamic_profiles_generate_code_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicProfiles/{}/generateCode",
        dynamicProfileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}/generateCode
/// Generates code for a dynamic profile, which will need unescaping.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_profiles_generate_code_execute()` or `dfareporting_dynamic_profiles_generate_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_generate_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_generate_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicProfileGenerateCodeResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicProfileGenerateCodeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}/generateCode
/// Generates code for a dynamic profile, which will need unescaping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_profiles_generate_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_profiles_generate_code_task()`.
/// For the simplest API, use `dfareporting_dynamic_profiles_generate_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_generate_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_profiles_generate_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DynamicProfileGenerateCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_profiles_generate_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_profiles_generate_code`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicProfilesGenerateCodeArgs {
    /// Path parameter: dynamicProfileId
    pub dynamicProfileId: String,
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}/generateCode
/// Generates code for a dynamic profile, which will need unescaping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_profiles_generate_code_builder()` + `dfareporting_dynamic_profiles_generate_code_execute()`.
/// For task-level control, use `dfareporting_dynamic_profiles_generate_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_generate_code(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicProfilesGenerateCodeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DynamicProfileGenerateCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_dynamic_profiles_generate_code_builder(client, &args.dynamicProfileId)?;
    dfareporting_dynamic_profiles_generate_code_execute(builder)
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}
/// Gets a dynamic profile by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_profiles_get_execute()` to send, or `dfareporting_dynamic_profiles_get` for simplest API.

pub fn dfareporting_dynamic_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicProfiles/{}",
        dynamicProfileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}
/// Gets a dynamic profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_profiles_get_execute()` or `dfareporting_dynamic_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}
/// Gets a dynamic profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_profiles_get_task()`.
/// For the simplest API, use `dfareporting_dynamic_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicProfilesGetArgs {
    /// Path parameter: dynamicProfileId
    pub dynamicProfileId: String,
}

/// GET studio/dynamicProfiles/{dynamicProfilesId}
/// Gets a dynamic profile by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_profiles_get_builder()` + `dfareporting_dynamic_profiles_get_execute()`.
/// For task-level control, use `dfareporting_dynamic_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_get(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_profiles_get_builder(client, &args.dynamicProfileId)?;
    dfareporting_dynamic_profiles_get_execute(builder)
}

/// POST studio/dynamicProfiles
/// Inserts a new dynamic profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_profiles_insert_execute()` to send, or `dfareporting_dynamic_profiles_insert` for simplest API.

pub fn dfareporting_dynamic_profiles_insert_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicProfiles",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/dynamicProfiles
/// Inserts a new dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_profiles_insert_execute()` or `dfareporting_dynamic_profiles_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/dynamicProfiles
/// Inserts a new dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_profiles_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_profiles_insert_task()`.
/// For the simplest API, use `dfareporting_dynamic_profiles_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_profiles_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_profiles_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST studio/dynamicProfiles
/// Inserts a new dynamic profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_profiles_insert_builder()` + `dfareporting_dynamic_profiles_insert_execute()`.
/// For task-level control, use `dfareporting_dynamic_profiles_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_insert(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_profiles_insert_builder(client)?;
    dfareporting_dynamic_profiles_insert_execute(builder)
}

/// POST studio/dynamicProfiles/{dynamicProfilesId}/publish
/// Publish for a dynamic profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_profiles_publish_execute()` to send, or `dfareporting_dynamic_profiles_publish` for simplest API.

pub fn dfareporting_dynamic_profiles_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicProfileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicProfiles/{}/publish",
        dynamicProfileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/dynamicProfiles/{dynamicProfilesId}/publish
/// Publish for a dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_profiles_publish_execute()` or `dfareporting_dynamic_profiles_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/dynamicProfiles/{dynamicProfilesId}/publish
/// Publish for a dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_profiles_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_profiles_publish_task()`.
/// For the simplest API, use `dfareporting_dynamic_profiles_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_profiles_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_profiles_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_profiles_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicProfilesPublishArgs {
    /// Path parameter: dynamicProfileId
    pub dynamicProfileId: String,
}

/// POST studio/dynamicProfiles/{dynamicProfilesId}/publish
/// Publish for a dynamic profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_profiles_publish_builder()` + `dfareporting_dynamic_profiles_publish_execute()`.
/// For task-level control, use `dfareporting_dynamic_profiles_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_publish(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicProfilesPublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_profiles_publish_builder(client, &args.dynamicProfileId)?;
    dfareporting_dynamic_profiles_publish_execute(builder)
}

/// PUT studio/dynamicProfiles
/// Updates an existing dynamic profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_profiles_update_execute()` to send, or `dfareporting_dynamic_profiles_update` for simplest API.

pub fn dfareporting_dynamic_profiles_update_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicProfiles",);

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT studio/dynamicProfiles
/// Updates an existing dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_profiles_update_execute()` or `dfareporting_dynamic_profiles_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT studio/dynamicProfiles
/// Updates an existing dynamic profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_profiles_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_profiles_update_task()`.
/// For the simplest API, use `dfareporting_dynamic_profiles_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_profiles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_profiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_profiles_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT studio/dynamicProfiles
/// Updates an existing dynamic profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_profiles_update_builder()` + `dfareporting_dynamic_profiles_update_execute()`.
/// For task-level control, use `dfareporting_dynamic_profiles_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_profiles_update(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_profiles_update_builder(client)?;
    dfareporting_dynamic_profiles_update_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/dynamicTargetingKeys/{dynamicTargetingKeysId}
/// Deletes an existing dynamic targeting key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_targeting_keys_delete_execute()` to send, or `dfareporting_dynamic_targeting_keys_delete` for simplest API.

pub fn dfareporting_dynamic_targeting_keys_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    objectId: &String,
    name: &Option<String>,
    objectType: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/dynamicTargetingKeys/{}",
        profileId,
        objectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = objectType.as_ref() {
        query_parts.push(format!("objectType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/dynamicTargetingKeys/{dynamicTargetingKeysId}
/// Deletes an existing dynamic targeting key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_targeting_keys_delete_execute()` or `dfareporting_dynamic_targeting_keys_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/dynamicTargetingKeys/{dynamicTargetingKeysId}
/// Deletes an existing dynamic targeting key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_targeting_keys_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_delete_task()`.
/// For the simplest API, use `dfareporting_dynamic_targeting_keys_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_targeting_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_targeting_keys_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_targeting_keys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicTargetingKeysDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: objectId
    pub objectId: String,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: objectType
    pub objectType: Option<String>,
}

/// DELETE userprofiles/{userprofilesId}/dynamicTargetingKeys/{dynamicTargetingKeysId}
/// Deletes an existing dynamic targeting key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_targeting_keys_delete_builder()` + `dfareporting_dynamic_targeting_keys_delete_execute()`.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_delete(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicTargetingKeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_targeting_keys_delete_builder(
        client,
        &args.profileId,
        &args.objectId,
        &args.name,
        &args.objectType,
    )?;
    dfareporting_dynamic_targeting_keys_delete_execute(builder)
}

/// POST userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Inserts a new dynamic targeting key. Keys must be created at the advertiser level before being assigned to the advertiser''s ads, creatives, or placements. There is a maximum of 1000 keys per advertiser, out of which a maximum of 20 keys can be assigned per ad, creative, or placement.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_targeting_keys_insert_execute()` to send, or `dfareporting_dynamic_targeting_keys_insert` for simplest API.

pub fn dfareporting_dynamic_targeting_keys_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/dynamicTargetingKeys",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Inserts a new dynamic targeting key. Keys must be created at the advertiser level before being assigned to the advertiser''s ads, creatives, or placements. There is a maximum of 1000 keys per advertiser, out of which a maximum of 20 keys can be assigned per ad, creative, or placement.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_targeting_keys_insert_execute()` or `dfareporting_dynamic_targeting_keys_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicTargetingKey>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicTargetingKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Inserts a new dynamic targeting key. Keys must be created at the advertiser level before being assigned to the advertiser''s ads, creatives, or placements. There is a maximum of 1000 keys per advertiser, out of which a maximum of 20 keys can be assigned per ad, creative, or placement.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_targeting_keys_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_insert_task()`.
/// For the simplest API, use `dfareporting_dynamic_targeting_keys_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_targeting_keys_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicTargetingKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_targeting_keys_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_targeting_keys_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicTargetingKeysInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Inserts a new dynamic targeting key. Keys must be created at the advertiser level before being assigned to the advertiser''s ads, creatives, or placements. There is a maximum of 1000 keys per advertiser, out of which a maximum of 20 keys can be assigned per ad, creative, or placement.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_targeting_keys_insert_builder()` + `dfareporting_dynamic_targeting_keys_insert_execute()`.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_insert(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicTargetingKeysInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicTargetingKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_targeting_keys_insert_builder(client, &args.profileId)?;
    dfareporting_dynamic_targeting_keys_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Retrieves a list of dynamic targeting keys.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_dynamic_targeting_keys_list_execute()` to send, or `dfareporting_dynamic_targeting_keys_list` for simplest API.

pub fn dfareporting_dynamic_targeting_keys_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &Option<String>,
    names: &Option<String>,
    objectId: &Option<String>,
    objectType: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/dynamicTargetingKeys",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = names.as_ref() {
        query_parts.push(format!("names={}", val));
    }
    if let Some(val) = objectId.as_ref() {
        query_parts.push(format!("objectId={}", val));
    }
    if let Some(val) = objectType.as_ref() {
        query_parts.push(format!("objectType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Retrieves a list of dynamic targeting keys.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_dynamic_targeting_keys_list_execute()` or `dfareporting_dynamic_targeting_keys_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicTargetingKeysListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicTargetingKeysListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Retrieves a list of dynamic targeting keys.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_dynamic_targeting_keys_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_list_task()`.
/// For the simplest API, use `dfareporting_dynamic_targeting_keys_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_dynamic_targeting_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_dynamic_targeting_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DynamicTargetingKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_dynamic_targeting_keys_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_dynamic_targeting_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicTargetingKeysListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: names
    pub names: Option<String>,
    /// Query parameter: objectId
    pub objectId: Option<String>,
    /// Query parameter: objectType
    pub objectType: Option<String>,
}

/// GET userprofiles/{userprofilesId}/dynamicTargetingKeys
/// Retrieves a list of dynamic targeting keys.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_dynamic_targeting_keys_list_builder()` + `dfareporting_dynamic_targeting_keys_list_execute()`.
/// For task-level control, use `dfareporting_dynamic_targeting_keys_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_dynamic_targeting_keys_list(
    client: &SimpleHttpClient,
    args: &DfareportingDynamicTargetingKeysListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DynamicTargetingKeysListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_dynamic_targeting_keys_list_builder(
        client,
        &args.profileId,
        &args.advertiserId,
        &args.names,
        &args.objectId,
        &args.objectType,
    )?;
    dfareporting_dynamic_targeting_keys_list_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Deletes an existing event tag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_delete_execute()` to send, or `dfareporting_event_tags_delete` for simplest API.

pub fn dfareporting_event_tags_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Deletes an existing event tag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_delete_execute()` or `dfareporting_event_tags_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Deletes an existing event tag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_delete_task()`.
/// For the simplest API, use `dfareporting_event_tags_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Deletes an existing event tag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_delete_builder()` + `dfareporting_event_tags_delete_execute()`.
/// For task-level control, use `dfareporting_event_tags_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_delete(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_event_tags_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Gets one event tag by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_get_execute()` to send, or `dfareporting_event_tags_get` for simplest API.

pub fn dfareporting_event_tags_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Gets one event tag by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_get_execute()` or `dfareporting_event_tags_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTag>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTag = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Gets one event tag by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_get_task()`.
/// For the simplest API, use `dfareporting_event_tags_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/eventTags/{eventTagsId}
/// Gets one event tag by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_get_builder()` + `dfareporting_event_tags_get_execute()`.
/// For task-level control, use `dfareporting_event_tags_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_get(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_event_tags_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/eventTags
/// Inserts a new event tag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_insert_execute()` to send, or `dfareporting_event_tags_insert` for simplest API.

pub fn dfareporting_event_tags_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/eventTags
/// Inserts a new event tag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_insert_execute()` or `dfareporting_event_tags_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTag>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTag = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/eventTags
/// Inserts a new event tag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_insert_task()`.
/// For the simplest API, use `dfareporting_event_tags_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/eventTags
/// Inserts a new event tag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_insert_builder()` + `dfareporting_event_tags_insert_execute()`.
/// For task-level control, use `dfareporting_event_tags_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_insert(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_insert_builder(client, &args.profileId)?;
    dfareporting_event_tags_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/eventTags
/// Retrieves a list of event tags, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_list_execute()` to send, or `dfareporting_event_tags_list` for simplest API.

pub fn dfareporting_event_tags_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    adId: &Option<String>,
    advertiserId: &Option<String>,
    campaignId: &Option<String>,
    definitionsOnly: &Option<String>,
    enabled: &Option<String>,
    eventTagTypes: &Option<String>,
    ids: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adId.as_ref() {
        query_parts.push(format!("adId={}", val));
    }
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = campaignId.as_ref() {
        query_parts.push(format!("campaignId={}", val));
    }
    if let Some(val) = definitionsOnly.as_ref() {
        query_parts.push(format!("definitionsOnly={}", val));
    }
    if let Some(val) = enabled.as_ref() {
        query_parts.push(format!("enabled={}", val));
    }
    if let Some(val) = eventTagTypes.as_ref() {
        query_parts.push(format!("eventTagTypes={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/eventTags
/// Retrieves a list of event tags, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_list_execute()` or `dfareporting_event_tags_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTagsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTagsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/eventTags
/// Retrieves a list of event tags, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_list_task()`.
/// For the simplest API, use `dfareporting_event_tags_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: adId
    pub adId: Option<String>,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: campaignId
    pub campaignId: Option<String>,
    /// Query parameter: definitionsOnly
    pub definitionsOnly: Option<String>,
    /// Query parameter: enabled
    pub enabled: Option<String>,
    /// Query parameter: eventTagTypes
    pub eventTagTypes: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/eventTags
/// Retrieves a list of event tags, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_list_builder()` + `dfareporting_event_tags_list_execute()`.
/// For task-level control, use `dfareporting_event_tags_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_list(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_list_builder(
        client,
        &args.profileId,
        &args.adId,
        &args.advertiserId,
        &args.campaignId,
        &args.definitionsOnly,
        &args.enabled,
        &args.eventTagTypes,
        &args.ids,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_event_tags_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_patch_execute()` to send, or `dfareporting_event_tags_patch` for simplest API.

pub fn dfareporting_event_tags_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_patch_execute()` or `dfareporting_event_tags_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTag>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTag = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_patch_task()`.
/// For the simplest API, use `dfareporting_event_tags_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_patch_builder()` + `dfareporting_event_tags_patch_execute()`.
/// For task-level control, use `dfareporting_event_tags_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_patch(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_event_tags_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_event_tags_update_execute()` to send, or `dfareporting_event_tags_update` for simplest API.

pub fn dfareporting_event_tags_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/eventTags",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_event_tags_update_execute()` or `dfareporting_event_tags_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTag>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventTag = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_event_tags_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_event_tags_update_task()`.
/// For the simplest API, use `dfareporting_event_tags_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_event_tags_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_event_tags_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_event_tags_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_event_tags_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingEventTagsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/eventTags
/// Updates an existing event tag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_event_tags_update_builder()` + `dfareporting_event_tags_update_execute()`.
/// For task-level control, use `dfareporting_event_tags_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_event_tags_update(
    client: &SimpleHttpClient,
    args: &DfareportingEventTagsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventTag>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_event_tags_update_builder(client, &args.profileId)?;
    dfareporting_event_tags_update_execute(builder)
}

/// GET reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_files_get_execute()` to send, or `dfareporting_files_get` for simplest API.

pub fn dfareporting_files_get_builder<R>(
    client: &SimpleHttpClient<R>,
    reportId: &String,
    fileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/reports/{}/files/{}",
        reportId, fileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_files_get_execute()` or `dfareporting_files_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_files_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_files_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_files_get_task()`.
/// For the simplest API, use `dfareporting_files_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_files_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_files_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFilesGetArgs {
    /// Path parameter: reportId
    pub reportId: String,
    /// Path parameter: fileId
    pub fileId: String,
}

/// GET reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_files_get_builder()` + `dfareporting_files_get_execute()`.
/// For task-level control, use `dfareporting_files_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_files_get(
    client: &SimpleHttpClient,
    args: &DfareportingFilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_files_get_builder(client, &args.reportId, &args.fileId)?;
    dfareporting_files_get_execute(builder)
}

/// GET userprofiles/{profileId}/files
/// Lists files for a user profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_files_list_execute()` to send, or `dfareporting_files_list` for simplest API.

pub fn dfareporting_files_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    scope: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/files",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = scope.as_ref() {
        query_parts.push(format!("scope={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/files
/// Lists files for a user profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_files_list_execute()` or `dfareporting_files_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_files_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FileList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/files
/// Lists files for a user profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_files_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_files_list_task()`.
/// For the simplest API, use `dfareporting_files_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_files_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_files_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFilesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: scope
    pub scope: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{profileId}/files
/// Lists files for a user profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_files_list_builder()` + `dfareporting_files_list_execute()`.
/// For task-level control, use `dfareporting_files_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_files_list(
    client: &SimpleHttpClient,
    args: &DfareportingFilesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_files_list_builder(
        client,
        &args.profileId,
        &args.maxResults,
        &args.pageToken,
        &args.scope,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_files_list_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Deletes an existing floodlight activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_delete_execute()` to send, or `dfareporting_floodlight_activities_delete` for simplest API.

pub fn dfareporting_floodlight_activities_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Deletes an existing floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_delete_execute()` or `dfareporting_floodlight_activities_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Deletes an existing floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_delete_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Deletes an existing floodlight activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_delete_builder()` + `dfareporting_floodlight_activities_delete_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_delete(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_activities_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_activities_delete_execute(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivities/generatetag
/// Generates a tag for a floodlight activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_generatetag_execute()` to send, or `dfareporting_floodlight_activities_generatetag` for simplest API.

pub fn dfareporting_floodlight_activities_generatetag_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    floodlightActivityId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities/generatetag",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = floodlightActivityId.as_ref() {
        query_parts.push(format!("floodlightActivityId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivities/generatetag
/// Generates a tag for a floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_generatetag_execute()` or `dfareporting_floodlight_activities_generatetag`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_generatetag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_generatetag_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivitiesGenerateTagResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivitiesGenerateTagResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/floodlightActivities/generatetag
/// Generates a tag for a floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_generatetag_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_generatetag_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_generatetag()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_generatetag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_generatetag_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivitiesGenerateTagResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_generatetag_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_generatetag`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesGeneratetagArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: floodlightActivityId
    pub floodlightActivityId: Option<String>,
}

/// POST userprofiles/{userprofilesId}/floodlightActivities/generatetag
/// Generates a tag for a floodlight activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_generatetag_builder()` + `dfareporting_floodlight_activities_generatetag_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_generatetag_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_generatetag(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesGeneratetagArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivitiesGenerateTagResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activities_generatetag_builder(
        client,
        &args.profileId,
        &args.floodlightActivityId,
    )?;
    dfareporting_floodlight_activities_generatetag_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Gets one floodlight activity by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_get_execute()` to send, or `dfareporting_floodlight_activities_get` for simplest API.

pub fn dfareporting_floodlight_activities_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Gets one floodlight activity by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_get_execute()` or `dfareporting_floodlight_activities_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Gets one floodlight activity by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_get_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/floodlightActivities/{floodlightActivitiesId}
/// Gets one floodlight activity by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_get_builder()` + `dfareporting_floodlight_activities_get_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_get(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_activities_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_activities_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivities
/// Inserts a new floodlight activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_insert_execute()` to send, or `dfareporting_floodlight_activities_insert` for simplest API.

pub fn dfareporting_floodlight_activities_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivities
/// Inserts a new floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_insert_execute()` or `dfareporting_floodlight_activities_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/floodlightActivities
/// Inserts a new floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_insert_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/floodlightActivities
/// Inserts a new floodlight activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_insert_builder()` + `dfareporting_floodlight_activities_insert_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_insert(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activities_insert_builder(client, &args.profileId)?;
    dfareporting_floodlight_activities_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivities
/// Retrieves a list of floodlight activities, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_list_execute()` to send, or `dfareporting_floodlight_activities_list` for simplest API.

pub fn dfareporting_floodlight_activities_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &Option<String>,
    floodlightActivityGroupIds: &Option<String>,
    floodlightActivityGroupName: &Option<String>,
    floodlightActivityGroupTagString: &Option<String>,
    floodlightActivityGroupType: &Option<String>,
    floodlightConfigurationId: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    tagString: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = floodlightActivityGroupIds.as_ref() {
        query_parts.push(format!("floodlightActivityGroupIds={}", val));
    }
    if let Some(val) = floodlightActivityGroupName.as_ref() {
        query_parts.push(format!("floodlightActivityGroupName={}", val));
    }
    if let Some(val) = floodlightActivityGroupTagString.as_ref() {
        query_parts.push(format!("floodlightActivityGroupTagString={}", val));
    }
    if let Some(val) = floodlightActivityGroupType.as_ref() {
        query_parts.push(format!("floodlightActivityGroupType={}", val));
    }
    if let Some(val) = floodlightConfigurationId.as_ref() {
        query_parts.push(format!("floodlightConfigurationId={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = tagString.as_ref() {
        query_parts.push(format!("tagString={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivities
/// Retrieves a list of floodlight activities, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_list_execute()` or `dfareporting_floodlight_activities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivitiesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivitiesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightActivities
/// Retrieves a list of floodlight activities, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_list_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivitiesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: floodlightActivityGroupIds
    pub floodlightActivityGroupIds: Option<String>,
    /// Query parameter: floodlightActivityGroupName
    pub floodlightActivityGroupName: Option<String>,
    /// Query parameter: floodlightActivityGroupTagString
    pub floodlightActivityGroupTagString: Option<String>,
    /// Query parameter: floodlightActivityGroupType
    pub floodlightActivityGroupType: Option<String>,
    /// Query parameter: floodlightConfigurationId
    pub floodlightConfigurationId: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: tagString
    pub tagString: Option<String>,
}

/// GET userprofiles/{userprofilesId}/floodlightActivities
/// Retrieves a list of floodlight activities, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_list_builder()` + `dfareporting_floodlight_activities_list_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_list(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivitiesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activities_list_builder(
        client,
        &args.profileId,
        &args.advertiserId,
        &args.floodlightActivityGroupIds,
        &args.floodlightActivityGroupName,
        &args.floodlightActivityGroupTagString,
        &args.floodlightActivityGroupType,
        &args.floodlightConfigurationId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.tagString,
    )?;
    dfareporting_floodlight_activities_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_patch_execute()` to send, or `dfareporting_floodlight_activities_patch` for simplest API.

pub fn dfareporting_floodlight_activities_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_patch_execute()` or `dfareporting_floodlight_activities_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_patch_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_patch_builder()` + `dfareporting_floodlight_activities_patch_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_patch(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_activities_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_activities_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activities_update_execute()` to send, or `dfareporting_floodlight_activities_update` for simplest API.

pub fn dfareporting_floodlight_activities_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivities",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activities_update_execute()` or `dfareporting_floodlight_activities_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activities_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activities_update_task()`.
/// For the simplest API, use `dfareporting_floodlight_activities_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activities_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activities_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activities_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activities_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivitiesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/floodlightActivities
/// Updates an existing floodlight activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activities_update_builder()` + `dfareporting_floodlight_activities_update_execute()`.
/// For task-level control, use `dfareporting_floodlight_activities_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activities_update(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivitiesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activities_update_builder(client, &args.profileId)?;
    dfareporting_floodlight_activities_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups/{floodlightActivityGroupsId}
/// Gets one floodlight activity group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activity_groups_get_execute()` to send, or `dfareporting_floodlight_activity_groups_get` for simplest API.

pub fn dfareporting_floodlight_activity_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivityGroups/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups/{floodlightActivityGroupsId}
/// Gets one floodlight activity group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activity_groups_get_execute()` or `dfareporting_floodlight_activity_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivityGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivityGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups/{floodlightActivityGroupsId}
/// Gets one floodlight activity group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activity_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activity_groups_get_task()`.
/// For the simplest API, use `dfareporting_floodlight_activity_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activity_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activity_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activity_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivityGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups/{floodlightActivityGroupsId}
/// Gets one floodlight activity group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activity_groups_get_builder()` + `dfareporting_floodlight_activity_groups_get_execute()`.
/// For task-level control, use `dfareporting_floodlight_activity_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivityGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_activity_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_activity_groups_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivityGroups
/// Inserts a new floodlight activity group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activity_groups_insert_execute()` to send, or `dfareporting_floodlight_activity_groups_insert` for simplest API.

pub fn dfareporting_floodlight_activity_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivityGroups",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/floodlightActivityGroups
/// Inserts a new floodlight activity group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activity_groups_insert_execute()` or `dfareporting_floodlight_activity_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivityGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivityGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/floodlightActivityGroups
/// Inserts a new floodlight activity group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activity_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activity_groups_insert_task()`.
/// For the simplest API, use `dfareporting_floodlight_activity_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activity_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activity_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activity_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivityGroupsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/floodlightActivityGroups
/// Inserts a new floodlight activity group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activity_groups_insert_builder()` + `dfareporting_floodlight_activity_groups_insert_execute()`.
/// For task-level control, use `dfareporting_floodlight_activity_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_insert(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivityGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activity_groups_insert_builder(client, &args.profileId)?;
    dfareporting_floodlight_activity_groups_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups
/// Retrieves a list of floodlight activity groups, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activity_groups_list_execute()` to send, or `dfareporting_floodlight_activity_groups_list` for simplest API.

pub fn dfareporting_floodlight_activity_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &Option<String>,
    floodlightConfigurationId: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivityGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = floodlightConfigurationId.as_ref() {
        query_parts.push(format!("floodlightConfigurationId={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups
/// Retrieves a list of floodlight activity groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activity_groups_list_execute()` or `dfareporting_floodlight_activity_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivityGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivityGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups
/// Retrieves a list of floodlight activity groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activity_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activity_groups_list_task()`.
/// For the simplest API, use `dfareporting_floodlight_activity_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activity_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivityGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activity_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activity_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivityGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: floodlightConfigurationId
    pub floodlightConfigurationId: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET userprofiles/{userprofilesId}/floodlightActivityGroups
/// Retrieves a list of floodlight activity groups, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activity_groups_list_builder()` + `dfareporting_floodlight_activity_groups_list_execute()`.
/// For task-level control, use `dfareporting_floodlight_activity_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivityGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightActivityGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activity_groups_list_builder(
        client,
        &args.profileId,
        &args.advertiserId,
        &args.floodlightConfigurationId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.type_rs,
    )?;
    dfareporting_floodlight_activity_groups_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activity_groups_patch_execute()` to send, or `dfareporting_floodlight_activity_groups_patch` for simplest API.

pub fn dfareporting_floodlight_activity_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivityGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activity_groups_patch_execute()` or `dfareporting_floodlight_activity_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivityGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivityGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activity_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activity_groups_patch_task()`.
/// For the simplest API, use `dfareporting_floodlight_activity_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activity_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activity_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activity_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivityGroupsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activity_groups_patch_builder()` + `dfareporting_floodlight_activity_groups_patch_execute()`.
/// For task-level control, use `dfareporting_floodlight_activity_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_patch(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivityGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_activity_groups_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_activity_groups_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_activity_groups_update_execute()` to send, or `dfareporting_floodlight_activity_groups_update` for simplest API.

pub fn dfareporting_floodlight_activity_groups_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightActivityGroups",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_activity_groups_update_execute()` or `dfareporting_floodlight_activity_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivityGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivityGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_activity_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_activity_groups_update_task()`.
/// For the simplest API, use `dfareporting_floodlight_activity_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_activity_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_activity_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_activity_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_activity_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightActivityGroupsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/floodlightActivityGroups
/// Updates an existing floodlight activity group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_activity_groups_update_builder()` + `dfareporting_floodlight_activity_groups_update_execute()`.
/// For task-level control, use `dfareporting_floodlight_activity_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_activity_groups_update(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightActivityGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivityGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_activity_groups_update_builder(client, &args.profileId)?;
    dfareporting_floodlight_activity_groups_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations/{floodlightConfigurationsId}
/// Gets one floodlight configuration by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_configurations_get_execute()` to send, or `dfareporting_floodlight_configurations_get` for simplest API.

pub fn dfareporting_floodlight_configurations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightConfigurations/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations/{floodlightConfigurationsId}
/// Gets one floodlight configuration by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_configurations_get_execute()` or `dfareporting_floodlight_configurations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations/{floodlightConfigurationsId}
/// Gets one floodlight configuration by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_configurations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_configurations_get_task()`.
/// For the simplest API, use `dfareporting_floodlight_configurations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_configurations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_configurations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_configurations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightConfigurationsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations/{floodlightConfigurationsId}
/// Gets one floodlight configuration by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_configurations_get_builder()` + `dfareporting_floodlight_configurations_get_execute()`.
/// For task-level control, use `dfareporting_floodlight_configurations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_get(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightConfigurationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_configurations_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_configurations_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations
/// Retrieves a list of floodlight configurations, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_configurations_list_execute()` to send, or `dfareporting_floodlight_configurations_list` for simplest API.

pub fn dfareporting_floodlight_configurations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightConfigurations",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations
/// Retrieves a list of floodlight configurations, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_configurations_list_execute()` or `dfareporting_floodlight_configurations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightConfigurationsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightConfigurationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations
/// Retrieves a list of floodlight configurations, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_configurations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_configurations_list_task()`.
/// For the simplest API, use `dfareporting_floodlight_configurations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightConfigurationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_configurations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_configurations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightConfigurationsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
}

/// GET userprofiles/{userprofilesId}/floodlightConfigurations
/// Retrieves a list of floodlight configurations, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_configurations_list_builder()` + `dfareporting_floodlight_configurations_list_execute()`.
/// For task-level control, use `dfareporting_floodlight_configurations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_list(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightConfigurationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FloodlightConfigurationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_configurations_list_builder(client, &args.profileId, &args.ids)?;
    dfareporting_floodlight_configurations_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_configurations_patch_execute()` to send, or `dfareporting_floodlight_configurations_patch` for simplest API.

pub fn dfareporting_floodlight_configurations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightConfigurations",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_configurations_patch_execute()` or `dfareporting_floodlight_configurations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_configurations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_configurations_patch_task()`.
/// For the simplest API, use `dfareporting_floodlight_configurations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_configurations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_configurations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_configurations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightConfigurationsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_configurations_patch_builder()` + `dfareporting_floodlight_configurations_patch_execute()`.
/// For task-level control, use `dfareporting_floodlight_configurations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_patch(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightConfigurationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_floodlight_configurations_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_floodlight_configurations_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_floodlight_configurations_update_execute()` to send, or `dfareporting_floodlight_configurations_update` for simplest API.

pub fn dfareporting_floodlight_configurations_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/floodlightConfigurations",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_floodlight_configurations_update_execute()` or `dfareporting_floodlight_configurations_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_floodlight_configurations_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_floodlight_configurations_update_task()`.
/// For the simplest API, use `dfareporting_floodlight_configurations_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_floodlight_configurations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_floodlight_configurations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_floodlight_configurations_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_floodlight_configurations_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingFloodlightConfigurationsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/floodlightConfigurations
/// Updates an existing floodlight configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_floodlight_configurations_update_builder()` + `dfareporting_floodlight_configurations_update_execute()`.
/// For task-level control, use `dfareporting_floodlight_configurations_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_floodlight_configurations_update(
    client: &SimpleHttpClient,
    args: &DfareportingFloodlightConfigurationsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_floodlight_configurations_update_builder(client, &args.profileId)?;
    dfareporting_floodlight_configurations_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/languages
/// Retrieves a list of languages.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_languages_list_execute()` to send, or `dfareporting_languages_list` for simplest API.

pub fn dfareporting_languages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/languages",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/languages
/// Retrieves a list of languages.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_languages_list_execute()` or `dfareporting_languages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_languages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_languages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LanguagesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LanguagesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/languages
/// Retrieves a list of languages.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_languages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_languages_list_task()`.
/// For the simplest API, use `dfareporting_languages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_languages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_languages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LanguagesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_languages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_languages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingLanguagesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/languages
/// Retrieves a list of languages.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_languages_list_builder()` + `dfareporting_languages_list_execute()`.
/// For task-level control, use `dfareporting_languages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_languages_list(
    client: &SimpleHttpClient,
    args: &DfareportingLanguagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LanguagesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_languages_list_builder(client, &args.profileId)?;
    dfareporting_languages_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/metros
/// Retrieves a list of metros.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_metros_list_execute()` to send, or `dfareporting_metros_list` for simplest API.

pub fn dfareporting_metros_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/metros",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/metros
/// Retrieves a list of metros.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_metros_list_execute()` or `dfareporting_metros_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_metros_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_metros_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetrosListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetrosListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/metros
/// Retrieves a list of metros.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_metros_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_metros_list_task()`.
/// For the simplest API, use `dfareporting_metros_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_metros_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_metros_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetrosListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_metros_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_metros_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingMetrosListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/metros
/// Retrieves a list of metros.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_metros_list_builder()` + `dfareporting_metros_list_execute()`.
/// For task-level control, use `dfareporting_metros_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_metros_list(
    client: &SimpleHttpClient,
    args: &DfareportingMetrosListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetrosListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_metros_list_builder(client, &args.profileId)?;
    dfareporting_metros_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/mobileApps/{mobileAppsId}
/// Gets one mobile app by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_mobile_apps_get_execute()` to send, or `dfareporting_mobile_apps_get` for simplest API.

pub fn dfareporting_mobile_apps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/mobileApps/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/mobileApps/{mobileAppsId}
/// Gets one mobile app by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_mobile_apps_get_execute()` or `dfareporting_mobile_apps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_apps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MobileApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MobileApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/mobileApps/{mobileAppsId}
/// Gets one mobile app by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_mobile_apps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_mobile_apps_get_task()`.
/// For the simplest API, use `dfareporting_mobile_apps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_mobile_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_mobile_apps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_mobile_apps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingMobileAppsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/mobileApps/{mobileAppsId}
/// Gets one mobile app by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_mobile_apps_get_builder()` + `dfareporting_mobile_apps_get_execute()`.
/// For task-level control, use `dfareporting_mobile_apps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_apps_get(
    client: &SimpleHttpClient,
    args: &DfareportingMobileAppsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_mobile_apps_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_mobile_apps_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/mobileApps
/// Retrieves list of available mobile apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_mobile_apps_list_execute()` to send, or `dfareporting_mobile_apps_list` for simplest API.

pub fn dfareporting_mobile_apps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    directories: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/mobileApps",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = directories.as_ref() {
        query_parts.push(format!("directories={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/mobileApps
/// Retrieves list of available mobile apps.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_mobile_apps_list_execute()` or `dfareporting_mobile_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MobileAppsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MobileAppsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/mobileApps
/// Retrieves list of available mobile apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_mobile_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_mobile_apps_list_task()`.
/// For the simplest API, use `dfareporting_mobile_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_mobile_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_mobile_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_mobile_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingMobileAppsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: directories
    pub directories: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
}

/// GET userprofiles/{userprofilesId}/mobileApps
/// Retrieves list of available mobile apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_mobile_apps_list_builder()` + `dfareporting_mobile_apps_list_execute()`.
/// For task-level control, use `dfareporting_mobile_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_apps_list(
    client: &SimpleHttpClient,
    args: &DfareportingMobileAppsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileAppsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_mobile_apps_list_builder(
        client,
        &args.profileId,
        &args.directories,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
    )?;
    dfareporting_mobile_apps_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/mobileCarriers/{mobileCarriersId}
/// Gets one mobile carrier by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_mobile_carriers_get_execute()` to send, or `dfareporting_mobile_carriers_get` for simplest API.

pub fn dfareporting_mobile_carriers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/mobileCarriers/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/mobileCarriers/{mobileCarriersId}
/// Gets one mobile carrier by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_mobile_carriers_get_execute()` or `dfareporting_mobile_carriers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_carriers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_carriers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MobileCarrier>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MobileCarrier = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/mobileCarriers/{mobileCarriersId}
/// Gets one mobile carrier by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_mobile_carriers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_mobile_carriers_get_task()`.
/// For the simplest API, use `dfareporting_mobile_carriers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_carriers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_mobile_carriers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileCarrier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_mobile_carriers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_mobile_carriers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingMobileCarriersGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/mobileCarriers/{mobileCarriersId}
/// Gets one mobile carrier by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_mobile_carriers_get_builder()` + `dfareporting_mobile_carriers_get_execute()`.
/// For task-level control, use `dfareporting_mobile_carriers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_carriers_get(
    client: &SimpleHttpClient,
    args: &DfareportingMobileCarriersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MobileCarrier>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_mobile_carriers_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_mobile_carriers_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/mobileCarriers
/// Retrieves a list of mobile carriers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_mobile_carriers_list_execute()` to send, or `dfareporting_mobile_carriers_list` for simplest API.

pub fn dfareporting_mobile_carriers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/mobileCarriers",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/mobileCarriers
/// Retrieves a list of mobile carriers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_mobile_carriers_list_execute()` or `dfareporting_mobile_carriers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_carriers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_carriers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MobileCarriersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MobileCarriersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/mobileCarriers
/// Retrieves a list of mobile carriers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_mobile_carriers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_mobile_carriers_list_task()`.
/// For the simplest API, use `dfareporting_mobile_carriers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_mobile_carriers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_mobile_carriers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MobileCarriersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_mobile_carriers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_mobile_carriers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingMobileCarriersListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/mobileCarriers
/// Retrieves a list of mobile carriers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_mobile_carriers_list_builder()` + `dfareporting_mobile_carriers_list_execute()`.
/// For task-level control, use `dfareporting_mobile_carriers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_mobile_carriers_list(
    client: &SimpleHttpClient,
    args: &DfareportingMobileCarriersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MobileCarriersListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_mobile_carriers_list_builder(client, &args.profileId)?;
    dfareporting_mobile_carriers_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions/{operatingSystemVersionsId}
/// Gets one operating system version by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_operating_system_versions_get_execute()` to send, or `dfareporting_operating_system_versions_get` for simplest API.

pub fn dfareporting_operating_system_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/operatingSystemVersions/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions/{operatingSystemVersionsId}
/// Gets one operating system version by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_operating_system_versions_get_execute()` or `dfareporting_operating_system_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_system_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_system_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperatingSystemVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperatingSystemVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions/{operatingSystemVersionsId}
/// Gets one operating system version by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_operating_system_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_operating_system_versions_get_task()`.
/// For the simplest API, use `dfareporting_operating_system_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_system_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_operating_system_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperatingSystemVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_operating_system_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_operating_system_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingOperatingSystemVersionsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions/{operatingSystemVersionsId}
/// Gets one operating system version by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_operating_system_versions_get_builder()` + `dfareporting_operating_system_versions_get_execute()`.
/// For task-level control, use `dfareporting_operating_system_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_system_versions_get(
    client: &SimpleHttpClient,
    args: &DfareportingOperatingSystemVersionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperatingSystemVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_operating_system_versions_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_operating_system_versions_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions
/// Retrieves a list of operating system versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_operating_system_versions_list_execute()` to send, or `dfareporting_operating_system_versions_list` for simplest API.

pub fn dfareporting_operating_system_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/operatingSystemVersions",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions
/// Retrieves a list of operating system versions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_operating_system_versions_list_execute()` or `dfareporting_operating_system_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_system_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_system_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperatingSystemVersionsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperatingSystemVersionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions
/// Retrieves a list of operating system versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_operating_system_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_operating_system_versions_list_task()`.
/// For the simplest API, use `dfareporting_operating_system_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_system_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_operating_system_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OperatingSystemVersionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_operating_system_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_operating_system_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingOperatingSystemVersionsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/operatingSystemVersions
/// Retrieves a list of operating system versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_operating_system_versions_list_builder()` + `dfareporting_operating_system_versions_list_execute()`.
/// For task-level control, use `dfareporting_operating_system_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_system_versions_list(
    client: &SimpleHttpClient,
    args: &DfareportingOperatingSystemVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OperatingSystemVersionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_operating_system_versions_list_builder(client, &args.profileId)?;
    dfareporting_operating_system_versions_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystems/{operatingSystemsId}
/// Gets one operating system by DART ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_operating_systems_get_execute()` to send, or `dfareporting_operating_systems_get` for simplest API.

pub fn dfareporting_operating_systems_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    dartId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/operatingSystems/{}",
        profileId, dartId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystems/{operatingSystemsId}
/// Gets one operating system by DART ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_operating_systems_get_execute()` or `dfareporting_operating_systems_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_systems_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_systems_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperatingSystem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperatingSystem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/operatingSystems/{operatingSystemsId}
/// Gets one operating system by DART ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_operating_systems_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_operating_systems_get_task()`.
/// For the simplest API, use `dfareporting_operating_systems_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_systems_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_operating_systems_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperatingSystem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_operating_systems_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_operating_systems_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingOperatingSystemsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: dartId
    pub dartId: String,
}

/// GET userprofiles/{userprofilesId}/operatingSystems/{operatingSystemsId}
/// Gets one operating system by DART ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_operating_systems_get_builder()` + `dfareporting_operating_systems_get_execute()`.
/// For task-level control, use `dfareporting_operating_systems_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_systems_get(
    client: &SimpleHttpClient,
    args: &DfareportingOperatingSystemsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperatingSystem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_operating_systems_get_builder(client, &args.profileId, &args.dartId)?;
    dfareporting_operating_systems_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystems
/// Retrieves a list of operating systems.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_operating_systems_list_execute()` to send, or `dfareporting_operating_systems_list` for simplest API.

pub fn dfareporting_operating_systems_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/operatingSystems",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/operatingSystems
/// Retrieves a list of operating systems.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_operating_systems_list_execute()` or `dfareporting_operating_systems_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_systems_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_systems_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperatingSystemsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperatingSystemsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/operatingSystems
/// Retrieves a list of operating systems.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_operating_systems_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_operating_systems_list_task()`.
/// For the simplest API, use `dfareporting_operating_systems_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_operating_systems_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_operating_systems_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OperatingSystemsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_operating_systems_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_operating_systems_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingOperatingSystemsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/operatingSystems
/// Retrieves a list of operating systems.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_operating_systems_list_builder()` + `dfareporting_operating_systems_list_execute()`.
/// For task-level control, use `dfareporting_operating_systems_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_operating_systems_list(
    client: &SimpleHttpClient,
    args: &DfareportingOperatingSystemsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OperatingSystemsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_operating_systems_list_builder(client, &args.profileId)?;
    dfareporting_operating_systems_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placementGroups/{placementGroupsId}
/// Gets one placement group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_groups_get_execute()` to send, or `dfareporting_placement_groups_get` for simplest API.

pub fn dfareporting_placement_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementGroups/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placementGroups/{placementGroupsId}
/// Gets one placement group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_groups_get_execute()` or `dfareporting_placement_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placementGroups/{placementGroupsId}
/// Gets one placement group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_groups_get_task()`.
/// For the simplest API, use `dfareporting_placement_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/placementGroups/{placementGroupsId}
/// Gets one placement group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_groups_get_builder()` + `dfareporting_placement_groups_get_execute()`.
/// For task-level control, use `dfareporting_placement_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_placement_groups_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/placementGroups
/// Inserts a new placement group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_groups_insert_execute()` to send, or `dfareporting_placement_groups_insert` for simplest API.

pub fn dfareporting_placement_groups_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementGroups",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/placementGroups
/// Inserts a new placement group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_groups_insert_execute()` or `dfareporting_placement_groups_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/placementGroups
/// Inserts a new placement group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_groups_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_groups_insert_task()`.
/// For the simplest API, use `dfareporting_placement_groups_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_groups_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_groups_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementGroupsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/placementGroups
/// Inserts a new placement group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_groups_insert_builder()` + `dfareporting_placement_groups_insert_execute()`.
/// For task-level control, use `dfareporting_placement_groups_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_insert(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementGroupsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_groups_insert_builder(client, &args.profileId)?;
    dfareporting_placement_groups_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placementGroups
/// Retrieves a list of placement groups, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_groups_list_execute()` to send, or `dfareporting_placement_groups_list` for simplest API.

pub fn dfareporting_placement_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    activeStatus: &Option<String>,
    advertiserIds: &Option<String>,
    campaignIds: &Option<String>,
    contentCategoryIds: &Option<String>,
    directorySiteIds: &Option<String>,
    ids: &Option<String>,
    maxEndDate: &Option<String>,
    maxResults: &Option<String>,
    maxStartDate: &Option<String>,
    minEndDate: &Option<String>,
    minStartDate: &Option<String>,
    pageToken: &Option<String>,
    placementGroupType: &Option<String>,
    placementStrategyIds: &Option<String>,
    pricingTypes: &Option<String>,
    searchString: &Option<String>,
    siteIds: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = activeStatus.as_ref() {
        query_parts.push(format!("activeStatus={}", val));
    }
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = campaignIds.as_ref() {
        query_parts.push(format!("campaignIds={}", val));
    }
    if let Some(val) = contentCategoryIds.as_ref() {
        query_parts.push(format!("contentCategoryIds={}", val));
    }
    if let Some(val) = directorySiteIds.as_ref() {
        query_parts.push(format!("directorySiteIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxEndDate.as_ref() {
        query_parts.push(format!("maxEndDate={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = maxStartDate.as_ref() {
        query_parts.push(format!("maxStartDate={}", val));
    }
    if let Some(val) = minEndDate.as_ref() {
        query_parts.push(format!("minEndDate={}", val));
    }
    if let Some(val) = minStartDate.as_ref() {
        query_parts.push(format!("minStartDate={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = placementGroupType.as_ref() {
        query_parts.push(format!("placementGroupType={}", val));
    }
    if let Some(val) = placementStrategyIds.as_ref() {
        query_parts.push(format!("placementStrategyIds={}", val));
    }
    if let Some(val) = pricingTypes.as_ref() {
        query_parts.push(format!("pricingTypes={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = siteIds.as_ref() {
        query_parts.push(format!("siteIds={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placementGroups
/// Retrieves a list of placement groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_groups_list_execute()` or `dfareporting_placement_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placementGroups
/// Retrieves a list of placement groups, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_groups_list_task()`.
/// For the simplest API, use `dfareporting_placement_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: activeStatus
    pub activeStatus: Option<String>,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: campaignIds
    pub campaignIds: Option<String>,
    /// Query parameter: contentCategoryIds
    pub contentCategoryIds: Option<String>,
    /// Query parameter: directorySiteIds
    pub directorySiteIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxEndDate
    pub maxEndDate: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: maxStartDate
    pub maxStartDate: Option<String>,
    /// Query parameter: minEndDate
    pub minEndDate: Option<String>,
    /// Query parameter: minStartDate
    pub minStartDate: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: placementGroupType
    pub placementGroupType: Option<String>,
    /// Query parameter: placementStrategyIds
    pub placementStrategyIds: Option<String>,
    /// Query parameter: pricingTypes
    pub pricingTypes: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: siteIds
    pub siteIds: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/placementGroups
/// Retrieves a list of placement groups, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_groups_list_builder()` + `dfareporting_placement_groups_list_execute()`.
/// For task-level control, use `dfareporting_placement_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_groups_list_builder(
        client,
        &args.profileId,
        &args.activeStatus,
        &args.advertiserIds,
        &args.campaignIds,
        &args.contentCategoryIds,
        &args.directorySiteIds,
        &args.ids,
        &args.maxEndDate,
        &args.maxResults,
        &args.maxStartDate,
        &args.minEndDate,
        &args.minStartDate,
        &args.pageToken,
        &args.placementGroupType,
        &args.placementStrategyIds,
        &args.pricingTypes,
        &args.searchString,
        &args.siteIds,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_placement_groups_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_groups_patch_execute()` to send, or `dfareporting_placement_groups_patch` for simplest API.

pub fn dfareporting_placement_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementGroups",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_groups_patch_execute()` or `dfareporting_placement_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_groups_patch_task()`.
/// For the simplest API, use `dfareporting_placement_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementGroupsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_groups_patch_builder()` + `dfareporting_placement_groups_patch_execute()`.
/// For task-level control, use `dfareporting_placement_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_patch(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_groups_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_placement_groups_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_groups_update_execute()` to send, or `dfareporting_placement_groups_update` for simplest API.

pub fn dfareporting_placement_groups_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementGroups",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_groups_update_execute()` or `dfareporting_placement_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_groups_update_task()`.
/// For the simplest API, use `dfareporting_placement_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementGroupsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/placementGroups
/// Updates an existing placement group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_groups_update_builder()` + `dfareporting_placement_groups_update_execute()`.
/// For task-level control, use `dfareporting_placement_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_groups_update(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_groups_update_builder(client, &args.profileId)?;
    dfareporting_placement_groups_update_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Deletes an existing placement strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_delete_execute()` to send, or `dfareporting_placement_strategies_delete` for simplest API.

pub fn dfareporting_placement_strategies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Deletes an existing placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_delete_execute()` or `dfareporting_placement_strategies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Deletes an existing placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_delete_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Deletes an existing placement strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_delete_builder()` + `dfareporting_placement_strategies_delete_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_delete(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dfareporting_placement_strategies_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_placement_strategies_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Gets one placement strategy by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_get_execute()` to send, or `dfareporting_placement_strategies_get` for simplest API.

pub fn dfareporting_placement_strategies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Gets one placement strategy by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_get_execute()` or `dfareporting_placement_strategies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementStrategy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementStrategy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Gets one placement strategy by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_get_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/placementStrategies/{placementStrategiesId}
/// Gets one placement strategy by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_get_builder()` + `dfareporting_placement_strategies_get_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_get(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_strategies_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_placement_strategies_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/placementStrategies
/// Inserts a new placement strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_insert_execute()` to send, or `dfareporting_placement_strategies_insert` for simplest API.

pub fn dfareporting_placement_strategies_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/placementStrategies
/// Inserts a new placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_insert_execute()` or `dfareporting_placement_strategies_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementStrategy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementStrategy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/placementStrategies
/// Inserts a new placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_insert_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/placementStrategies
/// Inserts a new placement strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_insert_builder()` + `dfareporting_placement_strategies_insert_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_insert(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_strategies_insert_builder(client, &args.profileId)?;
    dfareporting_placement_strategies_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placementStrategies
/// Retrieves a list of placement strategies, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_list_execute()` to send, or `dfareporting_placement_strategies_list` for simplest API.

pub fn dfareporting_placement_strategies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placementStrategies
/// Retrieves a list of placement strategies, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_list_execute()` or `dfareporting_placement_strategies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementStrategiesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementStrategiesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placementStrategies
/// Retrieves a list of placement strategies, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_list_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementStrategiesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/placementStrategies
/// Retrieves a list of placement strategies, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_list_builder()` + `dfareporting_placement_strategies_list_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_list(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementStrategiesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_strategies_list_builder(
        client,
        &args.profileId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_placement_strategies_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_patch_execute()` to send, or `dfareporting_placement_strategies_patch` for simplest API.

pub fn dfareporting_placement_strategies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_patch_execute()` or `dfareporting_placement_strategies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementStrategy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementStrategy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_patch_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_patch_builder()` + `dfareporting_placement_strategies_patch_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_patch(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_placement_strategies_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_placement_strategies_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placement_strategies_update_execute()` to send, or `dfareporting_placement_strategies_update` for simplest API.

pub fn dfareporting_placement_strategies_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placementStrategies",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placement_strategies_update_execute()` or `dfareporting_placement_strategies_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementStrategy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementStrategy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placement_strategies_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placement_strategies_update_task()`.
/// For the simplest API, use `dfareporting_placement_strategies_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placement_strategies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placement_strategies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placement_strategies_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placement_strategies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementStrategiesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/placementStrategies
/// Updates an existing placement strategy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placement_strategies_update_builder()` + `dfareporting_placement_strategies_update_execute()`.
/// For task-level control, use `dfareporting_placement_strategies_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placement_strategies_update(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementStrategiesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementStrategy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placement_strategies_update_builder(client, &args.profileId)?;
    dfareporting_placement_strategies_update_execute(builder)
}

/// POST userprofiles/{userprofilesId}/placements/generatetags
/// Generates tags for a placement.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_generatetags_execute()` to send, or `dfareporting_placements_generatetags` for simplest API.

pub fn dfareporting_placements_generatetags_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    campaignId: &Option<String>,
    placementIds: &Option<String>,
    tagFormats: &Option<String>,
    tagProperties_dcDbmMacroIncluded: &Option<String>,
    tagProperties_gppMacrosIncluded: &Option<String>,
    tagProperties_tcfGdprMacrosIncluded: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements/generatetags",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = campaignId.as_ref() {
        query_parts.push(format!("campaignId={}", val));
    }
    if let Some(val) = placementIds.as_ref() {
        query_parts.push(format!("placementIds={}", val));
    }
    if let Some(val) = tagFormats.as_ref() {
        query_parts.push(format!("tagFormats={}", val));
    }
    if let Some(val) = tagProperties_dcDbmMacroIncluded.as_ref() {
        query_parts.push(format!("tagProperties.dcDbmMacroIncluded={}", val));
    }
    if let Some(val) = tagProperties_gppMacrosIncluded.as_ref() {
        query_parts.push(format!("tagProperties.gppMacrosIncluded={}", val));
    }
    if let Some(val) = tagProperties_tcfGdprMacrosIncluded.as_ref() {
        query_parts.push(format!("tagProperties.tcfGdprMacrosIncluded={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/placements/generatetags
/// Generates tags for a placement.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_generatetags_execute()` or `dfareporting_placements_generatetags`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_generatetags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_generatetags_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementsGenerateTagsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementsGenerateTagsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/placements/generatetags
/// Generates tags for a placement.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_generatetags_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_generatetags_task()`.
/// For the simplest API, use `dfareporting_placements_generatetags()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_generatetags_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_generatetags_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementsGenerateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placements_generatetags_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_generatetags`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsGeneratetagsArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: campaignId
    pub campaignId: Option<String>,
    /// Query parameter: placementIds
    pub placementIds: Option<String>,
    /// Query parameter: tagFormats
    pub tagFormats: Option<String>,
    /// Query parameter: tagProperties_dcDbmMacroIncluded
    pub tagProperties_dcDbmMacroIncluded: Option<String>,
    /// Query parameter: tagProperties_gppMacrosIncluded
    pub tagProperties_gppMacrosIncluded: Option<String>,
    /// Query parameter: tagProperties_tcfGdprMacrosIncluded
    pub tagProperties_tcfGdprMacrosIncluded: Option<String>,
}

/// POST userprofiles/{userprofilesId}/placements/generatetags
/// Generates tags for a placement.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_generatetags_builder()` + `dfareporting_placements_generatetags_execute()`.
/// For task-level control, use `dfareporting_placements_generatetags_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_generatetags(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsGeneratetagsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlacementsGenerateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_generatetags_builder(
        client,
        &args.profileId,
        &args.campaignId,
        &args.placementIds,
        &args.tagFormats,
        &args.tagProperties_dcDbmMacroIncluded,
        &args.tagProperties_gppMacrosIncluded,
        &args.tagProperties_tcfGdprMacrosIncluded,
    )?;
    dfareporting_placements_generatetags_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placements/{placementsId}
/// Gets one placement by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_get_execute()` to send, or `dfareporting_placements_get` for simplest API.

pub fn dfareporting_placements_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placements/{placementsId}
/// Gets one placement by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_get_execute()` or `dfareporting_placements_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Placement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Placement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placements/{placementsId}
/// Gets one placement by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_get_task()`.
/// For the simplest API, use `dfareporting_placements_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_placements_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/placements/{placementsId}
/// Gets one placement by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_get_builder()` + `dfareporting_placements_get_execute()`.
/// For task-level control, use `dfareporting_placements_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_get(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_placements_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/placements
/// Inserts a new placement.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_insert_execute()` to send, or `dfareporting_placements_insert` for simplest API.

pub fn dfareporting_placements_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/placements
/// Inserts a new placement.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_insert_execute()` or `dfareporting_placements_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Placement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Placement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/placements
/// Inserts a new placement.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_insert_task()`.
/// For the simplest API, use `dfareporting_placements_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_placements_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/placements
/// Inserts a new placement.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_insert_builder()` + `dfareporting_placements_insert_execute()`.
/// For task-level control, use `dfareporting_placements_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_insert(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_insert_builder(client, &args.profileId)?;
    dfareporting_placements_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/placements
/// Retrieves a list of placements, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_list_execute()` to send, or `dfareporting_placements_list` for simplest API.

pub fn dfareporting_placements_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    activeStatus: &Option<String>,
    advertiserIds: &Option<String>,
    campaignIds: &Option<String>,
    compatibilities: &Option<String>,
    contentCategoryIds: &Option<String>,
    directorySiteIds: &Option<String>,
    groupIds: &Option<String>,
    ids: &Option<String>,
    maxEndDate: &Option<String>,
    maxResults: &Option<String>,
    maxStartDate: &Option<String>,
    minEndDate: &Option<String>,
    minStartDate: &Option<String>,
    pageToken: &Option<String>,
    paymentSource: &Option<String>,
    placementStrategyIds: &Option<String>,
    pricingTypes: &Option<String>,
    searchString: &Option<String>,
    siteIds: &Option<String>,
    sizeIds: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = activeStatus.as_ref() {
        query_parts.push(format!("activeStatus={}", val));
    }
    if let Some(val) = advertiserIds.as_ref() {
        query_parts.push(format!("advertiserIds={}", val));
    }
    if let Some(val) = campaignIds.as_ref() {
        query_parts.push(format!("campaignIds={}", val));
    }
    if let Some(val) = compatibilities.as_ref() {
        query_parts.push(format!("compatibilities={}", val));
    }
    if let Some(val) = contentCategoryIds.as_ref() {
        query_parts.push(format!("contentCategoryIds={}", val));
    }
    if let Some(val) = directorySiteIds.as_ref() {
        query_parts.push(format!("directorySiteIds={}", val));
    }
    if let Some(val) = groupIds.as_ref() {
        query_parts.push(format!("groupIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxEndDate.as_ref() {
        query_parts.push(format!("maxEndDate={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = maxStartDate.as_ref() {
        query_parts.push(format!("maxStartDate={}", val));
    }
    if let Some(val) = minEndDate.as_ref() {
        query_parts.push(format!("minEndDate={}", val));
    }
    if let Some(val) = minStartDate.as_ref() {
        query_parts.push(format!("minStartDate={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = paymentSource.as_ref() {
        query_parts.push(format!("paymentSource={}", val));
    }
    if let Some(val) = placementStrategyIds.as_ref() {
        query_parts.push(format!("placementStrategyIds={}", val));
    }
    if let Some(val) = pricingTypes.as_ref() {
        query_parts.push(format!("pricingTypes={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = siteIds.as_ref() {
        query_parts.push(format!("siteIds={}", val));
    }
    if let Some(val) = sizeIds.as_ref() {
        query_parts.push(format!("sizeIds={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/placements
/// Retrieves a list of placements, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_list_execute()` or `dfareporting_placements_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlacementsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/placements
/// Retrieves a list of placements, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_list_task()`.
/// For the simplest API, use `dfareporting_placements_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_placements_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: activeStatus
    pub activeStatus: Option<String>,
    /// Query parameter: advertiserIds
    pub advertiserIds: Option<String>,
    /// Query parameter: campaignIds
    pub campaignIds: Option<String>,
    /// Query parameter: compatibilities
    pub compatibilities: Option<String>,
    /// Query parameter: contentCategoryIds
    pub contentCategoryIds: Option<String>,
    /// Query parameter: directorySiteIds
    pub directorySiteIds: Option<String>,
    /// Query parameter: groupIds
    pub groupIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxEndDate
    pub maxEndDate: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: maxStartDate
    pub maxStartDate: Option<String>,
    /// Query parameter: minEndDate
    pub minEndDate: Option<String>,
    /// Query parameter: minStartDate
    pub minStartDate: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: paymentSource
    pub paymentSource: Option<String>,
    /// Query parameter: placementStrategyIds
    pub placementStrategyIds: Option<String>,
    /// Query parameter: pricingTypes
    pub pricingTypes: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: siteIds
    pub siteIds: Option<String>,
    /// Query parameter: sizeIds
    pub sizeIds: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/placements
/// Retrieves a list of placements, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_list_builder()` + `dfareporting_placements_list_execute()`.
/// For task-level control, use `dfareporting_placements_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_list(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlacementsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_list_builder(
        client,
        &args.profileId,
        &args.activeStatus,
        &args.advertiserIds,
        &args.campaignIds,
        &args.compatibilities,
        &args.contentCategoryIds,
        &args.directorySiteIds,
        &args.groupIds,
        &args.ids,
        &args.maxEndDate,
        &args.maxResults,
        &args.maxStartDate,
        &args.minEndDate,
        &args.minStartDate,
        &args.pageToken,
        &args.paymentSource,
        &args.placementStrategyIds,
        &args.pricingTypes,
        &args.searchString,
        &args.siteIds,
        &args.sizeIds,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_placements_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/placements
/// Updates an existing placement. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_patch_execute()` to send, or `dfareporting_placements_patch` for simplest API.

pub fn dfareporting_placements_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/placements
/// Updates an existing placement. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_patch_execute()` or `dfareporting_placements_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Placement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Placement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/placements
/// Updates an existing placement. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_patch_task()`.
/// For the simplest API, use `dfareporting_placements_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_placements_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/placements
/// Updates an existing placement. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_patch_builder()` + `dfareporting_placements_patch_execute()`.
/// For task-level control, use `dfareporting_placements_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_patch(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_placements_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/placements
/// Updates an existing placement.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_placements_update_execute()` to send, or `dfareporting_placements_update` for simplest API.

pub fn dfareporting_placements_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/placements",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/placements
/// Updates an existing placement.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_placements_update_execute()` or `dfareporting_placements_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Placement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Placement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/placements
/// Updates an existing placement.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_placements_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_placements_update_task()`.
/// For the simplest API, use `dfareporting_placements_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_placements_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_placements_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_placements_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_placements_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/placements
/// Updates an existing placement.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_placements_update_builder()` + `dfareporting_placements_update_execute()`.
/// For task-level control, use `dfareporting_placements_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_placements_update(
    client: &SimpleHttpClient,
    args: &DfareportingPlacementsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Placement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_placements_update_builder(client, &args.profileId)?;
    dfareporting_placements_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/platformTypes/{platformTypesId}
/// Gets one platform type by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_platform_types_get_execute()` to send, or `dfareporting_platform_types_get` for simplest API.

pub fn dfareporting_platform_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/platformTypes/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/platformTypes/{platformTypesId}
/// Gets one platform type by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_platform_types_get_execute()` or `dfareporting_platform_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_platform_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_platform_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlatformType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlatformType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/platformTypes/{platformTypesId}
/// Gets one platform type by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_platform_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_platform_types_get_task()`.
/// For the simplest API, use `dfareporting_platform_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_platform_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_platform_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_platform_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_platform_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlatformTypesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/platformTypes/{platformTypesId}
/// Gets one platform type by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_platform_types_get_builder()` + `dfareporting_platform_types_get_execute()`.
/// For task-level control, use `dfareporting_platform_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_platform_types_get(
    client: &SimpleHttpClient,
    args: &DfareportingPlatformTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_platform_types_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_platform_types_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/platformTypes
/// Retrieves a list of platform types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_platform_types_list_execute()` to send, or `dfareporting_platform_types_list` for simplest API.

pub fn dfareporting_platform_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/platformTypes",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/platformTypes
/// Retrieves a list of platform types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_platform_types_list_execute()` or `dfareporting_platform_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_platform_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_platform_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlatformTypesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlatformTypesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/platformTypes
/// Retrieves a list of platform types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_platform_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_platform_types_list_task()`.
/// For the simplest API, use `dfareporting_platform_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_platform_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_platform_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformTypesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_platform_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_platform_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlatformTypesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/platformTypes
/// Retrieves a list of platform types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_platform_types_list_builder()` + `dfareporting_platform_types_list_execute()`.
/// For task-level control, use `dfareporting_platform_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_platform_types_list(
    client: &SimpleHttpClient,
    args: &DfareportingPlatformTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformTypesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_platform_types_list_builder(client, &args.profileId)?;
    dfareporting_platform_types_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/postalCodes/{postalCodesId}
/// Gets one postal code by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_postal_codes_get_execute()` to send, or `dfareporting_postal_codes_get` for simplest API.

pub fn dfareporting_postal_codes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    code: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/postalCodes/{}",
        profileId, code,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/postalCodes/{postalCodesId}
/// Gets one postal code by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_postal_codes_get_execute()` or `dfareporting_postal_codes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_postal_codes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_postal_codes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostalCode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostalCode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/postalCodes/{postalCodesId}
/// Gets one postal code by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_postal_codes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_postal_codes_get_task()`.
/// For the simplest API, use `dfareporting_postal_codes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_postal_codes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_postal_codes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostalCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_postal_codes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_postal_codes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPostalCodesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: code
    pub code: String,
}

/// GET userprofiles/{userprofilesId}/postalCodes/{postalCodesId}
/// Gets one postal code by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_postal_codes_get_builder()` + `dfareporting_postal_codes_get_execute()`.
/// For task-level control, use `dfareporting_postal_codes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_postal_codes_get(
    client: &SimpleHttpClient,
    args: &DfareportingPostalCodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostalCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_postal_codes_get_builder(client, &args.profileId, &args.code)?;
    dfareporting_postal_codes_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/postalCodes
/// Retrieves a list of postal codes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_postal_codes_list_execute()` to send, or `dfareporting_postal_codes_list` for simplest API.

pub fn dfareporting_postal_codes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/postalCodes",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/postalCodes
/// Retrieves a list of postal codes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_postal_codes_list_execute()` or `dfareporting_postal_codes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_postal_codes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_postal_codes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostalCodesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostalCodesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/postalCodes
/// Retrieves a list of postal codes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_postal_codes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_postal_codes_list_task()`.
/// For the simplest API, use `dfareporting_postal_codes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_postal_codes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_postal_codes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostalCodesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_postal_codes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_postal_codes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPostalCodesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/postalCodes
/// Retrieves a list of postal codes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_postal_codes_list_builder()` + `dfareporting_postal_codes_list_execute()`.
/// For task-level control, use `dfareporting_postal_codes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_postal_codes_list(
    client: &SimpleHttpClient,
    args: &DfareportingPostalCodesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostalCodesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_postal_codes_list_builder(client, &args.profileId)?;
    dfareporting_postal_codes_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/regions
/// Retrieves a list of regions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_regions_list_execute()` to send, or `dfareporting_regions_list` for simplest API.

pub fn dfareporting_regions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/regions",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/regions
/// Retrieves a list of regions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_regions_list_execute()` or `dfareporting_regions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_regions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_regions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/regions
/// Retrieves a list of regions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_regions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_regions_list_task()`.
/// For the simplest API, use `dfareporting_regions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_regions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_regions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_regions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_regions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRegionsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/regions
/// Retrieves a list of regions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_regions_list_builder()` + `dfareporting_regions_list_execute()`.
/// For task-level control, use `dfareporting_regions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_regions_list(
    client: &SimpleHttpClient,
    args: &DfareportingRegionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_regions_list_builder(client, &args.profileId)?;
    dfareporting_regions_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingListShares/{remarketingListSharesId}
/// Gets one remarketing list share by remarketing list ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_list_shares_get_execute()` to send, or `dfareporting_remarketing_list_shares_get` for simplest API.

pub fn dfareporting_remarketing_list_shares_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    remarketingListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingListShares/{}",
        profileId,
        remarketingListId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingListShares/{remarketingListSharesId}
/// Gets one remarketing list share by remarketing list ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_list_shares_get_execute()` or `dfareporting_remarketing_list_shares_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingListShare>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingListShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/remarketingListShares/{remarketingListSharesId}
/// Gets one remarketing list share by remarketing list ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_list_shares_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_list_shares_get_task()`.
/// For the simplest API, use `dfareporting_remarketing_list_shares_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_list_shares_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_list_shares_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_list_shares_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListSharesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: remarketingListId
    pub remarketingListId: String,
}

/// GET userprofiles/{userprofilesId}/remarketingListShares/{remarketingListSharesId}
/// Gets one remarketing list share by remarketing list ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_list_shares_get_builder()` + `dfareporting_remarketing_list_shares_get_execute()`.
/// For task-level control, use `dfareporting_remarketing_list_shares_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_get(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListSharesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_list_shares_get_builder(
        client,
        &args.profileId,
        &args.remarketingListId,
    )?;
    dfareporting_remarketing_list_shares_get_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_list_shares_patch_execute()` to send, or `dfareporting_remarketing_list_shares_patch` for simplest API.

pub fn dfareporting_remarketing_list_shares_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingListShares",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_list_shares_patch_execute()` or `dfareporting_remarketing_list_shares_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingListShare>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingListShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_list_shares_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_list_shares_patch_task()`.
/// For the simplest API, use `dfareporting_remarketing_list_shares_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_list_shares_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_list_shares_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_list_shares_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListSharesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_list_shares_patch_builder()` + `dfareporting_remarketing_list_shares_patch_execute()`.
/// For task-level control, use `dfareporting_remarketing_list_shares_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_patch(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListSharesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_remarketing_list_shares_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_remarketing_list_shares_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_list_shares_update_execute()` to send, or `dfareporting_remarketing_list_shares_update` for simplest API.

pub fn dfareporting_remarketing_list_shares_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingListShares",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_list_shares_update_execute()` or `dfareporting_remarketing_list_shares_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingListShare>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingListShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_list_shares_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_list_shares_update_task()`.
/// For the simplest API, use `dfareporting_remarketing_list_shares_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_list_shares_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_list_shares_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_list_shares_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_list_shares_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListSharesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/remarketingListShares
/// Updates an existing remarketing list share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_list_shares_update_builder()` + `dfareporting_remarketing_list_shares_update_execute()`.
/// For task-level control, use `dfareporting_remarketing_list_shares_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_list_shares_update(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListSharesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingListShare>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_list_shares_update_builder(client, &args.profileId)?;
    dfareporting_remarketing_list_shares_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingLists/{remarketingListsId}
/// Gets one remarketing list by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_lists_get_execute()` to send, or `dfareporting_remarketing_lists_get` for simplest API.

pub fn dfareporting_remarketing_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingLists/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingLists/{remarketingListsId}
/// Gets one remarketing list by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_lists_get_execute()` or `dfareporting_remarketing_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/remarketingLists/{remarketingListsId}
/// Gets one remarketing list by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_lists_get_task()`.
/// For the simplest API, use `dfareporting_remarketing_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/remarketingLists/{remarketingListsId}
/// Gets one remarketing list by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_lists_get_builder()` + `dfareporting_remarketing_lists_get_execute()`.
/// For task-level control, use `dfareporting_remarketing_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_get(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_lists_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_remarketing_lists_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/remarketingLists
/// Inserts a new remarketing list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_lists_insert_execute()` to send, or `dfareporting_remarketing_lists_insert` for simplest API.

pub fn dfareporting_remarketing_lists_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingLists",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/remarketingLists
/// Inserts a new remarketing list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_lists_insert_execute()` or `dfareporting_remarketing_lists_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/remarketingLists
/// Inserts a new remarketing list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_lists_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_lists_insert_task()`.
/// For the simplest API, use `dfareporting_remarketing_lists_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_lists_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_lists_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_lists_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/remarketingLists
/// Inserts a new remarketing list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_lists_insert_builder()` + `dfareporting_remarketing_lists_insert_execute()`.
/// For task-level control, use `dfareporting_remarketing_lists_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_insert(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_lists_insert_builder(client, &args.profileId)?;
    dfareporting_remarketing_lists_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingLists
/// Retrieves a list of remarketing lists, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_lists_list_execute()` to send, or `dfareporting_remarketing_lists_list` for simplest API.

pub fn dfareporting_remarketing_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    advertiserId: &Option<String>,
    floodlightActivityId: &Option<String>,
    maxResults: &Option<String>,
    name: &Option<String>,
    pageToken: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingLists",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = floodlightActivityId.as_ref() {
        query_parts.push(format!("floodlightActivityId={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/remarketingLists
/// Retrieves a list of remarketing lists, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_lists_list_execute()` or `dfareporting_remarketing_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingListsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingListsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/remarketingLists
/// Retrieves a list of remarketing lists, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_lists_list_task()`.
/// For the simplest API, use `dfareporting_remarketing_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemarketingListsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: floodlightActivityId
    pub floodlightActivityId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/remarketingLists
/// Retrieves a list of remarketing lists, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_lists_list_builder()` + `dfareporting_remarketing_lists_list_execute()`.
/// For task-level control, use `dfareporting_remarketing_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_list(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemarketingListsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_lists_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.advertiserId,
        &args.floodlightActivityId,
        &args.maxResults,
        &args.name,
        &args.pageToken,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_remarketing_lists_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_lists_patch_execute()` to send, or `dfareporting_remarketing_lists_patch` for simplest API.

pub fn dfareporting_remarketing_lists_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingLists",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_lists_patch_execute()` or `dfareporting_remarketing_lists_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_lists_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_lists_patch_task()`.
/// For the simplest API, use `dfareporting_remarketing_lists_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_lists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_lists_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_lists_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_lists_patch_builder()` + `dfareporting_remarketing_lists_patch_execute()`.
/// For task-level control, use `dfareporting_remarketing_lists_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_patch(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_lists_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_remarketing_lists_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_remarketing_lists_update_execute()` to send, or `dfareporting_remarketing_lists_update` for simplest API.

pub fn dfareporting_remarketing_lists_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/remarketingLists",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_remarketing_lists_update_execute()` or `dfareporting_remarketing_lists_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemarketingList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_remarketing_lists_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_remarketing_lists_update_task()`.
/// For the simplest API, use `dfareporting_remarketing_lists_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_remarketing_lists_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_remarketing_lists_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_remarketing_lists_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_remarketing_lists_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingRemarketingListsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/remarketingLists
/// Updates an existing remarketing list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_remarketing_lists_update_builder()` + `dfareporting_remarketing_lists_update_execute()`.
/// For task-level control, use `dfareporting_remarketing_lists_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_remarketing_lists_update(
    client: &SimpleHttpClient,
    args: &DfareportingRemarketingListsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_remarketing_lists_update_builder(client, &args.profileId)?;
    dfareporting_remarketing_lists_update_execute(builder)
}

/// DELETE userprofiles/{profileId}/reports/{reportId}
/// Deletes a report by its ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_delete_execute()` to send, or `dfareporting_reports_delete` for simplest API.

pub fn dfareporting_reports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}",
        profileId, reportId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{profileId}/reports/{reportId}
/// Deletes a report by its ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_delete_execute()` or `dfareporting_reports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{profileId}/reports/{reportId}
/// Deletes a report by its ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_delete_task()`.
/// For the simplest API, use `dfareporting_reports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
}

/// DELETE userprofiles/{profileId}/reports/{reportId}
/// Deletes a report by its ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_delete_builder()` + `dfareporting_reports_delete_execute()`.
/// For task-level control, use `dfareporting_reports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_delete(
    client: &SimpleHttpClient,
    args: &DfareportingReportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_delete_builder(client, &args.profileId, &args.reportId)?;
    dfareporting_reports_delete_execute(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}
/// Retrieves a report by its ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_get_execute()` to send, or `dfareporting_reports_get` for simplest API.

pub fn dfareporting_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}",
        profileId, reportId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}
/// Retrieves a report by its ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_get_execute()` or `dfareporting_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/reports/{reportId}
/// Retrieves a report by its ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_get_task()`.
/// For the simplest API, use `dfareporting_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
}

/// GET userprofiles/{profileId}/reports/{reportId}
/// Retrieves a report by its ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_get_builder()` + `dfareporting_reports_get_execute()`.
/// For task-level control, use `dfareporting_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_get(
    client: &SimpleHttpClient,
    args: &DfareportingReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_get_builder(client, &args.profileId, &args.reportId)?;
    dfareporting_reports_get_execute(builder)
}

/// POST userprofiles/{profileId}/reports
/// Creates a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_insert_execute()` to send, or `dfareporting_reports_insert` for simplest API.

pub fn dfareporting_reports_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/reports
/// Creates a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_insert_execute()` or `dfareporting_reports_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/reports
/// Creates a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_insert_task()`.
/// For the simplest API, use `dfareporting_reports_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{profileId}/reports
/// Creates a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_insert_builder()` + `dfareporting_reports_insert_execute()`.
/// For task-level control, use `dfareporting_reports_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_insert(
    client: &SimpleHttpClient,
    args: &DfareportingReportsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_insert_builder(client, &args.profileId)?;
    dfareporting_reports_insert_execute(builder)
}

/// GET userprofiles/{profileId}/reports
/// Retrieves list of reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_list_execute()` to send, or `dfareporting_reports_list` for simplest API.

pub fn dfareporting_reports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    scope: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = scope.as_ref() {
        query_parts.push(format!("scope={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/reports
/// Retrieves list of reports.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_list_execute()` or `dfareporting_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/reports
/// Retrieves list of reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_list_task()`.
/// For the simplest API, use `dfareporting_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: scope
    pub scope: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{profileId}/reports
/// Retrieves list of reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_list_builder()` + `dfareporting_reports_list_execute()`.
/// For task-level control, use `dfareporting_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_list(
    client: &SimpleHttpClient,
    args: &DfareportingReportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_list_builder(
        client,
        &args.profileId,
        &args.maxResults,
        &args.pageToken,
        &args.scope,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_reports_list_execute(builder)
}

/// POST userprofiles/{profileId}/reports/{reportId}/run
/// Runs a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_run_execute()` to send, or `dfareporting_reports_run` for simplest API.

pub fn dfareporting_reports_run_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
    synchronous: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}/run",
        profileId, reportId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = synchronous.as_ref() {
        query_parts.push(format!("synchronous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/reports/{reportId}/run
/// Runs a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_run_execute()` or `dfareporting_reports_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/reports/{reportId}/run
/// Runs a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_run_task()`.
/// For the simplest API, use `dfareporting_reports_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsRunArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
    /// Query parameter: synchronous
    pub synchronous: Option<String>,
}

/// POST userprofiles/{profileId}/reports/{reportId}/run
/// Runs a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_run_builder()` + `dfareporting_reports_run_execute()`.
/// For task-level control, use `dfareporting_reports_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_run(
    client: &SimpleHttpClient,
    args: &DfareportingReportsRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_run_builder(
        client,
        &args.profileId,
        &args.reportId,
        &args.synchronous,
    )?;
    dfareporting_reports_run_execute(builder)
}

/// PUT userprofiles/{profileId}/reports/{reportId}
/// Updates a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_update_execute()` to send, or `dfareporting_reports_update` for simplest API.

pub fn dfareporting_reports_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}",
        profileId, reportId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{profileId}/reports/{reportId}
/// Updates a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_update_execute()` or `dfareporting_reports_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{profileId}/reports/{reportId}
/// Updates a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_update_task()`.
/// For the simplest API, use `dfareporting_reports_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
}

/// PUT userprofiles/{profileId}/reports/{reportId}
/// Updates a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_update_builder()` + `dfareporting_reports_update_execute()`.
/// For task-level control, use `dfareporting_reports_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_update(
    client: &SimpleHttpClient,
    args: &DfareportingReportsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_update_builder(client, &args.profileId, &args.reportId)?;
    dfareporting_reports_update_execute(builder)
}

/// POST userprofiles/{profileId}/reports/compatiblefields/query
/// Returns the fields that are compatible to be selected in the respective sections of a report criteria, given the fields already selected in the input report and user permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_compatible_fields_query_execute()` to send, or `dfareporting_reports_compatible_fields_query` for simplest API.

pub fn dfareporting_reports_compatible_fields_query_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/compatiblefields/query",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{profileId}/reports/compatiblefields/query
/// Returns the fields that are compatible to be selected in the respective sections of a report criteria, given the fields already selected in the input report and user permissions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_compatible_fields_query_execute()` or `dfareporting_reports_compatible_fields_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_compatible_fields_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_compatible_fields_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompatibleFields>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompatibleFields = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{profileId}/reports/compatiblefields/query
/// Returns the fields that are compatible to be selected in the respective sections of a report criteria, given the fields already selected in the input report and user permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_compatible_fields_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_compatible_fields_query_task()`.
/// For the simplest API, use `dfareporting_reports_compatible_fields_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_compatible_fields_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_compatible_fields_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompatibleFields>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_reports_compatible_fields_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_compatible_fields_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsCompatibleFieldsQueryArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{profileId}/reports/compatiblefields/query
/// Returns the fields that are compatible to be selected in the respective sections of a report criteria, given the fields already selected in the input report and user permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_compatible_fields_query_builder()` + `dfareporting_reports_compatible_fields_query_execute()`.
/// For task-level control, use `dfareporting_reports_compatible_fields_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_compatible_fields_query(
    client: &SimpleHttpClient,
    args: &DfareportingReportsCompatibleFieldsQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompatibleFields>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_compatible_fields_query_builder(client, &args.profileId)?;
    dfareporting_reports_compatible_fields_query_execute(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_files_get_execute()` to send, or `dfareporting_reports_files_get` for simplest API.

pub fn dfareporting_reports_files_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
    fileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}/files/{}",
        profileId, reportId, fileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_files_get_execute()` or `dfareporting_reports_files_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_files_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_files_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_files_get_task()`.
/// For the simplest API, use `dfareporting_reports_files_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_files_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_files_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_files_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsFilesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
    /// Path parameter: fileId
    pub fileId: String,
}

/// GET userprofiles/{profileId}/reports/{reportId}/files/{fileId}
/// Retrieves a report file by its report ID and file ID. This method supports media download.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_files_get_builder()` + `dfareporting_reports_files_get_execute()`.
/// For task-level control, use `dfareporting_reports_files_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_files_get(
    client: &SimpleHttpClient,
    args: &DfareportingReportsFilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_files_get_builder(
        client,
        &args.profileId,
        &args.reportId,
        &args.fileId,
    )?;
    dfareporting_reports_files_get_execute(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}/files
/// Lists files for a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_reports_files_list_execute()` to send, or `dfareporting_reports_files_list` for simplest API.

pub fn dfareporting_reports_files_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    reportId: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/reports/{}/files",
        profileId, reportId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}/reports/{reportId}/files
/// Lists files for a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_reports_files_list_execute()` or `dfareporting_reports_files_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_files_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FileList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}/reports/{reportId}/files
/// Lists files for a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_reports_files_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_reports_files_list_task()`.
/// For the simplest API, use `dfareporting_reports_files_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_reports_files_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_reports_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_reports_files_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_reports_files_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingReportsFilesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: reportId
    pub reportId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{profileId}/reports/{reportId}/files
/// Lists files for a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_reports_files_list_builder()` + `dfareporting_reports_files_list_execute()`.
/// For task-level control, use `dfareporting_reports_files_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_reports_files_list(
    client: &SimpleHttpClient,
    args: &DfareportingReportsFilesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_reports_files_list_builder(
        client,
        &args.profileId,
        &args.reportId,
        &args.maxResults,
        &args.pageToken,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_reports_files_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/sites/{sitesId}
/// Gets one site by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sites_get_execute()` to send, or `dfareporting_sites_get` for simplest API.

pub fn dfareporting_sites_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sites/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/sites/{sitesId}
/// Gets one site by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sites_get_execute()` or `dfareporting_sites_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/sites/{sitesId}
/// Gets one site by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sites_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sites_get_task()`.
/// For the simplest API, use `dfareporting_sites_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sites_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sites_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSitesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/sites/{sitesId}
/// Gets one site by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sites_get_builder()` + `dfareporting_sites_get_execute()`.
/// For task-level control, use `dfareporting_sites_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_get(
    client: &SimpleHttpClient,
    args: &DfareportingSitesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sites_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_sites_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/sites
/// Inserts a new site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sites_insert_execute()` to send, or `dfareporting_sites_insert` for simplest API.

pub fn dfareporting_sites_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sites",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/sites
/// Inserts a new site.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sites_insert_execute()` or `dfareporting_sites_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/sites
/// Inserts a new site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sites_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sites_insert_task()`.
/// For the simplest API, use `dfareporting_sites_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sites_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sites_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sites_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSitesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/sites
/// Inserts a new site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sites_insert_builder()` + `dfareporting_sites_insert_execute()`.
/// For task-level control, use `dfareporting_sites_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_insert(
    client: &SimpleHttpClient,
    args: &DfareportingSitesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sites_insert_builder(client, &args.profileId)?;
    dfareporting_sites_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/sites
/// Retrieves a list of sites, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sites_list_execute()` to send, or `dfareporting_sites_list` for simplest API.

pub fn dfareporting_sites_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    acceptsInStreamVideoPlacements: &Option<String>,
    acceptsInterstitialPlacements: &Option<String>,
    acceptsPublisherPaidPlacements: &Option<String>,
    adWordsSite: &Option<String>,
    approved: &Option<String>,
    campaignIds: &Option<String>,
    directorySiteIds: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    subaccountId: &Option<String>,
    unmappedSite: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sites",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acceptsInStreamVideoPlacements.as_ref() {
        query_parts.push(format!("acceptsInStreamVideoPlacements={}", val));
    }
    if let Some(val) = acceptsInterstitialPlacements.as_ref() {
        query_parts.push(format!("acceptsInterstitialPlacements={}", val));
    }
    if let Some(val) = acceptsPublisherPaidPlacements.as_ref() {
        query_parts.push(format!("acceptsPublisherPaidPlacements={}", val));
    }
    if let Some(val) = adWordsSite.as_ref() {
        query_parts.push(format!("adWordsSite={}", val));
    }
    if let Some(val) = approved.as_ref() {
        query_parts.push(format!("approved={}", val));
    }
    if let Some(val) = campaignIds.as_ref() {
        query_parts.push(format!("campaignIds={}", val));
    }
    if let Some(val) = directorySiteIds.as_ref() {
        query_parts.push(format!("directorySiteIds={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }
    if let Some(val) = unmappedSite.as_ref() {
        query_parts.push(format!("unmappedSite={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/sites
/// Retrieves a list of sites, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sites_list_execute()` or `dfareporting_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SitesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SitesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/sites
/// Retrieves a list of sites, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sites_list_task()`.
/// For the simplest API, use `dfareporting_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSitesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: acceptsInStreamVideoPlacements
    pub acceptsInStreamVideoPlacements: Option<String>,
    /// Query parameter: acceptsInterstitialPlacements
    pub acceptsInterstitialPlacements: Option<String>,
    /// Query parameter: acceptsPublisherPaidPlacements
    pub acceptsPublisherPaidPlacements: Option<String>,
    /// Query parameter: adWordsSite
    pub adWordsSite: Option<String>,
    /// Query parameter: approved
    pub approved: Option<String>,
    /// Query parameter: campaignIds
    pub campaignIds: Option<String>,
    /// Query parameter: directorySiteIds
    pub directorySiteIds: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
    /// Query parameter: unmappedSite
    pub unmappedSite: Option<String>,
}

/// GET userprofiles/{userprofilesId}/sites
/// Retrieves a list of sites, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sites_list_builder()` + `dfareporting_sites_list_execute()`.
/// For task-level control, use `dfareporting_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_list(
    client: &SimpleHttpClient,
    args: &DfareportingSitesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_sites_list_builder(
        client,
        &args.profileId,
        &args.acceptsInStreamVideoPlacements,
        &args.acceptsInterstitialPlacements,
        &args.acceptsPublisherPaidPlacements,
        &args.adWordsSite,
        &args.approved,
        &args.campaignIds,
        &args.directorySiteIds,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.subaccountId,
        &args.unmappedSite,
    )?;
    dfareporting_sites_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/sites
/// Updates an existing site. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sites_patch_execute()` to send, or `dfareporting_sites_patch` for simplest API.

pub fn dfareporting_sites_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sites",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/sites
/// Updates an existing site. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sites_patch_execute()` or `dfareporting_sites_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/sites
/// Updates an existing site. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sites_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sites_patch_task()`.
/// For the simplest API, use `dfareporting_sites_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sites_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sites_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sites_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSitesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/sites
/// Updates an existing site. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sites_patch_builder()` + `dfareporting_sites_patch_execute()`.
/// For task-level control, use `dfareporting_sites_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_patch(
    client: &SimpleHttpClient,
    args: &DfareportingSitesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sites_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_sites_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/sites
/// Updates an existing site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sites_update_execute()` to send, or `dfareporting_sites_update` for simplest API.

pub fn dfareporting_sites_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sites",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/sites
/// Updates an existing site.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sites_update_execute()` or `dfareporting_sites_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/sites
/// Updates an existing site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sites_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sites_update_task()`.
/// For the simplest API, use `dfareporting_sites_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sites_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sites_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sites_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sites_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSitesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/sites
/// Updates an existing site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sites_update_builder()` + `dfareporting_sites_update_execute()`.
/// For task-level control, use `dfareporting_sites_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sites_update(
    client: &SimpleHttpClient,
    args: &DfareportingSitesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sites_update_builder(client, &args.profileId)?;
    dfareporting_sites_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/sizes/{sizesId}
/// Gets one size by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sizes_get_execute()` to send, or `dfareporting_sizes_get` for simplest API.

pub fn dfareporting_sizes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sizes/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/sizes/{sizesId}
/// Gets one size by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sizes_get_execute()` or `dfareporting_sizes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Size>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Size = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/sizes/{sizesId}
/// Gets one size by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sizes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sizes_get_task()`.
/// For the simplest API, use `dfareporting_sizes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sizes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Size>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sizes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sizes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSizesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/sizes/{sizesId}
/// Gets one size by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sizes_get_builder()` + `dfareporting_sizes_get_execute()`.
/// For task-level control, use `dfareporting_sizes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_get(
    client: &SimpleHttpClient,
    args: &DfareportingSizesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Size>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sizes_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_sizes_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/sizes
/// Inserts a new size.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sizes_insert_execute()` to send, or `dfareporting_sizes_insert` for simplest API.

pub fn dfareporting_sizes_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sizes",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/sizes
/// Inserts a new size.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sizes_insert_execute()` or `dfareporting_sizes_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Size>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Size = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/sizes
/// Inserts a new size.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sizes_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sizes_insert_task()`.
/// For the simplest API, use `dfareporting_sizes_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sizes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Size>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_sizes_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sizes_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSizesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/sizes
/// Inserts a new size.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sizes_insert_builder()` + `dfareporting_sizes_insert_execute()`.
/// For task-level control, use `dfareporting_sizes_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_insert(
    client: &SimpleHttpClient,
    args: &DfareportingSizesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Size>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_sizes_insert_builder(client, &args.profileId)?;
    dfareporting_sizes_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/sizes
/// Retrieves a list of sizes, possibly filtered. Retrieved sizes are globally unique and may include values not currently in use by your account. Due to this, the list of sizes returned by this method may differ from the list seen in the Trafficking UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_sizes_list_execute()` to send, or `dfareporting_sizes_list` for simplest API.

pub fn dfareporting_sizes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    height: &Option<String>,
    iabStandard: &Option<String>,
    ids: &Option<String>,
    width: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/sizes",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = height.as_ref() {
        query_parts.push(format!("height={}", val));
    }
    if let Some(val) = iabStandard.as_ref() {
        query_parts.push(format!("iabStandard={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = width.as_ref() {
        query_parts.push(format!("width={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/sizes
/// Retrieves a list of sizes, possibly filtered. Retrieved sizes are globally unique and may include values not currently in use by your account. Due to this, the list of sizes returned by this method may differ from the list seen in the Trafficking UI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_sizes_list_execute()` or `dfareporting_sizes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SizesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SizesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/sizes
/// Retrieves a list of sizes, possibly filtered. Retrieved sizes are globally unique and may include values not currently in use by your account. Due to this, the list of sizes returned by this method may differ from the list seen in the Trafficking UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_sizes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_sizes_list_task()`.
/// For the simplest API, use `dfareporting_sizes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_sizes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_sizes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SizesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_sizes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_sizes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSizesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: height
    pub height: Option<String>,
    /// Query parameter: iabStandard
    pub iabStandard: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: width
    pub width: Option<String>,
}

/// GET userprofiles/{userprofilesId}/sizes
/// Retrieves a list of sizes, possibly filtered. Retrieved sizes are globally unique and may include values not currently in use by your account. Due to this, the list of sizes returned by this method may differ from the list seen in the Trafficking UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_sizes_list_builder()` + `dfareporting_sizes_list_execute()`.
/// For task-level control, use `dfareporting_sizes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_sizes_list(
    client: &SimpleHttpClient,
    args: &DfareportingSizesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SizesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_sizes_list_builder(
        client,
        &args.profileId,
        &args.height,
        &args.iabStandard,
        &args.ids,
        &args.width,
    )?;
    dfareporting_sizes_list_execute(builder)
}

/// POST studio/creativeAssets
/// Inserts a new studio creative asset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_studio_creative_assets_insert_execute()` to send, or `dfareporting_studio_creative_assets_insert` for simplest API.

pub fn dfareporting_studio_creative_assets_insert_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/creativeAssets",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/creativeAssets
/// Inserts a new studio creative asset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_studio_creative_assets_insert_execute()` or `dfareporting_studio_creative_assets_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creative_assets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creative_assets_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudioCreativeAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudioCreativeAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/creativeAssets
/// Inserts a new studio creative asset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_studio_creative_assets_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_studio_creative_assets_insert_task()`.
/// For the simplest API, use `dfareporting_studio_creative_assets_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creative_assets_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_studio_creative_assets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StudioCreativeAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_studio_creative_assets_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST studio/creativeAssets
/// Inserts a new studio creative asset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_studio_creative_assets_insert_builder()` + `dfareporting_studio_creative_assets_insert_execute()`.
/// For task-level control, use `dfareporting_studio_creative_assets_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creative_assets_insert(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StudioCreativeAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_studio_creative_assets_insert_builder(client)?;
    dfareporting_studio_creative_assets_insert_execute(builder)
}

/// GET studio/creatives/{creativesId}
/// Gets a studio creative by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_studio_creatives_get_execute()` to send, or `dfareporting_studio_creatives_get` for simplest API.

pub fn dfareporting_studio_creatives_get_builder<R>(
    client: &SimpleHttpClient<R>,
    studioCreativeId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/creatives/{}",
        studioCreativeId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET studio/creatives/{creativesId}
/// Gets a studio creative by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_studio_creatives_get_execute()` or `dfareporting_studio_creatives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudioCreative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudioCreative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET studio/creatives/{creativesId}
/// Gets a studio creative by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_studio_creatives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_studio_creatives_get_task()`.
/// For the simplest API, use `dfareporting_studio_creatives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_studio_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudioCreative>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_studio_creatives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_studio_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingStudioCreativesGetArgs {
    /// Path parameter: studioCreativeId
    pub studioCreativeId: String,
}

/// GET studio/creatives/{creativesId}
/// Gets a studio creative by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_studio_creatives_get_builder()` + `dfareporting_studio_creatives_get_execute()`.
/// For task-level control, use `dfareporting_studio_creatives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_get(
    client: &SimpleHttpClient,
    args: &DfareportingStudioCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudioCreative>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_studio_creatives_get_builder(client, &args.studioCreativeId)?;
    dfareporting_studio_creatives_get_execute(builder)
}

/// POST studio/creatives
/// Inserts a new studio creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_studio_creatives_insert_execute()` to send, or `dfareporting_studio_creatives_insert` for simplest API.

pub fn dfareporting_studio_creatives_insert_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://dfareporting.googleapis.com/dfareporting/v5/studio/creatives",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/creatives
/// Inserts a new studio creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_studio_creatives_insert_execute()` or `dfareporting_studio_creatives_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudioCreative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudioCreative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/creatives
/// Inserts a new studio creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_studio_creatives_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_studio_creatives_insert_task()`.
/// For the simplest API, use `dfareporting_studio_creatives_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_studio_creatives_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudioCreative>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_studio_creatives_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST studio/creatives
/// Inserts a new studio creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_studio_creatives_insert_builder()` + `dfareporting_studio_creatives_insert_execute()`.
/// For task-level control, use `dfareporting_studio_creatives_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_insert(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudioCreative>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_studio_creatives_insert_builder(client)?;
    dfareporting_studio_creatives_insert_execute(builder)
}

/// POST studio/creatives/{creativesId}/publish
/// Publish for a studio creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_studio_creatives_publish_execute()` to send, or `dfareporting_studio_creatives_publish` for simplest API.

pub fn dfareporting_studio_creatives_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    studioCreativeId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/creatives/{}/publish",
        studioCreativeId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST studio/creatives/{creativesId}/publish
/// Publish for a studio creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_studio_creatives_publish_execute()` or `dfareporting_studio_creatives_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST studio/creatives/{creativesId}/publish
/// Publish for a studio creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_studio_creatives_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_studio_creatives_publish_task()`.
/// For the simplest API, use `dfareporting_studio_creatives_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_studio_creatives_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_studio_creatives_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_studio_creatives_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_studio_creatives_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingStudioCreativesPublishArgs {
    /// Path parameter: studioCreativeId
    pub studioCreativeId: String,
}

/// POST studio/creatives/{creativesId}/publish
/// Publish for a studio creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_studio_creatives_publish_builder()` + `dfareporting_studio_creatives_publish_execute()`.
/// For task-level control, use `dfareporting_studio_creatives_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_studio_creatives_publish(
    client: &SimpleHttpClient,
    args: &DfareportingStudioCreativesPublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_studio_creatives_publish_builder(client, &args.studioCreativeId)?;
    dfareporting_studio_creatives_publish_execute(builder)
}

/// GET userprofiles/{userprofilesId}/subaccounts/{subaccountsId}
/// Gets one subaccount by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_subaccounts_get_execute()` to send, or `dfareporting_subaccounts_get` for simplest API.

pub fn dfareporting_subaccounts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/subaccounts/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/subaccounts/{subaccountsId}
/// Gets one subaccount by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_subaccounts_get_execute()` or `dfareporting_subaccounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subaccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subaccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/subaccounts/{subaccountsId}
/// Gets one subaccount by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_subaccounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_subaccounts_get_task()`.
/// For the simplest API, use `dfareporting_subaccounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_subaccounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_subaccounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_subaccounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSubaccountsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/subaccounts/{subaccountsId}
/// Gets one subaccount by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_subaccounts_get_builder()` + `dfareporting_subaccounts_get_execute()`.
/// For task-level control, use `dfareporting_subaccounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_get(
    client: &SimpleHttpClient,
    args: &DfareportingSubaccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_subaccounts_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_subaccounts_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/subaccounts
/// Inserts a new subaccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_subaccounts_insert_execute()` to send, or `dfareporting_subaccounts_insert` for simplest API.

pub fn dfareporting_subaccounts_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/subaccounts",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/subaccounts
/// Inserts a new subaccount.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_subaccounts_insert_execute()` or `dfareporting_subaccounts_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subaccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subaccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/subaccounts
/// Inserts a new subaccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_subaccounts_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_subaccounts_insert_task()`.
/// For the simplest API, use `dfareporting_subaccounts_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_subaccounts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_subaccounts_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_subaccounts_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSubaccountsInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/subaccounts
/// Inserts a new subaccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_subaccounts_insert_builder()` + `dfareporting_subaccounts_insert_execute()`.
/// For task-level control, use `dfareporting_subaccounts_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_insert(
    client: &SimpleHttpClient,
    args: &DfareportingSubaccountsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_subaccounts_insert_builder(client, &args.profileId)?;
    dfareporting_subaccounts_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/subaccounts
/// Gets a list of subaccounts, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_subaccounts_list_execute()` to send, or `dfareporting_subaccounts_list` for simplest API.

pub fn dfareporting_subaccounts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/subaccounts",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/subaccounts
/// Gets a list of subaccounts, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_subaccounts_list_execute()` or `dfareporting_subaccounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubaccountsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubaccountsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/subaccounts
/// Gets a list of subaccounts, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_subaccounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_subaccounts_list_task()`.
/// For the simplest API, use `dfareporting_subaccounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_subaccounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubaccountsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_subaccounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_subaccounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSubaccountsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/subaccounts
/// Gets a list of subaccounts, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_subaccounts_list_builder()` + `dfareporting_subaccounts_list_execute()`.
/// For task-level control, use `dfareporting_subaccounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_list(
    client: &SimpleHttpClient,
    args: &DfareportingSubaccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubaccountsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_subaccounts_list_builder(
        client,
        &args.profileId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_subaccounts_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_subaccounts_patch_execute()` to send, or `dfareporting_subaccounts_patch` for simplest API.

pub fn dfareporting_subaccounts_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/subaccounts",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_subaccounts_patch_execute()` or `dfareporting_subaccounts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subaccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subaccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_subaccounts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_subaccounts_patch_task()`.
/// For the simplest API, use `dfareporting_subaccounts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_subaccounts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_subaccounts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_subaccounts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSubaccountsPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_subaccounts_patch_builder()` + `dfareporting_subaccounts_patch_execute()`.
/// For task-level control, use `dfareporting_subaccounts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_patch(
    client: &SimpleHttpClient,
    args: &DfareportingSubaccountsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_subaccounts_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_subaccounts_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_subaccounts_update_execute()` to send, or `dfareporting_subaccounts_update` for simplest API.

pub fn dfareporting_subaccounts_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/subaccounts",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_subaccounts_update_execute()` or `dfareporting_subaccounts_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subaccount>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subaccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_subaccounts_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_subaccounts_update_task()`.
/// For the simplest API, use `dfareporting_subaccounts_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_subaccounts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_subaccounts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_subaccounts_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_subaccounts_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingSubaccountsUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/subaccounts
/// Updates an existing subaccount.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_subaccounts_update_builder()` + `dfareporting_subaccounts_update_execute()`.
/// For task-level control, use `dfareporting_subaccounts_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_subaccounts_update(
    client: &SimpleHttpClient,
    args: &DfareportingSubaccountsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subaccount>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_subaccounts_update_builder(client, &args.profileId)?;
    dfareporting_subaccounts_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists/{targetableRemarketingListsId}
/// Gets one remarketing list by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targetable_remarketing_lists_get_execute()` to send, or `dfareporting_targetable_remarketing_lists_get` for simplest API.

pub fn dfareporting_targetable_remarketing_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetableRemarketingLists/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists/{targetableRemarketingListsId}
/// Gets one remarketing list by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targetable_remarketing_lists_get_execute()` or `dfareporting_targetable_remarketing_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targetable_remarketing_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targetable_remarketing_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetableRemarketingList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetableRemarketingList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists/{targetableRemarketingListsId}
/// Gets one remarketing list by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targetable_remarketing_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targetable_remarketing_lists_get_task()`.
/// For the simplest API, use `dfareporting_targetable_remarketing_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targetable_remarketing_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targetable_remarketing_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetableRemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targetable_remarketing_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targetable_remarketing_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetableRemarketingListsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists/{targetableRemarketingListsId}
/// Gets one remarketing list by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targetable_remarketing_lists_get_builder()` + `dfareporting_targetable_remarketing_lists_get_execute()`.
/// For task-level control, use `dfareporting_targetable_remarketing_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targetable_remarketing_lists_get(
    client: &SimpleHttpClient,
    args: &DfareportingTargetableRemarketingListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetableRemarketingList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_targetable_remarketing_lists_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_targetable_remarketing_lists_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists
/// Retrieves a list of targetable remarketing lists, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targetable_remarketing_lists_list_execute()` to send, or `dfareporting_targetable_remarketing_lists_list` for simplest API.

pub fn dfareporting_targetable_remarketing_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    active: &Option<String>,
    advertiserId: &Option<String>,
    maxResults: &Option<String>,
    name: &Option<String>,
    pageToken: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetableRemarketingLists",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = active.as_ref() {
        query_parts.push(format!("active={}", val));
    }
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists
/// Retrieves a list of targetable remarketing lists, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targetable_remarketing_lists_list_execute()` or `dfareporting_targetable_remarketing_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targetable_remarketing_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targetable_remarketing_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetableRemarketingListsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetableRemarketingListsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists
/// Retrieves a list of targetable remarketing lists, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targetable_remarketing_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targetable_remarketing_lists_list_task()`.
/// For the simplest API, use `dfareporting_targetable_remarketing_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targetable_remarketing_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targetable_remarketing_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetableRemarketingListsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targetable_remarketing_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targetable_remarketing_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetableRemarketingListsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: active
    pub active: Option<String>,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/targetableRemarketingLists
/// Retrieves a list of targetable remarketing lists, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targetable_remarketing_lists_list_builder()` + `dfareporting_targetable_remarketing_lists_list_execute()`.
/// For task-level control, use `dfareporting_targetable_remarketing_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targetable_remarketing_lists_list(
    client: &SimpleHttpClient,
    args: &DfareportingTargetableRemarketingListsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetableRemarketingListsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_targetable_remarketing_lists_list_builder(
        client,
        &args.profileId,
        &args.active,
        &args.advertiserId,
        &args.maxResults,
        &args.name,
        &args.pageToken,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_targetable_remarketing_lists_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/targetingTemplates/{targetingTemplatesId}
/// Gets one targeting template by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targeting_templates_get_execute()` to send, or `dfareporting_targeting_templates_get` for simplest API.

pub fn dfareporting_targeting_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetingTemplates/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/targetingTemplates/{targetingTemplatesId}
/// Gets one targeting template by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targeting_templates_get_execute()` or `dfareporting_targeting_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/targetingTemplates/{targetingTemplatesId}
/// Gets one targeting template by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targeting_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targeting_templates_get_task()`.
/// For the simplest API, use `dfareporting_targeting_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targeting_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targeting_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targeting_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetingTemplatesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/targetingTemplates/{targetingTemplatesId}
/// Gets one targeting template by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targeting_templates_get_builder()` + `dfareporting_targeting_templates_get_execute()`.
/// For task-level control, use `dfareporting_targeting_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_get(
    client: &SimpleHttpClient,
    args: &DfareportingTargetingTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_targeting_templates_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_targeting_templates_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/targetingTemplates
/// Inserts a new targeting template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targeting_templates_insert_execute()` to send, or `dfareporting_targeting_templates_insert` for simplest API.

pub fn dfareporting_targeting_templates_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetingTemplates",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/targetingTemplates
/// Inserts a new targeting template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targeting_templates_insert_execute()` or `dfareporting_targeting_templates_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/targetingTemplates
/// Inserts a new targeting template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targeting_templates_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targeting_templates_insert_task()`.
/// For the simplest API, use `dfareporting_targeting_templates_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targeting_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targeting_templates_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targeting_templates_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetingTemplatesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/targetingTemplates
/// Inserts a new targeting template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targeting_templates_insert_builder()` + `dfareporting_targeting_templates_insert_execute()`.
/// For task-level control, use `dfareporting_targeting_templates_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_insert(
    client: &SimpleHttpClient,
    args: &DfareportingTargetingTemplatesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_targeting_templates_insert_builder(client, &args.profileId)?;
    dfareporting_targeting_templates_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/targetingTemplates
/// Retrieves a list of targeting templates, optionally filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targeting_templates_list_execute()` to send, or `dfareporting_targeting_templates_list` for simplest API.

pub fn dfareporting_targeting_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    advertiserId: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetingTemplates",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/targetingTemplates
/// Retrieves a list of targeting templates, optionally filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targeting_templates_list_execute()` or `dfareporting_targeting_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingTemplatesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingTemplatesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/targetingTemplates
/// Retrieves a list of targeting templates, optionally filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targeting_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targeting_templates_list_task()`.
/// For the simplest API, use `dfareporting_targeting_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targeting_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetingTemplatesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targeting_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targeting_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetingTemplatesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET userprofiles/{userprofilesId}/targetingTemplates
/// Retrieves a list of targeting templates, optionally filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targeting_templates_list_builder()` + `dfareporting_targeting_templates_list_execute()`.
/// For task-level control, use `dfareporting_targeting_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_list(
    client: &SimpleHttpClient,
    args: &DfareportingTargetingTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetingTemplatesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_targeting_templates_list_builder(
        client,
        &args.profileId,
        &args.advertiserId,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
    )?;
    dfareporting_targeting_templates_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targeting_templates_patch_execute()` to send, or `dfareporting_targeting_templates_patch` for simplest API.

pub fn dfareporting_targeting_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetingTemplates",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targeting_templates_patch_execute()` or `dfareporting_targeting_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targeting_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targeting_templates_patch_task()`.
/// For the simplest API, use `dfareporting_targeting_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targeting_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targeting_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targeting_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetingTemplatesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targeting_templates_patch_builder()` + `dfareporting_targeting_templates_patch_execute()`.
/// For task-level control, use `dfareporting_targeting_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_patch(
    client: &SimpleHttpClient,
    args: &DfareportingTargetingTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_targeting_templates_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_targeting_templates_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_targeting_templates_update_execute()` to send, or `dfareporting_targeting_templates_update` for simplest API.

pub fn dfareporting_targeting_templates_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/targetingTemplates",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_targeting_templates_update_execute()` or `dfareporting_targeting_templates_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_targeting_templates_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_targeting_templates_update_task()`.
/// For the simplest API, use `dfareporting_targeting_templates_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_targeting_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_targeting_templates_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_targeting_templates_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_targeting_templates_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTargetingTemplatesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/targetingTemplates
/// Updates an existing targeting template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_targeting_templates_update_builder()` + `dfareporting_targeting_templates_update_execute()`.
/// For task-level control, use `dfareporting_targeting_templates_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_targeting_templates_update(
    client: &SimpleHttpClient,
    args: &DfareportingTargetingTemplatesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_targeting_templates_update_builder(client, &args.profileId)?;
    dfareporting_targeting_templates_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/tvCampaignDetails/{tvCampaignDetailsId}
/// Gets one TvCampaignDetail by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_tv_campaign_details_get_execute()` to send, or `dfareporting_tv_campaign_details_get` for simplest API.

pub fn dfareporting_tv_campaign_details_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
    accountId: &Option<String>,
    countryDartId: &Option<String>,
    tvDataProvider: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/tvCampaignDetails/{}",
        profileId, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accountId.as_ref() {
        query_parts.push(format!("accountId={}", val));
    }
    if let Some(val) = countryDartId.as_ref() {
        query_parts.push(format!("countryDartId={}", val));
    }
    if let Some(val) = tvDataProvider.as_ref() {
        query_parts.push(format!("tvDataProvider={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/tvCampaignDetails/{tvCampaignDetailsId}
/// Gets one TvCampaignDetail by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_tv_campaign_details_get_execute()` or `dfareporting_tv_campaign_details_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_tv_campaign_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_tv_campaign_details_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TvCampaignDetail>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TvCampaignDetail = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/tvCampaignDetails/{tvCampaignDetailsId}
/// Gets one TvCampaignDetail by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_tv_campaign_details_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_tv_campaign_details_get_task()`.
/// For the simplest API, use `dfareporting_tv_campaign_details_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_tv_campaign_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_tv_campaign_details_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TvCampaignDetail>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_tv_campaign_details_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_tv_campaign_details_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTvCampaignDetailsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: accountId
    pub accountId: Option<String>,
    /// Query parameter: countryDartId
    pub countryDartId: Option<String>,
    /// Query parameter: tvDataProvider
    pub tvDataProvider: Option<String>,
}

/// GET userprofiles/{userprofilesId}/tvCampaignDetails/{tvCampaignDetailsId}
/// Gets one TvCampaignDetail by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_tv_campaign_details_get_builder()` + `dfareporting_tv_campaign_details_get_execute()`.
/// For task-level control, use `dfareporting_tv_campaign_details_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_tv_campaign_details_get(
    client: &SimpleHttpClient,
    args: &DfareportingTvCampaignDetailsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TvCampaignDetail>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_tv_campaign_details_get_builder(
        client,
        &args.profileId,
        &args.id,
        &args.accountId,
        &args.countryDartId,
        &args.tvDataProvider,
    )?;
    dfareporting_tv_campaign_details_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/tvCampaignSummaries
/// Retrieves a list of TV campaign summaries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_tv_campaign_summaries_list_execute()` to send, or `dfareporting_tv_campaign_summaries_list` for simplest API.

pub fn dfareporting_tv_campaign_summaries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    accountId: &Option<String>,
    countryDartId: &Option<String>,
    name: &Option<String>,
    tvDataProvider: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/tvCampaignSummaries",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accountId.as_ref() {
        query_parts.push(format!("accountId={}", val));
    }
    if let Some(val) = countryDartId.as_ref() {
        query_parts.push(format!("countryDartId={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = tvDataProvider.as_ref() {
        query_parts.push(format!("tvDataProvider={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/tvCampaignSummaries
/// Retrieves a list of TV campaign summaries.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_tv_campaign_summaries_list_execute()` or `dfareporting_tv_campaign_summaries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_tv_campaign_summaries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_tv_campaign_summaries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TvCampaignSummariesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TvCampaignSummariesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/tvCampaignSummaries
/// Retrieves a list of TV campaign summaries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_tv_campaign_summaries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_tv_campaign_summaries_list_task()`.
/// For the simplest API, use `dfareporting_tv_campaign_summaries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_tv_campaign_summaries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_tv_campaign_summaries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TvCampaignSummariesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_tv_campaign_summaries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_tv_campaign_summaries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingTvCampaignSummariesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: accountId
    pub accountId: Option<String>,
    /// Query parameter: countryDartId
    pub countryDartId: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: tvDataProvider
    pub tvDataProvider: Option<String>,
}

/// GET userprofiles/{userprofilesId}/tvCampaignSummaries
/// Retrieves a list of TV campaign summaries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_tv_campaign_summaries_list_builder()` + `dfareporting_tv_campaign_summaries_list_execute()`.
/// For task-level control, use `dfareporting_tv_campaign_summaries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_tv_campaign_summaries_list(
    client: &SimpleHttpClient,
    args: &DfareportingTvCampaignSummariesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TvCampaignSummariesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_tv_campaign_summaries_list_builder(
        client,
        &args.profileId,
        &args.accountId,
        &args.countryDartId,
        &args.name,
        &args.tvDataProvider,
    )?;
    dfareporting_tv_campaign_summaries_list_execute(builder)
}

/// GET userprofiles/{profileId}
/// Gets one user profile by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_profiles_get_execute()` to send, or `dfareporting_user_profiles_get` for simplest API.

pub fn dfareporting_user_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{profileId}
/// Gets one user profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_profiles_get_execute()` or `dfareporting_user_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{profileId}
/// Gets one user profile by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_profiles_get_task()`.
/// For the simplest API, use `dfareporting_user_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserProfilesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{profileId}
/// Gets one user profile by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_profiles_get_builder()` + `dfareporting_user_profiles_get_execute()`.
/// For task-level control, use `dfareporting_user_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_profiles_get(
    client: &SimpleHttpClient,
    args: &DfareportingUserProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_profiles_get_builder(client, &args.profileId)?;
    dfareporting_user_profiles_get_execute(builder)
}

/// GET userprofiles
/// Retrieves list of user profiles for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_profiles_list_execute()` to send, or `dfareporting_user_profiles_list` for simplest API.

pub fn dfareporting_user_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://dfareporting.googleapis.com/dfareporting/v5/userprofiles",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles
/// Retrieves list of user profiles for a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_profiles_list_execute()` or `dfareporting_user_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserProfileList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserProfileList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles
/// Retrieves list of user profiles for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_profiles_list_task()`.
/// For the simplest API, use `dfareporting_user_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfileList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET userprofiles
/// Retrieves list of user profiles for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_profiles_list_builder()` + `dfareporting_user_profiles_list_execute()`.
/// For task-level control, use `dfareporting_user_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_profiles_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfileList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_user_profiles_list_builder(client)?;
    dfareporting_user_profiles_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups/{userRolePermissionGroupsId}
/// Gets one user role permission group by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_role_permission_groups_get_execute()` to send, or `dfareporting_user_role_permission_groups_get` for simplest API.

pub fn dfareporting_user_role_permission_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRolePermissionGroups/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups/{userRolePermissionGroupsId}
/// Gets one user role permission group by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_role_permission_groups_get_execute()` or `dfareporting_user_role_permission_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permission_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permission_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRolePermissionGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRolePermissionGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups/{userRolePermissionGroupsId}
/// Gets one user role permission group by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_role_permission_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_role_permission_groups_get_task()`.
/// For the simplest API, use `dfareporting_user_role_permission_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permission_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_role_permission_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolePermissionGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_role_permission_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_role_permission_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolePermissionGroupsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups/{userRolePermissionGroupsId}
/// Gets one user role permission group by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_role_permission_groups_get_builder()` + `dfareporting_user_role_permission_groups_get_execute()`.
/// For task-level control, use `dfareporting_user_role_permission_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permission_groups_get(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolePermissionGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolePermissionGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_user_role_permission_groups_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_user_role_permission_groups_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups
/// Gets a list of all supported user role permission groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_role_permission_groups_list_execute()` to send, or `dfareporting_user_role_permission_groups_list` for simplest API.

pub fn dfareporting_user_role_permission_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRolePermissionGroups",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups
/// Gets a list of all supported user role permission groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_role_permission_groups_list_execute()` or `dfareporting_user_role_permission_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permission_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permission_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRolePermissionGroupsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRolePermissionGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups
/// Gets a list of all supported user role permission groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_role_permission_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_role_permission_groups_list_task()`.
/// For the simplest API, use `dfareporting_user_role_permission_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permission_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_role_permission_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UserRolePermissionGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_role_permission_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_role_permission_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolePermissionGroupsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/userRolePermissionGroups
/// Gets a list of all supported user role permission groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_role_permission_groups_list_builder()` + `dfareporting_user_role_permission_groups_list_execute()`.
/// For task-level control, use `dfareporting_user_role_permission_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permission_groups_list(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolePermissionGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UserRolePermissionGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_user_role_permission_groups_list_builder(client, &args.profileId)?;
    dfareporting_user_role_permission_groups_list_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissions/{userRolePermissionsId}
/// Gets one user role permission by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_role_permissions_get_execute()` to send, or `dfareporting_user_role_permissions_get` for simplest API.

pub fn dfareporting_user_role_permissions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRolePermissions/{}",
        profileId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissions/{userRolePermissionsId}
/// Gets one user role permission by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_role_permissions_get_execute()` or `dfareporting_user_role_permissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRolePermission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRolePermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRolePermissions/{userRolePermissionsId}
/// Gets one user role permission by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_role_permissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_role_permissions_get_task()`.
/// For the simplest API, use `dfareporting_user_role_permissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_role_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolePermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_role_permissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_role_permissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolePermissionsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/userRolePermissions/{userRolePermissionsId}
/// Gets one user role permission by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_role_permissions_get_builder()` + `dfareporting_user_role_permissions_get_execute()`.
/// For task-level control, use `dfareporting_user_role_permissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permissions_get(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolePermissionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolePermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_user_role_permissions_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_user_role_permissions_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissions
/// Gets a list of user role permissions, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_role_permissions_list_execute()` to send, or `dfareporting_user_role_permissions_list` for simplest API.

pub fn dfareporting_user_role_permissions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    ids: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRolePermissions",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRolePermissions
/// Gets a list of user role permissions, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_role_permissions_list_execute()` or `dfareporting_user_role_permissions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permissions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRolePermissionsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRolePermissionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRolePermissions
/// Gets a list of user role permissions, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_role_permissions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_role_permissions_list_task()`.
/// For the simplest API, use `dfareporting_user_role_permissions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_role_permissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_role_permissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UserRolePermissionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_role_permissions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_role_permissions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolePermissionsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: ids
    pub ids: Option<String>,
}

/// GET userprofiles/{userprofilesId}/userRolePermissions
/// Gets a list of user role permissions, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_role_permissions_list_builder()` + `dfareporting_user_role_permissions_list_execute()`.
/// For task-level control, use `dfareporting_user_role_permissions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_role_permissions_list(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolePermissionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UserRolePermissionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dfareporting_user_role_permissions_list_builder(client, &args.profileId, &args.ids)?;
    dfareporting_user_role_permissions_list_execute(builder)
}

/// DELETE userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Deletes an existing user role.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_delete_execute()` to send, or `dfareporting_user_roles_delete` for simplest API.

pub fn dfareporting_user_roles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Deletes an existing user role.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_delete_execute()` or `dfareporting_user_roles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Deletes an existing user role.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_delete_task()`.
/// For the simplest API, use `dfareporting_user_roles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesDeleteArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Deletes an existing user role.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_delete_builder()` + `dfareporting_user_roles_delete_execute()`.
/// For task-level control, use `dfareporting_user_roles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_delete(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_delete_builder(client, &args.profileId, &args.id)?;
    dfareporting_user_roles_delete_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Gets one user role by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_get_execute()` to send, or `dfareporting_user_roles_get` for simplest API.

pub fn dfareporting_user_roles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Gets one user role by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_get_execute()` or `dfareporting_user_roles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRole>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRole = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Gets one user role by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_get_task()`.
/// For the simplest API, use `dfareporting_user_roles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/userRoles/{userRolesId}
/// Gets one user role by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_get_builder()` + `dfareporting_user_roles_get_execute()`.
/// For task-level control, use `dfareporting_user_roles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_get(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_user_roles_get_execute(builder)
}

/// POST userprofiles/{userprofilesId}/userRoles
/// Inserts a new user role.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_insert_execute()` to send, or `dfareporting_user_roles_insert` for simplest API.

pub fn dfareporting_user_roles_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles",
        profileId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST userprofiles/{userprofilesId}/userRoles
/// Inserts a new user role.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_insert_execute()` or `dfareporting_user_roles_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRole>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRole = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST userprofiles/{userprofilesId}/userRoles
/// Inserts a new user role.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_insert_task()`.
/// For the simplest API, use `dfareporting_user_roles_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesInsertArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// POST userprofiles/{userprofilesId}/userRoles
/// Inserts a new user role.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_insert_builder()` + `dfareporting_user_roles_insert_execute()`.
/// For task-level control, use `dfareporting_user_roles_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_insert(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_insert_builder(client, &args.profileId)?;
    dfareporting_user_roles_insert_execute(builder)
}

/// GET userprofiles/{userprofilesId}/userRoles
/// Retrieves a list of user roles, possibly filtered. This method supports paging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_list_execute()` to send, or `dfareporting_user_roles_list` for simplest API.

pub fn dfareporting_user_roles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    accountUserRoleOnly: &Option<String>,
    ids: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    searchString: &Option<String>,
    sortField: &Option<String>,
    sortOrder: &Option<String>,
    subaccountId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accountUserRoleOnly.as_ref() {
        query_parts.push(format!("accountUserRoleOnly={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = searchString.as_ref() {
        query_parts.push(format!("searchString={}", val));
    }
    if let Some(val) = sortField.as_ref() {
        query_parts.push(format!("sortField={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = subaccountId.as_ref() {
        query_parts.push(format!("subaccountId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/userRoles
/// Retrieves a list of user roles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_list_execute()` or `dfareporting_user_roles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRolesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRolesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/userRoles
/// Retrieves a list of user roles, possibly filtered. This method supports paging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_list_task()`.
/// For the simplest API, use `dfareporting_user_roles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesListArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: accountUserRoleOnly
    pub accountUserRoleOnly: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: searchString
    pub searchString: Option<String>,
    /// Query parameter: sortField
    pub sortField: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
    /// Query parameter: subaccountId
    pub subaccountId: Option<String>,
}

/// GET userprofiles/{userprofilesId}/userRoles
/// Retrieves a list of user roles, possibly filtered. This method supports paging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_list_builder()` + `dfareporting_user_roles_list_execute()`.
/// For task-level control, use `dfareporting_user_roles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_list(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRolesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_list_builder(
        client,
        &args.profileId,
        &args.accountUserRoleOnly,
        &args.ids,
        &args.maxResults,
        &args.pageToken,
        &args.searchString,
        &args.sortField,
        &args.sortOrder,
        &args.subaccountId,
    )?;
    dfareporting_user_roles_list_execute(builder)
}

/// PATCH userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_patch_execute()` to send, or `dfareporting_user_roles_patch` for simplest API.

pub fn dfareporting_user_roles_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles",
        profileId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_patch_execute()` or `dfareporting_user_roles_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRole>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRole = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_patch_task()`.
/// For the simplest API, use `dfareporting_user_roles_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesPatchArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Query parameter: id
    pub id: Option<String>,
}

/// PATCH userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_patch_builder()` + `dfareporting_user_roles_patch_execute()`.
/// For task-level control, use `dfareporting_user_roles_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_patch(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_patch_builder(client, &args.profileId, &args.id)?;
    dfareporting_user_roles_patch_execute(builder)
}

/// PUT userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_user_roles_update_execute()` to send, or `dfareporting_user_roles_update` for simplest API.

pub fn dfareporting_user_roles_update_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/userRoles",
        profileId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_user_roles_update_execute()` or `dfareporting_user_roles_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserRole>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserRole = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_user_roles_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_user_roles_update_task()`.
/// For the simplest API, use `dfareporting_user_roles_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_user_roles_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_user_roles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_user_roles_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_user_roles_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingUserRolesUpdateArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// PUT userprofiles/{userprofilesId}/userRoles
/// Updates an existing user role.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_user_roles_update_builder()` + `dfareporting_user_roles_update_execute()`.
/// For task-level control, use `dfareporting_user_roles_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_user_roles_update(
    client: &SimpleHttpClient,
    args: &DfareportingUserRolesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserRole>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_user_roles_update_builder(client, &args.profileId)?;
    dfareporting_user_roles_update_execute(builder)
}

/// GET userprofiles/{userprofilesId}/videoFormats/{videoFormatsId}
/// Gets one video format by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_video_formats_get_execute()` to send, or `dfareporting_video_formats_get` for simplest API.

pub fn dfareporting_video_formats_get_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/videoFormats/{}",
        profileId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/videoFormats/{videoFormatsId}
/// Gets one video format by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_video_formats_get_execute()` or `dfareporting_video_formats_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_video_formats_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_video_formats_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoFormat>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoFormat = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/videoFormats/{videoFormatsId}
/// Gets one video format by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_video_formats_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_video_formats_get_task()`.
/// For the simplest API, use `dfareporting_video_formats_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_video_formats_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_video_formats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoFormat>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dfareporting_video_formats_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_video_formats_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingVideoFormatsGetArgs {
    /// Path parameter: profileId
    pub profileId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET userprofiles/{userprofilesId}/videoFormats/{videoFormatsId}
/// Gets one video format by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_video_formats_get_builder()` + `dfareporting_video_formats_get_execute()`.
/// For task-level control, use `dfareporting_video_formats_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_video_formats_get(
    client: &SimpleHttpClient,
    args: &DfareportingVideoFormatsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoFormat>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dfareporting_video_formats_get_builder(client, &args.profileId, &args.id)?;
    dfareporting_video_formats_get_execute(builder)
}

/// GET userprofiles/{userprofilesId}/videoFormats
/// Lists available video formats.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dfareporting_video_formats_list_execute()` to send, or `dfareporting_video_formats_list` for simplest API.

pub fn dfareporting_video_formats_list_builder<R>(
    client: &SimpleHttpClient<R>,
    profileId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{}/videoFormats",
        profileId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userprofiles/{userprofilesId}/videoFormats
/// Lists available video formats.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dfareporting_video_formats_list_execute()` or `dfareporting_video_formats_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_video_formats_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_video_formats_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoFormatsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoFormatsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userprofiles/{userprofilesId}/videoFormats
/// Lists available video formats.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dfareporting_video_formats_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dfareporting_video_formats_list_task()`.
/// For the simplest API, use `dfareporting_video_formats_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dfareporting_video_formats_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dfareporting_video_formats_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoFormatsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dfareporting_video_formats_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dfareporting_video_formats_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingVideoFormatsListArgs {
    /// Path parameter: profileId
    pub profileId: String,
}

/// GET userprofiles/{userprofilesId}/videoFormats
/// Lists available video formats.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dfareporting_video_formats_list_builder()` + `dfareporting_video_formats_list_execute()`.
/// For task-level control, use `dfareporting_video_formats_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dfareporting_video_formats_list(
    client: &SimpleHttpClient,
    args: &DfareportingVideoFormatsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoFormatsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dfareporting_video_formats_list_builder(client, &args.profileId)?;
    dfareporting_video_formats_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for AccountActiveAdSummary
// =============================================================================

/// ResourceIdentifier implementation for AccountActiveAdSummary with DfareportingAccountActiveAdSummariesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountActiveAdSummariesGetArgs> for AccountActiveAdSummary {
    fn generate_resource_id(&self, input: &DfareportingAccountActiveAdSummariesGetArgs) -> String {
        format!(
            "gcp::dfareporting::AccountActiveAdSummary/{}/{}",
            input.profileId, input.summaryAccountId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountActiveAdSummary"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountPermissionGroup
// =============================================================================

/// ResourceIdentifier implementation for AccountPermissionGroup with DfareportingAccountPermissionGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountPermissionGroupsGetArgs> for AccountPermissionGroup {
    fn generate_resource_id(&self, input: &DfareportingAccountPermissionGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::AccountPermissionGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountPermissionGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountPermissionGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for AccountPermissionGroupsListResponse with DfareportingAccountPermissionGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountPermissionGroupsListArgs>
    for AccountPermissionGroupsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingAccountPermissionGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::AccountPermissionGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountPermissionGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountPermission
// =============================================================================

/// ResourceIdentifier implementation for AccountPermission with DfareportingAccountPermissionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountPermissionsGetArgs> for AccountPermission {
    fn generate_resource_id(&self, input: &DfareportingAccountPermissionsGetArgs) -> String {
        format!(
            "gcp::dfareporting::AccountPermission/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountPermission"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountPermissionsListResponse
// =============================================================================

/// ResourceIdentifier implementation for AccountPermissionsListResponse with DfareportingAccountPermissionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountPermissionsListArgs> for AccountPermissionsListResponse {
    fn generate_resource_id(&self, input: &DfareportingAccountPermissionsListArgs) -> String {
        format!(
            "gcp::dfareporting::AccountPermissionsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountPermissionsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountUserProfile
// =============================================================================

/// ResourceIdentifier implementation for AccountUserProfile with DfareportingAccountUserProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountUserProfilesGetArgs> for AccountUserProfile {
    fn generate_resource_id(&self, input: &DfareportingAccountUserProfilesGetArgs) -> String {
        format!(
            "gcp::dfareporting::AccountUserProfile/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountUserProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountUserProfile
// =============================================================================

/// ResourceIdentifier implementation for AccountUserProfile with DfareportingAccountUserProfilesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountUserProfilesInsertArgs> for AccountUserProfile {
    fn generate_resource_id(&self, input: &DfareportingAccountUserProfilesInsertArgs) -> String {
        format!("gcp::dfareporting::AccountUserProfile/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountUserProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountUserProfilesListResponse
// =============================================================================

/// ResourceIdentifier implementation for AccountUserProfilesListResponse with DfareportingAccountUserProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountUserProfilesListArgs>
    for AccountUserProfilesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingAccountUserProfilesListArgs) -> String {
        format!(
            "gcp::dfareporting::AccountUserProfilesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountUserProfilesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountUserProfile
// =============================================================================

/// ResourceIdentifier implementation for AccountUserProfile with DfareportingAccountUserProfilesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountUserProfilesPatchArgs> for AccountUserProfile {
    fn generate_resource_id(&self, input: &DfareportingAccountUserProfilesPatchArgs) -> String {
        format!("gcp::dfareporting::AccountUserProfile/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountUserProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountUserProfile
// =============================================================================

/// ResourceIdentifier implementation for AccountUserProfile with DfareportingAccountUserProfilesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountUserProfilesUpdateArgs> for AccountUserProfile {
    fn generate_resource_id(&self, input: &DfareportingAccountUserProfilesUpdateArgs) -> String {
        format!("gcp::dfareporting::AccountUserProfile/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountUserProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Account
// =============================================================================

/// ResourceIdentifier implementation for Account with DfareportingAccountsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountsGetArgs> for Account {
    fn generate_resource_id(&self, input: &DfareportingAccountsGetArgs) -> String {
        format!(
            "gcp::dfareporting::Account/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Account"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AccountsListResponse
// =============================================================================

/// ResourceIdentifier implementation for AccountsListResponse with DfareportingAccountsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountsListArgs> for AccountsListResponse {
    fn generate_resource_id(&self, input: &DfareportingAccountsListArgs) -> String {
        format!(
            "gcp::dfareporting::AccountsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AccountsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Account
// =============================================================================

/// ResourceIdentifier implementation for Account with DfareportingAccountsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountsPatchArgs> for Account {
    fn generate_resource_id(&self, input: &DfareportingAccountsPatchArgs) -> String {
        format!("gcp::dfareporting::Account/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Account"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Account
// =============================================================================

/// ResourceIdentifier implementation for Account with DfareportingAccountsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAccountsUpdateArgs> for Account {
    fn generate_resource_id(&self, input: &DfareportingAccountsUpdateArgs) -> String {
        format!("gcp::dfareporting::Account/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Account"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Ad
// =============================================================================

/// ResourceIdentifier implementation for Ad with DfareportingAdsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdsGetArgs> for Ad {
    fn generate_resource_id(&self, input: &DfareportingAdsGetArgs) -> String {
        format!("gcp::dfareporting::Ad/{}/{}", input.profileId, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Ad"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Ad
// =============================================================================

/// ResourceIdentifier implementation for Ad with DfareportingAdsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdsInsertArgs> for Ad {
    fn generate_resource_id(&self, input: &DfareportingAdsInsertArgs) -> String {
        format!("gcp::dfareporting::Ad/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Ad"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdsListResponse
// =============================================================================

/// ResourceIdentifier implementation for AdsListResponse with DfareportingAdsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdsListArgs> for AdsListResponse {
    fn generate_resource_id(&self, input: &DfareportingAdsListArgs) -> String {
        format!("gcp::dfareporting::AdsListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Ad
// =============================================================================

/// ResourceIdentifier implementation for Ad with DfareportingAdsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdsPatchArgs> for Ad {
    fn generate_resource_id(&self, input: &DfareportingAdsPatchArgs) -> String {
        format!("gcp::dfareporting::Ad/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Ad"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Ad
// =============================================================================

/// ResourceIdentifier implementation for Ad with DfareportingAdsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdsUpdateArgs> for Ad {
    fn generate_resource_id(&self, input: &DfareportingAdsUpdateArgs) -> String {
        format!("gcp::dfareporting::Ad/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Ad"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserGroup
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserGroup with DfareportingAdvertiserGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserGroupsGetArgs> for AdvertiserGroup {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::AdvertiserGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserGroup
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserGroup with DfareportingAdvertiserGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserGroupsInsertArgs> for AdvertiserGroup {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserGroupsInsertArgs) -> String {
        format!("gcp::dfareporting::AdvertiserGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserGroupsListResponse with DfareportingAdvertiserGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserGroupsListArgs> for AdvertiserGroupsListResponse {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::AdvertiserGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserGroup
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserGroup with DfareportingAdvertiserGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserGroupsPatchArgs> for AdvertiserGroup {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserGroupsPatchArgs) -> String {
        format!("gcp::dfareporting::AdvertiserGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserGroup
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserGroup with DfareportingAdvertiserGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserGroupsUpdateArgs> for AdvertiserGroup {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserGroupsUpdateArgs) -> String {
        format!("gcp::dfareporting::AdvertiserGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserInvoicesListResponse
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserInvoicesListResponse with DfareportingAdvertiserInvoicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserInvoicesListArgs> for AdvertiserInvoicesListResponse {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserInvoicesListArgs) -> String {
        format!(
            "gcp::dfareporting::AdvertiserInvoicesListResponse/{}/{}",
            input.profileId, input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserInvoicesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LandingPage
// =============================================================================

/// ResourceIdentifier implementation for LandingPage with DfareportingAdvertiserLandingPagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserLandingPagesGetArgs> for LandingPage {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserLandingPagesGetArgs) -> String {
        format!(
            "gcp::dfareporting::LandingPage/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::LandingPage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LandingPage
// =============================================================================

/// ResourceIdentifier implementation for LandingPage with DfareportingAdvertiserLandingPagesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserLandingPagesInsertArgs> for LandingPage {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserLandingPagesInsertArgs) -> String {
        format!("gcp::dfareporting::LandingPage/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::LandingPage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertiserLandingPagesListResponse
// =============================================================================

/// ResourceIdentifier implementation for AdvertiserLandingPagesListResponse with DfareportingAdvertiserLandingPagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserLandingPagesListArgs>
    for AdvertiserLandingPagesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingAdvertiserLandingPagesListArgs) -> String {
        format!(
            "gcp::dfareporting::AdvertiserLandingPagesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertiserLandingPagesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LandingPage
// =============================================================================

/// ResourceIdentifier implementation for LandingPage with DfareportingAdvertiserLandingPagesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserLandingPagesPatchArgs> for LandingPage {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserLandingPagesPatchArgs) -> String {
        format!("gcp::dfareporting::LandingPage/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::LandingPage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LandingPage
// =============================================================================

/// ResourceIdentifier implementation for LandingPage with DfareportingAdvertiserLandingPagesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertiserLandingPagesUpdateArgs> for LandingPage {
    fn generate_resource_id(&self, input: &DfareportingAdvertiserLandingPagesUpdateArgs) -> String {
        format!("gcp::dfareporting::LandingPage/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::LandingPage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DfareportingAdvertisersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertisersGetArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DfareportingAdvertisersGetArgs) -> String {
        format!(
            "gcp::dfareporting::Advertiser/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DfareportingAdvertisersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertisersInsertArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DfareportingAdvertisersInsertArgs) -> String {
        format!("gcp::dfareporting::Advertiser/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdvertisersListResponse
// =============================================================================

/// ResourceIdentifier implementation for AdvertisersListResponse with DfareportingAdvertisersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertisersListArgs> for AdvertisersListResponse {
    fn generate_resource_id(&self, input: &DfareportingAdvertisersListArgs) -> String {
        format!(
            "gcp::dfareporting::AdvertisersListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::AdvertisersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DfareportingAdvertisersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertisersPatchArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DfareportingAdvertisersPatchArgs) -> String {
        format!("gcp::dfareporting::Advertiser/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DfareportingAdvertisersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingAdvertisersUpdateArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DfareportingAdvertisersUpdateArgs) -> String {
        format!("gcp::dfareporting::Advertiser/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingAssignment
// =============================================================================

/// ResourceIdentifier implementation for BillingAssignment with DfareportingBillingAssignmentsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingAssignmentsInsertArgs> for BillingAssignment {
    fn generate_resource_id(&self, input: &DfareportingBillingAssignmentsInsertArgs) -> String {
        format!(
            "gcp::dfareporting::BillingAssignment/{}/{}",
            input.profileId, input.billingProfileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingAssignment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingAssignmentsListResponse
// =============================================================================

/// ResourceIdentifier implementation for BillingAssignmentsListResponse with DfareportingBillingAssignmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingAssignmentsListArgs> for BillingAssignmentsListResponse {
    fn generate_resource_id(&self, input: &DfareportingBillingAssignmentsListArgs) -> String {
        format!(
            "gcp::dfareporting::BillingAssignmentsListResponse/{}/{}",
            input.profileId, input.billingProfileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingAssignmentsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingProfile
// =============================================================================

/// ResourceIdentifier implementation for BillingProfile with DfareportingBillingProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingProfilesGetArgs> for BillingProfile {
    fn generate_resource_id(&self, input: &DfareportingBillingProfilesGetArgs) -> String {
        format!(
            "gcp::dfareporting::BillingProfile/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingProfilesListResponse
// =============================================================================

/// ResourceIdentifier implementation for BillingProfilesListResponse with DfareportingBillingProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingProfilesListArgs> for BillingProfilesListResponse {
    fn generate_resource_id(&self, input: &DfareportingBillingProfilesListArgs) -> String {
        format!(
            "gcp::dfareporting::BillingProfilesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingProfilesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingProfile
// =============================================================================

/// ResourceIdentifier implementation for BillingProfile with DfareportingBillingProfilesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingProfilesUpdateArgs> for BillingProfile {
    fn generate_resource_id(&self, input: &DfareportingBillingProfilesUpdateArgs) -> String {
        format!("gcp::dfareporting::BillingProfile/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BillingRatesListResponse
// =============================================================================

/// ResourceIdentifier implementation for BillingRatesListResponse with DfareportingBillingRatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBillingRatesListArgs> for BillingRatesListResponse {
    fn generate_resource_id(&self, input: &DfareportingBillingRatesListArgs) -> String {
        format!(
            "gcp::dfareporting::BillingRatesListResponse/{}/{}",
            input.profileId, input.billingProfileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BillingRatesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BrowsersListResponse
// =============================================================================

/// ResourceIdentifier implementation for BrowsersListResponse with DfareportingBrowsersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingBrowsersListArgs> for BrowsersListResponse {
    fn generate_resource_id(&self, input: &DfareportingBrowsersListArgs) -> String {
        format!(
            "gcp::dfareporting::BrowsersListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::BrowsersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CampaignCreativeAssociation
// =============================================================================

/// ResourceIdentifier implementation for CampaignCreativeAssociation with DfareportingCampaignCreativeAssociationsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignCreativeAssociationsInsertArgs>
    for CampaignCreativeAssociation
{
    fn generate_resource_id(
        &self,
        input: &DfareportingCampaignCreativeAssociationsInsertArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::CampaignCreativeAssociation/{}/{}",
            input.profileId, input.campaignId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CampaignCreativeAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CampaignCreativeAssociationsListResponse
// =============================================================================

/// ResourceIdentifier implementation for CampaignCreativeAssociationsListResponse with DfareportingCampaignCreativeAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignCreativeAssociationsListArgs>
    for CampaignCreativeAssociationsListResponse
{
    fn generate_resource_id(
        &self,
        input: &DfareportingCampaignCreativeAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::CampaignCreativeAssociationsListResponse/{}/{}",
            input.profileId, input.campaignId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CampaignCreativeAssociationsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DfareportingCampaignsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignsGetArgs> for Campaign {
    fn generate_resource_id(&self, input: &DfareportingCampaignsGetArgs) -> String {
        format!(
            "gcp::dfareporting::Campaign/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DfareportingCampaignsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignsInsertArgs> for Campaign {
    fn generate_resource_id(&self, input: &DfareportingCampaignsInsertArgs) -> String {
        format!("gcp::dfareporting::Campaign/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CampaignsListResponse
// =============================================================================

/// ResourceIdentifier implementation for CampaignsListResponse with DfareportingCampaignsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignsListArgs> for CampaignsListResponse {
    fn generate_resource_id(&self, input: &DfareportingCampaignsListArgs) -> String {
        format!(
            "gcp::dfareporting::CampaignsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CampaignsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DfareportingCampaignsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignsPatchArgs> for Campaign {
    fn generate_resource_id(&self, input: &DfareportingCampaignsPatchArgs) -> String {
        format!("gcp::dfareporting::Campaign/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DfareportingCampaignsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCampaignsUpdateArgs> for Campaign {
    fn generate_resource_id(&self, input: &DfareportingCampaignsUpdateArgs) -> String {
        format!("gcp::dfareporting::Campaign/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ChangeLog
// =============================================================================

/// ResourceIdentifier implementation for ChangeLog with DfareportingChangeLogsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingChangeLogsGetArgs> for ChangeLog {
    fn generate_resource_id(&self, input: &DfareportingChangeLogsGetArgs) -> String {
        format!(
            "gcp::dfareporting::ChangeLog/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ChangeLog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ChangeLogsListResponse
// =============================================================================

/// ResourceIdentifier implementation for ChangeLogsListResponse with DfareportingChangeLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingChangeLogsListArgs> for ChangeLogsListResponse {
    fn generate_resource_id(&self, input: &DfareportingChangeLogsListArgs) -> String {
        format!(
            "gcp::dfareporting::ChangeLogsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ChangeLogsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CitiesListResponse
// =============================================================================

/// ResourceIdentifier implementation for CitiesListResponse with DfareportingCitiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCitiesListArgs> for CitiesListResponse {
    fn generate_resource_id(&self, input: &DfareportingCitiesListArgs) -> String {
        format!("gcp::dfareporting::CitiesListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CitiesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConnectionType
// =============================================================================

/// ResourceIdentifier implementation for ConnectionType with DfareportingConnectionTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingConnectionTypesGetArgs> for ConnectionType {
    fn generate_resource_id(&self, input: &DfareportingConnectionTypesGetArgs) -> String {
        format!(
            "gcp::dfareporting::ConnectionType/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ConnectionType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConnectionTypesListResponse
// =============================================================================

/// ResourceIdentifier implementation for ConnectionTypesListResponse with DfareportingConnectionTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingConnectionTypesListArgs> for ConnectionTypesListResponse {
    fn generate_resource_id(&self, input: &DfareportingConnectionTypesListArgs) -> String {
        format!(
            "gcp::dfareporting::ConnectionTypesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ConnectionTypesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContentCategory
// =============================================================================

/// ResourceIdentifier implementation for ContentCategory with DfareportingContentCategoriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingContentCategoriesGetArgs> for ContentCategory {
    fn generate_resource_id(&self, input: &DfareportingContentCategoriesGetArgs) -> String {
        format!(
            "gcp::dfareporting::ContentCategory/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ContentCategory"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContentCategory
// =============================================================================

/// ResourceIdentifier implementation for ContentCategory with DfareportingContentCategoriesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingContentCategoriesInsertArgs> for ContentCategory {
    fn generate_resource_id(&self, input: &DfareportingContentCategoriesInsertArgs) -> String {
        format!("gcp::dfareporting::ContentCategory/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ContentCategory"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContentCategoriesListResponse
// =============================================================================

/// ResourceIdentifier implementation for ContentCategoriesListResponse with DfareportingContentCategoriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingContentCategoriesListArgs> for ContentCategoriesListResponse {
    fn generate_resource_id(&self, input: &DfareportingContentCategoriesListArgs) -> String {
        format!(
            "gcp::dfareporting::ContentCategoriesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ContentCategoriesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContentCategory
// =============================================================================

/// ResourceIdentifier implementation for ContentCategory with DfareportingContentCategoriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingContentCategoriesPatchArgs> for ContentCategory {
    fn generate_resource_id(&self, input: &DfareportingContentCategoriesPatchArgs) -> String {
        format!("gcp::dfareporting::ContentCategory/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ContentCategory"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContentCategory
// =============================================================================

/// ResourceIdentifier implementation for ContentCategory with DfareportingContentCategoriesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingContentCategoriesUpdateArgs> for ContentCategory {
    fn generate_resource_id(&self, input: &DfareportingContentCategoriesUpdateArgs) -> String {
        format!("gcp::dfareporting::ContentCategory/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ContentCategory"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConversionsBatchInsertResponse
// =============================================================================

/// ResourceIdentifier implementation for ConversionsBatchInsertResponse with DfareportingConversionsBatchinsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingConversionsBatchinsertArgs> for ConversionsBatchInsertResponse {
    fn generate_resource_id(&self, input: &DfareportingConversionsBatchinsertArgs) -> String {
        format!(
            "gcp::dfareporting::ConversionsBatchInsertResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ConversionsBatchInsertResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConversionsBatchUpdateResponse
// =============================================================================

/// ResourceIdentifier implementation for ConversionsBatchUpdateResponse with DfareportingConversionsBatchupdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingConversionsBatchupdateArgs> for ConversionsBatchUpdateResponse {
    fn generate_resource_id(&self, input: &DfareportingConversionsBatchupdateArgs) -> String {
        format!(
            "gcp::dfareporting::ConversionsBatchUpdateResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ConversionsBatchUpdateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Country
// =============================================================================

/// ResourceIdentifier implementation for Country with DfareportingCountriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCountriesGetArgs> for Country {
    fn generate_resource_id(&self, input: &DfareportingCountriesGetArgs) -> String {
        format!(
            "gcp::dfareporting::Country/{}/{}",
            input.profileId, input.dartId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Country"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CountriesListResponse
// =============================================================================

/// ResourceIdentifier implementation for CountriesListResponse with DfareportingCountriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCountriesListArgs> for CountriesListResponse {
    fn generate_resource_id(&self, input: &DfareportingCountriesListArgs) -> String {
        format!(
            "gcp::dfareporting::CountriesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CountriesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeAssetMetadata
// =============================================================================

/// ResourceIdentifier implementation for CreativeAssetMetadata with DfareportingCreativeAssetsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeAssetsInsertArgs> for CreativeAssetMetadata {
    fn generate_resource_id(&self, input: &DfareportingCreativeAssetsInsertArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeAssetMetadata/{}/{}",
            input.profileId, input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeAssetMetadata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldValue
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldValue with DfareportingCreativeFieldValuesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldValuesGetArgs> for CreativeFieldValue {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldValuesGetArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldValue/{}/{}/{}",
            input.profileId, input.creativeFieldId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldValue"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldValue
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldValue with DfareportingCreativeFieldValuesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldValuesInsertArgs> for CreativeFieldValue {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldValuesInsertArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldValue/{}/{}",
            input.profileId, input.creativeFieldId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldValue"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldValuesListResponse
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldValuesListResponse with DfareportingCreativeFieldValuesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldValuesListArgs>
    for CreativeFieldValuesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldValuesListArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldValuesListResponse/{}/{}",
            input.profileId, input.creativeFieldId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldValuesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldValue
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldValue with DfareportingCreativeFieldValuesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldValuesPatchArgs> for CreativeFieldValue {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldValuesPatchArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldValue/{}/{}",
            input.profileId, input.creativeFieldId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldValue"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldValue
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldValue with DfareportingCreativeFieldValuesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldValuesUpdateArgs> for CreativeFieldValue {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldValuesUpdateArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldValue/{}/{}",
            input.profileId, input.creativeFieldId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldValue"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeField
// =============================================================================

/// ResourceIdentifier implementation for CreativeField with DfareportingCreativeFieldsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldsGetArgs> for CreativeField {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldsGetArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeField/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeField"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeField
// =============================================================================

/// ResourceIdentifier implementation for CreativeField with DfareportingCreativeFieldsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldsInsertArgs> for CreativeField {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldsInsertArgs) -> String {
        format!("gcp::dfareporting::CreativeField/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeField"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeFieldsListResponse
// =============================================================================

/// ResourceIdentifier implementation for CreativeFieldsListResponse with DfareportingCreativeFieldsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldsListArgs> for CreativeFieldsListResponse {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldsListArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeFieldsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeFieldsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeField
// =============================================================================

/// ResourceIdentifier implementation for CreativeField with DfareportingCreativeFieldsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldsPatchArgs> for CreativeField {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldsPatchArgs) -> String {
        format!("gcp::dfareporting::CreativeField/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeField"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeField
// =============================================================================

/// ResourceIdentifier implementation for CreativeField with DfareportingCreativeFieldsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeFieldsUpdateArgs> for CreativeField {
    fn generate_resource_id(&self, input: &DfareportingCreativeFieldsUpdateArgs) -> String {
        format!("gcp::dfareporting::CreativeField/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeField"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeGroup
// =============================================================================

/// ResourceIdentifier implementation for CreativeGroup with DfareportingCreativeGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeGroupsGetArgs> for CreativeGroup {
    fn generate_resource_id(&self, input: &DfareportingCreativeGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeGroup
// =============================================================================

/// ResourceIdentifier implementation for CreativeGroup with DfareportingCreativeGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeGroupsInsertArgs> for CreativeGroup {
    fn generate_resource_id(&self, input: &DfareportingCreativeGroupsInsertArgs) -> String {
        format!("gcp::dfareporting::CreativeGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for CreativeGroupsListResponse with DfareportingCreativeGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeGroupsListArgs> for CreativeGroupsListResponse {
    fn generate_resource_id(&self, input: &DfareportingCreativeGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::CreativeGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeGroup
// =============================================================================

/// ResourceIdentifier implementation for CreativeGroup with DfareportingCreativeGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeGroupsPatchArgs> for CreativeGroup {
    fn generate_resource_id(&self, input: &DfareportingCreativeGroupsPatchArgs) -> String {
        format!("gcp::dfareporting::CreativeGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativeGroup
// =============================================================================

/// ResourceIdentifier implementation for CreativeGroup with DfareportingCreativeGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativeGroupsUpdateArgs> for CreativeGroup {
    fn generate_resource_id(&self, input: &DfareportingCreativeGroupsUpdateArgs) -> String {
        format!("gcp::dfareporting::CreativeGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativeGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DfareportingCreativesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativesGetArgs> for Creative {
    fn generate_resource_id(&self, input: &DfareportingCreativesGetArgs) -> String {
        format!(
            "gcp::dfareporting::Creative/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DfareportingCreativesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativesInsertArgs> for Creative {
    fn generate_resource_id(&self, input: &DfareportingCreativesInsertArgs) -> String {
        format!("gcp::dfareporting::Creative/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreativesListResponse
// =============================================================================

/// ResourceIdentifier implementation for CreativesListResponse with DfareportingCreativesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativesListArgs> for CreativesListResponse {
    fn generate_resource_id(&self, input: &DfareportingCreativesListArgs) -> String {
        format!(
            "gcp::dfareporting::CreativesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CreativesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DfareportingCreativesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativesPatchArgs> for Creative {
    fn generate_resource_id(&self, input: &DfareportingCreativesPatchArgs) -> String {
        format!("gcp::dfareporting::Creative/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DfareportingCreativesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingCreativesUpdateArgs> for Creative {
    fn generate_resource_id(&self, input: &DfareportingCreativesUpdateArgs) -> String {
        format!("gcp::dfareporting::Creative/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DimensionValueList
// =============================================================================

/// ResourceIdentifier implementation for DimensionValueList with DfareportingDimensionValuesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDimensionValuesQueryArgs> for DimensionValueList {
    fn generate_resource_id(&self, input: &DfareportingDimensionValuesQueryArgs) -> String {
        format!("gcp::dfareporting::DimensionValueList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DimensionValueList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DirectorySite
// =============================================================================

/// ResourceIdentifier implementation for DirectorySite with DfareportingDirectorySitesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDirectorySitesGetArgs> for DirectorySite {
    fn generate_resource_id(&self, input: &DfareportingDirectorySitesGetArgs) -> String {
        format!(
            "gcp::dfareporting::DirectorySite/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DirectorySite"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DirectorySite
// =============================================================================

/// ResourceIdentifier implementation for DirectorySite with DfareportingDirectorySitesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDirectorySitesInsertArgs> for DirectorySite {
    fn generate_resource_id(&self, input: &DfareportingDirectorySitesInsertArgs) -> String {
        format!("gcp::dfareporting::DirectorySite/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DirectorySite"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DirectorySitesListResponse
// =============================================================================

/// ResourceIdentifier implementation for DirectorySitesListResponse with DfareportingDirectorySitesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDirectorySitesListArgs> for DirectorySitesListResponse {
    fn generate_resource_id(&self, input: &DfareportingDirectorySitesListArgs) -> String {
        format!(
            "gcp::dfareporting::DirectorySitesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DirectorySitesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicFeed
// =============================================================================

/// ResourceIdentifier implementation for DynamicFeed with DfareportingDynamicFeedsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicFeedsGetArgs> for DynamicFeed {
    fn generate_resource_id(&self, input: &DfareportingDynamicFeedsGetArgs) -> String {
        format!("gcp::dfareporting::DynamicFeed/{}", input.dynamicFeedId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicFeed"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicFeed
// =============================================================================

/// ResourceIdentifier implementation for DynamicFeed with DfareportingDynamicFeedsRetransformArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicFeedsRetransformArgs> for DynamicFeed {
    fn generate_resource_id(&self, input: &DfareportingDynamicFeedsRetransformArgs) -> String {
        format!("gcp::dfareporting::DynamicFeed/{}", input.dynamicFeedId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicFeed"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicProfileGenerateCodeResponse
// =============================================================================

/// ResourceIdentifier implementation for DynamicProfileGenerateCodeResponse with DfareportingDynamicProfilesGenerateCodeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicProfilesGenerateCodeArgs>
    for DynamicProfileGenerateCodeResponse
{
    fn generate_resource_id(&self, input: &DfareportingDynamicProfilesGenerateCodeArgs) -> String {
        format!(
            "gcp::dfareporting::DynamicProfileGenerateCodeResponse/{}",
            input.dynamicProfileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicProfileGenerateCodeResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicProfile
// =============================================================================

/// ResourceIdentifier implementation for DynamicProfile with DfareportingDynamicProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicProfilesGetArgs> for DynamicProfile {
    fn generate_resource_id(&self, input: &DfareportingDynamicProfilesGetArgs) -> String {
        format!(
            "gcp::dfareporting::DynamicProfile/{}",
            input.dynamicProfileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicTargetingKey
// =============================================================================

/// ResourceIdentifier implementation for DynamicTargetingKey with DfareportingDynamicTargetingKeysInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicTargetingKeysInsertArgs> for DynamicTargetingKey {
    fn generate_resource_id(&self, input: &DfareportingDynamicTargetingKeysInsertArgs) -> String {
        format!("gcp::dfareporting::DynamicTargetingKey/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicTargetingKey"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DynamicTargetingKeysListResponse
// =============================================================================

/// ResourceIdentifier implementation for DynamicTargetingKeysListResponse with DfareportingDynamicTargetingKeysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingDynamicTargetingKeysListArgs>
    for DynamicTargetingKeysListResponse
{
    fn generate_resource_id(&self, input: &DfareportingDynamicTargetingKeysListArgs) -> String {
        format!(
            "gcp::dfareporting::DynamicTargetingKeysListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::DynamicTargetingKeysListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventTag
// =============================================================================

/// ResourceIdentifier implementation for EventTag with DfareportingEventTagsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingEventTagsGetArgs> for EventTag {
    fn generate_resource_id(&self, input: &DfareportingEventTagsGetArgs) -> String {
        format!(
            "gcp::dfareporting::EventTag/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::EventTag"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventTag
// =============================================================================

/// ResourceIdentifier implementation for EventTag with DfareportingEventTagsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingEventTagsInsertArgs> for EventTag {
    fn generate_resource_id(&self, input: &DfareportingEventTagsInsertArgs) -> String {
        format!("gcp::dfareporting::EventTag/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::EventTag"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventTagsListResponse
// =============================================================================

/// ResourceIdentifier implementation for EventTagsListResponse with DfareportingEventTagsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingEventTagsListArgs> for EventTagsListResponse {
    fn generate_resource_id(&self, input: &DfareportingEventTagsListArgs) -> String {
        format!(
            "gcp::dfareporting::EventTagsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::EventTagsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventTag
// =============================================================================

/// ResourceIdentifier implementation for EventTag with DfareportingEventTagsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingEventTagsPatchArgs> for EventTag {
    fn generate_resource_id(&self, input: &DfareportingEventTagsPatchArgs) -> String {
        format!("gcp::dfareporting::EventTag/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::EventTag"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EventTag
// =============================================================================

/// ResourceIdentifier implementation for EventTag with DfareportingEventTagsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingEventTagsUpdateArgs> for EventTag {
    fn generate_resource_id(&self, input: &DfareportingEventTagsUpdateArgs) -> String {
        format!("gcp::dfareporting::EventTag/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::EventTag"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for File
// =============================================================================

/// ResourceIdentifier implementation for File with DfareportingFilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFilesGetArgs> for File {
    fn generate_resource_id(&self, input: &DfareportingFilesGetArgs) -> String {
        format!(
            "gcp::dfareporting::File/{}/{}",
            input.reportId, input.fileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::File"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FileList
// =============================================================================

/// ResourceIdentifier implementation for FileList with DfareportingFilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFilesListArgs> for FileList {
    fn generate_resource_id(&self, input: &DfareportingFilesListArgs) -> String {
        format!("gcp::dfareporting::FileList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FileList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivitiesGenerateTagResponse
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivitiesGenerateTagResponse with DfareportingFloodlightActivitiesGeneratetagArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesGeneratetagArgs>
    for FloodlightActivitiesGenerateTagResponse
{
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightActivitiesGeneratetagArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivitiesGenerateTagResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivitiesGenerateTagResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivity
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivity with DfareportingFloodlightActivitiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesGetArgs> for FloodlightActivity {
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivitiesGetArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivity/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivity
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivity with DfareportingFloodlightActivitiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesInsertArgs> for FloodlightActivity {
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivitiesInsertArgs) -> String {
        format!("gcp::dfareporting::FloodlightActivity/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivitiesListResponse
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivitiesListResponse with DfareportingFloodlightActivitiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesListArgs>
    for FloodlightActivitiesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivitiesListArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivitiesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivitiesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivity
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivity with DfareportingFloodlightActivitiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesPatchArgs> for FloodlightActivity {
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivitiesPatchArgs) -> String {
        format!("gcp::dfareporting::FloodlightActivity/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivity
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivity with DfareportingFloodlightActivitiesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivitiesUpdateArgs> for FloodlightActivity {
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivitiesUpdateArgs) -> String {
        format!("gcp::dfareporting::FloodlightActivity/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivityGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivityGroup with DfareportingFloodlightActivityGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivityGroupsGetArgs> for FloodlightActivityGroup {
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivityGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivityGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivityGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivityGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivityGroup with DfareportingFloodlightActivityGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivityGroupsInsertArgs>
    for FloodlightActivityGroup
{
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightActivityGroupsInsertArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivityGroup/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivityGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivityGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivityGroupsListResponse with DfareportingFloodlightActivityGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivityGroupsListArgs>
    for FloodlightActivityGroupsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingFloodlightActivityGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivityGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivityGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivityGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivityGroup with DfareportingFloodlightActivityGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivityGroupsPatchArgs> for FloodlightActivityGroup {
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightActivityGroupsPatchArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivityGroup/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivityGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivityGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivityGroup with DfareportingFloodlightActivityGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightActivityGroupsUpdateArgs>
    for FloodlightActivityGroup
{
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightActivityGroupsUpdateArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightActivityGroup/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightActivityGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightConfiguration
// =============================================================================

/// ResourceIdentifier implementation for FloodlightConfiguration with DfareportingFloodlightConfigurationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightConfigurationsGetArgs> for FloodlightConfiguration {
    fn generate_resource_id(&self, input: &DfareportingFloodlightConfigurationsGetArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightConfiguration/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightConfigurationsListResponse
// =============================================================================

/// ResourceIdentifier implementation for FloodlightConfigurationsListResponse with DfareportingFloodlightConfigurationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightConfigurationsListArgs>
    for FloodlightConfigurationsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingFloodlightConfigurationsListArgs) -> String {
        format!(
            "gcp::dfareporting::FloodlightConfigurationsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightConfigurationsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightConfiguration
// =============================================================================

/// ResourceIdentifier implementation for FloodlightConfiguration with DfareportingFloodlightConfigurationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightConfigurationsPatchArgs> for FloodlightConfiguration {
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightConfigurationsPatchArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightConfiguration/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightConfiguration
// =============================================================================

/// ResourceIdentifier implementation for FloodlightConfiguration with DfareportingFloodlightConfigurationsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingFloodlightConfigurationsUpdateArgs>
    for FloodlightConfiguration
{
    fn generate_resource_id(
        &self,
        input: &DfareportingFloodlightConfigurationsUpdateArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::FloodlightConfiguration/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FloodlightConfiguration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LanguagesListResponse
// =============================================================================

/// ResourceIdentifier implementation for LanguagesListResponse with DfareportingLanguagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingLanguagesListArgs> for LanguagesListResponse {
    fn generate_resource_id(&self, input: &DfareportingLanguagesListArgs) -> String {
        format!(
            "gcp::dfareporting::LanguagesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::LanguagesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MetrosListResponse
// =============================================================================

/// ResourceIdentifier implementation for MetrosListResponse with DfareportingMetrosListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingMetrosListArgs> for MetrosListResponse {
    fn generate_resource_id(&self, input: &DfareportingMetrosListArgs) -> String {
        format!("gcp::dfareporting::MetrosListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::MetrosListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MobileApp
// =============================================================================

/// ResourceIdentifier implementation for MobileApp with DfareportingMobileAppsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingMobileAppsGetArgs> for MobileApp {
    fn generate_resource_id(&self, input: &DfareportingMobileAppsGetArgs) -> String {
        format!(
            "gcp::dfareporting::MobileApp/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::MobileApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MobileAppsListResponse
// =============================================================================

/// ResourceIdentifier implementation for MobileAppsListResponse with DfareportingMobileAppsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingMobileAppsListArgs> for MobileAppsListResponse {
    fn generate_resource_id(&self, input: &DfareportingMobileAppsListArgs) -> String {
        format!(
            "gcp::dfareporting::MobileAppsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::MobileAppsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MobileCarrier
// =============================================================================

/// ResourceIdentifier implementation for MobileCarrier with DfareportingMobileCarriersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingMobileCarriersGetArgs> for MobileCarrier {
    fn generate_resource_id(&self, input: &DfareportingMobileCarriersGetArgs) -> String {
        format!(
            "gcp::dfareporting::MobileCarrier/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::MobileCarrier"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MobileCarriersListResponse
// =============================================================================

/// ResourceIdentifier implementation for MobileCarriersListResponse with DfareportingMobileCarriersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingMobileCarriersListArgs> for MobileCarriersListResponse {
    fn generate_resource_id(&self, input: &DfareportingMobileCarriersListArgs) -> String {
        format!(
            "gcp::dfareporting::MobileCarriersListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::MobileCarriersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperatingSystemVersion
// =============================================================================

/// ResourceIdentifier implementation for OperatingSystemVersion with DfareportingOperatingSystemVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingOperatingSystemVersionsGetArgs> for OperatingSystemVersion {
    fn generate_resource_id(&self, input: &DfareportingOperatingSystemVersionsGetArgs) -> String {
        format!(
            "gcp::dfareporting::OperatingSystemVersion/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::OperatingSystemVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperatingSystemVersionsListResponse
// =============================================================================

/// ResourceIdentifier implementation for OperatingSystemVersionsListResponse with DfareportingOperatingSystemVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingOperatingSystemVersionsListArgs>
    for OperatingSystemVersionsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingOperatingSystemVersionsListArgs) -> String {
        format!(
            "gcp::dfareporting::OperatingSystemVersionsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::OperatingSystemVersionsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperatingSystem
// =============================================================================

/// ResourceIdentifier implementation for OperatingSystem with DfareportingOperatingSystemsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingOperatingSystemsGetArgs> for OperatingSystem {
    fn generate_resource_id(&self, input: &DfareportingOperatingSystemsGetArgs) -> String {
        format!(
            "gcp::dfareporting::OperatingSystem/{}/{}",
            input.profileId, input.dartId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::OperatingSystem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperatingSystemsListResponse
// =============================================================================

/// ResourceIdentifier implementation for OperatingSystemsListResponse with DfareportingOperatingSystemsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingOperatingSystemsListArgs> for OperatingSystemsListResponse {
    fn generate_resource_id(&self, input: &DfareportingOperatingSystemsListArgs) -> String {
        format!(
            "gcp::dfareporting::OperatingSystemsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::OperatingSystemsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementGroup
// =============================================================================

/// ResourceIdentifier implementation for PlacementGroup with DfareportingPlacementGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementGroupsGetArgs> for PlacementGroup {
    fn generate_resource_id(&self, input: &DfareportingPlacementGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementGroup
// =============================================================================

/// ResourceIdentifier implementation for PlacementGroup with DfareportingPlacementGroupsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementGroupsInsertArgs> for PlacementGroup {
    fn generate_resource_id(&self, input: &DfareportingPlacementGroupsInsertArgs) -> String {
        format!("gcp::dfareporting::PlacementGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for PlacementGroupsListResponse with DfareportingPlacementGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementGroupsListArgs> for PlacementGroupsListResponse {
    fn generate_resource_id(&self, input: &DfareportingPlacementGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementGroup
// =============================================================================

/// ResourceIdentifier implementation for PlacementGroup with DfareportingPlacementGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementGroupsPatchArgs> for PlacementGroup {
    fn generate_resource_id(&self, input: &DfareportingPlacementGroupsPatchArgs) -> String {
        format!("gcp::dfareporting::PlacementGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementGroup
// =============================================================================

/// ResourceIdentifier implementation for PlacementGroup with DfareportingPlacementGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementGroupsUpdateArgs> for PlacementGroup {
    fn generate_resource_id(&self, input: &DfareportingPlacementGroupsUpdateArgs) -> String {
        format!("gcp::dfareporting::PlacementGroup/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementStrategy
// =============================================================================

/// ResourceIdentifier implementation for PlacementStrategy with DfareportingPlacementStrategiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementStrategiesGetArgs> for PlacementStrategy {
    fn generate_resource_id(&self, input: &DfareportingPlacementStrategiesGetArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementStrategy/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementStrategy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementStrategy
// =============================================================================

/// ResourceIdentifier implementation for PlacementStrategy with DfareportingPlacementStrategiesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementStrategiesInsertArgs> for PlacementStrategy {
    fn generate_resource_id(&self, input: &DfareportingPlacementStrategiesInsertArgs) -> String {
        format!("gcp::dfareporting::PlacementStrategy/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementStrategy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementStrategiesListResponse
// =============================================================================

/// ResourceIdentifier implementation for PlacementStrategiesListResponse with DfareportingPlacementStrategiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementStrategiesListArgs>
    for PlacementStrategiesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingPlacementStrategiesListArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementStrategiesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementStrategiesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementStrategy
// =============================================================================

/// ResourceIdentifier implementation for PlacementStrategy with DfareportingPlacementStrategiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementStrategiesPatchArgs> for PlacementStrategy {
    fn generate_resource_id(&self, input: &DfareportingPlacementStrategiesPatchArgs) -> String {
        format!("gcp::dfareporting::PlacementStrategy/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementStrategy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementStrategy
// =============================================================================

/// ResourceIdentifier implementation for PlacementStrategy with DfareportingPlacementStrategiesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementStrategiesUpdateArgs> for PlacementStrategy {
    fn generate_resource_id(&self, input: &DfareportingPlacementStrategiesUpdateArgs) -> String {
        format!("gcp::dfareporting::PlacementStrategy/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementStrategy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementsGenerateTagsResponse
// =============================================================================

/// ResourceIdentifier implementation for PlacementsGenerateTagsResponse with DfareportingPlacementsGeneratetagsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsGeneratetagsArgs> for PlacementsGenerateTagsResponse {
    fn generate_resource_id(&self, input: &DfareportingPlacementsGeneratetagsArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementsGenerateTagsResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementsGenerateTagsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Placement
// =============================================================================

/// ResourceIdentifier implementation for Placement with DfareportingPlacementsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsGetArgs> for Placement {
    fn generate_resource_id(&self, input: &DfareportingPlacementsGetArgs) -> String {
        format!(
            "gcp::dfareporting::Placement/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Placement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Placement
// =============================================================================

/// ResourceIdentifier implementation for Placement with DfareportingPlacementsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsInsertArgs> for Placement {
    fn generate_resource_id(&self, input: &DfareportingPlacementsInsertArgs) -> String {
        format!("gcp::dfareporting::Placement/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Placement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlacementsListResponse
// =============================================================================

/// ResourceIdentifier implementation for PlacementsListResponse with DfareportingPlacementsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsListArgs> for PlacementsListResponse {
    fn generate_resource_id(&self, input: &DfareportingPlacementsListArgs) -> String {
        format!(
            "gcp::dfareporting::PlacementsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlacementsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Placement
// =============================================================================

/// ResourceIdentifier implementation for Placement with DfareportingPlacementsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsPatchArgs> for Placement {
    fn generate_resource_id(&self, input: &DfareportingPlacementsPatchArgs) -> String {
        format!("gcp::dfareporting::Placement/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Placement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Placement
// =============================================================================

/// ResourceIdentifier implementation for Placement with DfareportingPlacementsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlacementsUpdateArgs> for Placement {
    fn generate_resource_id(&self, input: &DfareportingPlacementsUpdateArgs) -> String {
        format!("gcp::dfareporting::Placement/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Placement"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlatformType
// =============================================================================

/// ResourceIdentifier implementation for PlatformType with DfareportingPlatformTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlatformTypesGetArgs> for PlatformType {
    fn generate_resource_id(&self, input: &DfareportingPlatformTypesGetArgs) -> String {
        format!(
            "gcp::dfareporting::PlatformType/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlatformType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PlatformTypesListResponse
// =============================================================================

/// ResourceIdentifier implementation for PlatformTypesListResponse with DfareportingPlatformTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPlatformTypesListArgs> for PlatformTypesListResponse {
    fn generate_resource_id(&self, input: &DfareportingPlatformTypesListArgs) -> String {
        format!(
            "gcp::dfareporting::PlatformTypesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PlatformTypesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostalCode
// =============================================================================

/// ResourceIdentifier implementation for PostalCode with DfareportingPostalCodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPostalCodesGetArgs> for PostalCode {
    fn generate_resource_id(&self, input: &DfareportingPostalCodesGetArgs) -> String {
        format!(
            "gcp::dfareporting::PostalCode/{}/{}",
            input.profileId, input.code
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PostalCode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostalCodesListResponse
// =============================================================================

/// ResourceIdentifier implementation for PostalCodesListResponse with DfareportingPostalCodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingPostalCodesListArgs> for PostalCodesListResponse {
    fn generate_resource_id(&self, input: &DfareportingPostalCodesListArgs) -> String {
        format!(
            "gcp::dfareporting::PostalCodesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::PostalCodesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionsListResponse
// =============================================================================

/// ResourceIdentifier implementation for RegionsListResponse with DfareportingRegionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRegionsListArgs> for RegionsListResponse {
    fn generate_resource_id(&self, input: &DfareportingRegionsListArgs) -> String {
        format!("gcp::dfareporting::RegionsListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RegionsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingListShare
// =============================================================================

/// ResourceIdentifier implementation for RemarketingListShare with DfareportingRemarketingListSharesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListSharesGetArgs> for RemarketingListShare {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListSharesGetArgs) -> String {
        format!(
            "gcp::dfareporting::RemarketingListShare/{}/{}",
            input.profileId, input.remarketingListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingListShare"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingListShare
// =============================================================================

/// ResourceIdentifier implementation for RemarketingListShare with DfareportingRemarketingListSharesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListSharesPatchArgs> for RemarketingListShare {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListSharesPatchArgs) -> String {
        format!(
            "gcp::dfareporting::RemarketingListShare/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingListShare"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingListShare
// =============================================================================

/// ResourceIdentifier implementation for RemarketingListShare with DfareportingRemarketingListSharesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListSharesUpdateArgs> for RemarketingListShare {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListSharesUpdateArgs) -> String {
        format!(
            "gcp::dfareporting::RemarketingListShare/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingListShare"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingList
// =============================================================================

/// ResourceIdentifier implementation for RemarketingList with DfareportingRemarketingListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListsGetArgs> for RemarketingList {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListsGetArgs) -> String {
        format!(
            "gcp::dfareporting::RemarketingList/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingList
// =============================================================================

/// ResourceIdentifier implementation for RemarketingList with DfareportingRemarketingListsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListsInsertArgs> for RemarketingList {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListsInsertArgs) -> String {
        format!("gcp::dfareporting::RemarketingList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingListsListResponse
// =============================================================================

/// ResourceIdentifier implementation for RemarketingListsListResponse with DfareportingRemarketingListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListsListArgs> for RemarketingListsListResponse {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListsListArgs) -> String {
        format!(
            "gcp::dfareporting::RemarketingListsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingListsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingList
// =============================================================================

/// ResourceIdentifier implementation for RemarketingList with DfareportingRemarketingListsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListsPatchArgs> for RemarketingList {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListsPatchArgs) -> String {
        format!("gcp::dfareporting::RemarketingList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemarketingList
// =============================================================================

/// ResourceIdentifier implementation for RemarketingList with DfareportingRemarketingListsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingRemarketingListsUpdateArgs> for RemarketingList {
    fn generate_resource_id(&self, input: &DfareportingRemarketingListsUpdateArgs) -> String {
        format!("gcp::dfareporting::RemarketingList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::RemarketingList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with DfareportingReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsGetArgs> for Report {
    fn generate_resource_id(&self, input: &DfareportingReportsGetArgs) -> String {
        format!(
            "gcp::dfareporting::Report/{}/{}",
            input.profileId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with DfareportingReportsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsInsertArgs> for Report {
    fn generate_resource_id(&self, input: &DfareportingReportsInsertArgs) -> String {
        format!("gcp::dfareporting::Report/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReportList
// =============================================================================

/// ResourceIdentifier implementation for ReportList with DfareportingReportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsListArgs> for ReportList {
    fn generate_resource_id(&self, input: &DfareportingReportsListArgs) -> String {
        format!("gcp::dfareporting::ReportList/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::ReportList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for File
// =============================================================================

/// ResourceIdentifier implementation for File with DfareportingReportsRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsRunArgs> for File {
    fn generate_resource_id(&self, input: &DfareportingReportsRunArgs) -> String {
        format!(
            "gcp::dfareporting::File/{}/{}",
            input.profileId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::File"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with DfareportingReportsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsUpdateArgs> for Report {
    fn generate_resource_id(&self, input: &DfareportingReportsUpdateArgs) -> String {
        format!(
            "gcp::dfareporting::Report/{}/{}",
            input.profileId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CompatibleFields
// =============================================================================

/// ResourceIdentifier implementation for CompatibleFields with DfareportingReportsCompatibleFieldsQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsCompatibleFieldsQueryArgs> for CompatibleFields {
    fn generate_resource_id(&self, input: &DfareportingReportsCompatibleFieldsQueryArgs) -> String {
        format!("gcp::dfareporting::CompatibleFields/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::CompatibleFields"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for File
// =============================================================================

/// ResourceIdentifier implementation for File with DfareportingReportsFilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsFilesGetArgs> for File {
    fn generate_resource_id(&self, input: &DfareportingReportsFilesGetArgs) -> String {
        format!(
            "gcp::dfareporting::File/{}/{}/{}",
            input.profileId, input.reportId, input.fileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::File"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FileList
// =============================================================================

/// ResourceIdentifier implementation for FileList with DfareportingReportsFilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingReportsFilesListArgs> for FileList {
    fn generate_resource_id(&self, input: &DfareportingReportsFilesListArgs) -> String {
        format!(
            "gcp::dfareporting::FileList/{}/{}",
            input.profileId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::FileList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DfareportingSitesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSitesGetArgs> for Site {
    fn generate_resource_id(&self, input: &DfareportingSitesGetArgs) -> String {
        format!("gcp::dfareporting::Site/{}/{}", input.profileId, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DfareportingSitesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSitesInsertArgs> for Site {
    fn generate_resource_id(&self, input: &DfareportingSitesInsertArgs) -> String {
        format!("gcp::dfareporting::Site/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SitesListResponse
// =============================================================================

/// ResourceIdentifier implementation for SitesListResponse with DfareportingSitesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSitesListArgs> for SitesListResponse {
    fn generate_resource_id(&self, input: &DfareportingSitesListArgs) -> String {
        format!("gcp::dfareporting::SitesListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::SitesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DfareportingSitesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSitesPatchArgs> for Site {
    fn generate_resource_id(&self, input: &DfareportingSitesPatchArgs) -> String {
        format!("gcp::dfareporting::Site/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DfareportingSitesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSitesUpdateArgs> for Site {
    fn generate_resource_id(&self, input: &DfareportingSitesUpdateArgs) -> String {
        format!("gcp::dfareporting::Site/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Size
// =============================================================================

/// ResourceIdentifier implementation for Size with DfareportingSizesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSizesGetArgs> for Size {
    fn generate_resource_id(&self, input: &DfareportingSizesGetArgs) -> String {
        format!("gcp::dfareporting::Size/{}/{}", input.profileId, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Size"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Size
// =============================================================================

/// ResourceIdentifier implementation for Size with DfareportingSizesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSizesInsertArgs> for Size {
    fn generate_resource_id(&self, input: &DfareportingSizesInsertArgs) -> String {
        format!("gcp::dfareporting::Size/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Size"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SizesListResponse
// =============================================================================

/// ResourceIdentifier implementation for SizesListResponse with DfareportingSizesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSizesListArgs> for SizesListResponse {
    fn generate_resource_id(&self, input: &DfareportingSizesListArgs) -> String {
        format!("gcp::dfareporting::SizesListResponse/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::SizesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for StudioCreative
// =============================================================================

/// ResourceIdentifier implementation for StudioCreative with DfareportingStudioCreativesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingStudioCreativesGetArgs> for StudioCreative {
    fn generate_resource_id(&self, input: &DfareportingStudioCreativesGetArgs) -> String {
        format!(
            "gcp::dfareporting::StudioCreative/{}",
            input.studioCreativeId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::StudioCreative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subaccount
// =============================================================================

/// ResourceIdentifier implementation for Subaccount with DfareportingSubaccountsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSubaccountsGetArgs> for Subaccount {
    fn generate_resource_id(&self, input: &DfareportingSubaccountsGetArgs) -> String {
        format!(
            "gcp::dfareporting::Subaccount/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Subaccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subaccount
// =============================================================================

/// ResourceIdentifier implementation for Subaccount with DfareportingSubaccountsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSubaccountsInsertArgs> for Subaccount {
    fn generate_resource_id(&self, input: &DfareportingSubaccountsInsertArgs) -> String {
        format!("gcp::dfareporting::Subaccount/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Subaccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SubaccountsListResponse
// =============================================================================

/// ResourceIdentifier implementation for SubaccountsListResponse with DfareportingSubaccountsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSubaccountsListArgs> for SubaccountsListResponse {
    fn generate_resource_id(&self, input: &DfareportingSubaccountsListArgs) -> String {
        format!(
            "gcp::dfareporting::SubaccountsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::SubaccountsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subaccount
// =============================================================================

/// ResourceIdentifier implementation for Subaccount with DfareportingSubaccountsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSubaccountsPatchArgs> for Subaccount {
    fn generate_resource_id(&self, input: &DfareportingSubaccountsPatchArgs) -> String {
        format!("gcp::dfareporting::Subaccount/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Subaccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subaccount
// =============================================================================

/// ResourceIdentifier implementation for Subaccount with DfareportingSubaccountsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingSubaccountsUpdateArgs> for Subaccount {
    fn generate_resource_id(&self, input: &DfareportingSubaccountsUpdateArgs) -> String {
        format!("gcp::dfareporting::Subaccount/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::Subaccount"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetableRemarketingList
// =============================================================================

/// ResourceIdentifier implementation for TargetableRemarketingList with DfareportingTargetableRemarketingListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetableRemarketingListsGetArgs>
    for TargetableRemarketingList
{
    fn generate_resource_id(
        &self,
        input: &DfareportingTargetableRemarketingListsGetArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::TargetableRemarketingList/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetableRemarketingList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetableRemarketingListsListResponse
// =============================================================================

/// ResourceIdentifier implementation for TargetableRemarketingListsListResponse with DfareportingTargetableRemarketingListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetableRemarketingListsListArgs>
    for TargetableRemarketingListsListResponse
{
    fn generate_resource_id(
        &self,
        input: &DfareportingTargetableRemarketingListsListArgs,
    ) -> String {
        format!(
            "gcp::dfareporting::TargetableRemarketingListsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetableRemarketingListsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingTemplate
// =============================================================================

/// ResourceIdentifier implementation for TargetingTemplate with DfareportingTargetingTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetingTemplatesGetArgs> for TargetingTemplate {
    fn generate_resource_id(&self, input: &DfareportingTargetingTemplatesGetArgs) -> String {
        format!(
            "gcp::dfareporting::TargetingTemplate/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetingTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingTemplate
// =============================================================================

/// ResourceIdentifier implementation for TargetingTemplate with DfareportingTargetingTemplatesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetingTemplatesInsertArgs> for TargetingTemplate {
    fn generate_resource_id(&self, input: &DfareportingTargetingTemplatesInsertArgs) -> String {
        format!("gcp::dfareporting::TargetingTemplate/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetingTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingTemplatesListResponse
// =============================================================================

/// ResourceIdentifier implementation for TargetingTemplatesListResponse with DfareportingTargetingTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetingTemplatesListArgs> for TargetingTemplatesListResponse {
    fn generate_resource_id(&self, input: &DfareportingTargetingTemplatesListArgs) -> String {
        format!(
            "gcp::dfareporting::TargetingTemplatesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetingTemplatesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingTemplate
// =============================================================================

/// ResourceIdentifier implementation for TargetingTemplate with DfareportingTargetingTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetingTemplatesPatchArgs> for TargetingTemplate {
    fn generate_resource_id(&self, input: &DfareportingTargetingTemplatesPatchArgs) -> String {
        format!("gcp::dfareporting::TargetingTemplate/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetingTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingTemplate
// =============================================================================

/// ResourceIdentifier implementation for TargetingTemplate with DfareportingTargetingTemplatesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTargetingTemplatesUpdateArgs> for TargetingTemplate {
    fn generate_resource_id(&self, input: &DfareportingTargetingTemplatesUpdateArgs) -> String {
        format!("gcp::dfareporting::TargetingTemplate/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TargetingTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TvCampaignDetail
// =============================================================================

/// ResourceIdentifier implementation for TvCampaignDetail with DfareportingTvCampaignDetailsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTvCampaignDetailsGetArgs> for TvCampaignDetail {
    fn generate_resource_id(&self, input: &DfareportingTvCampaignDetailsGetArgs) -> String {
        format!(
            "gcp::dfareporting::TvCampaignDetail/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TvCampaignDetail"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TvCampaignSummariesListResponse
// =============================================================================

/// ResourceIdentifier implementation for TvCampaignSummariesListResponse with DfareportingTvCampaignSummariesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingTvCampaignSummariesListArgs>
    for TvCampaignSummariesListResponse
{
    fn generate_resource_id(&self, input: &DfareportingTvCampaignSummariesListArgs) -> String {
        format!(
            "gcp::dfareporting::TvCampaignSummariesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::TvCampaignSummariesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserProfile
// =============================================================================

/// ResourceIdentifier implementation for UserProfile with DfareportingUserProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserProfilesGetArgs> for UserProfile {
    fn generate_resource_id(&self, input: &DfareportingUserProfilesGetArgs) -> String {
        format!("gcp::dfareporting::UserProfile/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRolePermissionGroup
// =============================================================================

/// ResourceIdentifier implementation for UserRolePermissionGroup with DfareportingUserRolePermissionGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolePermissionGroupsGetArgs> for UserRolePermissionGroup {
    fn generate_resource_id(&self, input: &DfareportingUserRolePermissionGroupsGetArgs) -> String {
        format!(
            "gcp::dfareporting::UserRolePermissionGroup/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRolePermissionGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRolePermissionGroupsListResponse
// =============================================================================

/// ResourceIdentifier implementation for UserRolePermissionGroupsListResponse with DfareportingUserRolePermissionGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolePermissionGroupsListArgs>
    for UserRolePermissionGroupsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingUserRolePermissionGroupsListArgs) -> String {
        format!(
            "gcp::dfareporting::UserRolePermissionGroupsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRolePermissionGroupsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRolePermission
// =============================================================================

/// ResourceIdentifier implementation for UserRolePermission with DfareportingUserRolePermissionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolePermissionsGetArgs> for UserRolePermission {
    fn generate_resource_id(&self, input: &DfareportingUserRolePermissionsGetArgs) -> String {
        format!(
            "gcp::dfareporting::UserRolePermission/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRolePermission"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRolePermissionsListResponse
// =============================================================================

/// ResourceIdentifier implementation for UserRolePermissionsListResponse with DfareportingUserRolePermissionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolePermissionsListArgs>
    for UserRolePermissionsListResponse
{
    fn generate_resource_id(&self, input: &DfareportingUserRolePermissionsListArgs) -> String {
        format!(
            "gcp::dfareporting::UserRolePermissionsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRolePermissionsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRole
// =============================================================================

/// ResourceIdentifier implementation for UserRole with DfareportingUserRolesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolesGetArgs> for UserRole {
    fn generate_resource_id(&self, input: &DfareportingUserRolesGetArgs) -> String {
        format!(
            "gcp::dfareporting::UserRole/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRole"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRole
// =============================================================================

/// ResourceIdentifier implementation for UserRole with DfareportingUserRolesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolesInsertArgs> for UserRole {
    fn generate_resource_id(&self, input: &DfareportingUserRolesInsertArgs) -> String {
        format!("gcp::dfareporting::UserRole/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRole"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRolesListResponse
// =============================================================================

/// ResourceIdentifier implementation for UserRolesListResponse with DfareportingUserRolesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolesListArgs> for UserRolesListResponse {
    fn generate_resource_id(&self, input: &DfareportingUserRolesListArgs) -> String {
        format!(
            "gcp::dfareporting::UserRolesListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRolesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRole
// =============================================================================

/// ResourceIdentifier implementation for UserRole with DfareportingUserRolesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolesPatchArgs> for UserRole {
    fn generate_resource_id(&self, input: &DfareportingUserRolesPatchArgs) -> String {
        format!("gcp::dfareporting::UserRole/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRole"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserRole
// =============================================================================

/// ResourceIdentifier implementation for UserRole with DfareportingUserRolesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingUserRolesUpdateArgs> for UserRole {
    fn generate_resource_id(&self, input: &DfareportingUserRolesUpdateArgs) -> String {
        format!("gcp::dfareporting::UserRole/{}", input.profileId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::UserRole"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VideoFormat
// =============================================================================

/// ResourceIdentifier implementation for VideoFormat with DfareportingVideoFormatsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingVideoFormatsGetArgs> for VideoFormat {
    fn generate_resource_id(&self, input: &DfareportingVideoFormatsGetArgs) -> String {
        format!(
            "gcp::dfareporting::VideoFormat/{}/{}",
            input.profileId, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::VideoFormat"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VideoFormatsListResponse
// =============================================================================

/// ResourceIdentifier implementation for VideoFormatsListResponse with DfareportingVideoFormatsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DfareportingVideoFormatsListArgs> for VideoFormatsListResponse {
    fn generate_resource_id(&self, input: &DfareportingVideoFormatsListArgs) -> String {
        format!(
            "gcp::dfareporting::VideoFormatsListResponse/{}",
            input.profileId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dfareporting::VideoFormatsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
