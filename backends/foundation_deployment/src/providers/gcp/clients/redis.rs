//! Auto-generated API clients for gcp/redis.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_get_execute()` to send, or `redis_projects_locations_get` for simplest API.

pub fn redis_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_get_execute()` or `redis_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_get_task()`.
/// For the simplest API, use `redis_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_get_builder()` + `redis_projects_locations_get_execute()`.
/// For task-level control, use `redis_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_get_builder(client, &args.name)?;
    redis_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sharedRegionalCertificateAuthority
/// Gets the details of regional certificate authority information for Redis cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_get_shared_regional_certificate_authority_execute()` to send, or `redis_projects_locations_get_shared_regional_certificate_authority` for simplest API.

pub fn redis_projects_locations_get_shared_regional_certificate_authority_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/sharedRegionalCertificateAuthority",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sharedRegionalCertificateAuthority
/// Gets the details of regional certificate authority information for Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_get_shared_regional_certificate_authority_execute()` or `redis_projects_locations_get_shared_regional_certificate_authority`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_get_shared_regional_certificate_authority_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_get_shared_regional_certificate_authority_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SharedRegionalCertificateAuthority>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SharedRegionalCertificateAuthority = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sharedRegionalCertificateAuthority
/// Gets the details of regional certificate authority information for Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_get_shared_regional_certificate_authority_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_get_shared_regional_certificate_authority_task()`.
/// For the simplest API, use `redis_projects_locations_get_shared_regional_certificate_authority()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_get_shared_regional_certificate_authority_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_get_shared_regional_certificate_authority_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SharedRegionalCertificateAuthority>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_get_shared_regional_certificate_authority_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_get_shared_regional_certificate_authority`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsGetSharedRegionalCertificateAuthorityArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sharedRegionalCertificateAuthority
/// Gets the details of regional certificate authority information for Redis cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_get_shared_regional_certificate_authority_builder()` + `redis_projects_locations_get_shared_regional_certificate_authority_execute()`.
/// For task-level control, use `redis_projects_locations_get_shared_regional_certificate_authority_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_get_shared_regional_certificate_authority(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsGetSharedRegionalCertificateAuthorityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SharedRegionalCertificateAuthority>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_get_shared_regional_certificate_authority_builder(
        client, &args.name,
    )?;
    redis_projects_locations_get_shared_regional_certificate_authority_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_list_execute()` to send, or `redis_projects_locations_list` for simplest API.

pub fn redis_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_list_execute()` or `redis_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_list_task()`.
/// For the simplest API, use `redis_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_list_builder()` + `redis_projects_locations_list_execute()`.
/// For task-level control, use `redis_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Creates an ACL Policy. The creation is executed synchronously and the policy is available for use immediately after the RPC returns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_acl_policies_create_execute()` to send, or `redis_projects_locations_acl_policies_create` for simplest API.

pub fn redis_projects_locations_acl_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    aclPolicyId: Option<&str>,
    requestId: Option<&str>,
    body: &AclPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/aclPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = aclPolicyId {
        query_parts.push(format!("aclPolicyId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Creates an ACL Policy. The creation is executed synchronously and the policy is available for use immediately after the RPC returns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_acl_policies_create_execute()` or `redis_projects_locations_acl_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Creates an ACL Policy. The creation is executed synchronously and the policy is available for use immediately after the RPC returns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_acl_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_acl_policies_create_task()`.
/// For the simplest API, use `redis_projects_locations_acl_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_acl_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_acl_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_acl_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsAclPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: aclPolicyId
    pub aclPolicyId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: AclPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Creates an ACL Policy. The creation is executed synchronously and the policy is available for use immediately after the RPC returns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_acl_policies_create_builder()` + `redis_projects_locations_acl_policies_create_execute()`.
/// For task-level control, use `redis_projects_locations_acl_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_create(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsAclPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_acl_policies_create_builder(
        client,
        &args.parent,
        args.aclPolicyId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_acl_policies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Deletes a specific Acl Policy. This action will delete the Acl Policy and all the rules associated with it. An ACL policy cannot be deleted if it is attached to a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_acl_policies_delete_execute()` to send, or `redis_projects_locations_acl_policies_delete` for simplest API.

pub fn redis_projects_locations_acl_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/aclPolicies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Deletes a specific Acl Policy. This action will delete the Acl Policy and all the rules associated with it. An ACL policy cannot be deleted if it is attached to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_acl_policies_delete_execute()` or `redis_projects_locations_acl_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Deletes a specific Acl Policy. This action will delete the Acl Policy and all the rules associated with it. An ACL policy cannot be deleted if it is attached to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_acl_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_acl_policies_delete_task()`.
/// For the simplest API, use `redis_projects_locations_acl_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_acl_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_acl_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_acl_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsAclPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Deletes a specific Acl Policy. This action will delete the Acl Policy and all the rules associated with it. An ACL policy cannot be deleted if it is attached to a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_acl_policies_delete_builder()` + `redis_projects_locations_acl_policies_delete_execute()`.
/// For task-level control, use `redis_projects_locations_acl_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsAclPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_acl_policies_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
        args.requestId.as_deref(),
    )?;
    redis_projects_locations_acl_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Gets the details of a specific Redis Cluster ACL Policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_acl_policies_get_execute()` to send, or `redis_projects_locations_acl_policies_get` for simplest API.

pub fn redis_projects_locations_acl_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/aclPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Gets the details of a specific Redis Cluster ACL Policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_acl_policies_get_execute()` or `redis_projects_locations_acl_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AclPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Gets the details of a specific Redis Cluster ACL Policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_acl_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_acl_policies_get_task()`.
/// For the simplest API, use `redis_projects_locations_acl_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_acl_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_acl_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_acl_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsAclPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Gets the details of a specific Redis Cluster ACL Policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_acl_policies_get_builder()` + `redis_projects_locations_acl_policies_get_execute()`.
/// For task-level control, use `redis_projects_locations_acl_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsAclPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AclPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_acl_policies_get_builder(client, &args.name)?;
    redis_projects_locations_acl_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Lists all ACL Policies owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_acl_policies_list_execute()` to send, or `redis_projects_locations_acl_policies_list` for simplest API.

pub fn redis_projects_locations_acl_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/aclPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Lists all ACL Policies owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_acl_policies_list_execute()` or `redis_projects_locations_acl_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAclPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAclPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Lists all ACL Policies owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_acl_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_acl_policies_list_task()`.
/// For the simplest API, use `redis_projects_locations_acl_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_acl_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAclPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_acl_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_acl_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsAclPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies
/// Lists all ACL Policies owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_acl_policies_list_builder()` + `redis_projects_locations_acl_policies_list_execute()`.
/// For task-level control, use `redis_projects_locations_acl_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsAclPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAclPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_acl_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_acl_policies_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Updates the ACL policy. The operation applies the updated ACL policy to all of the linked clusters. If Memorystore can apply the policy to all clusters, then the operation returns a SUCCESS status. If Memorystore can't apply the policy to all clusters, then to ensure eventual consistency, Memorystore uses reconciliation to apply the policy to the failed clusters. Completed longrunning.Operation will contain the new ACL Policy object in the response field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_acl_policies_patch_execute()` to send, or `redis_projects_locations_acl_policies_patch` for simplest API.

pub fn redis_projects_locations_acl_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &AclPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/aclPolicies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Updates the ACL policy. The operation applies the updated ACL policy to all of the linked clusters. If Memorystore can apply the policy to all clusters, then the operation returns a SUCCESS status. If Memorystore can't apply the policy to all clusters, then to ensure eventual consistency, Memorystore uses reconciliation to apply the policy to the failed clusters. Completed longrunning.Operation will contain the new ACL Policy object in the response field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_acl_policies_patch_execute()` or `redis_projects_locations_acl_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Updates the ACL policy. The operation applies the updated ACL policy to all of the linked clusters. If Memorystore can apply the policy to all clusters, then the operation returns a SUCCESS status. If Memorystore can't apply the policy to all clusters, then to ensure eventual consistency, Memorystore uses reconciliation to apply the policy to the failed clusters. Completed longrunning.Operation will contain the new ACL Policy object in the response field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_acl_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_acl_policies_patch_task()`.
/// For the simplest API, use `redis_projects_locations_acl_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_acl_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_acl_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_acl_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_acl_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsAclPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: AclPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclPolicies/{aclPoliciesId}
/// Updates the ACL policy. The operation applies the updated ACL policy to all of the linked clusters. If Memorystore can apply the policy to all clusters, then the operation returns a SUCCESS status. If Memorystore can't apply the policy to all clusters, then to ensure eventual consistency, Memorystore uses reconciliation to apply the policy to the failed clusters. Completed longrunning.Operation will contain the new ACL Policy object in the response field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_acl_policies_patch_builder()` + `redis_projects_locations_acl_policies_patch_execute()`.
/// For task-level control, use `redis_projects_locations_acl_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_acl_policies_patch(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsAclPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_acl_policies_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_acl_policies_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}
/// Get a backup collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_get_execute()` to send, or `redis_projects_locations_backup_collections_get` for simplest API.

pub fn redis_projects_locations_backup_collections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}
/// Get a backup collection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_get_execute()` or `redis_projects_locations_backup_collections_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BackupCollection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackupCollection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}
/// Get a backup collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_get_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupCollection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}
/// Get a backup collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_get_builder()` + `redis_projects_locations_backup_collections_get_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupCollection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_backup_collections_get_builder(client, &args.name)?;
    redis_projects_locations_backup_collections_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections
/// Lists all backup collections owned by a consumer project in either the specified location (region) or all locations. If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_list_execute()` to send, or `redis_projects_locations_backup_collections_list` for simplest API.

pub fn redis_projects_locations_backup_collections_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections
/// Lists all backup collections owned by a consumer project in either the specified location (region) or all locations. If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_list_execute()` or `redis_projects_locations_backup_collections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListBackupCollectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupCollectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections
/// Lists all backup collections owned by a consumer project in either the specified location (region) or all locations. If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_list_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBackupCollectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections
/// Lists all backup collections owned by a consumer project in either the specified location (region) or all locations. If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_list_builder()` + `redis_projects_locations_backup_collections_list_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBackupCollectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_backup_collections_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_backup_collections_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Deletes a specific backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_backups_delete_execute()` to send, or `redis_projects_locations_backup_collections_backups_delete` for simplest API.

pub fn redis_projects_locations_backup_collections_backups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections/{}/backups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Deletes a specific backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_backups_delete_execute()` or `redis_projects_locations_backup_collections_backups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Deletes a specific backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_backups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_delete_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_backups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_backups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_backups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_backups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsBackupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Deletes a specific backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_backups_delete_builder()` + `redis_projects_locations_backup_collections_backups_delete_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsBackupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_backup_collections_backups_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    redis_projects_locations_backup_collections_backups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}:export
/// Exports a specific backup to a customer target Cloud Storage URI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_backups_export_execute()` to send, or `redis_projects_locations_backup_collections_backups_export` for simplest API.

pub fn redis_projects_locations_backup_collections_backups_export_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExportBackupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections/{}/backups/{}:export",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}:export
/// Exports a specific backup to a customer target Cloud Storage URI.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_backups_export_execute()` or `redis_projects_locations_backup_collections_backups_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}:export
/// Exports a specific backup to a customer target Cloud Storage URI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_backups_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_export_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_backups_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_backups_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_backups_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_backups_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsBackupsExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExportBackupRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}:export
/// Exports a specific backup to a customer target Cloud Storage URI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_backups_export_builder()` + `redis_projects_locations_backup_collections_backups_export_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_export(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsBackupsExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_backup_collections_backups_export_builder(
        client, &args.name, &args.body,
    )?;
    redis_projects_locations_backup_collections_backups_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Gets the details of a specific backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_backups_get_execute()` to send, or `redis_projects_locations_backup_collections_backups_get` for simplest API.

pub fn redis_projects_locations_backup_collections_backups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections/{}/backups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Gets the details of a specific backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_backups_get_execute()` or `redis_projects_locations_backup_collections_backups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Gets the details of a specific backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_backups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_get_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_backups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_backups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_backups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_backups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsBackupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups/{backupsId}
/// Gets the details of a specific backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_backups_get_builder()` + `redis_projects_locations_backup_collections_backups_get_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsBackupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_backup_collections_backups_get_builder(client, &args.name)?;
    redis_projects_locations_backup_collections_backups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups
/// Lists all backups owned by a backup collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_backup_collections_backups_list_execute()` to send, or `redis_projects_locations_backup_collections_backups_list` for simplest API.

pub fn redis_projects_locations_backup_collections_backups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/backupCollections/{}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups
/// Lists all backups owned by a backup collection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_backup_collections_backups_list_execute()` or `redis_projects_locations_backup_collections_backups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups
/// Lists all backups owned by a backup collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_backup_collections_backups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_list_task()`.
/// For the simplest API, use `redis_projects_locations_backup_collections_backups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_backup_collections_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_backup_collections_backups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_backup_collections_backups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_backup_collections_backups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsBackupCollectionsBackupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupCollections/{backupCollectionsId}/backups
/// Lists all backups owned by a backup collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_backup_collections_backups_list_builder()` + `redis_projects_locations_backup_collections_backups_list_execute()`.
/// For task-level control, use `redis_projects_locations_backup_collections_backups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_backup_collections_backups_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsBackupCollectionsBackupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_backup_collections_backups_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_backup_collections_backups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser
/// Adds a token auth user for a token based auth enabled cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_add_token_auth_user_execute()` to send, or `redis_projects_locations_clusters_add_token_auth_user` for simplest API.

pub fn redis_projects_locations_clusters_add_token_auth_user_builder(
    client: &SimpleHttpClient,
    cluster: &str,
    body: &AddTokenAuthUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}:addTokenAuthUser",
        cluster,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser
/// Adds a token auth user for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_add_token_auth_user_execute()` or `redis_projects_locations_clusters_add_token_auth_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_add_token_auth_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_add_token_auth_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser
/// Adds a token auth user for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_add_token_auth_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_add_token_auth_user_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_add_token_auth_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_add_token_auth_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_add_token_auth_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_add_token_auth_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_add_token_auth_user`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersAddTokenAuthUserArgs {
    /// Path parameter: cluster
    pub cluster: String,
    /// Request body.
    pub body: AddTokenAuthUserRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser
/// Adds a token auth user for a token based auth enabled cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_add_token_auth_user_builder()` + `redis_projects_locations_clusters_add_token_auth_user_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_add_token_auth_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_add_token_auth_user(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersAddTokenAuthUserArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_add_token_auth_user_builder(
        client,
        &args.cluster,
        &args.body,
    )?;
    redis_projects_locations_clusters_add_token_auth_user_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup
/// Backup Redis Cluster. If this is the first time a backup is being created, a backup collection will be created at the backend, and this backup belongs to this collection. Both collection and backup will have a resource name. Backup will be executed for each shard. A replica (primary if `nonHA`) will be selected to perform the execution. Backup call will be rejected if there is an ongoing backup or update operation. Be aware that during preview, if the cluster's internal software version is too old, critical update will be performed before actual backup. Once the internal software version is updated to the minimum version required by the backup feature, subsequent backups will not require critical update. After preview, there will be no critical update needed for backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_backup_execute()` to send, or `redis_projects_locations_clusters_backup` for simplest API.

pub fn redis_projects_locations_clusters_backup_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &BackupClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}:backup",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup
/// Backup Redis Cluster. If this is the first time a backup is being created, a backup collection will be created at the backend, and this backup belongs to this collection. Both collection and backup will have a resource name. Backup will be executed for each shard. A replica (primary if `nonHA`) will be selected to perform the execution. Backup call will be rejected if there is an ongoing backup or update operation. Be aware that during preview, if the cluster's internal software version is too old, critical update will be performed before actual backup. Once the internal software version is updated to the minimum version required by the backup feature, subsequent backups will not require critical update. After preview, there will be no critical update needed for backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_backup_execute()` or `redis_projects_locations_clusters_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup
/// Backup Redis Cluster. If this is the first time a backup is being created, a backup collection will be created at the backend, and this backup belongs to this collection. Both collection and backup will have a resource name. Backup will be executed for each shard. A replica (primary if `nonHA`) will be selected to perform the execution. Backup call will be rejected if there is an ongoing backup or update operation. Be aware that during preview, if the cluster's internal software version is too old, critical update will be performed before actual backup. Once the internal software version is updated to the minimum version required by the backup feature, subsequent backups will not require critical update. After preview, there will be no critical update needed for backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_backup_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersBackupArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: BackupClusterRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup
/// Backup Redis Cluster. If this is the first time a backup is being created, a backup collection will be created at the backend, and this backup belongs to this collection. Both collection and backup will have a resource name. Backup will be executed for each shard. A replica (primary if `nonHA`) will be selected to perform the execution. Backup call will be rejected if there is an ongoing backup or update operation. Be aware that during preview, if the cluster's internal software version is too old, critical update will be performed before actual backup. Once the internal software version is updated to the minimum version required by the backup feature, subsequent backups will not require critical update. After preview, there will be no critical update needed for backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_backup_builder()` + `redis_projects_locations_clusters_backup_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_backup(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_backup_builder(client, &args.name, &args.body)?;
    redis_projects_locations_clusters_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a Redis cluster based on the specified properties. The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis cluster will be fully functional. The completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_create_execute()` to send, or `redis_projects_locations_clusters_create` for simplest API.

pub fn redis_projects_locations_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    clusterId: Option<&str>,
    requestId: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clusterId {
        query_parts.push(format!("clusterId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a Redis cluster based on the specified properties. The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis cluster will be fully functional. The completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_create_execute()` or `redis_projects_locations_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a Redis cluster based on the specified properties. The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis cluster will be fully functional. The completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_create_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: clusterId
    pub clusterId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a Redis cluster based on the specified properties. The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis cluster will be fully functional. The completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_create_builder()` + `redis_projects_locations_clusters_create_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_create(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_create_builder(
        client,
        &args.parent,
        args.clusterId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_clusters_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a specific Redis cluster. Cluster stops serving and data is deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_delete_execute()` to send, or `redis_projects_locations_clusters_delete` for simplest API.

pub fn redis_projects_locations_clusters_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a specific Redis cluster. Cluster stops serving and data is deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_delete_execute()` or `redis_projects_locations_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a specific Redis cluster. Cluster stops serving and data is deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_delete_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a specific Redis cluster. Cluster stops serving and data is deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_delete_builder()` + `redis_projects_locations_clusters_delete_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    redis_projects_locations_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets the details of a specific Redis cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_get_execute()` to send, or `redis_projects_locations_clusters_get` for simplest API.

pub fn redis_projects_locations_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets the details of a specific Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_get_execute()` or `redis_projects_locations_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets the details of a specific Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_get_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets the details of a specific Redis cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_get_builder()` + `redis_projects_locations_clusters_get_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_get_builder(client, &args.name)?;
    redis_projects_locations_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/certificateAuthority
/// Gets the details of certificate authority information for Redis cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_get_certificate_authority_execute()` to send, or `redis_projects_locations_clusters_get_certificate_authority` for simplest API.

pub fn redis_projects_locations_clusters_get_certificate_authority_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/certificateAuthority",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/certificateAuthority
/// Gets the details of certificate authority information for Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_get_certificate_authority_execute()` or `redis_projects_locations_clusters_get_certificate_authority`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_get_certificate_authority_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_get_certificate_authority_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<CertificateAuthority>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateAuthority = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/certificateAuthority
/// Gets the details of certificate authority information for Redis cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_get_certificate_authority_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_get_certificate_authority_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_get_certificate_authority()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_get_certificate_authority_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_get_certificate_authority_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateAuthority>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_get_certificate_authority_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_get_certificate_authority`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersGetCertificateAuthorityArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/certificateAuthority
/// Gets the details of certificate authority information for Redis cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_get_certificate_authority_builder()` + `redis_projects_locations_clusters_get_certificate_authority_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_get_certificate_authority_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_get_certificate_authority(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersGetCertificateAuthorityArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateAuthority>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_clusters_get_certificate_authority_builder(client, &args.name)?;
    redis_projects_locations_clusters_get_certificate_authority_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists all Redis clusters owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_list_execute()` to send, or `redis_projects_locations_clusters_list` for simplest API.

pub fn redis_projects_locations_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists all Redis clusters owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_list_execute()` or `redis_projects_locations_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists all Redis clusters owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_list_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists all Redis clusters owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_list_builder()` + `redis_projects_locations_clusters_list_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_clusters_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the metadata and configuration of a specific Redis cluster. Completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_patch_execute()` to send, or `redis_projects_locations_clusters_patch` for simplest API.

pub fn redis_projects_locations_clusters_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the metadata and configuration of a specific Redis cluster. Completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_patch_execute()` or `redis_projects_locations_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the metadata and configuration of a specific Redis cluster. Completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_patch_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the metadata and configuration of a specific Redis cluster. Completed longrunning.Operation will contain the new cluster object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_patch_builder()` + `redis_projects_locations_clusters_patch_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_patch(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance
/// Reschedules upcoming maintenance event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_reschedule_cluster_maintenance_execute()` to send, or `redis_projects_locations_clusters_reschedule_cluster_maintenance` for simplest API.

pub fn redis_projects_locations_clusters_reschedule_cluster_maintenance_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RescheduleClusterMaintenanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}:rescheduleClusterMaintenance",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance
/// Reschedules upcoming maintenance event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_reschedule_cluster_maintenance_execute()` or `redis_projects_locations_clusters_reschedule_cluster_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_reschedule_cluster_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_reschedule_cluster_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance
/// Reschedules upcoming maintenance event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_reschedule_cluster_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_reschedule_cluster_maintenance_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_reschedule_cluster_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_reschedule_cluster_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_reschedule_cluster_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_reschedule_cluster_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_reschedule_cluster_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersRescheduleClusterMaintenanceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RescheduleClusterMaintenanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance
/// Reschedules upcoming maintenance event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_reschedule_cluster_maintenance_builder()` + `redis_projects_locations_clusters_reschedule_cluster_maintenance_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_reschedule_cluster_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_reschedule_cluster_maintenance(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersRescheduleClusterMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_reschedule_cluster_maintenance_builder(
        client, &args.name, &args.body,
    )?;
    redis_projects_locations_clusters_reschedule_cluster_maintenance_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}:addAuthToken
/// Adds a auth token for a user of a token based auth enabled cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_add_auth_token_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_add_auth_token` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_add_auth_token_builder(
    client: &SimpleHttpClient,
    tokenAuthUser: &str,
    body: &AddAuthTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}:addAuthToken",
        tokenAuthUser,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}:addAuthToken
/// Adds a auth token for a user of a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_add_auth_token_execute()` or `redis_projects_locations_clusters_token_auth_users_add_auth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_add_auth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_add_auth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}:addAuthToken
/// Adds a auth token for a user of a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_add_auth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_add_auth_token_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_add_auth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_add_auth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_add_auth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_add_auth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_add_auth_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersAddAuthTokenArgs {
    /// Path parameter: tokenAuthUser
    pub tokenAuthUser: String,
    /// Request body.
    pub body: AddAuthTokenRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}:addAuthToken
/// Adds a auth token for a user of a token based auth enabled cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_add_auth_token_builder()` + `redis_projects_locations_clusters_token_auth_users_add_auth_token_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_add_auth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_add_auth_token(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersAddAuthTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_add_auth_token_builder(
        client,
        &args.tokenAuthUser,
        &args.body,
    )?;
    redis_projects_locations_clusters_token_auth_users_add_auth_token_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Deletes a token auth user for a token based auth enabled cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_delete_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_delete` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Deletes a token auth user for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_delete_execute()` or `redis_projects_locations_clusters_token_auth_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Deletes a token auth user for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_delete_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Deletes a token auth user for a token based auth enabled cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_delete_builder()` + `redis_projects_locations_clusters_token_auth_users_delete_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_delete_builder(
        client,
        &args.name,
        args.force,
        args.requestId.as_deref(),
    )?;
    redis_projects_locations_clusters_token_auth_users_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Gets a specific token auth user for a basic auth enabled cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_get_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_get` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Gets a specific token auth user for a basic auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_get_execute()` or `redis_projects_locations_clusters_token_auth_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TokenAuthUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TokenAuthUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Gets a specific token auth user for a basic auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_get_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TokenAuthUser>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}
/// Gets a specific token auth user for a basic auth enabled cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_get_builder()` + `redis_projects_locations_clusters_token_auth_users_get_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TokenAuthUser>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_clusters_token_auth_users_get_builder(client, &args.name)?;
    redis_projects_locations_clusters_token_auth_users_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers
/// Lists all the token auth users for a token based auth enabled cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_list_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_list` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers
/// Lists all the token auth users for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_list_execute()` or `redis_projects_locations_clusters_token_auth_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListTokenAuthUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTokenAuthUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers
/// Lists all the token auth users for a token based auth enabled cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_list_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTokenAuthUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers
/// Lists all the token auth users for a token based auth enabled cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_list_builder()` + `redis_projects_locations_clusters_token_auth_users_list_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTokenAuthUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_clusters_token_auth_users_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Removes a auth token for a user of a token based auth enabled instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}/authTokens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Removes a auth token for a user of a token based auth enabled instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_execute()` or `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Removes a auth token for a user of a token based auth enabled instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_auth_tokens_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersAuthTokensDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Removes a auth token for a user of a token based auth enabled instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder()` + `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersAuthTokensDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_builder(
        client, &args.name,
    )?;
    redis_projects_locations_clusters_token_auth_users_auth_tokens_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Gets a specific auth token for a specific token auth user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_auth_tokens_get` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}/authTokens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Gets a specific auth token for a specific token auth user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_execute()` or `redis_projects_locations_clusters_token_auth_users_auth_tokens_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AuthToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Gets a specific auth token for a specific token auth user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_auth_tokens_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_auth_tokens_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersAuthTokensGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens/{authTokensId}
/// Gets a specific auth token for a specific token auth user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder()` + `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersAuthTokensGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_auth_tokens_get_builder(
        client, &args.name,
    )?;
    redis_projects_locations_clusters_token_auth_users_auth_tokens_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens
/// Lists all the auth tokens for a specific token auth user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_execute()` to send, or `redis_projects_locations_clusters_token_auth_users_auth_tokens_list` for simplest API.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/clusters/{}/tokenAuthUsers/{}/authTokens",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens
/// Lists all the auth tokens for a specific token auth user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_execute()` or `redis_projects_locations_clusters_token_auth_users_auth_tokens_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAuthTokensResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens
/// Lists all the auth tokens for a specific token auth user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_task()`.
/// For the simplest API, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAuthTokensResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_clusters_token_auth_users_auth_tokens_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_clusters_token_auth_users_auth_tokens_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersTokenAuthUsersAuthTokensListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}/tokenAuthUsers/{tokenAuthUsersId}/authTokens
/// Lists all the auth tokens for a specific token auth user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder()` + `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_execute()`.
/// For task-level control, use `redis_projects_locations_clusters_token_auth_users_auth_tokens_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_clusters_token_auth_users_auth_tokens_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsClustersTokenAuthUsersAuthTokensListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAuthTokensResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_clusters_token_auth_users_auth_tokens_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_clusters_token_auth_users_auth_tokens_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Creates a Redis instance based on the specified tier and memory size. By default, the instance is accessible from the project's [default network](<https://cloud.google.`com/vpc/docs/vpc`>). The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis instance will be fully functional. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_create_execute()` to send, or `redis_projects_locations_instances_create` for simplest API.

pub fn redis_projects_locations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    instanceId: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = instanceId {
        query_parts.push(format!("instanceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Creates a Redis instance based on the specified tier and memory size. By default, the instance is accessible from the project's [default network](<https://cloud.google.`com/vpc/docs/vpc`>). The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis instance will be fully functional. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_create_execute()` or `redis_projects_locations_instances_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Creates a Redis instance based on the specified tier and memory size. By default, the instance is accessible from the project's [default network](<https://cloud.google.`com/vpc/docs/vpc`>). The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis instance will be fully functional. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_create_task()`.
/// For the simplest API, use `redis_projects_locations_instances_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: instanceId
    pub instanceId: Option<String>,
    /// Request body.
    pub body: Instance,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Creates a Redis instance based on the specified tier and memory size. By default, the instance is accessible from the project's [default network](<https://cloud.google.`com/vpc/docs/vpc`>). The creation is executed asynchronously and callers may check the returned operation to track its progress. Once the operation is completed the Redis instance will be fully functional. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_create_builder()` + `redis_projects_locations_instances_create_execute()`.
/// For task-level control, use `redis_projects_locations_instances_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_create(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_create_builder(
        client,
        &args.parent,
        args.instanceId.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_instances_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes a specific Redis instance. Instance stops serving and data is deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_delete_execute()` to send, or `redis_projects_locations_instances_delete` for simplest API.

pub fn redis_projects_locations_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes a specific Redis instance. Instance stops serving and data is deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_delete_execute()` or `redis_projects_locations_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes a specific Redis instance. Instance stops serving and data is deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_delete_task()`.
/// For the simplest API, use `redis_projects_locations_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes a specific Redis instance. Instance stops serving and data is deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_delete_builder()` + `redis_projects_locations_instances_delete_execute()`.
/// For task-level control, use `redis_projects_locations_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_delete_builder(client, &args.name)?;
    redis_projects_locations_instances_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export
/// Export Redis instance data into a Redis RDB format file in Cloud Storage. Redis will continue serving during this operation. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_export_execute()` to send, or `redis_projects_locations_instances_export` for simplest API.

pub fn redis_projects_locations_instances_export_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExportInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}:export",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export
/// Export Redis instance data into a Redis RDB format file in Cloud Storage. Redis will continue serving during this operation. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_export_execute()` or `redis_projects_locations_instances_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export
/// Export Redis instance data into a Redis RDB format file in Cloud Storage. Redis will continue serving during this operation. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_export_task()`.
/// For the simplest API, use `redis_projects_locations_instances_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExportInstanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export
/// Export Redis instance data into a Redis RDB format file in Cloud Storage. Redis will continue serving during this operation. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_export_builder()` + `redis_projects_locations_instances_export_execute()`.
/// For task-level control, use `redis_projects_locations_instances_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_export(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_instances_export_builder(client, &args.name, &args.body)?;
    redis_projects_locations_instances_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:failover
/// Initiates a failover of the primary node to current replica node for a specific STANDARD tier Cloud Memorystore for Redis instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_failover_execute()` to send, or `redis_projects_locations_instances_failover` for simplest API.

pub fn redis_projects_locations_instances_failover_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &FailoverInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}:failover",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:failover
/// Initiates a failover of the primary node to current replica node for a specific STANDARD tier Cloud Memorystore for Redis instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_failover_execute()` or `redis_projects_locations_instances_failover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_failover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:failover
/// Initiates a failover of the primary node to current replica node for a specific STANDARD tier Cloud Memorystore for Redis instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_failover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_failover_task()`.
/// For the simplest API, use `redis_projects_locations_instances_failover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_failover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_failover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_failover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesFailoverArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: FailoverInstanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:failover
/// Initiates a failover of the primary node to current replica node for a specific STANDARD tier Cloud Memorystore for Redis instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_failover_builder()` + `redis_projects_locations_instances_failover_execute()`.
/// For task-level control, use `redis_projects_locations_instances_failover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_failover(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesFailoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_instances_failover_builder(client, &args.name, &args.body)?;
    redis_projects_locations_instances_failover_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the details of a specific Redis instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_get_execute()` to send, or `redis_projects_locations_instances_get` for simplest API.

pub fn redis_projects_locations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the details of a specific Redis instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_get_execute()` or `redis_projects_locations_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the details of a specific Redis instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_get_task()`.
/// For the simplest API, use `redis_projects_locations_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets the details of a specific Redis instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_get_builder()` + `redis_projects_locations_instances_get_execute()`.
/// For task-level control, use `redis_projects_locations_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_get_builder(client, &args.name)?;
    redis_projects_locations_instances_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}/authString
/// Gets the AUTH string for a Redis instance. If AUTH is not enabled for the instance the response will be empty. This information is not included in the details returned to GetInstance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_get_auth_string_execute()` to send, or `redis_projects_locations_instances_get_auth_string` for simplest API.

pub fn redis_projects_locations_instances_get_auth_string_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}/authString",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}/authString
/// Gets the AUTH string for a Redis instance. If AUTH is not enabled for the instance the response will be empty. This information is not included in the details returned to GetInstance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_get_auth_string_execute()` or `redis_projects_locations_instances_get_auth_string`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_get_auth_string_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_get_auth_string_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<InstanceAuthString>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceAuthString = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}/authString
/// Gets the AUTH string for a Redis instance. If AUTH is not enabled for the instance the response will be empty. This information is not included in the details returned to GetInstance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_get_auth_string_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_get_auth_string_task()`.
/// For the simplest API, use `redis_projects_locations_instances_get_auth_string()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_get_auth_string_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_get_auth_string_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAuthString>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_get_auth_string_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_get_auth_string`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesGetAuthStringArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}/authString
/// Gets the AUTH string for a Redis instance. If AUTH is not enabled for the instance the response will be empty. This information is not included in the details returned to GetInstance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_get_auth_string_builder()` + `redis_projects_locations_instances_get_auth_string_execute()`.
/// For task-level control, use `redis_projects_locations_instances_get_auth_string_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_get_auth_string(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesGetAuthStringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAuthString>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_get_auth_string_builder(client, &args.name)?;
    redis_projects_locations_instances_get_auth_string_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import
/// Import a Redis RDB snapshot file from Cloud Storage into a Redis instance. Redis may stop serving during this operation. Instance state will be IMPORTING for entire operation. When complete, the instance will contain only data from the imported file. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_import_execute()` to send, or `redis_projects_locations_instances_import` for simplest API.

pub fn redis_projects_locations_instances_import_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ImportInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}:import",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import
/// Import a Redis RDB snapshot file from Cloud Storage into a Redis instance. Redis may stop serving during this operation. Instance state will be IMPORTING for entire operation. When complete, the instance will contain only data from the imported file. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_import_execute()` or `redis_projects_locations_instances_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import
/// Import a Redis RDB snapshot file from Cloud Storage into a Redis instance. Redis may stop serving during this operation. Instance state will be IMPORTING for entire operation. When complete, the instance will contain only data from the imported file. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_import_task()`.
/// For the simplest API, use `redis_projects_locations_instances_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesImportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ImportInstanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import
/// Import a Redis RDB snapshot file from Cloud Storage into a Redis instance. Redis may stop serving during this operation. Instance state will be IMPORTING for entire operation. When complete, the instance will contain only data from the imported file. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_import_builder()` + `redis_projects_locations_instances_import_execute()`.
/// For task-level control, use `redis_projects_locations_instances_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_import(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_instances_import_builder(client, &args.name, &args.body)?;
    redis_projects_locations_instances_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Lists all Redis instances owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_list_execute()` to send, or `redis_projects_locations_instances_list` for simplest API.

pub fn redis_projects_locations_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Lists all Redis instances owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_list_execute()` or `redis_projects_locations_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Lists all Redis instances owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_list_task()`.
/// For the simplest API, use `redis_projects_locations_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances
/// Lists all Redis instances owned by a project in either the specified location (region) or all locations. The location should have the following format: * `projects/{project_id}/locations/{location_id}` If location_id is specified as - (wildcard), then all regions available to the project are queried, and the results are aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_list_builder()` + `redis_projects_locations_instances_list_execute()`.
/// For task-level control, use `redis_projects_locations_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    redis_projects_locations_instances_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Updates the metadata and configuration of a specific Redis instance. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_patch_execute()` to send, or `redis_projects_locations_instances_patch` for simplest API.

pub fn redis_projects_locations_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Updates the metadata and configuration of a specific Redis instance. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_patch_execute()` or `redis_projects_locations_instances_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Updates the metadata and configuration of a specific Redis instance. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_patch_task()`.
/// For the simplest API, use `redis_projects_locations_instances_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Instance,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Updates the metadata and configuration of a specific Redis instance. Completed longrunning.Operation will contain the new instance object in the response field. The returned operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_patch_builder()` + `redis_projects_locations_instances_patch_execute()`.
/// For task-level control, use `redis_projects_locations_instances_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_patch(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    redis_projects_locations_instances_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rescheduleMaintenance
/// Reschedule maintenance for a given instance in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_reschedule_maintenance_execute()` to send, or `redis_projects_locations_instances_reschedule_maintenance` for simplest API.

pub fn redis_projects_locations_instances_reschedule_maintenance_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RescheduleMaintenanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}:rescheduleMaintenance",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rescheduleMaintenance
/// Reschedule maintenance for a given instance in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_reschedule_maintenance_execute()` or `redis_projects_locations_instances_reschedule_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_reschedule_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_reschedule_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rescheduleMaintenance
/// Reschedule maintenance for a given instance in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_reschedule_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_reschedule_maintenance_task()`.
/// For the simplest API, use `redis_projects_locations_instances_reschedule_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_reschedule_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_reschedule_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_reschedule_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_reschedule_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesRescheduleMaintenanceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RescheduleMaintenanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rescheduleMaintenance
/// Reschedule maintenance for a given instance in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_reschedule_maintenance_builder()` + `redis_projects_locations_instances_reschedule_maintenance_execute()`.
/// For task-level control, use `redis_projects_locations_instances_reschedule_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_reschedule_maintenance(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesRescheduleMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_instances_reschedule_maintenance_builder(
        client, &args.name, &args.body,
    )?;
    redis_projects_locations_instances_reschedule_maintenance_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:upgrade
/// Upgrades Redis instance to the newer Redis version specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_instances_upgrade_execute()` to send, or `redis_projects_locations_instances_upgrade` for simplest API.

pub fn redis_projects_locations_instances_upgrade_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpgradeInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/instances/{}:upgrade",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:upgrade
/// Upgrades Redis instance to the newer Redis version specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_instances_upgrade_execute()` or `redis_projects_locations_instances_upgrade`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_upgrade_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:upgrade
/// Upgrades Redis instance to the newer Redis version specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_instances_upgrade_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_instances_upgrade_task()`.
/// For the simplest API, use `redis_projects_locations_instances_upgrade()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_instances_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_instances_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_instances_upgrade_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_instances_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsInstancesUpgradeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UpgradeInstanceRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:upgrade
/// Upgrades Redis instance to the newer Redis version specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_instances_upgrade_builder()` + `redis_projects_locations_instances_upgrade_execute()`.
/// For task-level control, use `redis_projects_locations_instances_upgrade_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_instances_upgrade(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsInstancesUpgradeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        redis_projects_locations_instances_upgrade_builder(client, &args.name, &args.body)?;
    redis_projects_locations_instances_upgrade_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_operations_cancel_execute()` to send, or `redis_projects_locations_operations_cancel` for simplest API.

pub fn redis_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_operations_cancel_execute()` or `redis_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `redis_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_operations_cancel_builder()` + `redis_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `redis_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_operations_cancel_builder(client, &args.name)?;
    redis_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_operations_delete_execute()` to send, or `redis_projects_locations_operations_delete` for simplest API.

pub fn redis_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_operations_delete_execute()` or `redis_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_operations_delete_task()`.
/// For the simplest API, use `redis_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_operations_delete_builder()` + `redis_projects_locations_operations_delete_execute()`.
/// For task-level control, use `redis_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_operations_delete_builder(client, &args.name)?;
    redis_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_operations_get_execute()` to send, or `redis_projects_locations_operations_get` for simplest API.

pub fn redis_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_operations_get_execute()` or `redis_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_operations_get_task()`.
/// For the simplest API, use `redis_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = redis_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_operations_get_builder()` + `redis_projects_locations_operations_get_execute()`.
/// For task-level control, use `redis_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_operations_get_builder(client, &args.name)?;
    redis_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `redis_projects_locations_operations_list_execute()` to send, or `redis_projects_locations_operations_list` for simplest API.

pub fn redis_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://redis.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `redis_projects_locations_operations_list_execute()` or `redis_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `redis_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `redis_projects_locations_operations_list_task()`.
/// For the simplest API, use `redis_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `redis_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn redis_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = redis_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`redis_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `redis_projects_locations_operations_list_builder()` + `redis_projects_locations_operations_list_execute()`.
/// For task-level control, use `redis_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn redis_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &RedisProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = redis_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    redis_projects_locations_operations_list_execute(builder)
}
