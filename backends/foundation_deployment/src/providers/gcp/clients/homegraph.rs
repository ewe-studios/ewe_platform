//! Auto-generated API clients for gcp/homegraph.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// DELETE v1/agentUsers/{agentUsersId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_agent_users_delete_execute()` to send, or `homegraph_agent_users_delete` for simplest API.

pub fn homegraph_agent_users_delete_builder(
    client: &SimpleHttpClient,
    agentUserId: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://homegraph.googleapis.com/v1/agentUsers/{}",
        agentUserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/agentUsers/{agentUsersId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `homegraph_agent_users_delete_execute()` or `homegraph_agent_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_agent_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_agent_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/agentUsers/{agentUsersId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `homegraph_agent_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `homegraph_agent_users_delete_task()`.
/// For the simplest API, use `homegraph_agent_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_agent_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn homegraph_agent_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = homegraph_agent_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`homegraph_agent_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HomegraphAgentUsersDeleteArgs {
    /// Path parameter: agentUserId
    pub agentUserId: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/agentUsers/{agentUsersId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_agent_users_delete_builder()` + `homegraph_agent_users_delete_execute()`.
/// For task-level control, use `homegraph_agent_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_agent_users_delete(
    client: &SimpleHttpClient,
    args: &HomegraphAgentUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = homegraph_agent_users_delete_builder(client, &args.agentUserId, &args.requestId)?;
    homegraph_agent_users_delete_execute(builder)
}

/// POST v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_query_execute()` to send, or `homegraph_devices_query` for simplest API.

pub fn homegraph_devices_query_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://homegraph.googleapis.com/v1/devices:query",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `homegraph_devices_query_execute()` or `homegraph_devices_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `homegraph_devices_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `homegraph_devices_query_task()`.
/// For the simplest API, use `homegraph_devices_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn homegraph_devices_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = homegraph_devices_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_query_builder()` + `homegraph_devices_query_execute()`.
/// For task-level control, use `homegraph_devices_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_query(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_query_builder(client)?;
    homegraph_devices_query_execute(builder)
}

/// POST v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_report_state_and_notification_execute()` to send, or `homegraph_devices_report_state_and_notification` for simplest API.

pub fn homegraph_devices_report_state_and_notification_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://homegraph.googleapis.com/v1/devices:reportStateAndNotification",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `homegraph_devices_report_state_and_notification_execute()` or `homegraph_devices_report_state_and_notification`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_report_state_and_notification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_report_state_and_notification_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportStateAndNotificationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportStateAndNotificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `homegraph_devices_report_state_and_notification_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `homegraph_devices_report_state_and_notification_task()`.
/// For the simplest API, use `homegraph_devices_report_state_and_notification()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_report_state_and_notification_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn homegraph_devices_report_state_and_notification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportStateAndNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = homegraph_devices_report_state_and_notification_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_report_state_and_notification_builder()` + `homegraph_devices_report_state_and_notification_execute()`.
/// For task-level control, use `homegraph_devices_report_state_and_notification_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_report_state_and_notification(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportStateAndNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_report_state_and_notification_builder(client)?;
    homegraph_devices_report_state_and_notification_execute(builder)
}

/// POST v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_request_sync_execute()` to send, or `homegraph_devices_request_sync` for simplest API.

pub fn homegraph_devices_request_sync_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://homegraph.googleapis.com/v1/devices:requestSync",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `homegraph_devices_request_sync_execute()` or `homegraph_devices_request_sync`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_request_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_request_sync_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RequestSyncDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestSyncDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `homegraph_devices_request_sync_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `homegraph_devices_request_sync_task()`.
/// For the simplest API, use `homegraph_devices_request_sync()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_request_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn homegraph_devices_request_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestSyncDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = homegraph_devices_request_sync_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_request_sync_builder()` + `homegraph_devices_request_sync_execute()`.
/// For task-level control, use `homegraph_devices_request_sync_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_request_sync(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestSyncDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_request_sync_builder(client)?;
    homegraph_devices_request_sync_execute(builder)
}

/// POST v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_sync_execute()` to send, or `homegraph_devices_sync` for simplest API.

pub fn homegraph_devices_sync_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://homegraph.googleapis.com/v1/devices:sync",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `homegraph_devices_sync_execute()` or `homegraph_devices_sync`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_sync_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SyncResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SyncResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `homegraph_devices_sync_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `homegraph_devices_sync_task()`.
/// For the simplest API, use `homegraph_devices_sync()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `homegraph_devices_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn homegraph_devices_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SyncResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = homegraph_devices_sync_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_sync_builder()` + `homegraph_devices_sync_execute()`.
/// For task-level control, use `homegraph_devices_sync_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_sync(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SyncResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_sync_builder(client)?;
    homegraph_devices_sync_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HomegraphAgentUsersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HomegraphAgentUsersDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &HomegraphAgentUsersDeleteArgs) -> String {
        format!("gcp::homegraph::Empty/{}", input.agentUserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::homegraph::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryResponse with HomegraphDevicesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HomegraphDevicesQueryArgs> for QueryResponse {
    fn generate_resource_id(&self, input: &HomegraphDevicesQueryArgs) -> String {
        "gcp::homegraph::QueryResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::homegraph::QueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReportStateAndNotificationResponse
// =============================================================================

/// ResourceIdentifier implementation for ReportStateAndNotificationResponse with HomegraphDevicesReportStateAndNotificationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HomegraphDevicesReportStateAndNotificationArgs>
    for ReportStateAndNotificationResponse
{
    fn generate_resource_id(
        &self,
        input: &HomegraphDevicesReportStateAndNotificationArgs,
    ) -> String {
        "gcp::homegraph::ReportStateAndNotificationResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::homegraph::ReportStateAndNotificationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RequestSyncDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for RequestSyncDevicesResponse with HomegraphDevicesRequestSyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HomegraphDevicesRequestSyncArgs> for RequestSyncDevicesResponse {
    fn generate_resource_id(&self, input: &HomegraphDevicesRequestSyncArgs) -> String {
        "gcp::homegraph::RequestSyncDevicesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::homegraph::RequestSyncDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SyncResponse
// =============================================================================

/// ResourceIdentifier implementation for SyncResponse with HomegraphDevicesSyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HomegraphDevicesSyncArgs> for SyncResponse {
    fn generate_resource_id(&self, input: &HomegraphDevicesSyncArgs) -> String {
        "gcp::homegraph::SyncResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::homegraph::SyncResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
