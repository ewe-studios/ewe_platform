//! Auto-generated API clients for gcp/homegraph.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+agentUserId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_agent_users_delete_execute()` to send, or `homegraph_agent_users_delete` for simplest API.

pub fn homegraph_agent_users_delete_builder(
    client: &SimpleHttpClient,
    agentUserId: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+agentUserId}", agentUserId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+agentUserId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `homegraph_agent_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from homegraph_agent_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_agent_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+agentUserId}
/// Unlinks the given third-party user from your smart home Action. All data related to this user will be deleted. For more details on how users link their accounts, see [fulfillment and authentication](<https://developers.home.google.`com/cloud-to-cloud/primer/fulfillment`>). The third-party user's identity is passed in via the agent_user_id (see DeleteAgentUserRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_agent_users_delete_builder()` + `homegraph_agent_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_agent_users_delete(
    client: &SimpleHttpClient,
    agentUserId: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = homegraph_agent_users_delete_builder(client, agentUserId, requestId)?;
    homegraph_agent_users_delete_execute(builder)
}

/// GET v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_query_execute()` to send, or `homegraph_devices_query` for simplest API.

pub fn homegraph_devices_query_builder(
    client: &SimpleHttpClient,
    body: &QueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/devices:query",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `homegraph_devices_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from homegraph_devices_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices:query
/// Gets the current states in Home Graph for the given set of the third-party user's devices. The third-party user's identity is passed in via the agent_user_id (see QueryRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_query_builder()` + `homegraph_devices_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_query(
    client: &SimpleHttpClient,
    body: &QueryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_query_builder(client, body)?;
    homegraph_devices_query_execute(builder)
}

/// GET v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_report_state_and_notification_execute()` to send, or `homegraph_devices_report_state_and_notification` for simplest API.

pub fn homegraph_devices_report_state_and_notification_builder(
    client: &SimpleHttpClient,
    body: &ReportStateAndNotificationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/devices:reportStateAndNotification",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `homegraph_devices_report_state_and_notification_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from homegraph_devices_report_state_and_notification_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_report_state_and_notification_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportStateAndNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportStateAndNotificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices:reportStateAndNotification
/// Reports device state and optionally sends device notifications. Called by your smart home Action when the state of a third-party device changes or you need to send a notification about the device. See [Implement Report State](<https://developers.home.google.`com/cloud-to-cloud/integration/report-state`>) for more information. This method updates the device state according to its declared [traits](<https://developers.home.google.`com/cloud-to-cloud/primer/device-types-and-traits`>). Publishing a new state value outside of these traits will result in an INVALID_ARGUMENT error response. The third-party user's identity is passed in via the agent_user_id (see ReportStateAndNotificationRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_report_state_and_notification_builder()` + `homegraph_devices_report_state_and_notification_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_report_state_and_notification(
    client: &SimpleHttpClient,
    body: &ReportStateAndNotificationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportStateAndNotificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_report_state_and_notification_builder(client, body)?;
    homegraph_devices_report_state_and_notification_execute(builder)
}

/// GET v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_request_sync_execute()` to send, or `homegraph_devices_request_sync` for simplest API.

pub fn homegraph_devices_request_sync_builder(
    client: &SimpleHttpClient,
    body: &RequestSyncDevicesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/devices:requestSync",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `homegraph_devices_request_sync_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from homegraph_devices_request_sync_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_request_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestSyncDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestSyncDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices:requestSync
/// Requests Google to send an action.devices.SYNC [intent](<https://developers.home.google.`com/cloud-to-cloud/intents/sync`>) to your smart home Action to update device metadata for the given user. The third-party user's identity is passed via the agent_user_id (see RequestSyncDevicesRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_request_sync_builder()` + `homegraph_devices_request_sync_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_request_sync(
    client: &SimpleHttpClient,
    body: &RequestSyncDevicesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RequestSyncDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_request_sync_builder(client, body)?;
    homegraph_devices_request_sync_execute(builder)
}

/// GET v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `homegraph_devices_sync_execute()` to send, or `homegraph_devices_sync` for simplest API.

pub fn homegraph_devices_sync_builder(
    client: &SimpleHttpClient,
    body: &SyncRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/devices:sync",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `homegraph_devices_sync_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from homegraph_devices_sync_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SyncResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SyncResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/devices:sync
/// Gets all the devices associated with the given third-party user. The third-party user's identity is passed in via the agent_user_id (see SyncRequest). This request must be authorized using service account credentials from your Actions console project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `homegraph_devices_sync_builder()` + `homegraph_devices_sync_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn homegraph_devices_sync(
    client: &SimpleHttpClient,
    body: &SyncRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SyncResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = homegraph_devices_sync_builder(client, body)?;
    homegraph_devices_sync_execute(builder)
}
