//! Auto-generated API clients for gcp/biglake.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_get_iam_policy_execute()` to send, or `biglake_projects_catalogs_get_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_get_iam_policy_execute()` or `biglake_projects_catalogs_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_get_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_get_iam_policy_builder()` + `biglake_projects_catalogs_get_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    biglake_projects_catalogs_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_set_iam_policy_execute()` to send, or `biglake_projects_catalogs_set_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_set_iam_policy_execute()` or `biglake_projects_catalogs_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_set_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_set_iam_policy_builder()` + `biglake_projects_catalogs_set_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_set_iam_policy_builder(client, &args.resource)?;
    biglake_projects_catalogs_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:testIamPermissions
/// Tests the IAM permissions for the specified catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_test_iam_permissions_execute()` to send, or `biglake_projects_catalogs_test_iam_permissions` for simplest API.

pub fn biglake_projects_catalogs_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:testIamPermissions
/// Tests the IAM permissions for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_test_iam_permissions_execute()` or `biglake_projects_catalogs_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:testIamPermissions
/// Tests the IAM permissions for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_test_iam_permissions_task()`.
/// For the simplest API, use `biglake_projects_catalogs_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}:testIamPermissions
/// Tests the IAM permissions for the specified catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_test_iam_permissions_builder()` + `biglake_projects_catalogs_test_iam_permissions_execute()`.
/// For task-level control, use `biglake_projects_catalogs_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_test_iam_permissions_builder(client, &args.resource)?;
    biglake_projects_catalogs_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_get_iam_policy_execute()` to send, or `biglake_projects_catalogs_namespaces_get_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_namespaces_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_get_iam_policy_execute()` or `biglake_projects_catalogs_namespaces_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_get_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_get_iam_policy_builder()` + `biglake_projects_catalogs_namespaces_get_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_namespaces_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    biglake_projects_catalogs_namespaces_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_set_iam_policy_execute()` to send, or `biglake_projects_catalogs_namespaces_set_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_namespaces_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_set_iam_policy_execute()` or `biglake_projects_catalogs_namespaces_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_set_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_set_iam_policy_builder()` + `biglake_projects_catalogs_namespaces_set_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_catalogs_namespaces_set_iam_policy_builder(client, &args.resource)?;
    biglake_projects_catalogs_namespaces_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:testIamPermissions
/// Tests the IAM permissions for the specified namespace.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_test_iam_permissions_execute()` to send, or `biglake_projects_catalogs_namespaces_test_iam_permissions` for simplest API.

pub fn biglake_projects_catalogs_namespaces_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:testIamPermissions
/// Tests the IAM permissions for the specified namespace.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_test_iam_permissions_execute()` or `biglake_projects_catalogs_namespaces_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:testIamPermissions
/// Tests the IAM permissions for the specified namespace.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_test_iam_permissions_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}:testIamPermissions
/// Tests the IAM permissions for the specified namespace.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_test_iam_permissions_builder()` + `biglake_projects_catalogs_namespaces_test_iam_permissions_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_catalogs_namespaces_test_iam_permissions_builder(client, &args.resource)?;
    biglake_projects_catalogs_namespaces_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_execute()` to send, or `biglake_projects_catalogs_namespaces_tables_get_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_execute()` or `biglake_projects_catalogs_namespaces_tables_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_tables_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_tables_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_tables_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesTablesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:getIamPolicy
/// Gets the IAM policy for the specified Catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder()` + `biglake_projects_catalogs_namespaces_tables_get_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesTablesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_namespaces_tables_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    biglake_projects_catalogs_namespaces_tables_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_execute()` to send, or `biglake_projects_catalogs_namespaces_tables_set_iam_policy` for simplest API.

pub fn biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_execute()` or `biglake_projects_catalogs_namespaces_tables_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_tables_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_tables_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_tables_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesTablesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:setIamPolicy
/// Sets the IAM policy for the specified catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder()` + `biglake_projects_catalogs_namespaces_tables_set_iam_policy_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesTablesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_catalogs_namespaces_tables_set_iam_policy_builder(client, &args.resource)?;
    biglake_projects_catalogs_namespaces_tables_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:testIamPermissions
/// Tests the IAM permissions for the specified table.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_execute()` to send, or `biglake_projects_catalogs_namespaces_tables_test_iam_permissions` for simplest API.

pub fn biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:testIamPermissions
/// Tests the IAM permissions for the specified table.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_execute()` or `biglake_projects_catalogs_namespaces_tables_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:testIamPermissions
/// Tests the IAM permissions for the specified table.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_task()`.
/// For the simplest API, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_catalogs_namespaces_tables_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_catalogs_namespaces_tables_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_catalogs_namespaces_tables_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsCatalogsNamespacesTablesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/catalogs/{catalogsId}/namespaces/{namespacesId}/tables/{tablesId}:testIamPermissions
/// Tests the IAM permissions for the specified table.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder()` + `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_execute()`.
/// For task-level control, use `biglake_projects_catalogs_namespaces_tables_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_catalogs_namespaces_tables_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsCatalogsNamespacesTablesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = biglake_projects_catalogs_namespaces_tables_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    biglake_projects_catalogs_namespaces_tables_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Creates a new catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_create_execute()` to send, or `biglake_projects_locations_catalogs_create` for simplest API.

pub fn biglake_projects_locations_catalogs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    catalogId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = catalogId.as_ref() {
        query_parts.push(format!("catalogId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Creates a new catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_create_execute()` or `biglake_projects_locations_catalogs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Catalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Catalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Creates a new catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_create_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: catalogId
    pub catalogId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Creates a new catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_create_builder()` + `biglake_projects_locations_catalogs_create_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_create(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_locations_catalogs_create_builder(client, &args.parent, &args.catalogId)?;
    biglake_projects_locations_catalogs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Deletes an existing catalog specified by the catalog ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_delete_execute()` to send, or `biglake_projects_locations_catalogs_delete` for simplest API.

pub fn biglake_projects_locations_catalogs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Deletes an existing catalog specified by the catalog ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_delete_execute()` or `biglake_projects_locations_catalogs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Catalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Catalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Deletes an existing catalog specified by the catalog ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_delete_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Deletes an existing catalog specified by the catalog ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_delete_builder()` + `biglake_projects_locations_catalogs_delete_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_delete(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_delete_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Gets the catalog specified by the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_get_execute()` to send, or `biglake_projects_locations_catalogs_get` for simplest API.

pub fn biglake_projects_locations_catalogs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Gets the catalog specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_get_execute()` or `biglake_projects_locations_catalogs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Catalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Catalog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Gets the catalog specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_get_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Gets the catalog specified by the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_get_builder()` + `biglake_projects_locations_catalogs_get_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_get(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Catalog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_get_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// List all catalogs in a specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_list_execute()` to send, or `biglake_projects_locations_catalogs_list` for simplest API.

pub fn biglake_projects_locations_catalogs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// List all catalogs in a specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_list_execute()` or `biglake_projects_locations_catalogs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCatalogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCatalogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// List all catalogs in a specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_list_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCatalogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs
/// List all catalogs in a specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_list_builder()` + `biglake_projects_locations_catalogs_list_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_list(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCatalogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    biglake_projects_locations_catalogs_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// Creates a new database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_create_execute()` to send, or `biglake_projects_locations_catalogs_databases_create` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    databaseId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseId.as_ref() {
        query_parts.push(format!("databaseId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// Creates a new database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_create_execute()` or `biglake_projects_locations_catalogs_databases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// Creates a new database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_create_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: databaseId
    pub databaseId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// Creates a new database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_create_builder()` + `biglake_projects_locations_catalogs_databases_create_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_create(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_create_builder(
        client,
        &args.parent,
        &args.databaseId,
    )?;
    biglake_projects_locations_catalogs_databases_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Deletes an existing database specified by the database ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_delete_execute()` to send, or `biglake_projects_locations_catalogs_databases_delete` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Deletes an existing database specified by the database ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_delete_execute()` or `biglake_projects_locations_catalogs_databases_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Deletes an existing database specified by the database ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_delete_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Deletes an existing database specified by the database ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_delete_builder()` + `biglake_projects_locations_catalogs_databases_delete_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_delete(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_delete_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_databases_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Gets the database specified by the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_get_execute()` to send, or `biglake_projects_locations_catalogs_databases_get` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Gets the database specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_get_execute()` or `biglake_projects_locations_catalogs_databases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Gets the database specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_get_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Gets the database specified by the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_get_builder()` + `biglake_projects_locations_catalogs_databases_get_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_get(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_get_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_databases_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// List all databases in a specified catalog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_list_execute()` to send, or `biglake_projects_locations_catalogs_databases_list` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// List all databases in a specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_list_execute()` or `biglake_projects_locations_catalogs_databases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDatabasesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatabasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// List all databases in a specified catalog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_list_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatabasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases
/// List all databases in a specified catalog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_list_builder()` + `biglake_projects_locations_catalogs_databases_list_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_list(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatabasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    biglake_projects_locations_catalogs_databases_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Updates an existing database specified by the database ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_patch_execute()` to send, or `biglake_projects_locations_catalogs_databases_patch` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Updates an existing database specified by the database ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_patch_execute()` or `biglake_projects_locations_catalogs_databases_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Updates an existing database specified by the database ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_patch_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}
/// Updates an existing database specified by the database ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_patch_builder()` + `biglake_projects_locations_catalogs_databases_patch_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_patch(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    biglake_projects_locations_catalogs_databases_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// Creates a new table.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_create_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_create` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    tableId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tableId.as_ref() {
        query_parts.push(format!("tableId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// Creates a new table.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_create_execute()` or `biglake_projects_locations_catalogs_databases_tables_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// Creates a new table.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_create_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: tableId
    pub tableId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// Creates a new table.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_create_builder()` + `biglake_projects_locations_catalogs_databases_tables_create_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_create(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_tables_create_builder(
        client,
        &args.parent,
        &args.tableId,
    )?;
    biglake_projects_locations_catalogs_databases_tables_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Deletes an existing table specified by the table ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_delete_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_delete` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Deletes an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_delete_execute()` or `biglake_projects_locations_catalogs_databases_tables_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Deletes an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_delete_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Deletes an existing table specified by the table ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_delete_builder()` + `biglake_projects_locations_catalogs_databases_tables_delete_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_delete(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_locations_catalogs_databases_tables_delete_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_databases_tables_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Gets the table specified by the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_get_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_get` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Gets the table specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_get_execute()` or `biglake_projects_locations_catalogs_databases_tables_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Gets the table specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_get_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Gets the table specified by the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_get_builder()` + `biglake_projects_locations_catalogs_databases_tables_get_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_get(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_locations_catalogs_databases_tables_get_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_databases_tables_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// List all tables in a specified database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_list_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_list` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// List all tables in a specified database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_list_execute()` or `biglake_projects_locations_catalogs_databases_tables_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTablesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTablesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// List all tables in a specified database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_list_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTablesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables
/// List all tables in a specified database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_list_builder()` + `biglake_projects_locations_catalogs_databases_tables_list_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_list(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTablesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_tables_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    biglake_projects_locations_catalogs_databases_tables_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Updates an existing table specified by the table ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_patch_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_patch` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Updates an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_patch_execute()` or `biglake_projects_locations_catalogs_databases_tables_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Updates an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_patch_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}
/// Updates an existing table specified by the table ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_patch_builder()` + `biglake_projects_locations_catalogs_databases_tables_patch_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_patch(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = biglake_projects_locations_catalogs_databases_tables_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    biglake_projects_locations_catalogs_databases_tables_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}:rename
/// Renames an existing table specified by the table ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `biglake_projects_locations_catalogs_databases_tables_rename_execute()` to send, or `biglake_projects_locations_catalogs_databases_tables_rename` for simplest API.

pub fn biglake_projects_locations_catalogs_databases_tables_rename_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://biglake.googleapis.com/v1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}:rename",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}:rename
/// Renames an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `biglake_projects_locations_catalogs_databases_tables_rename_execute()` or `biglake_projects_locations_catalogs_databases_tables_rename`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_rename_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Table = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}:rename
/// Renames an existing table specified by the table ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `biglake_projects_locations_catalogs_databases_tables_rename_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_rename_task()`.
/// For the simplest API, use `biglake_projects_locations_catalogs_databases_tables_rename()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `biglake_projects_locations_catalogs_databases_tables_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn biglake_projects_locations_catalogs_databases_tables_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = biglake_projects_locations_catalogs_databases_tables_rename_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`biglake_projects_locations_catalogs_databases_tables_rename`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BiglakeProjectsLocationsCatalogsDatabasesTablesRenameArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/databases/{databasesId}/tables/{tablesId}:rename
/// Renames an existing table specified by the table ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `biglake_projects_locations_catalogs_databases_tables_rename_builder()` + `biglake_projects_locations_catalogs_databases_tables_rename_execute()`.
/// For task-level control, use `biglake_projects_locations_catalogs_databases_tables_rename_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn biglake_projects_locations_catalogs_databases_tables_rename(
    client: &SimpleHttpClient,
    args: &BiglakeProjectsLocationsCatalogsDatabasesTablesRenameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Table>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        biglake_projects_locations_catalogs_databases_tables_rename_builder(client, &args.name)?;
    biglake_projects_locations_catalogs_databases_tables_rename_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &BiglakeProjectsCatalogsGetIamPolicyArgs) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &BiglakeProjectsCatalogsSetIamPolicyArgs) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BiglakeProjectsCatalogsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::biglake::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsNamespacesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsNamespacesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BiglakeProjectsCatalogsNamespacesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::biglake::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsNamespacesTablesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesTablesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesTablesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BiglakeProjectsCatalogsNamespacesTablesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesTablesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesTablesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::biglake::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BiglakeProjectsCatalogsNamespacesTablesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsCatalogsNamespacesTablesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsCatalogsNamespacesTablesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::biglake::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Catalog
// =============================================================================

/// ResourceIdentifier implementation for Catalog with BiglakeProjectsLocationsCatalogsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsCreateArgs> for Catalog {
    fn generate_resource_id(&self, input: &BiglakeProjectsLocationsCatalogsCreateArgs) -> String {
        format!("gcp::biglake::Catalog/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Catalog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Catalog
// =============================================================================

/// ResourceIdentifier implementation for Catalog with BiglakeProjectsLocationsCatalogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDeleteArgs> for Catalog {
    fn generate_resource_id(&self, input: &BiglakeProjectsLocationsCatalogsDeleteArgs) -> String {
        format!("gcp::biglake::Catalog/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Catalog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Catalog
// =============================================================================

/// ResourceIdentifier implementation for Catalog with BiglakeProjectsLocationsCatalogsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsGetArgs> for Catalog {
    fn generate_resource_id(&self, input: &BiglakeProjectsLocationsCatalogsGetArgs) -> String {
        format!("gcp::biglake::Catalog/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Catalog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCatalogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCatalogsResponse with BiglakeProjectsLocationsCatalogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsListArgs> for ListCatalogsResponse {
    fn generate_resource_id(&self, input: &BiglakeProjectsLocationsCatalogsListArgs) -> String {
        format!("gcp::biglake::ListCatalogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::ListCatalogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Database
// =============================================================================

/// ResourceIdentifier implementation for Database with BiglakeProjectsLocationsCatalogsDatabasesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesCreateArgs> for Database {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesCreateArgs,
    ) -> String {
        format!("gcp::biglake::Database/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Database
// =============================================================================

/// ResourceIdentifier implementation for Database with BiglakeProjectsLocationsCatalogsDatabasesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesDeleteArgs> for Database {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesDeleteArgs,
    ) -> String {
        format!("gcp::biglake::Database/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Database
// =============================================================================

/// ResourceIdentifier implementation for Database with BiglakeProjectsLocationsCatalogsDatabasesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesGetArgs> for Database {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesGetArgs,
    ) -> String {
        format!("gcp::biglake::Database/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDatabasesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDatabasesResponse with BiglakeProjectsLocationsCatalogsDatabasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesListArgs>
    for ListDatabasesResponse
{
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesListArgs,
    ) -> String {
        format!("gcp::biglake::ListDatabasesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::ListDatabasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Database
// =============================================================================

/// ResourceIdentifier implementation for Database with BiglakeProjectsLocationsCatalogsDatabasesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesPatchArgs> for Database {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesPatchArgs,
    ) -> String {
        format!("gcp::biglake::Database/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Table
// =============================================================================

/// ResourceIdentifier implementation for Table with BiglakeProjectsLocationsCatalogsDatabasesTablesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesCreateArgs> for Table {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesCreateArgs,
    ) -> String {
        format!("gcp::biglake::Table/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Table"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Table
// =============================================================================

/// ResourceIdentifier implementation for Table with BiglakeProjectsLocationsCatalogsDatabasesTablesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesDeleteArgs> for Table {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesDeleteArgs,
    ) -> String {
        format!("gcp::biglake::Table/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Table"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Table
// =============================================================================

/// ResourceIdentifier implementation for Table with BiglakeProjectsLocationsCatalogsDatabasesTablesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesGetArgs> for Table {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesGetArgs,
    ) -> String {
        format!("gcp::biglake::Table/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Table"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTablesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTablesResponse with BiglakeProjectsLocationsCatalogsDatabasesTablesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesListArgs>
    for ListTablesResponse
{
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesListArgs,
    ) -> String {
        format!("gcp::biglake::ListTablesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::ListTablesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Table
// =============================================================================

/// ResourceIdentifier implementation for Table with BiglakeProjectsLocationsCatalogsDatabasesTablesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesPatchArgs> for Table {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesPatchArgs,
    ) -> String {
        format!("gcp::biglake::Table/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Table"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Table
// =============================================================================

/// ResourceIdentifier implementation for Table with BiglakeProjectsLocationsCatalogsDatabasesTablesRenameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BiglakeProjectsLocationsCatalogsDatabasesTablesRenameArgs> for Table {
    fn generate_resource_id(
        &self,
        input: &BiglakeProjectsLocationsCatalogsDatabasesTablesRenameArgs,
    ) -> String {
        format!("gcp::biglake::Table/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::biglake::Table"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
