//! Auto-generated API clients for Google Cloud Platform - firebaseappcheck.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Returns a public JWK set as specified by [RFC 7517](<https://tools.ietf.`org/html/rfc7517`>) that can be used to verify App Check tokens. Exactly one of the public keys in the returned set will successfully validate any App Check token that is currently valid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_jwks_get_execute()` to send, or `firebaseappcheck_jwks_get` for simplest API.

pub fn firebaseappcheck_jwks_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a public JWK set as specified by [RFC 7517](<https://tools.ietf.`org/html/rfc7517`>) that can be used to verify App Check tokens. Exactly one of the public keys in the returned set will successfully validate any App Check token that is currently valid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_jwks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_jwks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_jwks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PublicJwkSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1PublicJwkSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a public JWK set as specified by [RFC 7517](<https://tools.ietf.`org/html/rfc7517`>) that can be used to verify App Check tokens. Exactly one of the public keys in the returned set will successfully validate any App Check token that is currently valid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_jwks_get_builder()` + `firebaseappcheck_jwks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_jwks_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PublicJwkSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_jwks_get_builder(client, name)?;
    firebaseappcheck_jwks_get_execute(builder)
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_app_attest_assertion` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeAppAttestAssertion",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()` + `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder(client, app, body)?;
    firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute(builder)
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_app_attest_attestation` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeAppAttestAttestation",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()` + `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder(client, app, body)?;
    firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute(builder)
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_debug_token_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_debug_token` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+app}:exchangeDebugToken", app,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_debug_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_oauth_clients_exchange_debug_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_debug_token_builder()` + `firebaseappcheck_oauth_clients_exchange_debug_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_oauth_clients_exchange_debug_token_builder(client, app, body)?;
    firebaseappcheck_oauth_clients_exchange_debug_token_execute(builder)
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute()` to send, or `firebaseappcheck_oauth_clients_generate_app_attest_challenge` for simplest API.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:generateAppAttestChallenge",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()` + `firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder(client, app, body)?;
    firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute(builder)
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_app_attest_assertion_execute()` to send, or `firebaseappcheck_projects_apps_exchange_app_attest_assertion` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_assertion_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeAppAttestAssertion",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_app_attest_assertion_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_app_attest_assertion_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_assertion_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_app_attest_assertion_builder()` + `firebaseappcheck_projects_apps_exchange_app_attest_assertion_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_assertion(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_app_attest_assertion_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_app_attest_assertion_execute(builder)
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_app_attest_attestation_execute()` to send, or `firebaseappcheck_projects_apps_exchange_app_attest_attestation` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_attestation_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeAppAttestAttestation",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_app_attest_attestation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_app_attest_attestation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_attestation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_app_attest_attestation_builder()` + `firebaseappcheck_projects_apps_exchange_app_attest_attestation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_app_attest_attestation(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_app_attest_attestation_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_app_attest_attestation_execute(builder)
}

/// GET v1/{+app}:exchangeCustomToken
/// Validates a custom token signed using your project's Admin SDK service account credentials. If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_custom_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_custom_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_custom_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeCustomTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+app}:exchangeCustomToken", app,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeCustomToken
/// Validates a custom token signed using your project's Admin SDK service account credentials. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_custom_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_custom_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_custom_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeCustomToken
/// Validates a custom token signed using your project's Admin SDK service account credentials. If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_custom_token_builder()` + `firebaseappcheck_projects_apps_exchange_custom_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_custom_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeCustomTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_exchange_custom_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_custom_token_execute(builder)
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_debug_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_debug_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_debug_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+app}:exchangeDebugToken", app,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_debug_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_debug_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_debug_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_debug_token_builder()` + `firebaseappcheck_projects_apps_exchange_debug_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_debug_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_exchange_debug_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_debug_token_execute(builder)
}

/// GET v1/{+app}:exchangeDeviceCheckToken
/// Accepts a [device_token](<https://developer.apple.`com/documentation/devicecheck/dcdevice`>) issued by DeviceCheck, and attempts to validate it with Apple. If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_device_check_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_device_check_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_device_check_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDeviceCheckTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeDeviceCheckToken",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeDeviceCheckToken
/// Accepts a [device_token](<https://developer.apple.`com/documentation/devicecheck/dcdevice`>) issued by DeviceCheck, and attempts to validate it with Apple. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_device_check_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_device_check_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_device_check_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeDeviceCheckToken
/// Accepts a [device_token](<https://developer.apple.`com/documentation/devicecheck/dcdevice`>) issued by DeviceCheck, and attempts to validate it with Apple. If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_device_check_token_builder()` + `firebaseappcheck_projects_apps_exchange_device_check_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_device_check_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeDeviceCheckTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_device_check_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_device_check_token_execute(builder)
}

/// GET v1/{+app}:exchangePlayIntegrityToken
/// Validates an [integrity verdict response token from Play Integrity](<https://developer.android.`com/google/play/integrity/verdict`#decrypt-verify>). If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_play_integrity_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_play_integrity_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_play_integrity_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangePlayIntegrityTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangePlayIntegrityToken",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangePlayIntegrityToken
/// Validates an [integrity verdict response token from Play Integrity](<https://developer.android.`com/google/play/integrity/verdict`#decrypt-verify>). If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_play_integrity_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_play_integrity_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_play_integrity_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangePlayIntegrityToken
/// Validates an [integrity verdict response token from Play Integrity](<https://developer.android.`com/google/play/integrity/verdict`#decrypt-verify>). If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_play_integrity_token_builder()` + `firebaseappcheck_projects_apps_exchange_play_integrity_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_play_integrity_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangePlayIntegrityTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_play_integrity_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_play_integrity_token_execute(builder)
}

/// GET v1/{+app}:exchangeRecaptchaEnterpriseToken
/// Validates a [`reCAPTCHA` Enterprise response token](<https://cloud.google.`com/recaptcha-enterprise/docs/create-assessment`#retrieve_token>). If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeRecaptchaEnterpriseTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeRecaptchaEnterpriseToken",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeRecaptchaEnterpriseToken
/// Validates a [`reCAPTCHA` Enterprise response token](<https://cloud.google.`com/recaptcha-enterprise/docs/create-assessment`#retrieve_token>). If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeRecaptchaEnterpriseToken
/// Validates a [`reCAPTCHA` Enterprise response token](<https://cloud.google.`com/recaptcha-enterprise/docs/create-assessment`#retrieve_token>). If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_builder()` + `firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeRecaptchaEnterpriseTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_builder(
        client, app, body,
    )?;
    firebaseappcheck_projects_apps_exchange_recaptcha_enterprise_token_execute(builder)
}

/// GET v1/{+app}:exchangeRecaptchaV3Token
/// Validates a [`reCAPTCHA` v3 response token](<https://developers.google.`com/recaptcha/docs/v3`>). If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_recaptcha_v3_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeRecaptchaV3TokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeRecaptchaV3Token",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeRecaptchaV3Token
/// Validates a [`reCAPTCHA` v3 response token](<https://developers.google.`com/recaptcha/docs/v3`>). If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeRecaptchaV3Token
/// Validates a [`reCAPTCHA` v3 response token](<https://developers.google.`com/recaptcha/docs/v3`>). If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_builder()` + `firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_recaptcha_v3_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeRecaptchaV3TokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_recaptcha_v3_token_execute(builder)
}

/// GET v1/{+app}:exchangeSafetyNetToken
/// Validates a [SafetyNet token](<https://developer.android.`com/training/safetynet/attestation`#request-attestation-step>). If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_exchange_safety_net_token_execute()` to send, or `firebaseappcheck_projects_apps_exchange_safety_net_token` for simplest API.

pub fn firebaseappcheck_projects_apps_exchange_safety_net_token_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeSafetyNetTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:exchangeSafetyNetToken",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:exchangeSafetyNetToken
/// Validates a [SafetyNet token](<https://developer.android.`com/training/safetynet/attestation`#request-attestation-step>). If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_exchange_safety_net_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_exchange_safety_net_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_safety_net_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:exchangeSafetyNetToken
/// Validates a [SafetyNet token](<https://developer.android.`com/training/safetynet/attestation`#request-attestation-step>). If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_exchange_safety_net_token_builder()` + `firebaseappcheck_projects_apps_exchange_safety_net_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_exchange_safety_net_token(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1ExchangeSafetyNetTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_exchange_safety_net_token_builder(client, app, body)?;
    firebaseappcheck_projects_apps_exchange_safety_net_token_execute(builder)
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_generate_app_attest_challenge_execute()` to send, or `firebaseappcheck_projects_apps_generate_app_attest_challenge` for simplest API.

pub fn firebaseappcheck_projects_apps_generate_app_attest_challenge_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:generateAppAttestChallenge",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_generate_app_attest_challenge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_generate_app_attest_challenge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_generate_app_attest_challenge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_generate_app_attest_challenge_builder()` + `firebaseappcheck_projects_apps_generate_app_attest_challenge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_generate_app_attest_challenge(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_generate_app_attest_challenge_builder(client, app, body)?;
    firebaseappcheck_projects_apps_generate_app_attest_challenge_execute(builder)
}

/// GET v1/{+app}:generatePlayIntegrityChallenge
/// Generates a challenge that protects the integrity of an immediately following integrity verdict request to the Play Integrity API. The next call to ExchangePlayIntegrityToken using the resulting integrity token will verify the presence and validity of the challenge. A challenge should not be reused for multiple calls.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_generate_play_integrity_challenge_execute()` to send, or `firebaseappcheck_projects_apps_generate_play_integrity_challenge` for simplest API.

pub fn firebaseappcheck_projects_apps_generate_play_integrity_challenge_builder(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GeneratePlayIntegrityChallengeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+app}:generatePlayIntegrityChallenge",
        app,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+app}:generatePlayIntegrityChallenge
/// Generates a challenge that protects the integrity of an immediately following integrity verdict request to the Play Integrity API. The next call to ExchangePlayIntegrityToken using the resulting integrity token will verify the presence and validity of the challenge. A challenge should not be reused for multiple calls.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_generate_play_integrity_challenge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_generate_play_integrity_challenge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_generate_play_integrity_challenge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GeneratePlayIntegrityChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1GeneratePlayIntegrityChallengeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+app}:generatePlayIntegrityChallenge
/// Generates a challenge that protects the integrity of an immediately following integrity verdict request to the Play Integrity API. The next call to ExchangePlayIntegrityToken using the resulting integrity token will verify the presence and validity of the challenge. A challenge should not be reused for multiple calls.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_generate_play_integrity_challenge_builder()` + `firebaseappcheck_projects_apps_generate_play_integrity_challenge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_generate_play_integrity_challenge(
    client: &SimpleHttpClient,
    app: &str,
    body: &GoogleFirebaseAppcheckV1GeneratePlayIntegrityChallengeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GeneratePlayIntegrityChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_generate_play_integrity_challenge_builder(
        client, app, body,
    )?;
    firebaseappcheck_projects_apps_generate_play_integrity_challenge_execute(builder)
}

/// GET v1/{+parent}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_app_attest_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_app_attest_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/appAttestConfig:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()` + `firebaseappcheck_projects_apps_app_attest_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_app_attest_config_batch_get_builder(client, parent, names)?;
    firebaseappcheck_projects_apps_app_attest_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the AppAttestConfig for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_app_attest_config_get_execute()` to send, or `firebaseappcheck_projects_apps_app_attest_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_app_attest_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the AppAttestConfig for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_app_attest_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppAttestConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppAttestConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the AppAttestConfig for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_get_builder()` + `firebaseappcheck_projects_apps_app_attest_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppAttestConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_app_attest_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_app_attest_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the AppAttestConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange AppAttest tokens for App Check tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_app_attest_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_app_attest_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_app_attest_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1AppAttestConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the AppAttestConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange AppAttest tokens for App Check tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_app_attest_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppAttestConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppAttestConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the AppAttestConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange AppAttest tokens for App Check tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_patch_builder()` + `firebaseappcheck_projects_apps_app_attest_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1AppAttestConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppAttestConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_app_attest_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_app_attest_config_patch_execute(builder)
}

/// GET v1/{+parent}/debugTokens
/// Creates a new DebugToken for the specified app. For security reasons, after the creation operation completes, the token field cannot be updated or retrieved, but you can revoke the debug token using DeleteDebugToken. Each app can have a maximum of 20 debug tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_debug_tokens_create_execute()` to send, or `firebaseappcheck_projects_apps_debug_tokens_create` for simplest API.

pub fn firebaseappcheck_projects_apps_debug_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1DebugToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/debugTokens", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/debugTokens
/// Creates a new DebugToken for the specified app. For security reasons, after the creation operation completes, the token field cannot be updated or retrieved, but you can revoke the debug token using DeleteDebugToken. Each app can have a maximum of 20 debug tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_debug_tokens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1DebugToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/debugTokens
/// Creates a new DebugToken for the specified app. For security reasons, after the creation operation completes, the token field cannot be updated or retrieved, but you can revoke the debug token using DeleteDebugToken. Each app can have a maximum of 20 debug tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_create_builder()` + `firebaseappcheck_projects_apps_debug_tokens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1DebugToken,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_debug_tokens_create_builder(client, parent, body)?;
    firebaseappcheck_projects_apps_debug_tokens_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified DebugToken. A deleted debug token cannot be used to exchange for an App Check token. Use this method when you suspect the secret token has been compromised or when you no longer need the debug token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_debug_tokens_delete_execute()` to send, or `firebaseappcheck_projects_apps_debug_tokens_delete` for simplest API.

pub fn firebaseappcheck_projects_apps_debug_tokens_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified DebugToken. A deleted debug token cannot be used to exchange for an App Check token. Use this method when you suspect the secret token has been compromised or when you no longer need the debug token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_debug_tokens_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified DebugToken. A deleted debug token cannot be used to exchange for an App Check token. Use this method when you suspect the secret token has been compromised or when you no longer need the debug token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_delete_builder()` + `firebaseappcheck_projects_apps_debug_tokens_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_debug_tokens_delete_builder(client, name)?;
    firebaseappcheck_projects_apps_debug_tokens_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the specified DebugToken. For security reasons, the token field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_debug_tokens_get_execute()` to send, or `firebaseappcheck_projects_apps_debug_tokens_get` for simplest API.

pub fn firebaseappcheck_projects_apps_debug_tokens_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the specified DebugToken. For security reasons, the token field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_debug_tokens_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1DebugToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the specified DebugToken. For security reasons, the token field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_get_builder()` + `firebaseappcheck_projects_apps_debug_tokens_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_debug_tokens_get_builder(client, name)?;
    firebaseappcheck_projects_apps_debug_tokens_get_execute(builder)
}

/// GET v1/{+parent}/debugTokens
/// Lists all DebugTokens for the specified app. For security reasons, the token field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_debug_tokens_list_execute()` to send, or `firebaseappcheck_projects_apps_debug_tokens_list` for simplest API.

pub fn firebaseappcheck_projects_apps_debug_tokens_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/debugTokens", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/debugTokens
/// Lists all DebugTokens for the specified app. For security reasons, the token field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_debug_tokens_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListDebugTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ListDebugTokensResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/debugTokens
/// Lists all DebugTokens for the specified app. For security reasons, the token field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_list_builder()` + `firebaseappcheck_projects_apps_debug_tokens_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListDebugTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_debug_tokens_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    firebaseappcheck_projects_apps_debug_tokens_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the specified DebugToken. For security reasons, the token field cannot be updated, nor will it be populated in the response, but you can revoke the debug token using DeleteDebugToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_debug_tokens_patch_execute()` to send, or `firebaseappcheck_projects_apps_debug_tokens_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_debug_tokens_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1DebugToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the specified DebugToken. For security reasons, the token field cannot be updated, nor will it be populated in the response, but you can revoke the debug token using DeleteDebugToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_debug_tokens_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1DebugToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the specified DebugToken. For security reasons, the token field cannot be updated, nor will it be populated in the response, but you can revoke the debug token using DeleteDebugToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_debug_tokens_patch_builder()` + `firebaseappcheck_projects_apps_debug_tokens_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_debug_tokens_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1DebugToken,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DebugToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_debug_tokens_patch_builder(client, name, updateMask, body)?;
    firebaseappcheck_projects_apps_debug_tokens_patch_execute(builder)
}

/// GET v1/{+parent}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_device_check_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_device_check_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/deviceCheckConfig:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_device_check_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()` + `firebaseappcheck_projects_apps_device_check_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_device_check_config_batch_get_builder(
        client, parent, names,
    )?;
    firebaseappcheck_projects_apps_device_check_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the DeviceCheckConfig for the specified app. For security reasons, the private_key field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_device_check_config_get_execute()` to send, or `firebaseappcheck_projects_apps_device_check_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_device_check_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the DeviceCheckConfig for the specified app. For security reasons, the private_key field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_device_check_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DeviceCheckConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1DeviceCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the DeviceCheckConfig for the specified app. For security reasons, the private_key field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_get_builder()` + `firebaseappcheck_projects_apps_device_check_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DeviceCheckConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_device_check_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_device_check_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the DeviceCheckConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange DeviceCheck tokens for App Check tokens. For security reasons, the private_key field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_device_check_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_device_check_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_device_check_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1DeviceCheckConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the DeviceCheckConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange DeviceCheck tokens for App Check tokens. For security reasons, the private_key field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_device_check_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DeviceCheckConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1DeviceCheckConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the DeviceCheckConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange DeviceCheck tokens for App Check tokens. For security reasons, the private_key field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_patch_builder()` + `firebaseappcheck_projects_apps_device_check_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1DeviceCheckConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1DeviceCheckConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_device_check_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_device_check_config_patch_execute(builder)
}

/// GET v1/{+parent}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_play_integrity_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/playIntegrityConfig:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()` + `firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder(
        client, parent, names,
    )?;
    firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the PlayIntegrityConfig for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_play_integrity_config_get_execute()` to send, or `firebaseappcheck_projects_apps_play_integrity_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_play_integrity_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the PlayIntegrityConfig for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_play_integrity_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PlayIntegrityConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1PlayIntegrityConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the PlayIntegrityConfig for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_get_builder()` + `firebaseappcheck_projects_apps_play_integrity_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PlayIntegrityConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_play_integrity_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_play_integrity_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the PlayIntegrityConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange Play Integrity tokens for App Check tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_play_integrity_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_play_integrity_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_play_integrity_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1PlayIntegrityConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the PlayIntegrityConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange Play Integrity tokens for App Check tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_play_integrity_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PlayIntegrityConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1PlayIntegrityConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the PlayIntegrityConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange Play Integrity tokens for App Check tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_patch_builder()` + `firebaseappcheck_projects_apps_play_integrity_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1PlayIntegrityConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1PlayIntegrityConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_play_integrity_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_play_integrity_config_patch_execute(builder)
}

/// GET v1/{+parent}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/recaptchaEnterpriseConfig:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder(
        client, parent, names,
    )?;
    firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the RecaptchaEnterpriseConfig for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_enterprise_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the RecaptchaEnterpriseConfig for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the RecaptchaEnterpriseConfig for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_recaptcha_enterprise_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the RecaptchaEnterpriseConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` Enterprise tokens for App Check tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the RecaptchaEnterpriseConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` Enterprise tokens for App Check tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the RecaptchaEnterpriseConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` Enterprise tokens for App Check tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_builder()` + `firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaEnterpriseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_recaptcha_enterprise_config_patch_execute(builder)
}

/// GET v1/{+parent}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/recaptchaV3Config:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder(
        client, parent, names,
    )?;
    firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the RecaptchaV3Config for the specified app. For security reasons, the site_secret field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_v3_config_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_v3_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the RecaptchaV3Config for the specified app. For security reasons, the site_secret field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_v3_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaV3Config>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1RecaptchaV3Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the RecaptchaV3Config for the specified app. For security reasons, the site_secret field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_v3_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaV3Config>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_v3_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_recaptcha_v3_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the RecaptchaV3Config for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` V3 tokens for App Check tokens. For security reasons, the site_secret field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_v3_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_v3_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1RecaptchaV3Config,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the RecaptchaV3Config for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` V3 tokens for App Check tokens. For security reasons, the site_secret field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_recaptcha_v3_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaV3Config>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1RecaptchaV3Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the RecaptchaV3Config for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange `reCAPTCHA` V3 tokens for App Check tokens. For security reasons, the site_secret field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_patch_builder()` + `firebaseappcheck_projects_apps_recaptcha_v3_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1RecaptchaV3Config,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1RecaptchaV3Config>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_v3_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_recaptcha_v3_config_patch_execute(builder)
}

/// GET v1/{+parent}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_safety_net_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_safety_net_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/apps/-/safetyNetConfig:batchGet",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "names",
            names
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()` + `firebaseappcheck_projects_apps_safety_net_config_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get(
    client: &SimpleHttpClient,
    parent: &str,
    names: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_apps_safety_net_config_batch_get_builder(client, parent, names)?;
    firebaseappcheck_projects_apps_safety_net_config_batch_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the SafetyNetConfig for the specified app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_safety_net_config_get_execute()` to send, or `firebaseappcheck_projects_apps_safety_net_config_get` for simplest API.

pub fn firebaseappcheck_projects_apps_safety_net_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the SafetyNetConfig for the specified app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_safety_net_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1SafetyNetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1SafetyNetConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the SafetyNetConfig for the specified app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_get_builder()` + `firebaseappcheck_projects_apps_safety_net_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1SafetyNetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_safety_net_config_get_builder(client, name)?;
    firebaseappcheck_projects_apps_safety_net_config_get_execute(builder)
}

/// GET v1/{+name}
/// Updates the SafetyNetConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange SafetyNet tokens for App Check tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_safety_net_config_patch_execute()` to send, or `firebaseappcheck_projects_apps_safety_net_config_patch` for simplest API.

pub fn firebaseappcheck_projects_apps_safety_net_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1SafetyNetConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the SafetyNetConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange SafetyNet tokens for App Check tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_apps_safety_net_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1SafetyNetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1SafetyNetConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the SafetyNetConfig for the specified app. While this configuration is incomplete or invalid, the app will be unable to exchange SafetyNet tokens for App Check tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_patch_builder()` + `firebaseappcheck_projects_apps_safety_net_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1SafetyNetConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1SafetyNetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_safety_net_config_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_apps_safety_net_config_patch_execute(builder)
}

/// GET v1/{+parent}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_batch_update_execute()` to send, or `firebaseappcheck_projects_services_batch_update` for simplest API.

pub fn firebaseappcheck_projects_services_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1BatchUpdateServicesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/services:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchUpdateServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_batch_update_builder()` + `firebaseappcheck_projects_services_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1BatchUpdateServicesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_batch_update_builder(client, parent, body)?;
    firebaseappcheck_projects_services_batch_update_execute(builder)
}

/// GET v1/{+name}
/// Gets the Service configuration for the specified service name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_get_execute()` to send, or `firebaseappcheck_projects_services_get` for simplest API.

pub fn firebaseappcheck_projects_services_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the Service configuration for the specified service name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1Service>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the Service configuration for the specified service name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_get_builder()` + `firebaseappcheck_projects_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1Service>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_get_builder(client, name)?;
    firebaseappcheck_projects_services_get_execute(builder)
}

/// GET v1/{+parent}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_list_execute()` to send, or `firebaseappcheck_projects_services_list` for simplest API.

pub fn firebaseappcheck_projects_services_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/services", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ListServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_list_builder()` + `firebaseappcheck_projects_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_services_list_builder(client, parent, pageSize, pageToken)?;
    firebaseappcheck_projects_services_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the specified Service configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_patch_execute()` to send, or `firebaseappcheck_projects_services_patch` for simplest API.

pub fn firebaseappcheck_projects_services_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the specified Service configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1Service>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the specified Service configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_patch_builder()` + `firebaseappcheck_projects_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1Service,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1Service>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_patch_builder(client, name, updateMask, body)?;
    firebaseappcheck_projects_services_patch_execute(builder)
}

/// GET v1/{+parent}/resourcePolicies:batchUpdate
/// Atomically updates the specified ResourcePolicy configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_batch_update_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_batch_update` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1BatchUpdateResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/resourcePolicies:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/resourcePolicies:batchUpdate
/// Atomically updates the specified ResourcePolicy configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateResourcePoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchUpdateResourcePoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/resourcePolicies:batchUpdate
/// Atomically updates the specified ResourcePolicy configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_batch_update_builder()` + `firebaseappcheck_projects_services_resource_policies_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1BatchUpdateResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateResourcePoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_resource_policies_batch_update_builder(
        client, parent, body,
    )?;
    firebaseappcheck_projects_services_resource_policies_batch_update_execute(builder)
}

/// GET v1/{+parent}/resourcePolicies
/// Creates the specified ResourcePolicy configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_create_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_create` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1ResourcePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/resourcePolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/resourcePolicies
/// Creates the specified ResourcePolicy configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ResourcePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/resourcePolicies
/// Creates the specified ResourcePolicy configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_create_builder()` + `firebaseappcheck_projects_services_resource_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleFirebaseAppcheckV1ResourcePolicy,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_services_resource_policies_create_builder(client, parent, body)?;
    firebaseappcheck_projects_services_resource_policies_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified ResourcePolicy configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_delete_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_delete` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes the specified ResourcePolicy configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified ResourcePolicy configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_delete_builder()` + `firebaseappcheck_projects_services_resource_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaseappcheck_projects_services_resource_policies_delete_builder(client, name, etag)?;
    firebaseappcheck_projects_services_resource_policies_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the requested ResourcePolicy configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_get_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_get` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the requested ResourcePolicy configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ResourcePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the requested ResourcePolicy configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_get_builder()` + `firebaseappcheck_projects_services_resource_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_resource_policies_get_builder(client, name)?;
    firebaseappcheck_projects_services_resource_policies_get_execute(builder)
}

/// GET v1/{+parent}/resourcePolicies
/// Lists all ResourcePolicy configurations for the specified project and service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_list_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_list` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/resourcePolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/resourcePolicies
/// Lists all ResourcePolicy configurations for the specified project and service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListResourcePoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ListResourcePoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/resourcePolicies
/// Lists all ResourcePolicy configurations for the specified project and service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_list_builder()` + `firebaseappcheck_projects_services_resource_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListResourcePoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_resource_policies_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    firebaseappcheck_projects_services_resource_policies_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the specified ResourcePolicy configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_resource_policies_patch_execute()` to send, or `firebaseappcheck_projects_services_resource_policies_patch` for simplest API.

pub fn firebaseappcheck_projects_services_resource_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1ResourcePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the specified ResourcePolicy configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaseappcheck_projects_services_resource_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ResourcePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the specified ResourcePolicy configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_resource_policies_patch_builder()` + `firebaseappcheck_projects_services_resource_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_resource_policies_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleFirebaseAppcheckV1ResourcePolicy,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ResourcePolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_resource_policies_patch_builder(
        client, name, updateMask, body,
    )?;
    firebaseappcheck_projects_services_resource_policies_patch_execute(builder)
}
