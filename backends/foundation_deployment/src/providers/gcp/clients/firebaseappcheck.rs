//! Auto-generated API clients for gcp/firebaseappcheck.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_app_attest_assertion` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder(
    client: &SimpleHttpClient,
    app: String,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/oauthClients/{}:exchangeAppAttestAssertion",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute()` or `firebaseappcheck_oauth_clients_exchange_app_attest_assertion`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_task()`.
/// For the simplest API, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_oauth_clients_exchange_app_attest_assertion_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_oauth_clients_exchange_app_attest_assertion`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckOauthClientsExchangeAppAttestAssertionArgs {
    /// Path parameter: app
    pub app: String,
    /// Request body.
    pub body: GoogleFirebaseAppcheckV1ExchangeAppAttestAssertionRequest,
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAssertion
/// Accepts an App Attest assertion and an artifact previously obtained from ExchangeAppAttestAttestation and verifies those with Apple. If valid, returns an AppCheckToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder()` + `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute()`.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_app_attest_assertion_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_assertion(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckOauthClientsExchangeAppAttestAssertionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_oauth_clients_exchange_app_attest_assertion_builder(
        client,
        args.app.clone(),
        &args.body,
    )?;
    firebaseappcheck_oauth_clients_exchange_app_attest_assertion_execute(builder)
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_app_attest_attestation` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder(
    client: &SimpleHttpClient,
    app: String,
    body: &GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/oauthClients/{}:exchangeAppAttestAttestation",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute()` or `firebaseappcheck_oauth_clients_exchange_app_attest_attestation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_task()`.
/// For the simplest API, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_oauth_clients_exchange_app_attest_attestation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_oauth_clients_exchange_app_attest_attestation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckOauthClientsExchangeAppAttestAttestationArgs {
    /// Path parameter: app
    pub app: String,
    /// Request body.
    pub body: GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationRequest,
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeAppAttestAttestation
/// Accepts an App Attest CBOR attestation and verifies it with Apple using your preconfigured team and bundle IDs. If valid, returns an attestation artifact that can later be exchanged for an AppCheckToken using ExchangeAppAttestAssertion. For convenience and performance, this method's response object will also contain an AppCheckToken (if the verification is successful).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder()` + `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute()`.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_app_attest_attestation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_app_attest_attestation(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckOauthClientsExchangeAppAttestAttestationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1ExchangeAppAttestAttestationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_oauth_clients_exchange_app_attest_attestation_builder(
        client,
        args.app.clone(),
        &args.body,
    )?;
    firebaseappcheck_oauth_clients_exchange_app_attest_attestation_execute(builder)
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_exchange_debug_token_execute()` to send, or `firebaseappcheck_oauth_clients_exchange_debug_token` for simplest API.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token_builder(
    client: &SimpleHttpClient,
    app: String,
    body: &GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebaseappcheck.googleapis.com/v1/oauthClients/{}:exchangeDebugToken",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_oauth_clients_exchange_debug_token_execute()` or `firebaseappcheck_oauth_clients_exchange_debug_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_debug_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1AppCheckToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_oauth_clients_exchange_debug_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_debug_token_task()`.
/// For the simplest API, use `firebaseappcheck_oauth_clients_exchange_debug_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_exchange_debug_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_oauth_clients_exchange_debug_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_oauth_clients_exchange_debug_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckOauthClientsExchangeDebugTokenArgs {
    /// Path parameter: app
    pub app: String,
    /// Request body.
    pub body: GoogleFirebaseAppcheckV1ExchangeDebugTokenRequest,
}

/// GET v1/oauthClients/{oauthClientsId}:exchangeDebugToken
/// Validates a debug token secret that you have previously created using CreateDebugToken. If valid, returns an AppCheckToken. Note that a restrictive quota is enforced on this method to prevent accidental exposure of the app to abuse.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_exchange_debug_token_builder()` + `firebaseappcheck_oauth_clients_exchange_debug_token_execute()`.
/// For task-level control, use `firebaseappcheck_oauth_clients_exchange_debug_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_exchange_debug_token(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckOauthClientsExchangeDebugTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1AppCheckToken>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_oauth_clients_exchange_debug_token_builder(
        client,
        args.app.clone(),
        &args.body,
    )?;
    firebaseappcheck_oauth_clients_exchange_debug_token_execute(builder)
}

/// GET v1/oauthClients/{oauthClientsId}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute()` to send, or `firebaseappcheck_oauth_clients_generate_app_attest_challenge` for simplest API.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder(
    client: &SimpleHttpClient,
    app: String,
    body: &GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/oauthClients/{}:generateAppAttestChallenge",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/oauthClients/{oauthClientsId}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute()` or `firebaseappcheck_oauth_clients_generate_app_attest_challenge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/oauthClients/{oauthClientsId}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_task()`.
/// For the simplest API, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_oauth_clients_generate_app_attest_challenge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_oauth_clients_generate_app_attest_challenge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckOauthClientsGenerateAppAttestChallengeArgs {
    /// Path parameter: app
    pub app: String,
    /// Request body.
    pub body: GoogleFirebaseAppcheckV1GenerateAppAttestChallengeRequest,
}

/// GET v1/oauthClients/{oauthClientsId}:generateAppAttestChallenge
/// Generates a challenge that protects the integrity of an immediately following call to ExchangeAppAttestAttestation or ExchangeAppAttestAssertion. A challenge should not be reused for multiple calls.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder()` + `firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute()`.
/// For task-level control, use `firebaseappcheck_oauth_clients_generate_app_attest_challenge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_oauth_clients_generate_app_attest_challenge(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckOauthClientsGenerateAppAttestChallengeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1GenerateAppAttestChallengeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_oauth_clients_generate_app_attest_challenge_builder(
        client,
        args.app.clone(),
        &args.body,
    )?;
    firebaseappcheck_oauth_clients_generate_app_attest_challenge_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_app_attest_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_app_attest_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/appAttestConfig:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_execute()` or `firebaseappcheck_projects_apps_app_attest_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_app_attest_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_app_attest_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_app_attest_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsAppAttestConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/appAttestConfig:batchGet
/// Atomically gets the AppAttestConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_builder()` + `firebaseappcheck_projects_apps_app_attest_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_app_attest_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_app_attest_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsAppAttestConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetAppAttestConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_app_attest_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_app_attest_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_device_check_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_device_check_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/deviceCheckConfig:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_device_check_config_batch_get_execute()` or `firebaseappcheck_projects_apps_device_check_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_device_check_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_device_check_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_device_check_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_device_check_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsDeviceCheckConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/deviceCheckConfig:batchGet
/// Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the private_key field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_device_check_config_batch_get_builder()` + `firebaseappcheck_projects_apps_device_check_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_device_check_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_device_check_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsDeviceCheckConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetDeviceCheckConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_device_check_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_device_check_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_play_integrity_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/playIntegrityConfig:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute()` or `firebaseappcheck_projects_apps_play_integrity_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_play_integrity_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_play_integrity_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsPlayIntegrityConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/playIntegrityConfig:batchGet
/// Atomically gets the PlayIntegrityConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder()` + `firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_play_integrity_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_play_integrity_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsPlayIntegrityConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetPlayIntegrityConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_play_integrity_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_play_integrity_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/recaptchaEnterpriseConfig:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute()` or `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsRecaptchaEnterpriseConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaEnterpriseConfig:batchGet
/// Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsRecaptchaEnterpriseConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaEnterpriseConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_recaptcha_enterprise_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/recaptchaV3Config:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute()` or `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsRecaptchaV3ConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/recaptchaV3Config:batchGet
/// Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the site_secret field is never populated in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder()` + `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsRecaptchaV3ConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetRecaptchaV3ConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_recaptcha_v3_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_apps_safety_net_config_batch_get_execute()` to send, or `firebaseappcheck_projects_apps_safety_net_config_batch_get` for simplest API.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get_builder(
    client: &SimpleHttpClient,
    parent: String,
    names: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firebaseappcheck.googleapis.com/v1/projects/{}/apps/-/safetyNetConfig:batchGet",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_execute()` or `firebaseappcheck_projects_apps_safety_net_config_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_task()`.
/// For the simplest API, use `firebaseappcheck_projects_apps_safety_net_config_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_apps_safety_net_config_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_apps_safety_net_config_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsAppsSafetyNetConfigBatchGetArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/projects/{projectsId}/apps/-/safetyNetConfig:batchGet
/// Atomically gets the SafetyNetConfigs for the specified list of apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_builder()` + `firebaseappcheck_projects_apps_safety_net_config_batch_get_execute()`.
/// For task-level control, use `firebaseappcheck_projects_apps_safety_net_config_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_apps_safety_net_config_batch_get(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsAppsSafetyNetConfigBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchGetSafetyNetConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_apps_safety_net_config_batch_get_builder(
        client,
        args.parent.clone(),
        args.names.clone(),
    )?;
    firebaseappcheck_projects_apps_safety_net_config_batch_get_execute(builder)
}

/// GET v1/projects/{projectsId}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_batch_update_execute()` to send, or `firebaseappcheck_projects_services_batch_update` for simplest API.

pub fn firebaseappcheck_projects_services_batch_update_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleFirebaseAppcheckV1BatchUpdateServicesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebaseappcheck.googleapis.com/v1/projects/{}/services:batchUpdate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_services_batch_update_execute()` or `firebaseappcheck_projects_services_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_services_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateServicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1BatchUpdateServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_services_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_services_batch_update_task()`.
/// For the simplest API, use `firebaseappcheck_projects_services_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_services_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_services_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_services_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_services_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsServicesBatchUpdateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleFirebaseAppcheckV1BatchUpdateServicesRequest,
}

/// GET v1/projects/{projectsId}/services:batchUpdate
/// Atomically updates the specified Service configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_batch_update_builder()` + `firebaseappcheck_projects_services_batch_update_execute()`.
/// For task-level control, use `firebaseappcheck_projects_services_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_batch_update(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsServicesBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1BatchUpdateServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_batch_update_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    firebaseappcheck_projects_services_batch_update_execute(builder)
}

/// GET v1/projects/{projectsId}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaseappcheck_projects_services_list_execute()` to send, or `firebaseappcheck_projects_services_list` for simplest API.

pub fn firebaseappcheck_projects_services_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://firebaseappcheck.googleapis.com/v1/projects/{}/services",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaseappcheck_projects_services_list_execute()` or `firebaseappcheck_projects_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirebaseAppcheckV1ListServicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseAppcheckV1ListServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaseappcheck_projects_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaseappcheck_projects_services_list_task()`.
/// For the simplest API, use `firebaseappcheck_projects_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaseappcheck_projects_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaseappcheck_projects_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebaseappcheck_projects_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaseappcheck_projects_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseappcheckProjectsServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/services
/// Lists all Service configurations for the specified project. Only Services which were explicitly configured using UpdateService or BatchUpdateServices will be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaseappcheck_projects_services_list_builder()` + `firebaseappcheck_projects_services_list_execute()`.
/// For task-level control, use `firebaseappcheck_projects_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaseappcheck_projects_services_list(
    client: &SimpleHttpClient,
    args: &FirebaseappcheckProjectsServicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirebaseAppcheckV1ListServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebaseappcheck_projects_services_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    firebaseappcheck_projects_services_list_execute(builder)
}
