//! Auto-generated API clients for gcp/language.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/documents:analyzeEntities
/// Finds named entities (currently proper names and common nouns) in the text along with entity types, probability, mentions for each entity, and other properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `language_documents_analyze_entities_execute()` to send, or `language_documents_analyze_entities` for simplest API.

pub fn language_documents_analyze_entities_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:analyzeEntities",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/documents:analyzeEntities
/// Finds named entities (currently proper names and common nouns) in the text along with entity types, probability, mentions for each entity, and other properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `language_documents_analyze_entities_execute()` or `language_documents_analyze_entities`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_analyze_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_analyze_entities_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeEntitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/documents:analyzeEntities
/// Finds named entities (currently proper names and common nouns) in the text along with entity types, probability, mentions for each entity, and other properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `language_documents_analyze_entities_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `language_documents_analyze_entities_task()`.
/// For the simplest API, use `language_documents_analyze_entities()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_analyze_entities_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn language_documents_analyze_entities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = language_documents_analyze_entities_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/documents:analyzeEntities
/// Finds named entities (currently proper names and common nouns) in the text along with entity types, probability, mentions for each entity, and other properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `language_documents_analyze_entities_builder()` + `language_documents_analyze_entities_execute()`.
/// For task-level control, use `language_documents_analyze_entities_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_analyze_entities(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeEntitiesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = language_documents_analyze_entities_builder(client)?;
    language_documents_analyze_entities_execute(builder)
}

/// POST v2/documents:analyzeSentiment
/// Analyzes the sentiment of the provided text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `language_documents_analyze_sentiment_execute()` to send, or `language_documents_analyze_sentiment` for simplest API.

pub fn language_documents_analyze_sentiment_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:analyzeSentiment",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/documents:analyzeSentiment
/// Analyzes the sentiment of the provided text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `language_documents_analyze_sentiment_execute()` or `language_documents_analyze_sentiment`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_analyze_sentiment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_analyze_sentiment_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeSentimentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeSentimentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/documents:analyzeSentiment
/// Analyzes the sentiment of the provided text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `language_documents_analyze_sentiment_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `language_documents_analyze_sentiment_task()`.
/// For the simplest API, use `language_documents_analyze_sentiment()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_analyze_sentiment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn language_documents_analyze_sentiment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeSentimentResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = language_documents_analyze_sentiment_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/documents:analyzeSentiment
/// Analyzes the sentiment of the provided text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `language_documents_analyze_sentiment_builder()` + `language_documents_analyze_sentiment_execute()`.
/// For task-level control, use `language_documents_analyze_sentiment_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_analyze_sentiment(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeSentimentResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = language_documents_analyze_sentiment_builder(client)?;
    language_documents_analyze_sentiment_execute(builder)
}

/// POST v2/documents:annotateText
/// A convenience method that provides all features in one call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `language_documents_annotate_text_execute()` to send, or `language_documents_annotate_text` for simplest API.

pub fn language_documents_annotate_text_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:annotateText",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/documents:annotateText
/// A convenience method that provides all features in one call.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `language_documents_annotate_text_execute()` or `language_documents_annotate_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_annotate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_annotate_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnnotateTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnnotateTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/documents:annotateText
/// A convenience method that provides all features in one call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `language_documents_annotate_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `language_documents_annotate_text_task()`.
/// For the simplest API, use `language_documents_annotate_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_annotate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn language_documents_annotate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = language_documents_annotate_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/documents:annotateText
/// A convenience method that provides all features in one call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `language_documents_annotate_text_builder()` + `language_documents_annotate_text_execute()`.
/// For task-level control, use `language_documents_annotate_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_annotate_text(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = language_documents_annotate_text_builder(client)?;
    language_documents_annotate_text_execute(builder)
}

/// POST v2/documents:classifyText
/// Classifies a document into categories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `language_documents_classify_text_execute()` to send, or `language_documents_classify_text` for simplest API.

pub fn language_documents_classify_text_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:classifyText",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/documents:classifyText
/// Classifies a document into categories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `language_documents_classify_text_execute()` or `language_documents_classify_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_classify_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_classify_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClassifyTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClassifyTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/documents:classifyText
/// Classifies a document into categories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `language_documents_classify_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `language_documents_classify_text_task()`.
/// For the simplest API, use `language_documents_classify_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_classify_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn language_documents_classify_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClassifyTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = language_documents_classify_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/documents:classifyText
/// Classifies a document into categories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `language_documents_classify_text_builder()` + `language_documents_classify_text_execute()`.
/// For task-level control, use `language_documents_classify_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_classify_text(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClassifyTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = language_documents_classify_text_builder(client)?;
    language_documents_classify_text_execute(builder)
}

/// POST v2/documents:moderateText
/// Moderates a document for harmful and sensitive categories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `language_documents_moderate_text_execute()` to send, or `language_documents_moderate_text` for simplest API.

pub fn language_documents_moderate_text_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:moderateText",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/documents:moderateText
/// Moderates a document for harmful and sensitive categories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `language_documents_moderate_text_execute()` or `language_documents_moderate_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_moderate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_moderate_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ModerateTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModerateTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/documents:moderateText
/// Moderates a document for harmful and sensitive categories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `language_documents_moderate_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `language_documents_moderate_text_task()`.
/// For the simplest API, use `language_documents_moderate_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `language_documents_moderate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn language_documents_moderate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ModerateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = language_documents_moderate_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/documents:moderateText
/// Moderates a document for harmful and sensitive categories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `language_documents_moderate_text_builder()` + `language_documents_moderate_text_execute()`.
/// For task-level control, use `language_documents_moderate_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn language_documents_moderate_text(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ModerateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = language_documents_moderate_text_builder(client)?;
    language_documents_moderate_text_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for AnalyzeEntitiesResponse
// =============================================================================

/// ResourceIdentifier implementation for AnalyzeEntitiesResponse with LanguageDocumentsAnalyzeEntitiesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LanguageDocumentsAnalyzeEntitiesArgs> for AnalyzeEntitiesResponse {
    fn generate_resource_id(&self, input: &LanguageDocumentsAnalyzeEntitiesArgs) -> String {
        "gcp::language::AnalyzeEntitiesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::language::AnalyzeEntitiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnalyzeSentimentResponse
// =============================================================================

/// ResourceIdentifier implementation for AnalyzeSentimentResponse with LanguageDocumentsAnalyzeSentimentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LanguageDocumentsAnalyzeSentimentArgs> for AnalyzeSentimentResponse {
    fn generate_resource_id(&self, input: &LanguageDocumentsAnalyzeSentimentArgs) -> String {
        "gcp::language::AnalyzeSentimentResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::language::AnalyzeSentimentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnnotateTextResponse
// =============================================================================

/// ResourceIdentifier implementation for AnnotateTextResponse with LanguageDocumentsAnnotateTextArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LanguageDocumentsAnnotateTextArgs> for AnnotateTextResponse {
    fn generate_resource_id(&self, input: &LanguageDocumentsAnnotateTextArgs) -> String {
        "gcp::language::AnnotateTextResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::language::AnnotateTextResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClassifyTextResponse
// =============================================================================

/// ResourceIdentifier implementation for ClassifyTextResponse with LanguageDocumentsClassifyTextArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LanguageDocumentsClassifyTextArgs> for ClassifyTextResponse {
    fn generate_resource_id(&self, input: &LanguageDocumentsClassifyTextArgs) -> String {
        "gcp::language::ClassifyTextResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::language::ClassifyTextResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ModerateTextResponse
// =============================================================================

/// ResourceIdentifier implementation for ModerateTextResponse with LanguageDocumentsModerateTextArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LanguageDocumentsModerateTextArgs> for ModerateTextResponse {
    fn generate_resource_id(&self, input: &LanguageDocumentsModerateTextArgs) -> String {
        "gcp::language::ModerateTextResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::language::ModerateTextResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
