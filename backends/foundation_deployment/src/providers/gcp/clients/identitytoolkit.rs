//! Auto-generated API clients for gcp/identitytoolkit.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_create_auth_uri_execute()` to send, or `identitytoolkit_relyingparty_create_auth_uri` for simplest API.

pub fn identitytoolkit_relyingparty_create_auth_uri_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyCreateAuthUriRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcreateAuthUri",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_create_auth_uri_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_create_auth_uri_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_create_auth_uri_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAuthUriResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateAuthUriResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_create_auth_uri_builder()` + `identitytoolkit_relyingparty_create_auth_uri_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_create_auth_uri(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyCreateAuthUriRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAuthUriResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_create_auth_uri_builder(client, body)?;
    identitytoolkit_relyingparty_create_auth_uri_execute(builder)
}

/// GET deleteAccount
/// Delete user account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_delete_account_execute()` to send, or `identitytoolkit_relyingparty_delete_account` for simplest API.

pub fn identitytoolkit_relyingparty_delete_account_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyDeleteAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdeleteAccount",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET deleteAccount
/// Delete user account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_delete_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_delete_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_delete_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deleteAccount
/// Delete user account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_delete_account_builder()` + `identitytoolkit_relyingparty_delete_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_delete_account(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyDeleteAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_delete_account_builder(client, body)?;
    identitytoolkit_relyingparty_delete_account_execute(builder)
}

/// GET downloadAccount
/// Batch download user accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_download_account_execute()` to send, or `identitytoolkit_relyingparty_download_account` for simplest API.

pub fn identitytoolkit_relyingparty_download_account_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyDownloadAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdownloadAccount",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET downloadAccount
/// Batch download user accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_download_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_download_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_download_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DownloadAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET downloadAccount
/// Batch download user accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_download_account_builder()` + `identitytoolkit_relyingparty_download_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_download_account(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyDownloadAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_download_account_builder(client, body)?;
    identitytoolkit_relyingparty_download_account_execute(builder)
}

/// GET emailLinkSignin
/// Reset password for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_email_link_signin_execute()` to send, or `identitytoolkit_relyingparty_email_link_signin` for simplest API.

pub fn identitytoolkit_relyingparty_email_link_signin_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyEmailLinkSigninRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comemailLinkSignin",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET emailLinkSignin
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_email_link_signin_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_email_link_signin_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_email_link_signin_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmailLinkSigninResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EmailLinkSigninResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET emailLinkSignin
/// Reset password for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_email_link_signin_builder()` + `identitytoolkit_relyingparty_email_link_signin_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_email_link_signin(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyEmailLinkSigninRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmailLinkSigninResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_email_link_signin_builder(client, body)?;
    identitytoolkit_relyingparty_email_link_signin_execute(builder)
}

/// GET getAccountInfo
/// Returns the account info.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_account_info_execute()` to send, or `identitytoolkit_relyingparty_get_account_info` for simplest API.

pub fn identitytoolkit_relyingparty_get_account_info_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyGetAccountInfoRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comgetAccountInfo",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET getAccountInfo
/// Returns the account info.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_get_account_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_get_account_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_account_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetAccountInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getAccountInfo
/// Returns the account info.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_account_info_builder()` + `identitytoolkit_relyingparty_get_account_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_account_info(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyGetAccountInfoRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_account_info_builder(client, body)?;
    identitytoolkit_relyingparty_get_account_info_execute(builder)
}

/// GET getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_oob_confirmation_code_execute()` to send, or `identitytoolkit_relyingparty_get_oob_confirmation_code` for simplest API.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_builder(
    client: &SimpleHttpClient,
    body: &Relyingparty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comgetOobConfirmationCode",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_get_oob_confirmation_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetOobConfirmationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetOobConfirmationCodeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()` + `identitytoolkit_relyingparty_get_oob_confirmation_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code(
    client: &SimpleHttpClient,
    body: &Relyingparty,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetOobConfirmationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_oob_confirmation_code_builder(client, body)?;
    identitytoolkit_relyingparty_get_oob_confirmation_code_execute(builder)
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_project_config_execute()` to send, or `identitytoolkit_relyingparty_get_project_config` for simplest API.

pub fn identitytoolkit_relyingparty_get_project_config_builder(
    client: &SimpleHttpClient,
    delegatedProjectNumber: Option<&str>,
    projectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comgetProjectConfig",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "delegatedProjectNumber",
            delegatedProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectNumber",
            projectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_get_project_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_get_project_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_project_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetProjectConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_project_config_builder()` + `identitytoolkit_relyingparty_get_project_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_project_config(
    client: &SimpleHttpClient,
    delegatedProjectNumber: Option<&str>,
    projectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_project_config_builder(
        client,
        delegatedProjectNumber,
        projectNumber,
    )?;
    identitytoolkit_relyingparty_get_project_config_execute(builder)
}

/// GET publicKeys
/// Get token signing public key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_public_keys_execute()` to send, or `identitytoolkit_relyingparty_get_public_keys` for simplest API.

pub fn identitytoolkit_relyingparty_get_public_keys_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.compublicKeys",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET publicKeys
/// Get token signing public key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_get_public_keys_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_get_public_keys_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_public_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetPublicKeysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET publicKeys
/// Get token signing public key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_public_keys_builder()` + `identitytoolkit_relyingparty_get_public_keys_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_public_keys(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_public_keys_builder(client)?;
    identitytoolkit_relyingparty_get_public_keys_execute(builder)
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_recaptcha_param_execute()` to send, or `identitytoolkit_relyingparty_get_recaptcha_param` for simplest API.

pub fn identitytoolkit_relyingparty_get_recaptcha_param_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comgetRecaptchaParam",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_get_recaptcha_param_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_get_recaptcha_param_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_recaptcha_param_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRecaptchaParamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetRecaptchaParamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_recaptcha_param_builder()` + `identitytoolkit_relyingparty_get_recaptcha_param_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_recaptcha_param(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRecaptchaParamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_recaptcha_param_builder(client)?;
    identitytoolkit_relyingparty_get_recaptcha_param_execute(builder)
}

/// GET resetPassword
/// Reset password for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_reset_password_execute()` to send, or `identitytoolkit_relyingparty_reset_password` for simplest API.

pub fn identitytoolkit_relyingparty_reset_password_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyResetPasswordRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comresetPassword",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET resetPassword
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_reset_password_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_reset_password_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_reset_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetPasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET resetPassword
/// Reset password for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_reset_password_builder()` + `identitytoolkit_relyingparty_reset_password_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_reset_password(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyResetPasswordRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_reset_password_builder(client, body)?;
    identitytoolkit_relyingparty_reset_password_execute(builder)
}

/// GET sendVerificationCode
/// Send SMS verification code.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_send_verification_code_execute()` to send, or `identitytoolkit_relyingparty_send_verification_code` for simplest API.

pub fn identitytoolkit_relyingparty_send_verification_code_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySendVerificationCodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comsendVerificationCode",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET sendVerificationCode
/// Send SMS verification code.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_send_verification_code_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_send_verification_code_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_send_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySendVerificationCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET sendVerificationCode
/// Send SMS verification code.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_send_verification_code_builder()` + `identitytoolkit_relyingparty_send_verification_code_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_send_verification_code(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySendVerificationCodeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_send_verification_code_builder(client, body)?;
    identitytoolkit_relyingparty_send_verification_code_execute(builder)
}

/// GET setAccountInfo
/// Set account info for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_set_account_info_execute()` to send, or `identitytoolkit_relyingparty_set_account_info` for simplest API.

pub fn identitytoolkit_relyingparty_set_account_info_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySetAccountInfoRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comsetAccountInfo",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET setAccountInfo
/// Set account info for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_set_account_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_set_account_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_account_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetAccountInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET setAccountInfo
/// Set account info for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_set_account_info_builder()` + `identitytoolkit_relyingparty_set_account_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_account_info(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySetAccountInfoRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_set_account_info_builder(client, body)?;
    identitytoolkit_relyingparty_set_account_info_execute(builder)
}

/// GET setProjectConfig
/// Set project configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_set_project_config_execute()` to send, or `identitytoolkit_relyingparty_set_project_config` for simplest API.

pub fn identitytoolkit_relyingparty_set_project_config_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySetProjectConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comsetProjectConfig",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET setProjectConfig
/// Set project configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_set_project_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_set_project_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_project_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySetProjectConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET setProjectConfig
/// Set project configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_set_project_config_builder()` + `identitytoolkit_relyingparty_set_project_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_project_config(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySetProjectConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_set_project_config_builder(client, body)?;
    identitytoolkit_relyingparty_set_project_config_execute(builder)
}

/// GET signOutUser
/// Sign out user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_sign_out_user_execute()` to send, or `identitytoolkit_relyingparty_sign_out_user` for simplest API.

pub fn identitytoolkit_relyingparty_sign_out_user_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySignOutUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comsignOutUser",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET signOutUser
/// Sign out user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_sign_out_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_sign_out_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_sign_out_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySignOutUserResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET signOutUser
/// Sign out user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_sign_out_user_builder()` + `identitytoolkit_relyingparty_sign_out_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_sign_out_user(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySignOutUserRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_sign_out_user_builder(client, body)?;
    identitytoolkit_relyingparty_sign_out_user_execute(builder)
}

/// GET signupNewUser
/// Signup new user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_signup_new_user_execute()` to send, or `identitytoolkit_relyingparty_signup_new_user` for simplest API.

pub fn identitytoolkit_relyingparty_signup_new_user_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySignupNewUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comsignupNewUser",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET signupNewUser
/// Signup new user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_signup_new_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_signup_new_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_signup_new_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupNewUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupNewUserResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET signupNewUser
/// Signup new user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_signup_new_user_builder()` + `identitytoolkit_relyingparty_signup_new_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_signup_new_user(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartySignupNewUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupNewUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_signup_new_user_builder(client, body)?;
    identitytoolkit_relyingparty_signup_new_user_execute(builder)
}

/// GET uploadAccount
/// Batch upload existing user accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_upload_account_execute()` to send, or `identitytoolkit_relyingparty_upload_account` for simplest API.

pub fn identitytoolkit_relyingparty_upload_account_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyUploadAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comuploadAccount",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET uploadAccount
/// Batch upload existing user accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_upload_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_upload_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_upload_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET uploadAccount
/// Batch upload existing user accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_upload_account_builder()` + `identitytoolkit_relyingparty_upload_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_upload_account(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyUploadAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_upload_account_builder(client, body)?;
    identitytoolkit_relyingparty_upload_account_execute(builder)
}

/// GET verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_assertion_execute()` to send, or `identitytoolkit_relyingparty_verify_assertion` for simplest API.

pub fn identitytoolkit_relyingparty_verify_assertion_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyAssertionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comverifyAssertion",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_verify_assertion_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_verify_assertion_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_assertion_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyAssertionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyAssertionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_assertion_builder()` + `identitytoolkit_relyingparty_verify_assertion_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_assertion(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyAssertionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyAssertionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_assertion_builder(client, body)?;
    identitytoolkit_relyingparty_verify_assertion_execute(builder)
}

/// GET verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_custom_token_execute()` to send, or `identitytoolkit_relyingparty_verify_custom_token` for simplest API.

pub fn identitytoolkit_relyingparty_verify_custom_token_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyCustomTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comverifyCustomToken",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_verify_custom_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_verify_custom_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_custom_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyCustomTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyCustomTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_custom_token_builder()` + `identitytoolkit_relyingparty_verify_custom_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_custom_token(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyCustomTokenRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyCustomTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_custom_token_builder(client, body)?;
    identitytoolkit_relyingparty_verify_custom_token_execute(builder)
}

/// GET verifyPassword
/// Verifies the user entered password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_password_execute()` to send, or `identitytoolkit_relyingparty_verify_password` for simplest API.

pub fn identitytoolkit_relyingparty_verify_password_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyPasswordRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comverifyPassword",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET verifyPassword
/// Verifies the user entered password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_verify_password_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_verify_password_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyPasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET verifyPassword
/// Verifies the user entered password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_password_builder()` + `identitytoolkit_relyingparty_verify_password_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_password(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyPasswordRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_password_builder(client, body)?;
    identitytoolkit_relyingparty_verify_password_execute(builder)
}

/// GET verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_phone_number_execute()` to send, or `identitytoolkit_relyingparty_verify_phone_number` for simplest API.

pub fn identitytoolkit_relyingparty_verify_phone_number_builder(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyPhoneNumberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comverifyPhoneNumber",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `identitytoolkit_relyingparty_verify_phone_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from identitytoolkit_relyingparty_verify_phone_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_phone_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_phone_number_builder()` + `identitytoolkit_relyingparty_verify_phone_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_phone_number(
    client: &SimpleHttpClient,
    body: &IdentitytoolkitRelyingpartyVerifyPhoneNumberRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_phone_number_builder(client, body)?;
    identitytoolkit_relyingparty_verify_phone_number_execute(builder)
}
