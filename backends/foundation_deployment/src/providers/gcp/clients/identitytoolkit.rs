//! Auto-generated API clients for gcp/identitytoolkit.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_create_auth_uri_execute()` to send, or `identitytoolkit_relyingparty_create_auth_uri` for simplest API.

pub fn identitytoolkit_relyingparty_create_auth_uri_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/createAuthUri",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_create_auth_uri_execute()` or `identitytoolkit_relyingparty_create_auth_uri`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_create_auth_uri_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_create_auth_uri_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateAuthUriResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateAuthUriResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_create_auth_uri_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_create_auth_uri_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_create_auth_uri()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_create_auth_uri_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_create_auth_uri_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAuthUriResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_create_auth_uri_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST createAuthUri
/// Creates the URI used by the IdP to authenticate the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_create_auth_uri_builder()` + `identitytoolkit_relyingparty_create_auth_uri_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_create_auth_uri_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_create_auth_uri(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAuthUriResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_create_auth_uri_builder(client)?;
    identitytoolkit_relyingparty_create_auth_uri_execute(builder)
}

/// POST deleteAccount
/// Delete user account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_delete_account_execute()` to send, or `identitytoolkit_relyingparty_delete_account` for simplest API.

pub fn identitytoolkit_relyingparty_delete_account_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/deleteAccount",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST deleteAccount
/// Delete user account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_delete_account_execute()` or `identitytoolkit_relyingparty_delete_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_delete_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_delete_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteAccountResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST deleteAccount
/// Delete user account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_delete_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_delete_account_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_delete_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_delete_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_delete_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_delete_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST deleteAccount
/// Delete user account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_delete_account_builder()` + `identitytoolkit_relyingparty_delete_account_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_delete_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_delete_account(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_delete_account_builder(client)?;
    identitytoolkit_relyingparty_delete_account_execute(builder)
}

/// POST downloadAccount
/// Batch download user accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_download_account_execute()` to send, or `identitytoolkit_relyingparty_download_account` for simplest API.

pub fn identitytoolkit_relyingparty_download_account_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/downloadAccount",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST downloadAccount
/// Batch download user accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_download_account_execute()` or `identitytoolkit_relyingparty_download_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_download_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_download_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DownloadAccountResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DownloadAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST downloadAccount
/// Batch download user accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_download_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_download_account_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_download_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_download_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_download_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_download_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST downloadAccount
/// Batch download user accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_download_account_builder()` + `identitytoolkit_relyingparty_download_account_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_download_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_download_account(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_download_account_builder(client)?;
    identitytoolkit_relyingparty_download_account_execute(builder)
}

/// POST emailLinkSignin
/// Reset password for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_email_link_signin_execute()` to send, or `identitytoolkit_relyingparty_email_link_signin` for simplest API.

pub fn identitytoolkit_relyingparty_email_link_signin_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/emailLinkSignin",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST emailLinkSignin
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_email_link_signin_execute()` or `identitytoolkit_relyingparty_email_link_signin`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_email_link_signin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_email_link_signin_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EmailLinkSigninResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EmailLinkSigninResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST emailLinkSignin
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_email_link_signin_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_email_link_signin_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_email_link_signin()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_email_link_signin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_email_link_signin_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmailLinkSigninResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_email_link_signin_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST emailLinkSignin
/// Reset password for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_email_link_signin_builder()` + `identitytoolkit_relyingparty_email_link_signin_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_email_link_signin_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_email_link_signin(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmailLinkSigninResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_email_link_signin_builder(client)?;
    identitytoolkit_relyingparty_email_link_signin_execute(builder)
}

/// POST getAccountInfo
/// Returns the account info.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_account_info_execute()` to send, or `identitytoolkit_relyingparty_get_account_info` for simplest API.

pub fn identitytoolkit_relyingparty_get_account_info_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getAccountInfo",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST getAccountInfo
/// Returns the account info.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_get_account_info_execute()` or `identitytoolkit_relyingparty_get_account_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_account_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_account_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetAccountInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetAccountInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST getAccountInfo
/// Returns the account info.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_get_account_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_get_account_info_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_get_account_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_account_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_get_account_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_get_account_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST getAccountInfo
/// Returns the account info.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_account_info_builder()` + `identitytoolkit_relyingparty_get_account_info_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_get_account_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_account_info(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_account_info_builder(client)?;
    identitytoolkit_relyingparty_get_account_info_execute(builder)
}

/// POST getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_oob_confirmation_code_execute()` to send, or `identitytoolkit_relyingparty_get_oob_confirmation_code` for simplest API.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/identitytoolkit/v3/relyingparty/getOobConfirmationCode",
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_get_oob_confirmation_code_execute()` or `identitytoolkit_relyingparty_get_oob_confirmation_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetOobConfirmationCodeResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetOobConfirmationCodeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_get_oob_confirmation_code_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_get_oob_confirmation_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetOobConfirmationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_get_oob_confirmation_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST getOobConfirmationCode
/// Get a code for user action confirmation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_oob_confirmation_code_builder()` + `identitytoolkit_relyingparty_get_oob_confirmation_code_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_get_oob_confirmation_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_oob_confirmation_code(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetOobConfirmationCodeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_oob_confirmation_code_builder(client)?;
    identitytoolkit_relyingparty_get_oob_confirmation_code_execute(builder)
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_project_config_execute()` to send, or `identitytoolkit_relyingparty_get_project_config` for simplest API.

pub fn identitytoolkit_relyingparty_get_project_config_builder<R>(
    client: &SimpleHttpClient<R>,
    delegatedProjectNumber: &Option<String>,
    projectNumber: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getProjectConfig",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delegatedProjectNumber.as_ref() {
        query_parts.push(format!("delegatedProjectNumber={}", val));
    }
    if let Some(val) = projectNumber.as_ref() {
        query_parts.push(format!("projectNumber={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_get_project_config_execute()` or `identitytoolkit_relyingparty_get_project_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_project_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetProjectConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_get_project_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_get_project_config_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_get_project_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_get_project_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_get_project_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`identitytoolkit_relyingparty_get_project_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetProjectConfigArgs {
    /// Query parameter: delegatedProjectNumber
    pub delegatedProjectNumber: Option<String>,
    /// Query parameter: projectNumber
    pub projectNumber: Option<String>,
}

/// GET getProjectConfig
/// Get project configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_project_config_builder()` + `identitytoolkit_relyingparty_get_project_config_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_get_project_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_project_config(
    client: &SimpleHttpClient,
    args: &IdentitytoolkitRelyingpartyGetProjectConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_project_config_builder(
        client,
        &args.delegatedProjectNumber,
        &args.projectNumber,
    )?;
    identitytoolkit_relyingparty_get_project_config_execute(builder)
}

/// GET publicKeys
/// Get token signing public key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_public_keys_execute()` to send, or `identitytoolkit_relyingparty_get_public_keys` for simplest API.

pub fn identitytoolkit_relyingparty_get_public_keys_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/publicKeys",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET publicKeys
/// Get token signing public key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_get_public_keys_execute()` or `identitytoolkit_relyingparty_get_public_keys`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_public_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_public_keys_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetPublicKeysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET publicKeys
/// Get token signing public key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_get_public_keys_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_get_public_keys_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_get_public_keys()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_public_keys_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_get_public_keys_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_get_public_keys_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET publicKeys
/// Get token signing public key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_public_keys_builder()` + `identitytoolkit_relyingparty_get_public_keys_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_get_public_keys_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_public_keys(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_public_keys_builder(client)?;
    identitytoolkit_relyingparty_get_public_keys_execute(builder)
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_get_recaptcha_param_execute()` to send, or `identitytoolkit_relyingparty_get_recaptcha_param` for simplest API.

pub fn identitytoolkit_relyingparty_get_recaptcha_param_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getRecaptchaParam",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_get_recaptcha_param_execute()` or `identitytoolkit_relyingparty_get_recaptcha_param`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_recaptcha_param_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_recaptcha_param_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetRecaptchaParamResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetRecaptchaParamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_get_recaptcha_param_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_get_recaptcha_param_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_get_recaptcha_param()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_get_recaptcha_param_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_get_recaptcha_param_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRecaptchaParamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_get_recaptcha_param_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET getRecaptchaParam
/// Get recaptcha secure param.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_get_recaptcha_param_builder()` + `identitytoolkit_relyingparty_get_recaptcha_param_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_get_recaptcha_param_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_get_recaptcha_param(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRecaptchaParamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_get_recaptcha_param_builder(client)?;
    identitytoolkit_relyingparty_get_recaptcha_param_execute(builder)
}

/// POST resetPassword
/// Reset password for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_reset_password_execute()` to send, or `identitytoolkit_relyingparty_reset_password` for simplest API.

pub fn identitytoolkit_relyingparty_reset_password_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/resetPassword",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST resetPassword
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_reset_password_execute()` or `identitytoolkit_relyingparty_reset_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_reset_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_reset_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResetPasswordResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetPasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST resetPassword
/// Reset password for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_reset_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_reset_password_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_reset_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_reset_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_reset_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_reset_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST resetPassword
/// Reset password for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_reset_password_builder()` + `identitytoolkit_relyingparty_reset_password_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_reset_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_reset_password(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_reset_password_builder(client)?;
    identitytoolkit_relyingparty_reset_password_execute(builder)
}

/// POST sendVerificationCode
/// Send SMS verification code.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_send_verification_code_execute()` to send, or `identitytoolkit_relyingparty_send_verification_code` for simplest API.

pub fn identitytoolkit_relyingparty_send_verification_code_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/sendVerificationCode",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST sendVerificationCode
/// Send SMS verification code.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_send_verification_code_execute()` or `identitytoolkit_relyingparty_send_verification_code`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_send_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_send_verification_code_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySendVerificationCodeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST sendVerificationCode
/// Send SMS verification code.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_send_verification_code_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_send_verification_code_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_send_verification_code()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_send_verification_code_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_send_verification_code_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_send_verification_code_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST sendVerificationCode
/// Send SMS verification code.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_send_verification_code_builder()` + `identitytoolkit_relyingparty_send_verification_code_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_send_verification_code_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_send_verification_code(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_send_verification_code_builder(client)?;
    identitytoolkit_relyingparty_send_verification_code_execute(builder)
}

/// POST setAccountInfo
/// Set account info for a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_set_account_info_execute()` to send, or `identitytoolkit_relyingparty_set_account_info` for simplest API.

pub fn identitytoolkit_relyingparty_set_account_info_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/setAccountInfo",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST setAccountInfo
/// Set account info for a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_set_account_info_execute()` or `identitytoolkit_relyingparty_set_account_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_set_account_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_account_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetAccountInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetAccountInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST setAccountInfo
/// Set account info for a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_set_account_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_set_account_info_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_set_account_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_set_account_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_set_account_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_set_account_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST setAccountInfo
/// Set account info for a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_set_account_info_builder()` + `identitytoolkit_relyingparty_set_account_info_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_set_account_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_account_info(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SetAccountInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_set_account_info_builder(client)?;
    identitytoolkit_relyingparty_set_account_info_execute(builder)
}

/// POST setProjectConfig
/// Set project configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_set_project_config_execute()` to send, or `identitytoolkit_relyingparty_set_project_config` for simplest API.

pub fn identitytoolkit_relyingparty_set_project_config_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/setProjectConfig",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST setProjectConfig
/// Set project configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_set_project_config_execute()` or `identitytoolkit_relyingparty_set_project_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_set_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_project_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySetProjectConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST setProjectConfig
/// Set project configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_set_project_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_set_project_config_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_set_project_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_set_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_set_project_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_set_project_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST setProjectConfig
/// Set project configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_set_project_config_builder()` + `identitytoolkit_relyingparty_set_project_config_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_set_project_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_set_project_config(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_set_project_config_builder(client)?;
    identitytoolkit_relyingparty_set_project_config_execute(builder)
}

/// POST signOutUser
/// Sign out user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_sign_out_user_execute()` to send, or `identitytoolkit_relyingparty_sign_out_user` for simplest API.

pub fn identitytoolkit_relyingparty_sign_out_user_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/signOutUser",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST signOutUser
/// Sign out user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_sign_out_user_execute()` or `identitytoolkit_relyingparty_sign_out_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_sign_out_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_sign_out_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySignOutUserResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST signOutUser
/// Sign out user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_sign_out_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_sign_out_user_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_sign_out_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_sign_out_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_sign_out_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_sign_out_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST signOutUser
/// Sign out user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_sign_out_user_builder()` + `identitytoolkit_relyingparty_sign_out_user_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_sign_out_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_sign_out_user(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_sign_out_user_builder(client)?;
    identitytoolkit_relyingparty_sign_out_user_execute(builder)
}

/// POST signupNewUser
/// Signup new user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_signup_new_user_execute()` to send, or `identitytoolkit_relyingparty_signup_new_user` for simplest API.

pub fn identitytoolkit_relyingparty_signup_new_user_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/signupNewUser",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST signupNewUser
/// Signup new user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_signup_new_user_execute()` or `identitytoolkit_relyingparty_signup_new_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_signup_new_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_signup_new_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignupNewUserResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupNewUserResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST signupNewUser
/// Signup new user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_signup_new_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_signup_new_user_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_signup_new_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_signup_new_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_signup_new_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupNewUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_signup_new_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST signupNewUser
/// Signup new user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_signup_new_user_builder()` + `identitytoolkit_relyingparty_signup_new_user_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_signup_new_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_signup_new_user(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupNewUserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_signup_new_user_builder(client)?;
    identitytoolkit_relyingparty_signup_new_user_execute(builder)
}

/// POST uploadAccount
/// Batch upload existing user accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_upload_account_execute()` to send, or `identitytoolkit_relyingparty_upload_account` for simplest API.

pub fn identitytoolkit_relyingparty_upload_account_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/uploadAccount",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST uploadAccount
/// Batch upload existing user accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_upload_account_execute()` or `identitytoolkit_relyingparty_upload_account`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_upload_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_upload_account_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadAccountResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadAccountResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST uploadAccount
/// Batch upload existing user accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_upload_account_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_upload_account_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_upload_account()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_upload_account_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_upload_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_upload_account_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST uploadAccount
/// Batch upload existing user accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_upload_account_builder()` + `identitytoolkit_relyingparty_upload_account_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_upload_account_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_upload_account(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAccountResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_upload_account_builder(client)?;
    identitytoolkit_relyingparty_upload_account_execute(builder)
}

/// POST verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_assertion_execute()` to send, or `identitytoolkit_relyingparty_verify_assertion` for simplest API.

pub fn identitytoolkit_relyingparty_verify_assertion_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyAssertion",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_verify_assertion_execute()` or `identitytoolkit_relyingparty_verify_assertion`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_assertion_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_assertion_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyAssertionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyAssertionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_verify_assertion_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_verify_assertion_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_verify_assertion()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_assertion_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_verify_assertion_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyAssertionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_verify_assertion_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST verifyAssertion
/// Verifies the assertion returned by the IdP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_assertion_builder()` + `identitytoolkit_relyingparty_verify_assertion_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_verify_assertion_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_assertion(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyAssertionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_assertion_builder(client)?;
    identitytoolkit_relyingparty_verify_assertion_execute(builder)
}

/// POST verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_custom_token_execute()` to send, or `identitytoolkit_relyingparty_verify_custom_token` for simplest API.

pub fn identitytoolkit_relyingparty_verify_custom_token_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyCustomToken",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_verify_custom_token_execute()` or `identitytoolkit_relyingparty_verify_custom_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_custom_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_custom_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyCustomTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyCustomTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_verify_custom_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_verify_custom_token_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_verify_custom_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_custom_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_verify_custom_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyCustomTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_verify_custom_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST verifyCustomToken
/// Verifies the developer asserted ID token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_custom_token_builder()` + `identitytoolkit_relyingparty_verify_custom_token_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_verify_custom_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_custom_token(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyCustomTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_custom_token_builder(client)?;
    identitytoolkit_relyingparty_verify_custom_token_execute(builder)
}

/// POST verifyPassword
/// Verifies the user entered password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_password_execute()` to send, or `identitytoolkit_relyingparty_verify_password` for simplest API.

pub fn identitytoolkit_relyingparty_verify_password_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyPassword",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST verifyPassword
/// Verifies the user entered password.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_verify_password_execute()` or `identitytoolkit_relyingparty_verify_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyPasswordResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyPasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST verifyPassword
/// Verifies the user entered password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_verify_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_verify_password_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_verify_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_verify_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_verify_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST verifyPassword
/// Verifies the user entered password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_password_builder()` + `identitytoolkit_relyingparty_verify_password_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_verify_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_password(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_password_builder(client)?;
    identitytoolkit_relyingparty_verify_password_execute(builder)
}

/// POST verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `identitytoolkit_relyingparty_verify_phone_number_execute()` to send, or `identitytoolkit_relyingparty_verify_phone_number` for simplest API.

pub fn identitytoolkit_relyingparty_verify_phone_number_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyPhoneNumber",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `identitytoolkit_relyingparty_verify_phone_number_execute()` or `identitytoolkit_relyingparty_verify_phone_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_phone_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_phone_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `identitytoolkit_relyingparty_verify_phone_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `identitytoolkit_relyingparty_verify_phone_number_task()`.
/// For the simplest API, use `identitytoolkit_relyingparty_verify_phone_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `identitytoolkit_relyingparty_verify_phone_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn identitytoolkit_relyingparty_verify_phone_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = identitytoolkit_relyingparty_verify_phone_number_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST verifyPhoneNumber
/// Verifies ownership of a phone number and `creates/updates` the user account accordingly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `identitytoolkit_relyingparty_verify_phone_number_builder()` + `identitytoolkit_relyingparty_verify_phone_number_execute()`.
/// For task-level control, use `identitytoolkit_relyingparty_verify_phone_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn identitytoolkit_relyingparty_verify_phone_number(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = identitytoolkit_relyingparty_verify_phone_number_builder(client)?;
    identitytoolkit_relyingparty_verify_phone_number_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for IdentitytoolkitRelyingpartyGetProjectConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for IdentitytoolkitRelyingpartyGetProjectConfigResponse with IdentitytoolkitRelyingpartyGetProjectConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IdentitytoolkitRelyingpartyGetProjectConfigArgs>
    for IdentitytoolkitRelyingpartyGetProjectConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &IdentitytoolkitRelyingpartyGetProjectConfigArgs,
    ) -> String {
        "gcp::identitytoolkit::IdentitytoolkitRelyingpartyGetProjectConfigResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::identitytoolkit::IdentitytoolkitRelyingpartyGetProjectConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
