//! Auto-generated API clients for gcp/blogger.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/blogger`

#![cfg(feature = "gcp/blogger")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blog_user_infos_get_execute()` to send, or `blogger_blog_user_infos_get` for simplest API.

pub fn blogger_blog_user_infos_get_builder(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    maxPosts: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}",
        userId, blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPosts {
        query_parts.push(format!("maxPosts={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_blog_user_infos_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_blog_user_infos_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blog_user_infos_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BlogUserInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blog_user_infos_get_builder()` + `blogger_blog_user_infos_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blog_user_infos_get(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    maxPosts: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = blogger_blog_user_infos_get_builder(client, userId, blogId, maxPosts)?;
    blogger_blog_user_infos_get_execute(builder)
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_get_execute()` to send, or `blogger_blogs_get` for simplest API.

pub fn blogger_blogs_get_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    maxPosts: Option<i32>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/{}", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPosts {
        query_parts.push(format!("maxPosts={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_blogs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_blogs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Blog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_get_builder()` + `blogger_blogs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get(
    client: &SimpleHttpClient,
    blogId: &str,
    maxPosts: Option<i32>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_blogs_get_builder(client, blogId, maxPosts, view)?;
    blogger_blogs_get_execute(builder)
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_get_by_url_execute()` to send, or `blogger_blogs_get_by_url` for simplest API.

pub fn blogger_blogs_get_by_url_builder(
    client: &SimpleHttpClient,
    url: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/byurl", url,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_blogs_get_by_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_blogs_get_by_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_by_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Blog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_get_by_url_builder()` + `blogger_blogs_get_by_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_by_url(
    client: &SimpleHttpClient,
    url: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_blogs_get_by_url_builder(client, url, view)?;
    blogger_blogs_get_by_url_execute(builder)
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_list_by_user_execute()` to send, or `blogger_blogs_list_by_user` for simplest API.

pub fn blogger_blogs_list_by_user_builder(
    client: &SimpleHttpClient,
    userId: &str,
    fetchUserInfo: Option<bool>,
    role: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/users/{}/blogs", userId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchUserInfo {
        query_parts.push(format!("fetchUserInfo={}", val));
    }
    if let Some(val) = role {
        query_parts.push(format!("role={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_blogs_list_by_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_blogs_list_by_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_list_by_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BlogList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_list_by_user_builder()` + `blogger_blogs_list_by_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_list_by_user(
    client: &SimpleHttpClient,
    userId: &str,
    fetchUserInfo: Option<bool>,
    role: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_blogs_list_by_user_builder(client, userId, fetchUserInfo, role, status, view)?;
    blogger_blogs_list_by_user_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_approve_execute()` to send, or `blogger_comments_approve` for simplest API.

pub fn blogger_comments_approve_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/approve",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_approve_builder()` + `blogger_comments_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_approve(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_approve_builder(client, blogId, postId, commentId)?;
    blogger_comments_approve_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_delete_execute()` to send, or `blogger_comments_delete` for simplest API.

pub fn blogger_comments_delete_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_delete_builder()` + `blogger_comments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_delete(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_delete_builder(client, blogId, postId, commentId)?;
    blogger_comments_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_get_execute()` to send, or `blogger_comments_get` for simplest API.

pub fn blogger_comments_get_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}",
        blogId, postId, commentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_get_builder()` + `blogger_comments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_get(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_get_builder(client, blogId, postId, commentId, view)?;
    blogger_comments_get_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_list_execute()` to send, or `blogger_comments_list` for simplest API.

pub fn blogger_comments_list_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_list_builder()` + `blogger_comments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_list_builder(
        client,
        blogId,
        postId,
        endDate,
        fetchBodies,
        maxResults,
        pageToken,
        startDate,
        status,
        view,
    )?;
    blogger_comments_list_execute(builder)
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_list_by_blog_execute()` to send, or `blogger_comments_list_by_blog` for simplest API.

pub fn blogger_comments_list_by_blog_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/comments",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_list_by_blog_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_list_by_blog_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_by_blog_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_list_by_blog_builder()` + `blogger_comments_list_by_blog_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_by_blog(
    client: &SimpleHttpClient,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_list_by_blog_builder(
        client,
        blogId,
        endDate,
        fetchBodies,
        maxResults,
        pageToken,
        startDate,
        status,
    )?;
    blogger_comments_list_by_blog_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_mark_as_spam_execute()` to send, or `blogger_comments_mark_as_spam` for simplest API.

pub fn blogger_comments_mark_as_spam_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/spam",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_mark_as_spam_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_mark_as_spam_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_mark_as_spam_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_mark_as_spam_builder()` + `blogger_comments_mark_as_spam_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_mark_as_spam(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_mark_as_spam_builder(client, blogId, postId, commentId)?;
    blogger_comments_mark_as_spam_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_remove_content_execute()` to send, or `blogger_comments_remove_content` for simplest API.

pub fn blogger_comments_remove_content_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/removecontent",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_comments_remove_content_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_comments_remove_content_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_remove_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_remove_content_builder()` + `blogger_comments_remove_content_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_remove_content(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    commentId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_remove_content_builder(client, blogId, postId, commentId)?;
    blogger_comments_remove_content_execute(builder)
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_page_views_get_execute()` to send, or `blogger_page_views_get` for simplest API.

pub fn blogger_page_views_get_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    range: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pageviews",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = range {
        query_parts.push(format!("range={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_page_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_page_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_page_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Pageviews>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Pageviews = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_page_views_get_builder()` + `blogger_page_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_page_views_get(
    client: &SimpleHttpClient,
    blogId: &str,
    range: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Pageviews>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_page_views_get_builder(client, blogId, range)?;
    blogger_page_views_get_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_delete_execute()` to send, or `blogger_pages_delete` for simplest API.

pub fn blogger_pages_delete_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    useTrash: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = useTrash {
        query_parts.push(format!("useTrash={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_delete_builder()` + `blogger_pages_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_delete(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    useTrash: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_delete_builder(client, blogId, pageId, useTrash)?;
    blogger_pages_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_get_execute()` to send, or `blogger_pages_get` for simplest API.

pub fn blogger_pages_get_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_get_builder()` + `blogger_pages_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_get(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_get_builder(client, blogId, pageId, view)?;
    blogger_pages_get_execute(builder)
}

/// GET v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_insert_execute()` to send, or `blogger_pages_insert` for simplest API.

pub fn blogger_pages_insert_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    isDraft: Option<bool>,
    body: &Page,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/{}/pages", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isDraft {
        query_parts.push(format!("isDraft={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_insert_builder()` + `blogger_pages_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_insert(
    client: &SimpleHttpClient,
    blogId: &str,
    isDraft: Option<bool>,
    body: &Page,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_insert_builder(client, blogId, isDraft, body)?;
    blogger_pages_insert_execute(builder)
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_list_execute()` to send, or `blogger_pages_list` for simplest API.

pub fn blogger_pages_list_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/{}/pages", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_list_builder()` + `blogger_pages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_list(
    client: &SimpleHttpClient,
    blogId: &str,
    fetchBodies: Option<bool>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_list_builder(
        client,
        blogId,
        fetchBodies,
        maxResults,
        pageToken,
        status,
        view,
    )?;
    blogger_pages_list_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_patch_execute()` to send, or `blogger_pages_patch` for simplest API.

pub fn blogger_pages_patch_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Page,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publish {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_patch_builder()` + `blogger_pages_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_patch(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Page,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_patch_builder(client, blogId, pageId, publish, revert, body)?;
    blogger_pages_patch_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_publish_execute()` to send, or `blogger_pages_publish` for simplest API.

pub fn blogger_pages_publish_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}/publish",
        blogId, pageId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_publish_builder()` + `blogger_pages_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_publish(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_publish_builder(client, blogId, pageId)?;
    blogger_pages_publish_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_revert_execute()` to send, or `blogger_pages_revert` for simplest API.

pub fn blogger_pages_revert_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}/revert",
        blogId, pageId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_revert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_revert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_revert_builder()` + `blogger_pages_revert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_revert(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_revert_builder(client, blogId, pageId)?;
    blogger_pages_revert_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_update_execute()` to send, or `blogger_pages_update` for simplest API.

pub fn blogger_pages_update_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Page,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publish {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_pages_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_pages_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_update_builder()` + `blogger_pages_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_update(
    client: &SimpleHttpClient,
    blogId: &str,
    pageId: &str,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Page,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_update_builder(client, blogId, pageId, publish, revert, body)?;
    blogger_pages_update_execute(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_post_user_infos_get_execute()` to send, or `blogger_post_user_infos_get` for simplest API.

pub fn blogger_post_user_infos_get_builder(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    postId: &str,
    maxComments: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}/posts/{}",
        userId, blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxComments {
        query_parts.push(format!("maxComments={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_post_user_infos_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_post_user_infos_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostUserInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_post_user_infos_get_builder()` + `blogger_post_user_infos_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_get(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    postId: &str,
    maxComments: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = blogger_post_user_infos_get_builder(client, userId, blogId, postId, maxComments)?;
    blogger_post_user_infos_get_execute(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_post_user_infos_list_execute()` to send, or `blogger_post_user_infos_list` for simplest API.

pub fn blogger_post_user_infos_list_builder(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    labels: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}/posts",
        userId, blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = labels {
        query_parts.push(format!("labels={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_post_user_infos_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_post_user_infos_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfosList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostUserInfosList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_post_user_infos_list_builder()` + `blogger_post_user_infos_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_list(
    client: &SimpleHttpClient,
    userId: &str,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    labels: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfosList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = blogger_post_user_infos_list_builder(
        client,
        userId,
        blogId,
        endDate,
        fetchBodies,
        labels,
        maxResults,
        orderBy,
        pageToken,
        startDate,
        status,
        view,
    )?;
    blogger_post_user_infos_list_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_delete_execute()` to send, or `blogger_posts_delete` for simplest API.

pub fn blogger_posts_delete_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    useTrash: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = useTrash {
        query_parts.push(format!("useTrash={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_delete_builder()` + `blogger_posts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_delete(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    useTrash: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_delete_builder(client, blogId, postId, useTrash)?;
    blogger_posts_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_get_execute()` to send, or `blogger_posts_get` for simplest API.

pub fn blogger_posts_get_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_get_builder()` + `blogger_posts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_get_builder(
        client,
        blogId,
        postId,
        fetchBody,
        fetchImages,
        maxComments,
        view,
    )?;
    blogger_posts_get_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_get_by_path_execute()` to send, or `blogger_posts_get_by_path` for simplest API.

pub fn blogger_posts_get_by_path_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    path: &str,
    maxComments: Option<i32>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/bypath",
        blogId, path,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxComments {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_get_by_path_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_get_by_path_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_by_path_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_get_by_path_builder()` + `blogger_posts_get_by_path_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_by_path(
    client: &SimpleHttpClient,
    blogId: &str,
    path: &str,
    maxComments: Option<i32>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_get_by_path_builder(client, blogId, path, maxComments, view)?;
    blogger_posts_get_by_path_execute(builder)
}

/// GET v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_insert_execute()` to send, or `blogger_posts_insert` for simplest API.

pub fn blogger_posts_insert_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    isDraft: Option<bool>,
    body: &Post,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/{}/posts", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = isDraft {
        query_parts.push(format!("isDraft={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_insert_builder()` + `blogger_posts_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_insert(
    client: &SimpleHttpClient,
    blogId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    isDraft: Option<bool>,
    body: &Post,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_posts_insert_builder(client, blogId, fetchBody, fetchImages, isDraft, body)?;
    blogger_posts_insert_execute(builder)
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_list_execute()` to send, or `blogger_posts_list` for simplest API.

pub fn blogger_posts_list_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    fetchImages: Option<bool>,
    labels: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    sortOption: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/blogs/{}/posts", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = fetchImages {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = labels {
        query_parts.push(format!("labels={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortOption {
        query_parts.push(format!("sortOption={}", val));
    }
    if let Some(val) = startDate {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_list_builder()` + `blogger_posts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_list(
    client: &SimpleHttpClient,
    blogId: &str,
    endDate: Option<&str>,
    fetchBodies: Option<bool>,
    fetchImages: Option<bool>,
    labels: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    sortOption: Option<&str>,
    startDate: Option<&str>,
    status: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_list_builder(
        client,
        blogId,
        endDate,
        fetchBodies,
        fetchImages,
        labels,
        maxResults,
        orderBy,
        pageToken,
        sortOption,
        startDate,
        status,
        view,
    )?;
    blogger_posts_list_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_patch_execute()` to send, or `blogger_posts_patch` for simplest API.

pub fn blogger_posts_patch_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Post,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = publish {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_patch_builder()` + `blogger_posts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_patch(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Post,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_patch_builder(
        client,
        blogId,
        postId,
        fetchBody,
        fetchImages,
        maxComments,
        publish,
        revert,
        body,
    )?;
    blogger_posts_patch_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_publish_execute()` to send, or `blogger_posts_publish` for simplest API.

pub fn blogger_posts_publish_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    publishDate: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/publish",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publishDate {
        query_parts.push(format!("publishDate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_publish_builder()` + `blogger_posts_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_publish(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    publishDate: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_publish_builder(client, blogId, postId, publishDate)?;
    blogger_posts_publish_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_revert_execute()` to send, or `blogger_posts_revert` for simplest API.

pub fn blogger_posts_revert_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/revert",
        blogId, postId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_revert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_revert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_revert_builder()` + `blogger_posts_revert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_revert(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_revert_builder(client, blogId, postId)?;
    blogger_posts_revert_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_search_execute()` to send, or `blogger_posts_search` for simplest API.

pub fn blogger_posts_search_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    q: &str,
    fetchBodies: Option<bool>,
    orderBy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/search",
        blogId, q,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBodies {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_search_builder()` + `blogger_posts_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_search(
    client: &SimpleHttpClient,
    blogId: &str,
    q: &str,
    fetchBodies: Option<bool>,
    orderBy: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_search_builder(client, blogId, q, fetchBodies, orderBy)?;
    blogger_posts_search_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_update_execute()` to send, or `blogger_posts_update` for simplest API.

pub fn blogger_posts_update_builder(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Post,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = publish {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_posts_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_posts_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_update_builder()` + `blogger_posts_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_update(
    client: &SimpleHttpClient,
    blogId: &str,
    postId: &str,
    fetchBody: Option<bool>,
    fetchImages: Option<bool>,
    maxComments: Option<i32>,
    publish: Option<bool>,
    revert: Option<bool>,
    body: &Post,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_update_builder(
        client,
        blogId,
        postId,
        fetchBody,
        fetchImages,
        maxComments,
        publish,
        revert,
        body,
    )?;
    blogger_posts_update_execute(builder)
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_users_get_execute()` to send, or `blogger_users_get` for simplest API.

pub fn blogger_users_get_builder(
    client: &SimpleHttpClient,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://blogger.googleapis.com/v3/users/{}", userId,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `blogger_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from blogger_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_users_get_builder()` + `blogger_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_users_get(
    client: &SimpleHttpClient,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_users_get_builder(client, userId)?;
    blogger_users_get_execute(builder)
}
