//! Auto-generated API clients for gcp/blogger.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blog_user_infos_get_execute()` to send, or `blogger_blog_user_infos_get` for simplest API.

pub fn blogger_blog_user_infos_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    blogId: &String,
    maxPosts: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}",
        userId, blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPosts.as_ref() {
        query_parts.push(format!("maxPosts={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_blog_user_infos_get_execute()` or `blogger_blog_user_infos_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blog_user_infos_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blog_user_infos_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BlogUserInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BlogUserInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_blog_user_infos_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_blog_user_infos_get_task()`.
/// For the simplest API, use `blogger_blog_user_infos_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blog_user_infos_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_blog_user_infos_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = blogger_blog_user_infos_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_blog_user_infos_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerBlogUserInfosGetArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: maxPosts
    pub maxPosts: Option<Option<String>>,
}

/// GET v3/users/{userId}/blogs/{blogId}
/// Gets one blog and user info pair by blog id and user id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blog_user_infos_get_builder()` + `blogger_blog_user_infos_get_execute()`.
/// For task-level control, use `blogger_blog_user_infos_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blog_user_infos_get(
    client: &SimpleHttpClient,
    args: &BloggerBlogUserInfosGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        blogger_blog_user_infos_get_builder(client, &args.userId, &args.blogId, &args.maxPosts)?;
    blogger_blog_user_infos_get_execute(builder)
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_get_execute()` to send, or `blogger_blogs_get` for simplest API.

pub fn blogger_blogs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    maxPosts: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/{}", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxPosts.as_ref() {
        query_parts.push(format!("maxPosts={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_blogs_get_execute()` or `blogger_blogs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Blog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Blog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_blogs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_blogs_get_task()`.
/// For the simplest API, use `blogger_blogs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_blogs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_blogs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_blogs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerBlogsGetArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: maxPosts
    pub maxPosts: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}
/// Gets a blog by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_get_builder()` + `blogger_blogs_get_execute()`.
/// For task-level control, use `blogger_blogs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get(
    client: &SimpleHttpClient,
    args: &BloggerBlogsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_blogs_get_builder(client, &args.blogId, &args.maxPosts, &args.view)?;
    blogger_blogs_get_execute(builder)
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_get_by_url_execute()` to send, or `blogger_blogs_get_by_url` for simplest API.

pub fn blogger_blogs_get_by_url_builder<R>(
    client: &SimpleHttpClient<R>,
    url: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/byurl",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = url.as_ref() {
        query_parts.push(format!("url={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_blogs_get_by_url_execute()` or `blogger_blogs_get_by_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_get_by_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_by_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Blog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Blog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_blogs_get_by_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_blogs_get_by_url_task()`.
/// For the simplest API, use `blogger_blogs_get_by_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_get_by_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_blogs_get_by_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_blogs_get_by_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_blogs_get_by_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerBlogsGetByUrlArgs {
    /// Query parameter: url
    pub url: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/byurl
/// Gets a blog by url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_get_by_url_builder()` + `blogger_blogs_get_by_url_execute()`.
/// For task-level control, use `blogger_blogs_get_by_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_get_by_url(
    client: &SimpleHttpClient,
    args: &BloggerBlogsGetByUrlArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Blog>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_blogs_get_by_url_builder(client, &args.url, &args.view)?;
    blogger_blogs_get_by_url_execute(builder)
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_blogs_list_by_user_execute()` to send, or `blogger_blogs_list_by_user` for simplest API.

pub fn blogger_blogs_list_by_user_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    fetchUserInfo: &Option<Option<String>>,
    role: &Option<Option<String>>,
    status: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/users/{}/blogs", userId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchUserInfo.as_ref() {
        query_parts.push(format!("fetchUserInfo={}", val));
    }
    if let Some(val) = role.as_ref() {
        query_parts.push(format!("role={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_blogs_list_by_user_execute()` or `blogger_blogs_list_by_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_list_by_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_list_by_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BlogList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BlogList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_blogs_list_by_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_blogs_list_by_user_task()`.
/// For the simplest API, use `blogger_blogs_list_by_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_blogs_list_by_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_blogs_list_by_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_blogs_list_by_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_blogs_list_by_user`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerBlogsListByUserArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Query parameter: fetchUserInfo
    pub fetchUserInfo: Option<Option<String>>,
    /// Query parameter: role
    pub role: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/users/{userId}/blogs
/// Lists blogs by user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_blogs_list_by_user_builder()` + `blogger_blogs_list_by_user_execute()`.
/// For task-level control, use `blogger_blogs_list_by_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_blogs_list_by_user(
    client: &SimpleHttpClient,
    args: &BloggerBlogsListByUserArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BlogList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_blogs_list_by_user_builder(
        client,
        &args.userId,
        &args.fetchUserInfo,
        &args.role,
        &args.status,
        &args.view,
    )?;
    blogger_blogs_list_by_user_execute(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_approve_execute()` to send, or `blogger_comments_approve` for simplest API.

pub fn blogger_comments_approve_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    commentId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/approve",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_approve_execute()` or `blogger_comments_approve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_approve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_approve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_approve_task()`.
/// For the simplest API, use `blogger_comments_approve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_approve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsApproveArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: commentId
    pub commentId: String,
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/approve
/// Marks a comment as not spam by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_approve_builder()` + `blogger_comments_approve_execute()`.
/// For task-level control, use `blogger_comments_approve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_approve(
    client: &SimpleHttpClient,
    args: &BloggerCommentsApproveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_comments_approve_builder(client, &args.blogId, &args.postId, &args.commentId)?;
    blogger_comments_approve_execute(builder)
}

/// DELETE v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_delete_execute()` to send, or `blogger_comments_delete` for simplest API.

pub fn blogger_comments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    commentId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_delete_execute()` or `blogger_comments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_delete_task()`.
/// For the simplest API, use `blogger_comments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsDeleteArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: commentId
    pub commentId: String,
}

/// DELETE v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Deletes a comment by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_delete_builder()` + `blogger_comments_delete_execute()`.
/// For task-level control, use `blogger_comments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_delete(
    client: &SimpleHttpClient,
    args: &BloggerCommentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_comments_delete_builder(client, &args.blogId, &args.postId, &args.commentId)?;
    blogger_comments_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_get_execute()` to send, or `blogger_comments_get` for simplest API.

pub fn blogger_comments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    commentId: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}",
        blogId, postId, commentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_get_execute()` or `blogger_comments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_get_task()`.
/// For the simplest API, use `blogger_comments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsGetArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: commentId
    pub commentId: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments/{commentId}
/// Gets a comment by id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_get_builder()` + `blogger_comments_get_execute()`.
/// For task-level control, use `blogger_comments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_get(
    client: &SimpleHttpClient,
    args: &BloggerCommentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_get_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.commentId,
        &args.view,
    )?;
    blogger_comments_get_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_list_execute()` to send, or `blogger_comments_list` for simplest API.

pub fn blogger_comments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    endDate: &Option<Option<String>>,
    fetchBodies: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    startDate: &Option<Option<String>>,
    status: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate.as_ref() {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate.as_ref() {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_list_execute()` or `blogger_comments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_list_task()`.
/// For the simplest API, use `blogger_comments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsListArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: endDate
    pub endDate: Option<Option<String>>,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: startDate
    pub startDate: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts/{postId}/comments
/// Lists comments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_list_builder()` + `blogger_comments_list_execute()`.
/// For task-level control, use `blogger_comments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list(
    client: &SimpleHttpClient,
    args: &BloggerCommentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_list_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.endDate,
        &args.fetchBodies,
        &args.maxResults,
        &args.pageToken,
        &args.startDate,
        &args.status,
        &args.view,
    )?;
    blogger_comments_list_execute(builder)
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_list_by_blog_execute()` to send, or `blogger_comments_list_by_blog` for simplest API.

pub fn blogger_comments_list_by_blog_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    endDate: &Option<Option<String>>,
    fetchBodies: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    startDate: &Option<Option<String>>,
    status: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/comments",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate.as_ref() {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate.as_ref() {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_list_by_blog_execute()` or `blogger_comments_list_by_blog`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_list_by_blog_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_by_blog_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_list_by_blog_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_list_by_blog_task()`.
/// For the simplest API, use `blogger_comments_list_by_blog()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_list_by_blog_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_list_by_blog_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_list_by_blog_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_list_by_blog`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsListByBlogArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: endDate
    pub endDate: Option<Option<String>>,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: startDate
    pub startDate: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/comments
/// Lists comments by blog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_list_by_blog_builder()` + `blogger_comments_list_by_blog_execute()`.
/// For task-level control, use `blogger_comments_list_by_blog_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_list_by_blog(
    client: &SimpleHttpClient,
    args: &BloggerCommentsListByBlogArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_list_by_blog_builder(
        client,
        &args.blogId,
        &args.endDate,
        &args.fetchBodies,
        &args.maxResults,
        &args.pageToken,
        &args.startDate,
        &args.status,
    )?;
    blogger_comments_list_by_blog_execute(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_mark_as_spam_execute()` to send, or `blogger_comments_mark_as_spam` for simplest API.

pub fn blogger_comments_mark_as_spam_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    commentId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/spam",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_mark_as_spam_execute()` or `blogger_comments_mark_as_spam`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_mark_as_spam_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_mark_as_spam_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_mark_as_spam_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_mark_as_spam_task()`.
/// For the simplest API, use `blogger_comments_mark_as_spam()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_mark_as_spam_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_mark_as_spam_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_mark_as_spam_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_mark_as_spam`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsMarkAsSpamArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: commentId
    pub commentId: String,
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/spam
/// Marks a comment as spam by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_mark_as_spam_builder()` + `blogger_comments_mark_as_spam_execute()`.
/// For task-level control, use `blogger_comments_mark_as_spam_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_mark_as_spam(
    client: &SimpleHttpClient,
    args: &BloggerCommentsMarkAsSpamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_comments_mark_as_spam_builder(client, &args.blogId, &args.postId, &args.commentId)?;
    blogger_comments_mark_as_spam_execute(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_comments_remove_content_execute()` to send, or `blogger_comments_remove_content` for simplest API.

pub fn blogger_comments_remove_content_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    commentId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/comments/{}/removecontent",
        blogId, postId, commentId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_comments_remove_content_execute()` or `blogger_comments_remove_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_remove_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_remove_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_comments_remove_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_comments_remove_content_task()`.
/// For the simplest API, use `blogger_comments_remove_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_comments_remove_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_comments_remove_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_comments_remove_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_comments_remove_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerCommentsRemoveContentArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: commentId
    pub commentId: String,
}

/// POST v3/blogs/{blogId}/posts/{postId}/comments/{commentId}/removecontent
/// Removes the content of a comment by blog id, post id and comment id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_comments_remove_content_builder()` + `blogger_comments_remove_content_execute()`.
/// For task-level control, use `blogger_comments_remove_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_comments_remove_content(
    client: &SimpleHttpClient,
    args: &BloggerCommentsRemoveContentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_comments_remove_content_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.commentId,
    )?;
    blogger_comments_remove_content_execute(builder)
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_page_views_get_execute()` to send, or `blogger_page_views_get` for simplest API.

pub fn blogger_page_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    range: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pageviews",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = range.as_ref() {
        query_parts.push(format!("range={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_page_views_get_execute()` or `blogger_page_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_page_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_page_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Pageviews>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Pageviews = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_page_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_page_views_get_task()`.
/// For the simplest API, use `blogger_page_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_page_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_page_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Pageviews>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_page_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_page_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPageViewsGetArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: range
    pub range: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/pageviews
/// Gets page views by blog id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_page_views_get_builder()` + `blogger_page_views_get_execute()`.
/// For task-level control, use `blogger_page_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_page_views_get(
    client: &SimpleHttpClient,
    args: &BloggerPageViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Pageviews>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_page_views_get_builder(client, &args.blogId, &args.range)?;
    blogger_page_views_get_execute(builder)
}

/// DELETE v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_delete_execute()` to send, or `blogger_pages_delete` for simplest API.

pub fn blogger_pages_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
    useTrash: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = useTrash.as_ref() {
        query_parts.push(format!("useTrash={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_delete_execute()` or `blogger_pages_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_delete_task()`.
/// For the simplest API, use `blogger_pages_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesDeleteArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Query parameter: useTrash
    pub useTrash: Option<Option<String>>,
}

/// DELETE v3/blogs/{blogId}/pages/{pageId}
/// Deletes a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_delete_builder()` + `blogger_pages_delete_execute()`.
/// For task-level control, use `blogger_pages_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_delete(
    client: &SimpleHttpClient,
    args: &BloggerPagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_delete_builder(client, &args.blogId, &args.pageId, &args.useTrash)?;
    blogger_pages_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_get_execute()` to send, or `blogger_pages_get` for simplest API.

pub fn blogger_pages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_get_execute()` or `blogger_pages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_get_task()`.
/// For the simplest API, use `blogger_pages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesGetArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/pages/{pageId}
/// Gets a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_get_builder()` + `blogger_pages_get_execute()`.
/// For task-level control, use `blogger_pages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_get(
    client: &SimpleHttpClient,
    args: &BloggerPagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_get_builder(client, &args.blogId, &args.pageId, &args.view)?;
    blogger_pages_get_execute(builder)
}

/// POST v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_insert_execute()` to send, or `blogger_pages_insert` for simplest API.

pub fn blogger_pages_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    isDraft: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/{}/pages", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = isDraft.as_ref() {
        query_parts.push(format!("isDraft={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_insert_execute()` or `blogger_pages_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_insert_task()`.
/// For the simplest API, use `blogger_pages_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesInsertArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: isDraft
    pub isDraft: Option<Option<String>>,
}

/// POST v3/blogs/{blogId}/pages
/// Inserts a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_insert_builder()` + `blogger_pages_insert_execute()`.
/// For task-level control, use `blogger_pages_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_insert(
    client: &SimpleHttpClient,
    args: &BloggerPagesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_insert_builder(client, &args.blogId, &args.isDraft)?;
    blogger_pages_insert_execute(builder)
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_list_execute()` to send, or `blogger_pages_list` for simplest API.

pub fn blogger_pages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    fetchBodies: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    status: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/{}/pages", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_list_execute()` or `blogger_pages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PageList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_list_task()`.
/// For the simplest API, use `blogger_pages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesListArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/pages
/// Lists pages.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_list_builder()` + `blogger_pages_list_execute()`.
/// For task-level control, use `blogger_pages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_list(
    client: &SimpleHttpClient,
    args: &BloggerPagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_list_builder(
        client,
        &args.blogId,
        &args.fetchBodies,
        &args.maxResults,
        &args.pageToken,
        &args.status,
        &args.view,
    )?;
    blogger_pages_list_execute(builder)
}

/// PATCH v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_patch_execute()` to send, or `blogger_pages_patch` for simplest API.

pub fn blogger_pages_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
    publish: &Option<Option<String>>,
    revert: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publish.as_ref() {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert.as_ref() {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_patch_execute()` or `blogger_pages_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_patch_task()`.
/// For the simplest API, use `blogger_pages_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesPatchArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Query parameter: publish
    pub publish: Option<Option<String>>,
    /// Query parameter: revert
    pub revert: Option<Option<String>>,
}

/// PATCH v3/blogs/{blogId}/pages/{pageId}
/// Patches a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_patch_builder()` + `blogger_pages_patch_execute()`.
/// For task-level control, use `blogger_pages_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_patch(
    client: &SimpleHttpClient,
    args: &BloggerPagesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_patch_builder(
        client,
        &args.blogId,
        &args.pageId,
        &args.publish,
        &args.revert,
    )?;
    blogger_pages_patch_execute(builder)
}

/// POST v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_publish_execute()` to send, or `blogger_pages_publish` for simplest API.

pub fn blogger_pages_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}/publish",
        blogId, pageId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_publish_execute()` or `blogger_pages_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_publish_task()`.
/// For the simplest API, use `blogger_pages_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesPublishArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// POST v3/blogs/{blogId}/pages/{pageId}/publish
/// Publishes a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_publish_builder()` + `blogger_pages_publish_execute()`.
/// For task-level control, use `blogger_pages_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_publish(
    client: &SimpleHttpClient,
    args: &BloggerPagesPublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_publish_builder(client, &args.blogId, &args.pageId)?;
    blogger_pages_publish_execute(builder)
}

/// POST v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_revert_execute()` to send, or `blogger_pages_revert` for simplest API.

pub fn blogger_pages_revert_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}/revert",
        blogId, pageId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_revert_execute()` or `blogger_pages_revert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_revert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_revert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_revert_task()`.
/// For the simplest API, use `blogger_pages_revert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_revert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_revert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesRevertArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
}

/// POST v3/blogs/{blogId}/pages/{pageId}/revert
/// Reverts a published or scheduled page to draft state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_revert_builder()` + `blogger_pages_revert_execute()`.
/// For task-level control, use `blogger_pages_revert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_revert(
    client: &SimpleHttpClient,
    args: &BloggerPagesRevertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_revert_builder(client, &args.blogId, &args.pageId)?;
    blogger_pages_revert_execute(builder)
}

/// PUT v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_pages_update_execute()` to send, or `blogger_pages_update` for simplest API.

pub fn blogger_pages_update_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    pageId: &String,
    publish: &Option<Option<String>>,
    revert: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/pages/{}",
        blogId, pageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publish.as_ref() {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert.as_ref() {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_pages_update_execute()` or `blogger_pages_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_pages_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_pages_update_task()`.
/// For the simplest API, use `blogger_pages_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_pages_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_pages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_pages_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_pages_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPagesUpdateArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: pageId
    pub pageId: String,
    /// Query parameter: publish
    pub publish: Option<Option<String>>,
    /// Query parameter: revert
    pub revert: Option<Option<String>>,
}

/// PUT v3/blogs/{blogId}/pages/{pageId}
/// Updates a page by blog id and page id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_pages_update_builder()` + `blogger_pages_update_execute()`.
/// For task-level control, use `blogger_pages_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_pages_update(
    client: &SimpleHttpClient,
    args: &BloggerPagesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_pages_update_builder(
        client,
        &args.blogId,
        &args.pageId,
        &args.publish,
        &args.revert,
    )?;
    blogger_pages_update_execute(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_post_user_infos_get_execute()` to send, or `blogger_post_user_infos_get` for simplest API.

pub fn blogger_post_user_infos_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    blogId: &String,
    postId: &String,
    maxComments: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}/posts/{}",
        userId, blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxComments.as_ref() {
        query_parts.push(format!("maxComments={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_post_user_infos_get_execute()` or `blogger_post_user_infos_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_post_user_infos_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostUserInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostUserInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_post_user_infos_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_post_user_infos_get_task()`.
/// For the simplest API, use `blogger_post_user_infos_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_post_user_infos_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_post_user_infos_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = blogger_post_user_infos_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_post_user_infos_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostUserInfosGetArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: maxComments
    pub maxComments: Option<Option<String>>,
}

/// GET v3/users/{userId}/blogs/{blogId}/posts/{postId}
/// Gets one post and user info pair, by post_id and user_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_post_user_infos_get_builder()` + `blogger_post_user_infos_get_execute()`.
/// For task-level control, use `blogger_post_user_infos_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_get(
    client: &SimpleHttpClient,
    args: &BloggerPostUserInfosGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = blogger_post_user_infos_get_builder(
        client,
        &args.userId,
        &args.blogId,
        &args.postId,
        &args.maxComments,
    )?;
    blogger_post_user_infos_get_execute(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_post_user_infos_list_execute()` to send, or `blogger_post_user_infos_list` for simplest API.

pub fn blogger_post_user_infos_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    blogId: &String,
    endDate: &Option<Option<String>>,
    fetchBodies: &Option<Option<String>>,
    labels: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    startDate: &Option<Option<String>>,
    status: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/users/{}/blogs/{}/posts",
        userId, blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate.as_ref() {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = labels.as_ref() {
        query_parts.push(format!("labels={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startDate.as_ref() {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_post_user_infos_list_execute()` or `blogger_post_user_infos_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_post_user_infos_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostUserInfosList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostUserInfosList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_post_user_infos_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_post_user_infos_list_task()`.
/// For the simplest API, use `blogger_post_user_infos_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_post_user_infos_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_post_user_infos_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfosList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = blogger_post_user_infos_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_post_user_infos_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostUserInfosListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: endDate
    pub endDate: Option<Option<String>>,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: labels
    pub labels: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: startDate
    pub startDate: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/users/{userId}/blogs/{blogId}/posts
/// Lists post and user info pairs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_post_user_infos_list_builder()` + `blogger_post_user_infos_list_execute()`.
/// For task-level control, use `blogger_post_user_infos_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_post_user_infos_list(
    client: &SimpleHttpClient,
    args: &BloggerPostUserInfosListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostUserInfosList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = blogger_post_user_infos_list_builder(
        client,
        &args.userId,
        &args.blogId,
        &args.endDate,
        &args.fetchBodies,
        &args.labels,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.startDate,
        &args.status,
        &args.view,
    )?;
    blogger_post_user_infos_list_execute(builder)
}

/// DELETE v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_delete_execute()` to send, or `blogger_posts_delete` for simplest API.

pub fn blogger_posts_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    useTrash: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = useTrash.as_ref() {
        query_parts.push(format!("useTrash={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_delete_execute()` or `blogger_posts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_delete_task()`.
/// For the simplest API, use `blogger_posts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsDeleteArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: useTrash
    pub useTrash: Option<Option<String>>,
}

/// DELETE v3/blogs/{blogId}/posts/{postId}
/// Deletes a post by blog id and post id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_delete_builder()` + `blogger_posts_delete_execute()`.
/// For task-level control, use `blogger_posts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_delete(
    client: &SimpleHttpClient,
    args: &BloggerPostsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_delete_builder(client, &args.blogId, &args.postId, &args.useTrash)?;
    blogger_posts_delete_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_get_execute()` to send, or `blogger_posts_get` for simplest API.

pub fn blogger_posts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    fetchBody: &Option<Option<String>>,
    fetchImages: &Option<Option<String>>,
    maxComments: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody.as_ref() {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages.as_ref() {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments.as_ref() {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_get_execute()` or `blogger_posts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_get_task()`.
/// For the simplest API, use `blogger_posts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsGetArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: fetchBody
    pub fetchBody: Option<Option<String>>,
    /// Query parameter: fetchImages
    pub fetchImages: Option<Option<String>>,
    /// Query parameter: maxComments
    pub maxComments: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts/{postId}
/// Gets a post by blog id and post id
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_get_builder()` + `blogger_posts_get_execute()`.
/// For task-level control, use `blogger_posts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get(
    client: &SimpleHttpClient,
    args: &BloggerPostsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_get_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.fetchBody,
        &args.fetchImages,
        &args.maxComments,
        &args.view,
    )?;
    blogger_posts_get_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_get_by_path_execute()` to send, or `blogger_posts_get_by_path` for simplest API.

pub fn blogger_posts_get_by_path_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    maxComments: &Option<Option<String>>,
    path: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/bypath",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxComments.as_ref() {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = path.as_ref() {
        query_parts.push(format!("path={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_get_by_path_execute()` or `blogger_posts_get_by_path`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_get_by_path_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_by_path_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_get_by_path_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_get_by_path_task()`.
/// For the simplest API, use `blogger_posts_get_by_path()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_get_by_path_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_get_by_path_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_get_by_path_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_get_by_path`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsGetByPathArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: maxComments
    pub maxComments: Option<Option<String>>,
    /// Query parameter: path
    pub path: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts/bypath
/// Gets a post by path.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_get_by_path_builder()` + `blogger_posts_get_by_path_execute()`.
/// For task-level control, use `blogger_posts_get_by_path_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_get_by_path(
    client: &SimpleHttpClient,
    args: &BloggerPostsGetByPathArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_get_by_path_builder(
        client,
        &args.blogId,
        &args.maxComments,
        &args.path,
        &args.view,
    )?;
    blogger_posts_get_by_path_execute(builder)
}

/// POST v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_insert_execute()` to send, or `blogger_posts_insert` for simplest API.

pub fn blogger_posts_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    fetchBody: &Option<Option<String>>,
    fetchImages: &Option<Option<String>>,
    isDraft: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/{}/posts", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody.as_ref() {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages.as_ref() {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = isDraft.as_ref() {
        query_parts.push(format!("isDraft={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_insert_execute()` or `blogger_posts_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_insert_task()`.
/// For the simplest API, use `blogger_posts_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsInsertArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: fetchBody
    pub fetchBody: Option<Option<String>>,
    /// Query parameter: fetchImages
    pub fetchImages: Option<Option<String>>,
    /// Query parameter: isDraft
    pub isDraft: Option<Option<String>>,
}

/// POST v3/blogs/{blogId}/posts
/// Inserts a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_insert_builder()` + `blogger_posts_insert_execute()`.
/// For task-level control, use `blogger_posts_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_insert(
    client: &SimpleHttpClient,
    args: &BloggerPostsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_insert_builder(
        client,
        &args.blogId,
        &args.fetchBody,
        &args.fetchImages,
        &args.isDraft,
    )?;
    blogger_posts_insert_execute(builder)
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_list_execute()` to send, or `blogger_posts_list` for simplest API.

pub fn blogger_posts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    endDate: &Option<Option<String>>,
    fetchBodies: &Option<Option<String>>,
    fetchImages: &Option<Option<String>>,
    labels: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    sortOption: &Option<Option<String>>,
    startDate: &Option<Option<String>>,
    status: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/blogs/{}/posts", blogId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate.as_ref() {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = fetchImages.as_ref() {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = labels.as_ref() {
        query_parts.push(format!("labels={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortOption.as_ref() {
        query_parts.push(format!("sortOption={}", val));
    }
    if let Some(val) = startDate.as_ref() {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_list_execute()` or `blogger_posts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_list_task()`.
/// For the simplest API, use `blogger_posts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsListArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: endDate
    pub endDate: Option<Option<String>>,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: fetchImages
    pub fetchImages: Option<Option<String>>,
    /// Query parameter: labels
    pub labels: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: sortOption
    pub sortOption: Option<Option<String>>,
    /// Query parameter: startDate
    pub startDate: Option<Option<String>>,
    /// Query parameter: status
    pub status: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts
/// Lists posts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_list_builder()` + `blogger_posts_list_execute()`.
/// For task-level control, use `blogger_posts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_list(
    client: &SimpleHttpClient,
    args: &BloggerPostsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_list_builder(
        client,
        &args.blogId,
        &args.endDate,
        &args.fetchBodies,
        &args.fetchImages,
        &args.labels,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
        &args.sortOption,
        &args.startDate,
        &args.status,
        &args.view,
    )?;
    blogger_posts_list_execute(builder)
}

/// PATCH v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_patch_execute()` to send, or `blogger_posts_patch` for simplest API.

pub fn blogger_posts_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    fetchBody: &Option<Option<String>>,
    fetchImages: &Option<Option<String>>,
    maxComments: &Option<Option<String>>,
    publish: &Option<Option<String>>,
    revert: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody.as_ref() {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages.as_ref() {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments.as_ref() {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = publish.as_ref() {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert.as_ref() {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_patch_execute()` or `blogger_posts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_patch_task()`.
/// For the simplest API, use `blogger_posts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsPatchArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: fetchBody
    pub fetchBody: Option<Option<String>>,
    /// Query parameter: fetchImages
    pub fetchImages: Option<Option<String>>,
    /// Query parameter: maxComments
    pub maxComments: Option<Option<String>>,
    /// Query parameter: publish
    pub publish: Option<Option<String>>,
    /// Query parameter: revert
    pub revert: Option<Option<String>>,
}

/// PATCH v3/blogs/{blogId}/posts/{postId}
/// Patches a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_patch_builder()` + `blogger_posts_patch_execute()`.
/// For task-level control, use `blogger_posts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_patch(
    client: &SimpleHttpClient,
    args: &BloggerPostsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_patch_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.fetchBody,
        &args.fetchImages,
        &args.maxComments,
        &args.publish,
        &args.revert,
    )?;
    blogger_posts_patch_execute(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_publish_execute()` to send, or `blogger_posts_publish` for simplest API.

pub fn blogger_posts_publish_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    publishDate: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/publish",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publishDate.as_ref() {
        query_parts.push(format!("publishDate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_publish_execute()` or `blogger_posts_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_publish_task()`.
/// For the simplest API, use `blogger_posts_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsPublishArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: publishDate
    pub publishDate: Option<Option<String>>,
}

/// POST v3/blogs/{blogId}/posts/{postId}/publish
/// Publishes a post.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_publish_builder()` + `blogger_posts_publish_execute()`.
/// For task-level control, use `blogger_posts_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_publish(
    client: &SimpleHttpClient,
    args: &BloggerPostsPublishArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        blogger_posts_publish_builder(client, &args.blogId, &args.postId, &args.publishDate)?;
    blogger_posts_publish_execute(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_revert_execute()` to send, or `blogger_posts_revert` for simplest API.

pub fn blogger_posts_revert_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}/revert",
        blogId, postId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_revert_execute()` or `blogger_posts_revert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_revert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_revert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_revert_task()`.
/// For the simplest API, use `blogger_posts_revert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_revert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_revert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsRevertArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
}

/// POST v3/blogs/{blogId}/posts/{postId}/revert
/// Reverts a published or scheduled post to draft state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_revert_builder()` + `blogger_posts_revert_execute()`.
/// For task-level control, use `blogger_posts_revert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_revert(
    client: &SimpleHttpClient,
    args: &BloggerPostsRevertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_revert_builder(client, &args.blogId, &args.postId)?;
    blogger_posts_revert_execute(builder)
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_search_execute()` to send, or `blogger_posts_search` for simplest API.

pub fn blogger_posts_search_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    fetchBodies: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    q: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/search",
        blogId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBodies.as_ref() {
        query_parts.push(format!("fetchBodies={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_search_execute()` or `blogger_posts_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PostList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_search_task()`.
/// For the simplest API, use `blogger_posts_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsSearchArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Query parameter: fetchBodies
    pub fetchBodies: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: q
    pub q: Option<Option<String>>,
}

/// GET v3/blogs/{blogId}/posts/search
/// Searches for posts matching given query terms in the specified blog.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_search_builder()` + `blogger_posts_search_execute()`.
/// For task-level control, use `blogger_posts_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_search(
    client: &SimpleHttpClient,
    args: &BloggerPostsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_search_builder(
        client,
        &args.blogId,
        &args.fetchBodies,
        &args.orderBy,
        &args.q,
    )?;
    blogger_posts_search_execute(builder)
}

/// PUT v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_posts_update_execute()` to send, or `blogger_posts_update` for simplest API.

pub fn blogger_posts_update_builder<R>(
    client: &SimpleHttpClient<R>,
    blogId: &String,
    postId: &String,
    fetchBody: &Option<Option<String>>,
    fetchImages: &Option<Option<String>>,
    maxComments: &Option<Option<String>>,
    publish: &Option<Option<String>>,
    revert: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://blogger.googleapis.com/v3/blogs/{}/posts/{}",
        blogId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fetchBody.as_ref() {
        query_parts.push(format!("fetchBody={}", val));
    }
    if let Some(val) = fetchImages.as_ref() {
        query_parts.push(format!("fetchImages={}", val));
    }
    if let Some(val) = maxComments.as_ref() {
        query_parts.push(format!("maxComments={}", val));
    }
    if let Some(val) = publish.as_ref() {
        query_parts.push(format!("publish={}", val));
    }
    if let Some(val) = revert.as_ref() {
        query_parts.push(format!("revert={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_posts_update_execute()` or `blogger_posts_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Post>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Post = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_posts_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_posts_update_task()`.
/// For the simplest API, use `blogger_posts_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_posts_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_posts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_posts_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_posts_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerPostsUpdateArgs {
    /// Path parameter: blogId
    pub blogId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: fetchBody
    pub fetchBody: Option<Option<String>>,
    /// Query parameter: fetchImages
    pub fetchImages: Option<Option<String>>,
    /// Query parameter: maxComments
    pub maxComments: Option<Option<String>>,
    /// Query parameter: publish
    pub publish: Option<Option<String>>,
    /// Query parameter: revert
    pub revert: Option<Option<String>>,
}

/// PUT v3/blogs/{blogId}/posts/{postId}
/// Updates a post by blog id and post id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_posts_update_builder()` + `blogger_posts_update_execute()`.
/// For task-level control, use `blogger_posts_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_posts_update(
    client: &SimpleHttpClient,
    args: &BloggerPostsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Post>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_posts_update_builder(
        client,
        &args.blogId,
        &args.postId,
        &args.fetchBody,
        &args.fetchImages,
        &args.maxComments,
        &args.publish,
        &args.revert,
    )?;
    blogger_posts_update_execute(builder)
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `blogger_users_get_execute()` to send, or `blogger_users_get` for simplest API.

pub fn blogger_users_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://blogger.googleapis.com/v3/users/{}", userId,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `blogger_users_get_execute()` or `blogger_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `blogger_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `blogger_users_get_task()`.
/// For the simplest API, use `blogger_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `blogger_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn blogger_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = blogger_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`blogger_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BloggerUsersGetArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// GET v3/users/{userId}
/// Gets one user by user_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `blogger_users_get_builder()` + `blogger_users_get_execute()`.
/// For task-level control, use `blogger_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn blogger_users_get(
    client: &SimpleHttpClient,
    args: &BloggerUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = blogger_users_get_builder(client, &args.userId)?;
    blogger_users_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BlogUserInfo
// =============================================================================

/// ResourceIdentifier implementation for BlogUserInfo with BloggerBlogUserInfosGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerBlogUserInfosGetArgs> for BlogUserInfo {
    fn generate_resource_id(&self, input: &BloggerBlogUserInfosGetArgs) -> String {
        format!(
            "gcp::blogger::BlogUserInfo/{}/{}",
            input.userId, input.blogId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::BlogUserInfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Blog
// =============================================================================

/// ResourceIdentifier implementation for Blog with BloggerBlogsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerBlogsGetArgs> for Blog {
    fn generate_resource_id(&self, input: &BloggerBlogsGetArgs) -> String {
        format!("gcp::blogger::Blog/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Blog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Blog
// =============================================================================

/// ResourceIdentifier implementation for Blog with BloggerBlogsGetByUrlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerBlogsGetByUrlArgs> for Blog {
    fn generate_resource_id(&self, input: &BloggerBlogsGetByUrlArgs) -> String {
        "gcp::blogger::Blog".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Blog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BlogList
// =============================================================================

/// ResourceIdentifier implementation for BlogList with BloggerBlogsListByUserArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerBlogsListByUserArgs> for BlogList {
    fn generate_resource_id(&self, input: &BloggerBlogsListByUserArgs) -> String {
        format!("gcp::blogger::BlogList/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::BlogList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Comment
// =============================================================================

/// ResourceIdentifier implementation for Comment with BloggerCommentsApproveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsApproveArgs> for Comment {
    fn generate_resource_id(&self, input: &BloggerCommentsApproveArgs) -> String {
        format!(
            "gcp::blogger::Comment/{}/{}/{}",
            input.blogId, input.postId, input.commentId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Comment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Comment
// =============================================================================

/// ResourceIdentifier implementation for Comment with BloggerCommentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsGetArgs> for Comment {
    fn generate_resource_id(&self, input: &BloggerCommentsGetArgs) -> String {
        format!(
            "gcp::blogger::Comment/{}/{}/{}",
            input.blogId, input.postId, input.commentId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Comment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommentList
// =============================================================================

/// ResourceIdentifier implementation for CommentList with BloggerCommentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsListArgs> for CommentList {
    fn generate_resource_id(&self, input: &BloggerCommentsListArgs) -> String {
        format!(
            "gcp::blogger::CommentList/{}/{}",
            input.blogId, input.postId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::CommentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommentList
// =============================================================================

/// ResourceIdentifier implementation for CommentList with BloggerCommentsListByBlogArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsListByBlogArgs> for CommentList {
    fn generate_resource_id(&self, input: &BloggerCommentsListByBlogArgs) -> String {
        format!("gcp::blogger::CommentList/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::CommentList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Comment
// =============================================================================

/// ResourceIdentifier implementation for Comment with BloggerCommentsMarkAsSpamArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsMarkAsSpamArgs> for Comment {
    fn generate_resource_id(&self, input: &BloggerCommentsMarkAsSpamArgs) -> String {
        format!(
            "gcp::blogger::Comment/{}/{}/{}",
            input.blogId, input.postId, input.commentId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Comment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Comment
// =============================================================================

/// ResourceIdentifier implementation for Comment with BloggerCommentsRemoveContentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerCommentsRemoveContentArgs> for Comment {
    fn generate_resource_id(&self, input: &BloggerCommentsRemoveContentArgs) -> String {
        format!(
            "gcp::blogger::Comment/{}/{}/{}",
            input.blogId, input.postId, input.commentId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Comment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Pageviews
// =============================================================================

/// ResourceIdentifier implementation for Pageviews with BloggerPageViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPageViewsGetArgs> for Pageviews {
    fn generate_resource_id(&self, input: &BloggerPageViewsGetArgs) -> String {
        format!("gcp::blogger::Pageviews/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Pageviews"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesGetArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesGetArgs) -> String {
        format!("gcp::blogger::Page/{}/{}", input.blogId, input.pageId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesInsertArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesInsertArgs) -> String {
        format!("gcp::blogger::Page/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PageList
// =============================================================================

/// ResourceIdentifier implementation for PageList with BloggerPagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesListArgs> for PageList {
    fn generate_resource_id(&self, input: &BloggerPagesListArgs) -> String {
        format!("gcp::blogger::PageList/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::PageList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesPatchArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesPatchArgs) -> String {
        format!("gcp::blogger::Page/{}/{}", input.blogId, input.pageId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesPublishArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesPublishArgs) -> String {
        format!("gcp::blogger::Page/{}/{}", input.blogId, input.pageId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesRevertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesRevertArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesRevertArgs) -> String {
        format!("gcp::blogger::Page/{}/{}", input.blogId, input.pageId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with BloggerPagesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPagesUpdateArgs> for Page {
    fn generate_resource_id(&self, input: &BloggerPagesUpdateArgs) -> String {
        format!("gcp::blogger::Page/{}/{}", input.blogId, input.pageId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostUserInfo
// =============================================================================

/// ResourceIdentifier implementation for PostUserInfo with BloggerPostUserInfosGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostUserInfosGetArgs> for PostUserInfo {
    fn generate_resource_id(&self, input: &BloggerPostUserInfosGetArgs) -> String {
        format!(
            "gcp::blogger::PostUserInfo/{}/{}/{}",
            input.userId, input.blogId, input.postId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::PostUserInfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostUserInfosList
// =============================================================================

/// ResourceIdentifier implementation for PostUserInfosList with BloggerPostUserInfosListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostUserInfosListArgs> for PostUserInfosList {
    fn generate_resource_id(&self, input: &BloggerPostUserInfosListArgs) -> String {
        format!(
            "gcp::blogger::PostUserInfosList/{}/{}",
            input.userId, input.blogId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::PostUserInfosList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsGetArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsGetArgs) -> String {
        format!("gcp::blogger::Post/{}/{}", input.blogId, input.postId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsGetByPathArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsGetByPathArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsGetByPathArgs) -> String {
        format!("gcp::blogger::Post/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsInsertArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsInsertArgs) -> String {
        format!("gcp::blogger::Post/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostList
// =============================================================================

/// ResourceIdentifier implementation for PostList with BloggerPostsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsListArgs> for PostList {
    fn generate_resource_id(&self, input: &BloggerPostsListArgs) -> String {
        format!("gcp::blogger::PostList/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::PostList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsPatchArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsPatchArgs) -> String {
        format!("gcp::blogger::Post/{}/{}", input.blogId, input.postId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsPublishArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsPublishArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsPublishArgs) -> String {
        format!("gcp::blogger::Post/{}/{}", input.blogId, input.postId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsRevertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsRevertArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsRevertArgs) -> String {
        format!("gcp::blogger::Post/{}/{}", input.blogId, input.postId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PostList
// =============================================================================

/// ResourceIdentifier implementation for PostList with BloggerPostsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsSearchArgs> for PostList {
    fn generate_resource_id(&self, input: &BloggerPostsSearchArgs) -> String {
        format!("gcp::blogger::PostList/{}", input.blogId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::PostList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Post
// =============================================================================

/// ResourceIdentifier implementation for Post with BloggerPostsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerPostsUpdateArgs> for Post {
    fn generate_resource_id(&self, input: &BloggerPostsUpdateArgs) -> String {
        format!("gcp::blogger::Post/{}/{}", input.blogId, input.postId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::Post"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with BloggerUsersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BloggerUsersGetArgs> for User {
    fn generate_resource_id(&self, input: &BloggerUsersGetArgs) -> String {
        format!("gcp::blogger::User/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::blogger::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
