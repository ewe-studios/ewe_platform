//! Auto-generated API clients for gcp/meet.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/meet`

#![cfg(feature = "gcp/meet")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_get_execute()` to send, or `meet_conference_records_get` for simplest API.

pub fn meet_conference_records_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConferenceRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConferenceRecord = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_get_builder()` + `meet_conference_records_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConferenceRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_get_builder(client, name)?;
    meet_conference_records_get_execute(builder)
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_list_execute()` to send, or `meet_conference_records_list` for simplest API.

pub fn meet_conference_records_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://meet.googleapis.com/v2/conferenceRecords",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConferenceRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConferenceRecordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_list_builder()` + `meet_conference_records_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConferenceRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_list_builder(client, filter, pageSize, pageToken)?;
    meet_conference_records_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}
/// Gets a participant by participant ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_participants_get_execute()` to send, or `meet_conference_records_participants_get` for simplest API.

pub fn meet_conference_records_participants_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/participants/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}
/// Gets a participant by participant ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_participants_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_participants_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Participant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Participant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}
/// Gets a participant by participant ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_participants_get_builder()` + `meet_conference_records_participants_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Participant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_conference_records_participants_get_builder(client, name)?;
    meet_conference_records_participants_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_participants_list_execute()` to send, or `meet_conference_records_participants_list` for simplest API.

pub fn meet_conference_records_participants_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/participants",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_participants_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_participants_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListParticipantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListParticipantsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_participants_list_builder()` + `meet_conference_records_participants_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListParticipantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_participants_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    meet_conference_records_participants_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions/{participantSessionsId}
/// Gets a participant session by participant session ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_participants_participant_sessions_get_execute()` to send, or `meet_conference_records_participants_participant_sessions_get` for simplest API.

pub fn meet_conference_records_participants_participant_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/participants/{}/participantSessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions/{participantSessionsId}
/// Gets a participant session by participant session ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_participants_participant_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_participants_participant_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_participant_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ParticipantSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ParticipantSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions/{participantSessionsId}
/// Gets a participant session by participant session ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_participants_participant_sessions_get_builder()` + `meet_conference_records_participants_participant_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_participant_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ParticipantSession>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        meet_conference_records_participants_participant_sessions_get_builder(client, name)?;
    meet_conference_records_participants_participant_sessions_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions
/// Lists the participant sessions of a participant in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted this API defaults to 'participantsessions/*, next_page_token'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_participants_participant_sessions_list_execute()` to send, or `meet_conference_records_participants_participant_sessions_list` for simplest API.

pub fn meet_conference_records_participants_participant_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/participants/{}/participantSessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions
/// Lists the participant sessions of a participant in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted this API defaults to 'participantsessions/*, next_page_token'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_participants_participant_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_participants_participant_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_participant_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListParticipantSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListParticipantSessionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants/{participantsId}/participantSessions
/// Lists the participant sessions of a participant in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted this API defaults to 'participantsessions/*, next_page_token'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_participants_participant_sessions_list_builder()` + `meet_conference_records_participants_participant_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_participant_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListParticipantSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_participants_participant_sessions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    meet_conference_records_participants_participant_sessions_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings/{recordingsId}
/// Gets a recording by recording ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_recordings_get_execute()` to send, or `meet_conference_records_recordings_get` for simplest API.

pub fn meet_conference_records_recordings_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/recordings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings/{recordingsId}
/// Gets a recording by recording ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_recordings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_recordings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Recording>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Recording = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings/{recordingsId}
/// Gets a recording by recording ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_recordings_get_builder()` + `meet_conference_records_recordings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Recording>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_conference_records_recordings_get_builder(client, name)?;
    meet_conference_records_recordings_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_recordings_list_execute()` to send, or `meet_conference_records_recordings_list` for simplest API.

pub fn meet_conference_records_recordings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/recordings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_recordings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_recordings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecordingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecordingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_recordings_list_builder()` + `meet_conference_records_recordings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecordingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        meet_conference_records_recordings_list_builder(client, parent, pageSize, pageToken)?;
    meet_conference_records_recordings_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes/{smartNotesId}
/// Gets smart notes by smart note ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_smart_notes_get_execute()` to send, or `meet_conference_records_smart_notes_get` for simplest API.

pub fn meet_conference_records_smart_notes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/smartNotes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes/{smartNotesId}
/// Gets smart notes by smart note ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_smart_notes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_smart_notes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SmartNote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SmartNote = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes/{smartNotesId}
/// Gets smart notes by smart note ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_smart_notes_get_builder()` + `meet_conference_records_smart_notes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SmartNote>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_conference_records_smart_notes_get_builder(client, name)?;
    meet_conference_records_smart_notes_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_smart_notes_list_execute()` to send, or `meet_conference_records_smart_notes_list` for simplest API.

pub fn meet_conference_records_smart_notes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/smartNotes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_smart_notes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_smart_notes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSmartNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSmartNotesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_smart_notes_list_builder()` + `meet_conference_records_smart_notes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSmartNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        meet_conference_records_smart_notes_list_builder(client, parent, pageSize, pageToken)?;
    meet_conference_records_smart_notes_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}
/// Gets a transcript by transcript ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_transcripts_get_execute()` to send, or `meet_conference_records_transcripts_get` for simplest API.

pub fn meet_conference_records_transcripts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/transcripts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}
/// Gets a transcript by transcript ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_transcripts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_transcripts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transcript>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transcript = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}
/// Gets a transcript by transcript ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_transcripts_get_builder()` + `meet_conference_records_transcripts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transcript>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_conference_records_transcripts_get_builder(client, name)?;
    meet_conference_records_transcripts_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_transcripts_list_execute()` to send, or `meet_conference_records_transcripts_list` for simplest API.

pub fn meet_conference_records_transcripts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/transcripts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_transcripts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_transcripts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTranscriptsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTranscriptsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_transcripts_list_builder()` + `meet_conference_records_transcripts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTranscriptsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        meet_conference_records_transcripts_list_builder(client, parent, pageSize, pageToken)?;
    meet_conference_records_transcripts_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries/{entriesId}
/// Gets a TranscriptEntry resource by entry ID. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_transcripts_entries_get_execute()` to send, or `meet_conference_records_transcripts_entries_get` for simplest API.

pub fn meet_conference_records_transcripts_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/transcripts/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries/{entriesId}
/// Gets a TranscriptEntry resource by entry ID. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_transcripts_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_transcripts_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranscriptEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TranscriptEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries/{entriesId}
/// Gets a TranscriptEntry resource by entry ID. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_transcripts_entries_get_builder()` + `meet_conference_records_transcripts_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranscriptEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_transcripts_entries_get_builder(client, name)?;
    meet_conference_records_transcripts_entries_get_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries
/// Lists the structured transcript entries per transcript. By default, ordered by start time and in ascending order. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_transcripts_entries_list_execute()` to send, or `meet_conference_records_transcripts_entries_list` for simplest API.

pub fn meet_conference_records_transcripts_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/conferenceRecords/{}/transcripts/{}/entries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries
/// Lists the structured transcript entries per transcript. By default, ordered by start time and in ascending order. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_conference_records_transcripts_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_conference_records_transcripts_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTranscriptEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTranscriptEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts/{transcriptsId}/entries
/// Lists the structured transcript entries per transcript. By default, ordered by start time and in ascending order. Note: The transcript entries returned by the Google Meet API might not match the transcription found in the Google Docs transcript file. This can occur when 1) we have interleaved speakers within milliseconds, or 2) the Google Docs transcript file is modified after generation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_transcripts_entries_list_builder()` + `meet_conference_records_transcripts_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTranscriptEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_transcripts_entries_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    meet_conference_records_transcripts_entries_list_execute(builder)
}

/// GET v2/spaces
/// Creates a space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_create_execute()` to send, or `meet_spaces_create` for simplest API.

pub fn meet_spaces_create_builder(
    client: &SimpleHttpClient,
    body: &Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://meet.googleapis.com/v2/spaces",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces
/// Creates a space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_spaces_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_spaces_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces
/// Creates a space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_create_builder()` + `meet_spaces_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_create(
    client: &SimpleHttpClient,
    body: &Space,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_create_builder(client, body)?;
    meet_spaces_create_execute(builder)
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_end_active_conference_execute()` to send, or `meet_spaces_end_active_conference` for simplest API.

pub fn meet_spaces_end_active_conference_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EndActiveConferenceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://meet.googleapis.com/v2/spaces/{}:endActiveConference",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_spaces_end_active_conference_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_spaces_end_active_conference_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_end_active_conference_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_end_active_conference_builder()` + `meet_spaces_end_active_conference_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_end_active_conference(
    client: &SimpleHttpClient,
    name: &str,
    body: &EndActiveConferenceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_end_active_conference_builder(client, name, body)?;
    meet_spaces_end_active_conference_execute(builder)
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_get_execute()` to send, or `meet_spaces_get` for simplest API.

pub fn meet_spaces_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://meet.googleapis.com/v2/spaces/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_spaces_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_spaces_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_get_builder()` + `meet_spaces_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_get_builder(client, name)?;
    meet_spaces_get_execute(builder)
}

/// GET v2/spaces/{spacesId}
/// Updates details about a meeting space. For an example, see [Update a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#update-meeting-space>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_patch_execute()` to send, or `meet_spaces_patch` for simplest API.

pub fn meet_spaces_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://meet.googleapis.com/v2/spaces/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}
/// Updates details about a meeting space. For an example, see [Update a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#update-meeting-space>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `meet_spaces_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from meet_spaces_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}
/// Updates details about a meeting space. For an example, see [Update a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#update-meeting-space>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_patch_builder()` + `meet_spaces_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Space,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_patch_builder(client, name, updateMask, body)?;
    meet_spaces_patch_execute(builder)
}
