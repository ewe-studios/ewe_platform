//! Auto-generated API clients for gcp/meet.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_get_execute()` to send, or `meet_conference_records_get` for simplest API.

pub fn meet_conference_records_get_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_get_execute()` or `meet_conference_records_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConferenceRecord>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConferenceRecord = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_get_task()`.
/// For the simplest API, use `meet_conference_records_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConferenceRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/conferenceRecords/{conferenceRecordsId}
/// Gets a conference record by conference ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_get_builder()` + `meet_conference_records_get_execute()`.
/// For task-level control, use `meet_conference_records_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_get(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConferenceRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_get_builder(client, args.name.clone())?;
    meet_conference_records_get_execute(builder)
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_list_execute()` to send, or `meet_conference_records_list` for simplest API.

pub fn meet_conference_records_list_builder(
    client: &SimpleHttpClient,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_list_execute()` or `meet_conference_records_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConferenceRecordsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConferenceRecordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_list_task()`.
/// For the simplest API, use `meet_conference_records_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConferenceRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/conferenceRecords
/// Lists the conference records. By default, ordered by start time and in descending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_list_builder()` + `meet_conference_records_list_execute()`.
/// For task-level control, use `meet_conference_records_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_list(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConferenceRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_list_builder(
        client,
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    meet_conference_records_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_participants_list_execute()` to send, or `meet_conference_records_participants_list` for simplest API.

pub fn meet_conference_records_participants_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}/participants",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_participants_list_execute()` or `meet_conference_records_participants_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_participants_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListParticipantsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListParticipantsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_participants_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_participants_list_task()`.
/// For the simplest API, use `meet_conference_records_participants_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_participants_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_participants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListParticipantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_participants_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_participants_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsParticipantsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/participants
/// Lists the participants in a conference record. By default, ordered by join time and in descending order. This API supports fields as standard parameters like every other API. However, when the fields request parameter is omitted, this API defaults to 'participants/*, next_page_token'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_participants_list_builder()` + `meet_conference_records_participants_list_execute()`.
/// For task-level control, use `meet_conference_records_participants_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_participants_list(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsParticipantsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListParticipantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_participants_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    meet_conference_records_participants_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_recordings_list_execute()` to send, or `meet_conference_records_recordings_list` for simplest API.

pub fn meet_conference_records_recordings_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}/recordings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_recordings_list_execute()` or `meet_conference_records_recordings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_recordings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRecordingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecordingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_recordings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_recordings_list_task()`.
/// For the simplest API, use `meet_conference_records_recordings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_recordings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_recordings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecordingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_recordings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_recordings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsRecordingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/recordings
/// Lists the recording resources from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_recordings_list_builder()` + `meet_conference_records_recordings_list_execute()`.
/// For task-level control, use `meet_conference_records_recordings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_recordings_list(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsRecordingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecordingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_recordings_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    meet_conference_records_recordings_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_smart_notes_list_execute()` to send, or `meet_conference_records_smart_notes_list` for simplest API.

pub fn meet_conference_records_smart_notes_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}/smartNotes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_smart_notes_list_execute()` or `meet_conference_records_smart_notes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_smart_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSmartNotesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSmartNotesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_smart_notes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_smart_notes_list_task()`.
/// For the simplest API, use `meet_conference_records_smart_notes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_smart_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_smart_notes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSmartNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_smart_notes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_smart_notes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsSmartNotesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/smartNotes
/// Lists the set of smart notes from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_smart_notes_list_builder()` + `meet_conference_records_smart_notes_list_execute()`.
/// For task-level control, use `meet_conference_records_smart_notes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_smart_notes_list(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsSmartNotesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSmartNotesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_smart_notes_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    meet_conference_records_smart_notes_list_execute(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_conference_records_transcripts_list_execute()` to send, or `meet_conference_records_transcripts_list` for simplest API.

pub fn meet_conference_records_transcripts_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/conferenceRecords/{}/transcripts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_conference_records_transcripts_list_execute()` or `meet_conference_records_transcripts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_transcripts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTranscriptsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTranscriptsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_conference_records_transcripts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_conference_records_transcripts_list_task()`.
/// For the simplest API, use `meet_conference_records_transcripts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_conference_records_transcripts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_conference_records_transcripts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTranscriptsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = meet_conference_records_transcripts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_conference_records_transcripts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetConferenceRecordsTranscriptsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/conferenceRecords/{conferenceRecordsId}/transcripts
/// Lists the set of transcripts from the conference record. By default, ordered by start time and in ascending order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_conference_records_transcripts_list_builder()` + `meet_conference_records_transcripts_list_execute()`.
/// For task-level control, use `meet_conference_records_transcripts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_conference_records_transcripts_list(
    client: &SimpleHttpClient,
    args: &MeetConferenceRecordsTranscriptsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTranscriptsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = meet_conference_records_transcripts_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    meet_conference_records_transcripts_list_execute(builder)
}

/// GET v2/spaces
/// Creates a space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_create_execute()` to send, or `meet_spaces_create` for simplest API.

pub fn meet_spaces_create_builder(
    client: &SimpleHttpClient,
    body: &Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/spaces",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces
/// Creates a space.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_spaces_create_execute()` or `meet_spaces_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Space>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/spaces
/// Creates a space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_spaces_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_spaces_create_task()`.
/// For the simplest API, use `meet_spaces_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_spaces_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = meet_spaces_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_spaces_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetSpacesCreateArgs {
    /// Request body.
    pub body: Space,
}

/// GET v2/spaces
/// Creates a space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_create_builder()` + `meet_spaces_create_execute()`.
/// For task-level control, use `meet_spaces_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_create(
    client: &SimpleHttpClient,
    args: &MeetSpacesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_create_builder(client, &args.body)?;
    meet_spaces_create_execute(builder)
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_end_active_conference_execute()` to send, or `meet_spaces_end_active_conference` for simplest API.

pub fn meet_spaces_end_active_conference_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &EndActiveConferenceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/spaces/{}:endActiveConference",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_spaces_end_active_conference_execute()` or `meet_spaces_end_active_conference`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_end_active_conference_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_end_active_conference_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_spaces_end_active_conference_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_spaces_end_active_conference_task()`.
/// For the simplest API, use `meet_spaces_end_active_conference()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_end_active_conference_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_spaces_end_active_conference_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = meet_spaces_end_active_conference_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_spaces_end_active_conference`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetSpacesEndActiveConferenceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: EndActiveConferenceRequest,
}

/// GET v2/spaces/{spacesId}:endActiveConference
/// Ends an active conference (if there's one). For an example, see [End active conference](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#end-active-conference>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_end_active_conference_builder()` + `meet_spaces_end_active_conference_execute()`.
/// For task-level control, use `meet_spaces_end_active_conference_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_end_active_conference(
    client: &SimpleHttpClient,
    args: &MeetSpacesEndActiveConferenceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_end_active_conference_builder(client, args.name.clone(), &args.body)?;
    meet_spaces_end_active_conference_execute(builder)
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `meet_spaces_get_execute()` to send, or `meet_spaces_get` for simplest API.

pub fn meet_spaces_get_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://meet.googleapis.com/v2/spaces/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `meet_spaces_get_execute()` or `meet_spaces_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Space>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `meet_spaces_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `meet_spaces_get_task()`.
/// For the simplest API, use `meet_spaces_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `meet_spaces_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn meet_spaces_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = meet_spaces_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`meet_spaces_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MeetSpacesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/spaces/{spacesId}
/// Gets details about a meeting space. For an example, see [Get a meeting space](<https://developers.google.`com/workspace/meet/api/guides/meeting-spaces`#get-meeting-space>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `meet_spaces_get_builder()` + `meet_spaces_get_execute()`.
/// For task-level control, use `meet_spaces_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn meet_spaces_get(
    client: &SimpleHttpClient,
    args: &MeetSpacesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Space>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = meet_spaces_get_builder(client, args.name.clone())?;
    meet_spaces_get_execute(builder)
}
