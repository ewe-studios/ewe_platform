//! Auto-generated API clients for gcp/toolresults.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET toolresults/v1beta3/projects/{projectId}/settings
/// Gets the Tool Results settings for a project. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from project
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_get_settings_execute()` to send, or `toolresults_projects_get_settings` for simplest API.

pub fn toolresults_projects_get_settings_builder(
    client: &SimpleHttpClient,
    projectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/settings",
        projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/settings
/// Gets the Tool Results settings for a project. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from project
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_get_settings_execute()` or `toolresults_projects_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/settings
/// Gets the Tool Results settings for a project. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from project
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_get_settings_task()`.
/// For the simplest API, use `toolresults_projects_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsGetSettingsArgs {
    /// Path parameter: projectId
    pub projectId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/settings
/// Gets the Tool Results settings for a project. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from project
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_get_settings_builder()` + `toolresults_projects_get_settings_execute()`.
/// For task-level control, use `toolresults_projects_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_get_settings(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_get_settings_builder(client, &args.projectId)?;
    toolresults_projects_get_settings_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}:initializeSettings
/// Creates resources for settings which have not yet been set. Currently, this creates a single resource: a Google Cloud Storage bucket, to be used as the default bucket for this project. The bucket is created in an FTL-own storage project. Except for in rare cases, calling this method in parallel from multiple clients will only create a single bucket. In order to avoid unnecessary storage charges, the bucket is configured to automatically delete objects older than 60 days. The bucket is created with the following permissions: - Owner access for owners of central storage project (FTL-owned) - Writer access for `owners/editors` of customer project - Reader access for viewers of customer project The default ACL on objects created in the bucket is: - Owner access for owners of central storage project - Reader access for `owners/editors/viewers` of customer project See Google Cloud Storage documentation for more details. If there is already a default bucket set and the project can access the bucket, this call does nothing. However, if the project doesn't have the permission to access the bucket or the bucket is deleted, a new bucket will be created. May return any canonical error codes, including the following: - PERMISSION_DENIED - if the user is not authorized to write to project - Any error code raised by Google Cloud Storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_initialize_settings_execute()` to send, or `toolresults_projects_initialize_settings` for simplest API.

pub fn toolresults_projects_initialize_settings_builder(
    client: &SimpleHttpClient,
    projectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}:initializeSettings",
        projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}:initializeSettings
/// Creates resources for settings which have not yet been set. Currently, this creates a single resource: a Google Cloud Storage bucket, to be used as the default bucket for this project. The bucket is created in an FTL-own storage project. Except for in rare cases, calling this method in parallel from multiple clients will only create a single bucket. In order to avoid unnecessary storage charges, the bucket is configured to automatically delete objects older than 60 days. The bucket is created with the following permissions: - Owner access for owners of central storage project (FTL-owned) - Writer access for `owners/editors` of customer project - Reader access for viewers of customer project The default ACL on objects created in the bucket is: - Owner access for owners of central storage project - Reader access for `owners/editors/viewers` of customer project See Google Cloud Storage documentation for more details. If there is already a default bucket set and the project can access the bucket, this call does nothing. However, if the project doesn't have the permission to access the bucket or the bucket is deleted, a new bucket will be created. May return any canonical error codes, including the following: - PERMISSION_DENIED - if the user is not authorized to write to project - Any error code raised by Google Cloud Storage
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_initialize_settings_execute()` or `toolresults_projects_initialize_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_initialize_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_initialize_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}:initializeSettings
/// Creates resources for settings which have not yet been set. Currently, this creates a single resource: a Google Cloud Storage bucket, to be used as the default bucket for this project. The bucket is created in an FTL-own storage project. Except for in rare cases, calling this method in parallel from multiple clients will only create a single bucket. In order to avoid unnecessary storage charges, the bucket is configured to automatically delete objects older than 60 days. The bucket is created with the following permissions: - Owner access for owners of central storage project (FTL-owned) - Writer access for `owners/editors` of customer project - Reader access for viewers of customer project The default ACL on objects created in the bucket is: - Owner access for owners of central storage project - Reader access for `owners/editors/viewers` of customer project See Google Cloud Storage documentation for more details. If there is already a default bucket set and the project can access the bucket, this call does nothing. However, if the project doesn't have the permission to access the bucket or the bucket is deleted, a new bucket will be created. May return any canonical error codes, including the following: - PERMISSION_DENIED - if the user is not authorized to write to project - Any error code raised by Google Cloud Storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_initialize_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_initialize_settings_task()`.
/// For the simplest API, use `toolresults_projects_initialize_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_initialize_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_initialize_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_initialize_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_initialize_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsInitializeSettingsArgs {
    /// Path parameter: projectId
    pub projectId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}:initializeSettings
/// Creates resources for settings which have not yet been set. Currently, this creates a single resource: a Google Cloud Storage bucket, to be used as the default bucket for this project. The bucket is created in an FTL-own storage project. Except for in rare cases, calling this method in parallel from multiple clients will only create a single bucket. In order to avoid unnecessary storage charges, the bucket is configured to automatically delete objects older than 60 days. The bucket is created with the following permissions: - Owner access for owners of central storage project (FTL-owned) - Writer access for `owners/editors` of customer project - Reader access for viewers of customer project The default ACL on objects created in the bucket is: - Owner access for owners of central storage project - Reader access for `owners/editors/viewers` of customer project See Google Cloud Storage documentation for more details. If there is already a default bucket set and the project can access the bucket, this call does nothing. However, if the project doesn't have the permission to access the bucket or the bucket is deleted, a new bucket will be created. May return any canonical error codes, including the following: - PERMISSION_DENIED - if the user is not authorized to write to project - Any error code raised by Google Cloud Storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_initialize_settings_builder()` + `toolresults_projects_initialize_settings_execute()`.
/// For task-level control, use `toolresults_projects_initialize_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_initialize_settings(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsInitializeSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_initialize_settings_builder(client, &args.projectId)?;
    toolresults_projects_initialize_settings_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Creates a History. The returned History will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing project does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_create_execute()` to send, or `toolresults_projects_histories_create` for simplest API.

pub fn toolresults_projects_histories_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    requestId: Option<&str>,
    body: &History,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Creates a History. The returned History will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing project does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_create_execute()` or `toolresults_projects_histories_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: History = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Creates a History. The returned History will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing project does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: History,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Creates a History. The returned History will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing project does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_create_builder()` + `toolresults_projects_histories_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_create_builder(
        client,
        &args.projectId,
        args.requestId.as_deref(),
        &args.body,
    )?;
    toolresults_projects_histories_create_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}
/// Gets a History. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_get_execute()` to send, or `toolresults_projects_histories_get` for simplest API.

pub fn toolresults_projects_histories_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}",
        projectId, historyId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}
/// Gets a History. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_get_execute()` or `toolresults_projects_histories_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: History = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}
/// Gets a History. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}
/// Gets a History. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_get_builder()` + `toolresults_projects_histories_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<History>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        toolresults_projects_histories_get_builder(client, &args.projectId, &args.historyId)?;
    toolresults_projects_histories_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Lists Histories for a given Project. The histories are sorted by modification time in descending order. The history_id key will be used to order the history with the same modification time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_list_execute()` to send, or `toolresults_projects_histories_list` for simplest API.

pub fn toolresults_projects_histories_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    filterByName: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filterByName {
        query_parts.push(format!("filterByName={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Lists Histories for a given Project. The histories are sorted by modification time in descending order. The history_id key will be used to order the history with the same modification time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_list_execute()` or `toolresults_projects_histories_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListHistoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHistoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Lists Histories for a given Project. The histories are sorted by modification time in descending order. The history_id key will be used to order the history with the same modification time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHistoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: filterByName
    pub filterByName: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories
/// Lists Histories for a given Project. The histories are sorted by modification time in descending order. The history_id key will be used to order the history with the same modification time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the History does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_list_builder()` + `toolresults_projects_histories_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHistoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_list_builder(
        client,
        &args.projectId,
        args.filterByName.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Creates an Execution. The returned Execution will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_create_execute()` to send, or `toolresults_projects_histories_executions_create` for simplest API.

pub fn toolresults_projects_histories_executions_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    requestId: Option<&str>,
    body: &Execution,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions",
        projectId,
        historyId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Creates an Execution. The returned Execution will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_create_execute()` or `toolresults_projects_histories_executions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Creates an Execution. The returned Execution will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Execution,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Creates an Execution. The returned Execution will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_create_builder()` + `toolresults_projects_histories_executions_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_create_builder(
        client,
        &args.projectId,
        &args.historyId,
        args.requestId.as_deref(),
        &args.body,
    )?;
    toolresults_projects_histories_executions_create_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Gets an Execution. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_get_execute()` to send, or `toolresults_projects_histories_executions_get` for simplest API.

pub fn toolresults_projects_histories_executions_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Gets an Execution. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_get_execute()` or `toolresults_projects_histories_executions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Gets an Execution. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Gets an Execution. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_get_builder()` + `toolresults_projects_histories_executions_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
    )?;
    toolresults_projects_histories_executions_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Lists Executions for a given History. The executions are sorted by creation_time in descending order. The execution_id key will be used to order the executions with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_list_execute()` to send, or `toolresults_projects_histories_executions_list` for simplest API.

pub fn toolresults_projects_histories_executions_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions",
        projectId,
        historyId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Lists Executions for a given History. The executions are sorted by creation_time in descending order. The execution_id key will be used to order the executions with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_list_execute()` or `toolresults_projects_histories_executions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListExecutionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExecutionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Lists Executions for a given History. The executions are sorted by creation_time in descending order. The execution_id key will be used to order the executions with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExecutionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
/// Lists Executions for a given History. The executions are sorted by creation_time in descending order. The execution_id key will be used to order the executions with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing History does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_list_builder()` + `toolresults_projects_histories_executions_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExecutionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_executions_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Updates an existing Execution with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal - NOT_FOUND - if the containing History does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_patch_execute()` to send, or `toolresults_projects_histories_executions_patch` for simplest API.

pub fn toolresults_projects_histories_executions_patch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    requestId: Option<&str>,
    body: &Execution,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Updates an existing Execution with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_patch_execute()` or `toolresults_projects_histories_executions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Execution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Updates an existing Execution with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal - NOT_FOUND - if the containing History does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_patch_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsPatchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Execution,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
/// Updates an existing Execution with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal - NOT_FOUND - if the containing History does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_patch_builder()` + `toolresults_projects_histories_executions_patch_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_patch(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Execution>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_patch_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        args.requestId.as_deref(),
        &args.body,
    )?;
    toolresults_projects_histories_executions_patch_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}
/// Retrieves a single screenshot cluster by its ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_clusters_get_execute()` to send, or `toolresults_projects_histories_executions_clusters_get` for simplest API.

pub fn toolresults_projects_histories_executions_clusters_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    clusterId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/clusters/{}",
        projectId,
        historyId,
        executionId,
        clusterId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}
/// Retrieves a single screenshot cluster by its ID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_clusters_get_execute()` or `toolresults_projects_histories_executions_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ScreenshotCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ScreenshotCluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}
/// Retrieves a single screenshot cluster by its ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_clusters_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScreenshotCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsClustersGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: clusterId
    pub clusterId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}
/// Retrieves a single screenshot cluster by its ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_clusters_get_builder()` + `toolresults_projects_histories_executions_clusters_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_clusters_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScreenshotCluster>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_clusters_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.clusterId,
    )?;
    toolresults_projects_histories_executions_clusters_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters
/// Lists Screenshot Clusters Returns the list of screenshot clusters corresponding to an execution. Screenshot clusters are created after the execution is finished. Clusters are created from a set of screenshots. Between any two screenshots, a matching score is calculated based off their metadata that determines how similar they are. Screenshots are placed in the cluster that has screens which have the highest matching scores.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_clusters_list_execute()` to send, or `toolresults_projects_histories_executions_clusters_list` for simplest API.

pub fn toolresults_projects_histories_executions_clusters_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/clusters",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters
/// Lists Screenshot Clusters Returns the list of screenshot clusters corresponding to an execution. Screenshot clusters are created after the execution is finished. Clusters are created from a set of screenshots. Between any two screenshots, a matching score is calculated based off their metadata that determines how similar they are. Screenshots are placed in the cluster that has screens which have the highest matching scores.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_clusters_list_execute()` or `toolresults_projects_histories_executions_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListScreenshotClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListScreenshotClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters
/// Lists Screenshot Clusters Returns the list of screenshot clusters corresponding to an execution. Screenshot clusters are created after the execution is finished. Clusters are created from a set of screenshots. Between any two screenshots, a matching score is calculated based off their metadata that determines how similar they are. Screenshots are placed in the cluster that has screens which have the highest matching scores.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_clusters_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListScreenshotClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsClustersListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters
/// Lists Screenshot Clusters Returns the list of screenshot clusters corresponding to an execution. Screenshot clusters are created after the execution is finished. Clusters are created from a set of screenshots. Between any two screenshots, a matching score is calculated based off their metadata that determines how similar they are. Screenshots are placed in the cluster that has screens which have the highest matching scores.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_clusters_list_builder()` + `toolresults_projects_histories_executions_clusters_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_clusters_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsClustersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListScreenshotClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_clusters_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
    )?;
    toolresults_projects_histories_executions_clusters_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}
/// Gets an Environment. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Environment does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_environments_get_execute()` to send, or `toolresults_projects_histories_executions_environments_get` for simplest API.

pub fn toolresults_projects_histories_executions_environments_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    environmentId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/environments/{}",
        projectId,
        historyId,
        executionId,
        environmentId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}
/// Gets an Environment. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Environment does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_environments_get_execute()` or `toolresults_projects_histories_executions_environments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_environments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}
/// Gets an Environment. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Environment does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_environments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_environments_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_environments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_environments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_environments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsEnvironmentsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: environmentId
    pub environmentId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}
/// Gets an Environment. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Environment does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_environments_get_builder()` + `toolresults_projects_histories_executions_environments_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_environments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_environments_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsEnvironmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_environments_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.environmentId,
    )?;
    toolresults_projects_histories_executions_environments_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments
/// Lists Environments for a given Execution. The Environments are sorted by display name. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_environments_list_execute()` to send, or `toolresults_projects_histories_executions_environments_list` for simplest API.

pub fn toolresults_projects_histories_executions_environments_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/environments",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments
/// Lists Environments for a given Execution. The Environments are sorted by display name. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_environments_list_execute()` or `toolresults_projects_histories_executions_environments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_environments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEnvironmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments
/// Lists Environments for a given Execution. The Environments are sorted by display name. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_environments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_environments_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_environments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_environments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_environments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_environments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsEnvironmentsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments
/// Lists Environments for a given Execution. The Environments are sorted by display name. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_environments_list_builder()` + `toolresults_projects_histories_executions_environments_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_environments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_environments_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsEnvironmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_environments_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_executions_environments_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectsId}/histories/{historiesId}/executions/{executionsId}/steps/{stepsId}:accessibilityClusters
/// Lists accessibility clusters for a given Step May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if the locale format is incorrect - NOT_FOUND - if the containing Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_accessibility_clusters_execute()` to send, or `toolresults_projects_histories_executions_steps_accessibility_clusters` for simplest API.

pub fn toolresults_projects_histories_executions_steps_accessibility_clusters_builder(
    client: &SimpleHttpClient,
    name: &str,
    locale: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}:accessibilityClusters",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale {
        query_parts.push(format!("locale={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectsId}/histories/{historiesId}/executions/{executionsId}/steps/{stepsId}:accessibilityClusters
/// Lists accessibility clusters for a given Step May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if the locale format is incorrect - NOT_FOUND - if the containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_accessibility_clusters_execute()` or `toolresults_projects_histories_executions_steps_accessibility_clusters`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_accessibility_clusters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_accessibility_clusters_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListStepAccessibilityClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStepAccessibilityClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectsId}/histories/{historiesId}/executions/{executionsId}/steps/{stepsId}:accessibilityClusters
/// Lists accessibility clusters for a given Step May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if the locale format is incorrect - NOT_FOUND - if the containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_accessibility_clusters_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_accessibility_clusters_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_accessibility_clusters()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_accessibility_clusters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_accessibility_clusters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStepAccessibilityClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_accessibility_clusters_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_accessibility_clusters`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsAccessibilityClustersArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: locale
    pub locale: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectsId}/histories/{historiesId}/executions/{executionsId}/steps/{stepsId}:accessibilityClusters
/// Lists accessibility clusters for a given Step May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if the locale format is incorrect - NOT_FOUND - if the containing Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_accessibility_clusters_builder()` + `toolresults_projects_histories_executions_steps_accessibility_clusters_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_accessibility_clusters_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_accessibility_clusters(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsAccessibilityClustersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStepAccessibilityClustersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_accessibility_clusters_builder(
        client,
        &args.name,
        args.locale.as_deref(),
    )?;
    toolresults_projects_histories_executions_steps_accessibility_clusters_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Creates a Step. The returned Step will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_create_execute()` to send, or `toolresults_projects_histories_executions_steps_create` for simplest API.

pub fn toolresults_projects_histories_executions_steps_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    requestId: Option<&str>,
    body: &Step,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Creates a Step. The returned Step will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_create_execute()` or `toolresults_projects_histories_executions_steps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Step = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Creates a Step. The returned Step will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Step,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Creates a Step. The returned Step will have the id set. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_create_builder()` + `toolresults_projects_histories_executions_steps_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_create_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        args.requestId.as_deref(),
        &args.body,
    )?;
    toolresults_projects_histories_executions_steps_create_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Gets a Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_get_execute()` to send, or `toolresults_projects_histories_executions_steps_get` for simplest API.

pub fn toolresults_projects_histories_executions_steps_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Gets a Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_get_execute()` or `toolresults_projects_histories_executions_steps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Step = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Gets a Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Gets a Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_get_builder()` + `toolresults_projects_histories_executions_steps_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
    )?;
    toolresults_projects_histories_executions_steps_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Retrieves a PerfMetricsSummary. May return any of the following error code(s): - NOT_FOUND - The specified PerfMetricsSummary does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_execute()` to send, or `toolresults_projects_histories_executions_steps_get_perf_metrics_summary` for simplest API.

pub fn toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfMetricsSummary",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Retrieves a PerfMetricsSummary. May return any of the following error code(s): - NOT_FOUND - The specified PerfMetricsSummary does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_execute()` or `toolresults_projects_histories_executions_steps_get_perf_metrics_summary`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_get_perf_metrics_summary_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PerfMetricsSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Retrieves a PerfMetricsSummary. May return any of the following error code(s): - NOT_FOUND - The specified PerfMetricsSummary does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_get_perf_metrics_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_get_perf_metrics_summary_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_get_perf_metrics_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsGetPerfMetricsSummaryArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Retrieves a PerfMetricsSummary. May return any of the following error code(s): - NOT_FOUND - The specified PerfMetricsSummary does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder()` + `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_get_perf_metrics_summary_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_get_perf_metrics_summary(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsGetPerfMetricsSummaryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_get_perf_metrics_summary_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
    )?;
    toolresults_projects_histories_executions_steps_get_perf_metrics_summary_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Lists Steps for a given Execution. The steps are sorted by creation_time in descending order. The step_id key will be used to order the steps with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if an attempt is made to list the children of a nonexistent Step - NOT_FOUND - if the containing Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_list_execute()` to send, or `toolresults_projects_histories_executions_steps_list` for simplest API.

pub fn toolresults_projects_histories_executions_steps_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps",
        projectId,
        historyId,
        executionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Lists Steps for a given Execution. The steps are sorted by creation_time in descending order. The step_id key will be used to order the steps with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if an attempt is made to list the children of a nonexistent Step - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_list_execute()` or `toolresults_projects_histories_executions_steps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListStepsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStepsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Lists Steps for a given Execution. The steps are sorted by creation_time in descending order. The step_id key will be used to order the steps with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if an attempt is made to list the children of a nonexistent Step - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStepsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
/// Lists Steps for a given Execution. The steps are sorted by creation_time in descending order. The step_id key will be used to order the steps with the same creation_time. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if an argument in the request happens to be invalid; e.g. if an attempt is made to list the children of a nonexistent Step - NOT_FOUND - if the containing Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_list_builder()` + `toolresults_projects_histories_executions_steps_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListStepsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_executions_steps_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Updates an existing Step with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal (e.g try to upload a duplicate xml file), if the updated step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_patch_execute()` to send, or `toolresults_projects_histories_executions_steps_patch` for simplest API.

pub fn toolresults_projects_histories_executions_steps_patch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    requestId: Option<&str>,
    body: &Step,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Updates an existing Step with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal (e.g try to upload a duplicate xml file), if the updated step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_patch_execute()` or `toolresults_projects_histories_executions_steps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Step = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Updates an existing Step with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal (e.g try to upload a duplicate xml file), if the updated step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_patch_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPatchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Step,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
/// Updates an existing Step with the supplied partial entity. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal (e.g try to upload a duplicate xml file), if the updated step is too large (more than 10Mib) - NOT_FOUND - if the containing Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_patch_builder()` + `toolresults_projects_histories_executions_steps_patch_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_patch(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_patch_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        args.requestId.as_deref(),
        &args.body,
    )?;
    toolresults_projects_histories_executions_steps_patch_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles
/// Publish xml files to an existing Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal, e.g. try to upload a duplicate xml file or a file too large. - NOT_FOUND - if the containing Execution does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_execute()` to send, or `toolresults_projects_histories_executions_steps_publish_xunit_xml_files` for simplest API.

pub fn toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    body: &PublishXunitXmlFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}:publishXunitXmlFiles",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles
/// Publish xml files to an existing Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal, e.g. try to upload a duplicate xml file or a file too large. - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_execute()` or `toolresults_projects_histories_executions_steps_publish_xunit_xml_files`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_publish_xunit_xml_files_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Step = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles
/// Publish xml files to an existing Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal, e.g. try to upload a duplicate xml file or a file too large. - NOT_FOUND - if the containing Execution does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_publish_xunit_xml_files_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_publish_xunit_xml_files_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_publish_xunit_xml_files`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPublishXunitXmlFilesArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Request body.
    pub body: PublishXunitXmlFilesRequest,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles
/// Publish xml files to an existing Step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write project - INVALID_ARGUMENT - if the request is malformed - FAILED_PRECONDITION - if the requested state transition is illegal, e.g. try to upload a duplicate xml file or a file too large. - NOT_FOUND - if the containing Execution does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder()` + `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_publish_xunit_xml_files_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_publish_xunit_xml_files(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPublishXunitXmlFilesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Step>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_publish_xunit_xml_files_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        &args.body,
    )?;
    toolresults_projects_histories_executions_steps_publish_xunit_xml_files_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Creates a PerfMetricsSummary resource. Returns the existing one if it has already been created. May return any of the following error code(s): - NOT_FOUND - The containing Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_metrics_summary_create` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    body: &PerfMetricsSummary,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfMetricsSummary",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Creates a PerfMetricsSummary resource. Returns the existing one if it has already been created. May return any of the following error code(s): - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_execute()` or `toolresults_projects_histories_executions_steps_perf_metrics_summary_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_metrics_summary_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PerfMetricsSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Creates a PerfMetricsSummary resource. Returns the existing one if it has already been created. May return any of the following error code(s): - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_metrics_summary_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_perf_metrics_summary_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_metrics_summary_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfMetricsSummaryCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Request body.
    pub body: PerfMetricsSummary,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
/// Creates a PerfMetricsSummary resource. Returns the existing one if it has already been created. May return any of the following error code(s): - NOT_FOUND - The containing Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder()` + `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_metrics_summary_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_metrics_summary_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfMetricsSummaryCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfMetricsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        toolresults_projects_histories_executions_steps_perf_metrics_summary_create_builder(
            client,
            &args.projectId,
            &args.historyId,
            &args.executionId,
            &args.stepId,
            &args.body,
        )?;
    toolresults_projects_histories_executions_steps_perf_metrics_summary_create_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Creates a PerfSampleSeries. May return any of the following error code(s): - ALREADY_EXISTS - PerfMetricSummary already exists for the given Step - NOT_FOUND - The containing Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_sample_series_create_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_sample_series_create` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    body: &PerfSampleSeries,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Creates a PerfSampleSeries. May return any of the following error code(s): - ALREADY_EXISTS - PerfMetricSummary already exists for the given Step - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_sample_series_create_execute()` or `toolresults_projects_histories_executions_steps_perf_sample_series_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PerfSampleSeries = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Creates a PerfSampleSeries. May return any of the following error code(s): - ALREADY_EXISTS - PerfMetricSummary already exists for the given Step - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_sample_series_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_perf_sample_series_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_sample_series_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Request body.
    pub body: PerfSampleSeries,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Creates a PerfSampleSeries. May return any of the following error code(s): - ALREADY_EXISTS - PerfMetricSummary already exists for the given Step - NOT_FOUND - The containing Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_create_builder()` + `toolresults_projects_histories_executions_steps_perf_sample_series_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        toolresults_projects_histories_executions_steps_perf_sample_series_create_builder(
            client,
            &args.projectId,
            &args.historyId,
            &args.executionId,
            &args.stepId,
            &args.body,
        )?;
    toolresults_projects_histories_executions_steps_perf_sample_series_create_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}
/// Gets a PerfSampleSeries. May return any of the following error code(s): - NOT_FOUND - The specified PerfSampleSeries does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_sample_series_get_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_sample_series_get` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    sampleSeriesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}",
        projectId,
        historyId,
        executionId,
        stepId,
        sampleSeriesId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}
/// Gets a PerfSampleSeries. May return any of the following error code(s): - NOT_FOUND - The specified PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_sample_series_get_execute()` or `toolresults_projects_histories_executions_steps_perf_sample_series_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PerfSampleSeries = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}
/// Gets a PerfSampleSeries. May return any of the following error code(s): - NOT_FOUND - The specified PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_sample_series_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_perf_sample_series_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_sample_series_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Path parameter: sampleSeriesId
    pub sampleSeriesId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}
/// Gets a PerfSampleSeries. May return any of the following error code(s): - NOT_FOUND - The specified PerfSampleSeries does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_get_builder()` + `toolresults_projects_histories_executions_steps_perf_sample_series_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PerfSampleSeries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_perf_sample_series_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        &args.sampleSeriesId,
    )?;
    toolresults_projects_histories_executions_steps_perf_sample_series_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Lists PerfSampleSeries for a given Step. The request provides an optional filter which specifies one or more PerfMetricsType to include in the result; if none returns all. The resulting PerfSampleSeries are sorted by ids. May return any of the following canonical error codes: - NOT_FOUND - The containing Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_sample_series_list_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_sample_series_list` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Lists PerfSampleSeries for a given Step. The request provides an optional filter which specifies one or more PerfMetricsType to include in the result; if none returns all. The resulting PerfSampleSeries are sorted by ids. May return any of the following canonical error codes: - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_sample_series_list_execute()` or `toolresults_projects_histories_executions_steps_perf_sample_series_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListPerfSampleSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPerfSampleSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Lists PerfSampleSeries for a given Step. The request provides an optional filter which specifies one or more PerfMetricsType to include in the result; if none returns all. The resulting PerfSampleSeries are sorted by ids. May return any of the following canonical error codes: - NOT_FOUND - The containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_sample_series_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPerfSampleSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_perf_sample_series_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_sample_series_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
/// Lists PerfSampleSeries for a given Step. The request provides an optional filter which specifies one or more PerfMetricsType to include in the result; if none returns all. The resulting PerfSampleSeries are sorted by ids. May return any of the following canonical error codes: - NOT_FOUND - The containing Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_list_builder()` + `toolresults_projects_histories_executions_steps_perf_sample_series_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPerfSampleSeriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_perf_sample_series_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        args.filter.as_deref(),
    )?;
    toolresults_projects_histories_executions_steps_perf_sample_series_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate
/// Creates a batch of PerfSamples - a client can submit multiple batches of Perf Samples through repeated calls to this method in order to split up a large request payload - duplicates and existing timestamp entries will be ignored. - the batch operation may partially succeed - the set of elements successfully inserted is returned in the response (omits items which already existed in the database). May return any of the following canonical error codes: - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    sampleSeriesId: &str,
    body: &BatchCreatePerfSamplesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}/samples:batchCreate",
        projectId,
        historyId,
        executionId,
        stepId,
        sampleSeriesId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate
/// Creates a batch of PerfSamples - a client can submit multiple batches of Perf Samples through repeated calls to this method in order to split up a large request payload - duplicates and existing timestamp entries will be ignored. - the batch operation may partially succeed - the set of elements successfully inserted is returned in the response (omits items which already existed in the database). May return any of the following canonical error codes: - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_execute()` or `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<BatchCreatePerfSamplesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreatePerfSamplesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate
/// Creates a batch of PerfSamples - a client can submit multiple batches of Perf Samples through repeated calls to this method in order to split up a large request payload - duplicates and existing timestamp entries will be ignored. - the batch operation may partially succeed - the set of elements successfully inserted is returned in the response (omits items which already existed in the database). May return any of the following canonical error codes: - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreatePerfSamplesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesBatchCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Path parameter: sampleSeriesId
    pub sampleSeriesId: String,
    /// Request body.
    pub body: BatchCreatePerfSamplesRequest,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate
/// Creates a batch of PerfSamples - a client can submit multiple batches of Perf Samples through repeated calls to this method in order to split up a large request payload - duplicates and existing timestamp entries will be ignored. - the batch operation may partially succeed - the set of elements successfully inserted is returned in the response (omits items which already existed in the database). May return any of the following canonical error codes: - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder()` + `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesBatchCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreatePerfSamplesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_builder(client, &args.projectId, &args.historyId, &args.executionId, &args.stepId, &args.sampleSeriesId, &args.body)?;
    toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_execute(
        builder,
    )
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples
/// Lists the Performance Samples of a given Sample Series - The list results are sorted by timestamps ascending - The default page size is 500 samples; and maximum size allowed 5000 - The response token indicates the last returned PerfSample timestamp - When the results size exceeds the page size, submit a subsequent request including the page token to return the rest of the samples up to the page limit May return any of the following canonical error codes: - OUT_OF_RANGE - The specified request page_token is out of valid range - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_execute()` to send, or `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list` for simplest API.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    sampleSeriesId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}/samples",
        projectId,
        historyId,
        executionId,
        stepId,
        sampleSeriesId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples
/// Lists the Performance Samples of a given Sample Series - The list results are sorted by timestamps ascending - The default page size is 500 samples; and maximum size allowed 5000 - The response token indicates the last returned PerfSample timestamp - When the results size exceeds the page size, submit a subsequent request including the page token to return the rest of the samples up to the page limit May return any of the following canonical error codes: - OUT_OF_RANGE - The specified request page_token is out of valid range - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_execute()` or `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListPerfSamplesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPerfSamplesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples
/// Lists the Performance Samples of a given Sample Series - The list results are sorted by timestamps ascending - The default page size is 500 samples; and maximum size allowed 5000 - The response token indicates the last returned PerfSample timestamp - When the results size exceeds the page size, submit a subsequent request including the page token to return the rest of the samples up to the page limit May return any of the following canonical error codes: - OUT_OF_RANGE - The specified request page_token is out of valid range - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPerfSamplesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_perf_sample_series_samples_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Path parameter: sampleSeriesId
    pub sampleSeriesId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples
/// Lists the Performance Samples of a given Sample Series - The list results are sorted by timestamps ascending - The default page size is 500 samples; and maximum size allowed 5000 - The response token indicates the last returned PerfSample timestamp - When the results size exceeds the page size, submit a subsequent request including the page token to return the rest of the samples up to the page limit May return any of the following canonical error codes: - OUT_OF_RANGE - The specified request page_token is out of valid range - NOT_FOUND - The containing PerfSampleSeries does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder()` + `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPerfSamplesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_builder(
            client,
            &args.projectId,
            &args.historyId,
            &args.executionId,
            &args.stepId,
            &args.sampleSeriesId,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}
/// Gets details of a Test Case for a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Test Case does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_test_cases_get_execute()` to send, or `toolresults_projects_histories_executions_steps_test_cases_get` for simplest API.

pub fn toolresults_projects_histories_executions_steps_test_cases_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    testCaseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/testCases/{}",
        projectId,
        historyId,
        executionId,
        stepId,
        testCaseId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}
/// Gets details of a Test Case for a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Test Case does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_test_cases_get_execute()` or `toolresults_projects_histories_executions_steps_test_cases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_test_cases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestCase>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}
/// Gets details of a Test Case for a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Test Case does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_test_cases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_test_cases_get_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_test_cases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_test_cases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestCase>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_test_cases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_test_cases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsTestCasesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Path parameter: testCaseId
    pub testCaseId: String,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}
/// Gets details of a Test Case for a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Test Case does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_test_cases_get_builder()` + `toolresults_projects_histories_executions_steps_test_cases_get_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_test_cases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_test_cases_get(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsTestCasesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestCase>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_test_cases_get_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        &args.testCaseId,
    )?;
    toolresults_projects_histories_executions_steps_test_cases_get_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases
/// Lists Test Cases attached to a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Step does not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_test_cases_list_execute()` to send, or `toolresults_projects_histories_executions_steps_test_cases_list` for simplest API.

pub fn toolresults_projects_histories_executions_steps_test_cases_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/testCases",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases
/// Lists Test Cases attached to a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_test_cases_list_execute()` or `toolresults_projects_histories_executions_steps_test_cases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_test_cases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListTestCasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTestCasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases
/// Lists Test Cases attached to a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Step does not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_test_cases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_test_cases_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_test_cases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_test_cases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTestCasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_test_cases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_test_cases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsTestCasesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases
/// Lists Test Cases attached to a Step. Experimental test cases API. Still in active development. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to write to project - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the containing Step does not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_test_cases_list_builder()` + `toolresults_projects_histories_executions_steps_test_cases_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_test_cases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_test_cases_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsTestCasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTestCasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_test_cases_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_executions_steps_test_cases_list_execute(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails
/// Lists thumbnails of images attached to a step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from the project, or from any of the images - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the step does not exist, or if any of the images do not exist
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `toolresults_projects_histories_executions_steps_thumbnails_list_execute()` to send, or `toolresults_projects_histories_executions_steps_thumbnails_list` for simplest API.

pub fn toolresults_projects_histories_executions_steps_thumbnails_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    historyId: &str,
    executionId: &str,
    stepId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/thumbnails",
        projectId,
        historyId,
        executionId,
        stepId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails
/// Lists thumbnails of images attached to a step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from the project, or from any of the images - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the step does not exist, or if any of the images do not exist
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `toolresults_projects_histories_executions_steps_thumbnails_list_execute()` or `toolresults_projects_histories_executions_steps_thumbnails_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_thumbnails_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_thumbnails_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListStepThumbnailsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStepThumbnailsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails
/// Lists thumbnails of images attached to a step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from the project, or from any of the images - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the step does not exist, or if any of the images do not exist
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `toolresults_projects_histories_executions_steps_thumbnails_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `toolresults_projects_histories_executions_steps_thumbnails_list_task()`.
/// For the simplest API, use `toolresults_projects_histories_executions_steps_thumbnails_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `toolresults_projects_histories_executions_steps_thumbnails_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn toolresults_projects_histories_executions_steps_thumbnails_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStepThumbnailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = toolresults_projects_histories_executions_steps_thumbnails_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`toolresults_projects_histories_executions_steps_thumbnails_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsThumbnailsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: historyId
    pub historyId: String,
    /// Path parameter: executionId
    pub executionId: String,
    /// Path parameter: stepId
    pub stepId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails
/// Lists thumbnails of images attached to a step. May return any of the following canonical error codes: - PERMISSION_DENIED - if the user is not authorized to read from the project, or from any of the images - INVALID_ARGUMENT - if the request is malformed - NOT_FOUND - if the step does not exist, or if any of the images do not exist
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `toolresults_projects_histories_executions_steps_thumbnails_list_builder()` + `toolresults_projects_histories_executions_steps_thumbnails_list_execute()`.
/// For task-level control, use `toolresults_projects_histories_executions_steps_thumbnails_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn toolresults_projects_histories_executions_steps_thumbnails_list(
    client: &SimpleHttpClient,
    args: &ToolresultsProjectsHistoriesExecutionsStepsThumbnailsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStepThumbnailsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = toolresults_projects_histories_executions_steps_thumbnails_list_builder(
        client,
        &args.projectId,
        &args.historyId,
        &args.executionId,
        &args.stepId,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    toolresults_projects_histories_executions_steps_thumbnails_list_execute(builder)
}
