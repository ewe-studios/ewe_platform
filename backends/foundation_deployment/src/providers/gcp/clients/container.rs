//! Auto-generated API clients for gcp/container.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_aggregated_usable_subnetworks_list_execute()` to send, or `container_projects_aggregated_usable_subnetworks_list` for simplest API.

pub fn container_projects_aggregated_usable_subnetworks_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://container.googleapis.com/v1/projects/{}/aggregated/usableSubnetworks",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_aggregated_usable_subnetworks_list_execute()` or `container_projects_aggregated_usable_subnetworks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_aggregated_usable_subnetworks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_aggregated_usable_subnetworks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUsableSubnetworksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUsableSubnetworksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_aggregated_usable_subnetworks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_aggregated_usable_subnetworks_list_task()`.
/// For the simplest API, use `container_projects_aggregated_usable_subnetworks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_aggregated_usable_subnetworks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_aggregated_usable_subnetworks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUsableSubnetworksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = container_projects_aggregated_usable_subnetworks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_aggregated_usable_subnetworks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsAggregatedUsableSubnetworksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_aggregated_usable_subnetworks_list_builder()` + `container_projects_aggregated_usable_subnetworks_list_execute()`.
/// For task-level control, use `container_projects_aggregated_usable_subnetworks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_aggregated_usable_subnetworks_list(
    client: &SimpleHttpClient,
    args: &ContainerProjectsAggregatedUsableSubnetworksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUsableSubnetworksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_aggregated_usable_subnetworks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    container_projects_aggregated_usable_subnetworks_list_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_get_serverconfig_execute()` to send, or `container_projects_zones_get_serverconfig` for simplest API.

pub fn container_projects_zones_get_serverconfig_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    name: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/serverconfig",
        projectId, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_get_serverconfig_execute()` or `container_projects_zones_get_serverconfig`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_get_serverconfig_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_get_serverconfig_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServerConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServerConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_get_serverconfig_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_get_serverconfig_task()`.
/// For the simplest API, use `container_projects_zones_get_serverconfig()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_get_serverconfig_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_get_serverconfig_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = container_projects_zones_get_serverconfig_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_get_serverconfig`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesGetServerconfigArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_get_serverconfig_builder()` + `container_projects_zones_get_serverconfig_execute()`.
/// For task-level control, use `container_projects_zones_get_serverconfig_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_get_serverconfig(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesGetServerconfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_get_serverconfig_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.name,
    )?;
    container_projects_zones_get_serverconfig_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_addons_execute()` to send, or `container_projects_zones_clusters_addons` for simplest API.

pub fn container_projects_zones_clusters_addons_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetAddonsConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/addons",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_addons_execute()` or `container_projects_zones_clusters_addons`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_addons_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_addons_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_addons_task()`.
/// For the simplest API, use `container_projects_zones_clusters_addons()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_addons_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_addons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersAddonsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetAddonsConfigRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_addons_builder()` + `container_projects_zones_clusters_addons_execute()`.
/// For task-level control, use `container_projects_zones_clusters_addons_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_addons(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersAddonsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_addons_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_addons_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_complete_ip_rotation_execute()` to send, or `container_projects_zones_clusters_complete_ip_rotation` for simplest API.

pub fn container_projects_zones_clusters_complete_ip_rotation_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &CompleteIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:completeIpRotation",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_complete_ip_rotation_execute()` or `container_projects_zones_clusters_complete_ip_rotation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_complete_ip_rotation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_complete_ip_rotation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_complete_ip_rotation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_complete_ip_rotation_task()`.
/// For the simplest API, use `container_projects_zones_clusters_complete_ip_rotation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_complete_ip_rotation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_complete_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_complete_ip_rotation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_complete_ip_rotation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersCompleteIpRotationArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: CompleteIPRotationRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_complete_ip_rotation_builder()` + `container_projects_zones_clusters_complete_ip_rotation_execute()`.
/// For task-level control, use `container_projects_zones_clusters_complete_ip_rotation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_complete_ip_rotation(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersCompleteIpRotationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_complete_ip_rotation_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_complete_ip_rotation_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_create_execute()` to send, or `container_projects_zones_clusters_create` for simplest API.

pub fn container_projects_zones_clusters_create_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    body: &CreateClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters",
        projectId, zone,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_create_execute()` or `container_projects_zones_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_create_task()`.
/// For the simplest API, use `container_projects_zones_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Request body.
    pub body: CreateClusterRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_create_builder()` + `container_projects_zones_clusters_create_execute()`.
/// For task-level control, use `container_projects_zones_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_create(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_create_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.body,
    )?;
    container_projects_zones_clusters_create_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_delete_execute()` to send, or `container_projects_zones_clusters_delete` for simplest API.

pub fn container_projects_zones_clusters_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    name: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}",
        projectId, zone, clusterId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_delete_execute()` or `container_projects_zones_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_delete_task()`.
/// For the simplest API, use `container_projects_zones_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_delete_builder()` + `container_projects_zones_clusters_delete_execute()`.
/// For task-level control, use `container_projects_zones_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_delete(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_delete_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.name,
    )?;
    container_projects_zones_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_legacy_abac_execute()` to send, or `container_projects_zones_clusters_legacy_abac` for simplest API.

pub fn container_projects_zones_clusters_legacy_abac_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetLegacyAbacRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/legacyAbac",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_legacy_abac_execute()` or `container_projects_zones_clusters_legacy_abac`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_legacy_abac_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_legacy_abac_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_legacy_abac_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_legacy_abac_task()`.
/// For the simplest API, use `container_projects_zones_clusters_legacy_abac()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_legacy_abac_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_legacy_abac_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_legacy_abac_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_legacy_abac`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLegacyAbacArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetLegacyAbacRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_legacy_abac_builder()` + `container_projects_zones_clusters_legacy_abac_execute()`.
/// For task-level control, use `container_projects_zones_clusters_legacy_abac_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_legacy_abac(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersLegacyAbacArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_legacy_abac_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_legacy_abac_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_locations_execute()` to send, or `container_projects_zones_clusters_locations` for simplest API.

pub fn container_projects_zones_clusters_locations_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetLocationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/locations",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_locations_execute()` or `container_projects_zones_clusters_locations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_locations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_locations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_locations_task()`.
/// For the simplest API, use `container_projects_zones_clusters_locations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_locations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_locations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLocationsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetLocationsRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_locations_builder()` + `container_projects_zones_clusters_locations_execute()`.
/// For task-level control, use `container_projects_zones_clusters_locations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_locations(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersLocationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_locations_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_locations_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_logging_execute()` to send, or `container_projects_zones_clusters_logging` for simplest API.

pub fn container_projects_zones_clusters_logging_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetLoggingServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/logging",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_logging_execute()` or `container_projects_zones_clusters_logging`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_logging_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_logging_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_logging_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_logging_task()`.
/// For the simplest API, use `container_projects_zones_clusters_logging()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_logging_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_logging_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_logging_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_logging`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLoggingArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetLoggingServiceRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_logging_builder()` + `container_projects_zones_clusters_logging_execute()`.
/// For task-level control, use `container_projects_zones_clusters_logging_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_logging(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersLoggingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_logging_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_logging_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_master_execute()` to send, or `container_projects_zones_clusters_master` for simplest API.

pub fn container_projects_zones_clusters_master_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &UpdateMasterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/master",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_master_execute()` or `container_projects_zones_clusters_master`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_master_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_master_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_master_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_master_task()`.
/// For the simplest API, use `container_projects_zones_clusters_master()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_master_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_master_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_master_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_master`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersMasterArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: UpdateMasterRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_master_builder()` + `container_projects_zones_clusters_master_execute()`.
/// For task-level control, use `container_projects_zones_clusters_master_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_master(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersMasterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_master_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_master_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_monitoring_execute()` to send, or `container_projects_zones_clusters_monitoring` for simplest API.

pub fn container_projects_zones_clusters_monitoring_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetMonitoringServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/monitoring",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_monitoring_execute()` or `container_projects_zones_clusters_monitoring`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_monitoring_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_monitoring_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_monitoring_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_monitoring_task()`.
/// For the simplest API, use `container_projects_zones_clusters_monitoring()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_monitoring_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_monitoring_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_monitoring_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_monitoring`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersMonitoringArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetMonitoringServiceRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_monitoring_builder()` + `container_projects_zones_clusters_monitoring_execute()`.
/// For task-level control, use `container_projects_zones_clusters_monitoring_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_monitoring(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersMonitoringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_monitoring_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_monitoring_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_resource_labels_execute()` to send, or `container_projects_zones_clusters_resource_labels` for simplest API.

pub fn container_projects_zones_clusters_resource_labels_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/resourceLabels",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_resource_labels_execute()` or `container_projects_zones_clusters_resource_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_resource_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_resource_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_resource_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_resource_labels_task()`.
/// For the simplest API, use `container_projects_zones_clusters_resource_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_resource_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_resource_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_resource_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_resource_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersResourceLabelsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetLabelsRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_resource_labels_builder()` + `container_projects_zones_clusters_resource_labels_execute()`.
/// For task-level control, use `container_projects_zones_clusters_resource_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_resource_labels(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersResourceLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_resource_labels_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_resource_labels_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_maintenance_policy_execute()` to send, or `container_projects_zones_clusters_set_maintenance_policy` for simplest API.

pub fn container_projects_zones_clusters_set_maintenance_policy_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetMaintenancePolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setMaintenancePolicy",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_set_maintenance_policy_execute()` or `container_projects_zones_clusters_set_maintenance_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_maintenance_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_maintenance_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_set_maintenance_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_set_maintenance_policy_task()`.
/// For the simplest API, use `container_projects_zones_clusters_set_maintenance_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_maintenance_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_set_maintenance_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_set_maintenance_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_set_maintenance_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetMaintenancePolicyArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetMaintenancePolicyRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_maintenance_policy_builder()` + `container_projects_zones_clusters_set_maintenance_policy_execute()`.
/// For task-level control, use `container_projects_zones_clusters_set_maintenance_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_maintenance_policy(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersSetMaintenancePolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_maintenance_policy_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_set_maintenance_policy_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_master_auth_execute()` to send, or `container_projects_zones_clusters_set_master_auth` for simplest API.

pub fn container_projects_zones_clusters_set_master_auth_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetMasterAuthRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setMasterAuth",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_set_master_auth_execute()` or `container_projects_zones_clusters_set_master_auth`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_master_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_master_auth_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_set_master_auth_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_set_master_auth_task()`.
/// For the simplest API, use `container_projects_zones_clusters_set_master_auth()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_master_auth_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_set_master_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_set_master_auth_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_set_master_auth`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetMasterAuthArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetMasterAuthRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_master_auth_builder()` + `container_projects_zones_clusters_set_master_auth_execute()`.
/// For task-level control, use `container_projects_zones_clusters_set_master_auth_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_master_auth(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersSetMasterAuthArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_master_auth_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_set_master_auth_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_network_policy_execute()` to send, or `container_projects_zones_clusters_set_network_policy` for simplest API.

pub fn container_projects_zones_clusters_set_network_policy_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &SetNetworkPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setNetworkPolicy",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_set_network_policy_execute()` or `container_projects_zones_clusters_set_network_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_network_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_network_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_set_network_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_set_network_policy_task()`.
/// For the simplest API, use `container_projects_zones_clusters_set_network_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_set_network_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_set_network_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_set_network_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_set_network_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetNetworkPolicyArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: SetNetworkPolicyRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_network_policy_builder()` + `container_projects_zones_clusters_set_network_policy_execute()`.
/// For task-level control, use `container_projects_zones_clusters_set_network_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_network_policy(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersSetNetworkPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_network_policy_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_set_network_policy_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_start_ip_rotation_execute()` to send, or `container_projects_zones_clusters_start_ip_rotation` for simplest API.

pub fn container_projects_zones_clusters_start_ip_rotation_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &StartIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:startIpRotation",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_start_ip_rotation_execute()` or `container_projects_zones_clusters_start_ip_rotation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_start_ip_rotation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_start_ip_rotation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_start_ip_rotation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_start_ip_rotation_task()`.
/// For the simplest API, use `container_projects_zones_clusters_start_ip_rotation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_start_ip_rotation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_start_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_start_ip_rotation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_start_ip_rotation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersStartIpRotationArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: StartIPRotationRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_start_ip_rotation_builder()` + `container_projects_zones_clusters_start_ip_rotation_execute()`.
/// For task-level control, use `container_projects_zones_clusters_start_ip_rotation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_start_ip_rotation(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersStartIpRotationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_start_ip_rotation_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_start_ip_rotation_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_autoscaling_execute()` to send, or `container_projects_zones_clusters_node_pools_autoscaling` for simplest API.

pub fn container_projects_zones_clusters_node_pools_autoscaling_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    body: &SetNodePoolAutoscalingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/autoscaling",
        projectId,
        zone,
        clusterId,
        nodePoolId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_autoscaling_execute()` or `container_projects_zones_clusters_node_pools_autoscaling`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_autoscaling_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_autoscaling_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_autoscaling_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_autoscaling_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_autoscaling()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_autoscaling_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_autoscaling_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_autoscaling_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_autoscaling`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsAutoscalingArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Request body.
    pub body: SetNodePoolAutoscalingRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_autoscaling_builder()` + `container_projects_zones_clusters_node_pools_autoscaling_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_autoscaling_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_autoscaling(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsAutoscalingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_autoscaling_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_autoscaling_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_create_execute()` to send, or `container_projects_zones_clusters_node_pools_create` for simplest API.

pub fn container_projects_zones_clusters_node_pools_create_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    body: &CreateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools",
        projectId, zone, clusterId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_create_execute()` or `container_projects_zones_clusters_node_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_create_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Request body.
    pub body: CreateNodePoolRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_create_builder()` + `container_projects_zones_clusters_node_pools_create_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_create(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_create_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_create_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_delete_execute()` to send, or `container_projects_zones_clusters_node_pools_delete` for simplest API.

pub fn container_projects_zones_clusters_node_pools_delete_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    name: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}",
        projectId, zone, clusterId, nodePoolId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_delete_execute()` or `container_projects_zones_clusters_node_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_delete_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_delete_builder()` + `container_projects_zones_clusters_node_pools_delete_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_delete(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_delete_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.name,
    )?;
    container_projects_zones_clusters_node_pools_delete_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_rollback_execute()` to send, or `container_projects_zones_clusters_node_pools_rollback` for simplest API.

pub fn container_projects_zones_clusters_node_pools_rollback_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    body: &RollbackNodePoolUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}:rollback",
        projectId,
        zone,
        clusterId,
        nodePoolId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_rollback_execute()` or `container_projects_zones_clusters_node_pools_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_rollback_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsRollbackArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Request body.
    pub body: RollbackNodePoolUpgradeRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_rollback_builder()` + `container_projects_zones_clusters_node_pools_rollback_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_rollback(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_rollback_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_rollback_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_set_management_execute()` to send, or `container_projects_zones_clusters_node_pools_set_management` for simplest API.

pub fn container_projects_zones_clusters_node_pools_set_management_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    body: &SetNodePoolManagementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setManagement",
        projectId,
        zone,
        clusterId,
        nodePoolId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_set_management_execute()` or `container_projects_zones_clusters_node_pools_set_management`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_set_management_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_management_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_set_management_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_set_management_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_set_management()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_set_management_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_set_management_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_set_management_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_set_management`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsSetManagementArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Request body.
    pub body: SetNodePoolManagementRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_set_management_builder()` + `container_projects_zones_clusters_node_pools_set_management_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_set_management_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_management(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsSetManagementArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_set_management_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_set_management_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_set_size_execute()` to send, or `container_projects_zones_clusters_node_pools_set_size` for simplest API.

pub fn container_projects_zones_clusters_node_pools_set_size_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    body: &SetNodePoolSizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setSize",
        projectId, zone, clusterId, nodePoolId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_set_size_execute()` or `container_projects_zones_clusters_node_pools_set_size`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_set_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_size_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_set_size_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_set_size_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_set_size()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_set_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_set_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_set_size_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_set_size`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsSetSizeArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Request body.
    pub body: SetNodePoolSizeRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_set_size_builder()` + `container_projects_zones_clusters_node_pools_set_size_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_set_size_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_size(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsSetSizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_set_size_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_set_size_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_update_execute()` to send, or `container_projects_zones_clusters_node_pools_update` for simplest API.

pub fn container_projects_zones_clusters_node_pools_update_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    clusterId: &String,
    nodePoolId: &String,
    body: &UpdateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/update",
        projectId, zone, clusterId, nodePoolId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_clusters_node_pools_update_execute()` or `container_projects_zones_clusters_node_pools_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_clusters_node_pools_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_clusters_node_pools_update_task()`.
/// For the simplest API, use `container_projects_zones_clusters_node_pools_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_clusters_node_pools_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_clusters_node_pools_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_clusters_node_pools_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_clusters_node_pools_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsUpdateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: clusterId
    pub clusterId: String,
    /// Path parameter: nodePoolId
    pub nodePoolId: String,
    /// Request body.
    pub body: UpdateNodePoolRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_update_builder()` + `container_projects_zones_clusters_node_pools_update_execute()`.
/// For task-level control, use `container_projects_zones_clusters_node_pools_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_update(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesClustersNodePoolsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_update_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.clusterId,
        &args.nodePoolId,
        &args.body,
    )?;
    container_projects_zones_clusters_node_pools_update_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_cancel_execute()` to send, or `container_projects_zones_operations_cancel` for simplest API.

pub fn container_projects_zones_operations_cancel_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    operationId: &String,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations/{}:cancel",
        projectId, zone, operationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_operations_cancel_execute()` or `container_projects_zones_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_operations_cancel_task()`.
/// For the simplest API, use `container_projects_zones_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsCancelArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: operationId
    pub operationId: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_cancel_builder()` + `container_projects_zones_operations_cancel_execute()`.
/// For task-level control, use `container_projects_zones_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_cancel(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_operations_cancel_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.operationId,
        &args.body,
    )?;
    container_projects_zones_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_get_execute()` to send, or `container_projects_zones_operations_get` for simplest API.

pub fn container_projects_zones_operations_get_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    operationId: &String,
    name: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations/{}",
        projectId, zone, operationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_operations_get_execute()` or `container_projects_zones_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_operations_get_task()`.
/// For the simplest API, use `container_projects_zones_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = container_projects_zones_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Path parameter: operationId
    pub operationId: String,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_get_builder()` + `container_projects_zones_operations_get_execute()`.
/// For task-level control, use `container_projects_zones_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_get(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_operations_get_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.operationId,
        &args.name,
    )?;
    container_projects_zones_operations_get_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_list_execute()` to send, or `container_projects_zones_operations_list` for simplest API.

pub fn container_projects_zones_operations_list_builder(
    client: &SimpleHttpClient,
    projectId: &String,
    zone: &String,
    parent: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations",
        projectId, zone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `container_projects_zones_operations_list_execute()` or `container_projects_zones_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `container_projects_zones_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `container_projects_zones_operations_list_task()`.
/// For the simplest API, use `container_projects_zones_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `container_projects_zones_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn container_projects_zones_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = container_projects_zones_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`container_projects_zones_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: zone
    pub zone: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_list_builder()` + `container_projects_zones_operations_list_execute()`.
/// For task-level control, use `container_projects_zones_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_list(
    client: &SimpleHttpClient,
    args: &ContainerProjectsZonesOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_operations_list_builder(
        client,
        &args.projectId,
        &args.zone,
        &args.parent,
    )?;
    container_projects_zones_operations_list_execute(builder)
}
