//! Auto-generated API clients for Google Cloud Platform - container.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_aggregated_usable_subnetworks_list_execute()` to send, or `container_projects_aggregated_usable_subnetworks_list` for simplest API.

pub fn container_projects_aggregated_usable_subnetworks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/aggregated/usableSubnetworks",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_aggregated_usable_subnetworks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_aggregated_usable_subnetworks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_aggregated_usable_subnetworks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUsableSubnetworksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUsableSubnetworksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/aggregated/usableSubnetworks
/// Lists subnetworks that are usable for creating clusters in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_aggregated_usable_subnetworks_list_builder()` + `container_projects_aggregated_usable_subnetworks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_aggregated_usable_subnetworks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUsableSubnetworksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_aggregated_usable_subnetworks_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    container_projects_aggregated_usable_subnetworks_list_execute(builder)
}

/// GET v1/{+name}/serverConfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_get_server_config_execute()` to send, or `container_projects_locations_get_server_config` for simplest API.

pub fn container_projects_locations_get_server_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/serverConfig", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/serverConfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_get_server_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_get_server_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_get_server_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServerConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/serverConfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_get_server_config_builder()` + `container_projects_locations_get_server_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_get_server_config(
    client: &SimpleHttpClient,
    name: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_get_server_config_builder(client, name, projectId, zone)?;
    container_projects_locations_get_server_config_execute(builder)
}

/// GET v1/{+name}:checkAutopilotCompatibility
/// Checks the cluster compatibility with Autopilot mode, and returns a list of compatibility issues.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_check_autopilot_compatibility_execute()` to send, or `container_projects_locations_clusters_check_autopilot_compatibility` for simplest API.

pub fn container_projects_locations_clusters_check_autopilot_compatibility_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:checkAutopilotCompatibility",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:checkAutopilotCompatibility
/// Checks the cluster compatibility with Autopilot mode, and returns a list of compatibility issues.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_check_autopilot_compatibility_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_check_autopilot_compatibility_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_check_autopilot_compatibility_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckAutopilotCompatibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAutopilotCompatibilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:checkAutopilotCompatibility
/// Checks the cluster compatibility with Autopilot mode, and returns a list of compatibility issues.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_check_autopilot_compatibility_builder()` + `container_projects_locations_clusters_check_autopilot_compatibility_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_check_autopilot_compatibility(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckAutopilotCompatibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_check_autopilot_compatibility_builder(client, name)?;
    container_projects_locations_clusters_check_autopilot_compatibility_execute(builder)
}

/// GET v1/{+name}:completeIpRotation
/// Completes master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_complete_ip_rotation_execute()` to send, or `container_projects_locations_clusters_complete_ip_rotation` for simplest API.

pub fn container_projects_locations_clusters_complete_ip_rotation_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:completeIpRotation", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:completeIpRotation
/// Completes master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_complete_ip_rotation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_complete_ip_rotation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_complete_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:completeIpRotation
/// Completes master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_complete_ip_rotation_builder()` + `container_projects_locations_clusters_complete_ip_rotation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_complete_ip_rotation(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteIPRotationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_complete_ip_rotation_builder(client, name, body)?;
    container_projects_locations_clusters_complete_ip_rotation_execute(builder)
}

/// GET v1/{+parent}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_create_execute()` to send, or `container_projects_locations_clusters_create` for simplest API.

pub fn container_projects_locations_clusters_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clusters", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_create_builder()` + `container_projects_locations_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_create_builder(client, parent, body)?;
    container_projects_locations_clusters_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_delete_execute()` to send, or `container_projects_locations_clusters_delete` for simplest API.

pub fn container_projects_locations_clusters_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterId",
            clusterId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_delete_builder()` + `container_projects_locations_clusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_delete(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_delete_builder(
        client, name, clusterId, projectId, zone,
    )?;
    container_projects_locations_clusters_delete_execute(builder)
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_fetch_cluster_upgrade_info_execute()` to send, or `container_projects_locations_clusters_fetch_cluster_upgrade_info` for simplest API.

pub fn container_projects_locations_clusters_fetch_cluster_upgrade_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:fetchClusterUpgradeInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_fetch_cluster_upgrade_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_fetch_cluster_upgrade_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_fetch_cluster_upgrade_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClusterUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClusterUpgradeInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_fetch_cluster_upgrade_info_builder()` + `container_projects_locations_clusters_fetch_cluster_upgrade_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_fetch_cluster_upgrade_info(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClusterUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_fetch_cluster_upgrade_info_builder(
        client, name, version,
    )?;
    container_projects_locations_clusters_fetch_cluster_upgrade_info_execute(builder)
}

/// GET v1/{+name}
/// Gets the details of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_get_execute()` to send, or `container_projects_locations_clusters_get` for simplest API.

pub fn container_projects_locations_clusters_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterId",
            clusterId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Gets the details of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the details of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_get_builder()` + `container_projects_locations_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_get(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_get_builder(
        client, name, clusterId, projectId, zone,
    )?;
    container_projects_locations_clusters_get_execute(builder)
}

/// GET v1/{+parent}/jwks
/// Gets the public component of the cluster signing keys in JSON Web Key format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_get_jwks_execute()` to send, or `container_projects_locations_clusters_get_jwks` for simplest API.

pub fn container_projects_locations_clusters_get_jwks_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/jwks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/jwks
/// Gets the public component of the cluster signing keys in JSON Web Key format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_get_jwks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_get_jwks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_get_jwks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetJSONWebKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetJSONWebKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/jwks
/// Gets the public component of the cluster signing keys in JSON Web Key format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_get_jwks_builder()` + `container_projects_locations_clusters_get_jwks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_get_jwks(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetJSONWebKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_get_jwks_builder(client, parent)?;
    container_projects_locations_clusters_get_jwks_execute(builder)
}

/// GET v1/{+parent}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_list_execute()` to send, or `container_projects_locations_clusters_list` for simplest API.

pub fn container_projects_locations_clusters_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clusters", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_list_builder()` + `container_projects_locations_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_list(
    client: &SimpleHttpClient,
    parent: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_list_builder(client, parent, projectId, zone)?;
    container_projects_locations_clusters_list_execute(builder)
}

/// GET v1/{+name}:setAddons
/// Sets the addons for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_addons_execute()` to send, or `container_projects_locations_clusters_set_addons` for simplest API.

pub fn container_projects_locations_clusters_set_addons_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetAddonsConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setAddons", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setAddons
/// Sets the addons for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_addons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_addons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setAddons
/// Sets the addons for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_addons_builder()` + `container_projects_locations_clusters_set_addons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_addons(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetAddonsConfigRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_set_addons_builder(client, name, body)?;
    container_projects_locations_clusters_set_addons_execute(builder)
}

/// GET v1/{+name}:setLegacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_legacy_abac_execute()` to send, or `container_projects_locations_clusters_set_legacy_abac` for simplest API.

pub fn container_projects_locations_clusters_set_legacy_abac_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLegacyAbacRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setLegacyAbac", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setLegacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_legacy_abac_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_legacy_abac_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_legacy_abac_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setLegacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_legacy_abac_builder()` + `container_projects_locations_clusters_set_legacy_abac_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_legacy_abac(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLegacyAbacRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_set_legacy_abac_builder(client, name, body)?;
    container_projects_locations_clusters_set_legacy_abac_execute(builder)
}

/// GET v1/{+name}:setLocations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_locations_execute()` to send, or `container_projects_locations_clusters_set_locations` for simplest API.

pub fn container_projects_locations_clusters_set_locations_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLocationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setLocations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setLocations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_locations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_locations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setLocations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_locations_builder()` + `container_projects_locations_clusters_set_locations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_locations(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLocationsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_set_locations_builder(client, name, body)?;
    container_projects_locations_clusters_set_locations_execute(builder)
}

/// GET v1/{+name}:setLogging
/// Sets the logging service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_logging_execute()` to send, or `container_projects_locations_clusters_set_logging` for simplest API.

pub fn container_projects_locations_clusters_set_logging_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLoggingServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setLogging", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setLogging
/// Sets the logging service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_logging_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_logging_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_logging_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setLogging
/// Sets the logging service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_logging_builder()` + `container_projects_locations_clusters_set_logging_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_logging(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLoggingServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_set_logging_builder(client, name, body)?;
    container_projects_locations_clusters_set_logging_execute(builder)
}

/// GET v1/{+name}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_maintenance_policy_execute()` to send, or `container_projects_locations_clusters_set_maintenance_policy` for simplest API.

pub fn container_projects_locations_clusters_set_maintenance_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMaintenancePolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:setMaintenancePolicy",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_maintenance_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_maintenance_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_maintenance_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_maintenance_policy_builder()` + `container_projects_locations_clusters_set_maintenance_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_maintenance_policy(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMaintenancePolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_set_maintenance_policy_builder(client, name, body)?;
    container_projects_locations_clusters_set_maintenance_policy_execute(builder)
}

/// GET v1/{+name}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_master_auth_execute()` to send, or `container_projects_locations_clusters_set_master_auth` for simplest API.

pub fn container_projects_locations_clusters_set_master_auth_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMasterAuthRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setMasterAuth", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_master_auth_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_master_auth_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_master_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_master_auth_builder()` + `container_projects_locations_clusters_set_master_auth_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_master_auth(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMasterAuthRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_set_master_auth_builder(client, name, body)?;
    container_projects_locations_clusters_set_master_auth_execute(builder)
}

/// GET v1/{+name}:setMonitoring
/// Sets the monitoring service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_monitoring_execute()` to send, or `container_projects_locations_clusters_set_monitoring` for simplest API.

pub fn container_projects_locations_clusters_set_monitoring_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMonitoringServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setMonitoring", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setMonitoring
/// Sets the monitoring service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_monitoring_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_monitoring_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_monitoring_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setMonitoring
/// Sets the monitoring service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_monitoring_builder()` + `container_projects_locations_clusters_set_monitoring_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_monitoring(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetMonitoringServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_set_monitoring_builder(client, name, body)?;
    container_projects_locations_clusters_set_monitoring_execute(builder)
}

/// GET v1/{+name}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_network_policy_execute()` to send, or `container_projects_locations_clusters_set_network_policy` for simplest API.

pub fn container_projects_locations_clusters_set_network_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNetworkPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setNetworkPolicy", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_network_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_network_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_network_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_network_policy_builder()` + `container_projects_locations_clusters_set_network_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_network_policy(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNetworkPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_set_network_policy_builder(client, name, body)?;
    container_projects_locations_clusters_set_network_policy_execute(builder)
}

/// GET v1/{+name}:setResourceLabels
/// Sets labels on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_set_resource_labels_execute()` to send, or `container_projects_locations_clusters_set_resource_labels` for simplest API.

pub fn container_projects_locations_clusters_set_resource_labels_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setResourceLabels", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setResourceLabels
/// Sets labels on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_set_resource_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_set_resource_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_resource_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setResourceLabels
/// Sets labels on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_set_resource_labels_builder()` + `container_projects_locations_clusters_set_resource_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_set_resource_labels(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_set_resource_labels_builder(client, name, body)?;
    container_projects_locations_clusters_set_resource_labels_execute(builder)
}

/// GET v1/{+name}:startIpRotation
/// Starts master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_start_ip_rotation_execute()` to send, or `container_projects_locations_clusters_start_ip_rotation` for simplest API.

pub fn container_projects_locations_clusters_start_ip_rotation_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:startIpRotation", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:startIpRotation
/// Starts master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_start_ip_rotation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_start_ip_rotation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_start_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:startIpRotation
/// Starts master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_start_ip_rotation_builder()` + `container_projects_locations_clusters_start_ip_rotation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_start_ip_rotation(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartIPRotationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_start_ip_rotation_builder(client, name, body)?;
    container_projects_locations_clusters_start_ip_rotation_execute(builder)
}

/// GET v1/{+name}
/// Updates the settings of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_update_execute()` to send, or `container_projects_locations_clusters_update` for simplest API.

pub fn container_projects_locations_clusters_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the settings of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the settings of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_update_builder()` + `container_projects_locations_clusters_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_update_builder(client, name, body)?;
    container_projects_locations_clusters_update_execute(builder)
}

/// GET v1/{+name}:updateMaster
/// Updates the master for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_update_master_execute()` to send, or `container_projects_locations_clusters_update_master` for simplest API.

pub fn container_projects_locations_clusters_update_master_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateMasterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:updateMaster", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:updateMaster
/// Updates the master for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_update_master_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_update_master_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_update_master_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:updateMaster
/// Updates the master for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_update_master_builder()` + `container_projects_locations_clusters_update_master_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_update_master(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateMasterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_update_master_builder(client, name, body)?;
    container_projects_locations_clusters_update_master_execute(builder)
}

/// GET v1/{+name}:completeUpgrade
/// CompleteNodePoolUpgrade will signal an on-going node pool upgrade to complete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_complete_upgrade_execute()` to send, or `container_projects_locations_clusters_node_pools_complete_upgrade` for simplest API.

pub fn container_projects_locations_clusters_node_pools_complete_upgrade_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteNodePoolUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:completeUpgrade", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:completeUpgrade
/// CompleteNodePoolUpgrade will signal an on-going node pool upgrade to complete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_complete_upgrade_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_complete_upgrade_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_complete_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:completeUpgrade
/// CompleteNodePoolUpgrade will signal an on-going node pool upgrade to complete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_complete_upgrade_builder()` + `container_projects_locations_clusters_node_pools_complete_upgrade_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_complete_upgrade(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteNodePoolUpgradeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_complete_upgrade_builder(
        client, name, body,
    )?;
    container_projects_locations_clusters_node_pools_complete_upgrade_execute(builder)
}

/// GET v1/{+parent}/nodePools
/// Creates a node pool for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_create_execute()` to send, or `container_projects_locations_clusters_node_pools_create` for simplest API.

pub fn container_projects_locations_clusters_node_pools_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/nodePools", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/nodePools
/// Creates a node pool for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/nodePools
/// Creates a node pool for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_create_builder()` + `container_projects_locations_clusters_node_pools_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_node_pools_create_builder(client, parent, body)?;
    container_projects_locations_clusters_node_pools_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a node pool from a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_delete_execute()` to send, or `container_projects_locations_clusters_node_pools_delete` for simplest API.

pub fn container_projects_locations_clusters_node_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    nodePoolId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterId",
            clusterId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "nodePoolId",
            nodePoolId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a node pool from a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a node pool from a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_delete_builder()` + `container_projects_locations_clusters_node_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_delete(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    nodePoolId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_delete_builder(
        client, name, clusterId, nodePoolId, projectId, zone,
    )?;
    container_projects_locations_clusters_node_pools_delete_execute(builder)
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_execute()` to send, or `container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info` for simplest API.

pub fn container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:fetchNodePoolUpgradeInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePoolUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodePoolUpgradeInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_builder()` + `container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePoolUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_builder(
            client, name, version,
        )?;
    container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_execute(builder)
}

/// GET v1/{+name}
/// Retrieves the requested node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_get_execute()` to send, or `container_projects_locations_clusters_node_pools_get` for simplest API.

pub fn container_projects_locations_clusters_node_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    nodePoolId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterId",
            clusterId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "nodePoolId",
            nodePoolId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Retrieves the requested node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves the requested node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_get_builder()` + `container_projects_locations_clusters_node_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_get(
    client: &SimpleHttpClient,
    name: &str,
    clusterId: Option<&str>,
    nodePoolId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_get_builder(
        client, name, clusterId, nodePoolId, projectId, zone,
    )?;
    container_projects_locations_clusters_node_pools_get_execute(builder)
}

/// GET v1/{+parent}/nodePools
/// Lists the node pools for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_list_execute()` to send, or `container_projects_locations_clusters_node_pools_list` for simplest API.

pub fn container_projects_locations_clusters_node_pools_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/nodePools", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterId",
            clusterId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/nodePools
/// Lists the node pools for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodePoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/nodePools
/// Lists the node pools for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_list_builder()` + `container_projects_locations_clusters_node_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_list(
    client: &SimpleHttpClient,
    parent: &str,
    clusterId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodePoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_list_builder(
        client, parent, clusterId, projectId, zone,
    )?;
    container_projects_locations_clusters_node_pools_list_execute(builder)
}

/// GET v1/{+name}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_rollback_execute()` to send, or `container_projects_locations_clusters_node_pools_rollback` for simplest API.

pub fn container_projects_locations_clusters_node_pools_rollback_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RollbackNodePoolUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:rollback", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_rollback_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_rollback_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_rollback_builder()` + `container_projects_locations_clusters_node_pools_rollback_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_rollback(
    client: &SimpleHttpClient,
    name: &str,
    body: &RollbackNodePoolUpgradeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_node_pools_rollback_builder(client, name, body)?;
    container_projects_locations_clusters_node_pools_rollback_execute(builder)
}

/// GET v1/{+name}:setAutoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_set_autoscaling_execute()` to send, or `container_projects_locations_clusters_node_pools_set_autoscaling` for simplest API.

pub fn container_projects_locations_clusters_node_pools_set_autoscaling_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolAutoscalingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setAutoscaling", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setAutoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_set_autoscaling_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_set_autoscaling_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_autoscaling_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setAutoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_set_autoscaling_builder()` + `container_projects_locations_clusters_node_pools_set_autoscaling_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_autoscaling(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolAutoscalingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_set_autoscaling_builder(
        client, name, body,
    )?;
    container_projects_locations_clusters_node_pools_set_autoscaling_execute(builder)
}

/// GET v1/{+name}:setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_set_management_execute()` to send, or `container_projects_locations_clusters_node_pools_set_management` for simplest API.

pub fn container_projects_locations_clusters_node_pools_set_management_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolManagementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setManagement", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_set_management_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_set_management_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_management_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_set_management_builder()` + `container_projects_locations_clusters_node_pools_set_management_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_management(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolManagementRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_clusters_node_pools_set_management_builder(
        client, name, body,
    )?;
    container_projects_locations_clusters_node_pools_set_management_execute(builder)
}

/// GET v1/{+name}:setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_set_size_execute()` to send, or `container_projects_locations_clusters_node_pools_set_size` for simplest API.

pub fn container_projects_locations_clusters_node_pools_set_size_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolSizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setSize", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_set_size_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_set_size_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_set_size_builder()` + `container_projects_locations_clusters_node_pools_set_size_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_set_size(
    client: &SimpleHttpClient,
    name: &str,
    body: &SetNodePoolSizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_node_pools_set_size_builder(client, name, body)?;
    container_projects_locations_clusters_node_pools_set_size_execute(builder)
}

/// GET v1/{+name}
/// Updates the version `and/or` image type for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_node_pools_update_execute()` to send, or `container_projects_locations_clusters_node_pools_update` for simplest API.

pub fn container_projects_locations_clusters_node_pools_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the version `and/or` image type for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_node_pools_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_node_pools_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the version `and/or` image type for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_node_pools_update_builder()` + `container_projects_locations_clusters_node_pools_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_node_pools_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_node_pools_update_builder(client, name, body)?;
    container_projects_locations_clusters_node_pools_update_execute(builder)
}

/// GET v1/{+parent}/.well-known/openid-configuration
/// Gets the OIDC discovery document for the cluster. See the [OpenID Connect Discovery 1.0 specification](<https://openid.`net/specs/openid-connect-discovery-1_0`.html>) for details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_clusters_well_known_get_openid_configuration_execute()` to send, or `container_projects_locations_clusters_well_known_get_openid_configuration` for simplest API.

pub fn container_projects_locations_clusters_well_known_get_openid_configuration_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/.well-known/openid-configuration",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/.well-known/openid-configuration
/// Gets the OIDC discovery document for the cluster. See the [OpenID Connect Discovery 1.0 specification](<https://openid.`net/specs/openid-connect-discovery-1_0`.html>) for details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_clusters_well_known_get_openid_configuration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_clusters_well_known_get_openid_configuration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_well_known_get_openid_configuration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetOpenIDConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetOpenIDConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/.well-known/openid-configuration
/// Gets the OIDC discovery document for the cluster. See the [OpenID Connect Discovery 1.0 specification](<https://openid.`net/specs/openid-connect-discovery-1_0`.html>) for details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_clusters_well_known_get_openid_configuration_builder()` + `container_projects_locations_clusters_well_known_get_openid_configuration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_clusters_well_known_get_openid_configuration(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetOpenIDConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_clusters_well_known_get_openid_configuration_builder(
            client, parent,
        )?;
    container_projects_locations_clusters_well_known_get_openid_configuration_execute(builder)
}

/// GET v1/{+name}:cancel
/// Cancels the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_operations_cancel_execute()` to send, or `container_projects_locations_operations_cancel` for simplest API.

pub fn container_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Cancels the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Cancels the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_operations_cancel_builder()` + `container_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_operations_cancel_builder(client, name, body)?;
    container_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Gets the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_operations_get_execute()` to send, or `container_projects_locations_operations_get` for simplest API.

pub fn container_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    operationId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "operationId",
            operationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Gets the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_operations_get_builder()` + `container_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
    operationId: Option<&str>,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_locations_operations_get_builder(
        client,
        name,
        operationId,
        projectId,
        zone,
    )?;
    container_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+parent}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_locations_operations_list_execute()` to send, or `container_projects_locations_operations_list` for simplest API.

pub fn container_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/operations", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "projectId",
            projectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "zone",
            zone.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_locations_operations_list_builder()` + `container_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_locations_operations_list(
    client: &SimpleHttpClient,
    parent: &str,
    projectId: Option<&str>,
    zone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_locations_operations_list_builder(client, parent, projectId, zone)?;
    container_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_get_serverconfig_execute()` to send, or `container_projects_zones_get_serverconfig` for simplest API.

pub fn container_projects_zones_get_serverconfig_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/serverconfig",
        projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_get_serverconfig_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_get_serverconfig_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_get_serverconfig_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServerConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig
/// Returns configuration info about the Google Kubernetes Engine service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_get_serverconfig_builder()` + `container_projects_zones_get_serverconfig_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_get_serverconfig(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_get_serverconfig_builder(client, projectId, zone, name)?;
    container_projects_zones_get_serverconfig_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_addons_execute()` to send, or `container_projects_zones_clusters_addons` for simplest API.

pub fn container_projects_zones_clusters_addons_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetAddonsConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/addons",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_addons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_addons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
/// Sets the addons for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_addons_builder()` + `container_projects_zones_clusters_addons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_addons(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetAddonsConfigRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_addons_builder(client, clusterId, projectId, zone, body)?;
    container_projects_zones_clusters_addons_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_complete_ip_rotation_execute()` to send, or `container_projects_zones_clusters_complete_ip_rotation` for simplest API.

pub fn container_projects_zones_clusters_complete_ip_rotation_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &CompleteIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}:completeIpRotation",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_complete_ip_rotation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_complete_ip_rotation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_complete_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
/// Completes master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_complete_ip_rotation_builder()` + `container_projects_zones_clusters_complete_ip_rotation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_complete_ip_rotation(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &CompleteIPRotationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_complete_ip_rotation_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_complete_ip_rotation_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_create_execute()` to send, or `container_projects_zones_clusters_create` for simplest API.

pub fn container_projects_zones_clusters_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    body: &CreateClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters",
        projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Creates a cluster, consisting of the specified number and type of Google Compute Engine instances. By default, the cluster is created in the project's [default network](<https://cloud.google.`com/compute/docs/networks-and-firewalls`#networks>). One firewall is added for the cluster. After cluster creation, the kubelet creates routes for each node to allow the containers on that node to communicate with all other instances in the cluster. Finally, an entry is added to the project's global metadata indicating which CIDR range the cluster is using.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_create_builder()` + `container_projects_zones_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_create(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    body: &CreateClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_create_builder(client, projectId, zone, body)?;
    container_projects_zones_clusters_create_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_delete_execute()` to send, or `container_projects_zones_clusters_delete` for simplest API.

pub fn container_projects_zones_clusters_delete_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Deletes the cluster, including the Kubernetes endpoint and all worker nodes. Firewalls and routes that were configured during cluster creation are also deleted. Other Google Compute Engine resources that might be in use by the cluster, such as load balancer resources, are not deleted if they weren't present when the cluster was initially created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_delete_builder()` + `container_projects_zones_clusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_delete(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_delete_builder(client, clusterId, projectId, zone, name)?;
    container_projects_zones_clusters_delete_execute(builder)
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_fetch_cluster_upgrade_info_execute()` to send, or `container_projects_zones_clusters_fetch_cluster_upgrade_info` for simplest API.

pub fn container_projects_zones_clusters_fetch_cluster_upgrade_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:fetchClusterUpgradeInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_fetch_cluster_upgrade_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_fetch_cluster_upgrade_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_fetch_cluster_upgrade_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClusterUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClusterUpgradeInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:fetchClusterUpgradeInfo
/// Fetch upgrade information of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_fetch_cluster_upgrade_info_builder()` + `container_projects_zones_clusters_fetch_cluster_upgrade_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_fetch_cluster_upgrade_info(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClusterUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_fetch_cluster_upgrade_info_builder(
        client, name, version,
    )?;
    container_projects_zones_clusters_fetch_cluster_upgrade_info_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Gets the details of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_get_execute()` to send, or `container_projects_zones_clusters_get` for simplest API.

pub fn container_projects_zones_clusters_get_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Gets the details of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Gets the details of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_get_builder()` + `container_projects_zones_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_get(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_get_builder(client, clusterId, projectId, zone, name)?;
    container_projects_zones_clusters_get_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_legacy_abac_execute()` to send, or `container_projects_zones_clusters_legacy_abac` for simplest API.

pub fn container_projects_zones_clusters_legacy_abac_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLegacyAbacRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/legacyAbac",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_legacy_abac_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_legacy_abac_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_legacy_abac_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
/// Enables or disables the ABAC authorization mechanism on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_legacy_abac_builder()` + `container_projects_zones_clusters_legacy_abac_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_legacy_abac(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLegacyAbacRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_legacy_abac_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_legacy_abac_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_list_execute()` to send, or `container_projects_zones_clusters_list` for simplest API.

pub fn container_projects_zones_clusters_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters",
        projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters
/// Lists all clusters owned by a project in either the specified zone or all zones.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_list_builder()` + `container_projects_zones_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_list(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_list_builder(client, projectId, zone, parent)?;
    container_projects_zones_clusters_list_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_locations_execute()` to send, or `container_projects_zones_clusters_locations` for simplest API.

pub fn container_projects_zones_clusters_locations_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLocationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/locations",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_locations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_locations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
/// Sets the locations for a specific cluster. Deprecated. Use [projects.locations.clusters.update](<https://cloud.google.`com/kubernetes-engine/docs/reference/rest/v1/projects`.locations.`clusters/update`>) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_locations_builder()` + `container_projects_zones_clusters_locations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_locations(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLocationsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_locations_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_locations_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_logging_execute()` to send, or `container_projects_zones_clusters_logging` for simplest API.

pub fn container_projects_zones_clusters_logging_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLoggingServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/logging",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_logging_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_logging_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_logging_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
/// Sets the logging service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_logging_builder()` + `container_projects_zones_clusters_logging_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_logging(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLoggingServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_logging_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_logging_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_master_execute()` to send, or `container_projects_zones_clusters_master` for simplest API.

pub fn container_projects_zones_clusters_master_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateMasterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/master",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_master_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_master_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_master_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
/// Updates the master for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_master_builder()` + `container_projects_zones_clusters_master_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_master(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateMasterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_master_builder(client, clusterId, projectId, zone, body)?;
    container_projects_zones_clusters_master_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_monitoring_execute()` to send, or `container_projects_zones_clusters_monitoring` for simplest API.

pub fn container_projects_zones_clusters_monitoring_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMonitoringServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/monitoring",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_monitoring_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_monitoring_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_monitoring_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
/// Sets the monitoring service for a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_monitoring_builder()` + `container_projects_zones_clusters_monitoring_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_monitoring(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMonitoringServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_monitoring_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_monitoring_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_resource_labels_execute()` to send, or `container_projects_zones_clusters_resource_labels` for simplest API.

pub fn container_projects_zones_clusters_resource_labels_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/resourceLabels",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_resource_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_resource_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_resource_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
/// Sets labels on a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_resource_labels_builder()` + `container_projects_zones_clusters_resource_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_resource_labels(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_resource_labels_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_resource_labels_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_maintenance_policy_execute()` to send, or `container_projects_zones_clusters_set_maintenance_policy` for simplest API.

pub fn container_projects_zones_clusters_set_maintenance_policy_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMaintenancePolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}:setMaintenancePolicy",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_set_maintenance_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_set_maintenance_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_maintenance_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
/// Sets the maintenance policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_maintenance_policy_builder()` + `container_projects_zones_clusters_set_maintenance_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_maintenance_policy(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMaintenancePolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_maintenance_policy_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_set_maintenance_policy_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_master_auth_execute()` to send, or `container_projects_zones_clusters_set_master_auth` for simplest API.

pub fn container_projects_zones_clusters_set_master_auth_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMasterAuthRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}:setMasterAuth",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_set_master_auth_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_set_master_auth_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_master_auth_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
/// Sets master auth materials. Currently supports changing the admin password or a specific cluster, either via password generation or explicitly setting the password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_master_auth_builder()` + `container_projects_zones_clusters_set_master_auth_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_master_auth(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetMasterAuthRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_master_auth_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_set_master_auth_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_set_network_policy_execute()` to send, or `container_projects_zones_clusters_set_network_policy` for simplest API.

pub fn container_projects_zones_clusters_set_network_policy_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNetworkPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}:setNetworkPolicy",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_set_network_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_set_network_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_network_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
/// Enables or disables Network Policy for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_set_network_policy_builder()` + `container_projects_zones_clusters_set_network_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_set_network_policy(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNetworkPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_set_network_policy_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_set_network_policy_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_start_ip_rotation_execute()` to send, or `container_projects_zones_clusters_start_ip_rotation` for simplest API.

pub fn container_projects_zones_clusters_start_ip_rotation_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &StartIPRotationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}:startIpRotation",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_start_ip_rotation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_start_ip_rotation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_start_ip_rotation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
/// Starts master IP rotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_start_ip_rotation_builder()` + `container_projects_zones_clusters_start_ip_rotation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_start_ip_rotation(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &StartIPRotationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_start_ip_rotation_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_start_ip_rotation_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Updates the settings of a specific cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_update_execute()` to send, or `container_projects_zones_clusters_update` for simplest API.

pub fn container_projects_zones_clusters_update_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Updates the settings of a specific cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
/// Updates the settings of a specific cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_update_builder()` + `container_projects_zones_clusters_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_update(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_update_builder(client, clusterId, projectId, zone, body)?;
    container_projects_zones_clusters_update_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_autoscaling_execute()` to send, or `container_projects_zones_clusters_node_pools_autoscaling` for simplest API.

pub fn container_projects_zones_clusters_node_pools_autoscaling_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolAutoscalingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}/autoscaling",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_autoscaling_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_autoscaling_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_autoscaling_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
/// Sets the autoscaling settings for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_autoscaling_builder()` + `container_projects_zones_clusters_node_pools_autoscaling_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_autoscaling(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolAutoscalingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_autoscaling_builder(
        client, clusterId, nodePoolId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_autoscaling_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_create_execute()` to send, or `container_projects_zones_clusters_node_pools_create` for simplest API.

pub fn container_projects_zones_clusters_node_pools_create_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &CreateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Creates a node pool for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_create_builder()` + `container_projects_zones_clusters_node_pools_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_create(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    body: &CreateNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_create_builder(
        client, clusterId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_create_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_delete_execute()` to send, or `container_projects_zones_clusters_node_pools_delete` for simplest API.

pub fn container_projects_zones_clusters_node_pools_delete_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Deletes a node pool from a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_delete_builder()` + `container_projects_zones_clusters_node_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_delete(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_delete_builder(
        client, clusterId, nodePoolId, projectId, zone, name,
    )?;
    container_projects_zones_clusters_node_pools_delete_execute(builder)
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_execute()` to send, or `container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info` for simplest API.

pub fn container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:fetchNodePoolUpgradeInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePoolUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodePoolUpgradeInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:fetchNodePoolUpgradeInfo
/// Fetch upgrade information of a specific nodepool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_builder()` + `container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePoolUpgradeInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_builder(
            client, name, version,
        )?;
    container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Retrieves the requested node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_get_execute()` to send, or `container_projects_zones_clusters_node_pools_get` for simplest API.

pub fn container_projects_zones_clusters_node_pools_get_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Retrieves the requested node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
/// Retrieves the requested node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_get_builder()` + `container_projects_zones_clusters_node_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_get(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_get_builder(
        client, clusterId, nodePoolId, projectId, zone, name,
    )?;
    container_projects_zones_clusters_node_pools_get_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Lists the node pools for a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_list_execute()` to send, or `container_projects_zones_clusters_node_pools_list` for simplest API.

pub fn container_projects_zones_clusters_node_pools_list_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools",
        clusterId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Lists the node pools for a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodePoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNodePoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
/// Lists the node pools for a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_list_builder()` + `container_projects_zones_clusters_node_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_list(
    client: &SimpleHttpClient,
    clusterId: &str,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodePoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_list_builder(
        client, clusterId, projectId, zone, parent,
    )?;
    container_projects_zones_clusters_node_pools_list_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_rollback_execute()` to send, or `container_projects_zones_clusters_node_pools_rollback` for simplest API.

pub fn container_projects_zones_clusters_node_pools_rollback_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &RollbackNodePoolUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}:rollback",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_rollback_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_rollback_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
/// Rolls back a previously Aborted or Failed NodePool upgrade. This makes no changes if the last upgrade successfully completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_rollback_builder()` + `container_projects_zones_clusters_node_pools_rollback_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_rollback(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &RollbackNodePoolUpgradeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_rollback_builder(
        client, clusterId, nodePoolId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_rollback_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_set_management_execute()` to send, or `container_projects_zones_clusters_node_pools_set_management` for simplest API.

pub fn container_projects_zones_clusters_node_pools_set_management_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolManagementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setManagement",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_set_management_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_set_management_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_management_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
/// Sets the NodeManagement options for a node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_set_management_builder()` + `container_projects_zones_clusters_node_pools_set_management_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_management(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolManagementRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_set_management_builder(
        client, clusterId, nodePoolId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_set_management_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_set_size_execute()` to send, or `container_projects_zones_clusters_node_pools_set_size` for simplest API.

pub fn container_projects_zones_clusters_node_pools_set_size_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolSizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setSize",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_set_size_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_set_size_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
/// Sets the size for a specific node pool. The new size will be used for all replicas, including future replicas created by modifying NodePool.locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_set_size_builder()` + `container_projects_zones_clusters_node_pools_set_size_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_set_size(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &SetNodePoolSizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_set_size_builder(
        client, clusterId, nodePoolId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_set_size_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_clusters_node_pools_update_execute()` to send, or `container_projects_zones_clusters_node_pools_update` for simplest API.

pub fn container_projects_zones_clusters_node_pools_update_builder(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateNodePoolRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/clusters/{}/nodePools/{}/update",
        clusterId, nodePoolId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_clusters_node_pools_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_clusters_node_pools_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
/// Updates the version `and/or` image type for the specified node pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_clusters_node_pools_update_builder()` + `container_projects_zones_clusters_node_pools_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_clusters_node_pools_update(
    client: &SimpleHttpClient,
    clusterId: &str,
    nodePoolId: &str,
    projectId: &str,
    zone: &str,
    body: &UpdateNodePoolRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_clusters_node_pools_update_builder(
        client, clusterId, nodePoolId, projectId, zone, body,
    )?;
    container_projects_zones_clusters_node_pools_update_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_cancel_execute()` to send, or `container_projects_zones_operations_cancel` for simplest API.

pub fn container_projects_zones_operations_cancel_builder(
    client: &SimpleHttpClient,
    operationId: &str,
    projectId: &str,
    zone: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/operations/{}:cancel",
        operationId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
/// Cancels the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_cancel_builder()` + `container_projects_zones_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_cancel(
    client: &SimpleHttpClient,
    operationId: &str,
    projectId: &str,
    zone: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_operations_cancel_builder(
        client,
        operationId,
        projectId,
        zone,
        body,
    )?;
    container_projects_zones_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_get_execute()` to send, or `container_projects_zones_operations_get` for simplest API.

pub fn container_projects_zones_operations_get_builder(
    client: &SimpleHttpClient,
    operationId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/operations/{}",
        operationId, projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
/// Gets the specified operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_get_builder()` + `container_projects_zones_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_get(
    client: &SimpleHttpClient,
    operationId: &str,
    projectId: &str,
    zone: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = container_projects_zones_operations_get_builder(
        client,
        operationId,
        projectId,
        zone,
        name,
    )?;
    container_projects_zones_operations_get_execute(builder)
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `container_projects_zones_operations_list_execute()` to send, or `container_projects_zones_operations_list` for simplest API.

pub fn container_projects_zones_operations_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/zones/{}/operations",
        projectId, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `container_projects_zones_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from container_projects_zones_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/zones/{zone}/operations
/// Lists all operations in a project in a specific zone or all zones.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `container_projects_zones_operations_list_builder()` + `container_projects_zones_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn container_projects_zones_operations_list(
    client: &SimpleHttpClient,
    projectId: &str,
    zone: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        container_projects_zones_operations_list_builder(client, projectId, zone, parent)?;
    container_projects_zones_operations_list_execute(builder)
}
