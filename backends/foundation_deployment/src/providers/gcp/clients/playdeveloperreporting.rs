//! Auto-generated API clients for gcp/playdeveloperreporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_anomalies_list_execute()` to send, or `playdeveloperreporting_anomalies_list` for simplest API.

pub fn playdeveloperreporting_anomalies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anomalies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_anomalies_list_execute()` or `playdeveloperreporting_anomalies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_anomalies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_anomalies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_anomalies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_anomalies_list_task()`.
/// For the simplest API, use `playdeveloperreporting_anomalies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_anomalies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_anomalies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_anomalies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_anomalies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAnomaliesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_anomalies_list_builder()` + `playdeveloperreporting_anomalies_list_execute()`.
/// For task-level control, use `playdeveloperreporting_anomalies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_anomalies_list(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingAnomaliesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_anomalies_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    playdeveloperreporting_anomalies_list_execute(builder)
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_apps_fetch_release_filter_options_execute()` to send, or `playdeveloperreporting_apps_fetch_release_filter_options` for simplest API.

pub fn playdeveloperreporting_apps_fetch_release_filter_options_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}:fetchReleaseFilterOptions",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_apps_fetch_release_filter_options_execute()` or `playdeveloperreporting_apps_fetch_release_filter_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_apps_fetch_release_filter_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_fetch_release_filter_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_apps_fetch_release_filter_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_apps_fetch_release_filter_options_task()`.
/// For the simplest API, use `playdeveloperreporting_apps_fetch_release_filter_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_apps_fetch_release_filter_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_apps_fetch_release_filter_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_apps_fetch_release_filter_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_apps_fetch_release_filter_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_apps_fetch_release_filter_options_builder()` + `playdeveloperreporting_apps_fetch_release_filter_options_execute()`.
/// For task-level control, use `playdeveloperreporting_apps_fetch_release_filter_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_fetch_release_filter_options(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_apps_fetch_release_filter_options_builder(client, &args.name)?;
    playdeveloperreporting_apps_fetch_release_filter_options_execute(builder)
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_apps_search_execute()` to send, or `playdeveloperreporting_apps_search` for simplest API.

pub fn playdeveloperreporting_apps_search_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://playdeveloperreporting.googleapis.com/v1beta1/apps:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_apps_search_execute()` or `playdeveloperreporting_apps_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_apps_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_apps_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_apps_search_task()`.
/// For the simplest API, use `playdeveloperreporting_apps_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_apps_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_apps_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_apps_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_apps_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAppsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_apps_search_builder()` + `playdeveloperreporting_apps_search_execute()`.
/// For task-level control, use `playdeveloperreporting_apps_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_search(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingAppsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_apps_search_builder(client, &args.pageSize, &args.pageToken)?;
    playdeveloperreporting_apps_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_anrrate_get_execute()` to send, or `playdeveloperreporting_vitals_anrrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_anrrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anrRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_anrrate_get_execute()` or `playdeveloperreporting_vitals_anrrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_anrrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1AnrRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_anrrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_anrrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_anrrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_anrrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_anrrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_anrrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_anrrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsAnrrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_anrrate_get_builder()` + `playdeveloperreporting_vitals_anrrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_anrrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsAnrrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_anrrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_anrrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_anrrate_query_execute()` to send, or `playdeveloperreporting_vitals_anrrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_anrrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anrRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_anrrate_query_execute()` or `playdeveloperreporting_vitals_anrrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_anrrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_anrrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_anrrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_anrrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_anrrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_anrrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_anrrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_anrrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsAnrrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_anrrate_query_builder()` + `playdeveloperreporting_vitals_anrrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_anrrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsAnrrateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_anrrate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_anrrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_crashrate_get_execute()` to send, or `playdeveloperreporting_vitals_crashrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_crashrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/crashRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_crashrate_get_execute()` or `playdeveloperreporting_vitals_crashrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_crashrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1CrashRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_crashrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_crashrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_crashrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_crashrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_crashrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_crashrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_crashrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsCrashrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_crashrate_get_builder()` + `playdeveloperreporting_vitals_crashrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_crashrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsCrashrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_crashrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_crashrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_crashrate_query_execute()` to send, or `playdeveloperreporting_vitals_crashrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_crashrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/crashRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_crashrate_query_execute()` or `playdeveloperreporting_vitals_crashrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_crashrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_crashrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_crashrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_crashrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_crashrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_crashrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_crashrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_crashrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsCrashrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_crashrate_query_builder()` + `playdeveloperreporting_vitals_crashrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_crashrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsCrashrateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_crashrate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_crashrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_counts_get_execute()` to send, or `playdeveloperreporting_vitals_errors_counts_get` for simplest API.

pub fn playdeveloperreporting_vitals_errors_counts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorCountMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_errors_counts_get_execute()` or `playdeveloperreporting_vitals_errors_counts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_counts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_errors_counts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_errors_counts_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_errors_counts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_counts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_errors_counts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_errors_counts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_errors_counts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsCountsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_get_builder()` + `playdeveloperreporting_vitals_errors_counts_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_errors_counts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsErrorsCountsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_counts_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_errors_counts_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_counts_query_execute()` to send, or `playdeveloperreporting_vitals_errors_counts_query` for simplest API.

pub fn playdeveloperreporting_vitals_errors_counts_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorCountMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_errors_counts_query_execute()` or `playdeveloperreporting_vitals_errors_counts_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_counts_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_errors_counts_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_errors_counts_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_errors_counts_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_counts_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_errors_counts_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_errors_counts_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_errors_counts_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsCountsQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_query_builder()` + `playdeveloperreporting_vitals_errors_counts_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_errors_counts_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsErrorsCountsQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_counts_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_errors_counts_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_issues_search_execute()` to send, or `playdeveloperreporting_vitals_errors_issues_search` for simplest API.

pub fn playdeveloperreporting_vitals_errors_issues_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    interval_endTime_day: &Option<String>,
    interval_endTime_hours: &Option<String>,
    interval_endTime_minutes: &Option<String>,
    interval_endTime_month: &Option<String>,
    interval_endTime_nanos: &Option<String>,
    interval_endTime_seconds: &Option<String>,
    interval_endTime_timeZone_id: &Option<String>,
    interval_endTime_timeZone_version: &Option<String>,
    interval_endTime_utcOffset: &Option<String>,
    interval_endTime_year: &Option<String>,
    interval_startTime_day: &Option<String>,
    interval_startTime_hours: &Option<String>,
    interval_startTime_minutes: &Option<String>,
    interval_startTime_month: &Option<String>,
    interval_startTime_nanos: &Option<String>,
    interval_startTime_seconds: &Option<String>,
    interval_startTime_timeZone_id: &Option<String>,
    interval_startTime_timeZone_version: &Option<String>,
    interval_startTime_utcOffset: &Option<String>,
    interval_startTime_year: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    sampleErrorReportLimit: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorIssues:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime_day.as_ref() {
        query_parts.push(format!("interval.endTime.day={}", val));
    }
    if let Some(val) = interval_endTime_hours.as_ref() {
        query_parts.push(format!("interval.endTime.hours={}", val));
    }
    if let Some(val) = interval_endTime_minutes.as_ref() {
        query_parts.push(format!("interval.endTime.minutes={}", val));
    }
    if let Some(val) = interval_endTime_month.as_ref() {
        query_parts.push(format!("interval.endTime.month={}", val));
    }
    if let Some(val) = interval_endTime_nanos.as_ref() {
        query_parts.push(format!("interval.endTime.nanos={}", val));
    }
    if let Some(val) = interval_endTime_seconds.as_ref() {
        query_parts.push(format!("interval.endTime.seconds={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_id.as_ref() {
        query_parts.push(format!("interval.endTime.timeZone.id={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_version.as_ref() {
        query_parts.push(format!("interval.endTime.timeZone.version={}", val));
    }
    if let Some(val) = interval_endTime_utcOffset.as_ref() {
        query_parts.push(format!("interval.endTime.utcOffset={}", val));
    }
    if let Some(val) = interval_endTime_year.as_ref() {
        query_parts.push(format!("interval.endTime.year={}", val));
    }
    if let Some(val) = interval_startTime_day.as_ref() {
        query_parts.push(format!("interval.startTime.day={}", val));
    }
    if let Some(val) = interval_startTime_hours.as_ref() {
        query_parts.push(format!("interval.startTime.hours={}", val));
    }
    if let Some(val) = interval_startTime_minutes.as_ref() {
        query_parts.push(format!("interval.startTime.minutes={}", val));
    }
    if let Some(val) = interval_startTime_month.as_ref() {
        query_parts.push(format!("interval.startTime.month={}", val));
    }
    if let Some(val) = interval_startTime_nanos.as_ref() {
        query_parts.push(format!("interval.startTime.nanos={}", val));
    }
    if let Some(val) = interval_startTime_seconds.as_ref() {
        query_parts.push(format!("interval.startTime.seconds={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_id.as_ref() {
        query_parts.push(format!("interval.startTime.timeZone.id={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_version.as_ref() {
        query_parts.push(format!("interval.startTime.timeZone.version={}", val));
    }
    if let Some(val) = interval_startTime_utcOffset.as_ref() {
        query_parts.push(format!("interval.startTime.utcOffset={}", val));
    }
    if let Some(val) = interval_startTime_year.as_ref() {
        query_parts.push(format!("interval.startTime.year={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sampleErrorReportLimit.as_ref() {
        query_parts.push(format!("sampleErrorReportLimit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_errors_issues_search_execute()` or `playdeveloperreporting_vitals_errors_issues_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_issues_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_issues_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_errors_issues_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_errors_issues_search_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_errors_issues_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_issues_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_errors_issues_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_errors_issues_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_errors_issues_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsIssuesSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: interval_endTime_day
    pub interval_endTime_day: Option<String>,
    /// Query parameter: interval_endTime_hours
    pub interval_endTime_hours: Option<String>,
    /// Query parameter: interval_endTime_minutes
    pub interval_endTime_minutes: Option<String>,
    /// Query parameter: interval_endTime_month
    pub interval_endTime_month: Option<String>,
    /// Query parameter: interval_endTime_nanos
    pub interval_endTime_nanos: Option<String>,
    /// Query parameter: interval_endTime_seconds
    pub interval_endTime_seconds: Option<String>,
    /// Query parameter: interval_endTime_timeZone_id
    pub interval_endTime_timeZone_id: Option<String>,
    /// Query parameter: interval_endTime_timeZone_version
    pub interval_endTime_timeZone_version: Option<String>,
    /// Query parameter: interval_endTime_utcOffset
    pub interval_endTime_utcOffset: Option<String>,
    /// Query parameter: interval_endTime_year
    pub interval_endTime_year: Option<String>,
    /// Query parameter: interval_startTime_day
    pub interval_startTime_day: Option<String>,
    /// Query parameter: interval_startTime_hours
    pub interval_startTime_hours: Option<String>,
    /// Query parameter: interval_startTime_minutes
    pub interval_startTime_minutes: Option<String>,
    /// Query parameter: interval_startTime_month
    pub interval_startTime_month: Option<String>,
    /// Query parameter: interval_startTime_nanos
    pub interval_startTime_nanos: Option<String>,
    /// Query parameter: interval_startTime_seconds
    pub interval_startTime_seconds: Option<String>,
    /// Query parameter: interval_startTime_timeZone_id
    pub interval_startTime_timeZone_id: Option<String>,
    /// Query parameter: interval_startTime_timeZone_version
    pub interval_startTime_timeZone_version: Option<String>,
    /// Query parameter: interval_startTime_utcOffset
    pub interval_startTime_utcOffset: Option<String>,
    /// Query parameter: interval_startTime_year
    pub interval_startTime_year: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sampleErrorReportLimit
    pub sampleErrorReportLimit: Option<String>,
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_issues_search_builder()` + `playdeveloperreporting_vitals_errors_issues_search_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_errors_issues_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_issues_search(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsErrorsIssuesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_issues_search_builder(
        client,
        &args.parent,
        &args.filter,
        &args.interval_endTime_day,
        &args.interval_endTime_hours,
        &args.interval_endTime_minutes,
        &args.interval_endTime_month,
        &args.interval_endTime_nanos,
        &args.interval_endTime_seconds,
        &args.interval_endTime_timeZone_id,
        &args.interval_endTime_timeZone_version,
        &args.interval_endTime_utcOffset,
        &args.interval_endTime_year,
        &args.interval_startTime_day,
        &args.interval_startTime_hours,
        &args.interval_startTime_minutes,
        &args.interval_startTime_month,
        &args.interval_startTime_nanos,
        &args.interval_startTime_seconds,
        &args.interval_startTime_timeZone_id,
        &args.interval_startTime_timeZone_version,
        &args.interval_startTime_utcOffset,
        &args.interval_startTime_year,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.sampleErrorReportLimit,
    )?;
    playdeveloperreporting_vitals_errors_issues_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_reports_search_execute()` to send, or `playdeveloperreporting_vitals_errors_reports_search` for simplest API.

pub fn playdeveloperreporting_vitals_errors_reports_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    interval_endTime_day: &Option<String>,
    interval_endTime_hours: &Option<String>,
    interval_endTime_minutes: &Option<String>,
    interval_endTime_month: &Option<String>,
    interval_endTime_nanos: &Option<String>,
    interval_endTime_seconds: &Option<String>,
    interval_endTime_timeZone_id: &Option<String>,
    interval_endTime_timeZone_version: &Option<String>,
    interval_endTime_utcOffset: &Option<String>,
    interval_endTime_year: &Option<String>,
    interval_startTime_day: &Option<String>,
    interval_startTime_hours: &Option<String>,
    interval_startTime_minutes: &Option<String>,
    interval_startTime_month: &Option<String>,
    interval_startTime_nanos: &Option<String>,
    interval_startTime_seconds: &Option<String>,
    interval_startTime_timeZone_id: &Option<String>,
    interval_startTime_timeZone_version: &Option<String>,
    interval_startTime_utcOffset: &Option<String>,
    interval_startTime_year: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorReports:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime_day.as_ref() {
        query_parts.push(format!("interval.endTime.day={}", val));
    }
    if let Some(val) = interval_endTime_hours.as_ref() {
        query_parts.push(format!("interval.endTime.hours={}", val));
    }
    if let Some(val) = interval_endTime_minutes.as_ref() {
        query_parts.push(format!("interval.endTime.minutes={}", val));
    }
    if let Some(val) = interval_endTime_month.as_ref() {
        query_parts.push(format!("interval.endTime.month={}", val));
    }
    if let Some(val) = interval_endTime_nanos.as_ref() {
        query_parts.push(format!("interval.endTime.nanos={}", val));
    }
    if let Some(val) = interval_endTime_seconds.as_ref() {
        query_parts.push(format!("interval.endTime.seconds={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_id.as_ref() {
        query_parts.push(format!("interval.endTime.timeZone.id={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_version.as_ref() {
        query_parts.push(format!("interval.endTime.timeZone.version={}", val));
    }
    if let Some(val) = interval_endTime_utcOffset.as_ref() {
        query_parts.push(format!("interval.endTime.utcOffset={}", val));
    }
    if let Some(val) = interval_endTime_year.as_ref() {
        query_parts.push(format!("interval.endTime.year={}", val));
    }
    if let Some(val) = interval_startTime_day.as_ref() {
        query_parts.push(format!("interval.startTime.day={}", val));
    }
    if let Some(val) = interval_startTime_hours.as_ref() {
        query_parts.push(format!("interval.startTime.hours={}", val));
    }
    if let Some(val) = interval_startTime_minutes.as_ref() {
        query_parts.push(format!("interval.startTime.minutes={}", val));
    }
    if let Some(val) = interval_startTime_month.as_ref() {
        query_parts.push(format!("interval.startTime.month={}", val));
    }
    if let Some(val) = interval_startTime_nanos.as_ref() {
        query_parts.push(format!("interval.startTime.nanos={}", val));
    }
    if let Some(val) = interval_startTime_seconds.as_ref() {
        query_parts.push(format!("interval.startTime.seconds={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_id.as_ref() {
        query_parts.push(format!("interval.startTime.timeZone.id={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_version.as_ref() {
        query_parts.push(format!("interval.startTime.timeZone.version={}", val));
    }
    if let Some(val) = interval_startTime_utcOffset.as_ref() {
        query_parts.push(format!("interval.startTime.utcOffset={}", val));
    }
    if let Some(val) = interval_startTime_year.as_ref() {
        query_parts.push(format!("interval.startTime.year={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_errors_reports_search_execute()` or `playdeveloperreporting_vitals_errors_reports_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_reports_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_errors_reports_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_errors_reports_search_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_errors_reports_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_errors_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_errors_reports_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_errors_reports_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_errors_reports_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsReportsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: interval_endTime_day
    pub interval_endTime_day: Option<String>,
    /// Query parameter: interval_endTime_hours
    pub interval_endTime_hours: Option<String>,
    /// Query parameter: interval_endTime_minutes
    pub interval_endTime_minutes: Option<String>,
    /// Query parameter: interval_endTime_month
    pub interval_endTime_month: Option<String>,
    /// Query parameter: interval_endTime_nanos
    pub interval_endTime_nanos: Option<String>,
    /// Query parameter: interval_endTime_seconds
    pub interval_endTime_seconds: Option<String>,
    /// Query parameter: interval_endTime_timeZone_id
    pub interval_endTime_timeZone_id: Option<String>,
    /// Query parameter: interval_endTime_timeZone_version
    pub interval_endTime_timeZone_version: Option<String>,
    /// Query parameter: interval_endTime_utcOffset
    pub interval_endTime_utcOffset: Option<String>,
    /// Query parameter: interval_endTime_year
    pub interval_endTime_year: Option<String>,
    /// Query parameter: interval_startTime_day
    pub interval_startTime_day: Option<String>,
    /// Query parameter: interval_startTime_hours
    pub interval_startTime_hours: Option<String>,
    /// Query parameter: interval_startTime_minutes
    pub interval_startTime_minutes: Option<String>,
    /// Query parameter: interval_startTime_month
    pub interval_startTime_month: Option<String>,
    /// Query parameter: interval_startTime_nanos
    pub interval_startTime_nanos: Option<String>,
    /// Query parameter: interval_startTime_seconds
    pub interval_startTime_seconds: Option<String>,
    /// Query parameter: interval_startTime_timeZone_id
    pub interval_startTime_timeZone_id: Option<String>,
    /// Query parameter: interval_startTime_timeZone_version
    pub interval_startTime_timeZone_version: Option<String>,
    /// Query parameter: interval_startTime_utcOffset
    pub interval_startTime_utcOffset: Option<String>,
    /// Query parameter: interval_startTime_year
    pub interval_startTime_year: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_reports_search_builder()` + `playdeveloperreporting_vitals_errors_reports_search_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_errors_reports_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_reports_search(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsErrorsReportsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_reports_search_builder(
        client,
        &args.parent,
        &args.filter,
        &args.interval_endTime_day,
        &args.interval_endTime_hours,
        &args.interval_endTime_minutes,
        &args.interval_endTime_month,
        &args.interval_endTime_nanos,
        &args.interval_endTime_seconds,
        &args.interval_endTime_timeZone_id,
        &args.interval_endTime_timeZone_version,
        &args.interval_endTime_utcOffset,
        &args.interval_endTime_year,
        &args.interval_startTime_day,
        &args.interval_startTime_hours,
        &args.interval_startTime_minutes,
        &args.interval_startTime_month,
        &args.interval_startTime_nanos,
        &args.interval_startTime_seconds,
        &args.interval_startTime_timeZone_id,
        &args.interval_startTime_timeZone_version,
        &args.interval_startTime_utcOffset,
        &args.interval_startTime_year,
        &args.pageSize,
        &args.pageToken,
    )?;
    playdeveloperreporting_vitals_errors_reports_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_excessivewakeuprate_get_execute()` to send, or `playdeveloperreporting_vitals_excessivewakeuprate_get` for simplest API.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/excessiveWakeupRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_excessivewakeuprate_get_execute()` or `playdeveloperreporting_vitals_excessivewakeuprate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_excessivewakeuprate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_excessivewakeuprate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_excessivewakeuprate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_excessivewakeuprate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()` + `playdeveloperreporting_vitals_excessivewakeuprate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_excessivewakeuprate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_excessivewakeuprate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_excessivewakeuprate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_excessivewakeuprate_query_execute()` to send, or `playdeveloperreporting_vitals_excessivewakeuprate_query` for simplest API.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/excessiveWakeupRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_excessivewakeuprate_query_execute()` or `playdeveloperreporting_vitals_excessivewakeuprate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_excessivewakeuprate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_excessivewakeuprate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_excessivewakeuprate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_excessivewakeuprate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()` + `playdeveloperreporting_vitals_excessivewakeuprate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_excessivewakeuprate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_excessivewakeuprate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_excessivewakeuprate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_lmkrate_get_execute()` to send, or `playdeveloperreporting_vitals_lmkrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_lmkrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/lmkRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_lmkrate_get_execute()` or `playdeveloperreporting_vitals_lmkrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_lmkrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1LmkRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_lmkrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_lmkrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_lmkrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_lmkrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_lmkrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_lmkrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_lmkrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsLmkrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_get_builder()` + `playdeveloperreporting_vitals_lmkrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_lmkrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsLmkrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_lmkrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_lmkrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_lmkrate_query_execute()` to send, or `playdeveloperreporting_vitals_lmkrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_lmkrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/lmkRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_lmkrate_query_execute()` or `playdeveloperreporting_vitals_lmkrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_lmkrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_lmkrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_lmkrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_lmkrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_lmkrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_lmkrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_lmkrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_lmkrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsLmkrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_query_builder()` + `playdeveloperreporting_vitals_lmkrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_lmkrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsLmkrateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_lmkrate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_lmkrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowrenderingrate_get_execute()` to send, or `playdeveloperreporting_vitals_slowrenderingrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowRenderingRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_slowrenderingrate_get_execute()` or `playdeveloperreporting_vitals_slowrenderingrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowrenderingrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_slowrenderingrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_slowrenderingrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_slowrenderingrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowrenderingrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_slowrenderingrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_slowrenderingrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowrenderingrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_get_builder()` + `playdeveloperreporting_vitals_slowrenderingrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_slowrenderingrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsSlowrenderingrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowrenderingrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_slowrenderingrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowrenderingrate_query_execute()` to send, or `playdeveloperreporting_vitals_slowrenderingrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowRenderingRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_slowrenderingrate_query_execute()` or `playdeveloperreporting_vitals_slowrenderingrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowrenderingrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_slowrenderingrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_slowrenderingrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_slowrenderingrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowrenderingrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_slowrenderingrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_slowrenderingrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_query_builder()` + `playdeveloperreporting_vitals_slowrenderingrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_slowrenderingrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_slowrenderingrate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_slowrenderingrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowstartrate_get_execute()` to send, or `playdeveloperreporting_vitals_slowstartrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_slowstartrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowStartRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_slowstartrate_get_execute()` or `playdeveloperreporting_vitals_slowstartrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowstartrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_slowstartrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_slowstartrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_slowstartrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowstartrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_slowstartrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_slowstartrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_slowstartrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowstartrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_get_builder()` + `playdeveloperreporting_vitals_slowstartrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_slowstartrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsSlowstartrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowstartrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_slowstartrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowstartrate_query_execute()` to send, or `playdeveloperreporting_vitals_slowstartrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_slowstartrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowStartRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_slowstartrate_query_execute()` or `playdeveloperreporting_vitals_slowstartrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowstartrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_slowstartrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_slowstartrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_slowstartrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_slowstartrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_slowstartrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_slowstartrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_slowstartrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowstartrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_query_builder()` + `playdeveloperreporting_vitals_slowstartrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_slowstartrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsSlowstartrateQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowstartrate_query_builder(client, &args.name)?;
    playdeveloperreporting_vitals_slowstartrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute()` to send, or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/stuckBackgroundWakelockRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute()` or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()` + `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder(client, &args.name)?;
    playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute(builder)
}

/// POST v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute()` to send, or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/stuckBackgroundWakelockRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute()` or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
        Ready = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, ApiError>,
        Pending = ApiPending,
        Spawner = BoxedSendExecutionAction
    > + Send + 'static,
    ApiError,
>{
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_task()`.
/// For the simplest API, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
>{
    let task = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()` + `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute()`.
/// For task-level control, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query(
    client: &SimpleHttpClient,
    args: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
>{
    let builder = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder(
        client, &args.name,
    )?;
    playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse with PlaydeveloperreportingAnomaliesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingAnomaliesListArgs>
    for GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingAnomaliesListArgs) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions with PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs>
    for GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse with PlaydeveloperreportingAppsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingAppsSearchArgs>
    for GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingAppsSearchArgs) -> String {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1AnrRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1AnrRateMetricSet with PlaydeveloperreportingVitalsAnrrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsAnrrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1AnrRateMetricSet
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingVitalsAnrrateGetArgs) -> String {
        format!(
            "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1AnrRateMetricSet/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1AnrRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse with PlaydeveloperreportingVitalsAnrrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsAnrrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingVitalsAnrrateQueryArgs) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1CrashRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1CrashRateMetricSet with PlaydeveloperreportingVitalsCrashrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsCrashrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1CrashRateMetricSet
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingVitalsCrashrateGetArgs) -> String {
        format!(
            "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1CrashRateMetricSet/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1CrashRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse with PlaydeveloperreportingVitalsCrashrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsCrashrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsCrashrateQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet with PlaydeveloperreportingVitalsErrorsCountsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsErrorsCountsGetArgs>
    for GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsErrorsCountsGetArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse with PlaydeveloperreportingVitalsErrorsCountsQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsErrorsCountsQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsErrorsCountsQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse with PlaydeveloperreportingVitalsErrorsIssuesSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsErrorsIssuesSearchArgs>
    for GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsErrorsIssuesSearchArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse with PlaydeveloperreportingVitalsErrorsReportsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsErrorsReportsSearchArgs>
    for GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsErrorsReportsSearchArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet with PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs>
    for GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse with PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1LmkRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1LmkRateMetricSet with PlaydeveloperreportingVitalsLmkrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsLmkrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1LmkRateMetricSet
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingVitalsLmkrateGetArgs) -> String {
        format!(
            "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1LmkRateMetricSet/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1LmkRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse with PlaydeveloperreportingVitalsLmkrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsLmkrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse
{
    fn generate_resource_id(&self, input: &PlaydeveloperreportingVitalsLmkrateQueryArgs) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet with PlaydeveloperreportingVitalsSlowrenderingrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsSlowrenderingrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsSlowrenderingrateGetArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse with PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet with PlaydeveloperreportingVitalsSlowstartrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsSlowstartrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsSlowstartrateGetArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse with PlaydeveloperreportingVitalsSlowstartrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsSlowstartrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsSlowstartrateQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet with PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs>
    for GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse
// =============================================================================

/// ResourceIdentifier implementation for GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse with PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs>
    for GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse
{
    fn generate_resource_id(
        &self,
        input: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs,
    ) -> String {
        format!("gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::playdeveloperreporting::GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
