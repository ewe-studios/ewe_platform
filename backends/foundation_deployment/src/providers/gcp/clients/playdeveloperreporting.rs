//! Auto-generated API clients for gcp/playdeveloperreporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/playdeveloperreporting`

#![cfg(feature = "gcp/playdeveloperreporting")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_anomalies_list_execute()` to send, or `playdeveloperreporting_anomalies_list` for simplest API.

pub fn playdeveloperreporting_anomalies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anomalies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_anomalies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_anomalies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_anomalies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/anomalies
/// Lists anomalies in any of the datasets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_anomalies_list_builder()` + `playdeveloperreporting_anomalies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_anomalies_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_anomalies_list_builder(client, parent, filter, pageSize, pageToken)?;
    playdeveloperreporting_anomalies_list_execute(builder)
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_apps_fetch_release_filter_options_execute()` to send, or `playdeveloperreporting_apps_fetch_release_filter_options` for simplest API.

pub fn playdeveloperreporting_apps_fetch_release_filter_options_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}:fetchReleaseFilterOptions",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_apps_fetch_release_filter_options_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_apps_fetch_release_filter_options_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_fetch_release_filter_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}:fetchReleaseFilterOptions
/// Describes filtering options for releases.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_apps_fetch_release_filter_options_builder()` + `playdeveloperreporting_apps_fetch_release_filter_options_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_fetch_release_filter_options(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_apps_fetch_release_filter_options_builder(client, name)?;
    playdeveloperreporting_apps_fetch_release_filter_options_execute(builder)
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_apps_search_execute()` to send, or `playdeveloperreporting_apps_search` for simplest API.

pub fn playdeveloperreporting_apps_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://playdeveloperreporting.googleapis.com/v1beta1/apps:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_apps_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_apps_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps:search
/// Searches for Apps accessible by the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_apps_search_builder()` + `playdeveloperreporting_apps_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_apps_search(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_apps_search_builder(client, pageSize, pageToken)?;
    playdeveloperreporting_apps_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_anrrate_get_execute()` to send, or `playdeveloperreporting_vitals_anrrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_anrrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anrRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_anrrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_anrrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1AnrRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_anrrate_get_builder()` + `playdeveloperreporting_vitals_anrrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_anrrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_anrrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_anrrate_query_execute()` to send, or `playdeveloperreporting_vitals_anrrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_anrrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/anrRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_anrrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_anrrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/anrRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_anrrate_query_builder()` + `playdeveloperreporting_vitals_anrrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_anrrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_anrrate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_anrrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_crashrate_get_execute()` to send, or `playdeveloperreporting_vitals_crashrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_crashrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/crashRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_crashrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_crashrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1CrashRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_crashrate_get_builder()` + `playdeveloperreporting_vitals_crashrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_crashrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_crashrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_crashrate_query_execute()` to send, or `playdeveloperreporting_vitals_crashrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_crashrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/crashRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_crashrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_crashrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/crashRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_crashrate_query_builder()` + `playdeveloperreporting_vitals_crashrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_crashrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_crashrate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_crashrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_counts_get_execute()` to send, or `playdeveloperreporting_vitals_errors_counts_get` for simplest API.

pub fn playdeveloperreporting_vitals_errors_counts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorCountMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_errors_counts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet
/// Describes the properties of the metrics set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_get_builder()` + `playdeveloperreporting_vitals_errors_counts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_counts_get_builder(client, name)?;
    playdeveloperreporting_vitals_errors_counts_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_counts_query_execute()` to send, or `playdeveloperreporting_vitals_errors_counts_query` for simplest API.

pub fn playdeveloperreporting_vitals_errors_counts_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorCountMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_errors_counts_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/errorCountMetricSet:query
/// Queries the metrics in the metrics set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_counts_query_builder()` + `playdeveloperreporting_vitals_errors_counts_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_counts_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_counts_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_errors_counts_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_issues_search_execute()` to send, or `playdeveloperreporting_vitals_errors_issues_search` for simplest API.

pub fn playdeveloperreporting_vitals_errors_issues_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    interval_endTime_day: Option<i32>,
    interval_endTime_hours: Option<i32>,
    interval_endTime_minutes: Option<i32>,
    interval_endTime_month: Option<i32>,
    interval_endTime_nanos: Option<i32>,
    interval_endTime_seconds: Option<i32>,
    interval_endTime_timeZone_id: Option<&str>,
    interval_endTime_timeZone_version: Option<&str>,
    interval_endTime_utcOffset: Option<&str>,
    interval_endTime_year: Option<i32>,
    interval_startTime_day: Option<i32>,
    interval_startTime_hours: Option<i32>,
    interval_startTime_minutes: Option<i32>,
    interval_startTime_month: Option<i32>,
    interval_startTime_nanos: Option<i32>,
    interval_startTime_seconds: Option<i32>,
    interval_startTime_timeZone_id: Option<&str>,
    interval_startTime_timeZone_version: Option<&str>,
    interval_startTime_utcOffset: Option<&str>,
    interval_startTime_year: Option<i32>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    sampleErrorReportLimit: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorIssues:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime_day {
        query_parts.push(format!("interval_endTime_day={}", val));
    }
    if let Some(val) = interval_endTime_hours {
        query_parts.push(format!("interval_endTime_hours={}", val));
    }
    if let Some(val) = interval_endTime_minutes {
        query_parts.push(format!("interval_endTime_minutes={}", val));
    }
    if let Some(val) = interval_endTime_month {
        query_parts.push(format!("interval_endTime_month={}", val));
    }
    if let Some(val) = interval_endTime_nanos {
        query_parts.push(format!("interval_endTime_nanos={}", val));
    }
    if let Some(val) = interval_endTime_seconds {
        query_parts.push(format!("interval_endTime_seconds={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_id {
        query_parts.push(format!("interval_endTime_timeZone_id={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_version {
        query_parts.push(format!("interval_endTime_timeZone_version={}", val));
    }
    if let Some(val) = interval_endTime_utcOffset {
        query_parts.push(format!("interval_endTime_utcOffset={}", val));
    }
    if let Some(val) = interval_endTime_year {
        query_parts.push(format!("interval_endTime_year={}", val));
    }
    if let Some(val) = interval_startTime_day {
        query_parts.push(format!("interval_startTime_day={}", val));
    }
    if let Some(val) = interval_startTime_hours {
        query_parts.push(format!("interval_startTime_hours={}", val));
    }
    if let Some(val) = interval_startTime_minutes {
        query_parts.push(format!("interval_startTime_minutes={}", val));
    }
    if let Some(val) = interval_startTime_month {
        query_parts.push(format!("interval_startTime_month={}", val));
    }
    if let Some(val) = interval_startTime_nanos {
        query_parts.push(format!("interval_startTime_nanos={}", val));
    }
    if let Some(val) = interval_startTime_seconds {
        query_parts.push(format!("interval_startTime_seconds={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_id {
        query_parts.push(format!("interval_startTime_timeZone_id={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_version {
        query_parts.push(format!("interval_startTime_timeZone_version={}", val));
    }
    if let Some(val) = interval_startTime_utcOffset {
        query_parts.push(format!("interval_startTime_utcOffset={}", val));
    }
    if let Some(val) = interval_startTime_year {
        query_parts.push(format!("interval_startTime_year={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sampleErrorReportLimit {
        query_parts.push(format!("sampleErrorReportLimit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_errors_issues_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_errors_issues_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_issues_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/errorIssues:search
/// Searches all error issues in which reports have been grouped.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_issues_search_builder()` + `playdeveloperreporting_vitals_errors_issues_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_issues_search(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    interval_endTime_day: Option<i32>,
    interval_endTime_hours: Option<i32>,
    interval_endTime_minutes: Option<i32>,
    interval_endTime_month: Option<i32>,
    interval_endTime_nanos: Option<i32>,
    interval_endTime_seconds: Option<i32>,
    interval_endTime_timeZone_id: Option<&str>,
    interval_endTime_timeZone_version: Option<&str>,
    interval_endTime_utcOffset: Option<&str>,
    interval_endTime_year: Option<i32>,
    interval_startTime_day: Option<i32>,
    interval_startTime_hours: Option<i32>,
    interval_startTime_minutes: Option<i32>,
    interval_startTime_month: Option<i32>,
    interval_startTime_nanos: Option<i32>,
    interval_startTime_seconds: Option<i32>,
    interval_startTime_timeZone_id: Option<&str>,
    interval_startTime_timeZone_version: Option<&str>,
    interval_startTime_utcOffset: Option<&str>,
    interval_startTime_year: Option<i32>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    sampleErrorReportLimit: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_issues_search_builder(
        client,
        parent,
        filter,
        interval_endTime_day,
        interval_endTime_hours,
        interval_endTime_minutes,
        interval_endTime_month,
        interval_endTime_nanos,
        interval_endTime_seconds,
        interval_endTime_timeZone_id,
        interval_endTime_timeZone_version,
        interval_endTime_utcOffset,
        interval_endTime_year,
        interval_startTime_day,
        interval_startTime_hours,
        interval_startTime_minutes,
        interval_startTime_month,
        interval_startTime_nanos,
        interval_startTime_seconds,
        interval_startTime_timeZone_id,
        interval_startTime_timeZone_version,
        interval_startTime_utcOffset,
        interval_startTime_year,
        orderBy,
        pageSize,
        pageToken,
        sampleErrorReportLimit,
    )?;
    playdeveloperreporting_vitals_errors_issues_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_errors_reports_search_execute()` to send, or `playdeveloperreporting_vitals_errors_reports_search` for simplest API.

pub fn playdeveloperreporting_vitals_errors_reports_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    interval_endTime_day: Option<i32>,
    interval_endTime_hours: Option<i32>,
    interval_endTime_minutes: Option<i32>,
    interval_endTime_month: Option<i32>,
    interval_endTime_nanos: Option<i32>,
    interval_endTime_seconds: Option<i32>,
    interval_endTime_timeZone_id: Option<&str>,
    interval_endTime_timeZone_version: Option<&str>,
    interval_endTime_utcOffset: Option<&str>,
    interval_endTime_year: Option<i32>,
    interval_startTime_day: Option<i32>,
    interval_startTime_hours: Option<i32>,
    interval_startTime_minutes: Option<i32>,
    interval_startTime_month: Option<i32>,
    interval_startTime_nanos: Option<i32>,
    interval_startTime_seconds: Option<i32>,
    interval_startTime_timeZone_id: Option<&str>,
    interval_startTime_timeZone_version: Option<&str>,
    interval_startTime_utcOffset: Option<&str>,
    interval_startTime_year: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/errorReports:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime_day {
        query_parts.push(format!("interval_endTime_day={}", val));
    }
    if let Some(val) = interval_endTime_hours {
        query_parts.push(format!("interval_endTime_hours={}", val));
    }
    if let Some(val) = interval_endTime_minutes {
        query_parts.push(format!("interval_endTime_minutes={}", val));
    }
    if let Some(val) = interval_endTime_month {
        query_parts.push(format!("interval_endTime_month={}", val));
    }
    if let Some(val) = interval_endTime_nanos {
        query_parts.push(format!("interval_endTime_nanos={}", val));
    }
    if let Some(val) = interval_endTime_seconds {
        query_parts.push(format!("interval_endTime_seconds={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_id {
        query_parts.push(format!("interval_endTime_timeZone_id={}", val));
    }
    if let Some(val) = interval_endTime_timeZone_version {
        query_parts.push(format!("interval_endTime_timeZone_version={}", val));
    }
    if let Some(val) = interval_endTime_utcOffset {
        query_parts.push(format!("interval_endTime_utcOffset={}", val));
    }
    if let Some(val) = interval_endTime_year {
        query_parts.push(format!("interval_endTime_year={}", val));
    }
    if let Some(val) = interval_startTime_day {
        query_parts.push(format!("interval_startTime_day={}", val));
    }
    if let Some(val) = interval_startTime_hours {
        query_parts.push(format!("interval_startTime_hours={}", val));
    }
    if let Some(val) = interval_startTime_minutes {
        query_parts.push(format!("interval_startTime_minutes={}", val));
    }
    if let Some(val) = interval_startTime_month {
        query_parts.push(format!("interval_startTime_month={}", val));
    }
    if let Some(val) = interval_startTime_nanos {
        query_parts.push(format!("interval_startTime_nanos={}", val));
    }
    if let Some(val) = interval_startTime_seconds {
        query_parts.push(format!("interval_startTime_seconds={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_id {
        query_parts.push(format!("interval_startTime_timeZone_id={}", val));
    }
    if let Some(val) = interval_startTime_timeZone_version {
        query_parts.push(format!("interval_startTime_timeZone_version={}", val));
    }
    if let Some(val) = interval_startTime_utcOffset {
        query_parts.push(format!("interval_startTime_utcOffset={}", val));
    }
    if let Some(val) = interval_startTime_year {
        query_parts.push(format!("interval_startTime_year={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_errors_reports_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_errors_reports_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_reports_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/errorReports:search
/// Searches all error reports received for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_errors_reports_search_builder()` + `playdeveloperreporting_vitals_errors_reports_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_errors_reports_search(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    interval_endTime_day: Option<i32>,
    interval_endTime_hours: Option<i32>,
    interval_endTime_minutes: Option<i32>,
    interval_endTime_month: Option<i32>,
    interval_endTime_nanos: Option<i32>,
    interval_endTime_seconds: Option<i32>,
    interval_endTime_timeZone_id: Option<&str>,
    interval_endTime_timeZone_version: Option<&str>,
    interval_endTime_utcOffset: Option<&str>,
    interval_endTime_year: Option<i32>,
    interval_startTime_day: Option<i32>,
    interval_startTime_hours: Option<i32>,
    interval_startTime_minutes: Option<i32>,
    interval_startTime_month: Option<i32>,
    interval_startTime_nanos: Option<i32>,
    interval_startTime_seconds: Option<i32>,
    interval_startTime_timeZone_id: Option<&str>,
    interval_startTime_timeZone_version: Option<&str>,
    interval_startTime_utcOffset: Option<&str>,
    interval_startTime_year: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_errors_reports_search_builder(
        client,
        parent,
        filter,
        interval_endTime_day,
        interval_endTime_hours,
        interval_endTime_minutes,
        interval_endTime_month,
        interval_endTime_nanos,
        interval_endTime_seconds,
        interval_endTime_timeZone_id,
        interval_endTime_timeZone_version,
        interval_endTime_utcOffset,
        interval_endTime_year,
        interval_startTime_day,
        interval_startTime_hours,
        interval_startTime_minutes,
        interval_startTime_month,
        interval_startTime_nanos,
        interval_startTime_seconds,
        interval_startTime_timeZone_id,
        interval_startTime_timeZone_version,
        interval_startTime_utcOffset,
        interval_startTime_year,
        pageSize,
        pageToken,
    )?;
    playdeveloperreporting_vitals_errors_reports_search_execute(builder)
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_excessivewakeuprate_get_execute()` to send, or `playdeveloperreporting_vitals_excessivewakeuprate_get` for simplest API.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/excessiveWakeupRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_excessivewakeuprate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_get_builder()` + `playdeveloperreporting_vitals_excessivewakeuprate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_excessivewakeuprate_get_builder(client, name)?;
    playdeveloperreporting_vitals_excessivewakeuprate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_excessivewakeuprate_query_execute()` to send, or `playdeveloperreporting_vitals_excessivewakeuprate_query` for simplest API.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/excessiveWakeupRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_excessivewakeuprate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/excessiveWakeupRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_excessivewakeuprate_query_builder()` + `playdeveloperreporting_vitals_excessivewakeuprate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_excessivewakeuprate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_excessivewakeuprate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_excessivewakeuprate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_lmkrate_get_execute()` to send, or `playdeveloperreporting_vitals_lmkrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_lmkrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/lmkRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_lmkrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1LmkRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_get_builder()` + `playdeveloperreporting_vitals_lmkrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_lmkrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_lmkrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_lmkrate_query_execute()` to send, or `playdeveloperreporting_vitals_lmkrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_lmkrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/lmkRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_lmkrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/lmkRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_lmkrate_query_builder()` + `playdeveloperreporting_vitals_lmkrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_lmkrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_lmkrate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_lmkrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowrenderingrate_get_execute()` to send, or `playdeveloperreporting_vitals_slowrenderingrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowRenderingRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_slowrenderingrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_get_builder()` + `playdeveloperreporting_vitals_slowrenderingrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowrenderingrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_slowrenderingrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowrenderingrate_query_execute()` to send, or `playdeveloperreporting_vitals_slowrenderingrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowRenderingRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_slowrenderingrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowRenderingRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowrenderingrate_query_builder()` + `playdeveloperreporting_vitals_slowrenderingrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowrenderingrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_slowrenderingrate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_slowrenderingrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowstartrate_get_execute()` to send, or `playdeveloperreporting_vitals_slowstartrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_slowstartrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowStartRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_slowstartrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_get_builder()` + `playdeveloperreporting_vitals_slowstartrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowstartrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_slowstartrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_slowstartrate_query_execute()` to send, or `playdeveloperreporting_vitals_slowstartrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_slowstartrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/slowStartRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_slowstartrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/slowStartRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_slowstartrate_query_builder()` + `playdeveloperreporting_vitals_slowstartrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_slowstartrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playdeveloperreporting_vitals_slowstartrate_query_builder(client, name, body)?;
    playdeveloperreporting_vitals_slowstartrate_query_execute(builder)
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute()` to send, or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get` for simplest API.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/stuckBackgroundWakelockRateMetricSet",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet
/// Describes the properties of the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder()` + `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_builder(client, name)?;
    playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_execute(builder)
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute()` to send, or `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query` for simplest API.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/apps/{}/stuckBackgroundWakelockRateMetricSet:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
>{
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/apps/{appsId}/stuckBackgroundWakelockRateMetricSet:query
/// Queries the metrics in the metric set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder()` + `playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query(
    client: &SimpleHttpClient,
    name: &str,
    body: &GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetRequest,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
>{
    let builder = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_builder(
        client, name, body,
    )?;
    playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_execute(builder)
}
