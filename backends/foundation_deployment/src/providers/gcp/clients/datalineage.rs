//! Auto-generated API clients for gcp/datalineage.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/datalineage`

#![cfg(feature = "gcp/datalineage")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_folders_locations_config_get_execute()` to send, or `datalineage_folders_locations_config_get` for simplest API.

pub fn datalineage_folders_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/folders/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_folders_locations_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_folders_locations_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_folders_locations_config_get_builder()` + `datalineage_folders_locations_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_folders_locations_config_get_builder(client, name)?;
    datalineage_folders_locations_config_get_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_folders_locations_config_patch_execute()` to send, or `datalineage_folders_locations_config_patch` for simplest API.

pub fn datalineage_folders_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/folders/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_folders_locations_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_folders_locations_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_folders_locations_config_patch_builder()` + `datalineage_folders_locations_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_folders_locations_config_patch_builder(client, name, body)?;
    datalineage_folders_locations_config_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_organizations_locations_config_get_execute()` to send, or `datalineage_organizations_locations_config_get` for simplest API.

pub fn datalineage_organizations_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/organizations/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_organizations_locations_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_organizations_locations_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_organizations_locations_config_get_builder()` + `datalineage_organizations_locations_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_organizations_locations_config_get_builder(client, name)?;
    datalineage_organizations_locations_config_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_organizations_locations_config_patch_execute()` to send, or `datalineage_organizations_locations_config_patch` for simplest API.

pub fn datalineage_organizations_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/organizations/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_organizations_locations_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_organizations_locations_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_organizations_locations_config_patch_builder()` + `datalineage_organizations_locations_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_organizations_locations_config_patch_builder(client, name, body)?;
    datalineage_organizations_locations_config_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_batch_search_link_processes_execute()` to send, or `datalineage_projects_locations_batch_search_link_processes` for simplest API.

pub fn datalineage_projects_locations_batch_search_link_processes_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}:batchSearchLinkProcesses",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_batch_search_link_processes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_batch_search_link_processes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_batch_search_link_processes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_batch_search_link_processes_builder()` + `datalineage_projects_locations_batch_search_link_processes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_batch_search_link_processes(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_batch_search_link_processes_builder(client, parent, body)?;
    datalineage_projects_locations_batch_search_link_processes_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_process_open_lineage_run_event_execute()` to send, or `datalineage_projects_locations_process_open_lineage_run_event` for simplest API.

pub fn datalineage_projects_locations_process_open_lineage_run_event_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}:processOpenLineageRunEvent",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_process_open_lineage_run_event_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_process_open_lineage_run_event_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_process_open_lineage_run_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_process_open_lineage_run_event_builder()` + `datalineage_projects_locations_process_open_lineage_run_event_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_process_open_lineage_run_event(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_process_open_lineage_run_event_builder(
        client, parent, requestId,
    )?;
    datalineage_projects_locations_process_open_lineage_run_event_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_search_links_execute()` to send, or `datalineage_projects_locations_search_links` for simplest API.

pub fn datalineage_projects_locations_search_links_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatacatalogLineageV1SearchLinksRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}:searchLinks",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_search_links_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_search_links_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_search_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1SearchLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1SearchLinksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_search_links_builder()` + `datalineage_projects_locations_search_links_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_search_links(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatacatalogLineageV1SearchLinksRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1SearchLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_search_links_builder(client, parent, body)?;
    datalineage_projects_locations_search_links_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_config_get_execute()` to send, or `datalineage_projects_locations_config_get` for simplest API.

pub fn datalineage_projects_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_config_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_config_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_config_get_builder()` + `datalineage_projects_locations_config_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_config_get_builder(client, name)?;
    datalineage_projects_locations_config_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_config_patch_execute()` to send, or `datalineage_projects_locations_config_patch` for simplest API.

pub fn datalineage_projects_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_config_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_config_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_config_patch_builder()` + `datalineage_projects_locations_config_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatacatalogLineageConfigmanagementV1Config,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_config_patch_builder(client, name, body)?;
    datalineage_projects_locations_config_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_cancel_execute()` to send, or `datalineage_projects_locations_operations_cancel` for simplest API.

pub fn datalineage_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_cancel_builder()` + `datalineage_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_cancel_builder(client, name, body)?;
    datalineage_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_delete_execute()` to send, or `datalineage_projects_locations_operations_delete` for simplest API.

pub fn datalineage_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_delete_builder()` + `datalineage_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_delete_builder(client, name)?;
    datalineage_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_get_execute()` to send, or `datalineage_projects_locations_operations_get` for simplest API.

pub fn datalineage_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_get_builder()` + `datalineage_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_get_builder(client, name)?;
    datalineage_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_list_execute()` to send, or `datalineage_projects_locations_operations_list` for simplest API.

pub fn datalineage_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_list_builder()` + `datalineage_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    datalineage_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_create_execute()` to send, or `datalineage_projects_locations_processes_create` for simplest API.

pub fn datalineage_projects_locations_processes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Process,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_create_builder()` + `datalineage_projects_locations_processes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Process,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_processes_create_builder(client, parent, requestId, body)?;
    datalineage_projects_locations_processes_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_delete_execute()` to send, or `datalineage_projects_locations_processes_delete` for simplest API.

pub fn datalineage_projects_locations_processes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_delete_builder()` + `datalineage_projects_locations_processes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_processes_delete_builder(client, name, allowMissing)?;
    datalineage_projects_locations_processes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_get_execute()` to send, or `datalineage_projects_locations_processes_get` for simplest API.

pub fn datalineage_projects_locations_processes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_get_builder()` + `datalineage_projects_locations_processes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_get_builder(client, name)?;
    datalineage_projects_locations_processes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_list_execute()` to send, or `datalineage_projects_locations_processes_list` for simplest API.

pub fn datalineage_projects_locations_processes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListProcessesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_list_builder()` + `datalineage_projects_locations_processes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_processes_list_builder(client, parent, pageSize, pageToken)?;
    datalineage_projects_locations_processes_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_patch_execute()` to send, or `datalineage_projects_locations_processes_patch` for simplest API.

pub fn datalineage_projects_locations_processes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Process,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_patch_builder()` + `datalineage_projects_locations_processes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Process,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_patch_builder(
        client,
        name,
        allowMissing,
        requestId,
        updateMask,
        body,
    )?;
    datalineage_projects_locations_processes_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_create_execute()` to send, or `datalineage_projects_locations_processes_runs_create` for simplest API.

pub fn datalineage_projects_locations_processes_runs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Run,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_create_builder()` + `datalineage_projects_locations_processes_runs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Run,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_create_builder(
        client, parent, requestId, body,
    )?;
    datalineage_projects_locations_processes_runs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_delete_execute()` to send, or `datalineage_projects_locations_processes_runs_delete` for simplest API.

pub fn datalineage_projects_locations_processes_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_delete_builder()` + `datalineage_projects_locations_processes_runs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_processes_runs_delete_builder(client, name, allowMissing)?;
    datalineage_projects_locations_processes_runs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_get_execute()` to send, or `datalineage_projects_locations_processes_runs_get` for simplest API.

pub fn datalineage_projects_locations_processes_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_get_builder()` + `datalineage_projects_locations_processes_runs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_get_builder(client, name)?;
    datalineage_projects_locations_processes_runs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_list_execute()` to send, or `datalineage_projects_locations_processes_runs_list` for simplest API.

pub fn datalineage_projects_locations_processes_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListRunsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_list_builder()` + `datalineage_projects_locations_processes_runs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    datalineage_projects_locations_processes_runs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_patch_execute()` to send, or `datalineage_projects_locations_processes_runs_patch` for simplest API.

pub fn datalineage_projects_locations_processes_runs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Run,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_patch_builder()` + `datalineage_projects_locations_processes_runs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1Run,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_patch_builder(
        client,
        name,
        allowMissing,
        updateMask,
        body,
    )?;
    datalineage_projects_locations_processes_runs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_create_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_create` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1LineageEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}/lineageEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_lineage_events_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1LineageEvent =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_create_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &GoogleCloudDatacatalogLineageV1LineageEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_create_builder(
        client, parent, requestId, body,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_delete_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_delete` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}/lineageEvents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_lineage_events_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_delete_builder(
        client,
        name,
        allowMissing,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_get_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_get` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}/lineageEvents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_lineage_events_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1LineageEvent =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_get_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_processes_runs_lineage_events_get_builder(client, name)?;
    datalineage_projects_locations_processes_runs_lineage_events_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_list_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_list` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{}/processes/{}/runs/{}/lineageEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datalineage_projects_locations_processes_runs_lineage_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListLineageEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListLineageEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_list_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListLineageEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_list_execute(builder)
}
