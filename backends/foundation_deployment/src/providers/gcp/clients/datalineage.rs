//! Auto-generated API clients for gcp/datalineage.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_folders_locations_config_get_execute()` to send, or `datalineage_folders_locations_config_get` for simplest API.

pub fn datalineage_folders_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/folders/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_folders_locations_config_get_execute()` or `datalineage_folders_locations_config_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_folders_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_folders_locations_config_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_folders_locations_config_get_task()`.
/// For the simplest API, use `datalineage_folders_locations_config_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_folders_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_folders_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_folders_locations_config_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_folders_locations_config_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageFoldersLocationsConfigGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_folders_locations_config_get_builder()` + `datalineage_folders_locations_config_get_execute()`.
/// For task-level control, use `datalineage_folders_locations_config_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_get(
    client: &SimpleHttpClient,
    args: &DatalineageFoldersLocationsConfigGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_folders_locations_config_get_builder(client, &args.name)?;
    datalineage_folders_locations_config_get_execute(builder)
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_folders_locations_config_patch_execute()` to send, or `datalineage_folders_locations_config_patch` for simplest API.

pub fn datalineage_folders_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/folders/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_folders_locations_config_patch_execute()` or `datalineage_folders_locations_config_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_folders_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_folders_locations_config_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_folders_locations_config_patch_task()`.
/// For the simplest API, use `datalineage_folders_locations_config_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_folders_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_folders_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_folders_locations_config_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_folders_locations_config_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageFoldersLocationsConfigPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/folders/{foldersId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_folders_locations_config_patch_builder()` + `datalineage_folders_locations_config_patch_execute()`.
/// For task-level control, use `datalineage_folders_locations_config_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_folders_locations_config_patch(
    client: &SimpleHttpClient,
    args: &DatalineageFoldersLocationsConfigPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_folders_locations_config_patch_builder(client, &args.name)?;
    datalineage_folders_locations_config_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_organizations_locations_config_get_execute()` to send, or `datalineage_organizations_locations_config_get` for simplest API.

pub fn datalineage_organizations_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/organizations/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_organizations_locations_config_get_execute()` or `datalineage_organizations_locations_config_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_organizations_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_organizations_locations_config_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_organizations_locations_config_get_task()`.
/// For the simplest API, use `datalineage_organizations_locations_config_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_organizations_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_organizations_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_organizations_locations_config_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_organizations_locations_config_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageOrganizationsLocationsConfigGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_organizations_locations_config_get_builder()` + `datalineage_organizations_locations_config_get_execute()`.
/// For task-level control, use `datalineage_organizations_locations_config_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_get(
    client: &SimpleHttpClient,
    args: &DatalineageOrganizationsLocationsConfigGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_organizations_locations_config_get_builder(client, &args.name)?;
    datalineage_organizations_locations_config_get_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_organizations_locations_config_patch_execute()` to send, or `datalineage_organizations_locations_config_patch` for simplest API.

pub fn datalineage_organizations_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/organizations/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_organizations_locations_config_patch_execute()` or `datalineage_organizations_locations_config_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_organizations_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_organizations_locations_config_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_organizations_locations_config_patch_task()`.
/// For the simplest API, use `datalineage_organizations_locations_config_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_organizations_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_organizations_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_organizations_locations_config_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_organizations_locations_config_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageOrganizationsLocationsConfigPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_organizations_locations_config_patch_builder()` + `datalineage_organizations_locations_config_patch_execute()`.
/// For task-level control, use `datalineage_organizations_locations_config_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_organizations_locations_config_patch(
    client: &SimpleHttpClient,
    args: &DatalineageOrganizationsLocationsConfigPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_organizations_locations_config_patch_builder(client, &args.name)?;
    datalineage_organizations_locations_config_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_batch_search_link_processes_execute()` to send, or `datalineage_projects_locations_batch_search_link_processes` for simplest API.

pub fn datalineage_projects_locations_batch_search_link_processes_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}:batchSearchLinkProcesses",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_batch_search_link_processes_execute()` or `datalineage_projects_locations_batch_search_link_processes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_batch_search_link_processes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_batch_search_link_processes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_batch_search_link_processes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_batch_search_link_processes_task()`.
/// For the simplest API, use `datalineage_projects_locations_batch_search_link_processes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_batch_search_link_processes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_batch_search_link_processes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_batch_search_link_processes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_batch_search_link_processes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsBatchSearchLinkProcessesArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:batchSearchLinkProcesses
/// Retrieve information about LineageProcesses associated with specific links. LineageProcesses are transformation pipelines that result in data flowing from **source** to **target** assets. Links between assets represent this operation. If you have specific link names, you can use this method to verify which LineageProcesses contribute to creating those links. See the SearchLinks method for more information on how to retrieve link name. You can retrieve the LineageProcess information in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_batch_search_link_processes_builder()` + `datalineage_projects_locations_batch_search_link_processes_execute()`.
/// For task-level control, use `datalineage_projects_locations_batch_search_link_processes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_batch_search_link_processes(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsBatchSearchLinkProcessesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalineage_projects_locations_batch_search_link_processes_builder(client, &args.parent)?;
    datalineage_projects_locations_batch_search_link_processes_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_process_open_lineage_run_event_execute()` to send, or `datalineage_projects_locations_process_open_lineage_run_event` for simplest API.

pub fn datalineage_projects_locations_process_open_lineage_run_event_builder(
    client: &SimpleHttpClient,
    parent: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}:processOpenLineageRunEvent",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_process_open_lineage_run_event_execute()` or `datalineage_projects_locations_process_open_lineage_run_event`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_process_open_lineage_run_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_process_open_lineage_run_event_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_process_open_lineage_run_event_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_process_open_lineage_run_event_task()`.
/// For the simplest API, use `datalineage_projects_locations_process_open_lineage_run_event()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_process_open_lineage_run_event_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_process_open_lineage_run_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_process_open_lineage_run_event_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_process_open_lineage_run_event`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessOpenLineageRunEventArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:processOpenLineageRunEvent
/// Creates new lineage events together with their parents: process and run. Updates the process and run if they already exist. Mapped from Open Lineage specification: <https://github.`com/OpenLineage/OpenLineage/blob/main/spec/OpenLineage`.json.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_process_open_lineage_run_event_builder()` + `datalineage_projects_locations_process_open_lineage_run_event_execute()`.
/// For task-level control, use `datalineage_projects_locations_process_open_lineage_run_event_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_process_open_lineage_run_event(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessOpenLineageRunEventArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_process_open_lineage_run_event_builder(
        client,
        &args.parent,
        &args.requestId,
    )?;
    datalineage_projects_locations_process_open_lineage_run_event_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_search_links_execute()` to send, or `datalineage_projects_locations_search_links` for simplest API.

pub fn datalineage_projects_locations_search_links_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}:searchLinks",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_search_links_execute()` or `datalineage_projects_locations_search_links`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_search_links_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_search_links_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1SearchLinksResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1SearchLinksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_search_links_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_search_links_task()`.
/// For the simplest API, use `datalineage_projects_locations_search_links()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_search_links_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_search_links_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1SearchLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_search_links_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_search_links`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsSearchLinksArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:searchLinks
/// Retrieve a list of links connected to a specific asset. Links represent the data flow between **source** (upstream) and **target** (downstream) assets in transformation pipelines. Links are stored in the same project as the Lineage Events that create them. You can retrieve links in every project where you have the datalineage.events.get permission. The project provided in the URL is used for Billing and Quota.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_search_links_builder()` + `datalineage_projects_locations_search_links_execute()`.
/// For task-level control, use `datalineage_projects_locations_search_links_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_search_links(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsSearchLinksArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1SearchLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_search_links_builder(client, &args.parent)?;
    datalineage_projects_locations_search_links_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_config_get_execute()` to send, or `datalineage_projects_locations_config_get` for simplest API.

pub fn datalineage_projects_locations_config_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_config_get_execute()` or `datalineage_projects_locations_config_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_config_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_config_get_task()`.
/// For the simplest API, use `datalineage_projects_locations_config_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_config_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_config_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_config_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_config_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsConfigGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/config
/// Get the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_config_get_builder()` + `datalineage_projects_locations_config_get_execute()`.
/// For task-level control, use `datalineage_projects_locations_config_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_get(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsConfigGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_config_get_builder(client, &args.name)?;
    datalineage_projects_locations_config_get_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_config_patch_execute()` to send, or `datalineage_projects_locations_config_patch` for simplest API.

pub fn datalineage_projects_locations_config_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/config",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_config_patch_execute()` or `datalineage_projects_locations_config_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageConfigmanagementV1Config =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_config_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_config_patch_task()`.
/// For the simplest API, use `datalineage_projects_locations_config_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_config_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_config_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_config_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_config_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsConfigPatchArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/config
/// Update the Config for a given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_config_patch_builder()` + `datalineage_projects_locations_config_patch_execute()`.
/// For task-level control, use `datalineage_projects_locations_config_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_config_patch(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsConfigPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageConfigmanagementV1Config>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_config_patch_builder(client, &args.name)?;
    datalineage_projects_locations_config_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_cancel_execute()` to send, or `datalineage_projects_locations_operations_cancel` for simplest API.

pub fn datalineage_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_operations_cancel_execute()` or `datalineage_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `datalineage_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_cancel_builder()` + `datalineage_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `datalineage_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_cancel_builder(client, &args.name)?;
    datalineage_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_delete_execute()` to send, or `datalineage_projects_locations_operations_delete` for simplest API.

pub fn datalineage_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_operations_delete_execute()` or `datalineage_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_operations_delete_task()`.
/// For the simplest API, use `datalineage_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_delete_builder()` + `datalineage_projects_locations_operations_delete_execute()`.
/// For task-level control, use `datalineage_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_delete_builder(client, &args.name)?;
    datalineage_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_get_execute()` to send, or `datalineage_projects_locations_operations_get` for simplest API.

pub fn datalineage_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_operations_get_execute()` or `datalineage_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_operations_get_task()`.
/// For the simplest API, use `datalineage_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_get_builder()` + `datalineage_projects_locations_operations_get_execute()`.
/// For task-level control, use `datalineage_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_get_builder(client, &args.name)?;
    datalineage_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_operations_list_execute()` to send, or `datalineage_projects_locations_operations_list` for simplest API.

pub fn datalineage_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_operations_list_execute()` or `datalineage_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_operations_list_task()`.
/// For the simplest API, use `datalineage_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_operations_list_builder()` + `datalineage_projects_locations_operations_list_execute()`.
/// For task-level control, use `datalineage_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    datalineage_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_create_execute()` to send, or `datalineage_projects_locations_processes_create` for simplest API.

pub fn datalineage_projects_locations_processes_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_create_execute()` or `datalineage_projects_locations_processes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_create_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes
/// Creates a new process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_create_builder()` + `datalineage_projects_locations_processes_create_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_create(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_create_builder(
        client,
        &args.parent,
        &args.requestId,
    )?;
    datalineage_projects_locations_processes_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_delete_execute()` to send, or `datalineage_projects_locations_processes_delete` for simplest API.

pub fn datalineage_projects_locations_processes_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_delete_execute()` or `datalineage_projects_locations_processes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_delete_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Deletes the process with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_delete_builder()` + `datalineage_projects_locations_processes_delete_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_delete(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
    )?;
    datalineage_projects_locations_processes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_get_execute()` to send, or `datalineage_projects_locations_processes_get` for simplest API.

pub fn datalineage_projects_locations_processes_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_get_execute()` or `datalineage_projects_locations_processes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_get_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Gets the details of the specified process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_get_builder()` + `datalineage_projects_locations_processes_get_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_get(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_get_builder(client, &args.name)?;
    datalineage_projects_locations_processes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_list_execute()` to send, or `datalineage_projects_locations_processes_list` for simplest API.

pub fn datalineage_projects_locations_processes_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_list_execute()` or `datalineage_projects_locations_processes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListProcessesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListProcessesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_list_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes
/// List processes in the given project and location. List order is descending by insertion time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_list_builder()` + `datalineage_projects_locations_processes_list_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_list(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListProcessesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    datalineage_projects_locations_processes_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_patch_execute()` to send, or `datalineage_projects_locations_processes_patch` for simplest API.

pub fn datalineage_projects_locations_processes_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_patch_execute()` or `datalineage_projects_locations_processes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Process = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_patch_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}
/// Updates a process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_patch_builder()` + `datalineage_projects_locations_processes_patch_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_patch(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Process>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.requestId,
        &args.updateMask,
    )?;
    datalineage_projects_locations_processes_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_create_execute()` to send, or `datalineage_projects_locations_processes_runs_create` for simplest API.

pub fn datalineage_projects_locations_processes_runs_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_create_execute()` or `datalineage_projects_locations_processes_runs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_create_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Creates a new run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_create_builder()` + `datalineage_projects_locations_processes_runs_create_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_create(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_create_builder(
        client,
        &args.parent,
        &args.requestId,
    )?;
    datalineage_projects_locations_processes_runs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_delete_execute()` to send, or `datalineage_projects_locations_processes_runs_delete` for simplest API.

pub fn datalineage_projects_locations_processes_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_delete_execute()` or `datalineage_projects_locations_processes_runs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_delete_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Deletes the run with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_delete_builder()` + `datalineage_projects_locations_processes_runs_delete_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_delete(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
    )?;
    datalineage_projects_locations_processes_runs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_get_execute()` to send, or `datalineage_projects_locations_processes_runs_get` for simplest API.

pub fn datalineage_projects_locations_processes_runs_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_get_execute()` or `datalineage_projects_locations_processes_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_get_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Gets the details of the specified run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_get_builder()` + `datalineage_projects_locations_processes_runs_get_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_get(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_get_builder(client, &args.name)?;
    datalineage_projects_locations_processes_runs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_list_execute()` to send, or `datalineage_projects_locations_processes_runs_list` for simplest API.

pub fn datalineage_projects_locations_processes_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_list_execute()` or `datalineage_projects_locations_processes_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListRunsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListRunsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_list_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs
/// Lists runs in the given project and location. List order is descending by start_time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_list_builder()` + `datalineage_projects_locations_processes_runs_list_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_list(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1ListRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    datalineage_projects_locations_processes_runs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_patch_execute()` to send, or `datalineage_projects_locations_processes_runs_patch` for simplest API.

pub fn datalineage_projects_locations_processes_runs_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_patch_execute()` or `datalineage_projects_locations_processes_runs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1Run = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_patch_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}
/// Updates a run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_patch_builder()` + `datalineage_projects_locations_processes_runs_patch_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_patch(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1Run>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    datalineage_projects_locations_processes_runs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_create_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_create` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_lineage_events_create_execute()` or `datalineage_projects_locations_processes_runs_lineage_events_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1LineageEvent =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_lineage_events_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_create_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_lineage_events_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_lineage_events_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_lineage_events_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsLineageEventsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Creates a new lineage event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_create_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_create_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_create(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsLineageEventsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_create_builder(
        client,
        &args.parent,
        &args.requestId,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_delete_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_delete` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_lineage_events_delete_execute()` or `datalineage_projects_locations_processes_runs_lineage_events_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_delete_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_lineage_events_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_lineage_events_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_lineage_events_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsLineageEventsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Deletes the lineage event with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_delete_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_delete_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_delete(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsLineageEventsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_delete_builder(
        client,
        &args.name,
        &args.allowMissing,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_get_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_get` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_lineage_events_get_execute()` or `datalineage_projects_locations_processes_runs_lineage_events_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1LineageEvent =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_lineage_events_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_get_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_lineage_events_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_lineage_events_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_lineage_events_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsLineageEventsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents/{lineageEventsId}
/// Gets details of a specified lineage event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_get_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_get_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_get(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsLineageEventsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogLineageV1LineageEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_get_builder(
        client, &args.name,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalineage_projects_locations_processes_runs_lineage_events_list_execute()` to send, or `datalineage_projects_locations_processes_runs_lineage_events_list` for simplest API.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datalineage.googleapis.com/v1/projects/{}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalineage_projects_locations_processes_runs_lineage_events_list_execute()` or `datalineage_projects_locations_processes_runs_lineage_events_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListLineageEventsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogLineageV1ListLineageEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalineage_projects_locations_processes_runs_lineage_events_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_list_task()`.
/// For the simplest API, use `datalineage_projects_locations_processes_runs_lineage_events_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalineage_projects_locations_processes_runs_lineage_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListLineageEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalineage_projects_locations_processes_runs_lineage_events_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalineage_projects_locations_processes_runs_lineage_events_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalineageProjectsLocationsProcessesRunsLineageEventsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processes/{processesId}/runs/{runsId}/lineageEvents
/// Lists lineage events in the given project and location. The list order is not defined.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalineage_projects_locations_processes_runs_lineage_events_list_builder()` + `datalineage_projects_locations_processes_runs_lineage_events_list_execute()`.
/// For task-level control, use `datalineage_projects_locations_processes_runs_lineage_events_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalineage_projects_locations_processes_runs_lineage_events_list(
    client: &SimpleHttpClient,
    args: &DatalineageProjectsLocationsProcessesRunsLineageEventsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatacatalogLineageV1ListLineageEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalineage_projects_locations_processes_runs_lineage_events_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    datalineage_projects_locations_processes_runs_lineage_events_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageFoldersLocationsConfigGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageFoldersLocationsConfigGetArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(&self, input: &DatalineageFoldersLocationsConfigGetArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageFoldersLocationsConfigPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageFoldersLocationsConfigPatchArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(&self, input: &DatalineageFoldersLocationsConfigPatchArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageOrganizationsLocationsConfigGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageOrganizationsLocationsConfigGetArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(
        &self,
        input: &DatalineageOrganizationsLocationsConfigGetArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageOrganizationsLocationsConfigPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageOrganizationsLocationsConfigPatchArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(
        &self,
        input: &DatalineageOrganizationsLocationsConfigPatchArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse with DatalineageProjectsLocationsBatchSearchLinkProcessesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsBatchSearchLinkProcessesArgs>
    for GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsBatchSearchLinkProcessesArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1BatchSearchLinkProcessesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse with DatalineageProjectsLocationsProcessOpenLineageRunEventArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessOpenLineageRunEventArgs>
    for GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessOpenLineageRunEventArgs,
    ) -> String {
        format!("gcp::datalineage::GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1ProcessOpenLineageRunEventResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1SearchLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1SearchLinksResponse with DatalineageProjectsLocationsSearchLinksArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsSearchLinksArgs>
    for GoogleCloudDatacatalogLineageV1SearchLinksResponse
{
    fn generate_resource_id(&self, input: &DatalineageProjectsLocationsSearchLinksArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1SearchLinksResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1SearchLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageProjectsLocationsConfigGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsConfigGetArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(&self, input: &DatalineageProjectsLocationsConfigGetArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageConfigmanagementV1Config with DatalineageProjectsLocationsConfigPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsConfigPatchArgs>
    for GoogleCloudDatacatalogLineageConfigmanagementV1Config
{
    fn generate_resource_id(&self, input: &DatalineageProjectsLocationsConfigPatchArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageConfigmanagementV1Config"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DatalineageProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsOperationsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::datalineage::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DatalineageProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsOperationsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::datalineage::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DatalineageProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with DatalineageProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process with DatalineageProjectsLocationsProcessesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesCreateArgs>
    for GoogleCloudDatacatalogLineageV1Process
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesCreateArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DatalineageProjectsLocationsProcessesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesDeleteArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process with DatalineageProjectsLocationsProcessesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesGetArgs>
    for GoogleCloudDatacatalogLineageV1Process
{
    fn generate_resource_id(&self, input: &DatalineageProjectsLocationsProcessesGetArgs) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListProcessesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListProcessesResponse with DatalineageProjectsLocationsProcessesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesListArgs>
    for GoogleCloudDatacatalogLineageV1ListProcessesResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesListArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListProcessesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListProcessesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Process with DatalineageProjectsLocationsProcessesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesPatchArgs>
    for GoogleCloudDatacatalogLineageV1Process
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesPatchArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Process"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run with DatalineageProjectsLocationsProcessesRunsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsCreateArgs>
    for GoogleCloudDatacatalogLineageV1Run
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsCreateArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DatalineageProjectsLocationsProcessesRunsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsDeleteArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run with DatalineageProjectsLocationsProcessesRunsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsGetArgs>
    for GoogleCloudDatacatalogLineageV1Run
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsGetArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListRunsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListRunsResponse with DatalineageProjectsLocationsProcessesRunsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsListArgs>
    for GoogleCloudDatacatalogLineageV1ListRunsResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsListArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListRunsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListRunsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1Run with DatalineageProjectsLocationsProcessesRunsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsPatchArgs>
    for GoogleCloudDatacatalogLineageV1Run
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsPatchArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1Run"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1LineageEvent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1LineageEvent with DatalineageProjectsLocationsProcessesRunsLineageEventsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsLineageEventsCreateArgs>
    for GoogleCloudDatacatalogLineageV1LineageEvent
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsLineageEventsCreateArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1LineageEvent/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1LineageEvent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DatalineageProjectsLocationsProcessesRunsLineageEventsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsLineageEventsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsLineageEventsDeleteArgs,
    ) -> String {
        format!("gcp::datalineage::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1LineageEvent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1LineageEvent with DatalineageProjectsLocationsProcessesRunsLineageEventsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsLineageEventsGetArgs>
    for GoogleCloudDatacatalogLineageV1LineageEvent
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsLineageEventsGetArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1LineageEvent/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1LineageEvent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListLineageEventsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatacatalogLineageV1ListLineageEventsResponse with DatalineageProjectsLocationsProcessesRunsLineageEventsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatalineageProjectsLocationsProcessesRunsLineageEventsListArgs>
    for GoogleCloudDatacatalogLineageV1ListLineageEventsResponse
{
    fn generate_resource_id(
        &self,
        input: &DatalineageProjectsLocationsProcessesRunsLineageEventsListArgs,
    ) -> String {
        format!(
            "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListLineageEventsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datalineage::GoogleCloudDatacatalogLineageV1ListLineageEventsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
