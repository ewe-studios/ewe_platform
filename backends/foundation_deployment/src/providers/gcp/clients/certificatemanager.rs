//! Auto-generated API clients for gcp/certificatemanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_get_execute()` to send, or `certificatemanager_projects_locations_get` for simplest API.

pub fn certificatemanager_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_get_execute()` or `certificatemanager_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_get_builder()` + `certificatemanager_projects_locations_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_list_execute()` to send, or `certificatemanager_projects_locations_list` for simplest API.

pub fn certificatemanager_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_list_execute()` or `certificatemanager_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_list_builder()` + `certificatemanager_projects_locations_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_create_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateIssuanceConfigId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateIssuanceConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateIssuanceConfigId.as_ref() {
        query_parts.push(format!("certificateIssuanceConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_issuance_configs_create_execute()` or `certificatemanager_projects_locations_certificate_issuance_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_issuance_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_issuance_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_issuance_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateIssuanceConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateIssuanceConfigId
    pub certificateIssuanceConfigId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_issuance_configs_create_builder(
            client,
            &args.parent,
            &args.certificateIssuanceConfigId,
        )?;
    certificatemanager_projects_locations_certificate_issuance_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Deletes a single CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Deletes a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_execute()` or `certificatemanager_projects_locations_certificate_issuance_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Deletes a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_issuance_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_issuance_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_issuance_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateIssuanceConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Deletes a single CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_issuance_configs_delete_builder(
            client, &args.name,
        )?;
    certificatemanager_projects_locations_certificate_issuance_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_get_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_issuance_configs_get_execute()` or `certificatemanager_projects_locations_certificate_issuance_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateIssuanceConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateIssuanceConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_issuance_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateIssuanceConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_issuance_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_issuance_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateIssuanceConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateIssuanceConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_get_builder(
        client, &args.name,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_list_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateIssuanceConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_issuance_configs_list_execute()` or `certificatemanager_projects_locations_certificate_issuance_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateIssuanceConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateIssuanceConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_issuance_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateIssuanceConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_issuance_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_issuance_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateIssuanceConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateIssuanceConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Updates a CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Updates a CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_execute()` or `certificatemanager_projects_locations_certificate_issuance_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Updates a CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_issuance_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_issuance_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_issuance_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateIssuanceConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateIssuanceConfigs/{certificateIssuanceConfigsId}
/// Updates a CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_create_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateMapId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateMapId.as_ref() {
        query_parts.push(format!("certificateMapId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_create_execute()` or `certificatemanager_projects_locations_certificate_maps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificate_maps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateMapId
    pub certificateMapId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_create_builder()` + `certificatemanager_projects_locations_certificate_maps_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_create_builder(
        client,
        &args.parent,
        &args.certificateMapId,
    )?;
    certificatemanager_projects_locations_certificate_maps_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_delete_execute()` or `certificatemanager_projects_locations_certificate_maps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificate_maps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_delete_builder()` + `certificatemanager_projects_locations_certificate_maps_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_maps_delete_builder(client, &args.name)?;
    certificatemanager_projects_locations_certificate_maps_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Gets details of a single CertificateMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_get_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Gets details of a single CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_get_execute()` or `certificatemanager_projects_locations_certificate_maps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateMap>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Gets details of a single CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificate_maps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Gets details of a single CertificateMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_get_builder()` + `certificatemanager_projects_locations_certificate_maps_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_maps_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_certificate_maps_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_list_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_list_execute()` or `certificatemanager_projects_locations_certificate_maps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateMapsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateMapsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificate_maps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_list_builder()` + `certificatemanager_projects_locations_certificate_maps_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_certificate_maps_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Updates a CertificateMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Updates a CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_patch_execute()` or `certificatemanager_projects_locations_certificate_maps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Updates a CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificate_maps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}
/// Updates a CertificateMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_patch_builder()` + `certificatemanager_projects_locations_certificate_maps_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    certificatemanager_projects_locations_certificate_maps_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateMapEntryId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateMapEntryId.as_ref() {
        query_parts.push(format!("certificateMapEntryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute()` or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateMapEntryId
    pub certificateMapEntryId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder(client, &args.parent, &args.certificateMapEntryId)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute(
        builder,
    )
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Deletes a single CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Deletes a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute()` or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Deletes a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Deletes a single CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder(client, &args.name)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Gets details of a single CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Gets details of a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute()` or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateMapEntry>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateMapEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Gets details of a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMapEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Gets details of a single CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMapEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder(
            client, &args.name,
        )?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute()` or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateMapEntriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateMapEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder(client, &args.parent, &args.filter, &args.orderBy, &args.pageSize, &args.pageToken)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute(
        builder,
    )
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Updates a CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Updates a CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute()` or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Updates a CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateMaps/{certificateMapsId}/certificateMapEntries/{certificateMapEntriesId}
/// Updates a CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder(client, &args.name, &args.updateMask)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_create_execute()` to send, or `certificatemanager_projects_locations_certificates_create` for simplest API.

pub fn certificatemanager_projects_locations_certificates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateId.as_ref() {
        query_parts.push(format!("certificateId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificates_create_execute()` or `certificatemanager_projects_locations_certificates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificates_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateId
    pub certificateId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_create_builder()` + `certificatemanager_projects_locations_certificates_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_create_builder(
        client,
        &args.parent,
        &args.certificateId,
    )?;
    certificatemanager_projects_locations_certificates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Deletes a single Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_delete_execute()` to send, or `certificatemanager_projects_locations_certificates_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Deletes a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificates_delete_execute()` or `certificatemanager_projects_locations_certificates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Deletes a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificates_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Deletes a single Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_delete_builder()` + `certificatemanager_projects_locations_certificates_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificates_delete_builder(client, &args.name)?;
    certificatemanager_projects_locations_certificates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Gets details of a single Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_get_execute()` to send, or `certificatemanager_projects_locations_certificates_get` for simplest API.

pub fn certificatemanager_projects_locations_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Gets details of a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificates_get_execute()` or `certificatemanager_projects_locations_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Gets details of a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificates_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Gets details of a single Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_get_builder()` + `certificatemanager_projects_locations_certificates_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificates_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_certificates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Lists Certificates in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_list_execute()` to send, or `certificatemanager_projects_locations_certificates_list` for simplest API.

pub fn certificatemanager_projects_locations_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Lists Certificates in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificates_list_execute()` or `certificatemanager_projects_locations_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Lists Certificates in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificates_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificates
/// Lists Certificates in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_list_builder()` + `certificatemanager_projects_locations_certificates_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_certificates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates a Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_patch_execute()` to send, or `certificatemanager_projects_locations_certificates_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_certificates_patch_execute()` or `certificatemanager_projects_locations_certificates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_certificates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_certificates_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_certificates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_certificates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_certificates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsCertificatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificates/{certificatesId}
/// Updates a Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_patch_builder()` + `certificatemanager_projects_locations_certificates_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_certificates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsCertificatesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    certificatemanager_projects_locations_certificates_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_create_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_create` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dnsAuthorizationId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsAuthorizations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dnsAuthorizationId.as_ref() {
        query_parts.push(format!("dnsAuthorizationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_dns_authorizations_create_execute()` or `certificatemanager_projects_locations_dns_authorizations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_dns_authorizations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_dns_authorizations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_dns_authorizations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_dns_authorizations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_dns_authorizations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsDnsAuthorizationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dnsAuthorizationId
    pub dnsAuthorizationId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_create_builder()` + `certificatemanager_projects_locations_dns_authorizations_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsDnsAuthorizationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_create_builder(
        client,
        &args.parent,
        &args.dnsAuthorizationId,
    )?;
    certificatemanager_projects_locations_dns_authorizations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Deletes a single DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_delete_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_delete` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Deletes a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_dns_authorizations_delete_execute()` or `certificatemanager_projects_locations_dns_authorizations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Deletes a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_dns_authorizations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_dns_authorizations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_dns_authorizations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_dns_authorizations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_dns_authorizations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsDnsAuthorizationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Deletes a single DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_delete_builder()` + `certificatemanager_projects_locations_dns_authorizations_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsDnsAuthorizationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_delete_builder(
        client, &args.name,
    )?;
    certificatemanager_projects_locations_dns_authorizations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Gets details of a single DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_get_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_get` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Gets details of a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_dns_authorizations_get_execute()` or `certificatemanager_projects_locations_dns_authorizations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DnsAuthorization>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Gets details of a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_dns_authorizations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_dns_authorizations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_dns_authorizations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_dns_authorizations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_dns_authorizations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsDnsAuthorizationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Gets details of a single DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_get_builder()` + `certificatemanager_projects_locations_dns_authorizations_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsDnsAuthorizationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_dns_authorizations_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_dns_authorizations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_list_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_list` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsAuthorizations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_dns_authorizations_list_execute()` or `certificatemanager_projects_locations_dns_authorizations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDnsAuthorizationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDnsAuthorizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_dns_authorizations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_dns_authorizations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_dns_authorizations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsAuthorizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_dns_authorizations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_dns_authorizations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsDnsAuthorizationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_list_builder()` + `certificatemanager_projects_locations_dns_authorizations_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsDnsAuthorizationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsAuthorizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_dns_authorizations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Updates a DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_patch_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_patch` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Updates a DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_dns_authorizations_patch_execute()` or `certificatemanager_projects_locations_dns_authorizations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Updates a DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_dns_authorizations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_dns_authorizations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_dns_authorizations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_dns_authorizations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_dns_authorizations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_dns_authorizations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsDnsAuthorizationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsAuthorizations/{dnsAuthorizationsId}
/// Updates a DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_patch_builder()` + `certificatemanager_projects_locations_dns_authorizations_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_dns_authorizations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsDnsAuthorizationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    certificatemanager_projects_locations_dns_authorizations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_cancel_execute()` to send, or `certificatemanager_projects_locations_operations_cancel` for simplest API.

pub fn certificatemanager_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_operations_cancel_execute()` or `certificatemanager_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_cancel_builder()` + `certificatemanager_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_operations_cancel_builder(client, &args.name)?;
    certificatemanager_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_delete_execute()` to send, or `certificatemanager_projects_locations_operations_delete` for simplest API.

pub fn certificatemanager_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_operations_delete_execute()` or `certificatemanager_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_operations_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_delete_builder()` + `certificatemanager_projects_locations_operations_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_operations_delete_builder(client, &args.name)?;
    certificatemanager_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_get_execute()` to send, or `certificatemanager_projects_locations_operations_get` for simplest API.

pub fn certificatemanager_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_operations_get_execute()` or `certificatemanager_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_operations_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_get_builder()` + `certificatemanager_projects_locations_operations_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_operations_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_list_execute()` to send, or `certificatemanager_projects_locations_operations_list` for simplest API.

pub fn certificatemanager_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_operations_list_execute()` or `certificatemanager_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_operations_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_list_builder()` + `certificatemanager_projects_locations_operations_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    certificatemanager_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_create_execute()` to send, or `certificatemanager_projects_locations_trust_configs_create` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    trustConfigId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/trustConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = trustConfigId.as_ref() {
        query_parts.push(format!("trustConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_trust_configs_create_execute()` or `certificatemanager_projects_locations_trust_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_trust_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_create_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_trust_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_trust_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_trust_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_trust_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsTrustConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: trustConfigId
    pub trustConfigId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_create_builder()` + `certificatemanager_projects_locations_trust_configs_create_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_create(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsTrustConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_create_builder(
        client,
        &args.parent,
        &args.trustConfigId,
    )?;
    certificatemanager_projects_locations_trust_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Deletes a single TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_delete_execute()` to send, or `certificatemanager_projects_locations_trust_configs_delete` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/trustConfigs/{trustConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Deletes a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_trust_configs_delete_execute()` or `certificatemanager_projects_locations_trust_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Deletes a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_trust_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_delete_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_trust_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_trust_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_trust_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_trust_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsTrustConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Deletes a single TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_delete_builder()` + `certificatemanager_projects_locations_trust_configs_delete_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_delete(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsTrustConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_delete_builder(
        client, &args.name, &args.etag,
    )?;
    certificatemanager_projects_locations_trust_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Gets details of a single TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_get_execute()` to send, or `certificatemanager_projects_locations_trust_configs_get` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/trustConfigs/{trustConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Gets details of a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_trust_configs_get_execute()` or `certificatemanager_projects_locations_trust_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TrustConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TrustConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Gets details of a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_trust_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_get_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_trust_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_trust_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrustConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_trust_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_trust_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsTrustConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Gets details of a single TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_get_builder()` + `certificatemanager_projects_locations_trust_configs_get_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_get(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsTrustConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrustConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_trust_configs_get_builder(client, &args.name)?;
    certificatemanager_projects_locations_trust_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_list_execute()` to send, or `certificatemanager_projects_locations_trust_configs_list` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/trustConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_trust_configs_list_execute()` or `certificatemanager_projects_locations_trust_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTrustConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTrustConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_trust_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_list_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_trust_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_trust_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTrustConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_trust_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_trust_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsTrustConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_list_builder()` + `certificatemanager_projects_locations_trust_configs_list_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_list(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsTrustConfigsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTrustConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    certificatemanager_projects_locations_trust_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Updates a TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_patch_execute()` to send, or `certificatemanager_projects_locations_trust_configs_patch` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://certificatemanager.googleapis.com/v1/projects/{}/locations/{locationsId}/trustConfigs/{trustConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Updates a TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `certificatemanager_projects_locations_trust_configs_patch_execute()` or `certificatemanager_projects_locations_trust_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Updates a TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `certificatemanager_projects_locations_trust_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_patch_task()`.
/// For the simplest API, use `certificatemanager_projects_locations_trust_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `certificatemanager_projects_locations_trust_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn certificatemanager_projects_locations_trust_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = certificatemanager_projects_locations_trust_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`certificatemanager_projects_locations_trust_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CertificatemanagerProjectsLocationsTrustConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/trustConfigs/{trustConfigsId}
/// Updates a TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_patch_builder()` + `certificatemanager_projects_locations_trust_configs_patch_execute()`.
/// For task-level control, use `certificatemanager_projects_locations_trust_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_patch(
    client: &SimpleHttpClient,
    args: &CertificatemanagerProjectsLocationsTrustConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    certificatemanager_projects_locations_trust_configs_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with CertificatemanagerProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &CertificatemanagerProjectsLocationsGetArgs) -> String {
        format!("gcp::certificatemanager::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with CertificatemanagerProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &CertificatemanagerProjectsLocationsListArgs) -> String {
        format!(
            "gcp::certificatemanager::ListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateIssuanceConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateIssuanceConfigsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateIssuanceConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateIssuanceConfigsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateIssuanceConfig
// =============================================================================

/// ResourceIdentifier implementation for CertificateIssuanceConfig with CertificatemanagerProjectsLocationsCertificateIssuanceConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateIssuanceConfigsGetArgs>
    for CertificateIssuanceConfig
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::CertificateIssuanceConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::CertificateIssuanceConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateIssuanceConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateIssuanceConfigsResponse with CertificatemanagerProjectsLocationsCertificateIssuanceConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateIssuanceConfigsListArgs>
    for ListCertificateIssuanceConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListCertificateIssuanceConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListCertificateIssuanceConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateIssuanceConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateIssuanceConfigsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateIssuanceConfigsPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateMapsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateMapsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateMap
// =============================================================================

/// ResourceIdentifier implementation for CertificateMap with CertificatemanagerProjectsLocationsCertificateMapsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateMapsGetArgs>
    for CertificateMap
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsGetArgs,
    ) -> String {
        format!("gcp::certificatemanager::CertificateMap/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::CertificateMap"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateMapsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateMapsResponse with CertificatemanagerProjectsLocationsCertificateMapsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateMapsListArgs>
    for ListCertificateMapsResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListCertificateMapsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListCertificateMapsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificateMapsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesCreateArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesDeleteArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateMapEntry
// =============================================================================

/// ResourceIdentifier implementation for CertificateMapEntry with CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesGetArgs,
    > for CertificateMapEntry
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesGetArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::CertificateMapEntry/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::CertificateMapEntry"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateMapEntriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateMapEntriesResponse with CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesListArgs,
    > for ListCertificateMapEntriesResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListCertificateMapEntriesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListCertificateMapEntriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesPatchArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificateMapsCertificateMapEntriesPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificatesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificatesCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificatesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificatesDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Certificate
// =============================================================================

/// ResourceIdentifier implementation for Certificate with CertificatemanagerProjectsLocationsCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificatesGetArgs> for Certificate {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificatesGetArgs,
    ) -> String {
        format!("gcp::certificatemanager::Certificate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Certificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificatesResponse with CertificatemanagerProjectsLocationsCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificatesListArgs>
    for ListCertificatesResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificatesListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListCertificatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsCertificatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsCertificatesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsCertificatesPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsDnsAuthorizationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsDnsAuthorizationsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsDnsAuthorizationsCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsDnsAuthorizationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsDnsAuthorizationsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsDnsAuthorizationsDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DnsAuthorization
// =============================================================================

/// ResourceIdentifier implementation for DnsAuthorization with CertificatemanagerProjectsLocationsDnsAuthorizationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsDnsAuthorizationsGetArgs>
    for DnsAuthorization
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsDnsAuthorizationsGetArgs,
    ) -> String {
        format!("gcp::certificatemanager::DnsAuthorization/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::DnsAuthorization"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDnsAuthorizationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDnsAuthorizationsResponse with CertificatemanagerProjectsLocationsDnsAuthorizationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsDnsAuthorizationsListArgs>
    for ListDnsAuthorizationsResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsDnsAuthorizationsListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListDnsAuthorizationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListDnsAuthorizationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsDnsAuthorizationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsDnsAuthorizationsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsDnsAuthorizationsPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with CertificatemanagerProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::certificatemanager::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with CertificatemanagerProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with CertificatemanagerProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsTrustConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsTrustConfigsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsTrustConfigsCreateArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsTrustConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsTrustConfigsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsTrustConfigsDeleteArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TrustConfig
// =============================================================================

/// ResourceIdentifier implementation for TrustConfig with CertificatemanagerProjectsLocationsTrustConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsTrustConfigsGetArgs> for TrustConfig {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsTrustConfigsGetArgs,
    ) -> String {
        format!("gcp::certificatemanager::TrustConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::TrustConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTrustConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTrustConfigsResponse with CertificatemanagerProjectsLocationsTrustConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsTrustConfigsListArgs>
    for ListTrustConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsTrustConfigsListArgs,
    ) -> String {
        format!(
            "gcp::certificatemanager::ListTrustConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::ListTrustConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CertificatemanagerProjectsLocationsTrustConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CertificatemanagerProjectsLocationsTrustConfigsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CertificatemanagerProjectsLocationsTrustConfigsPatchArgs,
    ) -> String {
        format!("gcp::certificatemanager::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::certificatemanager::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
