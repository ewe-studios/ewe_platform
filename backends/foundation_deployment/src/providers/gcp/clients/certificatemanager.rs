//! Auto-generated API clients for gcp/certificatemanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_get_execute()` to send, or `certificatemanager_projects_locations_get` for simplest API.

pub fn certificatemanager_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_get_builder()` + `certificatemanager_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_get_builder(client, name)?;
    certificatemanager_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_list_execute()` to send, or `certificatemanager_projects_locations_list` for simplest API.

pub fn certificatemanager_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_list_builder()` + `certificatemanager_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    certificatemanager_projects_locations_list_execute(builder)
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_create_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    certificateIssuanceConfigId: Option<&str>,
    body: &CertificateIssuanceConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateIssuanceConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "certificateIssuanceConfigId",
            certificateIssuanceConfigId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_issuance_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Creates a new CertificateIssuanceConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_create_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    certificateIssuanceConfigId: Option<&str>,
    body: &CertificateIssuanceConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_issuance_configs_create_builder(
            client,
            parent,
            certificateIssuanceConfigId,
            body,
        )?;
    certificatemanager_projects_locations_certificate_issuance_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_delete_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_issuance_configs_delete_builder(
            client, name,
        )?;
    certificatemanager_projects_locations_certificate_issuance_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_get_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_issuance_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateIssuanceConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateIssuanceConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_get_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateIssuanceConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_get_builder(
        client, name,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_get_execute(builder)
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_list_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateIssuanceConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_issuance_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateIssuanceConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateIssuanceConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateIssuanceConfigs
/// Lists CertificateIssuanceConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_list_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateIssuanceConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a CertificateIssuanceConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_issuance_configs_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_issuance_configs_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateIssuanceConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a CertificateIssuanceConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a CertificateIssuanceConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_issuance_configs_patch_builder()` + `certificatemanager_projects_locations_certificate_issuance_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_issuance_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateIssuanceConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_issuance_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    certificatemanager_projects_locations_certificate_issuance_configs_patch_execute(builder)
}

/// GET v1/{+parent}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_create_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    certificateMapId: Option<&str>,
    body: &CertificateMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateMaps",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "certificateMapId",
            certificateMapId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateMaps
/// Creates a new CertificateMap in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_create_builder()` + `certificatemanager_projects_locations_certificate_maps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_create(
    client: &SimpleHttpClient,
    parent: &str,
    certificateMapId: Option<&str>,
    body: &CertificateMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_create_builder(
        client,
        parent,
        certificateMapId,
        body,
    )?;
    certificatemanager_projects_locations_certificate_maps_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateMap. A Certificate Map can't be deleted if it contains Certificate Map Entries. Remove all the entries from the map before calling this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_delete_builder()` + `certificatemanager_projects_locations_certificate_maps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_maps_delete_builder(client, name)?;
    certificatemanager_projects_locations_certificate_maps_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single CertificateMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_get_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_get_builder()` + `certificatemanager_projects_locations_certificate_maps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_get_builder(client, name)?;
    certificatemanager_projects_locations_certificate_maps_get_execute(builder)
}

/// GET v1/{+parent}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_list_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateMaps",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateMapsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateMaps
/// Lists CertificateMaps in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_list_builder()` + `certificatemanager_projects_locations_certificate_maps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    certificatemanager_projects_locations_certificate_maps_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a CertificateMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a CertificateMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a CertificateMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_patch_builder()` + `certificatemanager_projects_locations_certificate_maps_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_patch_builder(
        client, name, updateMask, body,
    )?;
    certificatemanager_projects_locations_certificate_maps_patch_execute(builder)
}

/// GET v1/{+parent}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    certificateMapEntryId: Option<&str>,
    body: &CertificateMapEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateMapEntries",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "certificateMapEntryId",
            certificateMapEntryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateMapEntries
/// Creates a new CertificateMapEntry in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create(
    client: &SimpleHttpClient,
    parent: &str,
    certificateMapEntryId: Option<&str>,
    body: &CertificateMapEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_builder(client, parent, certificateMapEntryId, body)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_create_execute(
        builder,
    )
}

/// GET v1/{+name}
/// Deletes a single CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_builder(client, name)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_delete_execute(
        builder,
    )
}

/// GET v1/{+name}
/// Gets details of a single CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMapEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateMapEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateMapEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_builder(
            client, name,
        )?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_get_execute(
        builder,
    )
}

/// GET v1/{+parent}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/certificateMapEntries",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateMapEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificateMapEntries
/// Lists CertificateMapEntries in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateMapEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_builder(client, parent, filter, orderBy, pageSize, pageToken)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_list_execute(
        builder,
    )
}

/// GET v1/{+name}
/// Updates a CertificateMapEntry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute()` to send, or `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateMapEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a CertificateMapEntry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a CertificateMapEntry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder()` + `certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CertificateMapEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_builder(client, name, updateMask, body)?;
    certificatemanager_projects_locations_certificate_maps_certificate_map_entries_patch_execute(
        builder,
    )
}

/// GET v1/{+parent}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_create_execute()` to send, or `certificatemanager_projects_locations_certificates_create` for simplest API.

pub fn certificatemanager_projects_locations_certificates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    certificateId: Option<&str>,
    body: &Certificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "certificateId",
            certificateId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// Creates a new Certificate in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_create_builder()` + `certificatemanager_projects_locations_certificates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_create(
    client: &SimpleHttpClient,
    parent: &str,
    certificateId: Option<&str>,
    body: &Certificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_create_builder(
        client,
        parent,
        certificateId,
        body,
    )?;
    certificatemanager_projects_locations_certificates_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_delete_execute()` to send, or `certificatemanager_projects_locations_certificates_delete` for simplest API.

pub fn certificatemanager_projects_locations_certificates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_delete_builder()` + `certificatemanager_projects_locations_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_delete_builder(client, name)?;
    certificatemanager_projects_locations_certificates_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_get_execute()` to send, or `certificatemanager_projects_locations_certificates_get` for simplest API.

pub fn certificatemanager_projects_locations_certificates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_get_builder()` + `certificatemanager_projects_locations_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_get_builder(client, name)?;
    certificatemanager_projects_locations_certificates_get_execute(builder)
}

/// GET v1/{+parent}/certificates
/// Lists Certificates in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_list_execute()` to send, or `certificatemanager_projects_locations_certificates_list` for simplest API.

pub fn certificatemanager_projects_locations_certificates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/certificates", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/certificates
/// Lists Certificates in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/certificates
/// Lists Certificates in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_list_builder()` + `certificatemanager_projects_locations_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    certificatemanager_projects_locations_certificates_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_certificates_patch_execute()` to send, or `certificatemanager_projects_locations_certificates_patch` for simplest API.

pub fn certificatemanager_projects_locations_certificates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Certificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_certificates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_certificates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_certificates_patch_builder()` + `certificatemanager_projects_locations_certificates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_certificates_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Certificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_certificates_patch_builder(
        client, name, updateMask, body,
    )?;
    certificatemanager_projects_locations_certificates_patch_execute(builder)
}

/// GET v1/{+parent}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_create_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_create` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dnsAuthorizationId: Option<&str>,
    body: &DnsAuthorization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsAuthorizations",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dnsAuthorizationId",
            dnsAuthorizationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_dns_authorizations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsAuthorizations
/// Creates a new DnsAuthorization in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_create_builder()` + `certificatemanager_projects_locations_dns_authorizations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_create(
    client: &SimpleHttpClient,
    parent: &str,
    dnsAuthorizationId: Option<&str>,
    body: &DnsAuthorization,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_create_builder(
        client,
        parent,
        dnsAuthorizationId,
        body,
    )?;
    certificatemanager_projects_locations_dns_authorizations_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_delete_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_delete` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_dns_authorizations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_delete_builder()` + `certificatemanager_projects_locations_dns_authorizations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_dns_authorizations_delete_builder(client, name)?;
    certificatemanager_projects_locations_dns_authorizations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_get_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_get` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_dns_authorizations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_get_builder()` + `certificatemanager_projects_locations_dns_authorizations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsAuthorization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_dns_authorizations_get_builder(client, name)?;
    certificatemanager_projects_locations_dns_authorizations_get_execute(builder)
}

/// GET v1/{+parent}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_list_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_list` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/dnsAuthorizations",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_dns_authorizations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsAuthorizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDnsAuthorizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dnsAuthorizations
/// Lists DnsAuthorizations in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_list_builder()` + `certificatemanager_projects_locations_dns_authorizations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsAuthorizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    certificatemanager_projects_locations_dns_authorizations_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a DnsAuthorization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_dns_authorizations_patch_execute()` to send, or `certificatemanager_projects_locations_dns_authorizations_patch` for simplest API.

pub fn certificatemanager_projects_locations_dns_authorizations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &DnsAuthorization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a DnsAuthorization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_dns_authorizations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a DnsAuthorization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_dns_authorizations_patch_builder()` + `certificatemanager_projects_locations_dns_authorizations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_dns_authorizations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &DnsAuthorization,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_dns_authorizations_patch_builder(
        client, name, updateMask, body,
    )?;
    certificatemanager_projects_locations_dns_authorizations_patch_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_cancel_execute()` to send, or `certificatemanager_projects_locations_operations_cancel` for simplest API.

pub fn certificatemanager_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_cancel_builder()` + `certificatemanager_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_operations_cancel_builder(client, name, body)?;
    certificatemanager_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_delete_execute()` to send, or `certificatemanager_projects_locations_operations_delete` for simplest API.

pub fn certificatemanager_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_delete_builder()` + `certificatemanager_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_operations_delete_builder(client, name)?;
    certificatemanager_projects_locations_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_get_execute()` to send, or `certificatemanager_projects_locations_operations_get` for simplest API.

pub fn certificatemanager_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_get_builder()` + `certificatemanager_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_operations_get_builder(client, name)?;
    certificatemanager_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_operations_list_execute()` to send, or `certificatemanager_projects_locations_operations_list` for simplest API.

pub fn certificatemanager_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_operations_list_builder()` + `certificatemanager_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    certificatemanager_projects_locations_operations_list_execute(builder)
}

/// GET v1/{+parent}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_create_execute()` to send, or `certificatemanager_projects_locations_trust_configs_create` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    trustConfigId: Option<&str>,
    body: &TrustConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/trustConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "trustConfigId",
            trustConfigId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_trust_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_trust_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/trustConfigs
/// Creates a new TrustConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_create_builder()` + `certificatemanager_projects_locations_trust_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    trustConfigId: Option<&str>,
    body: &TrustConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_create_builder(
        client,
        parent,
        trustConfigId,
        body,
    )?;
    certificatemanager_projects_locations_trust_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_delete_execute()` to send, or `certificatemanager_projects_locations_trust_configs_delete` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_trust_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_trust_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_delete_builder()` + `certificatemanager_projects_locations_trust_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        certificatemanager_projects_locations_trust_configs_delete_builder(client, name, etag)?;
    certificatemanager_projects_locations_trust_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_get_execute()` to send, or `certificatemanager_projects_locations_trust_configs_get` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_trust_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_trust_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrustConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TrustConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_get_builder()` + `certificatemanager_projects_locations_trust_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrustConfig>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_get_builder(client, name)?;
    certificatemanager_projects_locations_trust_configs_get_execute(builder)
}

/// GET v1/{+parent}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_list_execute()` to send, or `certificatemanager_projects_locations_trust_configs_list` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/trustConfigs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_trust_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_trust_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTrustConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTrustConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/trustConfigs
/// Lists TrustConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_list_builder()` + `certificatemanager_projects_locations_trust_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTrustConfigsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    certificatemanager_projects_locations_trust_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a TrustConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `certificatemanager_projects_locations_trust_configs_patch_execute()` to send, or `certificatemanager_projects_locations_trust_configs_patch` for simplest API.

pub fn certificatemanager_projects_locations_trust_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &TrustConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a TrustConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `certificatemanager_projects_locations_trust_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from certificatemanager_projects_locations_trust_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a TrustConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `certificatemanager_projects_locations_trust_configs_patch_builder()` + `certificatemanager_projects_locations_trust_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn certificatemanager_projects_locations_trust_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &TrustConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = certificatemanager_projects_locations_trust_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    certificatemanager_projects_locations_trust_configs_patch_execute(builder)
}
