//! Auto-generated API clients for gcp/tagmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET tagmanager/v2/accounts/{accountsId}
/// Gets a GTM Account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tagmanager_accounts_get_execute()` to send, or `tagmanager_accounts_get` for simplest API.

pub fn tagmanager_accounts_get_builder(
    client: &SimpleHttpClient,
    path: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://tagmanager.googleapis.com/tagmanager/v2/accounts/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tagmanager/v2/accounts/{accountsId}
/// Gets a GTM Account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tagmanager_accounts_get_execute()` or `tagmanager_accounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tagmanager/v2/accounts/{accountsId}
/// Gets a GTM Account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tagmanager_accounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tagmanager_accounts_get_task()`.
/// For the simplest API, use `tagmanager_accounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tagmanager_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tagmanager_accounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tagmanager_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TagmanagerAccountsGetArgs {
    /// Path parameter: path
    pub path: String,
}

/// GET tagmanager/v2/accounts/{accountsId}
/// Gets a GTM Account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tagmanager_accounts_get_builder()` + `tagmanager_accounts_get_execute()`.
/// For task-level control, use `tagmanager_accounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_get(
    client: &SimpleHttpClient,
    args: &TagmanagerAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tagmanager_accounts_get_builder(client, args.path.clone())?;
    tagmanager_accounts_get_execute(builder)
}

/// GET tagmanager/v2/accounts
/// Lists all GTM Accounts that a user has access to.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tagmanager_accounts_list_execute()` to send, or `tagmanager_accounts_list` for simplest API.

pub fn tagmanager_accounts_list_builder(
    client: &SimpleHttpClient,
    includeGoogleTags: Option<bool>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://tagmanager.googleapis.com/tagmanager/v2/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeGoogleTags {
        query_parts.push(format!("includeGoogleTags={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tagmanager/v2/accounts
/// Lists all GTM Accounts that a user has access to.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tagmanager_accounts_list_execute()` or `tagmanager_accounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tagmanager/v2/accounts
/// Lists all GTM Accounts that a user has access to.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tagmanager_accounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tagmanager_accounts_list_task()`.
/// For the simplest API, use `tagmanager_accounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tagmanager_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tagmanager_accounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tagmanager_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TagmanagerAccountsListArgs {
    /// Query parameter: includeGoogleTags
    pub includeGoogleTags: Option<bool>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET tagmanager/v2/accounts
/// Lists all GTM Accounts that a user has access to.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tagmanager_accounts_list_builder()` + `tagmanager_accounts_list_execute()`.
/// For task-level control, use `tagmanager_accounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_list(
    client: &SimpleHttpClient,
    args: &TagmanagerAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tagmanager_accounts_list_builder(
        client,
        args.includeGoogleTags.clone(),
        args.pageToken.clone(),
    )?;
    tagmanager_accounts_list_execute(builder)
}

/// GET tagmanager/v2/accounts/{accountsId}/containers
/// Creates a Container.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tagmanager_accounts_containers_create_execute()` to send, or `tagmanager_accounts_containers_create` for simplest API.

pub fn tagmanager_accounts_containers_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &Container,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://tagmanager.googleapis.com/tagmanager/v2/accounts/{}/containers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tagmanager/v2/accounts/{accountsId}/containers
/// Creates a Container.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tagmanager_accounts_containers_create_execute()` or `tagmanager_accounts_containers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_containers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_containers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Container>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Container = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tagmanager/v2/accounts/{accountsId}/containers
/// Creates a Container.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tagmanager_accounts_containers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tagmanager_accounts_containers_create_task()`.
/// For the simplest API, use `tagmanager_accounts_containers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_containers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tagmanager_accounts_containers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Container>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tagmanager_accounts_containers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tagmanager_accounts_containers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TagmanagerAccountsContainersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Container,
}

/// GET tagmanager/v2/accounts/{accountsId}/containers
/// Creates a Container.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tagmanager_accounts_containers_create_builder()` + `tagmanager_accounts_containers_create_execute()`.
/// For task-level control, use `tagmanager_accounts_containers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_containers_create(
    client: &SimpleHttpClient,
    args: &TagmanagerAccountsContainersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Container>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        tagmanager_accounts_containers_create_builder(client, args.parent.clone(), &args.body)?;
    tagmanager_accounts_containers_create_execute(builder)
}

/// GET tagmanager/v2/accounts/containers:lookup
/// Looks up a Container by destination ID or tag ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tagmanager_accounts_containers_lookup_execute()` to send, or `tagmanager_accounts_containers_lookup` for simplest API.

pub fn tagmanager_accounts_containers_lookup_builder(
    client: &SimpleHttpClient,
    destinationId: Option<String>,
    tagId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://tagmanager.googleapis.com/tagmanager/v2/accounts/containers:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinationId {
        query_parts.push(format!("destinationId={}", val));
    }
    if let Some(val) = tagId {
        query_parts.push(format!("tagId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET tagmanager/v2/accounts/containers:lookup
/// Looks up a Container by destination ID or tag ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tagmanager_accounts_containers_lookup_execute()` or `tagmanager_accounts_containers_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_containers_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_containers_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Container>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Container = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tagmanager/v2/accounts/containers:lookup
/// Looks up a Container by destination ID or tag ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tagmanager_accounts_containers_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tagmanager_accounts_containers_lookup_task()`.
/// For the simplest API, use `tagmanager_accounts_containers_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_containers_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tagmanager_accounts_containers_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Container>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tagmanager_accounts_containers_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tagmanager_accounts_containers_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TagmanagerAccountsContainersLookupArgs {
    /// Query parameter: destinationId
    pub destinationId: Option<String>,
    /// Query parameter: tagId
    pub tagId: Option<String>,
}

/// GET tagmanager/v2/accounts/containers:lookup
/// Looks up a Container by destination ID or tag ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tagmanager_accounts_containers_lookup_builder()` + `tagmanager_accounts_containers_lookup_execute()`.
/// For task-level control, use `tagmanager_accounts_containers_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_containers_lookup(
    client: &SimpleHttpClient,
    args: &TagmanagerAccountsContainersLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Container>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tagmanager_accounts_containers_lookup_builder(
        client,
        args.destinationId.clone(),
        args.tagId.clone(),
    )?;
    tagmanager_accounts_containers_lookup_execute(builder)
}

/// GET tagmanager/v2/accounts/{accountsId}/user_permissions
/// Creates a user's Account & Container access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tagmanager_accounts_user_permissions_create_execute()` to send, or `tagmanager_accounts_user_permissions_create` for simplest API.

pub fn tagmanager_accounts_user_permissions_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &UserPermission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://tagmanager.googleapis.com/tagmanager/v2/accounts/{}/user_permissions",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET tagmanager/v2/accounts/{accountsId}/user_permissions
/// Creates a user's Account & Container access.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tagmanager_accounts_user_permissions_create_execute()` or `tagmanager_accounts_user_permissions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_user_permissions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_user_permissions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserPermission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET tagmanager/v2/accounts/{accountsId}/user_permissions
/// Creates a user's Account & Container access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tagmanager_accounts_user_permissions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tagmanager_accounts_user_permissions_create_task()`.
/// For the simplest API, use `tagmanager_accounts_user_permissions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tagmanager_accounts_user_permissions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tagmanager_accounts_user_permissions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tagmanager_accounts_user_permissions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tagmanager_accounts_user_permissions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TagmanagerAccountsUserPermissionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: UserPermission,
}

/// GET tagmanager/v2/accounts/{accountsId}/user_permissions
/// Creates a user's Account & Container access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tagmanager_accounts_user_permissions_create_builder()` + `tagmanager_accounts_user_permissions_create_execute()`.
/// For task-level control, use `tagmanager_accounts_user_permissions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tagmanager_accounts_user_permissions_create(
    client: &SimpleHttpClient,
    args: &TagmanagerAccountsUserPermissionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserPermission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tagmanager_accounts_user_permissions_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    tagmanager_accounts_user_permissions_create_execute(builder)
}
