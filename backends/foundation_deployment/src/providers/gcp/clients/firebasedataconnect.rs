//! Auto-generated API clients for gcp/firebasedataconnect.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_get_execute()` to send, or `firebasedataconnect_projects_locations_get` for simplest API.

pub fn firebasedataconnect_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_get_execute()` or `firebasedataconnect_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_get_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_get_builder()` + `firebasedataconnect_projects_locations_get_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_get(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_get_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_list_execute()` to send, or `firebasedataconnect_projects_locations_list` for simplest API.

pub fn firebasedataconnect_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_list_execute()` or `firebasedataconnect_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_list_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_list_builder()` + `firebasedataconnect_projects_locations_list_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_list(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    firebasedataconnect_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_operations_cancel_execute()` to send, or `firebasedataconnect_projects_locations_operations_cancel` for simplest API.

pub fn firebasedataconnect_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_operations_cancel_execute()` or `firebasedataconnect_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_operations_cancel_builder()` + `firebasedataconnect_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_operations_cancel_builder(
        client, &args.name, &args.body,
    )?;
    firebasedataconnect_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_operations_delete_execute()` to send, or `firebasedataconnect_projects_locations_operations_delete` for simplest API.

pub fn firebasedataconnect_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_operations_delete_execute()` or `firebasedataconnect_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_delete_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_operations_delete_builder()` + `firebasedataconnect_projects_locations_operations_delete_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_operations_delete_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_operations_get_execute()` to send, or `firebasedataconnect_projects_locations_operations_get` for simplest API.

pub fn firebasedataconnect_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_operations_get_execute()` or `firebasedataconnect_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_get_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_operations_get_builder()` + `firebasedataconnect_projects_locations_operations_get_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_operations_get_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_operations_list_execute()` to send, or `firebasedataconnect_projects_locations_operations_list` for simplest API.

pub fn firebasedataconnect_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_operations_list_execute()` or `firebasedataconnect_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_list_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_operations_list_builder()` + `firebasedataconnect_projects_locations_operations_list_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    firebasedataconnect_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_create_execute()` to send, or `firebasedataconnect_projects_locations_services_create` for simplest API.

pub fn firebasedataconnect_projects_locations_services_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    serviceId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceId {
        query_parts.push(format!("serviceId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_create_execute()` or `firebasedataconnect_projects_locations_services_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_create_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: serviceId
    pub serviceId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Service,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_create_builder()` + `firebasedataconnect_projects_locations_services_create_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_create(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_create_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        args.serviceId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_delete_execute()` to send, or `firebasedataconnect_projects_locations_services_delete` for simplest API.

pub fn firebasedataconnect_projects_locations_services_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_delete_execute()` or `firebasedataconnect_projects_locations_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_delete_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_delete_builder()` + `firebasedataconnect_projects_locations_services_delete_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_delete(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.force,
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    firebasedataconnect_projects_locations_services_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphql
/// Execute any GraphQL query and mutation against the Firebase Data Connect's generated GraphQL schema. Grants full read and write access to the connected data sources. Note: Use introspection query to explore the generated GraphQL schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_execute_graphql_execute()` to send, or `firebasedataconnect_projects_locations_services_execute_graphql` for simplest API.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GraphqlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}:executeGraphql",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphql
/// Execute any GraphQL query and mutation against the Firebase Data Connect's generated GraphQL schema. Grants full read and write access to the connected data sources. Note: Use introspection query to explore the generated GraphQL schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_execute_graphql_execute()` or `firebasedataconnect_projects_locations_services_execute_graphql`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_execute_graphql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GraphqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphql
/// Execute any GraphQL query and mutation against the Firebase Data Connect's generated GraphQL schema. Grants full read and write access to the connected data sources. Note: Use introspection query to explore the generated GraphQL schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_execute_graphql_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_execute_graphql_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_execute_graphql()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_execute_graphql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_execute_graphql_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_execute_graphql`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesExecuteGraphqlArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GraphqlRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphql
/// Execute any GraphQL query and mutation against the Firebase Data Connect's generated GraphQL schema. Grants full read and write access to the connected data sources. Note: Use introspection query to explore the generated GraphQL schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_execute_graphql_builder()` + `firebasedataconnect_projects_locations_services_execute_graphql_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_execute_graphql_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_execute_graphql(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesExecuteGraphqlArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_execute_graphql_builder(
        client, &args.name, &args.body,
    )?;
    firebasedataconnect_projects_locations_services_execute_graphql_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphqlRead
/// Execute any GraphQL query against the Firebase Data Connect's generated GraphQL schema. Grants full read to the connected data sources. ExecuteGraphqlRead is identical to ExecuteGraphql except it only accepts read-only query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_execute_graphql_read_execute()` to send, or `firebasedataconnect_projects_locations_services_execute_graphql_read` for simplest API.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_read_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GraphqlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}:executeGraphqlRead",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphqlRead
/// Execute any GraphQL query against the Firebase Data Connect's generated GraphQL schema. Grants full read to the connected data sources. ExecuteGraphqlRead is identical to ExecuteGraphql except it only accepts read-only query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_execute_graphql_read_execute()` or `firebasedataconnect_projects_locations_services_execute_graphql_read`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_execute_graphql_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_read_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GraphqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphqlRead
/// Execute any GraphQL query against the Firebase Data Connect's generated GraphQL schema. Grants full read to the connected data sources. ExecuteGraphqlRead is identical to ExecuteGraphql except it only accepts read-only query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_execute_graphql_read_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_execute_graphql_read_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_execute_graphql_read()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_execute_graphql_read_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_execute_graphql_read_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_execute_graphql_read`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesExecuteGraphqlReadArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GraphqlRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:executeGraphqlRead
/// Execute any GraphQL query against the Firebase Data Connect's generated GraphQL schema. Grants full read to the connected data sources. ExecuteGraphqlRead is identical to ExecuteGraphql except it only accepts read-only query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_execute_graphql_read_builder()` + `firebasedataconnect_projects_locations_services_execute_graphql_read_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_execute_graphql_read_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_execute_graphql_read(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesExecuteGraphqlReadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_execute_graphql_read_builder(
        client, &args.name, &args.body,
    )?;
    firebasedataconnect_projects_locations_services_execute_graphql_read_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_get_execute()` to send, or `firebasedataconnect_projects_locations_services_get` for simplest API.

pub fn firebasedataconnect_projects_locations_services_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_get_execute()` or `firebasedataconnect_projects_locations_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_get_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_get_builder()` + `firebasedataconnect_projects_locations_services_get_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_get(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_get_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_services_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:introspectGraphql
/// Execute introspection query against the Firebase Data Connect's generated GraphQL schema. GraphQL introspection query provides metadata such as what tables the schema have, what queries and mutations can be performed on the schema, and so on. Read more at <https://graphql.`org/learn/introspection`.> IntrospectGraphql can read schema metadata but cannot read rows from Cloud SQL instance, which can be done via ExecuteGraphqlRead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_introspect_graphql_execute()` to send, or `firebasedataconnect_projects_locations_services_introspect_graphql` for simplest API.

pub fn firebasedataconnect_projects_locations_services_introspect_graphql_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GraphqlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}:introspectGraphql",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:introspectGraphql
/// Execute introspection query against the Firebase Data Connect's generated GraphQL schema. GraphQL introspection query provides metadata such as what tables the schema have, what queries and mutations can be performed on the schema, and so on. Read more at <https://graphql.`org/learn/introspection`.> IntrospectGraphql can read schema metadata but cannot read rows from Cloud SQL instance, which can be done via ExecuteGraphqlRead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_introspect_graphql_execute()` or `firebasedataconnect_projects_locations_services_introspect_graphql`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_introspect_graphql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_introspect_graphql_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GraphqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:introspectGraphql
/// Execute introspection query against the Firebase Data Connect's generated GraphQL schema. GraphQL introspection query provides metadata such as what tables the schema have, what queries and mutations can be performed on the schema, and so on. Read more at <https://graphql.`org/learn/introspection`.> IntrospectGraphql can read schema metadata but cannot read rows from Cloud SQL instance, which can be done via ExecuteGraphqlRead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_introspect_graphql_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_introspect_graphql_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_introspect_graphql()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_introspect_graphql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_introspect_graphql_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_introspect_graphql_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_introspect_graphql`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesIntrospectGraphqlArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GraphqlRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:introspectGraphql
/// Execute introspection query against the Firebase Data Connect's generated GraphQL schema. GraphQL introspection query provides metadata such as what tables the schema have, what queries and mutations can be performed on the schema, and so on. Read more at <https://graphql.`org/learn/introspection`.> IntrospectGraphql can read schema metadata but cannot read rows from Cloud SQL instance, which can be done via ExecuteGraphqlRead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_introspect_graphql_builder()` + `firebasedataconnect_projects_locations_services_introspect_graphql_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_introspect_graphql_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_introspect_graphql(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesIntrospectGraphqlArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_introspect_graphql_builder(
        client, &args.name, &args.body,
    )?;
    firebasedataconnect_projects_locations_services_introspect_graphql_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_list_execute()` to send, or `firebasedataconnect_projects_locations_services_list` for simplest API.

pub fn firebasedataconnect_projects_locations_services_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_list_execute()` or `firebasedataconnect_projects_locations_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_list_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_list_builder()` + `firebasedataconnect_projects_locations_services_list_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_list(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    firebasedataconnect_projects_locations_services_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_patch_execute()` to send, or `firebasedataconnect_projects_locations_services_patch` for simplest API.

pub fn firebasedataconnect_projects_locations_services_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_patch_execute()` or `firebasedataconnect_projects_locations_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_patch_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Service,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_patch_builder()` + `firebasedataconnect_projects_locations_services_patch_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_patch(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Creates a new Connector in a given project and location. The operations are validated against and must be compatible with the active schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_create_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_create` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    connectorId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = connectorId {
        query_parts.push(format!("connectorId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Creates a new Connector in a given project and location. The operations are validated against and must be compatible with the active schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_create_execute()` or `firebasedataconnect_projects_locations_services_connectors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Creates a new Connector in a given project and location. The operations are validated against and must be compatible with the active schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_create_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_connectors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: connectorId
    pub connectorId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Connector,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Creates a new Connector in a given project and location. The operations are validated against and must be compatible with the active schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_create_builder()` + `firebasedataconnect_projects_locations_services_connectors_create_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_create(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_connectors_create_builder(
        client,
        &args.parent,
        args.connectorId.as_deref(),
        args.requestId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_connectors_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Deletes a single Connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_delete_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_delete` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Deletes a single Connector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_delete_execute()` or `firebasedataconnect_projects_locations_services_connectors_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Deletes a single Connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_delete_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_connectors_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Deletes a single Connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_delete_builder()` + `firebasedataconnect_projects_locations_services_connectors_delete_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_delete(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_connectors_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.force,
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    firebasedataconnect_projects_locations_services_connectors_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeMutation
/// Execute a predefined mutation in a Connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_execute_mutation` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteMutationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}:executeMutation",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeMutation
/// Execute a predefined mutation in a Connector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_execute()` or `firebasedataconnect_projects_locations_services_connectors_execute_mutation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_mutation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExecuteMutationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteMutationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeMutation
/// Execute a predefined mutation in a Connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_mutation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteMutationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        firebasedataconnect_projects_locations_services_connectors_execute_mutation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_execute_mutation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsExecuteMutationArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExecuteMutationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeMutation
/// Execute a predefined mutation in a Connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder()` + `firebasedataconnect_projects_locations_services_connectors_execute_mutation_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_execute_mutation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_mutation(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsExecuteMutationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteMutationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_services_connectors_execute_mutation_builder(
            client, &args.name, &args.body,
        )?;
    firebasedataconnect_projects_locations_services_connectors_execute_mutation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeQuery
/// Execute a predefined query in a Connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_execute_query_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_execute_query` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExecuteQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}:executeQuery",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeQuery
/// Execute a predefined query in a Connector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_execute_query_execute()` or `firebasedataconnect_projects_locations_services_connectors_execute_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_execute_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ExecuteQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeQuery
/// Execute a predefined query in a Connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_execute_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_execute_query_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_execute_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_execute_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        firebasedataconnect_projects_locations_services_connectors_execute_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_execute_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsExecuteQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ExecuteQueryRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:executeQuery
/// Execute a predefined query in a Connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_execute_query_builder()` + `firebasedataconnect_projects_locations_services_connectors_execute_query_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_execute_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_execute_query(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsExecuteQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecuteQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_connectors_execute_query_builder(
        client, &args.name, &args.body,
    )?;
    firebasedataconnect_projects_locations_services_connectors_execute_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Gets details of a single Connector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_get_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_get` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Gets details of a single Connector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_get_execute()` or `firebasedataconnect_projects_locations_services_connectors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Connector>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Connector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Gets details of a single Connector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_get_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connector>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_connectors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Gets details of a single Connector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_get_builder()` + `firebasedataconnect_projects_locations_services_connectors_get_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_get(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connector>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_services_connectors_get_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_services_connectors_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateMutation
/// Impersonate a mutation defined on a Firebase Data Connect connector. It grants the admin SDK access to mutations defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ImpersonateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}:impersonateMutation",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateMutation
/// Impersonate a mutation defined on a Firebase Data Connect connector. It grants the admin SDK access to mutations defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_execute()` or `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GraphqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateMutation
/// Impersonate a mutation defined on a Firebase Data Connect connector. It grants the admin SDK access to mutations defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_impersonate_mutation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsImpersonateMutationArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ImpersonateRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateMutation
/// Impersonate a mutation defined on a Firebase Data Connect connector. It grants the admin SDK access to mutations defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder()` + `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_mutation(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsImpersonateMutationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_builder(
            client, &args.name, &args.body,
        )?;
    firebasedataconnect_projects_locations_services_connectors_impersonate_mutation_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateQuery
/// Impersonate a query defined on a Firebase Data Connect connector. It grants the admin SDK access to queries defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_impersonate_query` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ImpersonateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}:impersonateQuery",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateQuery
/// Impersonate a query defined on a Firebase Data Connect connector. It grants the admin SDK access to queries defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_execute()` or `firebasedataconnect_projects_locations_services_connectors_impersonate_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GraphqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateQuery
/// Impersonate a query defined on a Firebase Data Connect connector. It grants the admin SDK access to queries defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        firebasedataconnect_projects_locations_services_connectors_impersonate_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_impersonate_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsImpersonateQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ImpersonateRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}:impersonateQuery
/// Impersonate a query defined on a Firebase Data Connect connector. It grants the admin SDK access to queries defined in the given connector. The caller can choose to impersonate a particular Firebase Auth user, or skip @auth completely.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder()` + `firebasedataconnect_projects_locations_services_connectors_impersonate_query_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_impersonate_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_impersonate_query(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsImpersonateQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GraphqlResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_services_connectors_impersonate_query_builder(
            client, &args.name, &args.body,
        )?;
    firebasedataconnect_projects_locations_services_connectors_impersonate_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Lists Connectors in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_list_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_list` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Lists Connectors in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_list_execute()` or `firebasedataconnect_projects_locations_services_connectors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListConnectorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Lists Connectors in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_list_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_connectors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors
/// Lists Connectors in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_list_builder()` + `firebasedataconnect_projects_locations_services_connectors_list_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_list(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_connectors_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    firebasedataconnect_projects_locations_services_connectors_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Updates the parameters of a single Connector, and creates a new ConnectorRevision with the updated Connector. The operations are validated against and must be compatible with the live schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_connectors_patch_execute()` to send, or `firebasedataconnect_projects_locations_services_connectors_patch` for simplest API.

pub fn firebasedataconnect_projects_locations_services_connectors_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Connector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/connectors/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Updates the parameters of a single Connector, and creates a new ConnectorRevision with the updated Connector. The operations are validated against and must be compatible with the live schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_connectors_patch_execute()` or `firebasedataconnect_projects_locations_services_connectors_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Updates the parameters of a single Connector, and creates a new ConnectorRevision with the updated Connector. The operations are validated against and must be compatible with the live schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_connectors_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_patch_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_connectors_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_connectors_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_connectors_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_connectors_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_connectors_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesConnectorsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Connector,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/connectors/{connectorsId}
/// Updates the parameters of a single Connector, and creates a new ConnectorRevision with the updated Connector. The operations are validated against and must be compatible with the live schema. If the operations and schema are not compatible or if the schema is not present, this will result in an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_connectors_patch_builder()` + `firebasedataconnect_projects_locations_services_connectors_patch_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_connectors_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_connectors_patch(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesConnectorsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_connectors_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_connectors_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Creates a new Schema in a given project and location. Only creation of `schemas/main` is supported and calling create with any other schema ID will result in an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_schemas_create_execute()` to send, or `firebasedataconnect_projects_locations_services_schemas_create` for simplest API.

pub fn firebasedataconnect_projects_locations_services_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    schemaId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = schemaId {
        query_parts.push(format!("schemaId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Creates a new Schema in a given project and location. Only creation of `schemas/main` is supported and calling create with any other schema ID will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_schemas_create_execute()` or `firebasedataconnect_projects_locations_services_schemas_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Creates a new Schema in a given project and location. Only creation of `schemas/main` is supported and calling create with any other schema ID will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_schemas_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_create_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_schemas_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_schemas_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_schemas_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesSchemasCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: schemaId
    pub schemaId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Schema,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Creates a new Schema in a given project and location. Only creation of `schemas/main` is supported and calling create with any other schema ID will result in an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_schemas_create_builder()` + `firebasedataconnect_projects_locations_services_schemas_create_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_create(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesSchemasCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_schemas_create_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        args.schemaId.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Deletes a single Schema. Because the schema and connectors must be compatible at all times, if this is called while any connectors are active, this will result in an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_schemas_delete_execute()` to send, or `firebasedataconnect_projects_locations_services_schemas_delete` for simplest API.

pub fn firebasedataconnect_projects_locations_services_schemas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    force: Option<bool>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Deletes a single Schema. Because the schema and connectors must be compatible at all times, if this is called while any connectors are active, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_schemas_delete_execute()` or `firebasedataconnect_projects_locations_services_schemas_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Deletes a single Schema. Because the schema and connectors must be compatible at all times, if this is called while any connectors are active, this will result in an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_schemas_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_delete_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_schemas_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_schemas_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_schemas_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesSchemasDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: force
    pub force: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Deletes a single Schema. Because the schema and connectors must be compatible at all times, if this is called while any connectors are active, this will result in an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_schemas_delete_builder()` + `firebasedataconnect_projects_locations_services_schemas_delete_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_delete(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesSchemasDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_schemas_delete_builder(
        client,
        &args.name,
        args.allowMissing,
        args.etag.as_deref(),
        args.force,
        args.requestId.as_deref(),
        args.validateOnly,
    )?;
    firebasedataconnect_projects_locations_services_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Gets details of a single Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_schemas_get_execute()` to send, or `firebasedataconnect_projects_locations_services_schemas_get` for simplest API.

pub fn firebasedataconnect_projects_locations_services_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Gets details of a single Schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_schemas_get_execute()` or `firebasedataconnect_projects_locations_services_schemas_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Gets details of a single Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_schemas_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_get_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_schemas_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_schemas_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_schemas_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesSchemasGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Gets details of a single Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_schemas_get_builder()` + `firebasedataconnect_projects_locations_services_schemas_get_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_get(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesSchemasGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firebasedataconnect_projects_locations_services_schemas_get_builder(client, &args.name)?;
    firebasedataconnect_projects_locations_services_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Lists Schemas in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_schemas_list_execute()` to send, or `firebasedataconnect_projects_locations_services_schemas_list` for simplest API.

pub fn firebasedataconnect_projects_locations_services_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Lists Schemas in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_schemas_list_execute()` or `firebasedataconnect_projects_locations_services_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSchemasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSchemasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Lists Schemas in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_list_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSchemasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesSchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas
/// Lists Schemas in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_schemas_list_builder()` + `firebasedataconnect_projects_locations_services_schemas_list_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_list(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesSchemasListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSchemasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_schemas_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    firebasedataconnect_projects_locations_services_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Updates the parameters of a single Schema, and creates a new SchemaRevision with the updated Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedataconnect_projects_locations_services_schemas_patch_execute()` to send, or `firebasedataconnect_projects_locations_services_schemas_patch` for simplest API.

pub fn firebasedataconnect_projects_locations_services_schemas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebasedataconnect.googleapis.com/v1/projects/{}/locations/{}/services/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Updates the parameters of a single Schema, and creates a new SchemaRevision with the updated Schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedataconnect_projects_locations_services_schemas_patch_execute()` or `firebasedataconnect_projects_locations_services_schemas_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Updates the parameters of a single Schema, and creates a new SchemaRevision with the updated Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedataconnect_projects_locations_services_schemas_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_patch_task()`.
/// For the simplest API, use `firebasedataconnect_projects_locations_services_schemas_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedataconnect_projects_locations_services_schemas_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedataconnect_projects_locations_services_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasedataconnect_projects_locations_services_schemas_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedataconnect_projects_locations_services_schemas_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedataconnectProjectsLocationsServicesSchemasPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Schema,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/schemas/{schemasId}
/// Updates the parameters of a single Schema, and creates a new SchemaRevision with the updated Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedataconnect_projects_locations_services_schemas_patch_builder()` + `firebasedataconnect_projects_locations_services_schemas_patch_execute()`.
/// For task-level control, use `firebasedataconnect_projects_locations_services_schemas_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedataconnect_projects_locations_services_schemas_patch(
    client: &SimpleHttpClient,
    args: &FirebasedataconnectProjectsLocationsServicesSchemasPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasedataconnect_projects_locations_services_schemas_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    firebasedataconnect_projects_locations_services_schemas_patch_execute(builder)
}
