//! Auto-generated API clients for gcp/analyticsdata.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/analyticsdata`

#![cfg(feature = "gcp/analyticsdata")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta/properties/{propertiesId}:batchRunPivotReports
/// Returns multiple pivot reports in a batch. All reports must be for the same Google Analytics property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_batch_run_pivot_reports_execute()` to send, or `analyticsdata_properties_batch_run_pivot_reports` for simplest API.

pub fn analyticsdata_properties_batch_run_pivot_reports_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &BatchRunPivotReportsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:batchRunPivotReports",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:batchRunPivotReports
/// Returns multiple pivot reports in a batch. All reports must be for the same Google Analytics property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_batch_run_pivot_reports_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_batch_run_pivot_reports_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_batch_run_pivot_reports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchRunPivotReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchRunPivotReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_batch_run_pivot_reports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesBatchRunPivotReportsArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: BatchRunPivotReportsRequest,
}

/// GET v1beta/properties/{propertiesId}:batchRunPivotReports
/// Returns multiple pivot reports in a batch. All reports must be for the same Google Analytics property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_batch_run_pivot_reports_builder()` + `analyticsdata_properties_batch_run_pivot_reports_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_batch_run_pivot_reports(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesBatchRunPivotReportsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchRunPivotReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsdata_properties_batch_run_pivot_reports_builder(
        client,
        &args.property,
        &args.body,
    )?;
    analyticsdata_properties_batch_run_pivot_reports_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:batchRunReports
/// Returns multiple reports in a batch. All reports must be for the same Google Analytics property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_batch_run_reports_execute()` to send, or `analyticsdata_properties_batch_run_reports` for simplest API.

pub fn analyticsdata_properties_batch_run_reports_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &BatchRunReportsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:batchRunReports",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:batchRunReports
/// Returns multiple reports in a batch. All reports must be for the same Google Analytics property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_batch_run_reports_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_batch_run_reports_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_batch_run_reports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchRunReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchRunReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_batch_run_reports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesBatchRunReportsArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: BatchRunReportsRequest,
}

/// GET v1beta/properties/{propertiesId}:batchRunReports
/// Returns multiple reports in a batch. All reports must be for the same Google Analytics property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_batch_run_reports_builder()` + `analyticsdata_properties_batch_run_reports_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_batch_run_reports(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesBatchRunReportsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchRunReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_batch_run_reports_builder(client, &args.property, &args.body)?;
    analyticsdata_properties_batch_run_reports_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:checkCompatibility
/// This compatibility method lists dimensions and metrics that can be added to a report request and maintain compatibility. This method fails if the request's dimensions and metrics are incompatible. In Google Analytics, reports fail if they request incompatible dimensions `and/or` metrics; in that case, you will need to remove dimensions `and/or` metrics from the incompatible report until the report is compatible. The Realtime and Core reports have different compatibility rules. This method checks compatibility for Core reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_check_compatibility_execute()` to send, or `analyticsdata_properties_check_compatibility` for simplest API.

pub fn analyticsdata_properties_check_compatibility_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &CheckCompatibilityRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:checkCompatibility",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:checkCompatibility
/// This compatibility method lists dimensions and metrics that can be added to a report request and maintain compatibility. This method fails if the request's dimensions and metrics are incompatible. In Google Analytics, reports fail if they request incompatible dimensions `and/or` metrics; in that case, you will need to remove dimensions `and/or` metrics from the incompatible report until the report is compatible. The Realtime and Core reports have different compatibility rules. This method checks compatibility for Core reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_check_compatibility_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_check_compatibility_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_check_compatibility_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckCompatibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckCompatibilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_check_compatibility`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesCheckCompatibilityArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: CheckCompatibilityRequest,
}

/// GET v1beta/properties/{propertiesId}:checkCompatibility
/// This compatibility method lists dimensions and metrics that can be added to a report request and maintain compatibility. This method fails if the request's dimensions and metrics are incompatible. In Google Analytics, reports fail if they request incompatible dimensions `and/or` metrics; in that case, you will need to remove dimensions `and/or` metrics from the incompatible report until the report is compatible. The Realtime and Core reports have different compatibility rules. This method checks compatibility for Core reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_check_compatibility_builder()` + `analyticsdata_properties_check_compatibility_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_check_compatibility(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesCheckCompatibilityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckCompatibilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_check_compatibility_builder(client, &args.property, &args.body)?;
    analyticsdata_properties_check_compatibility_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/metadata
/// Returns metadata for dimensions and metrics available in reporting methods. Used to explore the dimensions and metrics. In this method, a Google Analytics property identifier is specified in the request, and the metadata response includes Custom dimensions and metrics as well as Universal metadata. For example if a custom metric with parameter name levels_unlocked is registered to a property, the Metadata response will contain `customEvent`:levels_unlocked. Universal metadata are dimensions and metrics applicable to any property such as country and `totalUsers`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_get_metadata_execute()` to send, or `analyticsdata_properties_get_metadata` for simplest API.

pub fn analyticsdata_properties_get_metadata_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}/metadata",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/metadata
/// Returns metadata for dimensions and metrics available in reporting methods. Used to explore the dimensions and metrics. In this method, a Google Analytics property identifier is specified in the request, and the metadata response includes Custom dimensions and metrics as well as Universal metadata. For example if a custom metric with parameter name levels_unlocked is registered to a property, the Metadata response will contain `customEvent`:levels_unlocked. Universal metadata are dimensions and metrics applicable to any property such as country and `totalUsers`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_get_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_get_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_get_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Metadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_get_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesGetMetadataArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta/properties/{propertiesId}/metadata
/// Returns metadata for dimensions and metrics available in reporting methods. Used to explore the dimensions and metrics. In this method, a Google Analytics property identifier is specified in the request, and the metadata response includes Custom dimensions and metrics as well as Universal metadata. For example if a custom metric with parameter name levels_unlocked is registered to a property, the Metadata response will contain `customEvent`:levels_unlocked. Universal metadata are dimensions and metrics applicable to any property such as country and `totalUsers`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_get_metadata_builder()` + `analyticsdata_properties_get_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_get_metadata(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesGetMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = analyticsdata_properties_get_metadata_builder(client, &args.name)?;
    analyticsdata_properties_get_metadata_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:runPivotReport
/// Returns a customized pivot report of your Google Analytics event data. Pivot reports are more advanced and expressive formats than regular reports. In a pivot report, dimensions are only visible if they are included in a pivot. Multiple pivots can be specified to further dissect your data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_run_pivot_report_execute()` to send, or `analyticsdata_properties_run_pivot_report` for simplest API.

pub fn analyticsdata_properties_run_pivot_report_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &RunPivotReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:runPivotReport",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:runPivotReport
/// Returns a customized pivot report of your Google Analytics event data. Pivot reports are more advanced and expressive formats than regular reports. In a pivot report, dimensions are only visible if they are included in a pivot. Multiple pivots can be specified to further dissect your data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_run_pivot_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_run_pivot_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_pivot_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunPivotReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunPivotReportResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_run_pivot_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunPivotReportArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: RunPivotReportRequest,
}

/// GET v1beta/properties/{propertiesId}:runPivotReport
/// Returns a customized pivot report of your Google Analytics event data. Pivot reports are more advanced and expressive formats than regular reports. In a pivot report, dimensions are only visible if they are included in a pivot. Multiple pivots can be specified to further dissect your data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_run_pivot_report_builder()` + `analyticsdata_properties_run_pivot_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_pivot_report(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesRunPivotReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunPivotReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_run_pivot_report_builder(client, &args.property, &args.body)?;
    analyticsdata_properties_run_pivot_report_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:runRealtimeReport
/// Returns a customized report of realtime event data for your property. Events appear in realtime reports seconds after they have been sent to the Google Analytics. Realtime reports show events and usage data for the periods of time ranging from the present moment to 30 minutes ago (up to 60 minutes for Google Analytics 360 properties). For a guide to constructing realtime requests & understanding responses, see [Creating a Realtime Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/realtime-basics`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_run_realtime_report_execute()` to send, or `analyticsdata_properties_run_realtime_report` for simplest API.

pub fn analyticsdata_properties_run_realtime_report_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &RunRealtimeReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:runRealtimeReport",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:runRealtimeReport
/// Returns a customized report of realtime event data for your property. Events appear in realtime reports seconds after they have been sent to the Google Analytics. Realtime reports show events and usage data for the periods of time ranging from the present moment to 30 minutes ago (up to 60 minutes for Google Analytics 360 properties). For a guide to constructing realtime requests & understanding responses, see [Creating a Realtime Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/realtime-basics`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_run_realtime_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_run_realtime_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_realtime_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunRealtimeReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunRealtimeReportResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_run_realtime_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunRealtimeReportArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: RunRealtimeReportRequest,
}

/// GET v1beta/properties/{propertiesId}:runRealtimeReport
/// Returns a customized report of realtime event data for your property. Events appear in realtime reports seconds after they have been sent to the Google Analytics. Realtime reports show events and usage data for the periods of time ranging from the present moment to 30 minutes ago (up to 60 minutes for Google Analytics 360 properties). For a guide to constructing realtime requests & understanding responses, see [Creating a Realtime Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/realtime-basics`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_run_realtime_report_builder()` + `analyticsdata_properties_run_realtime_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_realtime_report(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesRunRealtimeReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunRealtimeReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_run_realtime_report_builder(client, &args.property, &args.body)?;
    analyticsdata_properties_run_realtime_report_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:runReport
/// Returns a customized report of your Google Analytics event data. Reports contain statistics derived from data collected by the Google Analytics tracking code. The data returned from the API is as a table with columns for the requested dimensions and metrics. Metrics are individual measurements of user activity on your property, such as active users or event count. Dimensions break down metrics across some common criteria, such as country or event name. For a guide to constructing requests & understanding responses, see [Creating a Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/basics`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_run_report_execute()` to send, or `analyticsdata_properties_run_report` for simplest API.

pub fn analyticsdata_properties_run_report_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &RunReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}:runReport",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:runReport
/// Returns a customized report of your Google Analytics event data. Reports contain statistics derived from data collected by the Google Analytics tracking code. The data returned from the API is as a table with columns for the requested dimensions and metrics. Metrics are individual measurements of user activity on your property, such as active users or event count. Dimensions break down metrics across some common criteria, such as country or event name. For a guide to constructing requests & understanding responses, see [Creating a Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/basics`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_run_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_run_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunReportResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_run_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunReportArgs {
    /// Path parameter: property
    pub property: String,
    /// Request body.
    pub body: RunReportRequest,
}

/// GET v1beta/properties/{propertiesId}:runReport
/// Returns a customized report of your Google Analytics event data. Reports contain statistics derived from data collected by the Google Analytics tracking code. The data returned from the API is as a table with columns for the requested dimensions and metrics. Metrics are individual measurements of user activity on your property, such as active users or event count. Dimensions break down metrics across some common criteria, such as country or event name. For a guide to constructing requests & understanding responses, see [Creating a Report](<https://developers.google.`com/analytics/devguides/reporting/data/v1/basics`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_run_report_builder()` + `analyticsdata_properties_run_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_run_report(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesRunReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunReportResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsdata_properties_run_report_builder(client, &args.property, &args.body)?;
    analyticsdata_properties_run_report_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Creates an audience export for later retrieval. This method quickly returns the audience export's resource name and initiates a long running asynchronous request to form an audience export. To export the users in an audience export, first create the audience export through this method and then send the audience resource name to the QueryAudienceExport method. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. An audience export is a snapshot of the users currently in the audience at the time of audience export creation. Creating audience exports for one audience on different days will return different results as users enter and exit the audience. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience exports contain the users in each audience. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_audience_exports_create_execute()` to send, or `analyticsdata_properties_audience_exports_create` for simplest API.

pub fn analyticsdata_properties_audience_exports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AudienceExport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}/audienceExports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Creates an audience export for later retrieval. This method quickly returns the audience export's resource name and initiates a long running asynchronous request to form an audience export. To export the users in an audience export, first create the audience export through this method and then send the audience resource name to the QueryAudienceExport method. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. An audience export is a snapshot of the users currently in the audience at the time of audience export creation. Creating audience exports for one audience on different days will return different results as users enter and exit the audience. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience exports contain the users in each audience. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_audience_exports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_audience_exports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_audience_exports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesAudienceExportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AudienceExport,
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Creates an audience export for later retrieval. This method quickly returns the audience export's resource name and initiates a long running asynchronous request to form an audience export. To export the users in an audience export, first create the audience export through this method and then send the audience resource name to the QueryAudienceExport method. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. An audience export is a snapshot of the users currently in the audience at the time of audience export creation. Creating audience exports for one audience on different days will return different results as users enter and exit the audience. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience exports contain the users in each audience. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_audience_exports_create_builder()` + `analyticsdata_properties_audience_exports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_create(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesAudienceExportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_audience_exports_create_builder(client, &args.parent, &args.body)?;
    analyticsdata_properties_audience_exports_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}
/// Gets configuration metadata about a specific audience export. This method can be used to understand an audience export after it has been created. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_audience_exports_get_execute()` to send, or `analyticsdata_properties_audience_exports_get` for simplest API.

pub fn analyticsdata_properties_audience_exports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}/audienceExports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}
/// Gets configuration metadata about a specific audience export. This method can be used to understand an audience export after it has been created. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_audience_exports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_audience_exports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AudienceExport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AudienceExport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_audience_exports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesAudienceExportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}
/// Gets configuration metadata about a specific audience export. This method can be used to understand an audience export after it has been created. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_audience_exports_get_builder()` + `analyticsdata_properties_audience_exports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_get(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesAudienceExportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AudienceExport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsdata_properties_audience_exports_get_builder(client, &args.name)?;
    analyticsdata_properties_audience_exports_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Lists all audience exports for a property. This method can be used for you to find and reuse existing audience exports rather than creating unnecessary new audience exports. The same audience can have multiple audience exports that represent the export of users that were in an audience on different days. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_audience_exports_list_execute()` to send, or `analyticsdata_properties_audience_exports_list` for simplest API.

pub fn analyticsdata_properties_audience_exports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}/audienceExports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Lists all audience exports for a property. This method can be used for you to find and reuse existing audience exports rather than creating unnecessary new audience exports. The same audience can have multiple audience exports that represent the export of users that were in an audience on different days. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_audience_exports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_audience_exports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAudienceExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAudienceExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_audience_exports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesAudienceExportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta/properties/{propertiesId}/audienceExports
/// Lists all audience exports for a property. This method can be used for you to find and reuse existing audience exports rather than creating unnecessary new audience exports. The same audience can have multiple audience exports that represent the export of users that were in an audience on different days. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_audience_exports_list_builder()` + `analyticsdata_properties_audience_exports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_list(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesAudienceExportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAudienceExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsdata_properties_audience_exports_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    analyticsdata_properties_audience_exports_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}:query
/// Retrieves an audience export of users. After creating an audience, the users are not immediately available for exporting. First, a request to CreateAudienceExport is necessary to create an audience export of users, and then second, this method is used to retrieve the users in the audience export. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsdata_properties_audience_exports_query_execute()` to send, or `analyticsdata_properties_audience_exports_query` for simplest API.

pub fn analyticsdata_properties_audience_exports_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &QueryAudienceExportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{}/audienceExports/{}:query",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}:query
/// Retrieves an audience export of users. After creating an audience, the users are not immediately available for exporting. First, a request to CreateAudienceExport is necessary to create an audience export of users, and then second, this method is used to retrieve the users in the audience export. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsdata_properties_audience_exports_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsdata_properties_audience_exports_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryAudienceExportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryAudienceExportResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`analyticsdata_properties_audience_exports_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesAudienceExportsQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: QueryAudienceExportRequest,
}

/// GET v1beta/properties/{propertiesId}/audienceExports/{audienceExportsId}:query
/// Retrieves an audience export of users. After creating an audience, the users are not immediately available for exporting. First, a request to CreateAudienceExport is necessary to create an audience export of users, and then second, this method is used to retrieve the users in the audience export. See [Creating an Audience Export](<https://developers.google.`com/analytics/devguides/reporting/data/v1/audience-list-basics`>) for an introduction to Audience Exports with examples. Audiences in Google Analytics 4 allow you to segment your users in the ways that are important to your business. To learn more, see <https://support.google.`com/analytics/answer/9267572`.> Audience Export APIs have some methods at alpha and other methods at beta stability. The intention is to advance methods to beta stability after some feedback and adoption. To give your feedback on this API, complete the [Google Analytics Audience Export API Feedback](<https://forms.`gle/EeA5u5LW6PEggtCEA`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsdata_properties_audience_exports_query_builder()` + `analyticsdata_properties_audience_exports_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsdata_properties_audience_exports_query(
    client: &SimpleHttpClient,
    args: &AnalyticsdataPropertiesAudienceExportsQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<QueryAudienceExportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsdata_properties_audience_exports_query_builder(client, &args.name, &args.body)?;
    analyticsdata_properties_audience_exports_query_execute(builder)
}
