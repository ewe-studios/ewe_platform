//! Auto-generated API clients for gcp/vmmigration.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_get_execute()` to send, or `vmmigration_projects_locations_get` for simplest API.

pub fn vmmigration_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_get_builder()` + `vmmigration_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_get_builder(client, name)?;
    vmmigration_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_list_execute()` to send, or `vmmigration_projects_locations_list` for simplest API.

pub fn vmmigration_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_list_builder()` + `vmmigration_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    vmmigration_projects_locations_list_execute(builder)
}

/// GET v1/{+group}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_add_group_migration_execute()` to send, or `vmmigration_projects_locations_groups_add_group_migration` for simplest API.

pub fn vmmigration_projects_locations_groups_add_group_migration_builder(
    client: &SimpleHttpClient,
    group: &str,
    body: &AddGroupMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+group}:addGroupMigration",
        group,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+group}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_add_group_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_add_group_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_add_group_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+group}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_add_group_migration_builder()` + `vmmigration_projects_locations_groups_add_group_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_add_group_migration(
    client: &SimpleHttpClient,
    group: &str,
    body: &AddGroupMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_groups_add_group_migration_builder(client, group, body)?;
    vmmigration_projects_locations_groups_add_group_migration_execute(builder)
}

/// GET v1/{+parent}/groups
/// Creates a new Group in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_create_execute()` to send, or `vmmigration_projects_locations_groups_create` for simplest API.

pub fn vmmigration_projects_locations_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    groupId: Option<&str>,
    requestId: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/groups", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupId",
            groupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/groups
/// Creates a new Group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/groups
/// Creates a new Group in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_create_builder()` + `vmmigration_projects_locations_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_create(
    client: &SimpleHttpClient,
    parent: &str,
    groupId: Option<&str>,
    requestId: Option<&str>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_create_builder(
        client, parent, groupId, requestId, body,
    )?;
    vmmigration_projects_locations_groups_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_delete_execute()` to send, or `vmmigration_projects_locations_groups_delete` for simplest API.

pub fn vmmigration_projects_locations_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_delete_builder()` + `vmmigration_projects_locations_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_delete_builder(client, name, requestId)?;
    vmmigration_projects_locations_groups_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_get_execute()` to send, or `vmmigration_projects_locations_groups_get` for simplest API.

pub fn vmmigration_projects_locations_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_get_builder()` + `vmmigration_projects_locations_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_get_builder(client, name)?;
    vmmigration_projects_locations_groups_get_execute(builder)
}

/// GET v1/{+parent}/groups
/// Lists Groups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_list_execute()` to send, or `vmmigration_projects_locations_groups_list` for simplest API.

pub fn vmmigration_projects_locations_groups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/groups", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/groups
/// Lists Groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/groups
/// Lists Groups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_list_builder()` + `vmmigration_projects_locations_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_groups_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_patch_execute()` to send, or `vmmigration_projects_locations_groups_patch` for simplest API.

pub fn vmmigration_projects_locations_groups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Group,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_patch_builder()` + `vmmigration_projects_locations_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Group,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    vmmigration_projects_locations_groups_patch_execute(builder)
}

/// GET v1/{+group}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_remove_group_migration_execute()` to send, or `vmmigration_projects_locations_groups_remove_group_migration` for simplest API.

pub fn vmmigration_projects_locations_groups_remove_group_migration_builder(
    client: &SimpleHttpClient,
    group: &str,
    body: &RemoveGroupMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+group}:removeGroupMigration",
        group,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+group}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_groups_remove_group_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_groups_remove_group_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_remove_group_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+group}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_remove_group_migration_builder()` + `vmmigration_projects_locations_groups_remove_group_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_remove_group_migration(
    client: &SimpleHttpClient,
    group: &str,
    body: &RemoveGroupMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_groups_remove_group_migration_builder(client, group, body)?;
    vmmigration_projects_locations_groups_remove_group_migration_execute(builder)
}

/// GET v1/{+parent}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_create_execute()` to send, or `vmmigration_projects_locations_image_imports_create` for simplest API.

pub fn vmmigration_projects_locations_image_imports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    imageImportId: Option<&str>,
    requestId: Option<&str>,
    body: &ImageImport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/imageImports", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "imageImportId",
            imageImportId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_create_builder()` + `vmmigration_projects_locations_image_imports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_create(
    client: &SimpleHttpClient,
    parent: &str,
    imageImportId: Option<&str>,
    requestId: Option<&str>,
    body: &ImageImport,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_create_builder(
        client,
        parent,
        imageImportId,
        requestId,
        body,
    )?;
    vmmigration_projects_locations_image_imports_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single ImageImport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_delete_execute()` to send, or `vmmigration_projects_locations_image_imports_delete` for simplest API.

pub fn vmmigration_projects_locations_image_imports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single ImageImport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_delete_builder()` + `vmmigration_projects_locations_image_imports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_image_imports_delete_builder(client, name, requestId)?;
    vmmigration_projects_locations_image_imports_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single ImageImport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_get_execute()` to send, or `vmmigration_projects_locations_image_imports_get` for simplest API.

pub fn vmmigration_projects_locations_image_imports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageImport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ImageImport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_get_builder()` + `vmmigration_projects_locations_image_imports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_get_builder(client, name)?;
    vmmigration_projects_locations_image_imports_get_execute(builder)
}

/// GET v1/{+parent}/imageImports
/// Lists ImageImports in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_list_execute()` to send, or `vmmigration_projects_locations_image_imports_list` for simplest API.

pub fn vmmigration_projects_locations_image_imports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/imageImports", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/imageImports
/// Lists ImageImports in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageImportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImageImportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/imageImports
/// Lists ImageImports in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_list_builder()` + `vmmigration_projects_locations_image_imports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageImportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_image_imports_list_execute(builder)
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelImageImportJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelImageImportJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder(
        client, name, body,
    )?;
    vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single ImageImportJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_get_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageImportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ImageImportJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_image_imports_image_import_jobs_get_builder(client, name)?;
    vmmigration_projects_locations_image_imports_image_import_jobs_get_execute(builder)
}

/// GET v1/{+parent}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_list_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/imageImportJobs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImageImportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImageImportJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImageImportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_image_import_jobs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_image_imports_image_import_jobs_list_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_cancel_execute()` to send, or `vmmigration_projects_locations_operations_cancel` for simplest API.

pub fn vmmigration_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_cancel_builder()` + `vmmigration_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_cancel_builder(client, name, body)?;
    vmmigration_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_delete_execute()` to send, or `vmmigration_projects_locations_operations_delete` for simplest API.

pub fn vmmigration_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_delete_builder()` + `vmmigration_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_delete_builder(client, name)?;
    vmmigration_projects_locations_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_get_execute()` to send, or `vmmigration_projects_locations_operations_get` for simplest API.

pub fn vmmigration_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_get_builder()` + `vmmigration_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_get_builder(client, name)?;
    vmmigration_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_list_execute()` to send, or `vmmigration_projects_locations_operations_list` for simplest API.

pub fn vmmigration_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_list_builder()` + `vmmigration_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    vmmigration_projects_locations_operations_list_execute(builder)
}

/// GET v1/{+parent}/sources
/// Creates a new Source in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_create_execute()` to send, or `vmmigration_projects_locations_sources_create` for simplest API.

pub fn vmmigration_projects_locations_sources_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sourceId: Option<&str>,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sources", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceId",
            sourceId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sources
/// Creates a new Source in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sources
/// Creates a new Source in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_create_builder()` + `vmmigration_projects_locations_sources_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sourceId: Option<&str>,
    body: &Source,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_create_builder(
        client, parent, requestId, sourceId, body,
    )?;
    vmmigration_projects_locations_sources_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_delete_execute()` to send, or `vmmigration_projects_locations_sources_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_delete_builder()` + `vmmigration_projects_locations_sources_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_delete_builder(client, name, requestId)?;
    vmmigration_projects_locations_sources_delete_execute(builder)
}

/// GET v1/{+source}:fetchInventory
/// List remote source's inventory of VMs. The remote source is the onprem vCenter (remote in the sense it's not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_fetch_inventory_execute()` to send, or `vmmigration_projects_locations_sources_fetch_inventory` for simplest API.

pub fn vmmigration_projects_locations_sources_fetch_inventory_builder(
    client: &SimpleHttpClient,
    source: &str,
    forceRefresh: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+source}:fetchInventory", source,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "forceRefresh",
            forceRefresh
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+source}:fetchInventory
/// List remote source's inventory of VMs. The remote source is the onprem vCenter (remote in the sense it's not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_fetch_inventory_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_fetch_inventory_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchInventoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+source}:fetchInventory
/// List remote source's inventory of VMs. The remote source is the onprem vCenter (remote in the sense it's not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_fetch_inventory_builder()` + `vmmigration_projects_locations_sources_fetch_inventory_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_inventory(
    client: &SimpleHttpClient,
    source: &str,
    forceRefresh: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_fetch_inventory_builder(
        client,
        source,
        forceRefresh,
        pageSize,
        pageToken,
    )?;
    vmmigration_projects_locations_sources_fetch_inventory_execute(builder)
}

/// GET v1/{+source}:fetchStorageInventory
/// List remote source's inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_fetch_storage_inventory_execute()` to send, or `vmmigration_projects_locations_sources_fetch_storage_inventory` for simplest API.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory_builder(
    client: &SimpleHttpClient,
    source: &str,
    forceRefresh: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+source}:fetchStorageInventory",
        source,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "forceRefresh",
            forceRefresh
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+source}:fetchStorageInventory
/// List remote source's inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_fetch_storage_inventory_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchStorageInventoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchStorageInventoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+source}:fetchStorageInventory
/// List remote source's inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()` + `vmmigration_projects_locations_sources_fetch_storage_inventory_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory(
    client: &SimpleHttpClient,
    source: &str,
    forceRefresh: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchStorageInventoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_fetch_storage_inventory_builder(
        client,
        source,
        forceRefresh,
        pageSize,
        pageToken,
        type_rs,
    )?;
    vmmigration_projects_locations_sources_fetch_storage_inventory_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_get_execute()` to send, or `vmmigration_projects_locations_sources_get` for simplest API.

pub fn vmmigration_projects_locations_sources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_get_builder()` + `vmmigration_projects_locations_sources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_get_builder(client, name)?;
    vmmigration_projects_locations_sources_get_execute(builder)
}

/// GET v1/{+parent}/sources
/// Lists Sources in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_list_execute()` to send, or `vmmigration_projects_locations_sources_list` for simplest API.

pub fn vmmigration_projects_locations_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sources", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sources
/// Lists Sources in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sources
/// Lists Sources in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_list_builder()` + `vmmigration_projects_locations_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_sources_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_patch_execute()` to send, or `vmmigration_projects_locations_sources_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Source,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_patch_builder()` + `vmmigration_projects_locations_sources_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Source,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    vmmigration_projects_locations_sources_patch_execute(builder)
}

/// GET v1/{+parent}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_create_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_create` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    datacenterConnectorId: Option<&str>,
    requestId: Option<&str>,
    body: &DatacenterConnector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/datacenterConnectors",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "datacenterConnectorId",
            datacenterConnectorId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_datacenter_connectors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create(
    client: &SimpleHttpClient,
    parent: &str,
    datacenterConnectorId: Option<&str>,
    requestId: Option<&str>,
    body: &DatacenterConnector,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_create_builder(
        client,
        parent,
        datacenterConnectorId,
        requestId,
        body,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single DatacenterConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_delete_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single DatacenterConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_delete_builder(
        client, name, requestId,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single DatacenterConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_get_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_get` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_datacenter_connectors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatacenterConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatacenterConnector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DatacenterConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatacenterConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_datacenter_connectors_get_builder(client, name)?;
    vmmigration_projects_locations_sources_datacenter_connectors_get_execute(builder)
}

/// GET v1/{+parent}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_list_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_list` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/datacenterConnectors",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_datacenter_connectors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatacenterConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatacenterConnectorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatacenterConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_list_execute(builder)
}

/// GET v1/{+datacenterConnector}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder(
    client: &SimpleHttpClient,
    datacenterConnector: &str,
    body: &UpgradeApplianceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+datacenterConnector}:upgradeAppliance",
        datacenterConnector,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+datacenterConnector}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+datacenterConnector}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance(
    client: &SimpleHttpClient,
    datacenterConnector: &str,
    body: &UpgradeApplianceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder(
            client,
            datacenterConnector,
            body,
        )?;
    vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute(builder)
}

/// GET v1/{+name}:cancel
/// Cancels the disk migration job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelDiskMigrationJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Cancels the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Cancels the disk migration job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelDiskMigrationJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder(
        client, name, body,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute(builder)
}

/// GET v1/{+parent}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    diskMigrationJobId: Option<&str>,
    requestId: Option<&str>,
    body: &DiskMigrationJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/diskMigrationJobs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "diskMigrationJobId",
            diskMigrationJobId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    diskMigrationJobId: Option<&str>,
    requestId: Option<&str>,
    body: &DiskMigrationJob,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_create_builder(
        client,
        parent,
        diskMigrationJobId,
        requestId,
        body,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder(client, name)?;
    vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskMigrationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskMigrationJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskMigrationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_disk_migration_jobs_get_builder(client, name)?;
    vmmigration_projects_locations_sources_disk_migration_jobs_get_execute(builder)
}

/// GET v1/{+parent}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/diskMigrationJobs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiskMigrationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDiskMigrationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiskMigrationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &DiskMigrationJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &DiskMigrationJob,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute(builder)
}

/// GET v1/{+name}:run
/// Runs the disk migration job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_run_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_run` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunDiskMigrationJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:run", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:run
/// Runs the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:run
/// Runs the disk migration job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run(
    client: &SimpleHttpClient,
    name: &str,
    body: &RunDiskMigrationJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_disk_migration_jobs_run_builder(client, name, body)?;
    vmmigration_projects_locations_sources_disk_migration_jobs_run_execute(builder)
}

/// GET v1/{+parent}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    migratingVmId: Option<&str>,
    requestId: Option<&str>,
    body: &MigratingVm,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/migratingVms", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "migratingVmId",
            migratingVmId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_create(
    client: &SimpleHttpClient,
    parent: &str,
    migratingVmId: Option<&str>,
    requestId: Option<&str>,
    body: &MigratingVm,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_create_builder(
        client,
        parent,
        migratingVmId,
        requestId,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_delete_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_delete_builder()` + `vmmigration_projects_locations_sources_migrating_vms_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_delete_builder(client, name)?;
    vmmigration_projects_locations_sources_migrating_vms_delete_execute(builder)
}

/// GET v1/{+migratingVm}:extendMigration
/// Extend the migrating VM time to live.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_extend_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &ExtendMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+migratingVm}:extendMigration",
        migratingVm,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+migratingVm}:extendMigration
/// Extend the migrating VM time to live.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+migratingVm}:extendMigration
/// Extend the migrating VM time to live.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &ExtendMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder(
        client,
        migratingVm,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute(builder)
}

/// GET v1/{+migratingVm}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_finalize_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &FinalizeMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+migratingVm}:finalizeMigration",
        migratingVm,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+migratingVm}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+migratingVm}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &FinalizeMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder(
        client,
        migratingVm,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Gets details of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigratingVm>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigratingVm = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigratingVm>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_get_builder(client, name, view)?;
    vmmigration_projects_locations_sources_migrating_vms_get_execute(builder)
}

/// GET v1/{+parent}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/migratingVms", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMigratingVmsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMigratingVmsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMigratingVmsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken, view,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_patch_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &MigratingVm,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_patch_builder()` + `vmmigration_projects_locations_sources_migrating_vms_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &MigratingVm,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_patch_execute(builder)
}

/// GET v1/{+migratingVm}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_pause_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &PauseMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+migratingVm}:pauseMigration",
        migratingVm,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+migratingVm}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+migratingVm}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &PauseMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder(
        client,
        migratingVm,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute(builder)
}

/// GET v1/{+migratingVm}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_resume_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &ResumeMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+migratingVm}:resumeMigration",
        migratingVm,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+migratingVm}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+migratingVm}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &ResumeMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder(
        client,
        migratingVm,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute(builder)
}

/// GET v1/{+migratingVm}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_start_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_start_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration_builder(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &StartMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+migratingVm}:startMigration",
        migratingVm,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+migratingVm}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+migratingVm}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_start_migration_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration(
    client: &SimpleHttpClient,
    migratingVm: &str,
    body: &StartMigrationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_start_migration_builder(
        client,
        migratingVm,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_start_migration_execute(builder)
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelCloneJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelCloneJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder(
        client, name, body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute(builder)
}

/// GET v1/{+parent}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cloneJobId: Option<&str>,
    requestId: Option<&str>,
    body: &CloneJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cloneJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cloneJobId",
            cloneJobId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    cloneJobId: Option<&str>,
    requestId: Option<&str>,
    body: &CloneJob,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder(
        client, parent, cloneJobId, requestId, body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single CloneJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CloneJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloneJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloneJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CloneJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloneJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder(client, name)?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute(builder)
}

/// GET v1/{+parent}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cloneJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCloneJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCloneJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCloneJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute(builder)
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelCutoverJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelCutoverJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder(
        client, name, body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute(builder)
}

/// GET v1/{+parent}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cutoverJobId: Option<&str>,
    requestId: Option<&str>,
    body: &CutoverJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cutoverJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cutoverJobId",
            cutoverJobId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    cutoverJobId: Option<&str>,
    requestId: Option<&str>,
    body: &CutoverJob,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder(
        client,
        parent,
        cutoverJobId,
        requestId,
        body,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single CutoverJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CutoverJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CutoverJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CutoverJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single CutoverJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CutoverJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder(
        client, name,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute(builder)
}

/// GET v1/{+parent}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cutoverJobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCutoverJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCutoverJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCutoverJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single ReplicationCycle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ReplicationCycle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplicationCycle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplicationCycle = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single ReplicationCycle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplicationCycle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder(
            client, name,
        )?;
    vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute(builder)
}

/// GET v1/{+parent}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/replicationCycles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReplicationCyclesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReplicationCyclesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReplicationCyclesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder(
            client, parent, filter, orderBy, pageSize, pageToken,
        )?;
    vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute(builder)
}

/// GET v1/{+parent}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_create_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_create` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    utilizationReportId: Option<&str>,
    body: &UtilizationReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/utilizationReports",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "utilizationReportId",
            utilizationReportId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_utilization_reports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_create_builder()` + `vmmigration_projects_locations_sources_utilization_reports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    utilizationReportId: Option<&str>,
    body: &UtilizationReport,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_create_builder(
        client,
        parent,
        requestId,
        utilizationReportId,
        body,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single Utilization Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_delete_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_utilization_reports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single Utilization Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_delete_builder()` + `vmmigration_projects_locations_sources_utilization_reports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_delete_builder(
        client, name, requestId,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a single Utilization Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_get_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_get` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Gets a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_utilization_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UtilizationReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UtilizationReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a single Utilization Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_get_builder()` + `vmmigration_projects_locations_sources_utilization_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UtilizationReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_utilization_reports_get_builder(client, name, view)?;
    vmmigration_projects_locations_sources_utilization_reports_get_execute(builder)
}

/// GET v1/{+parent}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_list_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_list` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/utilizationReports",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_sources_utilization_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUtilizationReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUtilizationReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_list_builder()` + `vmmigration_projects_locations_sources_utilization_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUtilizationReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken, view,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_list_execute(builder)
}

/// GET v1/{+parent}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_create_execute()` to send, or `vmmigration_projects_locations_target_projects_create` for simplest API.

pub fn vmmigration_projects_locations_target_projects_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    targetProjectId: Option<&str>,
    body: &TargetProject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/targetProjects", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "targetProjectId",
            targetProjectId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_target_projects_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_target_projects_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_create_builder()` + `vmmigration_projects_locations_target_projects_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    targetProjectId: Option<&str>,
    body: &TargetProject,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_create_builder(
        client,
        parent,
        requestId,
        targetProjectId,
        body,
    )?;
    vmmigration_projects_locations_target_projects_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_delete_execute()` to send, or `vmmigration_projects_locations_target_projects_delete` for simplest API.

pub fn vmmigration_projects_locations_target_projects_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_target_projects_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_target_projects_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_delete_builder()` + `vmmigration_projects_locations_target_projects_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_target_projects_delete_builder(client, name, requestId)?;
    vmmigration_projects_locations_target_projects_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_get_execute()` to send, or `vmmigration_projects_locations_target_projects_get` for simplest API.

pub fn vmmigration_projects_locations_target_projects_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_target_projects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_target_projects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetProject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_get_builder()` + `vmmigration_projects_locations_target_projects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_get_builder(client, name)?;
    vmmigration_projects_locations_target_projects_get_execute(builder)
}

/// GET v1/{+parent}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_list_execute()` to send, or `vmmigration_projects_locations_target_projects_list` for simplest API.

pub fn vmmigration_projects_locations_target_projects_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/targetProjects", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_target_projects_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_target_projects_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTargetProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_list_builder()` + `vmmigration_projects_locations_target_projects_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    vmmigration_projects_locations_target_projects_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_patch_execute()` to send, or `vmmigration_projects_locations_target_projects_patch` for simplest API.

pub fn vmmigration_projects_locations_target_projects_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &TargetProject,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `vmmigration_projects_locations_target_projects_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from vmmigration_projects_locations_target_projects_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_patch_builder()` + `vmmigration_projects_locations_target_projects_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &TargetProject,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    vmmigration_projects_locations_target_projects_patch_execute(builder)
}
