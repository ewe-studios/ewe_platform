//! Auto-generated API clients for gcp/vmmigration.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_get_execute()` to send, or `vmmigration_projects_locations_get` for simplest API.

pub fn vmmigration_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_get_execute()` or `vmmigration_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_get_builder()` + `vmmigration_projects_locations_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_get_builder(client, &args.name)?;
    vmmigration_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_list_execute()` to send, or `vmmigration_projects_locations_list` for simplest API.

pub fn vmmigration_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_list_execute()` or `vmmigration_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_list_builder()` + `vmmigration_projects_locations_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_add_group_migration_execute()` to send, or `vmmigration_projects_locations_groups_add_group_migration` for simplest API.

pub fn vmmigration_projects_locations_groups_add_group_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    group: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups/{groupsId}:addGroupMigration",
        group,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_add_group_migration_execute()` or `vmmigration_projects_locations_groups_add_group_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_add_group_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_add_group_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_add_group_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_add_group_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_add_group_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_add_group_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_add_group_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_add_group_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_add_group_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsAddGroupMigrationArgs {
    /// Path parameter: group
    pub group: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:addGroupMigration
/// Adds a MigratingVm to a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_add_group_migration_builder()` + `vmmigration_projects_locations_groups_add_group_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_add_group_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_add_group_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsAddGroupMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_groups_add_group_migration_builder(client, &args.group)?;
    vmmigration_projects_locations_groups_add_group_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new Group in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_create_execute()` to send, or `vmmigration_projects_locations_groups_create` for simplest API.

pub fn vmmigration_projects_locations_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    groupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupId.as_ref() {
        query_parts.push(format!("groupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new Group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_create_execute()` or `vmmigration_projects_locations_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new Group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: groupId
    pub groupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups
/// Creates a new Group in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_create_builder()` + `vmmigration_projects_locations_groups_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_create_builder(
        client,
        &args.parent,
        &args.groupId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_delete_execute()` to send, or `vmmigration_projects_locations_groups_delete` for simplest API.

pub fn vmmigration_projects_locations_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups/{groupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a single Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_delete_execute()` or `vmmigration_projects_locations_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Deletes a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_delete_builder()` + `vmmigration_projects_locations_groups_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_groups_delete_builder(client, &args.name, &args.requestId)?;
    vmmigration_projects_locations_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets details of a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_get_execute()` to send, or `vmmigration_projects_locations_groups_get` for simplest API.

pub fn vmmigration_projects_locations_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups/{groupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets details of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_get_execute()` or `vmmigration_projects_locations_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets details of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Gets details of a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_get_builder()` + `vmmigration_projects_locations_groups_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_get_builder(client, &args.name)?;
    vmmigration_projects_locations_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists Groups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_list_execute()` to send, or `vmmigration_projects_locations_groups_list` for simplest API.

pub fn vmmigration_projects_locations_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists Groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_list_execute()` or `vmmigration_projects_locations_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists Groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groups
/// Lists Groups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_list_builder()` + `vmmigration_projects_locations_groups_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a single Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_patch_execute()` to send, or `vmmigration_projects_locations_groups_patch` for simplest API.

pub fn vmmigration_projects_locations_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups/{groupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_patch_execute()` or `vmmigration_projects_locations_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a single Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_patch_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}
/// Updates the parameters of a single Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_patch_builder()` + `vmmigration_projects_locations_groups_patch_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_patch(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    vmmigration_projects_locations_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_groups_remove_group_migration_execute()` to send, or `vmmigration_projects_locations_groups_remove_group_migration` for simplest API.

pub fn vmmigration_projects_locations_groups_remove_group_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    group: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/groups/{groupsId}:removeGroupMigration",
        group,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_groups_remove_group_migration_execute()` or `vmmigration_projects_locations_groups_remove_group_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_remove_group_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_remove_group_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_groups_remove_group_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_groups_remove_group_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_groups_remove_group_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_groups_remove_group_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_groups_remove_group_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_groups_remove_group_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_groups_remove_group_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsGroupsRemoveGroupMigrationArgs {
    /// Path parameter: group
    pub group: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/groups/{groupsId}:removeGroupMigration
/// Removes a MigratingVm from a Group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_groups_remove_group_migration_builder()` + `vmmigration_projects_locations_groups_remove_group_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_groups_remove_group_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_groups_remove_group_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsGroupsRemoveGroupMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_groups_remove_group_migration_builder(client, &args.group)?;
    vmmigration_projects_locations_groups_remove_group_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_create_execute()` to send, or `vmmigration_projects_locations_image_imports_create` for simplest API.

pub fn vmmigration_projects_locations_image_imports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    imageImportId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = imageImportId.as_ref() {
        query_parts.push(format!("imageImportId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_create_execute()` or `vmmigration_projects_locations_image_imports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: imageImportId
    pub imageImportId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Creates a new ImageImport in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_create_builder()` + `vmmigration_projects_locations_image_imports_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_create_builder(
        client,
        &args.parent,
        &args.imageImportId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_image_imports_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Deletes a single ImageImport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_delete_execute()` to send, or `vmmigration_projects_locations_image_imports_delete` for simplest API.

pub fn vmmigration_projects_locations_image_imports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports/{imageImportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Deletes a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_delete_execute()` or `vmmigration_projects_locations_image_imports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Deletes a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Deletes a single ImageImport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_delete_builder()` + `vmmigration_projects_locations_image_imports_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    vmmigration_projects_locations_image_imports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Gets details of a single ImageImport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_get_execute()` to send, or `vmmigration_projects_locations_image_imports_get` for simplest API.

pub fn vmmigration_projects_locations_image_imports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports/{imageImportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Gets details of a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_get_execute()` or `vmmigration_projects_locations_image_imports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageImport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageImport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Gets details of a single ImageImport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}
/// Gets details of a single ImageImport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_get_builder()` + `vmmigration_projects_locations_image_imports_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_get_builder(client, &args.name)?;
    vmmigration_projects_locations_image_imports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Lists ImageImports in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_list_execute()` to send, or `vmmigration_projects_locations_image_imports_list` for simplest API.

pub fn vmmigration_projects_locations_image_imports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Lists ImageImports in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_list_execute()` or `vmmigration_projects_locations_image_imports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListImageImportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImageImportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Lists ImageImports in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageImportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports
/// Lists ImageImports in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_list_builder()` + `vmmigration_projects_locations_image_imports_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageImportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_image_imports_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute()` or `vmmigration_projects_locations_image_imports_image_import_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_image_import_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_image_import_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsImageImportJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}:cancel
/// Initiates the cancellation of a running ImageImportJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_cancel(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsImageImportJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_image_import_jobs_cancel_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_image_imports_image_import_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}
/// Gets details of a single ImageImportJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_get_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}
/// Gets details of a single ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_execute()` or `vmmigration_projects_locations_image_imports_image_import_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageImportJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageImportJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}
/// Gets details of a single ImageImportJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_image_import_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_image_import_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_image_import_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsImageImportJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs/{imageImportJobsId}
/// Gets details of a single ImageImportJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsImageImportJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageImportJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_image_import_jobs_get_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_image_imports_image_import_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_image_imports_image_import_jobs_list_execute()` to send, or `vmmigration_projects_locations_image_imports_image_import_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_execute()` or `vmmigration_projects_locations_image_imports_image_import_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListImageImportJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImageImportJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_image_imports_image_import_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImageImportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_image_imports_image_import_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_image_imports_image_import_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsImageImportsImageImportJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageImports/{imageImportsId}/imageImportJobs
/// Lists ImageImportJobs in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_builder()` + `vmmigration_projects_locations_image_imports_image_import_jobs_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_image_imports_image_import_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_image_imports_image_import_jobs_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsImageImportsImageImportJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListImageImportJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_image_imports_image_import_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_image_imports_image_import_jobs_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_cancel_execute()` to send, or `vmmigration_projects_locations_operations_cancel` for simplest API.

pub fn vmmigration_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_operations_cancel_execute()` or `vmmigration_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `vmmigration_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_cancel_builder()` + `vmmigration_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `vmmigration_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_cancel_builder(client, &args.name)?;
    vmmigration_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_delete_execute()` to send, or `vmmigration_projects_locations_operations_delete` for simplest API.

pub fn vmmigration_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_operations_delete_execute()` or `vmmigration_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_operations_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_delete_builder()` + `vmmigration_projects_locations_operations_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_delete_builder(client, &args.name)?;
    vmmigration_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_get_execute()` to send, or `vmmigration_projects_locations_operations_get` for simplest API.

pub fn vmmigration_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_operations_get_execute()` or `vmmigration_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_operations_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_get_builder()` + `vmmigration_projects_locations_operations_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_get_builder(client, &args.name)?;
    vmmigration_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_operations_list_execute()` to send, or `vmmigration_projects_locations_operations_list` for simplest API.

pub fn vmmigration_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_operations_list_execute()` or `vmmigration_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_operations_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_operations_list_builder()` + `vmmigration_projects_locations_operations_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    vmmigration_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new Source in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_create_execute()` to send, or `vmmigration_projects_locations_sources_create` for simplest API.

pub fn vmmigration_projects_locations_sources_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<Option<String>>,
    sourceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sourceId.as_ref() {
        query_parts.push(format!("sourceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new Source in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_create_execute()` or `vmmigration_projects_locations_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new Source in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: sourceId
    pub sourceId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources
/// Creates a new Source in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_create_builder()` + `vmmigration_projects_locations_sources_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.sourceId,
    )?;
    vmmigration_projects_locations_sources_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_delete_execute()` to send, or `vmmigration_projects_locations_sources_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a single Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_delete_execute()` or `vmmigration_projects_locations_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Deletes a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_delete_builder()` + `vmmigration_projects_locations_sources_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_delete_builder(client, &args.name, &args.requestId)?;
    vmmigration_projects_locations_sources_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchInventory
/// List remote source''s inventory of VMs. The remote source is the onprem vCenter (remote in the sense it''s not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_fetch_inventory_execute()` to send, or `vmmigration_projects_locations_sources_fetch_inventory` for simplest API.

pub fn vmmigration_projects_locations_sources_fetch_inventory_builder<R>(
    client: &SimpleHttpClient<R>,
    source: &String,
    forceRefresh: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}:fetchInventory",
        source,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = forceRefresh.as_ref() {
        query_parts.push(format!("forceRefresh={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchInventory
/// List remote source''s inventory of VMs. The remote source is the onprem vCenter (remote in the sense it''s not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_fetch_inventory_execute()` or `vmmigration_projects_locations_sources_fetch_inventory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_fetch_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_inventory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchInventoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchInventoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchInventory
/// List remote source''s inventory of VMs. The remote source is the onprem vCenter (remote in the sense it''s not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_fetch_inventory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_fetch_inventory_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_fetch_inventory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_fetch_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_fetch_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_fetch_inventory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_fetch_inventory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesFetchInventoryArgs {
    /// Path parameter: source
    pub source: String,
    /// Query parameter: forceRefresh
    pub forceRefresh: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchInventory
/// List remote source''s inventory of VMs. The remote source is the onprem vCenter (remote in the sense it''s not in Compute Engine). The inventory describes the list of existing VMs in that source. Note that this operation lists the VMs on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_fetch_inventory_builder()` + `vmmigration_projects_locations_sources_fetch_inventory_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_fetch_inventory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_inventory(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesFetchInventoryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchInventoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_fetch_inventory_builder(
        client,
        &args.source,
        &args.forceRefresh,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_fetch_inventory_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchStorageInventory
/// List remote source''s inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_fetch_storage_inventory_execute()` to send, or `vmmigration_projects_locations_sources_fetch_storage_inventory` for simplest API.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory_builder<R>(
    client: &SimpleHttpClient<R>,
    source: &String,
    forceRefresh: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    type_rs: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}:fetchStorageInventory",
        source,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = forceRefresh.as_ref() {
        query_parts.push(format!("forceRefresh={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchStorageInventory
/// List remote source''s inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_fetch_storage_inventory_execute()` or `vmmigration_projects_locations_sources_fetch_storage_inventory`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchStorageInventoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchStorageInventoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchStorageInventory
/// List remote source''s inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_fetch_storage_inventory_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_fetch_storage_inventory()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchStorageInventoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_fetch_storage_inventory_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_fetch_storage_inventory`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesFetchStorageInventoryArgs {
    /// Path parameter: source
    pub source: String,
    /// Query parameter: forceRefresh
    pub forceRefresh: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: type
    pub type_rs: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}:fetchStorageInventory
/// List remote source''s inventory of storage resources. The remote source is another cloud vendor (e.g. AWS, Azure). The inventory describes the list of existing storage resources in that source. Note that this operation lists the resources on the remote source, as opposed to listing the MigratingVms resources in the vmmigration service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_fetch_storage_inventory_builder()` + `vmmigration_projects_locations_sources_fetch_storage_inventory_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_fetch_storage_inventory_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_fetch_storage_inventory(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesFetchStorageInventoryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchStorageInventoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_fetch_storage_inventory_builder(
        client,
        &args.source,
        &args.forceRefresh,
        &args.pageSize,
        &args.pageToken,
        &args.type_rs,
    )?;
    vmmigration_projects_locations_sources_fetch_storage_inventory_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets details of a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_get_execute()` to send, or `vmmigration_projects_locations_sources_get` for simplest API.

pub fn vmmigration_projects_locations_sources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets details of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_get_execute()` or `vmmigration_projects_locations_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Source>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Source = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets details of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Gets details of a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_get_builder()` + `vmmigration_projects_locations_sources_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Source>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_get_builder(client, &args.name)?;
    vmmigration_projects_locations_sources_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists Sources in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_list_execute()` to send, or `vmmigration_projects_locations_sources_list` for simplest API.

pub fn vmmigration_projects_locations_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists Sources in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_list_execute()` or `vmmigration_projects_locations_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists Sources in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources
/// Lists Sources in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_list_builder()` + `vmmigration_projects_locations_sources_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a single Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_patch_execute()` to send, or `vmmigration_projects_locations_sources_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_patch_execute()` or `vmmigration_projects_locations_sources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a single Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_patch_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}
/// Updates the parameters of a single Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_patch_builder()` + `vmmigration_projects_locations_sources_patch_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_patch(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    vmmigration_projects_locations_sources_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_create_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_create` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    datacenterConnectorId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = datacenterConnectorId.as_ref() {
        query_parts.push(format!("datacenterConnectorId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_datacenter_connectors_create_execute()` or `vmmigration_projects_locations_sources_datacenter_connectors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_datacenter_connectors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_datacenter_connectors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_datacenter_connectors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDatacenterConnectorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: datacenterConnectorId
    pub datacenterConnectorId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Creates a new DatacenterConnector in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_create_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_create_builder(
        client,
        &args.parent,
        &args.datacenterConnectorId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Deletes a single DatacenterConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_delete_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Deletes a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_execute()` or `vmmigration_projects_locations_sources_datacenter_connectors_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Deletes a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_datacenter_connectors_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_datacenter_connectors_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_datacenter_connectors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDatacenterConnectorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Deletes a single DatacenterConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Gets details of a single DatacenterConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_get_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_get` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Gets details of a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_datacenter_connectors_get_execute()` or `vmmigration_projects_locations_sources_datacenter_connectors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatacenterConnector>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatacenterConnector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Gets details of a single DatacenterConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_datacenter_connectors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatacenterConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_datacenter_connectors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_datacenter_connectors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDatacenterConnectorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}
/// Gets details of a single DatacenterConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_get_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatacenterConnector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_get_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_list_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_list` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_datacenter_connectors_list_execute()` or `vmmigration_projects_locations_sources_datacenter_connectors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDatacenterConnectorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatacenterConnectorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_datacenter_connectors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatacenterConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_datacenter_connectors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_datacenter_connectors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDatacenterConnectorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors
/// Lists DatacenterConnectors in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_list_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDatacenterConnectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_datacenter_connectors_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_datacenter_connectors_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute()` to send, or `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance` for simplest API.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder<R>(
    client: &SimpleHttpClient<R>,
    datacenterConnector: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}:upgradeAppliance",
        datacenterConnector,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute()` or `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDatacenterConnectorsUpgradeApplianceArgs {
    /// Path parameter: datacenterConnector
    pub datacenterConnector: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/datacenterConnectors/{datacenterConnectorsId}:upgradeAppliance
/// Upgrades the appliance relate to this DatacenterConnector to the in-place updateable version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder()` + `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsUpgradeApplianceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_builder(
            client,
            &args.datacenterConnector,
        )?;
    vmmigration_projects_locations_sources_datacenter_connectors_upgrade_appliance_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:cancel
/// Cancels the disk migration job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:cancel
/// Cancels the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:cancel
/// Cancels the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:cancel
/// Cancels the disk migration job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_cancel(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_cancel_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_cancel_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    diskMigrationJobId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = diskMigrationJobId.as_ref() {
        query_parts.push(format!("diskMigrationJobId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: diskMigrationJobId
    pub diskMigrationJobId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Creates a new disk migration job in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_create_builder(
        client,
        &args.parent,
        &args.diskMigrationJobId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Deletes a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Deletes a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Deletes a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Deletes a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_delete_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Gets details of a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Gets details of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskMigrationJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskMigrationJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Gets details of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskMigrationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Gets details of a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskMigrationJob>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_disk_migration_jobs_get_builder(client, &args.name)?;
    vmmigration_projects_locations_sources_disk_migration_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDiskMigrationJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDiskMigrationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiskMigrationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs
/// Lists DiskMigrationJobs in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDiskMigrationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}
/// Updates the parameters of a single DiskMigrationJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_patch(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_disk_migration_jobs_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    vmmigration_projects_locations_sources_disk_migration_jobs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:run
/// Runs the disk migration job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_disk_migration_jobs_run_execute()` to send, or `vmmigration_projects_locations_sources_disk_migration_jobs_run` for simplest API.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:run",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:run
/// Runs the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_execute()` or `vmmigration_projects_locations_sources_disk_migration_jobs_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:run
/// Runs the disk migration job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_disk_migration_jobs_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_disk_migration_jobs_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_disk_migration_jobs_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesDiskMigrationJobsRunArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/diskMigrationJobs/{diskMigrationJobsId}:run
/// Runs the disk migration job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_builder()` + `vmmigration_projects_locations_sources_disk_migration_jobs_run_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_disk_migration_jobs_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_disk_migration_jobs_run(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_disk_migration_jobs_run_builder(client, &args.name)?;
    vmmigration_projects_locations_sources_disk_migration_jobs_run_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    migratingVmId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = migratingVmId.as_ref() {
        query_parts.push(format!("migratingVmId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_create_execute()` or `vmmigration_projects_locations_sources_migrating_vms_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: migratingVmId
    pub migratingVmId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Creates a new MigratingVm in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_create_builder(
        client,
        &args.parent,
        &args.migratingVmId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Deletes a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_delete_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Deletes a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_delete_execute()` or `vmmigration_projects_locations_sources_migrating_vms_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Deletes a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Deletes a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_delete_builder()` + `vmmigration_projects_locations_sources_migrating_vms_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_delete_builder(client, &args.name)?;
    vmmigration_projects_locations_sources_migrating_vms_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:extendMigration
/// Extend the migrating VM time to live.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_extend_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    migratingVm: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:extendMigration",
        migratingVm,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:extendMigration
/// Extend the migrating VM time to live.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute()` or `vmmigration_projects_locations_sources_migrating_vms_extend_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:extendMigration
/// Extend the migrating VM time to live.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_extend_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_extend_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsExtendMigrationArgs {
    /// Path parameter: migratingVm
    pub migratingVm: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:extendMigration
/// Extend the migrating VM time to live.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_extend_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_extend_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsExtendMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_extend_migration_builder(
        client,
        &args.migratingVm,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_extend_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_finalize_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    migratingVm: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:finalizeMigration",
        migratingVm,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute()` or `vmmigration_projects_locations_sources_migrating_vms_finalize_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_finalize_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_finalize_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsFinalizeMigrationArgs {
    /// Path parameter: migratingVm
    pub migratingVm: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:finalizeMigration
/// Marks a migration as completed, deleting migration resources that are no longer being used. Only applicable after cutover is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_finalize_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_finalize_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsFinalizeMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_finalize_migration_builder(
        client,
        &args.migratingVm,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_finalize_migration_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Gets details of a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Gets details of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_get_execute()` or `vmmigration_projects_locations_sources_migrating_vms_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigratingVm>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigratingVm = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Gets details of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigratingVm>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Gets details of a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigratingVm>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_get_builder(
        client, &args.name, &args.view,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_list_execute()` or `vmmigration_projects_locations_sources_migrating_vms_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMigratingVmsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMigratingVmsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMigratingVmsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms
/// Lists MigratingVms in a given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMigratingVmsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Updates the parameters of a single MigratingVm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_patch_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_patch` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Updates the parameters of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_patch_execute()` or `vmmigration_projects_locations_sources_migrating_vms_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Updates the parameters of a single MigratingVm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_patch_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}
/// Updates the parameters of a single MigratingVm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_patch_builder()` + `vmmigration_projects_locations_sources_migrating_vms_patch_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_patch(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_pause_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    migratingVm: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:pauseMigration",
        migratingVm,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute()` or `vmmigration_projects_locations_sources_migrating_vms_pause_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_pause_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_pause_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsPauseMigrationArgs {
    /// Path parameter: migratingVm
    pub migratingVm: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:pauseMigration
/// Pauses a migration for a VM. If cycle tasks are running they will be cancelled, preserving source task data. Further replication cycles will not be triggered while the VM is paused.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_pause_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_pause_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsPauseMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_pause_migration_builder(
        client,
        &args.migratingVm,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_pause_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_resume_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    migratingVm: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:resumeMigration",
        migratingVm,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute()` or `vmmigration_projects_locations_sources_migrating_vms_resume_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_resume_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_resume_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsResumeMigrationArgs {
    /// Path parameter: migratingVm
    pub migratingVm: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:resumeMigration
/// Resumes a migration for a VM. When called on a paused migration, will start the process of uploading data and creating snapshots; when called on a completed cut-over migration, will update the migration to active state and start the process of uploading data and creating snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_resume_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_resume_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsResumeMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_resume_migration_builder(
        client,
        &args.migratingVm,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_resume_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_start_migration_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_start_migration` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    migratingVm: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:startMigration",
        migratingVm,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_execute()` or `vmmigration_projects_locations_sources_migrating_vms_start_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_start_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_start_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_start_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsStartMigrationArgs {
    /// Path parameter: migratingVm
    pub migratingVm: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}:startMigration
/// Starts migration for a VM. Starts the process of uploading data and creating snapshots, in replication cycles scheduled by the policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_builder()` + `vmmigration_projects_locations_sources_migrating_vms_start_migration_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_start_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_start_migration(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsStartMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_start_migration_builder(
        client,
        &args.migratingVm,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_start_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute()` or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}:cancel
/// Initiates the cancellation of a running clone job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_cancel_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    cloneJobId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cloneJobId.as_ref() {
        query_parts.push(format!("cloneJobId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute()` or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: cloneJobId
    pub cloneJobId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Initiates a Clone of a specific migrating VM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_builder(
        client,
        &args.parent,
        &args.cloneJobId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}
/// Gets details of a single CloneJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}
/// Gets details of a single CloneJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute()` or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CloneJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CloneJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}
/// Gets details of a single CloneJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloneJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs/{cloneJobsId}
/// Gets details of a single CloneJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CloneJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute()` or `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCloneJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCloneJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCloneJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cloneJobs
/// Lists the CloneJobs of a migrating VM. Only 25 most recent CloneJobs are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCloneJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_clone_jobs_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute()` or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel
/// Initiates the cancellation of a running cutover job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    cutoverJobId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cutoverJobId.as_ref() {
        query_parts.push(format!("cutoverJobId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute()` or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: cutoverJobId
    pub cutoverJobId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Initiates a Cutover of a specific migrating VM. The returned LRO is completed when the cutover job resource is created and the job is initiated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_builder(
        client,
        &args.parent,
        &args.cutoverJobId,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}
/// Gets details of a single CutoverJob.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}
/// Gets details of a single CutoverJob.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute()` or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CutoverJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CutoverJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}
/// Gets details of a single CutoverJob.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CutoverJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}
/// Gets details of a single CutoverJob.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CutoverJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_builder(
        client, &args.name,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute()` or `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCutoverJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCutoverJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCutoverJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
/// Lists the CutoverJobs of a migrating VM. Only 25 most recent CutoverJobs are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCutoverJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles/{replicationCyclesId}
/// Gets details of a single ReplicationCycle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles/{replicationCyclesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles/{replicationCyclesId}
/// Gets details of a single ReplicationCycle.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute()` or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReplicationCycle>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplicationCycle = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles/{replicationCyclesId}
/// Gets details of a single ReplicationCycle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplicationCycle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles/{replicationCyclesId}
/// Gets details of a single ReplicationCycle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder()` + `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplicationCycle>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_builder(
            client, &args.name,
        )?;
    vmmigration_projects_locations_sources_migrating_vms_replication_cycles_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute()` to send, or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list` for simplest API.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute()` or `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReplicationCyclesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReplicationCyclesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReplicationCyclesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/replicationCycles
/// Lists ReplicationCycles in a given MigratingVM.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder()` + `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReplicationCyclesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    vmmigration_projects_locations_sources_migrating_vms_replication_cycles_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_create_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_create` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<Option<String>>,
    utilizationReportId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/utilizationReports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = utilizationReportId.as_ref() {
        query_parts.push(format!("utilizationReportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_utilization_reports_create_execute()` or `vmmigration_projects_locations_sources_utilization_reports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_utilization_reports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_utilization_reports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_utilization_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_utilization_reports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_utilization_reports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesUtilizationReportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: utilizationReportId
    pub utilizationReportId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Creates a new UtilizationReport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_create_builder()` + `vmmigration_projects_locations_sources_utilization_reports_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesUtilizationReportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.utilizationReportId,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Deletes a single Utilization Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_delete_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_delete` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Deletes a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_utilization_reports_delete_execute()` or `vmmigration_projects_locations_sources_utilization_reports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Deletes a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_utilization_reports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_utilization_reports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_utilization_reports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_utilization_reports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesUtilizationReportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Deletes a single Utilization Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_delete_builder()` + `vmmigration_projects_locations_sources_utilization_reports_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesUtilizationReportsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Gets a single Utilization Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_get_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_get` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Gets a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_utilization_reports_get_execute()` or `vmmigration_projects_locations_sources_utilization_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UtilizationReport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UtilizationReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Gets a single Utilization Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_utilization_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_utilization_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_utilization_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UtilizationReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_utilization_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_utilization_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesUtilizationReportsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports/{utilizationReportsId}
/// Gets a single Utilization Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_get_builder()` + `vmmigration_projects_locations_sources_utilization_reports_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesUtilizationReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UtilizationReport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_get_builder(
        client, &args.name, &args.view,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_sources_utilization_reports_list_execute()` to send, or `vmmigration_projects_locations_sources_utilization_reports_list` for simplest API.

pub fn vmmigration_projects_locations_sources_utilization_reports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    view: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/sources/{sourcesId}/utilizationReports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_sources_utilization_reports_list_execute()` or `vmmigration_projects_locations_sources_utilization_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUtilizationReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUtilizationReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_sources_utilization_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_sources_utilization_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_sources_utilization_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_sources_utilization_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUtilizationReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_sources_utilization_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_sources_utilization_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesUtilizationReportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: view
    pub view: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/utilizationReports
/// Lists Utilization Reports of the given Source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_sources_utilization_reports_list_builder()` + `vmmigration_projects_locations_sources_utilization_reports_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_sources_utilization_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_sources_utilization_reports_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsSourcesUtilizationReportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUtilizationReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_sources_utilization_reports_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    vmmigration_projects_locations_sources_utilization_reports_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_create_execute()` to send, or `vmmigration_projects_locations_target_projects_create` for simplest API.

pub fn vmmigration_projects_locations_target_projects_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<Option<String>>,
    targetProjectId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/targetProjects",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = targetProjectId.as_ref() {
        query_parts.push(format!("targetProjectId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_target_projects_create_execute()` or `vmmigration_projects_locations_target_projects_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_target_projects_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_target_projects_create_task()`.
/// For the simplest API, use `vmmigration_projects_locations_target_projects_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_target_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_target_projects_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_target_projects_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsTargetProjectsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: targetProjectId
    pub targetProjectId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Creates a new TargetProject in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_create_builder()` + `vmmigration_projects_locations_target_projects_create_execute()`.
/// For task-level control, use `vmmigration_projects_locations_target_projects_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_create(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsTargetProjectsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.targetProjectId,
    )?;
    vmmigration_projects_locations_target_projects_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_delete_execute()` to send, or `vmmigration_projects_locations_target_projects_delete` for simplest API.

pub fn vmmigration_projects_locations_target_projects_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/targetProjects/{targetProjectsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_target_projects_delete_execute()` or `vmmigration_projects_locations_target_projects_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_target_projects_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_target_projects_delete_task()`.
/// For the simplest API, use `vmmigration_projects_locations_target_projects_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_target_projects_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_target_projects_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_target_projects_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsTargetProjectsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Deletes a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_delete_builder()` + `vmmigration_projects_locations_target_projects_delete_execute()`.
/// For task-level control, use `vmmigration_projects_locations_target_projects_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_delete(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsTargetProjectsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    vmmigration_projects_locations_target_projects_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_get_execute()` to send, or `vmmigration_projects_locations_target_projects_get` for simplest API.

pub fn vmmigration_projects_locations_target_projects_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/targetProjects/{targetProjectsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_target_projects_get_execute()` or `vmmigration_projects_locations_target_projects_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetProject>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetProject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_target_projects_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_target_projects_get_task()`.
/// For the simplest API, use `vmmigration_projects_locations_target_projects_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_target_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_target_projects_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_target_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsTargetProjectsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Gets details of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_get_builder()` + `vmmigration_projects_locations_target_projects_get_execute()`.
/// For task-level control, use `vmmigration_projects_locations_target_projects_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_get(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsTargetProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_get_builder(client, &args.name)?;
    vmmigration_projects_locations_target_projects_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_list_execute()` to send, or `vmmigration_projects_locations_target_projects_list` for simplest API.

pub fn vmmigration_projects_locations_target_projects_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/targetProjects",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_target_projects_list_execute()` or `vmmigration_projects_locations_target_projects_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTargetProjectsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTargetProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_target_projects_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_target_projects_list_task()`.
/// For the simplest API, use `vmmigration_projects_locations_target_projects_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_target_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_target_projects_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_target_projects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsTargetProjectsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/targetProjects
/// Lists TargetProjects in a given project. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_list_builder()` + `vmmigration_projects_locations_target_projects_list_execute()`.
/// For task-level control, use `vmmigration_projects_locations_target_projects_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_list(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsTargetProjectsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    vmmigration_projects_locations_target_projects_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vmmigration_projects_locations_target_projects_patch_execute()` to send, or `vmmigration_projects_locations_target_projects_patch` for simplest API.

pub fn vmmigration_projects_locations_target_projects_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{}/locations/{locationsId}/targetProjects/{targetProjectsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vmmigration_projects_locations_target_projects_patch_execute()` or `vmmigration_projects_locations_target_projects_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vmmigration_projects_locations_target_projects_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vmmigration_projects_locations_target_projects_patch_task()`.
/// For the simplest API, use `vmmigration_projects_locations_target_projects_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vmmigration_projects_locations_target_projects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vmmigration_projects_locations_target_projects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vmmigration_projects_locations_target_projects_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vmmigration_projects_locations_target_projects_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsTargetProjectsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/targetProjects/{targetProjectsId}
/// Updates the parameters of a single TargetProject. NOTE: TargetProject is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vmmigration_projects_locations_target_projects_patch_builder()` + `vmmigration_projects_locations_target_projects_patch_execute()`.
/// For task-level control, use `vmmigration_projects_locations_target_projects_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vmmigration_projects_locations_target_projects_patch(
    client: &SimpleHttpClient,
    args: &VmmigrationProjectsLocationsTargetProjectsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vmmigration_projects_locations_target_projects_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    vmmigration_projects_locations_target_projects_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with VmmigrationProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGetArgs) -> String {
        format!("gcp::vmmigration::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with VmmigrationProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsListArgs) -> String {
        format!("gcp::vmmigration::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsGroupsAddGroupMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsAddGroupMigrationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsGroupsAddGroupMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.group)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGroupsCreateArgs) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGroupsDeleteArgs) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Group
// =============================================================================

/// ResourceIdentifier implementation for Group with VmmigrationProjectsLocationsGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsGetArgs> for Group {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGroupsGetArgs) -> String {
        format!("gcp::vmmigration::Group/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Group"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGroupsResponse with VmmigrationProjectsLocationsGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsListArgs> for ListGroupsResponse {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGroupsListArgs) -> String {
        format!("gcp::vmmigration::ListGroupsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsGroupsPatchArgs) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsGroupsRemoveGroupMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsGroupsRemoveGroupMigrationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsGroupsRemoveGroupMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.group)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsImageImportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsImageImportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ImageImport
// =============================================================================

/// ResourceIdentifier implementation for ImageImport with VmmigrationProjectsLocationsImageImportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsGetArgs> for ImageImport {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::ImageImport/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ImageImport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListImageImportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListImageImportsResponse with VmmigrationProjectsLocationsImageImportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsListArgs>
    for ListImageImportsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListImageImportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListImageImportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsImageImportsImageImportJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsImageImportJobsCancelArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsImageImportJobsCancelArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ImageImportJob
// =============================================================================

/// ResourceIdentifier implementation for ImageImportJob with VmmigrationProjectsLocationsImageImportsImageImportJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsImageImportJobsGetArgs>
    for ImageImportJob
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsImageImportJobsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::ImageImportJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ImageImportJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListImageImportJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListImageImportJobsResponse with VmmigrationProjectsLocationsImageImportsImageImportJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsImageImportsImageImportJobsListArgs>
    for ListImageImportJobsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsImageImportsImageImportJobsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListImageImportJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListImageImportJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VmmigrationProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::vmmigration::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with VmmigrationProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with VmmigrationProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::vmmigration::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchInventoryResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchInventoryResponse with VmmigrationProjectsLocationsSourcesFetchInventoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesFetchInventoryArgs>
    for FetchInventoryResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesFetchInventoryArgs,
    ) -> String {
        format!("gcp::vmmigration::FetchInventoryResponse/{}", input.source)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::FetchInventoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchStorageInventoryResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchStorageInventoryResponse with VmmigrationProjectsLocationsSourcesFetchStorageInventoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesFetchStorageInventoryArgs>
    for FetchStorageInventoryResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesFetchStorageInventoryArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::FetchStorageInventoryResponse/{}",
            input.source
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::FetchStorageInventoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Source
// =============================================================================

/// ResourceIdentifier implementation for Source with VmmigrationProjectsLocationsSourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesGetArgs> for Source {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsSourcesGetArgs) -> String {
        format!("gcp::vmmigration::Source/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Source"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSourcesResponse with VmmigrationProjectsLocationsSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesListArgs> for ListSourcesResponse {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsSourcesListArgs) -> String {
        format!("gcp::vmmigration::ListSourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &VmmigrationProjectsLocationsSourcesPatchArgs) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDatacenterConnectorsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDatacenterConnectorsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDatacenterConnectorsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDatacenterConnectorsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DatacenterConnector
// =============================================================================

/// ResourceIdentifier implementation for DatacenterConnector with VmmigrationProjectsLocationsSourcesDatacenterConnectorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDatacenterConnectorsGetArgs>
    for DatacenterConnector
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::DatacenterConnector/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::DatacenterConnector"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDatacenterConnectorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDatacenterConnectorsResponse with VmmigrationProjectsLocationsSourcesDatacenterConnectorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDatacenterConnectorsListArgs>
    for ListDatacenterConnectorsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListDatacenterConnectorsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListDatacenterConnectorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDatacenterConnectorsUpgradeApplianceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDatacenterConnectorsUpgradeApplianceArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDatacenterConnectorsUpgradeApplianceArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.datacenterConnector)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDiskMigrationJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsCancelArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsCancelArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDiskMigrationJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDiskMigrationJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DiskMigrationJob
// =============================================================================

/// ResourceIdentifier implementation for DiskMigrationJob with VmmigrationProjectsLocationsSourcesDiskMigrationJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsGetArgs>
    for DiskMigrationJob
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::DiskMigrationJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::DiskMigrationJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDiskMigrationJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDiskMigrationJobsResponse with VmmigrationProjectsLocationsSourcesDiskMigrationJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsListArgs>
    for ListDiskMigrationJobsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListDiskMigrationJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListDiskMigrationJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDiskMigrationJobsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsPatchArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesDiskMigrationJobsRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesDiskMigrationJobsRunArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesDiskMigrationJobsRunArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsExtendMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsExtendMigrationArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsExtendMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.migratingVm)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsFinalizeMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsFinalizeMigrationArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsFinalizeMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.migratingVm)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MigratingVm
// =============================================================================

/// ResourceIdentifier implementation for MigratingVm with VmmigrationProjectsLocationsSourcesMigratingVmsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsGetArgs> for MigratingVm {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::MigratingVm/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::MigratingVm"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMigratingVmsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMigratingVmsResponse with VmmigrationProjectsLocationsSourcesMigratingVmsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsListArgs>
    for ListMigratingVmsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListMigratingVmsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListMigratingVmsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsPatchArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsPauseMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsPauseMigrationArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsPauseMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.migratingVm)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsResumeMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsResumeMigrationArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsResumeMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.migratingVm)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsStartMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsStartMigrationArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsStartMigrationArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.migratingVm)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCancelArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCancelArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CloneJob
// =============================================================================

/// ResourceIdentifier implementation for CloneJob with VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsGetArgs>
    for CloneJob
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::CloneJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::CloneJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCloneJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCloneJobsResponse with VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsListArgs>
    for ListCloneJobsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCloneJobsListArgs,
    ) -> String {
        format!("gcp::vmmigration::ListCloneJobsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListCloneJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CutoverJob
// =============================================================================

/// ResourceIdentifier implementation for CutoverJob with VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs>
    for CutoverJob
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::CutoverJob/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::CutoverJob"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCutoverJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCutoverJobsResponse with VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs>
    for ListCutoverJobsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs,
    ) -> String {
        format!("gcp::vmmigration::ListCutoverJobsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListCutoverJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReplicationCycle
// =============================================================================

/// ResourceIdentifier implementation for ReplicationCycle with VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesGetArgs>
    for ReplicationCycle
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesGetArgs,
    ) -> String {
        format!("gcp::vmmigration::ReplicationCycle/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ReplicationCycle"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReplicationCyclesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReplicationCyclesResponse with VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesListArgs>
    for ListReplicationCyclesResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesMigratingVmsReplicationCyclesListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListReplicationCyclesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListReplicationCyclesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesUtilizationReportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesUtilizationReportsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesUtilizationReportsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsSourcesUtilizationReportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesUtilizationReportsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesUtilizationReportsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UtilizationReport
// =============================================================================

/// ResourceIdentifier implementation for UtilizationReport with VmmigrationProjectsLocationsSourcesUtilizationReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesUtilizationReportsGetArgs>
    for UtilizationReport
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesUtilizationReportsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::UtilizationReport/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::UtilizationReport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUtilizationReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUtilizationReportsResponse with VmmigrationProjectsLocationsSourcesUtilizationReportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsSourcesUtilizationReportsListArgs>
    for ListUtilizationReportsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsSourcesUtilizationReportsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListUtilizationReportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListUtilizationReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsTargetProjectsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsTargetProjectsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsTargetProjectsCreateArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsTargetProjectsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsTargetProjectsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsTargetProjectsDeleteArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetProject
// =============================================================================

/// ResourceIdentifier implementation for TargetProject with VmmigrationProjectsLocationsTargetProjectsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsTargetProjectsGetArgs> for TargetProject {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsTargetProjectsGetArgs,
    ) -> String {
        format!("gcp::vmmigration::TargetProject/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::TargetProject"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTargetProjectsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTargetProjectsResponse with VmmigrationProjectsLocationsTargetProjectsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsTargetProjectsListArgs>
    for ListTargetProjectsResponse
{
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsTargetProjectsListArgs,
    ) -> String {
        format!(
            "gcp::vmmigration::ListTargetProjectsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::ListTargetProjectsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with VmmigrationProjectsLocationsTargetProjectsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VmmigrationProjectsLocationsTargetProjectsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &VmmigrationProjectsLocationsTargetProjectsPatchArgs,
    ) -> String {
        format!("gcp::vmmigration::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::vmmigration::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
