//! Auto-generated API clients for Google Cloud Platform - clouderrorreporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_delete_events_execute()` to send, or `clouderrorreporting_projects_delete_events` for simplest API.

pub fn clouderrorreporting_projects_delete_events_builder(
    client: &SimpleHttpClient,
    projectName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/events",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_delete_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_delete_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_delete_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_delete_events_builder()` + `clouderrorreporting_projects_delete_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_delete_events(
    client: &SimpleHttpClient,
    projectName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_delete_events_builder(client, projectName)?;
    clouderrorreporting_projects_delete_events_execute(builder)
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_events_list_execute()` to send, or `clouderrorreporting_projects_events_list` for simplest API.

pub fn clouderrorreporting_projects_events_list_builder(
    client: &SimpleHttpClient,
    projectName: &str,
    groupId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/events",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupId",
            groupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_resourceType",
            serviceFilter_resourceType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_service",
            serviceFilter_service
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_version",
            serviceFilter_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeRange_period",
            timeRange_period
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_events_list_builder()` + `clouderrorreporting_projects_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_list(
    client: &SimpleHttpClient,
    projectName: &str,
    groupId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_events_list_builder(
        client,
        projectName,
        groupId,
        pageSize,
        pageToken,
        serviceFilter_resourceType,
        serviceFilter_service,
        serviceFilter_version,
        timeRange_period,
    )?;
    clouderrorreporting_projects_events_list_execute(builder)
}

/// GET v1beta1/{+projectName}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_events_report_execute()` to send, or `clouderrorreporting_projects_events_report` for simplest API.

pub fn clouderrorreporting_projects_events_report_builder(
    client: &SimpleHttpClient,
    projectName: &str,
    body: &ReportedErrorEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/events:report",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1beta1/{+projectName}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_events_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_events_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportErrorEventResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportErrorEventResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_events_report_builder()` + `clouderrorreporting_projects_events_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_report(
    client: &SimpleHttpClient,
    projectName: &str,
    body: &ReportedErrorEvent,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportErrorEventResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_events_report_builder(client, projectName, body)?;
    clouderrorreporting_projects_events_report_execute(builder)
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_group_stats_list_execute()` to send, or `clouderrorreporting_projects_group_stats_list` for simplest API.

pub fn clouderrorreporting_projects_group_stats_list_builder(
    client: &SimpleHttpClient,
    projectName: &str,
    alignment: Option<&str>,
    alignmentTime: Option<&str>,
    groupId: Option<&str>,
    order: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
    timedCountDuration: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/groupStats",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alignment",
            alignment
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "alignmentTime",
            alignmentTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "groupId",
            groupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_resourceType",
            serviceFilter_resourceType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_service",
            serviceFilter_service
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_version",
            serviceFilter_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeRange_period",
            timeRange_period
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timedCountDuration",
            timedCountDuration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_group_stats_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_group_stats_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_group_stats_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_group_stats_list_builder()` + `clouderrorreporting_projects_group_stats_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_group_stats_list(
    client: &SimpleHttpClient,
    projectName: &str,
    alignment: Option<&str>,
    alignmentTime: Option<&str>,
    groupId: Option<&str>,
    order: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
    timedCountDuration: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_group_stats_list_builder(
        client,
        projectName,
        alignment,
        alignmentTime,
        groupId,
        order,
        pageSize,
        pageToken,
        serviceFilter_resourceType,
        serviceFilter_service,
        serviceFilter_version,
        timeRange_period,
        timedCountDuration,
    )?;
    clouderrorreporting_projects_group_stats_list_execute(builder)
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_groups_get_execute()` to send, or `clouderrorreporting_projects_groups_get` for simplest API.

pub fn clouderrorreporting_projects_groups_get_builder(
    client: &SimpleHttpClient,
    groupName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1beta1/{+groupName}", groupName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ErrorGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_groups_get_builder()` + `clouderrorreporting_projects_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_groups_get(
    client: &SimpleHttpClient,
    groupName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_groups_get_builder(client, groupName)?;
    clouderrorreporting_projects_groups_get_execute(builder)
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_groups_update_execute()` to send, or `clouderrorreporting_projects_groups_update` for simplest API.

pub fn clouderrorreporting_projects_groups_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ErrorGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1beta1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_groups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_groups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ErrorGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_groups_update_builder()` + `clouderrorreporting_projects_groups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_groups_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &ErrorGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_groups_update_builder(client, name, body)?;
    clouderrorreporting_projects_groups_update_execute(builder)
}

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_locations_delete_events_execute()` to send, or `clouderrorreporting_projects_locations_delete_events` for simplest API.

pub fn clouderrorreporting_projects_locations_delete_events_builder(
    client: &SimpleHttpClient,
    projectName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/events",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_locations_delete_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_locations_delete_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_delete_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Deletes all error events of a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_locations_delete_events_builder()` + `clouderrorreporting_projects_locations_delete_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_delete_events(
    client: &SimpleHttpClient,
    projectName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouderrorreporting_projects_locations_delete_events_builder(client, projectName)?;
    clouderrorreporting_projects_locations_delete_events_execute(builder)
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_locations_events_list_execute()` to send, or `clouderrorreporting_projects_locations_events_list` for simplest API.

pub fn clouderrorreporting_projects_locations_events_list_builder(
    client: &SimpleHttpClient,
    projectName: &str,
    groupId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/events",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupId",
            groupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_resourceType",
            serviceFilter_resourceType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_service",
            serviceFilter_service
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_version",
            serviceFilter_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeRange_period",
            timeRange_period
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_locations_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_locations_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/events
/// Lists the specified events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_locations_events_list_builder()` + `clouderrorreporting_projects_locations_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_events_list(
    client: &SimpleHttpClient,
    projectName: &str,
    groupId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_locations_events_list_builder(
        client,
        projectName,
        groupId,
        pageSize,
        pageToken,
        serviceFilter_resourceType,
        serviceFilter_service,
        serviceFilter_version,
        timeRange_period,
    )?;
    clouderrorreporting_projects_locations_events_list_execute(builder)
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_locations_group_stats_list_execute()` to send, or `clouderrorreporting_projects_locations_group_stats_list` for simplest API.

pub fn clouderrorreporting_projects_locations_group_stats_list_builder(
    client: &SimpleHttpClient,
    projectName: &str,
    alignment: Option<&str>,
    alignmentTime: Option<&str>,
    groupId: Option<&str>,
    order: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
    timedCountDuration: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1beta1/{+projectName}/groupStats",
        projectName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "alignment",
            alignment
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "alignmentTime",
            alignmentTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "groupId",
            groupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_resourceType",
            serviceFilter_resourceType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_service",
            serviceFilter_service
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceFilter_version",
            serviceFilter_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timeRange_period",
            timeRange_period
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timedCountDuration",
            timedCountDuration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_locations_group_stats_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_locations_group_stats_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_group_stats_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+projectName}/groupStats
/// Lists the specified groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_locations_group_stats_list_builder()` + `clouderrorreporting_projects_locations_group_stats_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_group_stats_list(
    client: &SimpleHttpClient,
    projectName: &str,
    alignment: Option<&str>,
    alignmentTime: Option<&str>,
    groupId: Option<&str>,
    order: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    serviceFilter_resourceType: Option<&str>,
    serviceFilter_service: Option<&str>,
    serviceFilter_version: Option<&str>,
    timeRange_period: Option<&str>,
    timedCountDuration: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_locations_group_stats_list_builder(
        client,
        projectName,
        alignment,
        alignmentTime,
        groupId,
        order,
        pageSize,
        pageToken,
        serviceFilter_resourceType,
        serviceFilter_service,
        serviceFilter_version,
        timeRange_period,
        timedCountDuration,
    )?;
    clouderrorreporting_projects_locations_group_stats_list_execute(builder)
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_locations_groups_get_execute()` to send, or `clouderrorreporting_projects_locations_groups_get` for simplest API.

pub fn clouderrorreporting_projects_locations_groups_get_builder(
    client: &SimpleHttpClient,
    groupName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1beta1/{+groupName}", groupName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_locations_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_locations_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ErrorGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+groupName}
/// Get the specified group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_locations_groups_get_builder()` + `clouderrorreporting_projects_locations_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_groups_get(
    client: &SimpleHttpClient,
    groupName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_locations_groups_get_builder(client, groupName)?;
    clouderrorreporting_projects_locations_groups_get_execute(builder)
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_locations_groups_update_execute()` to send, or `clouderrorreporting_projects_locations_groups_update` for simplest API.

pub fn clouderrorreporting_projects_locations_groups_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ErrorGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1beta1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `clouderrorreporting_projects_locations_groups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from clouderrorreporting_projects_locations_groups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ErrorGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/{+name}
/// Replace the data for the specified group. Fails if the group does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_locations_groups_update_builder()` + `clouderrorreporting_projects_locations_groups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_locations_groups_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &ErrorGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ErrorGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_locations_groups_update_builder(client, name, body)?;
    clouderrorreporting_projects_locations_groups_update_execute(builder)
}
