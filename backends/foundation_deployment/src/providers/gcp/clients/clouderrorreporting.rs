//! Auto-generated API clients for gcp/clouderrorreporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/projects/{projectsId}/events
/// Deletes all error events of a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_delete_events_execute()` to send, or `clouderrorreporting_projects_delete_events` for simplest API.

pub fn clouderrorreporting_projects_delete_events_builder(
    client: &SimpleHttpClient,
    projectName: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://clouderrorreporting.googleapis.com/v1beta1/projects/{}/events",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/events
/// Deletes all error events of a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouderrorreporting_projects_delete_events_execute()` or `clouderrorreporting_projects_delete_events`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_delete_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_delete_events_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteEventsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteEventsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/events
/// Deletes all error events of a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouderrorreporting_projects_delete_events_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouderrorreporting_projects_delete_events_task()`.
/// For the simplest API, use `clouderrorreporting_projects_delete_events()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_delete_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouderrorreporting_projects_delete_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouderrorreporting_projects_delete_events_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouderrorreporting_projects_delete_events`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouderrorreportingProjectsDeleteEventsArgs {
    /// Path parameter: projectName
    pub projectName: String,
}

/// GET v1beta1/projects/{projectsId}/events
/// Deletes all error events of a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_delete_events_builder()` + `clouderrorreporting_projects_delete_events_execute()`.
/// For task-level control, use `clouderrorreporting_projects_delete_events_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_delete_events(
    client: &SimpleHttpClient,
    args: &ClouderrorreportingProjectsDeleteEventsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteEventsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_delete_events_builder(client, &args.projectName)?;
    clouderrorreporting_projects_delete_events_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_events_report_execute()` to send, or `clouderrorreporting_projects_events_report` for simplest API.

pub fn clouderrorreporting_projects_events_report_builder(
    client: &SimpleHttpClient,
    projectName: &String,
    body: &ReportedErrorEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://clouderrorreporting.googleapis.com/v1beta1/projects/{}/events:report",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/projects/{projectsId}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouderrorreporting_projects_events_report_execute()` or `clouderrorreporting_projects_events_report`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_events_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_report_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportErrorEventResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportErrorEventResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouderrorreporting_projects_events_report_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouderrorreporting_projects_events_report_task()`.
/// For the simplest API, use `clouderrorreporting_projects_events_report()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_events_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouderrorreporting_projects_events_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportErrorEventResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouderrorreporting_projects_events_report_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouderrorreporting_projects_events_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouderrorreportingProjectsEventsReportArgs {
    /// Path parameter: projectName
    pub projectName: String,
    /// Request body.
    pub body: ReportedErrorEvent,
}

/// GET v1beta1/projects/{projectsId}/events:report
/// Report an individual error event and record the event to a log. This endpoint accepts **either** an OAuth token, **or** an [API key](<https://support.google.`com/cloud/answer/6158862`>) for authentication. To use an API key, append it to the URL as the value of a key parameter. For example: POST <https://clouderrorreporting.googleapis.`com/v1beta1/{`projectName`}/events`:report?key=123ABC456> **Note:** [Error Reporting] (<https://cloud.google.`com/error-reporting`>) is a service built on Cloud Logging and can analyze log entries when all of the following are `true`: * Customer-managed encryption keys (CMEK) are disabled on the log bucket. * The log bucket satisfies one of the following: * The log bucket is stored in the same project where the logs originated. * The logs were routed to a project, and then that project stored those logs in a log bucket that it owns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_events_report_builder()` + `clouderrorreporting_projects_events_report_execute()`.
/// For task-level control, use `clouderrorreporting_projects_events_report_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_events_report(
    client: &SimpleHttpClient,
    args: &ClouderrorreportingProjectsEventsReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportErrorEventResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        clouderrorreporting_projects_events_report_builder(client, &args.projectName, &args.body)?;
    clouderrorreporting_projects_events_report_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/groupStats
/// Lists the specified groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `clouderrorreporting_projects_group_stats_list_execute()` to send, or `clouderrorreporting_projects_group_stats_list` for simplest API.

pub fn clouderrorreporting_projects_group_stats_list_builder(
    client: &SimpleHttpClient,
    projectName: &String,
    alignment: &Option<String>,
    alignmentTime: &Option<String>,
    groupId: &Option<String>,
    order: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    serviceFilter_resourceType: &Option<String>,
    serviceFilter_service: &Option<String>,
    serviceFilter_version: &Option<String>,
    timeRange_period: &Option<String>,
    timedCountDuration: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://clouderrorreporting.googleapis.com/v1beta1/projects/{}/groupStats",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = alignment.as_ref() {
        query_parts.push(format!("alignment={}", val));
    }
    if let Some(val) = alignmentTime.as_ref() {
        query_parts.push(format!("alignmentTime={}", val));
    }
    if let Some(val) = groupId.as_ref() {
        query_parts.push(format!("groupId={}", val));
    }
    if let Some(val) = order.as_ref() {
        query_parts.push(format!("order={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = serviceFilter_resourceType.as_ref() {
        query_parts.push(format!("serviceFilter.resourceType={}", val));
    }
    if let Some(val) = serviceFilter_service.as_ref() {
        query_parts.push(format!("serviceFilter.service={}", val));
    }
    if let Some(val) = serviceFilter_version.as_ref() {
        query_parts.push(format!("serviceFilter.version={}", val));
    }
    if let Some(val) = timeRange_period.as_ref() {
        query_parts.push(format!("timeRange.period={}", val));
    }
    if let Some(val) = timedCountDuration.as_ref() {
        query_parts.push(format!("timedCountDuration={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/groupStats
/// Lists the specified groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `clouderrorreporting_projects_group_stats_list_execute()` or `clouderrorreporting_projects_group_stats_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_group_stats_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_group_stats_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGroupStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/groupStats
/// Lists the specified groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `clouderrorreporting_projects_group_stats_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `clouderrorreporting_projects_group_stats_list_task()`.
/// For the simplest API, use `clouderrorreporting_projects_group_stats_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `clouderrorreporting_projects_group_stats_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn clouderrorreporting_projects_group_stats_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = clouderrorreporting_projects_group_stats_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`clouderrorreporting_projects_group_stats_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClouderrorreportingProjectsGroupStatsListArgs {
    /// Path parameter: projectName
    pub projectName: String,
    /// Query parameter: alignment
    pub alignment: Option<String>,
    /// Query parameter: alignmentTime
    pub alignmentTime: Option<String>,
    /// Query parameter: groupId
    pub groupId: Option<String>,
    /// Query parameter: order
    pub order: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: serviceFilter_resourceType
    pub serviceFilter_resourceType: Option<String>,
    /// Query parameter: serviceFilter_service
    pub serviceFilter_service: Option<String>,
    /// Query parameter: serviceFilter_version
    pub serviceFilter_version: Option<String>,
    /// Query parameter: timeRange_period
    pub timeRange_period: Option<String>,
    /// Query parameter: timedCountDuration
    pub timedCountDuration: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/groupStats
/// Lists the specified groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `clouderrorreporting_projects_group_stats_list_builder()` + `clouderrorreporting_projects_group_stats_list_execute()`.
/// For task-level control, use `clouderrorreporting_projects_group_stats_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn clouderrorreporting_projects_group_stats_list(
    client: &SimpleHttpClient,
    args: &ClouderrorreportingProjectsGroupStatsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = clouderrorreporting_projects_group_stats_list_builder(
        client,
        &args.projectName,
        &args.alignment,
        &args.alignmentTime,
        &args.groupId,
        &args.order,
        &args.pageSize,
        &args.pageToken,
        &args.serviceFilter_resourceType,
        &args.serviceFilter_service,
        &args.serviceFilter_version,
        &args.timeRange_period,
        &args.timedCountDuration,
    )?;
    clouderrorreporting_projects_group_stats_list_execute(builder)
}
