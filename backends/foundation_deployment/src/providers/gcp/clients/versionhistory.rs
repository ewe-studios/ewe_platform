//! Auto-generated API clients for gcp/versionhistory.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/{v1Id}/platforms
/// Returns list of platforms that are available for a given product. The resource "product" has no resource name in its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `versionhistory_platforms_list_execute()` to send, or `versionhistory_platforms_list` for simplest API.

pub fn versionhistory_platforms_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://versionhistory.googleapis.com/v1/{}/platforms",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/platforms
/// Returns list of platforms that are available for a given product. The resource "product" has no resource name in its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `versionhistory_platforms_list_execute()` or `versionhistory_platforms_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPlatformsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPlatformsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/platforms
/// Returns list of platforms that are available for a given product. The resource "product" has no resource name in its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `versionhistory_platforms_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `versionhistory_platforms_list_task()`.
/// For the simplest API, use `versionhistory_platforms_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn versionhistory_platforms_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPlatformsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = versionhistory_platforms_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`versionhistory_platforms_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VersionhistoryPlatformsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/{v1Id}/platforms
/// Returns list of platforms that are available for a given product. The resource "product" has no resource name in its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `versionhistory_platforms_list_builder()` + `versionhistory_platforms_list_execute()`.
/// For task-level control, use `versionhistory_platforms_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_list(
    client: &SimpleHttpClient,
    args: &VersionhistoryPlatformsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPlatformsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = versionhistory_platforms_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    versionhistory_platforms_list_execute(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels
/// Returns list of channels that are available for a given platform.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `versionhistory_platforms_channels_list_execute()` to send, or `versionhistory_platforms_channels_list` for simplest API.

pub fn versionhistory_platforms_channels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://versionhistory.googleapis.com/v1/{}/platforms/{platformsId}/channels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels
/// Returns list of channels that are available for a given platform.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `versionhistory_platforms_channels_list_execute()` or `versionhistory_platforms_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListChannelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListChannelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels
/// Returns list of channels that are available for a given platform.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `versionhistory_platforms_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `versionhistory_platforms_channels_list_task()`.
/// For the simplest API, use `versionhistory_platforms_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn versionhistory_platforms_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = versionhistory_platforms_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`versionhistory_platforms_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VersionhistoryPlatformsChannelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels
/// Returns list of channels that are available for a given platform.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `versionhistory_platforms_channels_list_builder()` + `versionhistory_platforms_channels_list_execute()`.
/// For task-level control, use `versionhistory_platforms_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_list(
    client: &SimpleHttpClient,
    args: &VersionhistoryPlatformsChannelsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = versionhistory_platforms_channels_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    versionhistory_platforms_channels_list_execute(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions
/// Returns list of version for the given `platform/channel`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `versionhistory_platforms_channels_versions_list_execute()` to send, or `versionhistory_platforms_channels_versions_list` for simplest API.

pub fn versionhistory_platforms_channels_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://versionhistory.googleapis.com/v1/{}/platforms/{platformsId}/channels/{channelsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions
/// Returns list of version for the given `platform/channel`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `versionhistory_platforms_channels_versions_list_execute()` or `versionhistory_platforms_channels_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVersionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions
/// Returns list of version for the given `platform/channel`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `versionhistory_platforms_channels_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `versionhistory_platforms_channels_versions_list_task()`.
/// For the simplest API, use `versionhistory_platforms_channels_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn versionhistory_platforms_channels_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = versionhistory_platforms_channels_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`versionhistory_platforms_channels_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VersionhistoryPlatformsChannelsVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions
/// Returns list of version for the given `platform/channel`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `versionhistory_platforms_channels_versions_list_builder()` + `versionhistory_platforms_channels_versions_list_execute()`.
/// For task-level control, use `versionhistory_platforms_channels_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_versions_list(
    client: &SimpleHttpClient,
    args: &VersionhistoryPlatformsChannelsVersionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = versionhistory_platforms_channels_versions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    versionhistory_platforms_channels_versions_list_execute(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions/{versionsId}/releases
/// Returns list of releases of the given version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `versionhistory_platforms_channels_versions_releases_list_execute()` to send, or `versionhistory_platforms_channels_versions_releases_list` for simplest API.

pub fn versionhistory_platforms_channels_versions_releases_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://versionhistory.googleapis.com/v1/{}/platforms/{platformsId}/channels/{channelsId}/versions/{versionsId}/releases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions/{versionsId}/releases
/// Returns list of releases of the given version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `versionhistory_platforms_channels_versions_releases_list_execute()` or `versionhistory_platforms_channels_versions_releases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_versions_releases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_versions_releases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReleasesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions/{versionsId}/releases
/// Returns list of releases of the given version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `versionhistory_platforms_channels_versions_releases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `versionhistory_platforms_channels_versions_releases_list_task()`.
/// For the simplest API, use `versionhistory_platforms_channels_versions_releases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `versionhistory_platforms_channels_versions_releases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn versionhistory_platforms_channels_versions_releases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = versionhistory_platforms_channels_versions_releases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`versionhistory_platforms_channels_versions_releases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VersionhistoryPlatformsChannelsVersionsReleasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/{v1Id}/platforms/{platformsId}/channels/{channelsId}/versions/{versionsId}/releases
/// Returns list of releases of the given version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `versionhistory_platforms_channels_versions_releases_list_builder()` + `versionhistory_platforms_channels_versions_releases_list_execute()`.
/// For task-level control, use `versionhistory_platforms_channels_versions_releases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn versionhistory_platforms_channels_versions_releases_list(
    client: &SimpleHttpClient,
    args: &VersionhistoryPlatformsChannelsVersionsReleasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = versionhistory_platforms_channels_versions_releases_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    versionhistory_platforms_channels_versions_releases_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ListPlatformsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPlatformsResponse with VersionhistoryPlatformsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VersionhistoryPlatformsListArgs> for ListPlatformsResponse {
    fn generate_resource_id(&self, input: &VersionhistoryPlatformsListArgs) -> String {
        format!(
            "gcp::versionhistory::ListPlatformsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::versionhistory::ListPlatformsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListChannelsResponse with VersionhistoryPlatformsChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VersionhistoryPlatformsChannelsListArgs> for ListChannelsResponse {
    fn generate_resource_id(&self, input: &VersionhistoryPlatformsChannelsListArgs) -> String {
        format!("gcp::versionhistory::ListChannelsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::versionhistory::ListChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVersionsResponse with VersionhistoryPlatformsChannelsVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VersionhistoryPlatformsChannelsVersionsListArgs> for ListVersionsResponse {
    fn generate_resource_id(
        &self,
        input: &VersionhistoryPlatformsChannelsVersionsListArgs,
    ) -> String {
        format!("gcp::versionhistory::ListVersionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::versionhistory::ListVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReleasesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReleasesResponse with VersionhistoryPlatformsChannelsVersionsReleasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<VersionhistoryPlatformsChannelsVersionsReleasesListArgs>
    for ListReleasesResponse
{
    fn generate_resource_id(
        &self,
        input: &VersionhistoryPlatformsChannelsVersionsReleasesListArgs,
    ) -> String {
        format!("gcp::versionhistory::ListReleasesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::versionhistory::ListReleasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
