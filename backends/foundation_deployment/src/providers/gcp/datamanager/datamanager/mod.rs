//! Auto-generated API module for gcp/datamanager datamanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datamanager_datamanager `

#![cfg(feature = "gcp_datamanager_datamanager")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PartnerAudienceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PartnerAudienceInfo {
    /// commercePartner property.
    pub commerce_partner: Option<String>,
    /// partnerAudienceSource property.
    pub partner_audience_source: Option<String>,
}

/// `Destination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Destination {
    /// linkedAccount property.
    pub linked_account: Option<ProductAccount>,
    /// loginAccount property.
    pub login_account: Option<ProductAccount>,
    /// operatingAccount property.
    pub operating_account: Option<ProductAccount>,
    /// productDestinationId property.
    pub product_destination_id: Option<String>,
    /// reference property.
    pub reference: Option<String>,
}

/// `AdIdentifiers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdIdentifiers {
    /// gbraid property.
    pub gbraid: Option<String>,
    /// gclid property.
    pub gclid: Option<String>,
    /// landingPageDeviceInfo property.
    pub landing_page_device_info: Option<DeviceInfo>,
    /// sessionAttributes property.
    pub session_attributes: Option<String>,
    /// wbraid property.
    pub wbraid: Option<String>,
}

/// `IngestAudienceMembersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestAudienceMembersResponse {
    /// requestId property.
    pub request_id: Option<String>,
}

/// `RetrieveRequestStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetrieveRequestStatusResponse {
    /// requestStatusPerDestination property.
    pub request_status_per_destination: Option<Vec<RequestStatusPerDestination>>,
}

/// `RemovePpidDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemovePpidDataStatus {
    /// ppidCount property.
    pub ppid_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `AudienceMember` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AudienceMember {
    /// consent property.
    pub consent: Option<Consent>,
    /// destinationReferences property.
    pub destination_references: Option<Vec<String>>,
    /// mobileData property.
    pub mobile_data: Option<MobileData>,
    /// pairData property.
    pub pair_data: Option<PairData>,
    /// ppidData property.
    pub ppid_data: Option<PpidData>,
    /// userData property.
    pub user_data: Option<UserData>,
    /// userIdData property.
    pub user_id_data: Option<UserIdData>,
}

/// `WarningCount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WarningCount {
    /// reason property.
    pub reason: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `IngestAudienceMembersRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestAudienceMembersRequest {
    /// audienceMembers property.
    pub audience_members: Option<Vec<AudienceMember>>,
    /// consent property.
    pub consent: Option<Consent>,
    /// destinations property.
    pub destinations: Option<Vec<Destination>>,
    /// encoding property.
    pub encoding: Option<String>,
    /// encryptionInfo property.
    pub encryption_info: Option<EncryptionInfo>,
    /// termsOfService property.
    pub terms_of_service: Option<TermsOfService>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `PpidData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PpidData {
    /// ppids property.
    pub ppids: Option<Vec<String>>,
}

/// `PairIdInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PairIdInfo {
    /// advertiserIdentifierCount property.
    pub advertiser_identifier_count: Option<String>,
    /// cleanRoomIdentifier property.
    pub clean_room_identifier: Option<String>,
    /// matchRatePercentage property.
    pub match_rate_percentage: Option<i64>,
    /// publisherId property.
    pub publisher_id: Option<String>,
    /// publisherName property.
    pub publisher_name: Option<String>,
}

/// `ListUserListsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUserListsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// userLists property.
    pub user_lists: Option<Vec<UserList>>,
}

/// `IngestUserDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestUserDataStatus {
    /// recordCount property.
    pub record_count: Option<String>,
    /// uploadMatchRateRange property.
    pub upload_match_rate_range: Option<String>,
    /// userIdentifierCount property.
    pub user_identifier_count: Option<String>,
}

/// `UserIdInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserIdInfo {
    /// dataSourceType property.
    pub data_source_type: Option<String>,
}

/// `PartnerLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PartnerLink {
    /// name property.
    pub name: Option<String>,
    /// owningAccount property.
    pub owning_account: Option<ProductAccount>,
    /// partnerAccount property.
    pub partner_account: Option<ProductAccount>,
    /// partnerLinkId property.
    pub partner_link_id: Option<String>,
}

/// `ListUserListGlobalLicensesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUserListGlobalLicensesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// userListGlobalLicenses property.
    pub user_list_global_licenses: Option<Vec<UserListGlobalLicense>>,
}

/// `Baseline` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Baseline {
    /// baselineLocation property.
    pub baseline_location: Option<Location>,
    /// locationAutoDetectionEnabled property.
    pub location_auto_detection_enabled: Option<bool>,
}

/// `IngestEventsStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestEventsStatus {
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `IngestMobileDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestMobileDataStatus {
    /// mobileIdCount property.
    pub mobile_id_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `MarketingDataInsight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MarketingDataInsight {
    /// attributes property.
    pub attributes: Option<Vec<MarketingDataInsightsAttribute>>,
    /// dimension property.
    pub dimension: Option<String>,
}

/// `Event` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Event {
    /// adIdentifiers property.
    pub ad_identifiers: Option<AdIdentifiers>,
    /// additionalEventParameters property.
    pub additional_event_parameters: Option<Vec<EventParameter>>,
    /// cartData property.
    pub cart_data: Option<CartData>,
    /// clientId property.
    pub client_id: Option<String>,
    /// consent property.
    pub consent: Option<Consent>,
    /// conversionValue property.
    pub conversion_value: Option<f64>,
    /// currency property.
    pub currency: Option<String>,
    /// customVariables property.
    pub custom_variables: Option<Vec<CustomVariable>>,
    /// destinationReferences property.
    pub destination_references: Option<Vec<String>>,
    /// eventDeviceInfo property.
    pub event_device_info: Option<DeviceInfo>,
    /// eventName property.
    pub event_name: Option<String>,
    /// eventSource property.
    pub event_source: Option<String>,
    /// eventTimestamp property.
    pub event_timestamp: Option<String>,
    /// experimentalFields property.
    pub experimental_fields: Option<Vec<ExperimentalField>>,
    /// lastUpdatedTimestamp property.
    pub last_updated_timestamp: Option<String>,
    /// transactionId property.
    pub transaction_id: Option<String>,
    /// userData property.
    pub user_data: Option<UserData>,
    /// userId property.
    pub user_id: Option<String>,
    /// userProperties property.
    pub user_properties: Option<UserProperties>,
}

/// `ListUserListDirectLicensesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUserListDirectLicensesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// userListDirectLicenses property.
    pub user_list_direct_licenses: Option<Vec<UserListDirectLicense>>,
}

/// `UserData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserData {
    /// userIdentifiers property.
    pub user_identifiers: Option<Vec<UserIdentifier>>,
}

/// `SizeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SizeInfo {
    /// displayNetworkMembersCount property.
    pub display_network_members_count: Option<String>,
    /// searchNetworkMembersCount property.
    pub search_network_members_count: Option<String>,
}

/// `IngestedUserListInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestedUserListInfo {
    /// contactIdInfo property.
    pub contact_id_info: Option<ContactIdInfo>,
    /// mobileIdInfo property.
    pub mobile_id_info: Option<MobileIdInfo>,
    /// pairIdInfo property.
    pub pair_id_info: Option<PairIdInfo>,
    /// partnerAudienceInfo property.
    pub partner_audience_info: Option<PartnerAudienceInfo>,
    /// pseudonymousIdInfo property.
    pub pseudonymous_id_info: Option<PseudonymousIdInfo>,
    /// uploadKeyTypes property.
    pub upload_key_types: Option<Vec<String>>,
    /// userIdInfo property.
    pub user_id_info: Option<UserIdInfo>,
}

/// `MobileData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileData {
    /// mobileIds property.
    pub mobile_ids: Option<Vec<String>>,
}

/// `CustomVariable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomVariable {
    /// destinationReferences property.
    pub destination_references: Option<Vec<String>>,
    /// value property.
    pub value: Option<String>,
    /// variable property.
    pub variable: Option<String>,
}

/// `RetrieveInsightsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetrieveInsightsRequest {
    /// baseline property.
    pub baseline: Option<Baseline>,
    /// userListId property.
    pub user_list_id: Option<String>,
}

/// `UserListLicenseMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserListLicenseMetrics {
    /// clickCount property.
    pub click_count: Option<String>,
    /// endDate property.
    pub end_date: Option<String>,
    /// impressionCount property.
    pub impression_count: Option<String>,
    /// revenueUsdMicros property.
    pub revenue_usd_micros: Option<String>,
    /// startDate property.
    pub start_date: Option<String>,
}

/// `UserListLicensePricing` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserListLicensePricing {
    /// buyerApprovalState property.
    pub buyer_approval_state: Option<String>,
    /// costMicros property.
    pub cost_micros: Option<String>,
    /// costType property.
    pub cost_type: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// maxCostMicros property.
    pub max_cost_micros: Option<String>,
    /// pricingActive property.
    pub pricing_active: Option<bool>,
    /// pricingId property.
    pub pricing_id: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ItemParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemParameter {
    /// parameterName property.
    pub parameter_name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Item` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Item {
    /// additionalItemParameters property.
    pub additional_item_parameters: Option<Vec<ItemParameter>>,
    /// itemId property.
    pub item_id: Option<String>,
    /// merchantProductId property.
    pub merchant_product_id: Option<String>,
    /// quantity property.
    pub quantity: Option<String>,
    /// unitPrice property.
    pub unit_price: Option<f64>,
}

/// `CartData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CartData {
    /// items property.
    pub items: Option<Vec<Item>>,
    /// merchantFeedLabel property.
    pub merchant_feed_label: Option<String>,
    /// merchantFeedLanguageCode property.
    pub merchant_feed_language_code: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// transactionDiscount property.
    pub transaction_discount: Option<f64>,
}

/// `IngestEventsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestEventsRequest {
    /// consent property.
    pub consent: Option<Consent>,
    /// destinations property.
    pub destinations: Option<Vec<Destination>>,
    /// encoding property.
    pub encoding: Option<String>,
    /// encryptionInfo property.
    pub encryption_info: Option<EncryptionInfo>,
    /// events property.
    pub events: Option<Vec<Event>>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `UserIdData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserIdData {
    /// userId property.
    pub user_id: Option<String>,
}

/// `IngestAudienceMembersStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestAudienceMembersStatus {
    /// mobileDataIngestionStatus property.
    pub mobile_data_ingestion_status: Option<IngestMobileDataStatus>,
    /// pairDataIngestionStatus property.
    pub pair_data_ingestion_status: Option<IngestPairDataStatus>,
    /// ppidDataIngestionStatus property.
    pub ppid_data_ingestion_status: Option<IngestPpidDataStatus>,
    /// userDataIngestionStatus property.
    pub user_data_ingestion_status: Option<IngestUserDataStatus>,
    /// userIdDataIngestionStatus property.
    pub user_id_data_ingestion_status: Option<IngestUserIdDataStatus>,
}

/// `UserProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserProperties {
    /// additionalUserProperties property.
    pub additional_user_properties: Option<Vec<UserProperty>>,
    /// customerType property.
    pub customer_type: Option<String>,
    /// customerValueBucket property.
    pub customer_value_bucket: Option<String>,
}

/// `ContactIdInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContactIdInfo {
    /// dataSourceType property.
    pub data_source_type: Option<String>,
    /// matchRatePercentage property.
    pub match_rate_percentage: Option<i64>,
}

/// `IngestPairDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestPairDataStatus {
    /// pairIdCount property.
    pub pair_id_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `IngestEventsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestEventsResponse {
    /// requestId property.
    pub request_id: Option<String>,
}

/// `PairData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PairData {
    /// pairIds property.
    pub pair_ids: Option<Vec<String>>,
}

/// `RemoveAudienceMembersStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveAudienceMembersStatus {
    /// mobileDataRemovalStatus property.
    pub mobile_data_removal_status: Option<RemoveMobileDataStatus>,
    /// pairDataRemovalStatus property.
    pub pair_data_removal_status: Option<RemovePairDataStatus>,
    /// ppidDataRemovalStatus property.
    pub ppid_data_removal_status: Option<RemovePpidDataStatus>,
    /// userDataRemovalStatus property.
    pub user_data_removal_status: Option<RemoveUserDataStatus>,
    /// userIdDataRemovalStatus property.
    pub user_id_data_removal_status: Option<RemoveUserIdDataStatus>,
}

/// `UserList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserList {
    /// accessReason property.
    pub access_reason: Option<String>,
    /// accountAccessStatus property.
    pub account_access_status: Option<String>,
    /// closingReason property.
    pub closing_reason: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ingestedUserListInfo property.
    pub ingested_user_list_info: Option<IngestedUserListInfo>,
    /// integrationCode property.
    pub integration_code: Option<String>,
    /// membershipDuration property.
    pub membership_duration: Option<String>,
    /// membershipStatus property.
    pub membership_status: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// readOnly property.
    pub read_only: Option<bool>,
    /// sizeInfo property.
    pub size_info: Option<SizeInfo>,
    /// targetNetworkInfo property.
    pub target_network_info: Option<TargetNetworkInfo>,
}

/// `TermsOfService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TermsOfService {
    /// customerMatchTermsOfServiceStatus property.
    pub customer_match_terms_of_service_status: Option<String>,
}

/// `ErrorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorInfo {
    /// errorCounts property.
    pub error_counts: Option<Vec<ErrorCount>>,
}

/// `ErrorCount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorCount {
    /// reason property.
    pub reason: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `RemoveMobileDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveMobileDataStatus {
    /// mobileIdCount property.
    pub mobile_id_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `Consent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Consent {
    /// adPersonalization property.
    pub ad_personalization: Option<String>,
    /// adUserData property.
    pub ad_user_data: Option<String>,
}

/// `MarketingDataInsightsAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MarketingDataInsightsAttribute {
    /// ageRange property.
    pub age_range: Option<String>,
    /// gender property.
    pub gender: Option<String>,
    /// lift property.
    pub lift: Option<f64>,
    /// userInterestId property.
    pub user_interest_id: Option<String>,
}

/// `RemoveAudienceMembersRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveAudienceMembersRequest {
    /// audienceMembers property.
    pub audience_members: Option<Vec<AudienceMember>>,
    /// destinations property.
    pub destinations: Option<Vec<Destination>>,
    /// encoding property.
    pub encoding: Option<String>,
    /// encryptionInfo property.
    pub encryption_info: Option<EncryptionInfo>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `TargetNetworkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetNetworkInfo {
    /// eligibleForDisplay property.
    pub eligible_for_display: Option<bool>,
    /// eligibleForSearch property.
    pub eligible_for_search: Option<bool>,
}

/// `AddressInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddressInfo {
    /// familyName property.
    pub family_name: Option<String>,
    /// givenName property.
    pub given_name: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `EncryptionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EncryptionInfo {
    /// awsWrappedKeyInfo property.
    pub aws_wrapped_key_info: Option<AwsWrappedKeyInfo>,
    /// gcpWrappedKeyInfo property.
    pub gcp_wrapped_key_info: Option<GcpWrappedKeyInfo>,
}

/// `UserProperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserProperty {
    /// propertyName property.
    pub property_name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
}

/// `IngestUserIdDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestUserIdDataStatus {
    /// recordCount property.
    pub record_count: Option<String>,
    /// userIdCount property.
    pub user_id_count: Option<String>,
}

/// `GcpWrappedKeyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcpWrappedKeyInfo {
    /// encryptedDek property.
    pub encrypted_dek: Option<String>,
    /// kekUri property.
    pub kek_uri: Option<String>,
    /// keyType property.
    pub key_type: Option<String>,
    /// wipProvider property.
    pub wip_provider: Option<String>,
}

/// `RemoveUserIdDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveUserIdDataStatus {
    /// recordCount property.
    pub record_count: Option<String>,
    /// userIdCount property.
    pub user_id_count: Option<String>,
}

/// `DeviceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceInfo {
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// userAgent property.
    pub user_agent: Option<String>,
}

/// `RemoveUserDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveUserDataStatus {
    /// recordCount property.
    pub record_count: Option<String>,
    /// userIdentifierCount property.
    pub user_identifier_count: Option<String>,
}

/// `RequestStatusPerDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestStatusPerDestination {
    /// audienceMembersIngestionStatus property.
    pub audience_members_ingestion_status: Option<IngestAudienceMembersStatus>,
    /// audienceMembersRemovalStatus property.
    pub audience_members_removal_status: Option<RemoveAudienceMembersStatus>,
    /// destination property.
    pub destination: Option<Destination>,
    /// errorInfo property.
    pub error_info: Option<ErrorInfo>,
    /// eventsIngestionStatus property.
    pub events_ingestion_status: Option<IngestEventsStatus>,
    /// requestStatus property.
    pub request_status: Option<String>,
    /// warningInfo property.
    pub warning_info: Option<WarningInfo>,
}

/// `RemoveAudienceMembersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveAudienceMembersResponse {
    /// requestId property.
    pub request_id: Option<String>,
}

/// `SearchPartnerLinksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchPartnerLinksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// partnerLinks property.
    pub partner_links: Option<Vec<PartnerLink>>,
}

/// `UserIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserIdentifier {
    /// address property.
    pub address: Option<AddressInfo>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
}

/// `RemovePairDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemovePairDataStatus {
    /// pairIdCount property.
    pub pair_id_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `UserListDirectLicense` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserListDirectLicense {
    /// clientAccountDisplayName property.
    pub client_account_display_name: Option<String>,
    /// clientAccountId property.
    pub client_account_id: Option<String>,
    /// clientAccountType property.
    pub client_account_type: Option<String>,
    /// historicalPricings property.
    pub historical_pricings: Option<Vec<UserListLicensePricing>>,
    /// metrics property.
    pub metrics: Option<UserListLicenseMetrics>,
    /// name property.
    pub name: Option<String>,
    /// pricing property.
    pub pricing: Option<UserListLicensePricing>,
    /// status property.
    pub status: Option<String>,
    /// userListDisplayName property.
    pub user_list_display_name: Option<String>,
    /// userListId property.
    pub user_list_id: Option<String>,
}

/// `UserListGlobalLicenseCustomerInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserListGlobalLicenseCustomerInfo {
    /// clientAccountDisplayName property.
    pub client_account_display_name: Option<String>,
    /// clientAccountId property.
    pub client_account_id: Option<String>,
    /// clientAccountType property.
    pub client_account_type: Option<String>,
    /// historicalPricings property.
    pub historical_pricings: Option<Vec<UserListLicensePricing>>,
    /// licenseType property.
    pub license_type: Option<String>,
    /// metrics property.
    pub metrics: Option<UserListLicenseMetrics>,
    /// name property.
    pub name: Option<String>,
    /// pricing property.
    pub pricing: Option<UserListLicensePricing>,
    /// status property.
    pub status: Option<String>,
    /// userListDisplayName property.
    pub user_list_display_name: Option<String>,
    /// userListId property.
    pub user_list_id: Option<String>,
}

/// `PseudonymousIdInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PseudonymousIdInfo {
    /// billableRecordCount property.
    pub billable_record_count: Option<String>,
    /// syncStatus property.
    pub sync_status: Option<String>,
}

/// `WarningInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WarningInfo {
    /// warningCounts property.
    pub warning_counts: Option<Vec<WarningCount>>,
}

/// `EventParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventParameter {
    /// parameterName property.
    pub parameter_name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ExperimentalField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExperimentalField {
    /// field property.
    pub field: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `RetrieveInsightsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetrieveInsightsResponse {
    /// marketingDataInsights property.
    pub marketing_data_insights: Option<Vec<MarketingDataInsight>>,
}

/// `UserListGlobalLicense` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserListGlobalLicense {
    /// historicalPricings property.
    pub historical_pricings: Option<Vec<UserListLicensePricing>>,
    /// licenseType property.
    pub license_type: Option<String>,
    /// metrics property.
    pub metrics: Option<UserListLicenseMetrics>,
    /// name property.
    pub name: Option<String>,
    /// pricing property.
    pub pricing: Option<UserListLicensePricing>,
    /// status property.
    pub status: Option<String>,
    /// userListDisplayName property.
    pub user_list_display_name: Option<String>,
    /// userListId property.
    pub user_list_id: Option<String>,
}

/// `ListUserListGlobalLicenseCustomerInfosResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUserListGlobalLicenseCustomerInfosResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// userListGlobalLicenseCustomerInfos property.
    pub user_list_global_license_customer_infos: Option<Vec<UserListGlobalLicenseCustomerInfo>>,
}

/// `ProductAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductAccount {
    /// accountId property.
    pub account_id: Option<String>,
    /// accountType property.
    pub account_type: Option<String>,
    /// product property.
    pub product: Option<String>,
}

/// `AwsWrappedKeyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AwsWrappedKeyInfo {
    /// encryptedDek property.
    pub encrypted_dek: Option<String>,
    /// kekUri property.
    pub kek_uri: Option<String>,
    /// keyType property.
    pub key_type: Option<String>,
    /// roleArn property.
    pub role_arn: Option<String>,
}

/// `IngestPpidDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestPpidDataStatus {
    /// ppidCount property.
    pub ppid_count: Option<String>,
    /// recordCount property.
    pub record_count: Option<String>,
}

/// `MobileIdInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileIdInfo {
    /// appId property.
    pub app_id: Option<String>,
    /// dataSourceType property.
    pub data_source_type: Option<String>,
    /// keySpace property.
    pub key_space: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datamanager.accountTypes.accounts.insights.retrieve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsInsightsRetrieveArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: RetrieveInsightsRequest,
}

/// Arguments for [`datamanager.accountTypes.accounts.partnerLinks.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsPartnerLinksCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: PartnerLink,
}

/// Arguments for [`datamanager.accountTypes.accounts.partnerLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsPartnerLinksDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamanager.accountTypes.accounts.partnerLinks.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsPartnerLinksSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListDirectLicenses.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListDirectLicensesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: UserListDirectLicense,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListDirectLicenses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListDirectLicensesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListDirectLicenses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListDirectLicensesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListDirectLicenses.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListDirectLicensesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: UserListDirectLicense,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListGlobalLicenses.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListGlobalLicensesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: UserListGlobalLicense,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListGlobalLicenses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListGlobalLicensesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListGlobalLicenses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListGlobalLicensesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListGlobalLicenses.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListGlobalLicensesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: UserListGlobalLicense,
}

/// Arguments for [`datamanager.accountTypes.accounts.userListGlobalLicenses.userListGlobalLicenseCustomerInfos.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListGlobalLicensesUserListGlobalLicenseCustomerInfosListArgs
{
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: UserList,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: UserList,
}

/// Arguments for [`datamanager.audienceMembers.ingest_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAudienceMembersIngestArgs {
    /// Request body.
    pub body: IngestAudienceMembersRequest,
}

/// Arguments for [`datamanager.audienceMembers.remove_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerAudienceMembersRemoveArgs {
    /// Request body.
    pub body: RemoveAudienceMembersRequest,
}

/// Arguments for [`datamanager.events.ingest_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerEventsIngestArgs {
    /// Request body.
    pub body: IngestEventsRequest,
}

/// Arguments for [`datamanager.requestStatus.retrieve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatamanagerRequestStatusRetrieveArgs {
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+parent}/insights:retrieve
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/insights:retrieve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_insights_retrieve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_insights_retrieve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsInsightsRetrieveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetrieveInsightsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/insights:retrieve",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RetrieveInsightsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/partnerLinks
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/partnerLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_partner_links_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_partner_links_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsPartnerLinksCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PartnerLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/partnerLinks",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PartnerLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_partner_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_partner_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsPartnerLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/partnerLinks:search
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/partnerLinks:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_partner_links_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_partner_links_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsPartnerLinksSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchPartnerLinksResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/partnerLinks:search",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchPartnerLinksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/userListDirectLicenses
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/userListDirectLicenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_direct_licenses_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_direct_licenses_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListDirectLicensesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListDirectLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userListDirectLicenses",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListDirectLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_direct_licenses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_direct_licenses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListDirectLicensesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListDirectLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListDirectLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/userListDirectLicenses
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/userListDirectLicenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_direct_licenses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_direct_licenses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListDirectLicensesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListUserListDirectLicensesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userListDirectLicenses",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUserListDirectLicensesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_direct_licenses_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_direct_licenses_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListDirectLicensesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListDirectLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListDirectLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/userListGlobalLicenses
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/userListGlobalLicenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_global_licenses_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_global_licenses_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListGlobalLicensesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListGlobalLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userListGlobalLicenses",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListGlobalLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_global_licenses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_global_licenses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListGlobalLicensesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListGlobalLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListGlobalLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/userListGlobalLicenses
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/userListGlobalLicenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_global_licenses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_global_licenses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListGlobalLicensesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListUserListGlobalLicensesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userListGlobalLicenses",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUserListGlobalLicensesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_global_licenses_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_global_licenses_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListGlobalLicensesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserListGlobalLicense>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserListGlobalLicense =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/userListGlobalLicenseCustomerInfos
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/userListGlobalLicenseCustomerInfos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_list_global_licenses_user_list_global_license_customer_infos_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_list_global_licenses_user_list_global_license_customer_infos_list_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListGlobalLicensesUserListGlobalLicenseCustomerInfosListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListUserListGlobalLicenseCustomerInfosResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userListGlobalLicenseCustomerInfos",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUserListGlobalLicenseCustomerInfosResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/userLists
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/userLists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userLists",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/userLists
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/userLists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUserListsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/{}/userLists",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUserListsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAccountTypesAccountsUserListsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/audienceMembers:ingest
// -----------------------------------------------------------------------------

/// POST v1/audienceMembers:ingest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_audience_members_ingest_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_audience_members_ingest_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAudienceMembersIngestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IngestAudienceMembersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/audienceMembers:ingest",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IngestAudienceMembersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/audienceMembers:remove
// -----------------------------------------------------------------------------

/// POST v1/audienceMembers:remove.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_audience_members_remove_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_audience_members_remove_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerAudienceMembersRemoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveAudienceMembersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/audienceMembers:remove",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemoveAudienceMembersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/events:ingest
// -----------------------------------------------------------------------------

/// POST v1/events:ingest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_events_ingest_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_events_ingest_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerEventsIngestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IngestEventsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/events:ingest",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IngestEventsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/requestStatus:retrieve
// -----------------------------------------------------------------------------

/// GET v1/requestStatus:retrieve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_request_status_retrieve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_request_status_retrieve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatamanagerRequestStatusRetrieveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetrieveRequestStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datamanager.googleapis.com/v1/requestStatus:retrieve",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RetrieveRequestStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
