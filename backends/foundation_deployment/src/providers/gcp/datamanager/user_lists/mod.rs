//! Auto-generated API module for gcp/datamanager userLists.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datamanager_user_lists `

#![cfg(feature = "gcp_datamanager_user_lists")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SizeInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SizeInfo {
    /// displayNetworkMembersCount property.
    pub display_network_members_count: Option<String>,
    /// searchNetworkMembersCount property.
    pub search_network_members_count: Option<String>,
}

/// `UserList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserList {
    /// accessReason property.
    pub access_reason: Option<String>,
    /// accountAccessStatus property.
    pub account_access_status: Option<String>,
    /// closingReason property.
    pub closing_reason: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ingestedUserListInfo property.
    pub ingested_user_list_info: Option<IngestedUserListInfo>,
    /// integrationCode property.
    pub integration_code: Option<String>,
    /// membershipDuration property.
    pub membership_duration: Option<String>,
    /// membershipStatus property.
    pub membership_status: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// readOnly property.
    pub read_only: Option<bool>,
    /// sizeInfo property.
    pub size_info: Option<SizeInfo>,
    /// targetNetworkInfo property.
    pub target_network_info: Option<TargetNetworkInfo>,
}

/// `MobileIdInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MobileIdInfo {
    /// appId property.
    pub app_id: Option<String>,
    /// dataSourceType property.
    pub data_source_type: Option<String>,
    /// keySpace property.
    pub key_space: Option<String>,
}

/// `PseudonymousIdInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PseudonymousIdInfo {
    /// billableRecordCount property.
    pub billable_record_count: Option<String>,
    /// syncStatus property.
    pub sync_status: Option<String>,
}

/// `ListUserListsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListUserListsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// userLists property.
    pub user_lists: Option<Vec<UserList>>,
}

/// `TargetNetworkInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TargetNetworkInfo {
    /// eligibleForDisplay property.
    pub eligible_for_display: Option<bool>,
    /// eligibleForSearch property.
    pub eligible_for_search: Option<bool>,
}

/// `IngestedUserListInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngestedUserListInfo {
    /// contactIdInfo property.
    pub contact_id_info: Option<ContactIdInfo>,
    /// mobileIdInfo property.
    pub mobile_id_info: Option<MobileIdInfo>,
    /// pairIdInfo property.
    pub pair_id_info: Option<PairIdInfo>,
    /// partnerAudienceInfo property.
    pub partner_audience_info: Option<PartnerAudienceInfo>,
    /// pseudonymousIdInfo property.
    pub pseudonymous_id_info: Option<PseudonymousIdInfo>,
    /// uploadKeyTypes property.
    pub upload_key_types: Option<Vec<String>>,
    /// userIdInfo property.
    pub user_id_info: Option<UserIdInfo>,
}

/// `PairIdInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PairIdInfo {
    /// advertiserIdentifierCount property.
    pub advertiser_identifier_count: Option<String>,
    /// cleanRoomIdentifier property.
    pub clean_room_identifier: Option<String>,
    /// matchRatePercentage property.
    pub match_rate_percentage: Option<i64>,
    /// publisherId property.
    pub publisher_id: Option<String>,
    /// publisherName property.
    pub publisher_name: Option<String>,
}

/// `PartnerAudienceInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartnerAudienceInfo {
    /// commercePartner property.
    pub commerce_partner: Option<String>,
    /// partnerAudienceSource property.
    pub partner_audience_source: Option<String>,
}

/// `UserIdInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserIdInfo {
    /// dataSourceType property.
    pub data_source_type: Option<String>,
}

/// `ContactIdInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContactIdInfo {
    /// dataSourceType property.
    pub data_source_type: Option<String>,
    /// matchRatePercentage property.
    pub match_rate_percentage: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datamanager.accountTypes.accounts.userLists.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamanager.accountTypes.accounts.userLists.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamanagerAccountTypesAccountsUserListsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists
// -----------------------------------------------------------------------------

/// POST v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/accountTypes/{{accountTypesId}}/accounts/{{accountsId}}/userLists",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}
// -----------------------------------------------------------------------------

/// DELETE v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/accountTypes/{{accountTypesId}}/accounts/{{accountsId}}/userLists/{{userListsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}
// -----------------------------------------------------------------------------

/// GET v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/accountTypes/{{accountTypesId}}/accounts/{{accountsId}}/userLists/{{userListsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists
// -----------------------------------------------------------------------------

/// GET v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUserListsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/accountTypes/{{accountTypesId}}/accounts/{{accountsId}}/userLists",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUserListsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}
// -----------------------------------------------------------------------------

/// PATCH v1/accountTypes/{accountTypesId}/accounts/{accountsId}/userLists/{userListsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamanager_account_types_accounts_user_lists_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamanager_account_types_accounts_user_lists_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamanager.googleapis.com/v1/accountTypes/{{accountTypesId}}/accounts/{{accountsId}}/userLists/{{userListsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
